---
draft: false
doc_id: d-20260905-165519-38a6df
title: UAC 3C - Detail Subjects And Analysis Assignment
added_date: "2026-09-05 16:55:19"
last_updated: "2026-09-05 17:14:53"
summary: Add exact Detail subjects and enable subject assignment directly in Analysis Works through the existing subject editor and revision-checked service.
ui_status: in-progress
parent_id: d-20260904-204816-0f31be
---
# UAC 3C - Detail Subjects And Analysis Assignment

## Outcome

Implement the next [UAC 3](/docs/?scope=studio&doc=d-20260904-204816-0f31be) outcomes together: `detail_uid` subjects in Projects and Analysis Works, and direct Assign subject in `analysis.works`. Use the existing metadata, assignment, projection and Copy paths. The shared Working editor also serves Processing and must remain consistent.

Subject choices are None, Series, Work and Detail, plus Folder only in `dotlineform`. A Detail UID such as `00008-001` encodes Work `00008` and Detail `001`; syntax validation and parsing require no ownership or existence lookup. One subject is stored at a time. Existing unsupported or conflicting source declarations remain visible for explicit correction; this delivery does not rewrite user documents in bulk.

The user chose to retain current Work/Series search until the coordinated generated-data switch in Delivery Sequence. Detail uses exact UID entry. Do not introduce Detail token syntax, a canonical Detail lookup or a new public media route in this subject delivery. Detail subjects remain exact metadata even when no image or document-link action exists for them yet.

## Delivery

- [x] Read the existing subject normalizer, customisation capabilities, editor, generated associations and Copy boundary.
- [x] Add Detail normalization, metadata assignment and private association projection; preserve exact IDs and authored body through Save and Copy.
- [x] Enable the existing assignment action directly in Analysis Works, independent of Copy lineage, and show Detail subjects clearly in the report and Info.
- [x] Verify Folder availability, exact revision checks, one-subject exclusivity, Detail round trips and public isolation.
- [x] Review the complete changed code and tracked runtime projection.
- [ ] User reviews assignment, clearing and Detail display in the live collections.

| changed contract | lowest adequate evidence | permanent test change |
| --- | --- | --- |
| exact Detail identity and one subject | pure Python/JavaScript normalization | extend subject cases; no Catalogue lookup |
| assignment and generated subject associations | service and builder tests | extend current revision-checked assignment coverage for Analysis and Detail |
| Copy to Analysis | existing transfer tests with Detail source | prove UID/body preservation and independent destination subject |
| Manage contribution and public isolation | module contract, public boundary and config/build checks | extend current contribution checks; reuse public isolation checks |
| modal choices and report presentation | user review | no permanent browser choreography |

## Boundaries

No Copy, Publish, Deploy, commit or push is performed against real documents. Catalogue canonical/editor changes and the generated-only consumer switch remain their own coordinated delivery. Existing Projects and Processing lineage owners remain separate. Build only the affected collections for the changed subject/configuration contract; Search remains explicit.

## Implementation And Review

Implemented and ready for manual review. The common four-field assignment replaces the previous subject atomically using the exact document target and source revision. None removes the fields; Detail stores only `detail_uid`. Analysis Works now uses the existing Manage subject contribution and assignment action. The shared Projects/Processing editor offers Detail as well. Analysis offers no Folder option and rejects Folder assignment at the service boundary.

Detail is displayed as an icon and UID without a destination link. Insert subject link reports that Detail links are not available yet. Work/Series search remains on its current provider until the coordinated generated-only switch; assigning a Detail requires no Catalogue record lookup.

Focused checks passed: 96 Python tests across subject normalization, customisations, assignment, sub-scope generation, Copy and public runtime boundaries; the shared collection JavaScript contract; changed Python/JavaScript lint; `git diff --check`; site code projection check and site validation. Rebuilt Analysis Works (6 docs), Projects (241) and Processing (1), with zero warnings. Only the Analysis Manage manifest and local browser configuration changed; public configuration and document payloads were unchanged. Site code update required no additional projection changes for this delivery.

Restart the local Python service through the normal launcher and reload Docs Viewer before review. In Analysis Works, open an existing document and use Assign subject to choose Work, Series or Detail, then reopen and clear it with None. Verify Detail `00008-001` displays intact in the row and Info. In Projects, verify existing Work/Series search and Folder assignment still behave normally. No user document was assigned, copied or published as test setup.
