---
draft: false
doc_id: d-20260924-144840-e62c72
title: Document Action Context And Toolbar Placement
added_date: "2026-09-24 14:48:40"
last_updated: "2026-09-24 17:24:26"
summary: Completed shared document and collection action context, top-row Back alignment and action spacing, and collection Sort placement; durable behavior is maintained in Docs Viewer Runtime.
ui_status: done
parent_id: d-20260428-000000-f5ff18
---
# Document Action Context And Toolbar Placement

One bounded delivery under [Planned Features](d-20260428-000000-f5ff18.md). Status: **complete; closeout accepted on 2026-09-24**. Controls consume consistent action context through the existing document/report integration, with the accepted top-row placement, Back spacing and collection Sort placement. Code review and the bounded implementation checks are complete. [Docs Viewer Runtime](d-20260331-000000-c313fd.md#document-action-context-and-toolbar-placement) owns the shipped behavior and evidence limits.

This delivery supersedes [Scope-configured Authoring Controls](d-20260910-151840-671351.md). The current single workspace needs a consistent way for existing controls to receive the displayed document, containing collection and applicable navigation context. The older proposal's per-scope availability configuration and Search/Find copy changes are not part of this delivery. Its completed breadcrumb removal remains in place.

## Requirements

Before this delivery, collection Edit and Delete received the same underlying document identity through different integration paths. Edit consumed collection report state forwarded into Manage and its common control resolver; Delete received the target directly in the report's detail-toolbar contribution. New received the collection target, because no document existed yet. Delete's operation was already management-owned; the report supplied its target, button mount and reconciliation callback.

The primary acceptance criterion is **one consistent resolution of the active document and collection context for the affected controls, independent of where their buttons are rendered**. Consolidate the duplicated handoffs and view-dependent target selection through the existing integration. Merely moving buttons while retaining separate target-resolution paths does not complete this delivery.

### Action Context

| Control | Context it needs |
| --- | --- |
| Edit, Delete, Draft/Ready and Copy Link for a displayed document | The exact current document target, including collection identity when applicable |
| New collection document and other whole-collection actions | The exact containing collection target; New has no existing document target |
| Actions on checked rows | The explicit current selection within its exact collection |
| Back from a collection subdocument | The current collection's return-to-list operation |

The displayed view supplies its validated document/collection state. The existing control integration resolves the appropriate context once for consumers. Management workflows retain operation-specific capability checks, service calls and outcomes; the toolbar presents the resulting controls. The report retains list membership, navigation, filtering, sorting and reconciliation after creation or deletion. These are responsibilities of the existing code, not a new ownership layer or persisted context store.

The visible acceptance criterion remains **a consistent subdocument Back position: in the page-wide top row, horizontally aligned with the document's left edge, in Manage and Published**. Back follows the current document width, including the narrower Moments presentation. The remaining document actions start three button widths to the right of Back's left edge and wrap within their available space. Back's alignment must remain stable as other buttons appear, disappear or wrap, including when Back is the only available document button. The top row is the existing row at the very top of the screen containing site Search and scope-level controls; document and collection actions join that row.

### Placement

| Context | Document controls in the page-wide top row | Beside the collection or report |
| --- | --- | --- |
| Ordinary document | Its existing document actions | No additional document-action toolbar |
| Collection subdocument in Manage | Back aligned with the document's left edge; available document actions such as Edit, Source, Draft/Ready, Copy Link and Delete start three button widths to its right | Subdocument content; no duplicate action row |
| Collection list | Existing host-document actions and available collection actions such as New, Regenerate and Actions | Search, filters, A–Z/date sorting immediately after Search with normal toolbar spacing, and row selection controls |
| Published collection subdocument | Back in the same left-aligned position, plus applicable reader controls | Subdocument content; no duplicate Back button or empty toolbar |
| Other embedded reports | Existing document actions | Their existing report controls remain with the report |

The table specifies placement, not new capabilities. Existing route, stage and action availability remain authoritative. Shared placement does not turn collection actions into ordinary-document actions or add management controls to Published.

### Behaviour To Preserve

- Document actions use the consistently resolved displayed-document context. Collection detail targets include the exact collection and document identity; resolution must not substitute the outer report host, a checked row or an unrelated selection.
- Collection-list actions retain their collection or explicitly selected-document targets. Host-document actions and collection actions can share a row while retaining those distinct meanings.
- Back calls the collection's existing return-to-list operation, preserving filter state, sort state, scroll position and focus. Other existing Back operations, such as returning from content detail, keep their own destinations; the collection Back must not override them.
- List/detail transitions, loading, errors, Source editing, refresh and leaving the report update the action context and visible controls together through the existing lifecycle. Old targets and duplicate buttons must not survive a transition. Document actions require a valid current target, and an operation or Source session retains the exact target with which it started.
- Button labels, keyboard operation, disabled/busy state and service restrictions remain intact. Public Back remains shared reader behaviour without importing management code.
- Empty action groups leave no separators or empty second toolbar. Search, filters and sorting retain their current behaviour beside the collection list.

## Deliverables

- A focused refactor consolidating the active document/collection context consumed by the affected management and report controls, with one consistent target-selection and lifecycle handoff through the existing integration.
- Removal of the superseded duplicate target plumbing and per-control view-resolution branches within this boundary, while retaining operation-specific validation and capability checks.
- Consistent top-row placement for the existing collection list/detail action buttons and subdocument Back in local and public readers, retaining search, filters and sorting beside the collection list.
- Removal of the replaced inline action mounts and their placement-only styling where no longer used.
- The required tracked public runtime projection and a concise update to the durable [Docs Viewer Runtime](d-20260331-000000-c313fd.md) description when implementation ships.

### Refactor Boundary

Build on the current [report control resolver](../../docs-viewer/runtime/js/management/docs-viewer-management-report-controls.js), report-state handoff, [shared collection report](../../docs-viewer/runtime/js/shared/docs-collection-report.js), [management collection composition](../../docs-viewer/runtime/js/management/docs-viewer-management-collection-composition.js), and top document toolbar. The report already publishes its list/detail state and exact target; Edit and Delete do not need independent identity discovery. Use that existing source of truth consistently.

Preserve the meaningful boundaries between view state, management operations and report reconciliation while simplifying the wiring between them. Existing function/module placement is not a requirement to retain duplicate plumbing. Resolve the active context and control targets once at the existing integration boundary; renderers and operation callbacks consume that result. A focused helper within that responsibility is appropriate if it removes repetition. Avoid collection-name checks, route guesses, DOM-position inference or repeated list/detail target and placement conditionals across individual buttons and unrelated controllers.

This delivery adds no ownership layer, toolbar framework, placement configuration, per-collection control policy or availability system. It does not change search coverage, Recents, editorial stars, document data, action semantics or the broader page layout. Shared top-row placement does not require the code's ownership boundaries to become visible groups in the UI.

## Process

1. Open an ordinary document. Its validated document context supplies the existing top-row actions.
2. Open a collection report. The active context identifies both the report host and its collection, so host-document actions and collection actions receive their respective targets. Search, filters and sorting stay beside the list.
3. Open a collection subdocument. The active document context changes to that exact child, and document controls consistently consume it. Back appears in the same page-wide top row, aligned with the document's left edge, with the collection's return operation.
4. Use Back to return to the retained collection list. The action context and top row return to their list state together, and the local search/filter controls are available again.
5. In Published, use the same Back position while only reader capabilities are available.

## Delivery Steps

### TP.0 — Readiness

- [x] Trace Edit, New, Delete and related controls through ordinary-document, collection-list and collection-detail contexts; distinguish common identity origin from duplicated handoffs and legitimate operation-specific checks.
- [x] Identify the existing context/control integration point for consolidation, including Source sessions, content-detail navigation and lifecycle cleanup.
- [x] Confirm the action-context and placement tables, bounded local/public change and verification selection. Raise any action whose target or navigation meaning is unclear before changing it.

Verification budget: targeted read-only source/config inspection; a short review with no generated writes or executable tests. Stop if the proposed result requires a new ownership/configuration mechanism or unrelated action redesign; revise the scope before implementation.

Record: targeted read-only inspection confirmed the documented split: Edit consumes the management report-state projection; detail Draft/Ready, Copy Link and Delete consume the contribution's separately supplied target; New and Regenerate use the containing collection, and checked-row actions use explicit collection selection. The existing shared report/document handoff and control integration can resolve those contexts once for consumers. The public reader currently lacks the management report-state forwarding path, so the shared handoff must also carry collection navigation without importing management code. No new ownership layer, persisted store, configuration mechanism or action redesign is needed.

The bounded implementation covers the shared collection report and document/control integration, management resolver and collection contributions, and existing top document toolbar/CSS, followed by the represented public projection. Report-owned list restoration and post-operation reconciliation remain intact. Source retains its captured target and mounted report; Content Detail retains its own Back destination. Loading, errors, navigation and unmount must clear stale action context and mounts in both readers, including public reports without a management contribution. Existing collection-specific detail controls keep their operation checks while sharing the same supplied context and top-row placement. Search, filters, sorting and row-selection controls stay beside the list. The action-context and placement tables need no scope change.

TP.1 verification selection: focused target/lifecycle and public-import review, explicit changed-file JavaScript lint, `git diff --check`, `bin/site-code-update`, `bin/site-code-update --check` and `bin/site-validate`. This is a short local static/projection pass; exact command cost depends on the changed files. No suitable current target-resolution test was identified: the inspected stage-target contract still imports retired scope/sub-scope APIs and asserts their old identities. It is not selected or changed. Static evidence cannot establish interaction or alignment; TP.2 retains the user-led Manage/Published checks, including narrow and Back-only public cases. No executable tests, generated writes or runtime edits occurred during readiness; only this delivery record was updated.

Gate: readiness complete; the user approved the bounded TP.1 implementation on 2026-09-24.

### TP.1 — Implementation

- [x] Consolidate the existing report-state/control integration so affected controls receive the appropriate document, collection, selection or navigation context from one resolution path.
- [x] Remove superseded parallel target plumbing within this boundary; preserve operation-specific validation, availability, busy state, Source-session identity and report reconciliation.
- [x] Present the controls in the agreed page-wide top row, with Back aligned with the document's left edge; retain local search/filter/sort controls and remove superseded inline action rendering.
- [x] Run the agreed focused checks and project changed shared/public runtime files through the existing site-code inventory.

Proposed verification budget: focused inspection of resolved targets and lifecycle transitions, explicit changed-file JavaScript lint, `git diff --check`, and required `bin/site-code-update`, `bin/site-code-update --check` and `bin/site-validate` follow-through. Select any justified existing target-resolution check at readiness after inspecting its documented coverage and cost; name evidence limits if none is suitable. These are local checks; projection writes the represented public code files. Expect a short focused pass, with exact cost confirmed after changed owners are known. No Docs/Search rebuild, publication or deployment is needed for this refactor. Additional test work or browser automation requires its own agreed specification; no broad suite is an automatic gate.

Record: the shared collection reader now supplies one resolved document/collection context through the document-controller handoff to both readers and management consumers. Edit, Open in VS Code and collection detail controls consume that resolved target; Source retains its captured session target. The parallel management-only forwarding and target reconstruction were removed, along with the direct-dispatch fallbacks for New and selected-document package actions. Collection selection and operation-specific validation remain with their existing owners. Report-owned reconciliation, list restoration and detail refresh retain their existing responsibilities.

The existing document toolbar sits in the page-wide top row alongside site Search and scope-level controls in Manage and Published, following the user's placement clarification. Collection Back and the remaining document actions now share a grid aligned with the document's left edge, including Moments' narrower presentation. Back occupies the start of a three-button-width column; the remaining actions wrap together in the next column. This replaces independent absolute positioning, which allowed Back to overlap the other icons in Moments. The narrow public header wraps document controls onto a full-width line within the header, retaining the same alignment and action spacing. Collection search, filters, sorting and selection controls remain beside the list. The report's inline detail header and its styling were removed. Source and Content Detail hide collection controls while retaining their mounted nodes; document/report transitions replace or clear the active mounts. Public report unmount cleanup no longer depends on a management contribution, and nested report content cannot replace the outer document's action context.

The user accepted the Back spacing correction and requested that collection sorting follow the search box. The shared Manage collection toolbar no longer uses an automatic leading margin to push sorting to the right edge; Moments, Concepts and Catalogue use the existing report toolbar gap of `0.55rem` after Search. This follow-up changes only local management styling and has no public projection.

Evidence: changed-file JavaScript lint passed for the affected canonical modules; the implementation projects changes to nine represented public files. `bin/site-code-update --check`, `bin/site-validate` and `git diff --check` passed. The user accepted Back alignment, the Moments spacing correction and the subsequent Sort placement. No tests were created, changed or run, and no automated browser or live-write verification was performed. Keyboard behavior, Back-only public controls and narrow public layout have no separately recorded manual run. No Docs/Search rebuild, Publish, deployment, commit or push was performed.

Gate: implementation and static evidence accepted; TP.2 completed below.

### TP.2 — Code Review And Manual Acceptance

- [x] Review the main outcome: affected controls consistently consume resolved active context, superseded target plumbing is removed, and view transitions cannot leave Edit and Delete addressing different documents. Confirm that New still targets its collection and selection actions retain their explicit selection.
- [x] Review operation ownership, captured workflow/Source targets, report reconciliation, public import boundaries, top-row placement and cleanup across list/detail/Source/content-detail transitions.
- [x] Resolve findings and rerun only the checks affected by those changes.
- [x] Record user acceptance of top-row placement, Back alignment and spacing, and collection Sort placement; distinguish accepted presentation from interaction cases without separately recorded execution.

Verification budget: scoped source/diff and selected target-context evidence, plus a short user-led visual and interaction pass. Public layout review includes a narrow viewport; local Manage review uses its desktop boundary. Visual alignment alone does not establish correct action targets, and static checks do not prove presentation. No destructive document action is needed to review the layout; any live-write verification must be explicitly agreed within the implementation evidence budget.

Record: the final source/diff review confirmed the shared context and consumer handoff, exact collection/selection targets, captured Source target, report-owned reconciliation, generation guards, stale-control cleanup and public-safe imports. No further code changes were identified. Earlier visual findings were resolved through the shared Back/action grid and removal of the Sort group's automatic leading margin. The user accepted those results and explicitly authorized continuing and closing on 2026-09-24. Existing lint/projection/site evidence was reused; no behavior or browser tests were added or run. The unexercised cases listed in TP.1 remain evidence limits, not claimed passes. Gate: review and bounded manual acceptance complete.

### TP.3 — Closeout

- [x] Update the durable runtime owner with the consolidated action-context flow, retained operation responsibilities and resulting toolbar placement; retain only the actual evidence and remaining limits here.
- [x] Confirm the outcome and present the retain-or-retire recommendation for this delivery and its superseded predecessor.

Verification budget: documentation/source review and whitespace checks only. Record: durable behavior and evidence limits transferred to Docs Viewer Runtime; completed delivery status and the predecessor's durable destination recorded. No runtime changes or repeated implementation checks were needed at closeout. Gate: closeout accepted by the user's explicit instruction; document deletion, commit and push remain separate actions.

| Document | Closeout recommendation | Durable destination |
| --- | --- | --- |
| This completed delivery | Retire through manual archiving; retained in the repository for now | [Docs Viewer Runtime](d-20260331-000000-c313fd.md#document-action-context-and-toolbar-placement) |
| [Scope-configured Authoring Controls](d-20260910-151840-671351.md) | Retire the superseded proposal through manual archiving; retained for now | Shipped toolbar and breadcrumb behavior is in Docs Viewer Runtime; the obsolete scope-policy and copy proposals are not carried forward |
| [Planned Features](d-20260428-000000-f5ff18.md) | Retain as the workflow authority | Existing durable parent; no feature-specific parent or temporary verification siblings were created |

## Follow-on

Search, Recents and editorial selection remain in [Collection Search And Site Search](d-20260924-135214-baa9ec.md). New control availability settings, a toolbar redesign or additional document actions would need their own requirements; they are not prerequisites for this delivery.
