---
draft: false
doc_id: d-20260906-202156-6c577c
title: Publishing Workflow - Content Alignment
added_date: "2026-09-06 20:21:56"
last_updated: "2026-09-06 22:16:00"
summary: "Complete: Works and Concepts are aligned, authoring content is consolidated in Working, and the stage review and durable transfer are accepted."
ui_status: done
parent_id: d-20260908-171728-3c539a
---
# Publishing Workflow - Content Alignment

## Outcome

Working owns the authored content from which the future Pre-publish action will regenerate the next stage. Both stages use the real collection IDs `works` and `concepts`. The earlier scope relocation preserved two different content sets; this migration aligns them before promotion is implemented.

Rename Working `projects` to `works` and Pre-publish `tags` to `concepts`, including folders, active configuration, report declarations and collection-specific code. Add Working `concepts` and copy the existing 249 Concept documents there. Copy Library and its children, the Analysis landing page and the Concepts host into Working, preserving document IDs and parent relationships. Working's existing Project Docs host becomes the Works host; its separate Works coverage report receives a distinct title. Current Pre-publish Works content is disposable; the important Series 143 document already has an identical body in Working.

The user performs folder renames and document/media copies. Codex pauses affected writers, records a baseline and supplies the exact map, then waits for completion. After read-only verification, Codex applies the real code/configuration rename, repairs moved report/media/document references and rebuilds Working. Necessary Pre-publish generated reconciliation follows its renamed collection. No temporary Move feature is added.

Copies may remain in Pre-publish until the future action replaces that content. Publication metadata stays as found; reviewing publication eligibility is deferred. Published and the deployed public site remain frozen. Catalogue Tag identity and `tag_id` associations remain independently owned; their document destinations must resolve the new Working Concepts collection explicitly. The old source-to-editorial copy/lineage flow retires rather than becoming the new promotion implementation.

## Current And Next

Complete and accepted with [Stage Structure And UI](/docs/?scope=studio&doc=d-20260906-160541-cf892c) 1.3 on 6 September 2026; its 1.4 review covers the alignment fixes. Closeout transfers the aligned collections and exact stage boundaries to [Source Organisation](/docs/?scope=studio&doc=d-20260423-000000-8fd731). This supporting delivery is ready for manual archive after review. The user will document remaining visibility/disabled-state issues separately; publishing-action design remains separate.

The approved manual handoff is `var/publishing-content-alignment/manual-map.md`: four folder renames, 265 Markdown copies and two media copies. The Library subtree contains 14 documents. Both Series 143 bodies were compared and matched. Publication metadata was preserved for later review.

The user's completed migration matched all 2,420 expected files exactly. Code/configuration activation and seven collection builds are complete, with no build warnings. Working contains 35 ordinary documents, 242 Works, 249 Concepts and one Processing document: 527 total. Pre-publish retains 17 ordinary documents, 249 Concepts and six Works pending its future replacement.

Active collections use `works` and `concepts`. Work customisations are named for their stage (`working_works`, `pre_publish_works`); Concepts uses one shared customisation. The frozen accepted/public snapshot still has its original `tags` collection paths; these are snapshot bindings, not source aliases or fallback paths. The old promotion registrations and report cues are removed. Retained generic transfer primitives use explicit synthetic registrations in tests; no production lineage workflow is registered. Existing source lineage files are inactive and unchanged.

Working now accepts existing `publishable` metadata independently of public output paths. Seven planned document edits update the Works host, Work Document Coverage title, both Concepts report declarations, two media references and one Library link. All other baseline files outside generated document output remain unchanged (1,857 checked). The Working media build has no registered producers; it installed the copied image and HTML worksheet and restored one missing generated PDF, with no removals or missing references. Published, Search and deployed public content remain frozen.

Studio's Tag association product and primary-document targets explicitly identify Analysis/Working/Concepts. All 245 associated Tags resolve there; the one saved primary pointer retains its exact document and Tag IDs. Local services are running for user review.

Review fix: the sub-scope New action dropped `stage` between the mounted report and the creation handler. The handoff now preserves the parent's explicit stage, retaining the mismatch guard. Live verification on Working Concepts opened the New title dialog successfully and cancelled without creating a document; focused JavaScript lint and the stage target contract passed.

Review fix: Uncataloged Files still rejected base-relative folders beginning with `projects/` and required every local target to use that root, although its service now supplies configured media roots. The browser accepts the service's relative folders and encoded targets. Its pure JavaScript contract covers Projects and Processing rows; all 60 live response rows normalized successfully, five producer tests passed, and source lint and whitespace checks passed. Bounded diff review confirmed this affects only the local report consumer; file opening remains validated by the existing service.

Review fix: the Working Series 143 Works document still linked to the old gallery ID. Its existing Working gallery copy is `d-20260906-164429-756174`; the link now names that ID and Working explicitly. Two embedded thumbnail URLs were updated to the configured Working media route. The watcher rebuilt both documents, and the media owner generated exactly those two thumbnail files with no removals or missing references; both local URLs returned HTTP 200. Media preview/build evidence is `var/publishing-content-alignment/series-143-gallery-media-*.json`.

## Delivery

### 1 — Manual Handoff

- [x] Check current collection ownership, exact document identity, host pages, saved Series content and required media.
- [x] Prepare the exact rename/copy map and post-move implementation boundary.
- [x] Pause writers, retain source/file evidence and hand over to the user.
- [x] User confirms the manual renames/copies are complete; verify destination files read-only before resuming.

**Gate:** expected files and identities are present; physical corrections remain user-owned.

### 2 — Rename Owners And Rebuild

- [x] Apply `works` and `concepts` as real collection identities across active configuration, report hosts, services and browser consumers; remove obsolete collection/promotion names without compatibility aliases.
- [x] Retarget document-owned Tag associations to Analysis/Working/Concepts, retaining canonical Tag identity.
- [x] Update moved media references and the one Library document link for their Working destination; preserve other authored content and metadata.
- [x] Rebuild the affected ordinary and child document collections, skipping Search and unrelated media producers. Reconcile the necessary Pre-publish generated collection after the rename.
- [x] Verify the Working collection counts, ordinary/child document reads, report declarations and moved media. Keep any public runtime code projection separate from content deployment. Visual review remains with the user.

**Verification budget:** exact configuration/target and generated-output checks, focused existing owner tests where code changes affect their contracts, and user review of the resulting collections. No browser-suite expansion or broad profile by default.

Evidence is retained under `var/publishing-content-alignment/`: `move-verification.json`, `builds.json`, `media-build.json`, `verification.json`, `focused-tests.txt`, `transfer-callers-tests.txt`, `studio-tests.txt` and `lint.json`. Focused checks passed: 220 Python document/workflow/Tag owner tests, 11 Studio Tag API tests, two JavaScript contracts, actual generated Works/Tag payload normalization, explicit source lint, site-code projection check, site validation and whitespace check. The only tracked public runtime delta belongs to the earlier stage-button mount fix; this alignment did not deploy public content.

### 3 — Review And Closeout

Project State review fix: resolve its document inputs from the configured Analysis/Working/Works collection and carry that exact target through the response and browser consumer. The physical Projects scan is unchanged.

Implemented in the service and local report consumer using response schema v3. Seventeen focused producer/service tests, the pure JavaScript response contract, source lint and whitespace checks passed. A fresh service invocation over actual content returned 206 folders and 232 matched document placements; the browser normalizer accepted every row and all 232 document links retained the exact Working target. Evidence is `var/publishing-content-alignment/project-state-response.json`. Bounded diff review found no fallback or public projection changes. The running Docs Viewer process still has the old Python module loaded and needs a service restart before user review.

| Changed contract | Lowest adequate evidence | Permanent regression check |
| --- | --- | --- |
| Project State reads Working Works and emits stage-specific document targets | Focused producer tests, pure browser response validation and an actual local response | Configured Working path selection and rejection of missing or wrong-stage report targets |
| Docs Media reads the selected stage and returns its media and document targets | Focused inventory/service checks and pure request/response checks against actual media | Isolation of identical media/document IDs between Working and Pre-publish; retained scope-only requests |

Docs Media review fix: the request now carries the stage already selected in scope configuration, the read-only service resolves that exact stage, and schema v2 retains it in the response and document targets. File targets come from configured media locations. The report defaults to its current viewer scope. Fourteen focused report/service tests, seven existing Pre-publish write-rejection checks, the pure JavaScript request/response contract and explicit source lint passed. Actual content produced 352 Working files with 347 document links and 41 Pre-publish files with 36 links; all rows normalized and every file path and document link resolved to its selected stage. Evidence is `var/publishing-content-alignment/docs-media-*-response.json`. Bounded diff review confirmed no authoring permission expansion or public projection changes. Restart the existing local service session to load the Python changes before user review.

- [x] Review the bounded diff for remaining old collection identities, wrong-stage reads/writes, lost metadata/media, active legacy promotion routes and unintended changes to frozen snapshots.
- [x] Record user acceptance and update the durable storage/runtime owner with the aligned collections.
- [x] Resume and complete the original stage UI review with Working as the sole authoring source; keep later Pre-publish action design separate.

**Gate:** content alignment is usable and reviewed; publishing-action design remains separate. **Record:** complete through the accepted Stage Structure And UI review and closeout. Existing migration, build, service/report, media and user-review evidence is retained above; no executable checks were repeated for this documentation closeout.
