---
draft: false
doc_id: d-20260926-204821-7ba18c
title: Selected Documents
added_date: "2026-09-26 20:48:21"
last_updated: "2026-09-26 22:07:14"
parent_id: d-20260428-000000-f5ff18
---
# Selected Documents

## Requirements

Every Working document, including each collection sub-document, can be selected through a star control. A selected star is filled yellow. Selection membership belongs to one canonical `working/source/documents/selected.json`; toggling it does not change document Markdown or `last_updated`. The dotlineform document (`d-20260426-164043-e14f49`) hosts a public Selected Documents report listing linked titles by `last_updated DESC`, with exact collection/host routing and deterministic identity ordering for equal dates.

Document builds refresh cached titles and dates for selected documents. Supported document deletion removes every deleted target from the selection list. Working retains drafts and unpublishable selections. Prepare Preview filters against its exact prepared ordinary/collection set, retaining inherited ordinary exclusions and excluded collection-host behavior. Publish copies the prepared `selected.json` unchanged. Search and Recents keep their current lifecycle.

## Deliverables And Process

One canonical selection file, one shared Working star control, ordinary/collection build and deletion integration, one public report on dotlineform, and selected-payload Preview/Publish/read configuration. Select documents in Working; review the filtered report in Preview before publishing through the existing workflow. The local control uses `star.svg` and its filled yellow variant. Public routes load only the prepared reader payload and shared report code.

## Delivery Steps

### SD-0 — Readiness

- [x] Confirm exact document action context, canonical workspace, report host and lifecycle owners.
- [x] Obtain implementation approval, including the yellow filled state.

Record: user approved the change set. The working tree was clean when implementation began. Whole-collection retirement remains unavailable under its existing owner; this delivery covers ordinary subtree deletion and individual collection-document deletion.

### SD-1 — Implementation

- [x] Add selection ownership, star control, report, build/deletion maintenance and Preview/Publish integration.
- [x] Initialize the canonical empty list and replace the dotlineform placeholder with the report declaration.
- [x] Project changed public code through the site-code inventory.

Verification budget: focused Python/JavaScript lint, read-only source/JSON inspection, required site-code projection/check and site validation, plus whitespace checks. These commands take seconds to minutes, write only the required code projection or lint caches, and do not publish. The ordinary source watcher owns the report-host rebuild; use the documented targeted build only if unavailable. No tests, fixtures, temporary regression scripts, browser automation, live Preview/Publish, commit or push are authorized by this delivery. UI interaction and yellow-star presentation remain user review gates.

Record: implemented the canonical list, a single exact-target Working control for ordinary and collection documents, the shared report, source-build metadata maintenance, deletion cleanup and filtered Preview/copy-only Publish integration. The user clarified that report refresh events are unnecessary; the report now loads only when opened, with no refresh listener. The running watcher rebuilt dotlineform with `report.id: selected_documents` and its generated report host; no manual document or Search build was run. The selection list starts empty. Python service changes require a Local Studio restart before manual use.

### SD-2 — Code Review

- [x] Review exact identities, stage exclusions, metadata freshness, deletion completion, public routing and error behavior.
- [x] Resolve findings and repeat only affected checks.

Record: source review caught and resolved the separate ordinary report-context configuration path, the projection tool's explicit public-report allowlist, and the workspace-owned selection file's exclusion from collection watcher paths. Collection deletion restores the prior selection file within its existing recovery operation. Publication retains selected bytes through both URL projection and repository copying. Initial lint passed for 14 Python and 11 JavaScript source files; affected Python files and the updated projection policy were linted again after review. `bin/site-code-update` added two public modules and changed three; `bin/site-code-update --check` reports 98 current projected files. `bin/site-validate` passes with 75 Docs Viewer runtime modules, 62 required files and seven required directories. `git diff --check` passes. These are source/static and watcher evidence, not browser or live deletion/Preview/Publish evidence. No tests or fixtures were changed or run.

### SD-3 — Closeout

- [x] Record focused evidence and remaining manual/lifecycle gates.
- [x] Transfer the durable contract to its maintained owner and recommend whether to retain this feature record.

Record: implementation complete and manually accepted by the user on 2026-09-26: “works great!” This records acceptance after manual use; specific deletion and Preview/Publish scenarios were not reported individually. Codex performed no Preview replacement, Publish, commit or push. [Source Organisation](d-20260423-000000-8fd731.md) owns the shipped storage/UI/lifecycle contract and Development Checklist has the durable guardrail. Recommend retiring this temporary delivery record now that acceptance is recorded; retain it until deletion is approved. No further feature or test work is implied.
