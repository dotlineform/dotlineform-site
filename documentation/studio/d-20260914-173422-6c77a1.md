---
draft: false
doc_id: d-20260914-173422-6c77a1
title: Archive Documents
added_date: "2026-09-14 17:34:22"
last_updated: "2026-09-14 17:37:45"
summary: Replace general document Copy and Move with Archive to Notes Working, including selected parents' descendants automatically.
ui_status: in_progress
parent_id: d-20260428-000000-f5ff18
---
# Archive Documents

## Approved Outcome

One Archive action moves selected ordinary scope documents to Notes. Selecting a parent includes every descendant; overlapping selections are counted once. The confirmation body states only the number of documents to be moved. General Copy/Move capabilities and tests that exist only for them are deleted. The user confirmed that child collections, including Analysis Works, are outside Archive.

## Delivery

- [x] Replace the general transfer menu, modal, client and service routes with fixed-destination Archive for ordinary Working documents. Notes does not offer Archive.
- [x] Preserve IDs, metadata, internal hierarchy and referenced local media; validate the exact preview before writing and finish Notes before removing source documents.
- [x] Remove sub-scope Copy presentation, Copy lineage mutation helpers, transfer customisation hooks and obsolete transfer tests. Retain the independently owned same-scope placement, Copy Link and existing lineage readers/deployment boundaries.
- [x] Verify subtree counting, request/receipt authority, collisions and stale previews, failure reporting, generated document completion and default clearing with focused service/generator tests.
- [x] Review the bounded code and test diff; update [Source Organisation](/docs/?scope=studio&doc=d-20260423-000000-8fd731) as the durable owner.
- [x] Finish shared runtime projection checks and restart the normal local services.
- [ ] User review of Archive selection and the count-only confirmation modal.

## Verification Record

The verification budget covers deterministic source movement, exact stage/destination requests, safe failure boundaries and actual generated output. Existing placement, capability, stage and route checks cover the shared integrations. No browser choreography test is added; presentation and modal interaction are manual review.

Archive: 21 tests passed, including a real builder integration. Placement: 21 passed. The other focused stage, capability, route and customisation checks passed in the same implementation run. The JavaScript stage/request contract passed. Lint passed for all changed Python and canonical JavaScript files.

`bin/site-code-update` changed only the two shared runtime projections; its check, `bin/site-validate` and `git diff --check` passed. The normal local services and watcher were restarted. Read-only live checks confirmed Archive available in Analysis, App, Processing and Studio Working, unavailable in Notes and every read-only stage, and both new runtime assets served successfully. Both documentation projections were confirmed.

Review also exposed 14 existing failures in retained lineage and public-delete tests: obsolete unstaged lifecycle assumptions. The same retained tests produce the same 14 failures against committed production code in an isolated fixture checkout. The committed public-delete test module additionally had an import from an already removed helper; deleting its retired Copy-dependent tests removes that collection failure. Archive does not change the retained lineage/deployment behavior. These separate lifecycle-test assumptions remain outside this replacement.

## Review And Closeout

Ready for manual interaction review. No live user document was moved as automated verification. Retain this short feature record until the user accepts the behavior and chooses to archive it.
