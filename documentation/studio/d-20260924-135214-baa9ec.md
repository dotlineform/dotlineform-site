---
draft: false
doc_id: d-20260924-135214-baa9ec
title: Collection Search And Site Search
added_date: "2026-09-24 13:52:14"
last_updated: "2026-09-24 22:11:17"
summary: Separate collection search, shared site-search and Recents coverage, and proposed editorial stars for a curated home-page report.
parent_id: d-20260428-000000-f5ff18
---
# Collection Search And Site Search

This feature records implemented site-search selection and Recents generation/copying, with proposed collection search and editorial curation. It is maintained under [Planned Features](d-20260428-000000-f5ff18.md). The Search and Recents slices below are implemented; the remaining candidates are separate deliveries.

Here, **site search** means the Docs Viewer main search over the selected workspace stage, including the public reader of its accepted snapshot. **Collection search** means search within one collection report. The separate Catalogue Works report and public Catalogue Search are distinct consumers.

## Three Independent Decisions

### Collection-Scoped Search

A collection represents a cohesive set of related documents. Searching within that set is useful in its own right, even when its documents also participate in site search. Every collection should therefore have its own scoped search, including Works and Processing. Site-search inclusion does not remove that need.

Collection search returns only that collection's documents. Following links from those results may take the reader elsewhere, but linked documents do not automatically enter the collection's search results.

### Full Text Or Fields

The appropriate search type depends on what the reader is trying to find. Works, Concepts and Moments contain readable documents whose meaning extends beyond their titles, so their collection searches should cover full text. Site search also remains full text.

Catalogue surfaces a large set of structured Work records through generated by-ID payloads. Its useful retrieval surface is selected Catalogue metadata: Title and Series, with additional report filters for fields such as Medium. The distinction is the records' purpose and scale; generated by-ID JSON is also used to display the other collections and does not itself determine the search type.

Processing also needs collection-scoped search. Full text is the suggested starting point for its documentation, but its exact collection-search field policy has not yet been agreed.

### Site-Search Inclusion

Works and Processing belong in site search alongside ordinary documents. Their documents may connect to Library or other groups of reference documents, and readers should be able to discover that wider material through one search.

Catalogue, Concepts and Moments should be searched within their own collections and excluded from the site-search document corpus. This is an explicit collection policy based on the intended reading experience. It must not be inferred from whether individual documents currently contain links, their titles, or their Subject metadata.

The agreed corpus is ordinary documents plus Works and Processing, excluding draft and unpublishable ordinary roots and their descendants, and filtering flat collection entries individually by draft state. Every source document requires explicit boolean `draft`. [Preview Stage Consolidation](d-20260924-201824-a27c31.md) replaced Pre-publish and local Published with one Preview snapshot. [Copy Working Search Into Preview](d-20260924-210855-8b419e.md) made Working the sole Search builder and copies its existing file unchanged through Preview and repository projection. The Search selection delivery below now applies the agreed coverage during Working builds.

The user subsequently selected [Remove Stage Context From Search](d-20260924-203338-b9417d.md) as the prerequisite before other Search changes. Its v4 format removes embedded stage and result URLs while retaining document/collection identity, current corpus membership and postings. Readers supply route context. It does not implement the new coverage policy or consolidate build ownership.

[Retire Public Document Locations](d-20260924-205346-c74d90.md) removed the unused location index/picker and legacy Catalogue document-URL refresh. Deploy Repo counts accepted documents directly. The Search copy workflow removes the old equality check between Search membership and the complete prepared document set; no coverage or readiness filter is added by that ownership change.

## Intended Search Matrix

| Content | Collection-scoped search | Collection search type | Included in site search and Recents |
| --- | --- | --- | --- |
| Ordinary documents, including Library and other reference groups | Not applicable | Not applicable | Yes |
| Works | Yes | Full text | Yes |
| Processing | Yes | To confirm; full text suggested | Yes |
| Concepts | Yes | Full text | No |
| Moments | Yes | Full text | No |
| Catalogue | Yes | Title and Series, with selected field filters | No |

Collection exclusion concerns the subdocs inside that collection. Its report landing page remains an ordinary document and follows the ordinary draft/unpublishable rules, so readers can still discover and open an otherwise eligible collection landing page.

## Site Search: Index Build Process Implemented

This Search change is implemented after the completed build/copy workflow. Select eligible IDs from existing Working metadata before reading document content. Ordinary documents form a tree; collections are flat sets of subdocs. Collection membership does not create a parent/child hierarchy between subdocs, and this implementation requires no collection `parent_id` field. [Docs Viewer Search Index](d-20260813-214350-4c7a21.md) owns the maintained contract.

- [x] Implement metadata selection, exact selected-content loading, and required collection inclusion configuration. New collections explicitly start excluded; Recents can reuse the shared selector in its separate delivery.
- [x] Run focused lint and a real Working Search dry run/write. The saved v4 index contains five ordinary documents and 11 Works, with 709 terms in 48,174 bytes. Processing is skipped because its configured ordinary host is draft. Eligible Catalogue, Concepts and Moments landing pages remain ordinary results. A subsequent dry run skipped unchanged content.
- [x] Review the changed source/config contract. Add the missing ordinary source/tree parent consistency check and remove the duplicate collection title comparison. Focused Python lint for all three changed modules and `git diff --check` pass after review.
- [x] Update the durable Search owners and Development Checklist. No tests, fixtures or harnesses were changed or run. Evidence covers the current real Working corpus; malformed-input/failure scenarios and a selected Processing host were not exercised. No Preview preparation, repository/public deployment, commit or push was performed.

The Recents delivery below now implements matching eligibility, compact collection metadata reads, and unchanged copying through Preview. Collection full-text search and editorial selection remain proposed.

### Inputs And Relevant Owners

Resolve all external paths through [workspace configuration](../../docs-viewer/config/workspace/docs-workspace.json) and [its path resolver](../../docs-viewer/services/docs_workspace_config.py). Paths beginning `working/` or `preview/` below are relative to the configured `$DOTLINEFORM_DOCS_BASE_DIR`; they are not repository directories.

| Input or output | Role in the build |
| --- | --- |
| `docs-viewer/config/workspace/docs-workspace.json` | Exact collection IDs, configured `report_host_doc_id` values, storage locations and ordered `search_fields`. Introduce the explicit site-search inclusion policy alongside collection configuration, with ordinary documents, Works and Processing selected; retain one inclusion decision for eventual reuse by Recents. Registration alone does not imply inclusion. |
| `working/generated/documents/index-tree.json` | Ordinary document IDs, titles, explicit `draft` values and nested `children`. Traverse this tree to select ordinary documents and eligible collection hosts. |
| `working/source/documents/unpublishable.json` | The separate authoritative array of excluded ordinary document IDs. Read it with `read_publication_ignore_ids` in [docs_publication_ignore.py](../../docs-viewer/services/docs_publication_ignore.py). It is not embedded in the tree or Search index. |
| `working/generated/collections/<collection>/documents/manage-manifest.json` | Flat subdoc IDs, titles, explicit `draft` values and update metadata. Use the Works and Processing management manifests; the reader `manifest.json` does not carry `draft` and is not the eligibility input. |
| `working/source/documents/<doc_id>.md` and `working/source/collections/<collection>/documents/<doc_id>.md` | Canonical searchable text for selected documents only. Resolve exact IDs through the configured source boundary and existing [source model](../../docs-viewer/services/docs_source_model.py). |
| `working/generated/collections/<collection>/documents/by-id/<doc_id>.json` | Retain the current selected-subdoc consistency checks against its source and management manifest: exact identity, title, update time and configured host URL. These payloads are not the authority for searchable text. |
| `working/generated/search/index.json` | The complete `docs_viewer_search_index_v4` output, built by [build_search.py](../../docs-viewer/build/build_search.py). |
| `preview/search/index.json` and repository `site/assets/data/search/analysis/index.json` | Unchanged copies made by Prepare Preview and Deploy Repo; neither applies a second coverage filter or rebuilds Search. |

The tree metadata is produced by [payloads.py](../../docs-viewer/build/docs_builder/payloads.py), and flat management metadata by [collection.py](../../docs-viewer/build/docs_builder/collection.py). Both already emit the required Working `draft` values. No additional metadata index or parent map for collections is needed.

### Build Sequence

1. **Resolve Working inputs and collection coverage.** Load the workspace configuration and the explicit inclusion policy. Select ordinary documents plus the exact `works` and `processing` collection IDs. Do not load Catalogue, Concepts or Moments manifests, sources or by-ID payloads for this Search build. Use configured host IDs; do not discover hosts by scanning document bodies, titles or routes.
2. **Account for `unpublishable.json`.** Read the current policy file once through `read_publication_ignore_ids` before selecting documents. Preserve its existing ordinary-document ownership and validation: an unreadable, absent or invalid file stops the build, and an empty JSON array means no explicit exclusions. Treat the returned IDs as ordinary subtree exclusion roots. Do not rewrite the file, turn its entries into a new per-collection policy, or assume that draft filtering covers this separate rule.
3. **Traverse the ordinary Working tree.** At each node, stop traversing that branch if its ID is in the unpublishable set or its explicit `draft` value is `true`. Otherwise retain its exact ID and display metadata, then visit its children. This single traversal inherits both kinds of exclusion through ordinary descendants. Collection report hosts participate as ordinary nodes, including when an excluded ancestor removes them. An ordinary collection landing page is not excluded merely because its subdocs are outside site-search coverage.
4. **Select flat collection entries.** For each included collection, first require its configured host to be among the selected ordinary IDs. If it is excluded, skip the entire collection without reading its manifest or content. Otherwise read that collection's Working `manage-manifest.json` and select each entry whose explicit `draft` value is `false`. Check subdocs individually: excluding one does not exclude its peers, and no descendant traversal or `parent_id` lookup occurs within a collection. Retain exact collection, subdoc and report-host IDs with each selected target.
5. **Load and validate selected content.** Only after metadata selection, read the selected Markdown files and the selected collection by-ID payloads needed by existing consistency checks. Adapt the current source loaders to accept the selected IDs instead of loading every source and filtering afterwards. Required metadata and selected source/output inconsistencies must fail clearly; do not fall back to a full source scan or silently substitute another identity. Search expects current Working document projections. Manage Rebuild already builds document outputs before Search; a direct Search build retains that prerequisite and does not rebuild documents itself.
6. **Extract fields and build postings.** Reuse the existing Markdown extraction, normalization, tokenization and field policy for Title, Heading, Summary, Body and Code. Construct the deterministic document table and term-to-document/field postings from the selected records only. Preserve exact target identity, ranking behavior and the v4 JSON shape. An excluded document contributes neither a result row nor searchable terms.
7. **Write the complete Working index.** Compute the existing content version over fields, document metadata and postings. Write the complete `working/generated/search/index.json` when its content changes; retain the current unchanged-content skip behavior. This remains a full index build, with no incremental posting updates and no Search rebuild on ordinary watcher or source-save events.
8. **Use the existing copy workflow.** Prepare Preview captures the saved Working index with its reviewed plan and copies its exact bytes while independently preparing eligible documents. Deploy Repo copies that same Search file onward. Coverage is decided during the Working Search build, never during copying or browser querying. [Copy Working Search Into Preview](d-20260924-210855-8b419e.md) owns the implemented copy and revision behavior.

### Implementation Boundary

The code change belongs primarily to `build_search.py`, the shared inclusion-policy owner and its workspace configuration. Reuse `docs_publication_ignore.py` for policy-file resolution and validation. Preserve the distinction between ordinary subtree exclusion and flat collection entry filtering; do not apply a generic descendant algorithm to subdocs. This section specifies the Search selection/build change only. Recents can consume the same collection inclusion decision through its separately described candidate-gathering change below; its date handling and build mechanics remain unchanged by this Search step.

Implementation should leave the existing v4 schema, text extraction, ranking and build/copy ownership intact. The observable result is an index containing only selected ordinary documents and non-draft Works/Processing subdocs whose ordinary hosts remain eligible. Tests, fixtures and harness changes require their separate agreed specification; this documentation update does not create or modify them.

## Recents And Editorial Selection

Recents should use the same document coverage as site search: ordinary documents, Works and Processing. Catalogue, Concepts and Moments entries are excluded from both. Use one explicit membership policy consumed by both builders, applying it before sorting and limiting Recents. Filtering an already truncated list could leave few or no entries even when other eligible recent documents exist. Shared coverage does not require Recents to load or rebuild the search index.

Recents remains an automatic view of document activity. The user agreed that it should apply the same draft/unpublishable eligibility as Search and follow the same Working-build/Preview-copy principle. Compact collection metadata reads are included in this delivery. Matching site-search coverage removes bulk Catalogue activity from the list, but does not provide editorial selection for a site entry page.

The proposed solution is an editorial **star**: manually select any ordinary or collection document, then list the selected documents in a report embedded in a home-page document. Catalogue, Concepts and Moments documents can be starred even though they are excluded from site search and Recents. A star does not change either inclusion policy.

The intended workflow is simple: open a document in Working, toggle its star, and review the selected list. The public home page displays the resulting document list without a star icon, selection control or public starred-state indicator. Its heading can describe the editorial purpose, such as Selected Work; the internal mechanism need not be presented to readers as Starred.

Stars require durable editorial state with exact document identity, independent of browser-local bookmarks. Choose the storage owner during specification, including how selection survives Catalogue record regeneration. The report uses the existing document/report navigation model and must distinguish ordinary documents from exact collection targets.

A document may be selected while still a draft. The public report includes only selected documents present in the accepted published set, with valid destinations. Starring does not publish a document or bypass its collection host's publication eligibility. Changes to the public selection follow the normal preparation, Publish and deployment lifecycle.

Selection and ordering are separate decisions. A star alone says which documents belong, not which is newest or most important. Newest-starred-first is a useful initial proposal because content edits would not reshuffle the presentation; manual ordering is another option if editorial placement matters. Neither is agreed yet. Also decide whether a curation-only change affects the document's edited timestamp; the recommendation is to keep editorial selection changes from making unchanged content appear recently edited.

## Inspection Before The Search Delivery

Read-only inspection before this delivery on 2026-09-24 found one site-search index per lifecycle snapshot. The workspace configuration enabled Title, Heading, Summary, Body and Code. The builder then included ordinary documents and all configured, validly hosted collections without an explicit per-collection site-search inclusion setting. The implemented Search section above supersedes that builder and Working artifact state; the observations below retain the planning baseline.

The inspected Working index, generated on 2026-09-23, contains 5,214 documents and occupies approximately 3.34 MB of raw JSON: 50 ordinary documents, 244 Works, one Processing document, 246 Concepts, 57 Moments and 4,616 Catalogue documents. Applying the proposed site-search membership to that snapshot would leave 295 documents. This is a document-count projection, not a measured index-size or latency improvement.

The local Published snapshot also includes Catalogue, Concepts and Moments. The tracked public-site index is an older snapshot containing Concepts and Moments but predating Catalogue's inclusion. The deployed website was not inspected. Lifecycle snapshots must be distinguished when checking search behaviour.

All collection reports currently use the same title-prefix filter: a query matches the beginning of a document title. They do not provide full-text collection search or Catalogue Title-and-Series search. The report fetches the entire manifest on opening and renders every row when the query is empty. Each input change filters and sorts the records and recreates the matching rows; individual document bodies are loaded when opened.

For Catalogue, the Working management manifest contains all 4,616 records and occupies approximately 1.49 MB. It has no Series or Medium projection for the proposed search/filter controls. The separate Catalogue Works report starts with no visible result rows, but still loads its full dataset on opening. Avoiding an initial list render and deferring data loading are separate improvements.

Both Manage and public routes currently use Recently Edited. The saved Recents payloads inspected on 2026-09-24 contain 16 Catalogue entries out of 20 in Working, and 20 out of 20 in both Pre-publish and local Published. These findings confirm the membership problem; the build process and bounded change are described below.

Existing bookmarks are saved in browser IndexedDB and provide personal navigation. They do not supply durable editorial selection to publication. Relevant owners are the [Recent candidate and payload builder](../../docs-viewer/build/docs_builder/payloads.py), its [build lifecycle](../../docs-viewer/build/docs_builder/pipeline.py), [route date-basis policy](../../docs-viewer/config/routes/docs-viewer-routes.json), and [bookmark controller](../../docs-viewer/runtime/js/shared/docs-viewer-bookmarks.js).

Current implementation owners are [workspace configuration](../../docs-viewer/config/workspace/docs-workspace.json), the [search builder](../../docs-viewer/build/build_search.py), the [shared search engine](../../docs-viewer/runtime/js/shared/docs-viewer-search.js), the [collection report](../../docs-viewer/runtime/js/shared/docs-collection-report.js), its [title filter](../../docs-viewer/runtime/js/shared/docs-collection-report-filter.js), and the separate [Catalogue Works report](../../docs-viewer/runtime/js/reports/catalogue-works-report.js). Existing [Docs Viewer Search documentation](d-20260602-160839-6d3cbb.md) contains older scope terminology and should be reconciled with the shipped result during delivery closeout.

## Recents: Working Build And Preview Copy Implemented

This delivery makes Working the sole Recents builder, applies the same collection coverage and document eligibility as Search, reads compact collection metadata, and copies the saved result unchanged through Preview and repository projection. Implementation and focused review are complete. [Generated Data Contracts](d-20260605-125108-c68916.md#recent-contract) owns the maintained Recents contract.

### Previous Implementation And Replaced Paths

The previous candidate builder reused ordinary records already loaded by the document build, then opened Markdown sources in every configured collection, including Working drafts and unpublishable documents. The pipeline generated Recents during full ordinary builds in both Working and temporary Preview, with separate local and publication projections. Those collection scans, Preview generation and publication variants are now removed. Targeted ordinary builds and collection-only builds continue leaving Recents untouched.

The payload no longer stores stage-specific `content_url` values, and the reader adapter no longer requires or consumes them. Result links continue using exact document/collection/report-host identity through the existing result controller. The `docs_recent_v1` schema and existing identity fields remain; copied payloads are validated to contain only stage-independent identity and display metadata. No compatibility alias, alternate stage payload or legacy URL reader was added.

### Working Build Sequence

1. **Use the shared inclusion policy.** Ordinary documents remain candidates. Select collections through `DocsStageConfig.site_search_collections`, currently Works and Processing. Do not read Catalogue, Concepts or Moments manifests, sources or by-ID payloads for Recents. Their eligible ordinary landing pages remain candidates.
2. **Apply ordinary eligibility.** Reuse ordinary document records already loaded by the full Working document build. Read `unpublishable.json` once through `read_publication_ignore_ids`; missing, unreadable or invalid policy stops the operation. Exclude draft and unpublishable ordinary roots with all descendants, using the current in-memory hierarchy. Do not reread ordinary Markdown or an older saved tree solely for Recents.
3. **Select compact collection metadata.** Require each included collection's configured host to survive ordinary eligibility before reading its Working `manage-manifest.json`. Use flat entries with exact `doc_id`, title, explicit boolean `draft` and date metadata. Select each non-draft entry independently; collection peers have no inherited exclusion or parent lookup. Do not load collection Markdown, render bodies, read by-ID bodies, or read the Search index to construct Recents.
4. **Preserve date inputs.** Management manifests already carry `last_updated`; include the existing effective `added_date` projection and retain it through full and targeted manifest updates so the supported added-date behavior does not require source reads. Keep current date handling, timestamp validation, deterministic tie-breaking and limit 20. The current selected basis is edited for both local and public readers; one Working payload supplies that basis through every stage.
5. **Finish selection before sorting and limiting.** Construct exact ordinary and collection targets from eligible metadata, then sort the complete eligible set and keep the newest entries. Missing or invalid required metadata fails clearly; no whole-source fallback or inferred replacement identity is permitted.
6. **Write Working Recents once.** Full Working document builds own generation and unchanged-content skip behavior. Targeted ordinary builds, collection-only builds, watcher saves and ordinary source saves leave Recents untouched. Manage Rebuild refreshes included collection metadata before the ordinary build generates Recents once; excluded collections retain their existing position after ordinary output. A direct full Working document build requires current saved collection metadata and does not rebuild unrelated collections. Source-service and watcher fallback rebuilds explicitly skip Recents.

The compact collection metadata change is part of this delivery. Ordinary records already loaded by the document build remain reusable; a broader metadata-storage redesign or performance benchmark is not required.

### Preview And Repository Copy

Prepare Preview captures the saved Working `recent.json`, validates its stage-independent contract and includes the exact bytes in the reviewed plan revision. A missing or invalid payload fails without rebuilding Recents. A changed payload invalidates the plan, including a change during preparation. The temporary Preview build copies the captured file before recording its complete generated manifest; it must not gather candidates, sort, limit, change display labels or regenerate timestamps.

The finished Preview snapshot and Deploy Repo retain those exact bytes. Remove Recents-specific URL rewriting and publication-variant substitution. Coverage and eligibility are decided during Working generation, with no second filter during copying or browser querying. Ordinary edits may leave the saved Recents payload stale until its owning full Working build; preparation does not impose source freshness or require Recents membership to equal the prepared document set.

Relevant lifecycle owners are [Prepare Preview](../../docs-viewer/services/docs_prepare_preview.py), the [captured Preview entrypoint](../../docs-viewer/build/build_preview.py), [rebuild orchestration](../../docs-viewer/services/docs_write_rebuild.py), [snapshot assembly](../../docs-viewer/services/docs_preview_snapshot.py), and [Deploy Repo](../../docs-viewer/services/docs_deploy_repo.py). Reuse the existing Search capture/revision/copy seams for Recents rather than introducing another lifecycle.

### Delivery And Review Gates

- [x] Agree matching Search eligibility, Working-only generation, exact Preview copying, and compact collection metadata reads within this delivery.
- [x] Implement shared Search/Recents metadata selection, management-manifest dates, the stage-independent payload/reader, required build ordering, authoring fallback suppression, and revision-bound copies through existing lifecycle owners.
- [x] Verify focused Python/JavaScript lint, shared asset projection/site validation, and real Working metadata/build diagnostics. Works and Processing builds changed only their management manifests; the ordinary build changed only Recents, producing 16 entries (five ordinary, 11 Works) in 3,490 bytes. Its next dry run skipped every write. The production JavaScript adapter read all 16 entries without stored URLs. Search's dry run retained its existing content version.
- [x] Verify isolated preparation and read-only repository projection over 4,935 real eligible documents. Working, generated-manifest, Preview and repository Recents hashes matched; Search copying remained unchanged. Live Preview's completion receipt was unchanged. Evidence: `var/recents-working-copy/verification.json`. The first diagnostic completed the build but failed on an incorrectly relative projection lookup; the corrected diagnostic produced the recorded evidence.
- [x] Review the final ownership/copy boundaries and document the result. Review narrowed the build-order change to included collections and added explicit malformed-basis rejection. No tests, fixtures or harnesses were changed or run; browser interaction, failure injection, full live Manage Rebuild, live Preview replacement, deployment, service restart, commit and push were not exercised. The next Prepare Preview uses the copy workflow after the running service reloads the changed Python code.

## Design Direction

Represent the three search decisions independently in collection policy, with site search and Recents consuming the same inclusion decision. Keep membership selection separate from query matching and ranking. Reuse the existing full-text extraction and query engine for readable collections, while letting each search surface select its exact corpus. Separate collection indexes loaded on demand are a candidate design; choose their physical layout and shared build work during delivery specification rather than freezing that architecture here. Editorial stars form a separate selection across all collections.

Catalogue should use a compact generated search projection of its selected fields, linked to the exact collection document and canonical Work identity. Resolve Series and Medium from their authoritative Catalogue fields. Search should not fetch every document body or scrape rendered content to reconstruct metadata. A Medium filter should preferably use a structured classification such as `medium_type`; confirm whether readers also need the descriptive `medium_caption`.

For Catalogue, start with an empty result list and show a bounded page of matches after a query or useful filter selection. Decide separately whether to load the compact search data on opening or on first use. Distinguish initial transfer and parsing, query work, and result rendering when assessing speed. Payload measurements identify plausible costs, but browser responsiveness has not been measured and no performance target is yet agreed.

Preserve exact stage, collection and document identity when opening results. Site-search inclusion changes retrieval membership, not publication eligibility. Working, Preview and the public projection retain distinct owners; prepared snapshots must contain the appropriate search artifacts without exposing local-only data. Specify refresh and build ownership for new indexes explicitly, preserving the current separation between ordinary document saves and explicit Search rebuilds.

## Candidate Deliveries

These are proposed outcome boundaries for discussion, not approved implementation steps or a fixed delivery order.

| Candidate | Complete result | Decisions needed before specification |
| --- | --- | --- |
| Site-search and Recents membership | Implemented: shared coverage/eligibility, compact Recents collection metadata, Working-only Recents generation and unchanged Preview/repository copying. | Complete within the evidence and live-operation limits recorded above. Any additional test work needs its own specification. |
| Collection full-text search | Works, Concepts and Moments can each be searched across their own document text; Processing receives its agreed search type. | Processing fields, index layout, refresh ownership, result presentation and initial loading behaviour. This can be split into independently complete collection deliveries. |
| Catalogue collection search | Title-and-Series search and selected field filters retrieve exact Catalogue documents with bounded result rendering. | Matching semantics, whether exact Work/Series IDs are also searchable, Medium fields, filter combinations, initial loading and a practical response target. |
| Editorial stars and home-page report | Any ordinary or collection document can be selected in Working; a home-page report presents the selected, published documents without exposing star controls or indicators publicly. | Durable selection owner, ordering, curation timestamps, regeneration preservation, report presentation and build/publication ownership. |

The user selected Search coverage first, so Concepts and Moments subdocs now rely on their existing collection title filters until full-text collection search ships. Works retains both search surfaces. Recents eligibility, compact metadata and build/copy adoption are implemented. Collection-scoped search and editorial stars remain separately proposed deliveries.

Each remaining delivery should state its observable outcome, generated and runtime artifacts, lifecycle effects, proportionate verification budget, and review gate. Any new or changed tests need their own agreed specification.
