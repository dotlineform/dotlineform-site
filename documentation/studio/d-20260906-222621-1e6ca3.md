---
draft: false
doc_id: d-20260906-222621-1e6ca3
title: Publishing Workflow - Analysis Media Namespace
added_date: "2026-09-06 22:26:21"
last_updated: "2026-09-06 22:35:00"
summary: "Complete: both Analysis stages use one logical media namespace, 348 Working tokens are migrated, and exact stage resolution is verified."
ui_status: done
parent_id: d-20260908-171728-3c539a
---
# Publishing Workflow - Analysis Media Namespace

## Outcome And Boundary

Both Analysis stages use `docs/analysis/<type>/<identity>` in authored media tokens. The exact document/build stage selects `/docs/media/analysis/working/...` or `/docs/media/analysis/pre-publish/...`. Remove the per-stage `media_namespace` setting. Staged builds reject unconfigured Docs media namespaces instead of sending them through generic media URL resolution.

The user approved this cleanup on 6 September 2026, superseding the earlier decision to retain `docs/dotlineform`. Update 348 existing tokens in 226 Working documents in place, preserving all other bytes, IDs and media filenames. No media moves, compatibility alias, promotion, Search/media build, Published/public change or broader semantic-token redesign is included.

## Delivery

- [x] Readiness: confirm configuration, token/rendering and media-serving owners; inventory affected source and unchanged output boundaries. Three ordinary and 223 Works documents need token edits.
- [x] Remove the override and migrate tokens while the affected local runner is stopped; rebuild affected document projections and local browser configuration.
- [x] Verify exact source changes, stage-specific rendering/media reads and unchanged Pre-publish, accepted snapshots, media and Search; restart the normal local runner.
- [x] Review the bounded code/config/test/source diff and resolve findings.
- [x] Update Source Organisation, record results and close the cleanup.

The user has approved implementation of this complete bounded change. Stop only for a material expansion or an unresolved finding; ordinary namespace cleanup does not reopen the completed stage delivery.

## Verification Budget

| Contract | Lowest adequate evidence |
| --- | --- |
| Identical token identity resolves only the selected stage | Focused stage renderer, source inventory and existing media-route tests using different fixture bytes in each stage |
| Unconfigured Docs namespaces cannot become generic remote media URLs in staged builds | Renderer rejection checks with the former Working namespace and another unconfigured namespace |
| Configuration and existing report/Tag consumers agree | Owning stage/config tests and affected fixture-based report/Tag tests; changed-source lint |
| Source migration changes only the approved token prefixes | Hash comparison against the expected transformation and retained inode/identity evidence |
| Actual output follows migrated source without content deployment | Two affected collection builds, generated payload inspection, protected-file hashes and fresh local service reads |

Evidence is retained in `var/analysis-media-namespace/`. No browser-suite expansion or broad profile is required.

## Result And Closeout

Complete on 6 September 2026. `docs_scope_config.py` now derives both stage media prefixes from the Analysis scope. `rendering.py` rejects unconfigured Docs namespaces in staged builds; explicit external URLs and non-workflow scope behavior are unchanged. The local browser projection changes only its four Working media prefixes. No shared/public browser runtime changed.

All 878 Working source files match the expected transformation and retain their file identities: exactly 348 prefix replacements in 226 documents, with all other bytes unchanged. All 347 distinct document/media references retain the correct Working rendered URL and an existing generated media file. Both affected collection builds passed with zero warnings and zero document payload changes, confirming that the new logical references produce the same output. The ordinary build targeted three documents; the owning Works builder reconciled its collection. No media producer or Search build ran.

All 1,879 protected files and their membership remain unchanged: Pre-publish source/generated content, both retained Published trees, Working generated media and Working Search. Working source media and collection data are covered by the source comparison. No tracked public output changed.

Verification passed: 107 focused Docs Python tests, eight Studio Tag workflow tests, the JavaScript stage request contract, explicit lint for seven changed Python files and whitespace review. The Docs and Studio tests were run separately after a combined run exposed the Docs fixture environment hiding the live association payload used by one Studio test; no product fix was needed. The JavaScript test is outside maintained source-lint targets and was verified by executing its contract.

Bounded code review found no unresolved issue or compatibility alias. The retained regression adds shared-identity image/SVG/file/HTML resolution and rejects unconfigured namespaces in each stage; existing tests prove differing stage media bytes, report targets and non-workflow behavior. No additional lint/type/reference adoption trigger was found.

The normal `bin/local-all` runner was restarted successfully. Ten read-only live checks passed across both stages: browser configuration, representative image/PDF/HTML bytes and an ordinary generated document. This restart also loads the prior stage delivery's default-Delete service fix; no destructive Delete cycle was repeated. Evidence is in `builds.json`, `verification.json`, `live-checks.json`, `docs-tests.txt`, `studio-tests.txt`, `python-lint.txt` and `local-all.log` under the evidence directory.

Source Organisation and parent navigation are updated; the initial manual move map is explicitly marked superseded for namespace retention. Documentation projections follow through the normal Studio watcher. This cleanup is ready for manual archive after review. No promotion, deployment, commit or push was performed.

## Durable Destination And Follow-on

[Source Organisation](/docs/?scope=studio&doc=d-20260423-000000-8fd731) owns the shipped layout and media contract. This delivery is temporary and can be manually archived after closeout review. The broader publishing concept, Catalogue schema decision, UAC 3 and publishing-action design remain separate.
