---
draft: false
doc_id: d-20260925-151229-341ee0
title: Catalogue Authoring In Docs Viewer End State
added_date: "2026-09-25 15:12:29"
last_updated: "2026-09-26 08:17:17"
summary: Establish shared local assets and stage-owned Catalogue JSON before report changes; Save prepares Working data, Prepare Preview captures JSON, Publish copies to the repository and R2, and Deploy runs GitHub Actions.
parent_id: d-20260925-134621-42c838
---
# Catalogue Authoring In Docs Viewer End State

Status: CSI-0–4 are complete and CSI-5 focused Save/Review verification is in progress. The maintained checkout serves stage-owned Catalogue JSON and shared local assets; a Preview is prepared and verified superseded stores are retired. Catalogue/Search/Recents byte comparisons, asset availability, direct local reads and public-code/site checks passed at cutover. The user's title-only Save for Work `00008` updated canonical and Working Catalogue titles while Preview retained its captured title. It exposed a stale Subject lookup; the approved reader correction passed focused lint, direct-read inspection and source review, with user review pending. Changed-image Save, Publish transfers and rollback remain unexercised. Moving the Work editor into Docs Viewer and relocating canonical records remain separate, open decisions.

**Catalogue Works means “what I have saved”.** It must show saved Catalogue data independently of later review or publication. Generating local Catalogue files is not called Publish. Publish copies prepared data to the repository and uploads referenced assets to R2; Deploy runs GitHub Actions. The interval between those two operations is accepted.

This document develops [Catalogue Reports Shared Front-End Browsing And Search](d-20260925-134621-42c838.md). The [Catalogue Save And Publish Action Map](d-20260925-144813-17e45c.md) records Save behavior before this delivery and will be reconciled at CSI-7. Its earlier standalone report-Publish proposal is superseded as the final workflow. The active runtime has adopted the storage and lifecycle changes through CSI-4; verification, review and durable documentation closeout remain open.

## The End State In Use

The author opens Catalogue Works in Working, finds a Work and opens its editor. In a possible integrated application, that editor is hosted in Docs Viewer; the data responsibilities are the same if it remains in Studio. The editor reads the exact saved canonical record. Unsaved changes belong to the editor session. Save persists edits and completes the Working outputs needed to find and display the saved Work.

Prepare Preview captures document and Catalogue JSON for Review. Preview reads those captured JSON files and the current shared local assets. There is one local reader-ready copy of each image, not a Working copy and a Preview copy. Replacing an image changes what both local stages display. If captured metadata such as dimensions no longer matches the image, the author prepares Preview again. No retained asset versions, additional asset hashes or automatic Preview invalidation mechanism are required.

Publish copies the prepared JSON to the repository and uploads its referenced current assets to R2. A later Deploy runs GitHub Actions against the committed repository state. Public images can consequently change at Publish before the site's JSON changes at Deploy. This is an accepted consequence of the manually gated deployment workflow, not a condition requiring a deployment-tracking system.

For example, Save a title change from “Study A” to “Study B”. Working uses “Study B”, while existing Preview and public JSON can still contain “Study A”. Prepare Preview captures “Study B”; Publish distributes that prepared JSON and referenced assets; Deploy releases the repository snapshot. An image replacement has different timing: both local stages see the replacement from `assets/`, and public readers can see it once Publish uploads it.

| Surface | Meaning of its data | Data owner |
| --- | --- | --- |
| Work editor | Saved canonical record plus unsaved form state. | Studio Catalogue authoring, regardless of the eventual UI host. |
| Catalogue Works in Working | Saved Works and the Manage metadata needed by the report. | Currently its canonical read path; later a generated Working report projection refreshed by the owning mutations. |
| Working document and media readers | Current Working JSON and current local assets. | Working document/Catalogue generation and the shared asset store. |
| Preview / Review | Captured document/Catalogue JSON with current local assets. | Preview preparation owns JSON and references; shared assets supply the bytes. |
| Site Preview and public site | Repository reader files and public asset URLs. | Published repository output and R2; Site Preview intentionally exercises these public paths. |

Saved Works and public Catalogue documents need not have identical membership. A Work can exist without a Catalogue document. Document eligibility continues to control document preparation; copying declared Catalogue by-ID files does not require a new Work-level publication policy.

## Operation Names And Ownership

The ambiguity to remove is “published from Studio” meaning “generated for another local reader”. Generation is an internal transformation. Review describes inspecting prepared content. Publish and Deploy have the distinct distribution responsibilities below.

| Operation | Required outcome |
| --- | --- |
| Save / generate | Persist canonical edits, prepare changed local images/downloads and generate Working Catalogue data. Studio owns image dimensions and media versions. |
| Build / Regenerate | Rebuild the outputs owned by that operation. A local build does not imply publication. Catalogue document Regenerate retains its separate document-generation responsibility. |
| Prepare Preview | Capture eligible document output and declared Catalogue JSON from Working; record referenced asset identities without copying asset bytes. |
| Review | Inspect Preview JSON using the shared current assets. Prepare Preview again when subsequent edits require a fresh review set. |
| Publish | Copy the prepared JSON to the configured repository destinations and upload referenced assets to R2. Preserve canonical data, prepared JSON and media versions. |
| Deploy | Run GitHub Actions against committed repository output. Git commit and push remain explicit, separate actions. |
| Manual cleanup | Deliberately reconcile obsolete assets when needed. Normal Save/Publish does not require a pending-deletion ledger or automatic remote deletion. |

The current user-facing Deploy Repo action becomes Publish in this delivery. Reuse its owning distribution workflow and completion/error reporting; do not introduce a second distribution implementation or compatibility endpoint merely for the name. Prepare Preview remains the operation name and `preview/` remains the storage name. The separate Docs Review package workflow is unchanged.

Media version represents an image change, not a transfer. Studio must establish the version when it completes preparation of the changed local image, before generating the JSON that Preview can capture. An unchanged image, a metadata-only edit, a relocation or an upload does not advance it merely because bytes were copied. Publish consumes the version already present in the prepared data and never writes a new canonical version after uploading.

Before this delivery, Catalogue media versions advanced after remote upload and Save included R2 work. CSI-2 moved those responsibilities to local image preparation and Publish respectively. Other Save work, such as canonical validation, Gallery/Series effects and private Studio lookup refresh, remains with its current owner unless a path adaptation is required.

## Canonical Records, Reader Data And UI Hosting

Authoring components need access to canonical records through their validated owner. Ordinary reader components should consume generated data shaped for their purpose. Hosting both in Docs Viewer does not merge those responsibilities or create another editable authority.

A generated report can represent the latest saved values without reading the canonical storage schema directly. Its generator owns field selection, joins and prepared search values; the report owns front-end filtering, sorting, pagination and navigation. Manage enrichment can extend a common public reader model by exact Work ID without placing private fields in public output.

The first storage delivery does not convert Catalogue Works to that model. It preserves existing report list/search/copy behavior and does not introduce its proposed metadata/index files. Shared image and stage-data readers do change to use the new locations. Report conversion and descending Work ID browsing with 20-row pages follow this delivery.

Canonical Catalogue records stay in `studio/data/canonical/catalogue/`. Private Studio lookups stay in `studio/data/generated/catalogue-lookup/`. Original Work images remain at their configured project sources. This delivery moves generated reader data and ready-to-read assets, not canonical records, project originals or the editor UI.

`work_id` identifies a Catalogue Work; `doc_id` identifies a document. The Works document collection is also distinct from canonical Catalogue Works. Exact generated associations connect them where an associated document exists. Co-locating output does not turn these identities into aliases or duplicate their editable fields.

## Agreed Storage Shape

`W` below means the existing workspace selected by `DOTLINEFORM_DOCS_BASE_DIR`, not the repository's application-code directory. Catalogue producers gain an explicit configured Docs-workspace destination; they do not reinterpret the Projects root as a Docs root or retain the old output directory as a fallback.

CSI-0 selected the following layout: the workspace-wide document asset namespace is `workspace`, and the collection namespace uses exact configured collection IDs. CSI-0a populated these targets within the existing configured workspace; CSI-4 activated them and retired the verified superseded stores.

```text
W/
  assets/
    media/
      workspace/
        img/  svg/  files/  html/
      collections/
        <collection-id>/
          img/  svg/  files/  html/
    works/
      primary/                       locally stored primary srcset images
      thumbs/                        locally stored Work thumbnails
      media/
        files/                       Catalogue Work downloads

  working/
    source/
      documents/…
      collections/…                  existing collection-owned sources
      media/
        build-source/…               editable build inputs such as Mermaid
    generated/
      documents/…
      collections/…                  document payloads and manifests
      search/…
      catalogue/
        media-config.json
        works/
          works_index.json
          index/<work_id>.json
        series/
          series_index.json
          index/<series_id>.json
        galleries/
          galleries_index.json
          index/<gallery_id>.json
        reports/…                    only when a later delivery adds a consumer

  preview/
    preview-manifest.json
    documents/…
    collections/…
    search/…
    catalogue/…                      declared JSON copied from Working
```

There is no persistent `working/generated/media/` or `preview/media/` byte store, including equivalent collection-owned stage media copies. Ready-to-read document media lives in `assets/media/`; Work derivatives and downloads live in `assets/works/`. Only editable build inputs remain under source media, including collection-owned build inputs where configured. Temporary converter/build files are allowed within their existing operation-owned boundaries; they are not another reader asset store.

Stage manifests record asset references in their existing document/build/snapshot structures, rather than retaining stage `media/` directories just to house a manifest. The design-time Catalogue copy inventory described below is separate from these generated reference lists. Existing JSON snapshot revisions and ordinary transfer comparison remain useful; they do not become an asset-version history or a requirement to invalidate Preview after an image edit.

The canonical Catalogue directory does not move into `working/source/catalogue/` in this delivery, and no empty placeholder there is required. Any later canonical relocation needs its own ownership and version-history decision. The `reports/` branch likewise illustrates an allowed future location, not a requirement to create unused report files now.

## Reader And Publication Contracts

### Stage-Owned JSON

Working Catalogue reads resolve from `W/working/generated/catalogue/`; Preview reads resolve from `W/preview/catalogue/`. Site Preview/public readers use their configured repository data paths. The active reader context selects the base. Catalogue JSON must not require embedded Working/Preview URLs or a request-time canonical-data join to choose its stage.

Preserve existing Work, Series and Gallery payload meaning and identity. Adapt media URL resolution where needed so local readers use local assets and public readers use the existing public destinations. This is not a redesign of the document schema, report search rules or collection policy.

Prepare Preview copies all by-ID JSON from explicitly declared Catalogue directories unchanged. It does not infer their selection from document titles or the set of prepared Catalogue documents. Root indexes, media policy and any report/system files come from an explicit design-time inventory. A suggested canonical configuration file is `docs-viewer/config/workspace/catalogue-artifacts.json`, with declared by-ID directory families and separately declared system/report files. The exact schema belongs to implementation, with one owner used by preparation and distribution.

The initial inventory covers current `works/index/`, `series/index/`, `galleries/index/`, their aggregate indexes and `media-config.json`. Details are retired: no Detail folder or index is a preparation, generation or publication input/output, including an empty index. Future report files must be explicitly classified; Manage-only report enrichment is excluded from the public preparation/distribution inventory. Other files are not copied merely because they happen to sit under the Catalogue root.

Publish takes Catalogue JSON from Preview, alongside the prepared document/Search output. It must not recopy live Working Catalogue files, regenerate from canonical records or adjust prepared media versions during distribution. Existing document eligibility and Search/Recents copy ownership remain unchanged.

### Shared Current Assets

Both local stages resolve asset identities into the same `W/assets/` tree. An image edit replaces the current local image; Preview deliberately displays that replacement. If Preview JSON is no longer suitable, the recovery is Prepare Preview again. Do not add historical asset copies, content-hash receipts, automatic invalidation or a hidden media-version store to make Preview immutable.

Local Studio and Docs Viewer must be able to read their managed images and downloads without R2. Missing required local files fail visibly. The old “retain a download by checking only its R2 object” path cannot remain the normal local-source contract after migration. Site Preview continues to read public asset URLs from R2, as the deployed site does. User-authored external links remain external links; this delivery concerns owned document/Catalogue assets.

Publish resolves the asset references of the prepared output to current local files and uploads the required assets to their configured remote destinations. It does not upload the entire asset directory indiscriminately. Catalogue references include media needed by the copied Work/Series/Gallery data, in addition to ordinary document media references. Required missing files are surfaced in its existing planning/error flow rather than fetched silently from R2.

Retain the existing configured public destination for each asset family. Repository-backed assets are copied alongside JSON where required; R2-backed assets are uploaded. The new shared local layout does not itself relocate public thumbnails or other public asset URLs.

R2 updates at Publish can affect the currently deployed site before Actions runs. That interval is accepted. Do not add tracking of whether Actions ran, deferred upload jobs or automatic deployment. Remote obsolete-asset deletion is left to a separate manual cleanup operation initially; no pending-deletion manifest is needed. Reconciliation of generated JSON remains distinct from deletion of shared asset bytes. Avoid automatic shared-asset cleanup as part of a routine Save or Publish.

### Saved Data And Document Freshness

The eventual Catalogue Works generated reader must show saved records after the owning mutation completes. Its generation may be independently callable for development or recovery, but a separate manual action cannot be its normal freshness boundary. That report-specific integration remains follow-on work.

Catalogue Regenerate currently updates document projections separately from Work Save. This delivery preserves that distinction while adapting the Catalogue input location. Later report/document work must decide which displayed values come from saved Work reader data and which belong to authored document content. Do not claim that moving files alone removes that possible metadata lag.

The CSI-5 Subject reader correction makes Working collection Subject labels and assignment choices read the current generated Catalogue targets through the existing stage-bound provider. Work Save already completes those Catalogue indexes. Reloading Works or reopening the Subject picker reads saved titles without rebuilding the private semantic-token lookup. This shared Subject UI also serves the Processing document collection. Exact Work/Series identities, document associations and link destinations retain their existing owners; authored document titles and Catalogue Regenerate remain separate.

## Delivery: Catalogue Storage And Publication Integration

Status: preparation, implementation and cutover through CSI-4 are complete; CSI-5 Save/Review verification is in progress. Current local Catalogue JSON, thumbnails, document/collection media, four Work PDFs and all 13,848 primary images are populated and verified. Details are retired and absent from the copy inventory. The maintained checkout serves the new roots and the captured Preview. This delivery precedes report conversion, report pagination/search changes and any editor integration; the approved CSI-5 correction only changes the existing Subject readers' data source. Its complete outcome is an operational local asset store and stage-owned Catalogue JSON, with Studio producing Working output and Publish distributing Preview JSON plus referenced current assets.

### Requirements

- Route generated Catalogue output into `W/working/generated/catalogue/`, retaining current canonical source and private Studio lookup ownership.
- Store all owned ready-to-read media once under `W/assets/`, with separate workspace-wide, collection and Catalogue Work identities. Remove persistent stage media byte copies from the active layout.
- Keep local image preparation, dimensions and media-version changes in Studio. Complete that work before emitting the Working JSON that Preview may capture. Save no longer uploads to, verifies against or deletes from R2 for ordinary Catalogue output completion.
- Create and populate the agreed target folders before coding. Copy current generated Catalogue JSON, local thumbnails, reader-ready document media and available Work downloads while retaining the active locations. Obtain primary srcset images through user-operated Cyberduck batch downloads from R2 and complete any other required remote-only file population. Verify these targets before implementing the new readers/producers. Full catalogue regeneration is not part of the selected migration approach.
- Prepare Preview copies declared Catalogue JSON from Working and records its asset references. Both local stages read current assets; image edits do not automatically invalidate Preview.
- Publish copies prepared JSON to repository destinations and uploads referenced current assets without mutating canonical data, captured JSON or media versions. It does not automatically delete remote assets.
- Rename the current user-facing Deploy Repo action to Publish; retain Deploy for GitHub Actions. Do not combine Git commit/push or Actions execution into Publish.
- Preserve report list/search/sort/copy behavior, existing Work/Series/Gallery semantics, document identity, eligibility, Search/Recents ownership and public destination policy. Shared reader/path adaptations are in scope.
- Remove superseded active path branches on cutover. No compatibility aliases, old-root fallbacks, duplicate canonical records or long-lived second asset store.

### Deliverables And Suggested Owners

These identify the current owners and suggested new configuration, not a frozen patch inventory. Implementation should keep path policy in the owning configuration/resolvers and domain work out of route controllers.

| Deliverable | Suggested owner / target |
| --- | --- |
| Explicit Docs-workspace Catalogue output and shared-asset paths | [Workspace config](../../docs-viewer/config/workspace/docs-workspace.json), [workspace resolution](../../docs-viewer/services/docs_workspace_config.py), [Catalogue output paths](../../studio/services/catalogue/catalogue_output_paths.py), [Catalogue media paths](../../studio/shared/python/catalogue_media_paths.py). |
| Design-time Catalogue copy inventory | New suggested `docs-viewer/config/workspace/catalogue-artifacts.json`; one shared interpretation for declared by-ID families and system/report files. |
| Working Catalogue generation with locally owned media completion | [Output completion](../../studio/services/catalogue/catalogue_output_service.py), [media completion](../../studio/services/catalogue/catalogue_output_media.py), [local preparation](../../studio/services/catalogue/catalogue_build_media.py), [media version owner](../../studio/services/catalogue/catalogue_media_version.py), [Catalogue generator](../../studio/services/catalogue/generate_work_pages.py). |
| Document media writes/builds use shared assets | [Media storage](../../docs-viewer/services/docs_media_storage.py), [media import](../../docs-viewer/services/docs_import_media.py), [Mermaid generation](../../docs-viewer/services/docs_mermaid_media.py), and their existing build/collection owners. Source build inputs remain separate. |
| Local stage-aware Catalogue reads and shared asset resolution | [Catalogue reads](../../docs-viewer/services/docs_catalogue_media.py), [Preview reads](../../docs-viewer/services/docs_preview_reads.py), [workspace provider](../../docs-viewer/runtime/js/shared/docs-viewer-workspace-provider.js), [Catalogue media policy](../../docs-viewer/runtime/js/shared/docs-viewer-catalogue-media-policy.js), and the existing confined Studio/Docs static-serving owners. |
| Preview JSON capture and asset-reference manifests | [Preparation](../../docs-viewer/services/docs_prepare_preview.py), [snapshot owner](../../docs-viewer/services/docs_preview_snapshot.py), and existing Working build/collection manifests. Remove asset bytes from snapshot ownership while retaining prepared JSON validation. |
| Publish prepared files and current referenced assets | [Current distribution owner](../../docs-viewer/services/docs_deploy_repo.py), [public media reconciliation](../../docs-viewer/services/docs_public_media_reconciliation.py), [R2 transport](../../studio/services/media/publish_media_to_r2.py). Preserve useful transfer comparison; remove upload-driven Catalogue version promotion from this flow and automatic asset deletion. |
| Publish naming and shared/public code projection | [Management controls](../../docs-viewer/runtime/js/management/docs-viewer-management-hosted-views.js) and related existing labels; [site code inventory](../../site-tools/config/site-code-update.json) for every affected shared/public asset. |
| Explicit migration and cutover record | A reviewed source-to-destination inventory for current generated Catalogue files, document media, thumbnails, primary variants and downloads; completed audit, user Cyberduck download gate, transfer verification, outstanding failures and final active roots. Record evidence here during delivery, not a permanent background migration subsystem. |
| Maintained contract updates at closeout | [Catalogue Build And Lookup Refresh](d-20260519-202931-0c703e.md), relevant Docs workspace/media/publication owners, [Development Checklist](d-20260523-190651-7157ec.md), and the [Save action map](d-20260925-144813-17e45c.md). Update observed behavior when implementation changes, not in advance. |

### Migration And Cutover

| Material before migration | Agreed destination / treatment, completed at CSI-4 |
| --- | --- |
| `$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/generated/` reader JSON | Copy into `W/working/generated/catalogue/`, retaining current filenames and JSON meaning. Copy thumbnails separately into assets; retain the active source locations until cutover. |
| Existing local Work thumbnails | Copy into `W/assets/works/thumbs/` before coding. |
| Primary Work srcset variants currently in R2 | The user downloads the contents of `works/img/` in batches using Cyberduck into `W/assets/works/primary/`, preserving the existing filenames directly in that directory. Do not introduce an extra `works/img/` nesting level. Complete the user download gate and local verification below before cutover. |
| Catalogue Work downloads | Copy available files into `W/assets/works/media/files/` and obtain required remote-only files before coding. Confirm this separate file set during preparation; it was not included in the primary-image audit. |
| Ordinary document reader-ready media | Copy into `W/assets/media/workspace/<type>/` before coding. |
| Collection-owned reader-ready media | Copy into `W/assets/media/collections/<collection-id>/<type>/` before coding, preserving the exact owner and relative identity. |
| Editable document media build inputs | Retain under the existing ordinary/collection source build-input boundary. Do not retain another ready-to-read image copy there. |
| Existing Preview | Prepare a new Preview under the new layout after Working output and assets are complete. It contains JSON and asset references, with no stage-owned media copies. |
| Canonical Catalogue, private Studio lookups and original Work sources | Remain at their current locations. |

The completed primary-image transfer is 13,848 files totalling 1,726,038,312 bytes (1.73 GB, approximately 1.61 GiB). The user downloaded the contents of `works/img/` directly into `W/assets/works/primary/`, without another nesting level. CSI-0c verified every filename, size and MD5 against both the saved inventory and a fresh R2 metadata listing; there were no missing, additional or mismatched images. The empty remote folder marker is not a local asset. A migration download does not introduce an R2 fallback into normal local reads.

Relocation and downloading preserve existing media versions. If a particular image later needs regeneration, treat that as an explicit exception and use Studio's image-change rule rather than the fact of copying/uploading to decide whether a version changes. Missing source or remote files remain explicit migration failures; do not substitute another Work's image or infer identity from titles.

The agreed sequence was to confirm the destination layout, create the required target directories in the existing configured workspace, copy the current local files and complete the downloads, then verify the populated targets before coding. The existing configured locations and Preview stayed active during preparation. CSI-4 ended that temporary duplication after verification; it did not create a second maintained store, runtime fallback, unused report/reference folder or replacement workspace root.

CSI-4 stopped the old local services, checked the populated targets against current generated JSON/media and fresh R2 metadata, then activated the implementation and restarted the services. No content had changed since preparation; only the pending media-policy projection and document URLs required their owning builders. Old and new producers did not run concurrently. Ordinary source-control recovery and the inspected migration inventory remain the recovery basis; no automatic backup tree, shadow writer or compatibility path was introduced.

Retire the obsolete active generated/media locations after verifying migration, within the separately authorized migration write set. Do not delete originals, unrelated Projects data or other workspace directories. A partial population leaves cutover incomplete; correct the failed items and repeat the affected migration step rather than activating readers with a silent remote fallback.

### Completed Primary-Image Audit And User Download Gate

The two preliminary checks completed on 2026-09-25 without a concerning result:

| Check | Recorded result | Cost and evidence limit |
| --- | --- | --- |
| R2 inventory against current canonical Works and configured primary widths | All 13,848 expected variants for 4,616 Works are present, with no empty expected images. The only additional object is the zero-byte `works/img/` folder marker. Total image bytes: 1,726,038,312. | Approximately 10 seconds; object metadata only, with no image-body downloads. Establishes remote completeness, not freshness against every original. |
| Regenerate a bounded sample with the existing converter and current configuration | All three variants for Works `00001`, `00008`, `00454`, `01161`, `02316`, `03470`, `04619` and `04623` matched R2 exactly by size and MD5. Includes the only Work at media version 2, older/middle/recent IDs and a source smaller than the largest variant. | Approximately 41 seconds; 24 isolated local derivatives totalling 2,534,958 bytes. Demonstrates reproduction for these eight Works, not the whole catalogue. No uploads or canonical changes. |

Local audit artifacts: [summary](../../var/catalogue-r2-audit/20260925-182104/README.md), [full R2 inventory](../../var/catalogue-r2-audit/20260925-182104/inventory.json) and [sample comparison](../../var/catalogue-r2-audit/20260925-182104/sample.json). These are ignored local evidence files; the durable outcome is recorded above. The audit covers active Work primary images only, excluding archives, legacy Detail media, thumbnails and downloadable files.

- [x] Complete the R2 inventory and bounded regeneration comparison; select Cyberduck batch downloads as the migration approach.
- [x] **User download gate — CSI-0b:** the user reported a successful R2 download. Inspection confirmed the actual destination `W/assets/works/primary/`, with all expected images directly inside it.
- [x] **Populated-target verification — CSI-0c:** all 13,848 primary names, sizes and MD5 values match the saved inventory and fresh R2 metadata; canonical hashes are unchanged. Every local copy still matches its source, and all four Work PDFs match fresh R2 size/MD5 metadata. Verification took approximately 20 seconds and found no transfer gaps. No image download, regeneration or runtime asset manifest was added.

The primary-image download gate does not establish completeness of Work downloads or other asset families; those remain part of the migration inventory and cutover checks.

### Process After Delivery

1. Save Catalogue edits in Studio. Canonical edits, required local media preparation, Studio-owned versions, Working Catalogue JSON and existing private lookups complete through their owners. Normal local consumption requires no R2 access.
2. Read Working documents, media and current reports. Shared readers resolve current Working Catalogue JSON and assets from the configured workspace. Report UI behavior remains as before this delivery.
3. Prepare Preview. Capture eligible document output and the declared Catalogue JSON set, retaining their asset references and copying no media bytes.
4. Review Preview. It uses captured JSON and the same current assets as Working. If a later image edit makes the JSON unsuitable, prepare Preview again.
5. Publish. Review the existing distribution plan, copy prepared JSON to repository destinations and upload the required current assets to R2. Do not regenerate Catalogue data, increment media versions or delete obsolete remote objects.
6. Commit and push explicitly, then run Deploy through GitHub Actions. Site Preview reads repository output and R2, exposing the same public paths. The timing gap is accepted.
7. Run separate manual asset cleanup when wanted. No routine authoring or publication operation waits for it.

### Delivery Steps

- [x] **CSI-0 — Readiness:** confirmed the existing Save/media/version, workspace, Preview and distribution owners against the agreed scope. Selected `assets/media/workspace/`, exact collection namespaces and the specified Catalogue/Work paths. The user's instruction to proceed authorizes the bounded delivery; primary transfer confirmation, populated-target verification and explicit live publication remain separate gates. Preparation evidence is limited to inventories, direct byte comparisons and remote size/MD5 checks, with no new tests or runtime patch.
- [x] **CSI-0a — Create folders and copy local files:** created the required targets in the existing configured workspace. Copied 5,061 Catalogue JSON files, 4,616 Work thumbnails and 460 document/collection media files, totalling 70,833,684 bytes, with byte equality verified. Populated all four required Work PDFs from their exact R2 keys, totalling 12,927,572 bytes, and verified size and MD5. Handed over `W/assets/works/primary/` for Cyberduck. Existing active files and configuration remained intact. The deliberately deleted empty Detail index is retired, not a missing migration input.
- [x] **CSI-0b — User download:** the user completed the primary-image transfer into `W/assets/works/primary/`; inspection confirmed the destination and all expected files. The four separately populated Work PDFs are also complete. No agent-run bulk primary download or full regeneration substituted for the user's transfer.
- [x] **CSI-0c — Verify populated targets:** verified all required primary filenames, sizes and MD5 against the saved audit and fresh R2 metadata. Canonical membership is unchanged; all copied JSON/media still match their sources, and all four Work PDFs match current remote metadata. No missing, unexpected, partial or mismatched files remain. The data-preparation gate is cleared for CSI-1–3; live roots have not switched and CSI-4 still requires a final refresh.
- [x] **CSI-1 — Configuration and contracts:** implemented mandatory `docs_workspace_v4` asset/Catalogue settings, typed destination resolution and the shared design-time copy inventory. Direct inventory inspection read all 5,061 prepared JSON files unchanged and resolved all 24 document-media directories. The ownership contract below fixes the reference and public-destination responsibilities for CSI-2/3. Gate met for these contracts: one explicit destination per material, no obsolete-root fallback, no asset history/invalidation and no canonical relocation. Existing producers and lifecycle flows have not adopted them yet.
- [x] **CSI-2 — Local producers and Save:** Studio generates Working Catalogue JSON in the Docs workspace and prepares primary images, thumbnails and downloads into shared assets. Complete prepared image sets and their dimensions/versions use the existing Catalogue transaction owner before JSON generation. Byte differences in existing renditions advance the version; unchanged bytes, missing-rendition repair and transfers do not. Save has no R2 client, verification, upload or deletion path; uploads no longer finalize canonical versions. Document import/build/read owners use one `asset_location`, with no source/generated/Preview media aliases or routine stale-asset deletion. Non-media Save completion and truthful saved/output reporting remain intact. Implementation gate reviewed with lint, source inspection and dry runs; changed-image writes and failure recovery remain unexercised CSI-5 evidence.
- [x] **CSI-3 — Readers, Preview and Publish:** Catalogue APIs require an explicit stage, and local Studio/Docs readers use the configured shared asset URL. Preview captures the declared Catalogue JSON byte-for-byte and records combined asset identities in its completion manifest. The existing distribution owner publishes Preview JSON and referenced current assets to the existing repository/R2 destinations, without source rebuilds, version mutation or automatic asset deletion. Publish naming and the shared/public code projection are updated. Implementation gate reviewed with focused static checks and read-only plans; real Preview replacement, transfers and presentation remain CSI-4/5 evidence.
- [x] **CSI-4 — Final refresh and cutover:** stopped the old services, verified all populated targets against current local sources and fresh R2 metadata, and integrated CSI-1–3 into the maintained serving checkout. Refreshed the pending Catalogue media policy and Working document URL projections, prepared a fresh Preview, retired verified superseded generated/media stores and restarted local services. All 18,526 prepared asset references resolve; Catalogue/Search/Recents bytes match Working. Canonical records, project originals and editable build inputs remain intact. Direct local image/download reads passed and the retired stage-media route returns 404. No R2 fallback or missing migration input remains in the selected local reader path.
- [ ] **CSI-5 — Focused verification and user review:** run the agreed static checks, direct inventory/payload checks and representative local Save → Prepare Preview → Review checks. Inspect a Publish plan and select any explicit live publication check with its concrete write set. Verify Site Preview's public paths separately from local application paths. Gate: evidence demonstrates the selected boundaries and records any unexercised transfer/error cases; manual presentation review is accepted without a new browser harness.
- [ ] **CSI-6 — Code review:** review ownership, stage resolution, copy inventory, media-version timing, missing-file behavior, removed R2/old-path dependencies, migration completeness and publication side effects. Confirm no report feature work, duplicate reader implementation or automatic cleanup entered the delivery. Resolve findings and repeat only affected checks. Gate: the final implementation matches the agreed shared-current-assets model and recorded evidence.
- [ ] **CSI-7 — Closeout:** update durable storage/media/publication documentation and the Save action map, record actual target files, migration outcome, verification and remaining limits. Reconcile the parent report proposal so its next delivery starts from this storage foundation. Gate: the implemented storage/publication outcome is complete, the next report/editor work remains separate, and useful planning material has an explicit retain/retire recommendation.

Preparation record: CSI-0–0c completed on 2026-09-25. The preparation [handoff and evidence](../../var/catalogue-storage-preparation/20260925-csi-0a/README.md) records the exact copy inventory and [successful target verification](../../var/catalogue-storage-preparation/20260925-csi-0a/target-verification.json). The user corrected the stale Detail-index requirement. Active generation, source reads and media owners no longer probe/recreate Detail storage or expose Detail media destinations. Historical conversion tooling and the frozen public archive retain separate ownership; no historical media or migration data was deleted. That correction left Work/Series/Gallery record meaning and then-current media-version timing unchanged; CSI-2 subsequently changed version ownership as specified above.

Correction review: explicit-path lint passed for all 11 changed Python files; both changed JSON configs parsed; site validation passed. The existing `catalogue_json_build.py` dry run found all 4,616 Work thumbnails and planned only the media-policy refresh, with no Detail artifact or deletion. Scoped source/diff review confirmed active Detail output/media branches were removed and the retained one-time conversion no longer depends on active Detail configuration. This checkpoint provided no Save, image generation, live write, publication or browser evidence; tests/fixtures were not changed. CSI-4 later applied the pending media-policy refresh through its owning generator.

CSI-1 review: the isolated `catalogue-storage` checkout contains the workspace schema/loader change, `catalogue-artifacts.json` and `docs_catalogue_artifacts.py`. The copy reader requires an explicit stage, reads only declared direct by-ID JSON and required system files, preserves their original bytes, rejects missing/invalid selected artifacts and cannot follow symlinks or fall back to another root. Unknown files elsewhere are not selected. The real Working inventory returned 4,616 Work, 139 Series and 302 Gallery records plus four system files: 5,061 files / 5,158,497 bytes. Both configs parsed and explicit-path Ruff plus `git diff --check` passed. The worktree has no installed lint environment, so the identical pinned Ruff executable from the serving checkout was used with this checkout's config and `--no-cache`. No tests or fixtures were changed; no Preview capture, Save, upload, browser or live-cutover evidence is claimed.

CSI-2 review: explicit-path Ruff and whitespace checks passed. The existing complete Catalogue JSON dry run found all 4,616 shared thumbnails, planned only the already-pending media-policy refresh and no deletions. A read-only media completion plan for Work `00008`, which has a local download, found no pending image or download changes. The inventory still reads all 5,061 Catalogue JSON files. Both stages resolve the same 24 document-media directories. Source review confirmed no normal Save R2 dependency, upload-owned version promotion, generator thumbnail cleanup, stage-media location fields or source-to-generated media copy loop. One diagnostic initially imported a nonexistent inventory helper; that unused import was removed and the path/inventory diagnostic passed. No tests, fixtures, source/generated data, media bytes or remote objects were written for verification.

CSI-2 also adapts direct thumbnail reads to the shared store, excludes Studio-owned Catalogue JSON from the Docs Build completion inventory, and removes Preview's document-media byte copy loop. These are required consequences of producer ownership, not completed CSI-3 lifecycle integration. Build and snapshot result structures now report document `asset_references`; CSI-3 subsequently completed persistence of the combined document/Catalogue reference set, explicit temporary-build access to shared assets, stage-aware Catalogue reads and Publish adoption. Editable build inputs and private media-source provenance retain their source owners.

CSI-3 review: explicit-path Ruff and ESLint passed; `bin/site-code-update` changed only the shared workspace provider projection, and `bin/site-code-update --check` plus `bin/site-validate` passed. Read-only inspection selected 5,061 Catalogue JSON files and resolved all 18,468 required assets: 13,848 primaries, 4,616 thumbnails and four downloads. The existing Prepare Preview planner selected 4,935 documents and 5,061 Catalogue JSON files, excluding 278 documents under existing eligibility policy. Local primary/download routes resolve to shared files, and both stages project the same document-media URLs. No tests, fixtures, live content, asset bytes or remote objects were written for verification.

CSI-4 review: the final frozen inventory found no changed copy inputs, canonical changes, conflicts or unowned Catalogue files. All 13,848 local primary images still match fresh R2 size/MD5 metadata, as do the four PDFs. The maintained checkout received the exact CSI-1–3 changes; its Catalogue generator wrote only `media-config.json`. Existing document builders reconciled all six owners with media and Recents generation skipped: 251 document payloads changed, with no warnings, removals or index/manifest changes. Search was not rebuilt. Verified obsolete Projects Catalogue output and Working source/generated reader-media copies were retired; source build inputs and originals were retained. A further 21 staged primary derivatives were byte-equal to shared assets and retired, retaining their seven staged input images.

The fresh Preview contains 4,935 documents, 5,061 Catalogue JSON files and 18,526 available asset references. Catalogue, Search and Recents match Working byte-for-byte; no retired stage-media roots remain. The restarted Studio, Docs and Site Preview services respond successfully. Working/Preview Catalogue reads and local primary/PDF requests passed; the retired stage-media route returns 404. `bin/site-code-update --check`, `bin/site-validate` and `git diff --check` passed. These are migration, builder and direct-read results, not live Save, Publish transfer, browser presentation or rollback evidence. R2 access during cutover was metadata-only; tests and fixtures were not changed.

CSI-5 evidence so far: the user saved Work `00008` from `nerve` to `nerve new title`. Read-only inspection confirmed the canonical record, Working by-ID record and Working aggregate index contain the saved title, while Preview still contains `nerve`. The separate private semantic-token lookup contained `nerve test`, explaining the stale Works Subject label. The approved correction routes the Subject column and picker through the existing generated Catalogue reader instead of adding another Save rebuild. `bin/lint-js` passed for the four changed management JavaScript modules and `git diff --check` passed. A direct read of `GET /docs/catalogue-media-targets?stage=working` returned the exact Work `00008` target with `nerve new title`. User confirmation of the column and picker is pending; this evidence does not establish image preparation, later Preview replacement, publication or failure recovery.

Subject reader correction review: traced the mounted collection provider through both consumers and checked the final diff. Subject selection remains limited to exact Work/Series identities; removed the old image/link requirements because the generated target response supplies subject identity and title independently of media presentation and document navigation. The reader has no private lookup fallback or Save change. All four changed runtime files are management-only and outside the public projection inventory. No tests or fixtures were created, changed or run. This is the bounded correction's review, not completion of the wider CSI-6 review.

Next gate: continue CSI-5 focused Save/Review verification, with exact representative Save records and any live Publish write set selected before those writes. The maintained checkout is active with uncommitted changes; the isolated checkout is an earlier CSI-3 checkpoint. No commit, push or deployment has run in this correction. The local [CSI-4 cutover handoff](../../var/catalogue-storage-implementation/CSI-4-handoff.md) records cutover roots, services, commands, evidence and remaining work. [CSI-3](../../var/catalogue-storage-implementation/CSI-3-handoff.md), [CSI-2](../../var/catalogue-storage-implementation/CSI-2-handoff.md) and [CSI-1](../../var/catalogue-storage-implementation/CSI-1-handoff.md) remain earlier implementation evidence.

### CSI-1 Ownership Contract

`docs-workspace.json` is the sole configured owner of the shared asset root/family paths, local asset URL prefix, Working/Preview Catalogue roots and repository Catalogue destination. `DocsWorkspaceConfig.assets` and `.catalogue` resolve them through the existing required Docs root. The config loader creates no directories; absent settings or an unavailable root fail. Collection media selection requires an exact registered collection ID; `None` explicitly selects the ordinary workspace namespace. CSI-2 removed the old source/generated/Preview media fields; each managed media type has one `asset_location`, and its owner records the shared `asset_root` for relative references.

| Material | Writer and local owner | Capture/distribution owner |
| --- | --- | --- |
| Work, Series and Gallery by-ID JSON; aggregate indexes; media policy | Studio generation writes `catalogue.working`. Canonical records and private lookup outputs keep their existing owners. | `docs_catalogue_artifacts.py` reads the one design-time inventory for either explicit stage. Prepare Preview copies Working bytes into `catalogue.preview`; Publish reads those Preview bytes and writes `catalogue.public_projection`. |
| Ordinary/collection reader media | Document import/storage and registered producers write the configured document-media family under `assets`, selected by exact collection and media type. Editable build inputs retain their source owner. | Existing document build/snapshot structures record asset references. Public distribution uses the existing configured document/collection public bindings. |
| Work primary variants, thumbnails and downloads | Studio local media completion writes `assets.work_primary`, `.work_thumbnails` and `.work_files`, before Working JSON generation. | Preview's references come from the captured Catalogue records and media policy. Publish reads current local bytes and preserves the existing public destination for each family, including repository-backed thumbnails. |

The reference representation is `asset_references`, a sorted unique list of exact POSIX file identities relative to the configured asset root, such as `media/workspace/img/example.webp` or `works/primary/00008-primary-800.webp`. It belongs in the existing build/snapshot structures, not in a separate asset manifest or stage media folder. `DocsAssetsConfig.resolve_reference` confines an identity to a configured asset family without a stage, hash, version or file creation. CSI-2 build/snapshot results collect document references. CSI-3 combines them with Catalogue references in `docs_preview_manifest_v2`. The existing Preview revision binds captured JSON plus reference identities, with no asset-byte hashes. Publish resolves that captured set against current shared files and rejects a plan with missing-file or destination errors before repository writes. Ordinary R2 transfer comparison uses current size/ETag metadata; it creates no asset history or runtime hash manifest.

Local readers select Catalogue JSON by explicit stage and resolve asset URLs against `assets.served_path_prefix`. Public readers retain their public route/media settings. Copying JSON never rewrites its public media policy to a machine path or local URL. Preparing Preview binds JSON bytes into its ordinary revision; shared asset bytes remain current and do not enter an asset-history or Preview-invalidation mechanism.

### Proportionate Verification Budget

| Risk / boundary | Proposed evidence and cost | Effects and limits |
| --- | --- | --- |
| Changed code/config or public projection is inconsistent | Explicit-path Python/JS lint, JSON parsing and `git diff --check`; where shared/public files change, `bin/site-code-update`, its check and `bin/site-validate`. Expected low local cost, one focused pass after the relevant edits. | Static evidence and tracked runtime projection only. No claim of live publication or media completeness. |
| Wrong/missing local files or stage-copy inputs | Read the declared inventory and inspect exact file identities, counts, required variants and local availability; compare copied JSON with its source. Cost scales with the file set and is unmeasured until inventory. | Read-only inspection apart from the separately authorized migration. No new asset-hash manifest or Preview invalidation is introduced. |
| Save still depends on R2 or advances versions on transfer | Source review plus a small agreed representative local Save selection covering unchanged and changed image behavior and current downloads. Select any existing automated check only after inspecting its coverage; otherwise use direct operation/manual evidence. | Save changes its selected canonical/generated files. Agree the exact records and write scope during implementation. New/changed tests or temporary regression scripts require a separate test specification. |
| Shared assets and captured JSON behave differently from the agreed model | User manual review of Working/Preview stage selection, local image display and a representative image update followed by preparing Preview again. | Demonstrates intended mutable shared media and explicit re-preparation. No automated browser choreography or assertion that Preview media is immutable. |
| Publish mutates source or copies the wrong stage | Inspect its concrete plan, file origins, destination set and version-preserving transfer path; an explicit live check, if selected, uses that reviewed write set. | Live Publish writes repository output and R2 and may change live images before Actions. A plan alone does not prove successful uploads. No automatic deletion, commit/push or Actions execution. |
| Bulk migration is incomplete or unexpectedly expensive | Preliminary inventory and sample comparison completed as recorded above. The user downloads 13,848 primary files / 1.73 GB in Cyberduck batches; transfer time remains dependent on the connection. After confirmation, verify filenames, sizes and MD5 locally against the inventory, reading approximately 1.73 GB without another image download or regeneration. Inventory the other asset families separately. | The user owns the bulk primary transfer. CSI-0b waits for confirmation; CSI-0c requires verified populated targets before coding. CSI-4 refreshes any subsequent changes before cutover. Refresh metadata if the remote/current required set changed. The audit does not prove every source is unchanged or cover downloadable files. |

Tests are a separately specified delivery under [Test Contract Discipline](d-20260627-212121-7cf7de.md). This plan does not authorize new tests, fixtures, harnesses or profile changes. Record chosen existing selectors and actual evidence limits during implementation. Reuse the normal publication plan/apply checks without expanding them into retained asset history.

### Follow-on

After this delivery, convert Catalogue Works to generated reader files with saved-data freshness, then introduce descending Work ID order, 20-row pagination and the agreed search improvements. Apply the same front-end/data-owner approach to other reports and collections without assuming identical record membership.

Retire old Catalogue lookups in a separate follow-on: inventory the remaining consumers of `docs-viewer/data/generated/semantic-tokens/target-lookup.json` and `studio/data/generated/catalogue-lookup/`, then move each to its appropriate generated Catalogue reader or explicitly owned report projection. The Subject column and picker have moved in CSI-5; remaining consumers, including Series image build-time resolution and Studio readers, need their own bounded migration. Remove each superseded lookup, producer, loader, configuration reference and refresh instruction once its last consumer has moved. Preserve exact identities and required private/public boundaries, with no compatibility reads or new Save dependency. Any affected tests or fixtures need a separately agreed test specification.

Moving the Work editor into Docs Viewer, moving canonical Catalogue data into the Docs workspace, changing Catalogue document regeneration, and redesigning further Save costs remain separately scoped decisions. Report-specific indexes remain acceptable. Automatic GitHub Actions deployment on commit is a possible later workflow change; deferring R2 upload until Actions is not required by the current delivery. Manual obsolete-asset cleanup may be developed separately when needed.

## Evidence And Planning Boundaries

The plan is grounded in the current [workspace configuration](../../docs-viewer/config/workspace/docs-workspace.json), [Catalogue output/media owners](../../studio/services/catalogue/catalogue_output_service.py), [media version owner](../../studio/services/catalogue/catalogue_media_version.py), [Preview snapshot owner](../../docs-viewer/services/docs_preview_snapshot.py), the Save action map and the user's explicit resolutions consolidated above. Those resolutions intentionally replace the earlier suggestions for retained asset versions, asset-hash invalidation and deployment tracking.

This document follows the [Planned Features delivery structure](d-20260428-000000-f5ff18.md) and [Development Checklist](d-20260523-190651-7157ec.md). Current runtime evidence and agreed target behavior remain distinct until the implementation and migration have completed.
