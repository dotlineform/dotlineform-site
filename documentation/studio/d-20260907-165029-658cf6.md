---
draft: false
doc_id: d-20260907-165029-658cf6
title: Restore Moments In Analysis
added_date: "2026-09-07 16:50:29"
last_updated: "2026-09-07 18:22:22"
summary: Restore 56 Moments documents into an Analysis Working sub-scope, retaining their identities, R2 images and poetry presentation.
ui_status: done
parent_id: d-20260908-171728-3c539a
---
# Restore Moments In Analysis

## Outcome

Restore Moments into `analysis / working / moments`, hosted by an ordinary **Moments** document. Preserve all 56 document IDs, titles, dates, text and intentional line breaks. The collection becomes editable through the existing sub-scope workflow.

Keep the 36 images at their existing R2 object keys under `docs/moments/`. Resolve image references through the supported public media URL without moving or uploading the objects. Restore poetry typography and the axis-aligned layout in *all the little cracks*, confined to Moments content.

## Sources And Boundary

- Markdown: `$DOTLINEFORM_PROJECTS_BASE_DIR/moments/moments - docs-viewer source/source/documents/`.
- Presentation reference: `$DOTLINEFORM_PROJECTS_BASE_DIR/docs-export/moments - 2026-08-09/`; the original stylesheet is also recoverable from `db0deb472^:site/docs-viewer/static/css/docs-viewer-moments.css`.
- Rebuild from source using the current Analysis contract. Preserve the backup and exported snapshot.
- This delivery ends with a working local collection. Pre-publish promotion, Publish, Deploy and book/EPUB/PDF editions remain separate. The former Moments scope and public route remain retired.

## Delivery

### MOM-0 — Readiness

- [x] Inspect the backup, current Analysis stages and presentation references: 56 unique IDs, no Analysis collisions, 36 image references and preserved styling.
- [x] Confirm the existing host/sub-scope, media-resolution and content-styling owners before editing them.

Creation now binds its source/config writes and rebuilds to the explicit Working stage and rejects Pre-publish authoring. Poetry styles use the existing source classes, without restoring a scope-specific route. The user confirmed retaining the existing R2 images for this restore. A possible Moment subject type remains deferred until after restoration.

### Separate Follow-up

Docs **Add image** still loads an unselected Analysis configuration and fails with `stage is required for scope 'analysis'`. Bind that workflow to the explicit authoring stage in a separate slice. Current Analysis image projection targets `site/`; downloadable files target R2. Decide whether to change the image destination before relocating Moments media.

| Contract or risk | Focused evidence | Permanent change |
| --- | --- | --- |
| Creation writes the wrong stage or changes accepted content | Service tests for exact Working paths/rebuilds and rejected missing/Pre-publish stages | Extend existing stage tests |
| Restored identity, text, media or layout changes | Source transformation hashes, collection payloads, image checks and ordinary/axis manual inspection | None; one-off restoration evidence |
| Shared styling reaches the public runtime correctly | Explicit site-code projection/check and site validation | Existing checks |

### MOM-1 — Restore And Verify

- [x] Create the Moments host and Working sub-scope through the existing lifecycle; restore documents with exact identities.
- [x] Resolve the existing R2 images and adapt the preserved poetry styling to the current viewer.
- [x] Build the collection and verify document count, metadata, content preservation and image resolution; inspect ordinary and axis-aligned poetry and project the shared CSS.

[Moments in Working](/docs/?scope=analysis&stage=working&doc=d-20260907-180859-c425b2) contains all 56 documents. The source files match the backup byte-for-byte except for the 36 explicit R2 URL expansions. Generated poem markup is preserved for all 56; all 36 image URLs returned HTTP 200. Hash checks confirm 847 backup, exported-edition and Pre-publish files are unchanged.

Creation rebuilt the empty collection and targeted host; the watcher rebuilt the restored documents. No Search rebuild, media upload, Publish or Deploy ran. Evidence is retained in `var/moments-restore/`.

- `test_docs_workflow_stages.py` and `test_docs_scope_lifecycle.py`: **60 passed**.
- Explicit Python lint for the changed service and stage tests: **passed**.
- Site-code projection changes only the shared stylesheet; projection check, site validation and `git diff --check`: **passed**.
- Live viewer inspection: *we are the vessel* shows its R2 image and ordinary verse; *all the little cracks* retains its word axis; Subdoc Source opens the exact poem; returning to the 56-document list restores normal width. Public/mobile presentation remains a later publication review.

### MOM-2 — Code Review

- [x] Review source/config/runtime changes and selected evidence for identity loss, broken media, styling leakage, duplicate ownership and compatibility aliases; resolve findings and rerun affected checks.

Review corrected axis-word overlap when the column is narrow and prevented hidden detail markup from narrowing the collection list. Live inspection confirms both corrections. Configuration changes are confined to Working; no namespace alias or replacement media owner was introduced. [Source Organisation](/docs/?scope=studio&doc=d-20260423-000000-8fd731) records stage-bound creation and the separate Add image gap.

### MOM-3 — Acceptance And Closeout

- [x] User accepts the restored collection and both poetry layouts; update the durable owner and record completion. Retain this delivery for manual archiving.

Closed on 7 September 2026 following user acceptance. Implementation and review evidence above remains the accepted baseline; this closeout changes delivery status only. The Add image stage issue and any future media relocation remain separate follow-up work.
