---
doc_id: d-20260715-225512-80198b
title: Recent Documents Delivery
added_date: 2026-07-15 22:55:12
last_updated: 2026-07-16
ui_status: proposed
summary: Make last_updated trustworthy and serve one route-appropriate Recent view without an Added/Edited choice.
parent_id: d-20260715-222129-8253ee
viewable: true
---
# Recent Documents Delivery

## Outcome

Docs Viewer can answer the route-appropriate Recent question accurately: “what is new?” for a catalogue-like collection and “what changed?” for a changing document collection.

One complete delivery makes `last_updated` a consistently maintained full timestamp for future edits, seeds only the bounded historical set needed to populate current edited projections, and replaces the current Recently Added-specific contract with one generic Recent contract. Public Moments receives an added-date projection. Public Library and Analysis, plus every scope displayed in `/docs/`, receive an edited-date projection. There is no Added/Edited toggle or second Recent control.

## Timestamp Contract

- New and changed `last_updated` values use the same `YYYY-MM-DD HH:MM:SS` local timestamp contract as `added_date`.
- Creating a document sets `added_date` and `last_updated` to the same timestamp.
- Every successful supported write that changes canonical Markdown content or front matter advances `last_updated`.
- Source-editor rebuild, metadata and placement changes, imports/overwrites, lifecycle-owned rewrites, and other focused mutation paths use one timestamp helper rather than maintaining the field independently.
- A no-op request, generated-output rebuild, search rebuild, read, preview, or failed mutation does not change it.
- Direct source edits through VS Code, Codex, or another editor must advance the field; watcher/check evidence should surface changed source whose timestamp did not advance rather than silently rewriting source.
- A one-time Git-history seed walks backward until each edited Recent projection has enough eligible documents to fill its configured limit, then writes the last author timestamp only to the union of those documents.
- The seed applies context eligibility before choosing its bounded set. With the current limit of 10, one manage and one public edited projection can require at most 20 distinct seeded documents per scope; the history scan may inspect more commits to find them. Public Moments needs no added-date seed, while editorial Moments needs only its manage set.
- The timestamp-only seed commit does not recursively advance the values it is seeding. A currently uncommitted document instead receives its real write-time timestamp.
- Existing date-only documents outside the seed remain unchanged and are ineligible for edited Recent ordering until their next real edit gives them a full timestamp. The delivery does not rewrite the whole corpus or wait for seeded documents to arise naturally.

The main implementation risk is incomplete write-path coverage. Code and tests are authoritative; do not rely on a permanent documentation inventory of every mutation module.

## Recent Projection

- replace `docs_recently_added_v1` and Added-specific paths, feature names, providers, and labels with one accurately named small Recent contract;
- make the public Moments payload order by canonical `added_date`, and make public Library, public Analysis, and every manage/local payload order by canonical `last_updated`, with each payload declaring its `added` or `edited` basis;
- require every route that enables Recent to declare an allowlisted `added` or `edited` basis; do not infer it from publicness, scope type, route path, route id, or scope id;
- allow the same source scope to have different working and published Recent projections: for example, Moments is recently edited in `/docs/` and recently added in `/moments/`;
- compute each limited projection from the complete eligible source set; do not derive a public added list by reordering an already truncated editor list, or vice versa;
- preserve the configured limit, scope/viewability rules, parent context, lazy loading, and `recent-results` host;
- keep one **Recent** control and make the route's code-owned policy select the basis; publicness alone does not imply recently added, and the browser does not expose an Added/Edited toggle or parallel view;
- keep the projection independent from `index-tree.json` and the search payload;
- include newly created documents naturally because creation is both their added date and first update;
- leave Catalogue-owned `/series/` recently added for the same catalogue reason as public Moments, and leave Docs Review without a Recent surface unless a separate review-package need is designed.

## Code Authority To Inspect

- document timestamp and front-matter helpers under `docs-viewer/services/`;
- management creation, source-editor, metadata, placement, import, lifecycle, and coordinated write/rebuild owners;
- Docs watcher/suppression and focused source-validation checks for direct edits;
- Docs builder Recent payload generation, generated-read services, route configuration, scope manifests, and the public publication projection;
- configured-scope provider, Recent controller/view registration, labels, and focused tests.

These are responsibility areas, not an exhaustive update list.

## Delivery Tracker

**Current state:** proposed; not started.

**Next checkpoint:** RD-1 — establish the timestamp writer.

Complete checkpoints in order. Keep command transcripts, mutation inventories, Git scan results, and verification logs out of this delivery; use a temporary sibling only when that detail must survive between sessions.

### [ ] RD-1 — Establish The Timestamp Writer

- [ ] Trace every supported canonical create and mutation boundary, recording a detailed inventory only in temporary working notes when needed.
- [ ] Add one timestamp helper and make successful content/front-matter writes advance `last_updated` exactly once.
- [ ] Preserve no-op, failed, preview, read, and rebuild-only behavior without timestamp churn.
- [ ] Add focused evidence for managed writes and for direct VS Code/Codex edits that omit the required timestamp change.

### [ ] RD-2 — Seed Bounded Git History

- [ ] Plan the seed from Git author history before changing source, walking far enough to fill each configured edited projection after scope and viewability rules.
- [ ] Seed only the union of eligible top-limit documents for manage and public edited contexts; do not normalize the remaining corpus.
- [ ] Treat the seed commit as a one-time migration exemption, and give any currently dirty source its actual write-time timestamp.
- [ ] Prove that every edited projection can fill its limit and that unseeded date-only documents remain outside edited ordering until genuinely changed.

### [ ] RD-3 — Build Generic Recent Projections

- [ ] Replace the Added-specific schema with one Recent projection function that accepts an allowlisted `added` or `edited` basis.
- [ ] Apply eligibility and public visibility before sorting and limiting, and declare the basis in every payload.
- [ ] Require an explicit basis for every route that enables Recent; add no Moments, route-id, scope-id, or publicness branch.
- [ ] Generate working and published projections independently from the complete eligible set rather than deriving either from a truncated list.

### [ ] RD-4 — Migrate The Recent Runtime

- [ ] Rename Added-specific routes, paths, manifests, providers, controller language, view definitions, and labels to the generic Recent contract without aliases.
- [ ] Keep one **Recent** control and have the configured route basis select its projection and result language.
- [ ] Ensure `/docs/` reads edited projections for every selected scope, including public scopes, while public routes read their own configured projection.
- [ ] Remove the superseded payload and fixed Added assumptions after a final active-use scan.

### [ ] RD-5 — Verify, Document, And Close

- [ ] Run focused timestamp, mutation, seed-planning, builder, publication, provider, and Recent-view evidence.
- [ ] Prove public Moments is added while editorial Moments is edited, and that public Library and Analysis remain edited.
- [ ] Run `git diff --check` and the focused private-path/secret scan; keep detailed results outside this delivery.
- [ ] Update the durable generated-data and document-metadata owners, then present the feature-document closeout list and wait for explicit retirement confirmation.

## Done When

- the bounded Git seed supplies enough full-precision candidates for every configured edited projection without rewriting unrelated source documents;
- unseeded date-only documents become eligible for edited Recent only after a real edit supplies a full timestamp;
- every supported successful canonical write advances `last_updated` exactly once and no-op/rebuild-only paths do not;
- direct source-edit workflows receive clear evidence when the timestamp was not advanced;
- the manage route returns the true latest edited set for public, local, and external-local scopes, even when documents are created and edited repeatedly on the same day;
- public Library and Analysis return their true latest edited sets, while public Moments returns its true latest added set;
- the Moments scope uses edited ordering in `/docs/` and added ordering in `/moments/` without presenting a mode choice;
- another route can adopt either basis through route policy without a new route-id or scope-id branch;
- generated/local/public payload, route, manifest, provider, and UI names describe the generic Recent contract and its declared basis without compatibility aliases;
- focused timestamp, mutation, builder, publication, provider, and Recent-view evidence passes;
- [Generated Data Providers](/docs/?scope=studio&doc=d-20260605-125108-c68916) and the durable metadata owner describe the shipped contracts.
