{
  "doc_id": "d-20260717-231652-d6d49e",
  "title": "Extract Imported SVG To Published Media",
  "added_date": "2026-07-17 23:16:52",
  "last_updated": "2026-07-17 23:20:05",
  "viewer_url": "/docs/?scope=studio&doc=d-20260717-231652-d6d49e",
  "parent_id": "d-20260714-234030-1e0af6",
  "summary": "Persist sanitized imported SVG as published image media instead of expanding canonical Markdown with inline markup.",
  "ui_status": "planned",
  "content_html": "<h1>Extract Imported SVG To Published Media</h1>\n<h2>Outcome</h2>\n<p>Docs Import stores sanitized SVG once at the target scope's configured published <code>img</code> location and writes a logical media token into canonical Markdown instead of retaining a large inline <code>&lt;svg&gt;</code> block.</p>\n<p>This applies both to a standalone staged SVG and to inline SVG encountered while converting imported HTML. It extends the existing inline-raster extraction model rather than creating a separate diagram store.</p>\n<h2>Import Contract</h2>\n<ul>\n<li>SVG is parsed and sanitized before publication. Script blocks, event-handler attributes, and other rejected content never reach the published artifact or canonical Markdown.</li>\n<li>Each extracted SVG receives a deterministic, collision-safe media identity associated with the target document. Overwrite imports retarget generated identities to the existing canonical <code>doc_id</code> in the same way as current extracted raster media.</li>\n<li>The target scope's <code>img</code> configuration selects repository, external-local, or R2 publication. Import logic does not choose a provider.</li>\n<li>Media publication and byte verification complete before source is written.</li>\n<li>Canonical Markdown contains an ordinary image reference such as:</li>\n</ul>\n<pre><code>![Diagram](&#91;&#91;media:docs/&lt;scope&gt;/img/&lt;identity&gt;.svg&#93;&#93;)</code></pre>\n<ul>\n<li>A useful SVG <code>&lt;title&gt;</code> supplies the initial image alt text when available. The author can refine that text after import.</li>\n<li>Existing manually authored inline SVG remains valid; this feature changes import conversion, not the general Markdown authoring contract.</li>\n</ul>\n<h2>Boundaries</h2>\n<p>In scope:</p>\n<ul>\n<li>standalone SVG source import;</li>\n<li>inline SVG extracted from imported HTML;</li>\n<li>sanitized SVG serialization, published-media planning, token rewriting, collision handling, and provider-independent apply;</li>\n<li>focused import preview, apply, failure, and rendered-result verification.</li>\n</ul>\n<p>Out of scope:</p>\n<ul>\n<li>scanning or migrating existing source documents that already contain inline SVG;</li>\n<li>extracting SVG from arbitrary existing Markdown;</li>\n<li>a Mermaid renderer or registered Mermaid build producer;</li>\n<li>source-editor <strong>Add image</strong> or <strong>Add file</strong> actions;</li>\n<li>a general SVG editor, optimizer, formatter, or asset registry.</li>\n</ul>\n<h2>Delivery</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260717-231652-4890d2\">Extract Imported SVG To Published Media Delivery</a> owns the implementation and verification boundary.</p>\n<h2>Durable Home</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260514-184303-7914e2\">Media And Asset Handling</a> will own the shipped SVG media path. <a href=\"/docs/?scope=studio&amp;doc=d-20260514-164722-3de65e\">Docs Import Architecture</a> will own the updated conversion and media-before-source workflow.</p>\n"
}
