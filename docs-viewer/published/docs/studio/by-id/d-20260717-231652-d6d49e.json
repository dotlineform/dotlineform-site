{
  "doc_id": "d-20260717-231652-d6d49e",
  "title": "Extract Imported SVG To Published Media",
  "added_date": "2026-07-17 23:16:52",
  "last_updated": "2026-07-18 09:15:33",
  "viewer_url": "/docs/?scope=studio&doc=d-20260717-231652-d6d49e",
  "parent_id": "d-20260714-234030-1e0af6",
  "summary": "Persist sanitized imported SVG as a distinct published media type instead of expanding canonical Markdown with inline markup.",
  "ui_status": "planned",
  "content_html": "<h1>Extract Imported SVG To Published Media</h1>\n<h2>Outcome</h2>\n<p>Docs Import stores sanitized SVG once at the target scope's configured published <code>svg</code> location and writes a logical media token into canonical Markdown instead of retaining a large inline <code>&lt;svg&gt;</code> block.</p>\n<p>For a standalone staged <code>.svg</code> file, import sanitizes and publishes the file as image media, then creates wrapper Markdown that links to it. For an inline <code>&lt;svg&gt;</code> encountered while converting imported HTML, import extracts that element, sanitizes and publishes it, then replaces its markup with an image link. <strong>Extraction</strong> therefore describes only the HTML-inline case; both paths share the same publication contract.</p>\n<p>This improves canonical Markdown editability rather than adding SVG editing to Docs Viewer. A separately published <code>.svg</code> can be handed to an external editor or ChatGPT and replaced without changing its stable Markdown token, but media browsing, editing, and replacement controls remain separate work.</p>\n<h2>Shared Sanitization Flow</h2>\n<pre><code class=\"language-text\">standalone .svg file ──┐\n                       ├─&gt; shared SVG sanitizer ─&gt; sanitized SVG bytes ─&gt; media plan\nHTML inline &lt;svg&gt; ─────┘\n</code></pre>\n<h2>Import Contract</h2>\n<ul>\n<li>Standalone and HTML-inline SVG pass through one shared SVG sanitizer. Source discovery and media planning remain caller concerns; parsing, safety policy, external-reference removal, and sanitized serialization have one owner.</li>\n<li>SVG is parsed and sanitized before publication. Script blocks, event-handler attributes, and other rejected content never reach the published artifact or canonical Markdown.</li>\n<li>Safe self-contained styling already inside the SVG is preserved, including presentation attributes, element <code>style</code> attributes, and internal <code>&lt;style&gt;</code> rules. Import does not resolve surrounding HTML CSS, compute inherited page styles, or fetch external stylesheets, fonts, images, or other resources.</li>\n<li>External references are stripped wherever they occur, including element reference attributes, stylesheet imports, external font sources, processing instructions, and CSS <code>url(...)</code> values. Same-document fragment references such as <code>#symbol</code> and <code>url(#gradient)</code> remain available when their target survives sanitization.</li>\n<li>Sanitization is best effort for visual fidelity. Import does not emit speculative warnings about styling that may have depended on the original page; before/after visual review determines whether the published result is acceptable. Malformed SVG and actual sanitization, publication, or source-write failures remain explicit import results. The sanitizer may retain non-user-facing removal diagnostics for tests and troubleshooting.</li>\n<li>A standalone SVG keeps its validated staged basename when published: <code>diagram.svg</code> remains <code>diagram.svg</code>. An SVG extracted from imported HTML uses the existing document-owned extracted-media convention, <code>&lt;canonical-doc-id&gt;-image-NN.svg</code>, numbered from <code>01</code> in document order. Overwrite imports retarget these generated HTML-inline identities to the existing canonical <code>doc_id</code>.</li>\n<li>The target scope's <code>svg</code> configuration selects the physical published location. Import logic does not branch on provider or choose a location from the <code>.svg</code> suffix.</li>\n<li>Media publication and byte verification complete before source is written.</li>\n<li>The existing document preview remains write-free and unchanged. SVG filenames, tokens, physical destinations, collision state, and replacement details stay internal; this feature adds no SVG-specific media preview or handling UI. Only actual import failures surface through the existing result boundary.</li>\n<li>Canonical Markdown contains an ordinary image reference such as:</li>\n</ul>\n<pre><code>![Diagram](&#91;&#91;media:docs/&lt;scope&gt;/svg/&lt;identity&gt;.svg&#93;&#93;)</code></pre>\n<ul>\n<li>A useful SVG <code>&lt;title&gt;</code> supplies the initial image alt text when available. The author can refine that text after import.</li>\n<li>Existing manually authored inline SVG remains valid; this feature changes import conversion, not the general Markdown authoring contract.</li>\n</ul>\n<h2>Media Type And Location Decision</h2>\n<p><code>svg</code> is a separate published media type rather than another suffix inside <code>img</code>:</p>\n<ul>\n<li><code>img</code> remains the raster-image contract, used mainly for photographic and other pixel-based media;</li>\n<li><code>svg</code> owns sanitized vector image publication, including imported diagrams and later Mermaid output;</li>\n<li>distinct <code>media/img/</code> and <code>media/svg/</code> folders make browsing, external editing, sanitization ownership, and future producer relationships clearer;</li>\n<li>the split is justified by stable format and handling differences, not by an assumption that every file extension requires its own media type.</li>\n</ul>\n<p>Storage policy is decided for each <strong>scope plus media type</strong>. The initial <code>svg</code> policy is repository storage for repo-backed scopes because SVG is generally small, and storage inside the external scope container for external-local scopes because their lifecycle remains self-contained. No scope initially configures <code>svg</code> in R2. Configuration remains authoritative, so a later location change does not require canonical Markdown rewriting and must not be implemented as hidden suffix-based routing.</p>\n<p><strong>Planning baseline:</strong> the Docs source, published workspace, and public scope-media roots contain no <code>.svg</code> files. The new type therefore starts empty and requires no media migration, compatibility alias, or legacy token handling.</p>\n<p>The initial logical and physical shapes are:</p>\n<pre><code class=\"language-text\">docs/&lt;scope&gt;/svg/&lt;filename&gt;\n\nsite/assets/data/docs/scopes/&lt;scope&gt;/media/svg/       # public repo-backed scope\ndocs-viewer/published/docs/&lt;scope&gt;/media/svg/         # local repo-backed scope\n&lt;external-scope&gt;/media/svg/                           # external-local scope\n</code></pre>\n<p>Downstream Mermaid and source-editor media features must publish SVG through this type, while raster images continue to use <code>img</code>.</p>\n<h2>Representation Decision</h2>\n<p>Docs Import does not offer an <strong>Extract</strong> versus <strong>Keep inline</strong> choice. Imported SVG always becomes sanitized published media:</p>\n<ul>\n<li>leaving a standalone <code>.svg</code> inline would deliberately expand a separate source file into Markdown;</li>\n<li>retaining HTML-inline SVG would not restore surrounding page CSS that HTML import already discards;</li>\n<li>two canonical representations would multiply preview, overwrite, sanitization, and verification paths;</li>\n<li>the author often cannot judge the better representation until seeing the rendered result.</li>\n</ul>\n<p>Manually authored inline SVG remains a deliberate expert exception. If before/after review exposes an unacceptable result, the author can edit or replace the separate SVG, or intentionally substitute inline SVG in canonical Markdown. Add an import-time representation choice only if repeated real examples show that this escape hatch is insufficient.</p>\n<h2>Re-import And Versioning</h2>\n<p>Docs Import maintains the current published SVG representation; it does not manage versions of the external source:</p>\n<ul>\n<li>the existing document-overwrite confirmation is the sole authority needed to replace that document's stable SVG media identities;</li>\n<li>matching published bytes remain <code>unchanged</code>, while changed bytes replace the existing object and are byte-verified before source mutation;</li>\n<li>re-import never creates <code>_2</code> or another fallback filename, asks for a separate media-overwrite confirmation, or retains history and rollback copies;</li>\n<li>when a later HTML re-import produces fewer SVGs, surplus numbered files may remain as quiet unreferenced media; import does not delete them or emit an orphan warning;</li>\n<li>authors who need SVG source versions keep them outside published Docs media;</li>\n<li>an existing media target during an unrelated new-document import is an ownership anomaly and blocks the import rather than being renamed or replaced.</li>\n</ul>\n<h2>Boundaries</h2>\n<p>In scope:</p>\n<ul>\n<li>standalone SVG source import;</li>\n<li>inline SVG extracted from imported HTML;</li>\n<li>one shared sanitization policy for both sources, including safe internal-style preservation and external-reference removal;</li>\n<li>registration, configuration, serving, inventory, and lifecycle support for the published <code>svg</code> media type;</li>\n<li>sanitized SVG serialization, published-media planning, token rewriting, collision handling, and provider-independent apply;</li>\n<li>unchanged write-free document preview, focused apply/failure verification, and before/after rendered-result review.</li>\n</ul>\n<p>Out of scope:</p>\n<ul>\n<li>scanning or migrating existing source documents that already contain inline SVG;</li>\n<li>extracting SVG from arbitrary existing Markdown;</li>\n<li>resolving surrounding HTML CSS, reproducing the browser cascade, fetching external dependencies, or guaranteeing pixel-identical output;</li>\n<li>a Mermaid renderer or registered Mermaid build producer;</li>\n<li>source-editor <strong>Add image</strong> or <strong>Add file</strong> actions;</li>\n<li>a general SVG editor, optimizer, formatter, media-replacement UI, or asset registry;</li>\n<li>imported-media version history, rollback copies, or external source-version management;</li>\n<li>automated visual diffing or speculative CSS-dependency warnings.</li>\n</ul>\n<h2>Delivery</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260717-231652-4890d2\">Extract Imported SVG To Published Media Delivery</a> owns the implementation and verification boundary.</p>\n<h2>Durable Home</h2>\n<p>Durable documentation changes wait until implementation. At closeout, <a href=\"/docs/?scope=studio&amp;doc=d-20260423-000000-8fd731\">Source Organisation</a> will clarify that storage is a practical scope-plus-media-type decision and register the <code>svg</code> topology. <a href=\"/docs/?scope=studio&amp;doc=d-20260423-155039-e36925\">Docs Images And Assets</a> will own the author-facing best practice: imported SVG is normalized to media, inline SVG is an intentional manual exception, and visual fidelity is checked before and after import. <a href=\"/docs/?scope=studio&amp;doc=d-20260514-184303-7914e2\">Media And Asset Handling</a> will own the shipped SVG media and sanitization boundary. <a href=\"/docs/?scope=studio&amp;doc=d-20260514-164722-3de65e\">Docs Import Architecture</a> will own the deterministic conversion and media-before-source workflow. The delivery document only tracks promotion into these durable owners; it must not become the long-term home for the guidance.</p>\n"
}
