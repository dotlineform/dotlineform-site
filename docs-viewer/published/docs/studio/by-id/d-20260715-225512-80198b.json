{
  "doc_id": "d-20260715-225512-80198b",
  "title": "Recent Documents Delivery",
  "added_date": "2026-07-15 22:55:12",
  "last_updated": "2026-07-16 17:52:03",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-225512-80198b",
  "parent_id": "d-20260715-222129-8253ee",
  "summary": "Make last_updated trustworthy and serve one route-appropriate Recent view without an Added/Edited choice.",
  "ui_status": "done",
  "content_html": "<h1>Recent Documents Delivery</h1>\n<h2>Outcome</h2>\n<p>Docs Viewer can answer the route-appropriate Recent question accurately: “what is new?” for a catalogue-like collection and “what changed?” for a changing document collection.</p>\n<p>One complete delivery makes <code>last_updated</code> a consistently maintained full timestamp for future edits, seeds only the bounded historical set needed to populate current edited projections, and replaces the current Recently Added-specific contract with one generic Recent contract. Public Moments receives an added-date projection. Public Library and Analysis, plus every scope displayed in <code>/docs/</code>, receive an edited-date projection. There is no Added/Edited toggle or second Recent control.</p>\n<h2>Timestamp Contract</h2>\n<ul>\n<li>New and changed <code>last_updated</code> values use the same <code>YYYY-MM-DD HH:MM:SS</code> local timestamp contract as <code>added_date</code>.</li>\n<li>Creating a document sets <code>added_date</code> and <code>last_updated</code> to the same timestamp.</li>\n<li>Only a change to the canonical body, <code>title</code>, or <code>summary</code> advances <code>last_updated</code>. This positive allowlist must be extended deliberately if another field should ever acquire edited-Recent semantics.</li>\n<li>Source-editor rebuild, title/summary metadata changes, imports/overwrites, and other focused mutation paths use the shared source comparison and timestamp owner rather than maintaining the field independently.</li>\n<li>A no-op request, generated-output rebuild, search rebuild, read, preview, or failed mutation does not change it.</li>\n<li>Direct source edits through VS Code, Codex, or another editor must advance the field when body, <code>title</code>, or <code>summary</code> changes; watcher/check evidence should surface missing timestamp evidence rather than silently rewriting source.</li>\n<li>A one-time Git-history seed walks backward through renames until each edited Recent projection has enough eligible documents to fill its configured limit, then writes the latest body/title/summary author timestamp only to the union of those documents.</li>\n<li>The seed applies context eligibility before choosing its bounded set. With the current limit of 10, one manage and one public edited projection can require at most 20 distinct seeded documents per scope; the history scan may inspect more commits to find them. Public Moments needs no added-date seed, while editorial Moments needs only its manage set.</li>\n<li>The timestamp-only seed commit does not recursively advance the values it is seeding. A currently uncommitted document instead receives its real write-time timestamp.</li>\n<li>Existing date-only documents outside the seed remain unchanged and are ineligible for edited Recent ordering until their next real edit gives them a full timestamp. The delivery does not rewrite the whole corpus or wait for seeded documents to arise naturally.</li>\n</ul>\n<p>The main implementation risk is incomplete write-path coverage. Code and tests are authoritative; do not rely on a permanent documentation inventory of every mutation module.</p>\n<h2>Recent Projection</h2>\n<ul>\n<li>replace <code>docs_recently_added_v1</code> and Added-specific paths, feature names, providers, and labels with one accurately named small Recent contract;</li>\n<li>make the public Moments payload order by canonical <code>added_date</code>, and make public Library, public Analysis, and every manage/local payload order by canonical <code>last_updated</code>, with each payload declaring its <code>added</code> or <code>edited</code> basis;</li>\n<li>require every route that enables Recent to declare an allowlisted <code>added</code> or <code>edited</code> basis; do not infer it from publicness, scope type, route path, route id, or scope id;</li>\n<li>allow the same source scope to have different working and published Recent projections: for example, Moments is recently edited in <code>/docs/</code> and recently added in <code>/moments/</code>;</li>\n<li>compute each limited projection from the complete eligible source set; do not derive a public added list by reordering an already truncated editor list, or vice versa;</li>\n<li>preserve the configured limit, scope/viewability rules, parent context, lazy loading, and <code>recent-results</code> host;</li>\n<li>keep one <strong>Recent</strong> control and make the route's code-owned policy select the basis; publicness alone does not imply recently added, and the browser does not expose an Added/Edited toggle or parallel view;</li>\n<li>keep the projection independent from <code>index-tree.json</code> and the search payload;</li>\n<li>include newly created documents naturally because creation is both their added date and first update;</li>\n<li>leave Catalogue-owned <code>/series/</code> recently added for the same catalogue reason as public Moments, and leave Docs Review without a Recent surface unless a separate review-package need is designed.</li>\n</ul>\n<h2>Code Authority To Inspect</h2>\n<ul>\n<li>document timestamp and front-matter helpers under <code>docs-viewer/services/</code>;</li>\n<li>management creation, source-editor, metadata, placement, import, lifecycle, and coordinated write/rebuild owners;</li>\n<li>Docs watcher/suppression and focused source-validation checks for direct edits;</li>\n<li>Docs builder Recent payload generation, generated-read services, route configuration, scope manifests, and the public publication projection;</li>\n<li>configured-scope provider, Recent controller/view registration, labels, and focused tests.</li>\n</ul>\n<p>These are responsibility areas, not an exhaustive update list.</p>\n<h2>Delivery Tracker</h2>\n<p><strong>Current state:</strong> implementation complete; the feature documents remain available for explicit closeout review.</p>\n<p><strong>Next checkpoint:</strong> user review of the closeout list; no retirement is authorized by delivery completion.</p>\n<p><strong>Temporary working note:</strong> <a href=\"/docs/?scope=studio&amp;doc=d-20260716-164217-a3c7e1\">Recent Documents Working Notes</a> carries the mutation inventory, seed scan details, and other disposable implementation evidence.</p>\n<p>Complete checkpoints in order. Keep command transcripts, mutation inventories, Git scan results, and verification logs out of this delivery; use a temporary sibling only when that detail must survive between sessions.</p>\n<h3>[x] RD-1 — Establish The Timestamp Writer</h3>\n<ul>\n<li>[x] Trace every supported canonical create and mutation boundary, recording a detailed inventory only in temporary working notes when needed.</li>\n<li>[x] Add one positive body/title/summary comparison and make qualifying writes advance <code>last_updated</code> exactly once.</li>\n<li>[x] Preserve no-op, failed, preview, read, and rebuild-only behavior without timestamp churn.</li>\n<li>[x] Add focused evidence for managed writes and for direct VS Code/Codex edits that omit the required timestamp change.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> every supported canonical write uses the same positive edit comparison, qualifying writes reach one timestamp owner, and an omitted direct-edit timestamp is surfaced clearly.</p>\n<h3>[x] RD-2 — Seed Bounded Git History</h3>\n<ul>\n<li>[x] Plan the seed from Git author history before changing source, walking far enough to fill each configured edited projection after scope and viewability rules.</li>\n<li>[x] Seed only the union of eligible top-limit documents for manage and public edited contexts; do not normalize the remaining corpus.</li>\n<li>[x] Treat the seed commit as a one-time migration exemption, and give any currently dirty source its actual write-time timestamp.</li>\n<li>[x] Prove that every edited projection can fill its limit and that unseeded date-only documents remain outside edited ordering until genuinely changed.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> the dry-run and applied seed identify the same bounded union, every edited projection can fill its configured limit, and no unrelated source document is rewritten.</p>\n<h3>[x] RD-3 — Build Generic Recent Projections</h3>\n<ul>\n<li>[x] Replace the Added-specific schema with one Recent projection function that accepts an allowlisted <code>added</code> or <code>edited</code> basis.</li>\n<li>[x] Apply eligibility and public visibility before sorting and limiting, and declare the basis in every payload.</li>\n<li>[x] Require an explicit basis for every route that enables Recent; add no Moments, route-id, scope-id, or publicness branch.</li>\n<li>[x] Generate working and published projections independently from the complete eligible set rather than deriving either from a truncated list.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> one pure projection produces the correct eligible top-limit set for either declared basis, and every Recent-enabled route resolves through configuration rather than an identity branch.</p>\n<h3>[x] RD-4 — Migrate The Recent Runtime</h3>\n<ul>\n<li>[x] Rename Added-specific routes, paths, manifests, providers, controller language, view definitions, and labels to the generic Recent contract without aliases.</li>\n<li>[x] Keep one <strong>Recent</strong> control and have the configured route basis select its projection and result language.</li>\n<li>[x] Ensure <code>/docs/</code> reads edited projections for every selected scope, including public scopes, while public routes read their own configured projection.</li>\n<li>[x] Remove the superseded payload and fixed Added assumptions after a final active-use scan.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> fresh public and manage boots use one generic Recent control and contract, the route matrix selects the intended basis, and no active Added-specific path remains.</p>\n<h3>[x] RD-5 — Verify, Document, And Close</h3>\n<ul>\n<li>[x] Run focused timestamp, mutation, seed-planning, builder, publication, provider, and Recent-view evidence.</li>\n<li>[x] Prove public Moments is added while editorial Moments is edited, and that public Library and Analysis remain edited.</li>\n<li>[x] Run <code>git diff --check</code> and the focused private-path/secret scan; keep detailed results outside this delivery.</li>\n<li>[x] Update the durable generated-data and document-metadata owners, then present the feature-document closeout list and wait for explicit retirement confirmation.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> all <strong>Done When</strong> conditions pass, durable owners describe the shipped contracts, and the retirement decision remains with the explicit closeout review rather than this tracker.</p>\n<h2>Done When</h2>\n<ul>\n<li>the bounded Git seed supplies enough full-precision candidates for every configured edited projection without rewriting unrelated source documents;</li>\n<li>unseeded date-only documents become eligible for edited Recent only after a real edit supplies a full timestamp;</li>\n<li>every supported successful body/title/summary change advances <code>last_updated</code> exactly once;</li>\n<li>direct source-edit workflows receive clear evidence when the timestamp was not advanced;</li>\n<li>the manage route returns the true latest edited set for public, local, and external-local scopes, even when documents are created and edited repeatedly on the same day;</li>\n<li>public Library and Analysis return their true latest edited sets, while public Moments returns its true latest added set;</li>\n<li>the Moments scope uses edited ordering in <code>/docs/</code> and added ordering in <code>/moments/</code> without presenting a mode choice;</li>\n<li>another route can adopt either basis through route policy without a new route-id or scope-id branch;</li>\n<li>generated/local/public payload, route, manifest, provider, and UI names describe the generic Recent contract and its declared basis without compatibility aliases;</li>\n<li>focused timestamp, mutation, builder, publication, provider, and Recent-view evidence passes;</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260605-125108-c68916\">Generated Data Providers</a> and the durable metadata owner describe the shipped contracts.</li>\n</ul>\n"
}
