{
  "doc_id": "d-20260718-105957-c72b2a",
  "title": "Standalone Docs Viewer Concept",
  "added_date": "2026-07-18 10:59:57",
  "last_updated": "2026-07-18 12:18:33",
  "viewer_url": "/docs/?scope=studio&doc=d-20260718-105957-c72b2a",
  "parent_id": "d-20260511-120352-90ffa6",
  "summary": "Define the Docs Viewer core, its integrated and standalone compositions, and the standalone application that proves the boundary.",
  "ui_status": "proposed",
  "content_html": "<h1>Standalone Docs Viewer Concept</h1>\n<h2>Problem</h2>\n<p>Docs Viewer has been repeatedly refactored as its reader, management, public, import, Data Sharing, and Docs Review responsibilities grew. Those changes created useful seams, but they did not answer the fundamental question: <strong>what is the core Docs Viewer before repository and adjacent-application integrations are attached?</strong></p>\n<p>Data Sharing made this ambiguity particularly visible. Storage and publication adapters partially separated workflows from artifact location, but location is only one architectural boundary. An adapter can answer where bytes live and how they are read or written without answering which document, scope, build, reader, and management behavior belongs to Docs Viewer itself.</p>\n<p>The result is a capable application whose browser composition is increasingly explicit while its Python startup, paths, builders, imports, exports, and optional services still inherit repository and cross-application context.</p>\n<h2>Desired Outcome</h2>\n<p>One concrete Docs Viewer core supports two application compositions from the same public repository:</p>\n<ul>\n<li>the <strong>integrated composition</strong> combines the core with repository, public-site, R2, Data Sharing, Docs Review, and Studio integrations;</li>\n<li>the <strong>standalone composition</strong> combines the same core with a manage-only, external-local application environment and direct local import/export.</li>\n</ul>\n<p>The standalone application is not a fork or reduced rewrite. It is the first independent consumer and executable proof that the core boundary is real. Extracting that boundary should make the integrated application easier to understand, test, and extend even if no user ever installs the standalone release.</p>\n<h2>Working Core Hypothesis</h2>\n<p>No authoritative core definition exists yet. The <a href=\"/docs/?scope=studio&amp;doc=d-20260718-121833-9fa2d4\">Core Definition Analysis Delivery</a> must test this working hypothesis against current code, composition consumers, environment assumptions, and open roadmap proposals before Transition Architecture begins.</p>\n<p>The core is the concrete Docs Viewer product behavior shared by both compositions:</p>\n<ul>\n<li>document and scope identity, hierarchy, metadata, and source rules;</li>\n<li>Markdown, scope-owned media, build, published-data, and logical-reference contracts;</li>\n<li>browsing, search, recent activity, rendering, and local reader state;</li>\n<li>document creation, editing, movement, deletion, rebuild, and scope lifecycle;</li>\n<li>generated document and search payloads;</li>\n<li>external-local artifact access and confinement;</li>\n<li>ordinary direct import and export;</li>\n<li>capability, route, view, action, and extension contracts needed to compose the manage application.</li>\n</ul>\n<p>This list may be revised by the analysis and later implementation evidence. Core is not a generic documentation framework and should not gain abstractions for hypothetical consumers. Interfaces belong only at demonstrated application, provider, path, and workflow seams.</p>\n<h2>Target Shape</h2>\n<pre><code class=\"language-text\">Docs Viewer core\n├── reader and manage UI\n├── source model and builders\n├── document and scope mutations\n├── external-local artifact access\n├── direct local import and export\n└── loopback service primitives\n\nIntegrated composition\n├── repository and R2 locations\n├── public routes and projections\n├── static-site publishing\n├── Data Sharing and Docs Review\n└── Studio and repository bootstrap\n\nStandalone composition\n├── manage route only\n├── external-local scopes only\n├── app-owned config, state, logs, staging, and exports\n└── bundled runtime and dependencies\n</code></pre>\n<p>This is one codebase with two deliberately different dependency closures. The integrated and standalone compositions may depend on core; core must not import either composition.</p>\n<h2>Boundary Layers</h2>\n<table>\n<thead>\n<tr>\n<th>boundary</th>\n<th>question answered</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>artifact adapter</td>\n<td>where are artifacts stored and how are they listed, read, written, verified, and referenced?</td>\n</tr>\n<tr>\n<td>Docs Viewer core</td>\n<td>which document, scope, build, reader, and management behaviors constitute Docs Viewer?</td>\n</tr>\n<tr>\n<td>application composition</td>\n<td>which optional providers, routes, workflows, adjacent applications, and delivery assumptions are attached here?</td>\n</tr>\n</tbody>\n</table>\n<p>The current provider-neutral artifact direction is valuable and should remain the I/O boundary. It is not by itself an application boundary: repository and R2 implementations, authentication, public exposure, Data Sharing formats, and route composition must still be attached from outside core.</p>\n<h2>Current Assessment</h2>\n<h3>Browser</h3>\n<p>The browser is closest to the target:</p>\n<ul>\n<li>shared boot and application-composition modules accept route context, collection providers, source adapters, view definitions, control renderers, shell renderers, and other contributions;</li>\n<li>the manage entrypoint is already a thin assembly of management contributions;</li>\n<li>route features, access projection, live backend capabilities, and registered views/actions are distinct decisions.</li>\n</ul>\n<p>Standalone remains <code>app_kind: manage</code>; it is a distribution and application composition, not a fourth route kind beside public, manage, and review.</p>\n<p>The public-safe shared runtime currently lives under <code>site/docs-viewer/</code>. That is not a runtime blocker: the repository is the source and release platform, so a standalone build may bundle those canonical modules. The installed application must not resolve them from a repository checkout.</p>\n<h3>Python And Services</h3>\n<p>The backend has useful focused modules but is not yet package-safe:</p>\n<ul>\n<li><code>docs_viewer_service.py</code> discovers a repository root, maps runtime and CSS from repository paths, and imports Docs Review at startup;</li>\n<li><code>docs_management_service.py</code> assembles core mutations alongside public publishing, Review sessions, and repository-oriented export;</li>\n<li><code>docs_import_source_service.py</code> imports Data Sharing package recognition, collection apply, review handoff, and Data Sharing workspace paths directly;</li>\n<li>builder common code derives repository paths from <code>__file__</code> and knows browser config under <code>site/</code>;</li>\n<li>the current static HTML export accepts repository-backed local scopes and rejects external-local scopes;</li>\n<li>global scope configuration and repeated <code>repo_root</code> parameters carry application-environment assumptions through otherwise reusable workflows.</li>\n</ul>\n<p><code>docs_artifact_locations.py</code> is the strongest existing backend seam. Its common location contract is reusable, but standalone packaging should include only external-local support while repository and R2 providers remain integrated-composition contributions.</p>\n<p>This assessment is a starting map, not a frozen module manifest. Exact dependency direction must be re-derived from current code before each extraction slice.</p>\n<h2>Composition Rules</h2>\n<ul>\n<li>Select behavior once at an application composition root; do not scatter <code>standalone</code> branches through shared workflows.</li>\n<li>Give core explicit application paths and services for assets, configuration, state, logs, staging, exports, temporary files, builders, and artifact access instead of letting it discover repository context.</li>\n<li>Register optional routes, import formats, export formats, providers, UI contributions, and capability records. Do not import an excluded integration and then merely hide it with a capability flag.</li>\n<li>Derive capability truth from the components actually registered and available at startup.</li>\n<li>Keep one source implementation for each shared behavior. The standalone application may have a new launcher, installer, configuration shell, and composition root, but not parallel document or management logic.</li>\n<li>Enforce the dependency direction mechanically so that standalone packaging cannot accidentally acquire repository, public, R2, Data Sharing, or Docs Review imports.</li>\n</ul>\n<h2>Standalone Product Boundary</h2>\n<p>Every standalone scope is external-local by definition. Its source, media, generated data, and other scope-owned artifacts live in an explicitly configured local workspace outside the installed application.</p>\n<p>Within that boundary the application retains full management:</p>\n<ul>\n<li>browse, search, create, edit, move, and delete documents;</li>\n<li>create, rename, configure, rebuild, and delete scopes;</li>\n<li>manage scope-owned local media and generated data;</li>\n<li>use ordinary direct local import and export.</li>\n</ul>\n<p>Import and export do not embed Data Sharing modules, reviewed-package workflows, return-import contracts, or Data Sharing route and application context. The standalone application also has no public scopes, public routes, public projections, static-site publishing, repository-backed scopes, R2 locations, or Docs Review surface.</p>\n<h2>Install, Data, And Upgrade Boundary</h2>\n<p>A download-and-overwrite upgrade is safe only when the installed application is disposable. Mutable configuration and user-owned data must not live beneath <code>&lt;install-root&gt;/docs-viewer/</code>.</p>\n<table>\n<thead>\n<tr>\n<th>root</th>\n<th>owns</th>\n<th>upgrade behavior</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>&lt;install-root&gt;/docs-viewer/</code></td>\n<td>runtime, bundled dependencies, browser assets, schemas, immutable defaults, and migration code</td>\n<td>replace completely with the downloaded release</td>\n</tr>\n<tr>\n<td><code>&lt;app-data-root&gt;/</code></td>\n<td>user configuration, scope registry, application-data schema version, logs, durable local state, import staging, and default export locations</td>\n<td>preserve across application replacement and migrate explicitly</td>\n</tr>\n<tr>\n<td><code>&lt;scope-root&gt;/</code></td>\n<td>external-local documents, media, generated data, and scope-owned settings</td>\n<td>remain user-owned and outside application upgrade lifecycle</td>\n</tr>\n<tr>\n<td><code>&lt;cache-root&gt;/</code></td>\n<td>reproducible caches and temporary material</td>\n<td>safe to discard and recreate</td>\n</tr>\n</tbody>\n</table>\n<p>The launcher should resolve app-data and cache roots through platform conventions or an explicit absolute override. Neither root should be inferred as a child of the installed application. Scope-specific settings that must travel with a workspace should live with that scope; the central registry should retain only application-level settings and the information needed to locate scopes.</p>\n<p>Configuration should have three layers:</p>\n<ol>\n<li>immutable defaults and schemas bundled with the installed release;</li>\n<li>a small mutable application overlay in the app-data root;</li>\n<li>scope-owned configuration in each external-local workspace where the setting belongs to that scope.</li>\n</ol>\n<p>Do not copy the complete bundled defaults into mutable configuration. New application defaults should normally arrive by replacing the install, while the user overlay stores only deliberate differences.</p>\n<p>App-data configuration must carry an explicit schema version. A built-in migration runner, shipped with the new release, should apply ordered migrations from the recorded version to the installed version. It should reject state written by a newer application, preserve a recoverable pre-migration copy, write changes atomically, and record successful completion. Deterministic non-destructive migrations may run during first startup; a migration that discards or reinterprets user choices must stop for explicit confirmation.</p>\n<p>Application-data migration must not silently become scope-content migration. Any future change to document, media, generated-data, or scope-owned configuration layouts needs its own previewed and confirmed scope migration boundary. Long-lived compatibility aliases are not a substitute for either migration path.</p>\n<h2>Integrated Repository Mapping</h2>\n<p>The same structural ownership model must apply when the core runs inside the repository. Otherwise core workflows would still receive one undifferentiated <code>repo_root</code> and the standalone composition would need special behavior throughout the application.</p>\n<p>The integrated composition has an additional repository-project role, but it should map every path explicitly:</p>\n<table>\n<thead>\n<tr>\n<th>logical role</th>\n<th>current integrated examples</th>\n<th>rule</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>application source and immutable defaults</td>\n<td><code>docs-viewer/runtime/</code>, <code>docs-viewer/services/</code>, <code>docs-viewer/build/</code>, templates, schemas, and default config</td>\n<td>running Docs Viewer reads these; only development changes them</td>\n</tr>\n<tr>\n<td>repository-owned project data</td>\n<td>tracked scope and route config, repository-backed source, <code>docs-viewer/published/</code>, and public projection under <code>site/</code></td>\n<td>changed only through explicit source, lifecycle, build, or publish workflows and reviewed through Git</td>\n</tr>\n<tr>\n<td>machine-local application data</td>\n<td><code>.env.local</code>, <code>var/docs/</code>, logs, suppressions, backups, caches, and temporary state</td>\n<td>untracked, replaceable where derived, and never treated as project source</td>\n</tr>\n<tr>\n<td>external scope workspaces</td>\n<td>configured external-local source, media, and published data</td>\n<td>independently owned and outside repository lifecycle</td>\n</tr>\n<tr>\n<td>adjacent-application data</td>\n<td>Data Sharing staging, review, and export workspaces</td>\n<td>owned by that integration and supplied only when the integrated composition registers it</td>\n</tr>\n</tbody>\n</table>\n<p>The repository checkout is therefore not the integrated equivalent of one large app-data directory. Tracked project configuration is source-controlled product data; machine-local operational state is not. Repository-backed scopes are an integrated provider capability, while external-local scopes retain their own lifecycle.</p>\n<p>Core should consume the same explicit application-environment and location contracts in both compositions. The integrated adapter may map some roles into the checkout, but core must not infer those mappings from its own file location or gain broader write authority merely because a repository root exists.</p>\n<h2>Configuration Ownership</h2>\n<p>Core extraction should replace the current broad idea of “Docs Viewer config” with explicit configuration roles. Every setting needs one owner, one canonical location, one mutation path, and a clear rule for any generated projection.</p>\n<table>\n<thead>\n<tr>\n<th>configuration role</th>\n<th>owner and behavior</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>core defaults and schemas</td>\n<td>code-owned, bundled with the application, immutable at runtime, and limited to behavior implemented by core</td>\n</tr>\n<tr>\n<td>composition configuration</td>\n<td>integrated- or standalone-application-owned declarations of registered routes, providers, integrations, and capabilities</td>\n</tr>\n<tr>\n<td>repository project configuration</td>\n<td>tracked declarations for repository scopes, routes, and publication behavior; changed only by explicit reviewed workflows</td>\n</tr>\n<tr>\n<td>application overlay</td>\n<td>user-owned settings and scope locations stored in the mutable app-data root rather than copied defaults</td>\n</tr>\n<tr>\n<td>scope-owned configuration</td>\n<td>settings whose meaning and lifecycle belong to one external-local workspace and should travel with it</td>\n</tr>\n<tr>\n<td>lifecycle evidence</td>\n<td>tool-owned records used to prove which resources a managed operation may rename or delete</td>\n</tr>\n<tr>\n<td>runtime projection</td>\n<td>generated browser-safe configuration derived from canonical declarations; rebuildable and never edited as authority</td>\n</tr>\n<tr>\n<td>secrets and machine environment</td>\n<td>machine- or platform-owned values outside tracked config and ordinary user-editable JSON</td>\n</tr>\n</tbody>\n</table>\n<p>The present repository tree mixes several of these roles beneath <code>docs-viewer/config/</code>:</p>\n<ul>\n<li><code>config/defaults/docs-viewer-service.json</code> and schemas behave like immutable application defaults;</li>\n<li><code>config/scopes/docs_scopes.json</code> and route config are canonical tracked project declarations and lifecycle mutation targets;</li>\n<li><code>config/scopes/docs_scope_manifest.json</code> is tool-managed lifecycle evidence;</li>\n<li>report and semantic-reference registries are code/product extension definitions;</li>\n<li><code>config/defaults/docs-viewer-config.json</code> and its public counterpart are written by the docs builder as browser-facing projections despite living under <code>defaults/</code>;</li>\n<li><code>.env.local</code>, <code>var/docs/</code>, and external workspaces hold machine-local or adjacent-application state outside that tree.</li>\n</ul>\n<p>Those current paths are evidence for the architecture, not a proposed rename plan. The first requirement is to make authority and mutation explicit in code and documentation. Physical reorganisation should follow only where it makes that ownership more truthful without introducing unnecessary copy or compatibility machinery.</p>\n<p>Core should define the schema and defaults for its own behavior, not the complete configuration of every composition. The integrated and standalone compositions decide which optional capabilities exist; users choose their allowed settings and scope locations; scopes retain configuration that belongs to their own data lifecycle.</p>\n<h2>User Configuration Surface</h2>\n<p>Configuration classified as user-editable needs a supported manage-mode UI. Requiring users to edit JSON would make the standalone app incomplete and would preserve the same ownership confusion in the integrated application.</p>\n<p>The surface should remain narrower than the complete configuration model:</p>\n<ul>\n<li><strong>Application settings</strong> edit the mutable application overlay and show the effective value after bundled defaults are applied.</li>\n<li><strong>Scope settings</strong> edit allowlisted settings owned by the selected scope or its registered project configuration.</li>\n<li><strong>Scope lifecycle</strong> remains the separate preview/apply workflow for creating, locating, renaming, and deleting scopes or changing structural storage/publication contracts.</li>\n<li><strong>Configuration diagnostics</strong> may show effective values, owners, canonical sources, projections, and blocked fields without making them editable.</li>\n</ul>\n<p>Do not provide a generic JSON editor. Each editable field should come from a code-owned settings contract that states its owner, level, value type, default and current value, canonical write target, validation, sensitivity, and whether the change requires rebuild, restart, or migration. The backend remains authoritative for allowlisting and writes.</p>\n<p>The integrated and standalone compositions may expose different fields because they register different capabilities and write authorities, but they should consume the same settings contract and UI primitives. A repository-project setting must be clearly labelled as a tracked project change; an application-overlay setting writes outside the checkout; a scope-owned setting writes with that workspace. Immutable defaults, generated projections, secrets, and composition-owned policy are never ordinary editable fields.</p>\n<p>The current implementation is a useful starting point:</p>\n<ul>\n<li><code>docs_source_config_settings.py</code> already declares editable, blocked, and deferred fields with source/projection paths, validation, and rebuild effects;</li>\n<li>the settings endpoint performs allowlisted atomic writes and can coordinate rebuilds;</li>\n<li>the manage UI has a Settings action and modal;</li>\n<li>the current workflow selects only the first editable field from the first returned scope, so it is not yet a complete multi-scope, multi-field, or application-settings surface;</li>\n<li><code>default_doc_id</code> is currently the only editable scope field and <code>recent_limit</code> is explicitly deferred until global settings are supported.</li>\n</ul>\n<p>Completing this surface should extend that contract rather than replace it with form-specific write logic. The UI should group fields by ownership and selected scope, submit only changed values, report their effects before confirmation when material, and refresh effective configuration after a successful write.</p>\n<h2>Proof Of The Boundary</h2>\n<p>The extraction is not complete merely because a package can be assembled. A standalone installation must be able to:</p>\n<ol>\n<li>start without a repository checkout, repository-shaped directory, or separately installed Python;</li>\n<li>survive complete replacement of the install while preserving and, when required, migrating application configuration;</li>\n<li>create or open an external-local scope;</li>\n<li>browse, search, edit, move, and rebuild its documents;</li>\n<li>manage scope-owned local media;</li>\n<li>complete one direct import and one direct export;</li>\n<li>inspect and change its allowlisted application and scope settings without editing JSON;</li>\n<li>report only capabilities that are actually present;</li>\n<li>operate with repository, public-projection, R2, Data Sharing, and Docs Review modules absent from the distribution.</li>\n</ol>\n<p>The integrated application must continue to compose the same core with its additional providers and workflows. That is the matching proof that extraction produced a shared product boundary rather than moving behavior into a standalone fork.</p>\n<h2>Benefit To The Integrated Application</h2>\n<ul>\n<li>Data Sharing and Docs Review become explicit integrations instead of ambient Docs Viewer context.</li>\n<li>Repository storage, R2, public projection, and site publishing become attached capabilities rather than assumptions inside ordinary management.</li>\n<li>Builders and mutations can be tested against an explicit application environment.</li>\n<li>Configuration becomes classifiable by owner, authority, mutation path, and projection instead of accumulating in one broad config tree.</li>\n<li>Users gain one supported settings surface that makes those ownership boundaries visible in both compositions.</li>\n<li>Startup and capability reporting become truthful about which components were assembled.</li>\n<li>Optional integrations can fail or be unavailable without making the core application unimportable.</li>\n<li>Future work gains one dependency rule: integrations depend on core; core does not depend on integrations.</li>\n</ul>\n<h2>Delivery And Licence Model</h2>\n<p>The public dotlineform repository remains the canonical source, collaboration space, and release platform for both compositions. The standalone release may be built from files whose canonical source lives anywhere appropriate in that repository; self-contained means only that the installed artifact bundles its runtime and dependencies and resolves no files from a checkout.</p>\n<p>The repository currently uses CC0 1.0 Universal. Confirm that this remains the intended software-distribution basis before the first packaged release. Licence governance is repository-wide and is not owned by this feature.</p>\n<h2>Python Runtime And Release Package</h2>\n<p>End users should not need to install or select Python. A GitHub audience may commonly have Python, but the application cannot safely assume the required version, architecture, environment, compiled dependencies, or command-path behavior. The standalone release should bundle a pinned CPython runtime and its complete runtime dependency set.</p>\n<p>The first packaging target should be a one-folder application bundle rather than a self-extracting one-file executable:</p>\n<ul>\n<li>the folder naturally contains the launcher, embedded runtime, Python extensions, browser assets, immutable defaults, schemas, and migration code;</li>\n<li>replacing that folder matches the intended download-and-overwrite upgrade;</li>\n<li>static assets and collected dependencies remain inspectable and easier to diagnose;</li>\n<li>startup does not need to unpack executable support files into a temporary directory;</li>\n<li>the package can carry third-party licence notices and a machine-readable dependency inventory beside the application.</li>\n</ul>\n<p>This is a user-facing application distribution, not a portable Python development environment. Do not expose the embedded interpreter as a general <code>python</code> command, ask users to run <code>pip</code>, or share packages with a system environment. Repository development may continue to use the configured project Python and development requirements independently of the packaged runtime.</p>\n<p>The standalone composition needs its own pinned runtime dependency manifest. It should include only packages reachable from core and standalone integrations. Repository-wide development, test, spreadsheet, Data Sharing, public-publishing, and other integrated-only dependencies must not enter the bundle merely because they appear in the repository <code>requirements.txt</code>.</p>\n<p>Current process execution requires architectural follow-through. <code>docs_write_rebuild.py</code> and <code>docs_live_rebuild_watcher.py</code> use <code>sys.executable</code> to launch builder scripts. In a frozen application <code>sys.executable</code> identifies the application launcher rather than a separately callable Python interpreter. Builders should therefore become importable core services invoked in-process, or explicit internal subcommands dispatched by the same packaged executable when process isolation is genuinely required. The package must not depend on loose <code>.py</code> entrypoints being executable by an external Python.</p>\n<p>Build one release artifact per supported operating-system and architecture combination using that target environment. GitHub Releases can carry the resulting archives, checksums, version metadata, and notices. Replacing the application folder upgrades the bundled Python and dependencies together; the separate app-data migration contract then advances mutable configuration.</p>\n<p>PyInstaller one-folder mode is the leading first implementation candidate because it bundles its active interpreter and dependencies and fits this folder-replacement model. That tool choice should be proven with a packaging spike rather than made part of the core architecture. A native installer or application-bundle tool can be considered later if platform integration, signing, or distribution requirements justify the extra lifecycle.</p>\n<h2>Refactor Execution</h2>\n<p>Run the extraction in an isolated feature worktree while the normal checkout remains the stable application and documentation control plane. The root-level <a href=\"/docs/?scope=studio&amp;doc=d-20260718-114645-7b9d24\">Docs Viewer Core Refactor</a> parent owns main-based planning and handoffs; its working method owns branch flow, local runtimes, document and generated-output ownership, checkpoints, and controlled integration.</p>\n<h2>Risks And Guardrails</h2>\n<ul>\n<li>A broad file move can create the appearance of a core without changing dependency direction. Prove boundaries through imports, composition, and clean-environment behavior.</li>\n<li>A global standalone-mode flag would preserve coupling and create two behaviors inside every workflow. Branch only at composition roots and registered integrations.</li>\n<li>A generic plugin platform would make this larger without answering the known product boundary. Extract only seams required by the two real compositions.</li>\n<li>A generic configuration editor would expose implementation structure as product UI and make generated or policy-owned fields appear safely editable. Render only registered user settings.</li>\n<li>Requiring system Python would turn user environment repair into an application support contract and make releases non-reproducible. Bundle and patch one runtime with the application.</li>\n<li>Bundling the repository-wide requirements would obscure the standalone dependency boundary and enlarge its security/update surface. Produce a dedicated runtime dependency closure.</li>\n<li>Packaging can distract from core extraction. First start a standalone composition from the repository against one external-local scope; package it only after that dependency boundary is true.</li>\n<li>Mutable state under the install root would make overwrite upgrades destructive. Treat install replacement and app-data migration as separate operations from the first standalone slice.</li>\n<li>Treating the repository root as every logical root would leave the integrated application structurally different from standalone and keep repository assumptions inside core. Apply the same role contracts to both compositions.</li>\n<li>Two copied implementations would drift in security, validation, generated contracts, and management behavior. Shared capabilities must retain one authority.</li>\n<li>Existing repository behavior can regress during extraction. Recompose the integrated application through the new boundary before treating standalone success as sufficient.</li>\n</ul>\n<h2>Direction Before A Delivery</h2>\n<p>The likely order is to freeze the capability/dependency map, introduce an explicit application environment and install/data roots, separate optional registrations, recompose the integrated application without product changes, start the external-local-only composition, and finally package and verify replacement plus migration outside a checkout.</p>\n<p>That order is architectural direction, not a delivery checklist. A proposed architecture should name the exact ownership boundaries and a first delivery should end in one complete, independently verifiable outcome.</p>\n<p>The <a href=\"/docs/?scope=studio&amp;doc=d-20260511-120352-90ffa6\">Standalone feature parent</a> owns packaging and release navigation. <a href=\"/docs/?scope=studio&amp;doc=d-20260718-114645-7b9d24\">Docs Viewer Core Refactor</a> owns extraction, main-based coordination, and development isolation. The <a href=\"/docs/?scope=studio&amp;doc=d-20260714-234030-1e0af6\">Docs Viewer Roadmap</a> owns scheduling and dependencies.</p>\n"
}
