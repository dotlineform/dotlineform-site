{
  "doc_id": "d-20260419-000000-cb98dd",
  "title": "Bulk Add Work",
  "added_date": "2026-04-19 00:00:00",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260419-000000-cb98dd",
  "parent_id": "d-20260423-000000-d015e6",
  "content_html": "<h1>Bulk Add Work</h1>\n<h2>What It Does</h2>\n<p><code>/studio/bulk-add-work/</code> imports new Works or Work details from the configured workbook into canonical catalogue JSON.</p>\n<p>The workbook path comes from the runtime projection of <code>_data/pipeline.json</code>; its current default is <code>data/works_bulk_import.xlsx</code>.</p>\n<p>This is a one-way create workflow:</p>\n<ul>\n<li>imported records are drafts;</li>\n<li>existing IDs are duplicates and are not updated;</li>\n<li>the workbook is never uploaded, edited, or written by Studio;</li>\n<li>schema changes are explicit importer/pipeline changes, not automatically inferred from new columns.</li>\n</ul>\n<h2>Preview And Apply</h2>\n<ol>\n<li>Choose <code>works</code> or <code>work_details</code> mode.</li>\n<li>Preview reads and validates the configured workbook without writing.</li>\n<li>The result separates importable, duplicate, and blocked rows and explains blocked reasons.</li>\n<li>Apply remains disabled while blockers exist.</li>\n<li>Apply repeats the plan against the current workbook, writes only importable new records, refreshes derived lookups, and records aggregated Studio Activity.</li>\n</ol>\n<p>Works require their referenced Series to exist. Work details require their parent Work and a valid section resolution. The server owns workbook parsing, field eligibility, source validation, and transaction behavior.</p>\n<h2>Ownership And Weak Spots</h2>\n<p><code>studio/app/frontend/js/bulk-add-work.js</code> coordinates the route. <code>bulk-add-work-workflow.js</code> owns workflow state and result projection. Browser endpoints are registered in <code>studio-transport.js</code>; Local Studio dispatches them through <code>studio_catalogue_api.py</code> to <code>catalogue_workbook_import.py</code> and the catalogue import service.</p>\n<p>The workbook is intentionally an operational input rather than a general interchange format. The import flow creates records but does not try to model every editor capability, publication step, or media workflow. That narrow boundary prevents spreadsheet shape from becoming the catalogue architecture.</p>\n<p>The route uses <code>#bulkAddWorkRoot</code> for the shared <a href=\"/docs/?scope=studio&amp;doc=d-20260625-162242-b0b6c7\">Route Ready State</a>. Preview and apply are the route-level busy operations.</p>\n"
}
