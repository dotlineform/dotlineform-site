{
  "doc_id": "d-20260719-202836-1769ad",
  "title": "Scope-First Storage Cutover Delivery",
  "added_date": "2026-07-19 20:28:36",
  "last_updated": "2026-07-19 21:29:46",
  "viewer_url": "/docs/?scope=studio&doc=d-20260719-202836-1769ad",
  "parent_id": "d-20260719-144956-cb5cb4",
  "summary": "Move every configured scope to one provider-neutral source and working-publication container without changing logical identities, public projections, or R2 object keys.",
  "ui_status": "active",
  "content_html": "<h1>Scope-First Storage Cutover Delivery</h1>\n<h2>Outcome</h2>\n<p>Every configured Docs Viewer scope uses one explicit repository or external-local scope root containing its canonical <code>source/</code> and working <code>published/</code> roles. Documents, search, references, source build inputs, and locally held media resolve through that root by artifact class. Scope and sub-scope lifecycle operations, builders, management reads, media publication, and local serving use the same configuration-owned location model. Reproducible application-level data that no scope owns resolves beneath the separate <code>docs-viewer/data/generated/</code> boundary.</p>\n<p>The cutover preserves document ids, Markdown tokens, management service and public routes, served media references, public document/search projections, and R2 object keys. It intentionally changes the registered manage-only semantic target lookup URL while preserving that artifact's schema and behavior. It removes the old role-first local trees after verified migration and adds no compatibility paths or duplicate canonical locations.</p>\n<h2>Why This Is One Delivery</h2>\n<p>The current config and runtime can describe source, documents, search, and media separately, but create, validation, manifest, rename, delete, fixtures, and some consumers still encode role-first roots. Moving bytes before those owners agree would leave a partially usable scope; retaining both layouts would create the compatibility layer this feature rejects.</p>\n<p>This delivery therefore owns one complete configuration, code, and byte cutover. Its checkpoints are implementation order inside that result, not separately shippable storage modes.</p>\n<h2>In Scope</h2>\n<ul>\n<li>add one explicit local scope-root location for each configured repository or external-local scope;</li>\n<li>derive or validate <code>source/</code> and working <code>published/</code> artifact locations beneath that root without repeating provider policy in consumers;</li>\n<li>place local published media beside <code>documents/</code> and <code>search/</code> beneath <code>published/</code>;</li>\n<li>keep sub-scopes one level deep and stop creating an unsupported nested <code>sub-scopes/</code> directory beneath a sub-scope;</li>\n<li>update config validation, provider-neutral resolution, manifests, create, rename, delete, sub-scope lifecycle, builders, management reads, media publication, watcher behavior, and serving to use the new model;</li>\n<li>migrate Studio, Library, Analysis, Moments, Processing, external-local Notes, and the Analysis <code>tags</code> sub-scope;</li>\n<li>introduce <code>docs-viewer/data/generated/</code> as the narrow home for reproducible non-scope application data and move the semantic-reference target lookup beneath it;</li>\n<li>preserve and verify canonical source and locally published media bytes, then rebuild consumer-ready document, search, reference, and registered media-producer output at the configured destinations;</li>\n<li>remove retired role-first roots only after the new locations and consumer contracts pass focused verification;</li>\n<li>update the durable source-organisation, scope-lifecycle, generated-data, and configuration owners with shipped behavior.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>moving <code>site/assets/data/docs/scopes/&lt;scope&gt;/</code>, <code>site/assets/data/search/&lt;scope&gt;/</code>, or R2 <code>docs/&lt;scope&gt;/...</code> destinations;</li>\n<li>changing public URLs, <code>/docs/</code> service routes, media tokens, <code>doc_id</code>, parent relationships, payload schemas, search behavior, or exposure policy;</li>\n<li>changing the semantic-reference target lookup schema, rows, matching behavior, or manage-only access boundary;</li>\n<li>creating speculative <code>data/canonical/</code>, runtime-state, cache, staging, or other application-data classes without a concrete owner;</li>\n<li>adding R2 document source or document/search publication;</li>\n<li>adding a persistent build cache, storage-version history, automatic remote cleanup, or general backup product;</li>\n<li>changing imported-versus-Mermaid-built SVG identity and collision behavior;</li>\n<li>recursive sub-scopes, sub-scope media authoring, or unrelated lifecycle features;</li>\n<li>retaining a dual-schema loader, old-path fallback, symlink, copy, route alias, or read-through compatibility layer after cutover.</li>\n</ul>\n<h2>Settled Decisions</h2>\n<ul>\n<li>Scope configuration owns exactly one repository or external-local root for the local scope workspace. Source and working-publication paths are confined descendants of that root; public projections and R2 media remain explicit external destinations.</li>\n<li>The stable local hierarchy is <code>&lt;scope-root&gt;/source/&lt;artifact-class&gt;</code> and <code>&lt;scope-root&gt;/published/&lt;artifact-class&gt;</code>. Build is a transformation, not a persistent sibling role.</li>\n<li>Local published media is <code>published/media/&lt;type&gt;</code>, not <code>published/documents/media/&lt;type&gt;</code>. Scope-owned document reference artifacts remain under <code>published/documents/references/</code>.</li>\n<li>Repository-wide generated artifacts are registered separately rather than placed under an arbitrary scope. The semantic-reference target lookup moves to <code>docs-viewer/data/generated/semantic-references/target-lookup.json</code> and its registry URL becomes <code>/docs-viewer/data/generated/semantic-references/target-lookup.json</code>.</li>\n<li><code>docs-viewer/data/</code> is the tracked application-level data boundary for artifacts that no scope owns. This delivery creates only <code>generated/</code>; config, scope content, and machine-local operational state retain their existing owners.</li>\n<li>The config schema changes once. Production code does not accept old and new scope-layout schemas concurrently.</li>\n<li>Migration uses a write-free preflight, collision refusal, temporary staging where byte verification needs it, one explicit config/code cutover, and post-cutover removal of old roots. Temporary staging is not a second canonical location.</li>\n<li>Public projections and R2 objects are verified as stable dependencies, not copied into the new local scope root.</li>\n<li>Detailed path and byte inventories stay in session evidence or a temporary linked working note if they must survive across sessions; this delivery records only durable decisions and gates.</li>\n</ul>\n<h2>Code Authority To Inspect</h2>\n<ul>\n<li>scope model and policy: <code>docs-viewer/config/scopes/docs_scopes.json</code> and <code>docs-viewer/services/docs_scope_config.py</code>;</li>\n<li>provider boundary: <code>docs-viewer/services/docs_artifact_locations.py</code>;</li>\n<li>manifest and lifecycle: <code>docs_scope_manifest.py</code>, <code>docs_scope_create.py</code>, <code>docs_scope_rename.py</code>, <code>docs_scope_delete.py</code>, <code>docs_sub_scope_lifecycle.py</code>, and <code>docs_scope_external_validation.py</code>;</li>\n<li>document, search, reference, and browser-config builders under <code>docs-viewer/build/docs_builder/</code> plus <code>docs-viewer/build/build_search.py</code>;</li>\n<li>media and build-source consumers: <code>docs_media_storage.py</code>, <code>docs_import_media.py</code>, <code>docs_mermaid_media.py</code>, <code>docs_staged_media_service.py</code>, and <code>docs_live_rebuild_watcher.py</code>;</li>\n<li>management route/static-file resolution and generated runtime configuration under <code>docs-viewer/services/</code>, <code>docs-viewer/runtime/</code>, and <code>site/docs-viewer/</code>;</li>\n<li>focused config, builder, media, lifecycle, route, and fixture contracts under <code>docs-viewer/tests/</code>.</li>\n</ul>\n<p>These are responsibility areas, not a permanent file inventory. Literal old paths found in active code must be removed or routed through the owning config helper; examples and assertions should describe only the new contract after cutover.</p>\n<h2>Delivery Tracker</h2>\n<p><strong>Current state:</strong> active. SFL-1 froze the current repository, external-local, public, R2, and sub-scope reality and settled the root-derived config and cutover contract. No scope config or scope bytes have changed. Generated follow-through exposed and repaired two builder/watcher reliability defects before SFL-2.</p>\n<p><strong>Next checkpoint:</strong> SFL-2 — Scope Root Model And Consumer Cutover, pending approval.</p>\n<p><strong>Temporary working note:</strong> <a href=\"/docs/?scope=studio&amp;doc=d-20260719-205459-c7e58a\">Scope-First Storage Cutover Working Notes</a> carries the detailed inventory, config shape, move map, and failure/removal gates. It is disposable at delivery closeout.</p>\n<h3>[x] SFL-1 — Reality Freeze And Cutover Contract</h3>\n<ul>\n<li>[x] Inventory every configured source, working-published, published-media, public-projection, R2, and sub-scope location for the six scopes.</li>\n<li>[x] Record the exact source and destination move set, file counts, byte totals or hashes, configured build outputs, empty marker directories, and destination collisions without writing.</li>\n<li>[x] Confirm the live external-local workspace is readable and writable; do not begin cutover from a substitute root when real Notes bytes are in scope.</li>\n<li>[x] Attribute unregistered or repo-wide artifacts before deciding whether they move; route the semantic target lookup to <code>data/generated/</code> and exclude incidental filesystem metadata.</li>\n<li>[x] Freeze unchanged logical contracts: ids, tokens, served references, public paths, R2 keys, payload schemas, and service/route URLs; record the semantic target lookup data URL as the deliberate exception.</li>\n<li>[x] Define the new config shape, resolver invariants, schema cutover, migration failure boundary, and exact removal gate before editing bytes.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> the clean inventory found no symlinks or occupied targets, confirmed the live Notes workspace and all three old role roots readable and writable, attributed tracked markers and local media, and froze public/R2 destinations. The accepted SFL-2 contract is one <code>docs_scopes_v3</code> <code>scope_root</code>, fixed root-relative local artifact classes, explicit independent public/R2 destinations, and no dual-schema or old-path reader.</p>\n<p><strong>Follow-through repair:</strong> direct document/search builder entrypoints now load the repository-local environment before scope configuration imports. Targeted rebuild fallback now checks the current canonical source set, so a source document absent from the old index or generated payloads triggers a full build instead of failing the targeted builder prerequisite. Focused builder, write/rebuild, and watcher regressions cover both cases.</p>\n<h3>[ ] SFL-2 — Scope Root Model And Consumer Cutover</h3>\n<ul>\n<li>[ ] Add the explicit scope-root model and enforce provider, confinement, source/published non-overlap, artifact-class, media-target, and public-projection rules.</li>\n<li>[ ] Replace hard-coded <code>docs-viewer/source</code>, <code>docs-viewer/published/docs</code>, and <code>docs-viewer/published/search</code> construction in active production owners with normalized config/resolver calls.</li>\n<li>[ ] Make document/search builders, reference generation, browser-config generation, management reads, media publication, Mermaid builds, watcher invalidation, and local media serving resolve the new destinations.</li>\n<li>[ ] Move the semantic target lookup builder default and registry URL to <code>docs-viewer/data/generated/semantic-references/target-lookup.json</code> while making its configured scope inputs follow the new published-document paths.</li>\n<li>[ ] Update fixtures and focused tests directly to the new schema; add no old-schema acceptance path.</li>\n</ul>\n<h3>[ ] SFL-3 — Lifecycle And Sub-Scope Cutover</h3>\n<ul>\n<li>[ ] Make scope create materialize one root and only configured source/published branches.</li>\n<li>[ ] Make rename treat the enclosing scope container as the local move boundary while updating registered public/R2 identities only where existing lifecycle policy requires it.</li>\n<li>[ ] Make delete preview and apply enumerate the root plus independently registered projections or remote artifacts without deriving role-first paths.</li>\n<li>[ ] Make manifest/backfill records express the scope root and registered artifacts without treating a source root or published-documents root as the scope container.</li>\n<li>[ ] Derive the Analysis <code>tags</code> source, document publication, and search publication from its parent scope while preserving its current public projection.</li>\n<li>[ ] Stop materializing nested sub-scope containers and prove configured sub-scopes remain one-level leaf document sets.</li>\n</ul>\n<h3>[ ] SFL-4 — Verified All-Scope Byte Migration</h3>\n<ul>\n<li>[ ] Re-run preflight immediately before mutation and refuse any unexpected destination, collision, missing source, or changed inventory.</li>\n<li>[ ] Move repository canonical source and local published trees into <code>docs-viewer/scopes/&lt;scope&gt;/</code>; stage and byte-verify the external-local Notes move beneath <code>$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/scopes/notes/</code>.</li>\n<li>[ ] Move every local published media branch intact so direct-published bytes are preserved; rerun registered producers afterward so reproducible outputs are refreshed by their owner.</li>\n<li>[ ] Switch the scope config and active consumers to the new roots without an interval in which both layouts are accepted as canonical.</li>\n<li>[ ] Rebuild document, search, reference, and registered media-producer output at the new working destinations and verify source/media inventories against the preflight record.</li>\n<li>[ ] Regenerate the semantic target lookup beneath <code>docs-viewer/data/generated/</code> and remove its old <code>docs-viewer/published/semantic-references/</code> location.</li>\n<li>[ ] Confirm public projections and R2 objects stayed at their existing destinations and logical references still resolve.</li>\n<li>[ ] Remove the old role-first roots and obsolete empty source-media or nested-sub-scope markers only after all post-cutover checks pass; no top-level <code>docs-viewer/source/</code> or <code>docs-viewer/published/</code> remains.</li>\n</ul>\n<h3>[ ] SFL-5 — Verification And Closeout</h3>\n<ul>\n<li>[ ] Validate the config and run focused resolver, builder, search, semantic-reference, media, lifecycle, external-local, and route/service tests.</li>\n<li>[ ] Prove full and targeted builds, management document/search reads, local media serving, Mermaid rebuild, create/rename/delete preview and apply in isolation, and the Analysis <code>tags</code> sub-scope.</li>\n<li>[ ] Prove catalogue follow-through rebuilds the semantic target lookup and the source-editor picker loads it from the new registered URL.</li>\n<li>[ ] Verify the public Library, Analysis, and Moments payload/media routes without changing their deployment destinations.</li>\n<li>[ ] Scan active code, config, runtime projections, tests, and manifests for retired role-first assumptions; keep only historical prose that is explicitly labelled current-before-cutover.</li>\n<li>[ ] Confirm no old roots, fallback code, aliases, duplicate canonical bytes, migration staging, or disposable migration helpers remain.</li>\n<li>[ ] Update <a href=\"/docs/?scope=studio&amp;doc=d-20260423-000000-8fd731\">Source Organisation</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260515-000000-f43740\">Scope Lifecycle</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260605-125108-c68916\">Generated Data Contracts</a>, and <a href=\"/docs/?scope=studio&amp;doc=d-20260512-142027-54b354\">Configuration And Extension Points</a> with shipped behavior.</li>\n<li>[ ] Mark the feature, delivery, and roadmap done only after the complete repository and external-local result is verified.</li>\n</ul>\n<h2>Done When</h2>\n<ul>\n<li>every configured repository or external-local scope has one explicit scope root containing its source and working publication;</li>\n<li>source documents, source build media, local documents, local search, reference artifacts, and local published media occupy the agreed artifact-class paths;</li>\n<li>all active consumers and lifecycle operations resolve those locations through config rather than role-first constants;</li>\n<li>Analysis <code>tags</code> remains a one-level leaf sub-scope and new lifecycle operations do not create recursive sub-scope directories;</li>\n<li>canonical source and direct-published media match the preflight inventory, and reproducible output rebuilds successfully at the new locations;</li>\n<li>public projections, R2 keys, service routes, logical tokens, and document identities are unchanged;</li>\n<li>the semantic target lookup retains its schema and behavior at <code>docs-viewer/data/generated/semantic-references/target-lookup.json</code>;</li>\n<li>old role-first roots and compatibility mechanisms are absent;</li>\n<li>focused repository and real external-local verification passes;</li>\n<li>durable documentation describes only the shipped scope-first layout and its independently registered destinations.</li>\n</ul>\n<h2>Durable Documentation Boundary</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260423-000000-8fd731\">Source Organisation</a> owns the stable hierarchy and source/build/published/location model. <a href=\"/docs/?scope=studio&amp;doc=d-20260515-000000-f43740\">Scope Lifecycle</a> owns create, rename, delete, and sub-scope consequences. <a href=\"/docs/?scope=studio&amp;doc=d-20260605-125108-c68916\">Generated Data Contracts</a> owns document, search, reference, and repo-wide generated payload locations. <a href=\"/docs/?scope=studio&amp;doc=d-20260512-142027-54b354\">Configuration And Extension Points</a> owns the root and external-destination schema without reproducing the migration sequence.</p>\n"
}
