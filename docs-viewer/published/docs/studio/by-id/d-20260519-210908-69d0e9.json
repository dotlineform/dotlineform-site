{
  "doc_id": "d-20260519-210908-69d0e9",
  "title": "Search Build Pipeline",
  "added_date": "2026-05-19 21:09:08",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260519-210908-69d0e9",
  "parent_id": "d-20260331-000000-5dcf32",
  "content_html": "<h1>Search Build Pipeline</h1>\n<p>Both search domains follow the same high-level flow:</p>\n<pre><code class=\"language-text\">canonical source -&gt; domain projection -&gt; search builder -&gt; static JSON index -&gt; browser runtime\n</code></pre>\n<p>The shared shape is a convention, not a shared build service.</p>\n<h2>Catalogue</h2>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 studio/services/catalogue/search/build_search.py --scope catalogue\n$HOME/miniconda3/bin/python3 studio/services/catalogue/search/build_search.py --scope catalogue --write\n</code></pre>\n<ul>\n<li>input: generated public catalogue indexes plus per-work public payloads</li>\n<li>dependency registry: <code>studio/services/catalogue/search/build_config.json</code></li>\n<li>output: <code>site/assets/data/search/catalogue/index.json</code></li>\n<li>runtime: <code>site/assets/js/catalogue-search.js</code> and <code>site/assets/js/search/catalogue-search-runtime.js</code></li>\n</ul>\n<p>Targeted Catalogue updates use <code>--only-records kind:id</code>. Current config is additive-only: a new work or series may be inserted, while changes to an existing record require a full Catalogue search rebuild.</p>\n<h2>Docs Viewer</h2>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 docs-viewer/build/build_search.py --scope studio\n$HOME/miniconda3/bin/python3 docs-viewer/build/build_search.py --scope studio --write\n</code></pre>\n<ul>\n<li>input: Markdown front matter from the configured scope source</li>\n<li>scope/output registry: <code>docs-viewer/config/scopes/docs_scopes.json</code></li>\n<li>local/published output: configured per scope</li>\n<li>runtime: shared Docs Viewer search modules under <code>site/docs-viewer/runtime/js/shared/</code></li>\n</ul>\n<p>Targeted Docs updates use <code>--only-doc-ids</code>. Deleting, renaming, or making a target non-viewable also requires <code>--remove-missing</code>. The local watcher calculates affected ids and falls back to a full same-scope build when it cannot prove a safe target set.</p>\n<h2>Build Guarantees</h2>\n<ul>\n<li>dry-run is the default</li>\n<li><code>--write</code> persists output</li>\n<li>content hashes avoid unnecessary rewrites unless <code>--force</code> is supplied</li>\n<li>each builder validates the minimum structure it owns before output</li>\n<li>public publishing paths are explicit config; a local scope does not become public merely because it has a search index</li>\n</ul>\n<h2>Change Guide</h2>\n<ul>\n<li>source field or record shape: start in the domain builder and its config</li>\n<li>ranking or match behaviour: start in the domain runtime</li>\n<li>scope/output path: start in Docs scope config or Catalogue public-path/build config</li>\n<li>UI timing/copy: start in Catalogue search policy or Docs Viewer config/controller</li>\n<li>write orchestration: inspect the owning catalogue build command or Docs watcher/service</li>\n</ul>\n<p>Do not maintain field tables or score tables here. Read the builder/runtime when exact behaviour matters.</p>\n"
}
