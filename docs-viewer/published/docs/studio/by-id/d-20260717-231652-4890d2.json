{
  "doc_id": "d-20260717-231652-4890d2",
  "title": "Extract Imported SVG To Published Media Delivery",
  "added_date": "2026-07-17 23:16:52",
  "last_updated": "2026-07-17 23:16:52",
  "viewer_url": "/docs/?scope=studio&doc=d-20260717-231652-4890d2",
  "parent_id": "d-20260717-231652-d6d49e",
  "summary": "Extend Docs Import media planning so sanitized standalone and HTML-inline SVG becomes published image media with a Markdown token.",
  "ui_status": "planned",
  "content_html": "<h1>Extract Imported SVG To Published Media Delivery</h1>\n<h2>Outcome</h2>\n<p>Standalone and HTML-inline SVG imports produce sanitized <code>.svg</code> media at the target scope's configured published <code>img</code> location and compact canonical Markdown image references. No imported SVG markup is retained inline.</p>\n<p>The delivery reuses the existing import media plan, location adapter, media-before-source ordering, canonical identity allocation, and coordinated rebuild boundaries.</p>\n<h2>Delivery Boundary</h2>\n<p>In scope:</p>\n<ul>\n<li>normalize standalone and HTML-inline SVG into one sanitized media intent;</li>\n<li>deterministic target-document-owned SVG identities and alt text derived from safe import context;</li>\n<li>preview of the resulting token, media target, warnings, and collision state without writing;</li>\n<li>repository, external-local, and R2 publication through the configured <code>img</code> location;</li>\n<li>media-before-source apply, byte verification, source token insertion, rebuild follow-through, and bounded failure reporting;</li>\n<li>focused ordinary, overwrite, multi-SVG, unsafe-SVG, provider, collision, and partial-failure tests;</li>\n<li>promotion of the shipped contract into the existing import and media owners.</li>\n</ul>\n<p>Out of scope:</p>\n<ul>\n<li>existing-document migration;</li>\n<li>manual source-editor media insertion;</li>\n<li>Markdown package SVG-link interpretation unless it already reaches the same normalized inline-SVG boundary;</li>\n<li>Mermaid source, rendering, freshness checks, or repository tool installation;</li>\n<li>SVG optimization, visual diffing, or content extraction beyond title/alt assistance.</li>\n</ul>\n<h2>Code Authority To Inspect</h2>\n<ul>\n<li><code>docs-viewer/services/docs_html_markdown.py</code> for SVG parsing, safety warnings, sanitization, and HTML conversion;</li>\n<li><code>docs-viewer/services/docs_import_preview.py</code> for standalone SVG and HTML preview dispatch;</li>\n<li><code>docs-viewer/services/docs_import_media.py</code> for extracted-media planning, deterministic names, tokens, and provider-independent apply;</li>\n<li><code>docs-viewer/services/docs_import_document.py</code> and import service orchestration for media-before-source mutation and rebuild follow-through;</li>\n<li>scope media configuration and artifact-location adapters for published <code>img</code> resolution and byte verification;</li>\n<li>focused Docs Import source-format, media, package, collection, and service tests.</li>\n</ul>\n<p>These are responsibility areas, not a permanent module inventory.</p>\n<h2>Delivery Tracker</h2>\n<p><strong>Current state:</strong> planned; standalone and HTML-inline SVG are currently sanitized but serialized directly into canonical Markdown.</p>\n<p><strong>Next checkpoint:</strong> SVG-1 — define one normalized sanitized SVG media plan and prove preview remains write-free.</p>\n<p>Complete checkpoints in order. Keep detailed fixtures, generated SVG bodies, and command transcripts outside this delivery.</p>\n<h3>[ ] SVG-1 — Normalize Sanitized SVG Media Intent</h3>\n<ul>\n<li>[ ] Reuse the current SVG parser/sanitizer while returning sanitized bytes, title/alt assistance, warnings, and media intent instead of final inline Markdown.</li>\n<li>[ ] Normalize standalone SVG and each HTML-inline SVG through the same media-plan shape.</li>\n<li>[ ] Allocate deterministic target-document-owned identities without writing, including overwrite retargeting and stable multi-SVG ordering.</li>\n<li>[ ] Preview the resulting logical <code>img</code> token, target provider label, collision state, and sanitization warnings without exposing absolute paths or credentials.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> the same safe input and target state produces one deterministic write-free SVG media plan; unsafe content is absent from its candidate bytes.</p>\n<h3>[ ] SVG-2 — Publish Then Write Source</h3>\n<ul>\n<li>[ ] Publish sanitized SVG bytes through the target scope's configured <code>img</code> location adapter and byte-verify the result.</li>\n<li>[ ] Replace standalone or HTML-inline SVG markup with ordinary Markdown image references using the configured logical media prefix.</li>\n<li>[ ] Preserve the established media-before-source ordering so failed publication cannot commit a broken token.</li>\n<li>[ ] Apply existing collision and overwrite authority without silently replacing an owned artifact.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> repository, external-local, and R2 fixtures produce the same logical token while only the configured physical location changes.</p>\n<h3>[ ] SVG-3 — Integrate Import And Failure Semantics</h3>\n<ul>\n<li>[ ] Carry SVG media plans through ordinary import preview/apply and the shared per-document boundary used by collection imports where applicable.</li>\n<li>[ ] Report sanitization, publication, collision, source-write, and rebuild outcomes separately.</li>\n<li>[ ] Ensure partial media publication or source-write failure leaves an explicit result and never claims an unverified token was committed.</li>\n<li>[ ] Keep imported document identity, timestamps, hierarchy, viewability defaults, and rebuild behavior unchanged apart from SVG representation.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> an SVG publication failure stops the matching source write, while a later rebuild failure does not erase successfully committed source and media.</p>\n<h3>[ ] SVG-4 — Verify, Document, And Close</h3>\n<ul>\n<li>[ ] Prove standalone, HTML-inline, multiple, titled, untitled, unsafe, externally referenced, and malformed SVG cases at the lowest stable layers.</li>\n<li>[ ] Prove deterministic naming, overwrite retargeting, collision refusal, provider-neutral tokens, byte verification, and compact resulting Markdown.</li>\n<li>[ ] Prove existing inline-raster extraction and ordinary image/file import behavior remain unchanged.</li>\n<li>[ ] Update the durable import/media owners, rebuild Studio docs/search, run <code>git diff --check</code>, and run the focused private-path/secret scan.</li>\n</ul>\n<p><strong>Checkpoint evidence:</strong> imported source contains media tokens rather than SVG markup, every referenced SVG exists at its registered published location, and existing non-SVG import behavior remains green.</p>\n<h2>Done When</h2>\n<ul>\n<li>standalone and HTML-inline SVG imports publish sanitized <code>.svg</code> media and write compact logical image tokens;</li>\n<li>unsafe original SVG bytes are never published or committed to canonical Markdown;</li>\n<li>target scope configuration alone chooses repository, external-local, or R2 placement;</li>\n<li>media publication and byte verification precede source mutation;</li>\n<li>naming, collisions, overwrite retargeting, partial failures, and rebuild results are explicit and tested;</li>\n<li>current raster extraction and non-SVG imports remain unchanged;</li>\n<li>durable import and media documentation describes the shipped behavior.</li>\n</ul>\n"
}
