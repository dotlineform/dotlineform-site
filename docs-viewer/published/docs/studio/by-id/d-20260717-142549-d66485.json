{
  "doc_id": "d-20260717-142549-d66485",
  "title": "Source, Build, Published, And Location Concept",
  "added_date": "2026-07-17 14:25:49",
  "last_updated": "2026-07-17 15:00:57",
  "viewer_url": "/docs/?scope=studio&doc=d-20260717-142549-d66485",
  "parent_id": "d-20260717-132231-5eae95",
  "summary": "Separate Docs Viewer artifact roles, build relationships, storage locations, and public exposure for documents and media.",
  "ui_status": "planned",
  "content_html": "<h1>Source, Build, Published, And Location Concept</h1>\n<p>This concept separates semantic roles from physical paths. The complete section below is intended to move intact into the durable Docs Viewer reference documentation when the source and publication model refactor ships.</p>\n<h2>Source, Build, Published, Location, And Exposure</h2>\n<h3>Core Terms</h3>\n<table>\n<thead>\n<tr>\n<th>term</th>\n<th>meaning</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>Document</strong></td>\n<td>The conceptual Docs Viewer entity identified by <code>doc_id</code>, not any one file or rendered representation.</td>\n</tr>\n<tr>\n<td><strong>Source</strong></td>\n<td>The one canonical application/build input retained by Docs Viewer for an artifact that must be transformed. A file is not source merely because it originated elsewhere or sits below a path named <code>source</code>.</td>\n</tr>\n<tr>\n<td><strong>Build</strong></td>\n<td>A manual or automated transformation from source to a published artifact.</td>\n</tr>\n<tr>\n<td><strong>Published</strong></td>\n<td>The one consumer-ready artifact. Published means ready for Docs Viewer to reference, render, serve, or download; it does not mean publicly accessible on the internet.</td>\n</tr>\n<tr>\n<td><strong>Location</strong></td>\n<td>The registered physical root and backing provider where a source or published artifact is held: repository, external-local filesystem, or R2.</td>\n</tr>\n<tr>\n<td><strong>Public exposure</strong></td>\n<td>Whether an artifact is available through a public route or snapshot. Exposure is independent of artifact role and storage provider.</td>\n</tr>\n<tr>\n<td><strong>Rendered view</strong></td>\n<td>The readable or interactive result composed by the viewer from document payloads and referenced published media. It is not the document source.</td>\n</tr>\n</tbody>\n</table>\n<p><code>generated</code> describes provenance: an artifact was produced by a build. It is not the durable umbrella term for consumer-ready artifacts, because images, HTML, files, and other published media may require no generation. A physical directory that is currently named <code>generated/</code> may contain published artifacts, but its name does not define their semantic role.</p>\n<p>For a normal document, the Markdown file is document source, the Docs Viewer build produces published document payloads, and the viewer composes the rendered document or document view. The document itself is the conceptual entity that persists across those representations.</p>\n<h3>Per-Media-Type Contract</h3>\n<p>The source/build/published decision is made per media type within a scope, such as <code>img</code>, <code>files</code>, <code>html</code>, <code>video</code>, or a future build-source type. It is not inferred from a filename suffix alone and is not a single all-or-nothing setting for the scope.</p>\n<p>Each media type has this cardinality:</p>\n<ul>\n<li>zero or one registered source location;</li>\n<li>zero or one registered build relationship;</li>\n<li>exactly one registered published location.</li>\n</ul>\n<p>If no build is required, there is no Docs Viewer source for that media type. The ready bytes are the published artifact and are stored once at the published location. Import, upload, or manual placement puts the bytes there directly; those operations do not create a second source copy.</p>\n<p>If a build is required, there is exactly one source location and exactly one published location. The source is read-only to the build, and the output is written directly to the published location. A source and its output may use the same backing provider, but they remain different artifacts at different registered paths.</p>\n<p>Changing a producer from manual to Docs Viewer automation does not change the source or published identity and should not move files. Two producers must not silently claim the same published identity. An externally originated file is provenance, not automatically Docs Viewer source: it becomes source only when Docs Viewer retains it as the canonical input to a registered build.</p>\n<p>This cardinality governs media bytes. Docs Viewer document and search payloads may still have separately configured working and public projections; those payload-delivery contracts do not justify source/published duplication for media.</p>\n<h3>Roles Do Not Choose Locations</h3>\n<p>The target role model does not choose a storage provider. Source and published locations are configured independently, and the same workflow uses them through a common location contract.</p>\n<p>Current support is narrower:</p>\n<table>\n<thead>\n<tr>\n<th>artifact role</th>\n<th>repository</th>\n<th>external-local filesystem</th>\n<th>R2</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Canonical document source</td>\n<td>Supported.</td>\n<td>Supported.</td>\n<td>Not supported.</td>\n</tr>\n<tr>\n<td>Working document/search build output, currently called generated</td>\n<td>Supported.</td>\n<td>Supported.</td>\n<td>Not supported.</td>\n</tr>\n<tr>\n<td>Published media</td>\n<td>Supported.</td>\n<td>Supported.</td>\n<td>Supported.</td>\n</tr>\n<tr>\n<td>Public document/search snapshots</td>\n<td>Supported beneath <code>site/assets/</code>.</td>\n<td>Not supported.</td>\n<td>Not supported.</td>\n</tr>\n</tbody>\n</table>\n<p>This table classifies artifacts by semantic role, not by current path name. Ready repository or external-local media may currently sit beneath a scope path named <code>source</code>, but it is published media under this model. An R2 upload is likewise a published media artifact, not generated output or canonical source.</p>\n<p>The restrictions are consequences of the current implementation rather than inherent workflow requirements: document source and build output are configured as filesystem paths; R2 exists as a special public-media upload mode; and import/publish code branches on storage modes. The target removes that coupling from the main workflows.</p>\n<p>All three providers can back source or published roles when the selected location adapter supplies the operations required by that workflow:</p>\n<table>\n<thead>\n<tr>\n<th>role</th>\n<th>repository</th>\n<th>external-local filesystem</th>\n<th>R2</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Source</td>\n<td>Target-supported when the canonical build input belongs in the repository.</td>\n<td>Target-supported when the scope and canonical build input belong in the external workspace.</td>\n<td>Target-supported when a canonical build input is intentionally retained in R2 and the workflow's required source operations are available.</td>\n</tr>\n<tr>\n<td>Published</td>\n<td>Target-supported for practical repository-sized artifacts and tracked site delivery.</td>\n<td>Target-supported for artifacts owned by an external-local scope.</td>\n<td>Target-supported for large, established remote, or otherwise impractical repository artifacts.</td>\n</tr>\n</tbody>\n</table>\n<h3>Location Adapter Contract</h3>\n<p>Build, import, inventory, and publish orchestration must not contain separate repository, external-local, and R2 workflows. Each asks a shared location boundary to operate on a logical scope, artifact role, media type, and filename identity.</p>\n<p>That boundary supplies location-specific helpers for:</p>\n<ul>\n<li>resolving and confining repository paths, external workspace paths, or R2 object keys;</li>\n<li>listing, reading, writing, replacing, deleting, statting, and byte-verifying artifacts;</li>\n<li>acquiring authentication and clients where a remote provider requires them;</li>\n<li>creating a temporary local input or staging an output when a builder requires filesystem paths;</li>\n<li>resolving the served reference or URL independently of physical backing;</li>\n<li>reporting supported capabilities before a workflow starts.</li>\n</ul>\n<p>Repository and external-local storage can share filesystem operations while retaining different root and lifecycle policies. R2 supplies the same logical operations through object storage, including any required download/upload staging. Builders and publishers call the common boundary and do not know which adapter fulfilled the request.</p>\n<p>Different artifact roles require different capabilities:</p>\n<table>\n<thead>\n<tr>\n<th>workflow role</th>\n<th>required location capabilities</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Canonical managed document source</td>\n<td>Enumerate, read, create, replace, rename, delete, and observe change.</td>\n</tr>\n<tr>\n<td>Media build source</td>\n<td>Enumerate, read, byte-verify, and provide a local input when the builder requires one; mutation is required only for an owning management/import workflow.</td>\n</tr>\n<tr>\n<td>Published media</td>\n<td>List, stat, read, write/replace, byte-verify, delete under explicit authority, and resolve a served reference.</td>\n</tr>\n<tr>\n<td>Document/search build output</td>\n<td>Write a consistent artifact set, read current output, remove stale output, and expose it to the configured consumer.</td>\n</tr>\n</tbody>\n</table>\n<p>Configuration may reject a role/location combination when an adapter genuinely lacks a required capability. It must not reject it merely because an existing workflow was written around a different provider. For example, R2 cannot become managed document source simply by accepting a URL: management and watcher semantics also need implementations. Once those operations exist, the document workflow should require no R2-specific branch.</p>\n<p>Provider capability is also separate from scope policy. Repo-backed scopes may select repository or R2 per media type regardless of public projection. External-local scopes deliberately keep all owned media in their external folder; that remains a policy constraint even though the common location code can operate on repository and R2 locations elsewhere.</p>\n<h3>Docs Viewer Published Workspace</h3>\n<p>The Docs Viewer-owned local output workspace is a published location, not a statement about how every artifact was produced. Its physical roots are:</p>\n<pre><code class=\"language-text\">docs-viewer/published/docs/&lt;scope&gt;/\ndocs-viewer/published/search/&lt;scope&gt;/\n\n$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/published/docs/&lt;scope&gt;/\n$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/published/search/&lt;scope&gt;/\n</code></pre>\n<p>These replace the current repository and external-local <code>generated/</code> roots. The workspace can contain build-produced document/search payloads and directly placed consumer-ready artifacts without misclassifying the latter as generated. <code>generated</code> remains a valid provenance adjective or payload field where it describes an actual build event; it is not the physical workspace role.</p>\n<p>Scope configuration is the sole topology authority. Production builders, readers, serving code, validators, lifecycle operations, manifests, and browser-config generation resolve the registered published locations through the shared location boundary. They do not construct, require, or validate a literal <code>docs-viewer/generated/</code> or external <code>generated/</code> path. After this cutover, changing a published workspace path requires a configuration change, moving the artifacts, and rebuilding where necessary—not a workflow-code change.</p>\n<p>Configuration distinguishes the Docs Viewer local published projection from optional public exposure instead of overloading generic <code>output</code> and <code>publish_output</code> meanings. Public website snapshots remain beneath conventional <code>site/assets/data/</code> paths, and R2 objects remain at their configured published locations. Neither is moved merely because the Docs Viewer-owned local workspace is renamed.</p>\n<p>Service routes are logical interfaces and do not mirror the directory name. Neutral routes such as <code>/docs/index-tree</code>, <code>/docs/doc</code>, and <code>/docs/search</code> remain valid regardless of backing location. Direct static URLs for repository-published artifacts are derived from configuration. No compatibility path is retained solely for the old physical <code>generated/</code> directory.</p>\n<p>Public scopes may continue to have a local published management projection plus a separately exposed public website snapshot. That document/search projection contract is distinct from the one-published-location rule for media bytes.</p>\n<p>Repository/R2 selection applies to <strong>repo-backed scopes</strong>, regardless of whether a scope has a public projection. Public exposure may affect the serving path, but it does not decide whether repository or R2 is the appropriate backing.</p>\n<p>External-local scopes use the same semantic folder structure as repo-backed scopes, but repository/R2 routing does not apply. All source and published media owned by an external-local scope remains physically inside that external scope folder and follows the scope's create, rename, and delete lifecycle.</p>\n<h3>Folder And Scope Shape</h3>\n<p>A repo-backed scope makes documents, build-source media, and sub-scopes visibly distinct:</p>\n<pre><code class=\"language-text\">docs-viewer/source/&lt;scope&gt;/\n  documents/\n    *.md\n  media/\n    mermaid/\n      *.mmd\n  sub-scopes/\n    &lt;sub-scope&gt;/\n      documents/\n        *.md\n      media/\n        &lt;build-source-type&gt;/\n</code></pre>\n<p><code>documents/</code> contains one flat set of canonical Markdown files. It contains neither the document hierarchy nor structural subdirectories. Document hierarchy remains metadata-owned, while <code>sub-scopes/</code> explicitly marks nested collection ownership instead of making a sub-scope look like a folder within the parent's document collection. A sub-scope repeats the same <code>documents/</code>, <code>media/</code>, and <code>sub-scopes/</code> shape as needed.</p>\n<p>Source <code>media/</code> contains only canonical inputs to registered builds. Directly usable media does not gain a redundant source copy. Its published root is registered separately, for example:</p>\n<pre><code class=\"language-text\">site/assets/data/docs/scopes/&lt;scope&gt;/media/&lt;type&gt;/\nR2: docs/&lt;scope&gt;/&lt;type&gt;/\n</code></pre>\n<p>An external-local scope is a self-contained physical container for both roles:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/source/&lt;scope&gt;/\n  documents/                 # document source\n  media/\n    mermaid/                 # build source, when configured\n    img/                     # published media, when configured\n    files/                   # published media, when configured\n    html/                    # published media, when configured\n  sub-scopes/\n</code></pre>\n<p>The parent directory happens to be named <code>source</code>, but the registered role of each child remains authoritative. Renaming that existing external workspace container is not required to make the role model correct.</p>\n<p>Media folders describe stable format or handling boundaries, not changeable behaviour. HTML belongs under <code>media/html/</code> whether it is static or interactive; sandboxing, script permission, and interactivity are rendering-code concerns. Files do not move because their behaviour changes.</p>\n<h3>Examples</h3>\n<table>\n<thead>\n<tr>\n<th>artifact</th>\n<th>source</th>\n<th>build</th>\n<th>published artifact</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Markdown document</td>\n<td>Canonical <code>.md</code>.</td>\n<td>Docs Viewer document build.</td>\n<td>Document/search payload consumed by the viewer or public route.</td>\n</tr>\n<tr>\n<td>Mermaid diagram</td>\n<td><code>media/mermaid/diagram.mmd</code>.</td>\n<td>Mermaid to SVG, initially manual and later optionally Docs Viewer-owned.</td>\n<td><code>media/img/diagram.svg</code> at the configured <code>img</code> published location.</td>\n</tr>\n<tr>\n<td>JPEG or WebP image</td>\n<td>None.</td>\n<td>None.</td>\n<td>The image placed once at the configured <code>img</code> published location.</td>\n</tr>\n<tr>\n<td>Ready HTML companion</td>\n<td>None.</td>\n<td>None.</td>\n<td>The HTML placed once at the configured <code>html</code> published location; sandbox behaviour is separate.</td>\n</tr>\n<tr>\n<td>PDF, Word file, or archive</td>\n<td>None.</td>\n<td>None.</td>\n<td>The file placed once at the configured <code>files</code> published location.</td>\n</tr>\n<tr>\n<td>Video requiring transcoding</td>\n<td>The retained canonical master.</td>\n<td>Registered transcode.</td>\n<td>The delivery video at the configured <code>video</code> published location.</td>\n</tr>\n<tr>\n<td>Ready video</td>\n<td>None.</td>\n<td>None.</td>\n<td>The delivery video placed directly at the configured <code>video</code> published location.</td>\n</tr>\n<tr>\n<td>Processing or visualization output</td>\n<td>Retained project/build input only when Docs Viewer owns the build.</td>\n<td>Registered export when one exists.</td>\n<td>The resulting HTML, image, video, or bundle at that output type's published location.</td>\n</tr>\n</tbody>\n</table>\n<p>Mermaid therefore does not retain a source SVG when Docs Viewer can reproduce it. During a manual phase, the author renders the <code>.mmd</code> and places the SVG directly in the configured published <code>img</code> location. A later Docs Viewer builder performs that same build-and-write step without changing either path.</p>\n<h3>Ownership And Inventory</h3>\n<p>A scope owns artifacts through its registered roots, not solely through containment beneath <code>docs-viewer/source/&lt;scope&gt;/</code>. Registered roots may include document/build source, repository-published media, R2 prefixes, external-local media, document/search payloads, and public projections.</p>\n<p>The inventory records enough information to make that ownership explicit:</p>\n<ul>\n<li>logical scope, media type, and filename identity;</li>\n<li>semantic role: source or published;</li>\n<li>registered location and backing provider;</li>\n<li>producer/build relationship where one exists;</li>\n<li>document references and served path;</li>\n<li>byte evidence needed for migration or cleanup.</li>\n</ul>\n<p>An owned artifact need not currently be referenced by a document. Conversely, a document reference alone does not prove that Docs Viewer owns the target bytes. External Asset Collections remain independently owned roots even when Docs Viewer inventories or references them.</p>\n<h3>Repository And R2 Policy</h3>\n<p>Storage is a practical, per-media-type decision rather than a purist classification by suffix:</p>\n<table>\n<thead>\n<tr>\n<th>scope and media type</th>\n<th>source</th>\n<th>build</th>\n<th>published location</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Analysis and Library <code>img</code></td>\n<td>None for ready images.</td>\n<td>None.</td>\n<td>Repository-first at <code>site/assets/data/docs/scopes/&lt;scope&gt;/media/img/</code>.</td>\n</tr>\n<tr>\n<td>Moments <code>img</code></td>\n<td>None for ready images.</td>\n<td>None.</td>\n<td>Existing R2 <code>docs/moments/img/</code> contract.</td>\n</tr>\n<tr>\n<td>Repo-backed <code>files</code> such as PDF, Word, and archives</td>\n<td>None for ready files.</td>\n<td>None.</td>\n<td>R2 unless a specific media-type decision says otherwise.</td>\n</tr>\n<tr>\n<td>Ready HTML and SVG of practical repository size</td>\n<td>None.</td>\n<td>None.</td>\n<td>The configured repository-published <code>html</code> or <code>img</code> root.</td>\n</tr>\n<tr>\n<td>Mermaid</td>\n<td>Repository or external-local <code>media/mermaid/</code>.</td>\n<td>Manual first; optionally Docs Viewer-owned later.</td>\n<td>The configured published <code>img</code> root.</td>\n</tr>\n<tr>\n<td>Large video, audio, datasets, or similar media</td>\n<td>Only when a registered build retains an input.</td>\n<td>Optional by type.</td>\n<td>Prefer R2 when repository size is impractical.</td>\n</tr>\n<tr>\n<td>Any external-local media type</td>\n<td>Optional canonical build input within the external scope.</td>\n<td>Optional by type.</td>\n<td>Within the same external scope folder; never redirected to repository or R2.</td>\n</tr>\n</tbody>\n</table>\n<p>An R2 object that needs no build is not both source and published. It is only the published artifact. Likewise, moving a ready Analysis image from R2 into the repository changes its published location; it does not create or move a source artifact.</p>\n<p>Logical scope/type/filename identity stays independent of the provider. A later repository/R2 decision should require only:</p>\n<ol>\n<li>changing the configured published path/provider for that media type;</li>\n<li>moving and byte-verifying the affected artifacts;</li>\n<li>rebuilding dependent document payloads where needed.</li>\n</ol>\n<p>Canonical document source should not need rewriting. Removal of the old copy follows inventory and byte verification; unexplained or uniquely held objects are retained until ownership is established.</p>\n<p>Existing R2 and cache behaviour remains unchanged. R2 is allowed to cache normally, and any exceptional manual cache clearing remains an operator action. This model adds no cache purge, fingerprint, query-version, or automatic invalidation requirement.</p>\n<h3>Build Direction</h3>\n<p>A full Docs Viewer build follows one direction:</p>\n<ol>\n<li>inventory canonical document source, registered media build source, and existing published media;</li>\n<li>run registered media builders from source directly into each configured published location;</li>\n<li>render document payloads against the published media inventory;</li>\n<li>publish document and search projections where configured.</li>\n</ol>\n<p>The build never writes reproducible output back into source. Media requiring no build bypasses the build stage and is inventoried at its published location. This keeps scope ownership, filesystem structure, and future build automation stable without duplicating artifacts or moving them merely because their behaviour or producer changes.</p>\n<h2>Implementation And Promotion Note</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260717-132231-5eae95\">Docs Viewer Source And Publication Model Refactor</a> and its <a href=\"/docs/?scope=studio&amp;doc=d-20260717-132231-8cfc4d\">delivery</a> own implementation. At closeout, copy the complete <strong>Source, Build, Published, Location, And Exposure</strong> section into <a href=\"/docs/?scope=studio&amp;doc=d-20260423-000000-8fd731\">Source Organisation</a>. The focused media, lifecycle, sub-scope, and diagram references should link to or adopt only the consequences they own; this concept should not remain as a competing durable authority after promotion.</p>\n"
}
