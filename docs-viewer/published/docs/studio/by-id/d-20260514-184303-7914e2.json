{
  "doc_id": "d-20260514-184303-7914e2",
  "title": "Media And Asset Handling",
  "added_date": "2026-05-14 18:43:03",
  "last_updated": "2026-07-17",
  "viewer_url": "/docs/?scope=studio&doc=d-20260514-184303-7914e2",
  "parent_id": "d-20260424-000000-50b63f",
  "summary": "Media intake, content interpretation, document representation, storage, link resolution, extension methods, and safety boundaries.",
  "content_html": "<h1>Docs Viewer Media And Asset Handling</h1>\n<p>Media support is not one capability. A useful description must answer five separate questions:</p>\n<ol>\n<li>Can the workflow accept these source bytes?</li>\n<li>Does it interpret the contents or treat the file as an opaque download?</li>\n<li>What document source or link does it create?</li>\n<li>Where are the bytes stored?</li>\n<li>How does the rendered viewer reach them?</li>\n</ol>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260423-155039-e36925\">Docs Images And Assets</a> answers author and operator questions. This document owns the architecture, configuration, extension method, and weak spots.</p>\n<h2>Capability Map</h2>\n<table>\n<thead>\n<tr>\n<th>intake</th>\n<th>content interpretation</th>\n<th>document representation</th>\n<th>asset handling</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Standalone raster image</td>\n<td>No image-content analysis.</td>\n<td>New wrapper document containing an image link.</td>\n<td>Original bytes are published or copied.</td>\n</tr>\n<tr>\n<td>Ordinary PDF, ZIP, CSV, TSV, JSON, JSONL, or supported Office file</td>\n<td>None; the file is opaque.</td>\n<td>New wrapper document containing a download link.</td>\n<td>Original bytes are published or copied.</td>\n</tr>\n<tr>\n<td>Trusted Data Sharing documents JSON/JSONL</td>\n<td>Package metadata and document schema are interpreted.</td>\n<td>Complete collection plan producing canonical documents.</td>\n<td>Embedded raster data URLs can become media; other declared assets need an explicit mapping.</td>\n</tr>\n<tr>\n<td>HTML, Markdown, or text</td>\n<td>Converted or normalized into Markdown content.</td>\n<td>New canonical document.</td>\n<td>Markdown-image data URLs can be extracted as raster media.</td>\n</tr>\n<tr>\n<td>Markdown package folder</td>\n<td>Markdown plus supported local links are interpreted.</td>\n<td>One canonical document with rewritten links.</td>\n<td>Images become WebP; attachments are copied unchanged.</td>\n</tr>\n<tr>\n<td>Standalone SVG</td>\n<td>SVG markup is sanitized.</td>\n<td>New wrapper document containing inline SVG.</td>\n<td>No separate ordinary media object is created.</td>\n</tr>\n<tr>\n<td>Role-marked interactive HTML</td>\n<td>Treated as a companion application asset.</td>\n<td>No automatic source placement; the author adds a token.</td>\n<td>Copied to the sandboxed interactive-assets location, not ordinary media.</td>\n</tr>\n</tbody>\n</table>\n<p>The exact suffix allowlists remain in <code>docs_import_common.py</code>. “Supported import format” should not be used alone: say whether a format is accepted as an opaque file, interpreted as document content, or recognized as a trusted collection.</p>\n<h2>Execution Paths</h2>\n<h3>Imported Media</h3>\n<pre><code class=\"language-text\">staged source\n    |\n    v\nserver classification\n    |\n    +-- interpreted content --------&gt; Markdown / collection plan\n    |\n    +-- standalone image or file ---&gt; wrapper Markdown + media plan\n                                               |\n                                               v\n                                    target-scope storage policy\n                                      |          |          |\n                                     R2    scope-owned    manual\n                                      |     local media      |\n                                      +----------+-----------+\n                                               |\n                                               v\n                                      canonical source write\n                                               |\n                                               v\n                                      build + rendered link\n</code></pre>\n<p>Required R2 publication happens before the source write so import does not commit a new token for media it failed to publish. Repo-backed and external-local media are copied beneath their confined scope source roots. Manual mode returns an instruction rather than pretending the bytes were placed.</p>\n<h3>Authored Links</h3>\n<pre><code class=\"language-text\">Markdown source\n    |\n    +-- https://media.dotlineform.com/docs/&lt;scope&gt;/&lt;class&gt;/&lt;file&gt;\n    |             |\n    |             v\n    |      docs builder + configured media_base -&gt; public media URL\n    |\n    +-- /docs/media/&lt;scope&gt;/&lt;class&gt;/&lt;file&gt;\n                  |\n                  v\n          local service -&gt; configured/derived local media root\n</code></pre>\n<p>Authoring a link does not copy, publish, validate, index, or establish ownership of the target asset.</p>\n<h2>Source Classification</h2>\n<p>Ordinary suffix classification is code-owned by <code>SOURCE_IMPORTERS</code> in <code>docs_import_common.py</code>. <code>file</code> is a generic media source format: its contents are not parsed.</p>\n<p>JSON and JSONL have an intentional precedence rule. <code>docs_import_data_sharing_package.py</code> first checks for a trusted, supported documents export. A match is classified as <code>data_sharing_documents</code>; anything else falls back to generic downloadable <code>file</code>. The same extension therefore supports two different outcomes, but only trusted package identity changes the behavior.</p>\n<p>Markdown package folders and interactive HTML companions are also classified outside the ordinary suffix registry. <a href=\"/docs/?scope=studio&amp;doc=d-20260514-164722-3de65e\">Docs Import Architecture</a> owns the complete source-dispatch and document-planning boundary.</p>\n<h2>Scope Configuration</h2>\n<p><code>docs-viewer/config/scopes/docs_scopes.json</code> supplies the target policy:</p>\n<ul>\n<li><code>media_path_prefix</code>: logical public media namespace, normally <code>docs/&lt;scope&gt;</code></li>\n<li><code>import_media_storage.storage_mode</code>: publication or copy behavior</li>\n<li>repo path/public path prefixes when local repo storage is used</li>\n</ul>\n<p><code>docs_scope_config.py</code> validates combinations before import code consumes them.</p>\n<table>\n<thead>\n<tr>\n<th>storage mode</th>\n<th>permitted role</th>\n<th>link written by import</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>r2_upload</code></td>\n<td>Public read-only scopes.</td>\n<td><code>https://media.dotlineform.com/docs/&lt;scope&gt;/&lt;class&gt;/&lt;filename&gt;</code></td>\n</tr>\n<tr>\n<td><code>repo_assets</code></td>\n<td>Repo-backed local scopes; media is scope-owned under <code>&lt;source root&gt;/media/</code>.</td>\n<td>Configured public path, currently <code>/docs/media/&lt;scope&gt;/...</code></td>\n</tr>\n<tr>\n<td><code>external_assets</code></td>\n<td>External-local scopes; media is scope-owned under <code>&lt;source root&gt;/media/</code>.</td>\n<td><code>/docs/media/&lt;scope&gt;/...</code> path</td>\n</tr>\n<tr>\n<td><code>staging_manual</code></td>\n<td>Explicit manual/portable workflow.</td>\n<td>Media token plus manual copy instruction</td>\n</tr>\n</tbody>\n</table>\n<p>Only two ordinary media classes currently exist: <code>img</code> and <code>files</code>. The class controls the object/path namespace and whether the local service serves a file inline or as an attachment; it is not a MIME-type registry.</p>\n<p>The two local storage-mode names describe how the path is configured, not different lifecycle ownership. Both materialize ordinary Docs Viewer-managed media beneath the scope source root. <code>external_assets</code> is unrelated to <a href=\"/docs/?scope=studio&amp;doc=d-20260714-194217-b2bdd7\">External Asset Collections</a>, which inventory independently named user-owned roots outside <code>source/&lt;scope&gt;/</code> without importing or owning them.</p>\n<p>Configured local directory paths are concrete workspace structure. Confirmed scope creation immediately creates <code>media/</code>, <code>media/img/</code>, and <code>media/files/</code>; local service startup idempotently restores any missing configured directories. Config parsing itself stays read-only. Repo-backed empty class directories contain tracked marker files so they remain present in a clean checkout.</p>\n<h2>Link Resolution And Delivery</h2>\n<p>The docs builder resolves <code>media</code> tokens in <code>docs_builder/rendering.py</code>. Relative token paths are joined to the configured site media base; image tokens may also project validated positive-integer width and height attributes. The build does not contact R2 or prove that an object exists.</p>\n<p><code>docs_media_storage.py</code> owns public R2 object keys, local media roots, and <code>/docs/media/</code> route resolution:</p>\n<ul>\n<li>R2 keys are <code>docs/&lt;scope&gt;/&lt;class&gt;/&lt;filename&gt;</code> and require an exact configured public scope.</li>\n<li>Local routes accept one scope, <code>img|files</code>, and one safe filename.</li>\n<li>Every local media root is exactly <code>&lt;scope source root&gt;/media/</code>; repo roots remain inside the repository and external roots remain inside the configured external workspace.</li>\n<li>Traversal, nested filenames, symlink escape, and interactive HTML through the ordinary media route are rejected.</li>\n<li>The local service sends <code>nosniff</code>; <code>files</code> responses are attachments.</li>\n</ul>\n<h2>Import Materialization</h2>\n<p><code>docs_import_media.py</code> creates the logical link and media plan, then materializes according to scope policy.</p>\n<ul>\n<li>Standalone raster and file imports retain their original bytes.</li>\n<li>Inline raster data URLs are decoded to deterministic <code>&lt;doc_id&gt;-image-NN.&lt;ext&gt;</code> names.</li>\n<li>Markdown package images are converted to WebP, limited to 800px width without upscaling, and rejected if animation would be flattened.</li>\n<li>Package attachments retain their bytes and receive readable <code>&lt;doc_id&gt;-attachment-NN.&lt;ext&gt;</code> names.</li>\n<li>A replacement <code>doc_id</code> retargets generated filenames and source links before write.</li>\n</ul>\n<p>Collection records reuse the same inline-raster and per-document apply paths. A declared package asset without an authorized mapping remains a warning and preserves its source reference; a document overwrite decision does not grant asset overwrite authority.</p>\n<h2>Extension Methods</h2>\n<h3>Accept Another Opaque Download Type</h3>\n<p>Extend the server allowlist only after deciding that storing and linking uninspected bytes is safe. Add listing, preview, content-type, materialization, and route/download tests. Do not describe the extension as content import.</p>\n<h3>Interpret A New Content Format</h3>\n<p>Add a focused parser or trusted wrapper adapter and normalize its output through Docs Import. Do not overload generic file handling or infer structured behavior from an extension alone.</p>\n<h3>Add A Media Class</h3>\n<p>Extend the media-class allowlist, object/path mapping, import representation, local response behavior, and tests together. A class is a delivery contract, not just another folder name.</p>\n<h3>Add A Storage Mode</h3>\n<p>Extend scope-config validation, link selection, materialization/publication, failure semantics, and containment tests. Format converters should continue to emit media intent without choosing physical storage.</p>\n<h3>Add A New Reference Form</h3>\n<p>The docs builder owns source-token resolution; the local/public delivery layer owns the resulting URL. Do not hide a new token inside import-only code.</p>\n<h2>Why This Structure Exists</h2>\n<ul>\n<li>Source classification decides meaning before media storage is involved.</li>\n<li>Scope configuration lets the same imported asset respect public, scope-owned local, or manual boundaries.</li>\n<li>Logical public tokens keep the media origin out of canonical Markdown.</li>\n<li>Confined local routes avoid exposing absolute roots or arbitrary filesystem reads.</li>\n<li>Media-before-source ordering prevents newly generated links from committing ahead of required publication.</li>\n<li>Separate interactive and SVG paths keep executable or markup content out of the ordinary download contract.</li>\n</ul>\n<h2>Weak Spots</h2>\n<ul>\n<li>JSON/JSONL has dual behavior: trusted packages are interpreted, ordinary files are downloads. Picker labels and documentation must preserve that distinction.</li>\n<li>The generic download allowlist proves only suffix acceptance, not content validity, usefulness, or safety beyond download handling.</li>\n<li>A public media token can point at a missing object because build-time resolution is string-based and does not query R2.</li>\n<li>Docs media has no asset registry, reference count, automatic remote deletion, or cache-version contract. Shared or orphaned objects require manual judgment, and changed R2 bytes may remain edge-cached.</li>\n<li><code>staging_manual</code> reports an intended destination but cannot verify that the manual copy happens.</li>\n<li>Interactive companions are discovered across the shared staging root rather than explicitly associated with one selected source.</li>\n<li>The legacy <code>external_assets</code> storage label still sounds like the separate External Asset Collections capability even though it now means ordinary scope-owned external-local media.</li>\n</ul>\n<p>For manual public upload commands and R2 overwrite safety, see <a href=\"/docs/?scope=studio&amp;doc=d-20260508-093841-3e27b1\">Publish Media To R2</a>.</p>\n"
}
