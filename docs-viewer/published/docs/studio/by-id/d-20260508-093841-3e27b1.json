{
  "doc_id": "d-20260508-093841-3e27b1",
  "title": "Publish Media To R2",
  "added_date": "2026-05-08 09:38:41",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260508-093841-3e27b1",
  "parent_id": "d-20260423-000000-d015e6",
  "content_html": "<h1>Publish Media To R2</h1>\n<h2>What It Owns</h2>\n<p><code>studio/services/media/publish_media_to_r2.py</code> previews, uploads, replaces, or exactly deletes approved Cloudflare R2 objects.</p>\n<p>It supports two deliberately different modes:</p>\n<ul>\n<li><strong>catalogue media</strong> publishes complete Work or Work-detail primary variant sets and coordinates confirmed media versions;</li>\n<li><strong>Docs media</strong> publishes one exact staged file to one configured public Docs Viewer scope/class.</li>\n</ul>\n<p>The shared implementation owns credentials, object mapping, remote comparison, upload/delete requests, safe reporting, and write confirmation. The source/workflow rules remain with the catalogue or Docs feature that invokes it.</p>\n<h2>Catalogue Flow</h2>\n<p>Local source is the externally staged primary tree below <code>$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/media/</code>.</p>\n<p>Object families are derived from checked pipeline and site configuration, for example:</p>\n<pre><code class=\"language-text\">works/img/&lt;work_id&gt;-primary-&lt;width&gt;.webp\nwork_details/img/&lt;detail_uid&gt;-primary-&lt;width&gt;.webp\n</code></pre>\n<p>The normal required catalogue widths are resolved from pipeline configuration. A group missing a required width is blocked unless an operator explicitly allows a partial CLI upload; partial groups are never promoted as the confirmed public version.</p>\n<p>Preview one Work, then apply after reviewing it:</p>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 studio/services/media/publish_media_to_r2.py --scope catalogue --kind works --id 01007\n$HOME/miniconda3/bin/python3 studio/services/media/publish_media_to_r2.py --scope catalogue --kind works --id 01007 --write\n</code></pre>\n<p>Changed remote objects require <code>--force</code>. Exact deletion requires <code>--delete --write</code> plus one kind and ID; broad <code>--all</code> deletion is intentionally unavailable.</p>\n<h2>Confirmed Versions</h2>\n<p>Works and details carry positive canonical <code>media_version</code> values. Public primary URLs append the confirmed version as a cache token.</p>\n<ul>\n<li>A complete upload that changes at least one required remote object increments the item's version once.</li>\n<li>An all-current result rebuilds the focused public payload at the existing version.</li>\n<li>Failed, blocked, or partial groups do not advance the version.</li>\n<li>Ordinary source save and local derivative generation do not advance it.</li>\n<li>Work-detail versions are projected inside the owning Work JSON.</li>\n</ul>\n<p>The publisher updates the canonical version only after the complete remote set succeeds, then rebuilds the focused Work payload. This keeps “new local bytes exist” separate from “public clients may safely request a new confirmed version.”</p>\n<h2>Work Editor Composition</h2>\n<p>The Work editor calls importable preview/apply services rather than shelling out or implementing R2 logic in the browser.</p>\n<p>For an eligible saved published Work, <code>Save</code> can continue into media publication. The browser sees compact per-width state and confirmed version only; credentials, local paths, object keys, checksums, and signed requests remain server-side.</p>\n<p>Preview is write-free and returns an opaque fingerprint of the compared local/remote state. Apply repeats version and comparison guards and rejects drift before upload. New publication and replacement require distinct confirmations.</p>\n<p>If the composed media step is cancelled or fails, the canonical source save remains complete and the media action stays retryable. It is not represented as a rollback of the source edit.</p>\n<p>Detail-section creation publishes the newly created complete detail sets after the local build. Section or Work deletion performs the authoritative local transaction first, then attempts exact remote cleanup. Missing objects count as clean; remote failure becomes a visible manual-cleanup warning rather than restoring deleted canonical source.</p>\n<h2>Docs Exact-File Flow</h2>\n<p>Docs publishing reads one direct-child file from the shared import-staging drop zone and maps it to:</p>\n<pre><code class=\"language-text\">docs/&lt;scope&gt;/&lt;img-or-files&gt;/&lt;filename&gt;\n</code></pre>\n<p>Preview and apply one exact file:</p>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 studio/services/media/publish_media_to_r2.py --scope docs --docs-scope library --kind img --staged-filename diagram.png\n$HOME/miniconda3/bin/python3 studio/services/media/publish_media_to_r2.py --scope docs --docs-scope library --kind img --staged-filename diagram.png --write\n</code></pre>\n<p>The Docs scope must explicitly use the matching R2 upload/media-prefix policy. There is no Docs <code>--all</code> or remote-delete mode. Changed objects require <code>--force</code>, though a new filename is preferable when cache-safe replacement matters.</p>\n<p>This command provides a transport capability; it does not decide which Docs assets are supported, how they are linked, or which document owns them. Those rules belong to the Docs media/asset-collection workflow.</p>\n<h2>Credentials And Safety</h2>\n<p>Local runs load R2 settings from <code>.env.local</code>, with optional additional <code>--env-file</code> inputs and environment fallback for hosted runs. Missing variables are named; configured values are never printed.</p>\n<p>The publisher:</p>\n<ul>\n<li>confines discovery to allowlisted staging roots and rejects traversal/symlink escapes;</li>\n<li>defaults to dry-run and requires <code>--write</code> for remote mutation;</li>\n<li>compares size and ETag/MD5 before deciding current, new, or changed state;</li>\n<li>skips current objects and blocks changed ones without explicit force;</li>\n<li>keeps reports/logs to non-secret identifiers and statuses;</li>\n<li>promotes catalogue versions only after complete success.</li>\n</ul>\n<p>Run <code>--help</code> for the exact current flags. <a href=\"/docs/?scope=studio&amp;doc=d-20260331-000000-0a1f71\">Srcset Builder</a> owns derivative creation; <a href=\"/docs/?scope=studio&amp;doc=d-20260418-000000-4ec702\">Scoped JSON Catalogue Build</a> owns local/public build planning.</p>\n"
}
