{
  "doc_id": "d-20260715-181845-524859",
  "title": "Document Visualisations Concept",
  "added_date": "2026-07-15 18:18:45",
  "last_updated": "2026-07-17 22:19:08",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-181845-524859",
  "parent_id": "d-20260715-181845-0158f9",
  "summary": "Concept for documentation diagrams using external authoring tool.",
  "ui_status": "proposed",
  "content_html": "<h1>Document Diagrams Concept</h1>\n<h2>Why Consider It</h2>\n<p>High-level diagrams could make execution paths, ownership, and public/manage/server boundaries easier to understand than prose or text art.</p>\n<h2>Authored-Diagram Decision</h2>\n<p>Use Mermaid as the default source language for authored architecture and workflow diagrams in any Docs Viewer scope.</p>\n<p>Mermaid source is the durable authority. An author-local command renders it to static SVG before Docs Viewer reads the document; Docs Viewer displays the SVG as an ordinary image and does not parse Mermaid or load a diagram runtime in the browser.</p>\n<h2>Two Different Capabilities</h2>\n<h3>Build-Rendered Figures</h3>\n<p>A command turns diagram source into SVG or another static asset before publication. Readers receive the result without downloading the rendering library.</p>\n<p>This improves runtime independence and export behavior. For the current need it adds an author-local tool and generated-asset ownership, but not a CI or repository package dependency.</p>\n<h3>Authored Diagrams</h3>\n<p>Text in Markdown describes a flowchart, sequence, state, relationship, or architecture map. The diagram changes when the author edits that text.</p>\n<p>The <a href=\"https://github.com/mermaid-js/mermaid-cli\">Mermaid CLI</a> is the upstream authority for command behavior. If the repository later gains a wrapper, its supported version and render options become the project authority.</p>\n<h2>Suggested Repository Convention</h2>\n<p>Use the existing configured source and media roots:</p>\n<pre><code class=\"language-text\">&lt;scope source root&gt;/media/mermaid/&lt;diagram-name&gt;.mmd\n&lt;configured published img root&gt;/&lt;diagram-name&gt;.svg\n</code></pre>\n<ul>\n<li>For a repo-backed scope the source maps to <code>docs-viewer/source/&lt;scope&gt;/media/mermaid/</code>.</li>\n<li>The published SVG can live in the public site media root, the local Docs Viewer published-media root, or R2 according to the configured <code>img</code> location.</li>\n</ul>\n<p>Markdown uses the same logical token in every scope: <code>https://media.dotlineform.com/docs/&lt;scope&gt;/img/&lt;diagram-name&gt;.svg</code>. The builder resolves that token through the configured <code>img</code> publication location and emits the corresponding served URL.</p>\n<p>Additional conventions:</p>\n<ul>\n<li>use lowercase kebab-case names describing the diagram, not the rendering tool;</li>\n<li>keep one <code>.mmd</code> source and one <code>.svg</code> output with the same basename;</li>\n<li>do not edit the generated SVG by hand;</li>\n<li>render with explicit background, theme, dimensions, and other material options once a project command exists;</li>\n<li>include useful surrounding prose and alt text; the SVG is not the sole statement of the document's meaning;</li>\n<li>if two scopes need the same diagram, name one owning source rather than silently forking two <code>.mmd</code> files.</li>\n</ul>\n<h2>Invariants</h2>\n<ul>\n<li>Markdown remains readable and editable without a graphical renderer.</li>\n<li>A broken diagram does not prevent the rest of its document from rendering.</li>\n<li>Ordinary documents do not load a visualisation dependency they do not use.</li>\n<li>Public routes receive no local-only renderer, service, or data source by accident.</li>\n<li>Data-driven visualisations preserve the canonical data owner's authority.</li>\n<li>The semantic graph consumes existing reference and catalogue identities rather than defining parallel nodes or edges.</li>\n<li>Markdown does not become a route for arbitrary JavaScript execution.</li>\n<li>The supported local tool version and material render options are recorded once the first project command exists, and generated results are reproducible enough to review through source control and the publication gate.</li>\n</ul>\n<h2>Questions Before A Delivery</h2>\n<ol>\n<li>Which one or two current documents should prove the Mermaid convention?</li>\n<li>Is a direct local <code>mmdc</code> command sufficient initially, or would a small repository wrapper materially improve consistency?</li>\n<li>Which Mermaid CLI version, theme, sizing, and accessibility defaults are enough for the first diagram?</li>\n<li>Should a later check detect an SVG older than its <code>.mmd</code> source without making rendering part of every docs build?</li>\n<li>For the semantic graph, what question should the first view answer that the current reference report cannot?</li>\n<li>Which node and edge types make a bounded useful first graph, and how large is the real dataset?</li>\n<li>Should the graph begin as a local Docs Viewer panel before any public <code>/analysis/</code> promotion?</li>\n</ol>\n<h2>Separate Delivery Candidates</h2>\n<ul>\n<li>Authored documentation diagrams: prove the <code>.mmd</code>-to-SVG convention on one useful diagram, record the local tool/version command, and add only the consistency checks the use case demonstrates it needs.</li>\n<li>Semantic relationship graph: prove one read-only Docs Viewer panel over an explicit generated graph projection, initially limited to the node and edge types needed for a useful index view.</li>\n</ul>\n<p>These should remain separate roadmap deliveries. Static Mermaid publishing does not itself justify repository-level JavaScript tooling; the interactive D3 graph may do so later.</p>\n<p>When those answers point to one complete result, add an architecture document if needed and promote one bounded delivery into the <a href=\"/docs/?scope=studio&amp;doc=d-20260714-234030-1e0af6\">Docs Viewer Roadmap</a>.</p>\n"
}
