{
  "doc_id": "d-20260716-193410-98d1a8",
  "title": "Dated Export Snapshots",
  "added_date": "2026-07-16 19:34:10",
  "last_updated": "2026-07-18 16:21:54",
  "viewer_url": "/docs/?scope=studio&doc=d-20260716-193410-98d1a8",
  "parent_id": "d-20260714-234030-1e0af6",
  "summary": "Extend static HTML export into dated whole-scope or subtree snapshots stored outside Docs Viewer.",
  "ui_status": "paused",
  "content_html": "<h1>Dated Export Snapshots</h1>\n<h2>Outcome</h2>\n<p>Docs Viewer can write a small, browseable snapshot before significant document or roadmap changes without filling Docs Viewer itself with an archive that later needs organisation and retirement.</p>\n<p>The snapshot is rendered static HTML on the external projects drive. It is useful as a visual reference or last-resort manual source of text, but it is deliberately not exact canonical source and has no restore, import, editing, synchronization, or return path.</p>\n<h2>Snapshot Modes</h2>\n<ul>\n<li><strong>Whole scope:</strong> export the complete manage tree to <code>&lt;scope&gt; - YYYY-MM-DD</code>.</li>\n<li><strong>Selected subtree:</strong> export one selected parent and every descendant to <code>&lt;parent title&gt; - YYYY-MM-DD</code>.</li>\n</ul>\n<p>Folder labels use the local export date and only the minimum filesystem-safe normalization needed for titles. The snapshot preserves original document ids inside the non-canonical artifact.</p>\n<p>If the destination folder does not exist, confirmation creates it. If it already exists, the only choices are <strong>Replace</strong> and <strong>Cancel</strong>. There is no <strong>Save As</strong> or naming workflow, and apply must never wipe an existing folder before explicit replacement confirmation.</p>\n<p>Because duplicate or filesystem-equivalent titles can produce the same dated folder name, each snapshot carries a small provenance record with its source scope, optional root <code>doc_id</code>, document count, and generation time. A replacement prompt identifies the existing snapshot rather than treating the folder name as identity.</p>\n<h2>Boundaries</h2>\n<ul>\n<li>Snapshot input is existing generated Docs Viewer payloads, including the manage-visible non-viewable tree; it does not parse or package canonical Markdown.</li>\n<li>Any configured scope shown in <code>/docs/</code> may participate when its generated tree and document payloads are readable, regardless of whether its source is public, repo-local, or external-local.</li>\n<li>The export copies no media, download, or interactive-asset bytes. Rendered references retain their existing targets.</li>\n<li>Whole-scope and subtree snapshots share one service contract, destination policy, HTML renderer, and Replace/Cancel boundary.</li>\n<li>Snapshot folders remain user-owned external files. Docs Viewer does not index, list, retain, prune, or manage them as an archive.</li>\n<li>The feature remains separate from Publish, Data Sharing packages, and Copy Subtree To Scope.</li>\n</ul>\n<h2>Current Boundary</h2>\n<p>The current <a href=\"/docs/?scope=studio&amp;doc=d-20260629-152159-ac1af9\">Export</a> supports one whole repo-backed local scope at a time, always writes to <code>/docs-export/&lt;scope&gt;/</code>, and replaces that folder on every apply. It has no subtree mode, dated naming, or preflight replacement choice.</p>\n<h2>Delivery</h2>\n<p>No delivery is planned yet. This enhancement follows the independently useful Copy Subtree feature but does not depend on it.</p>\n<h2>Durable Home</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260629-152159-ac1af9\">Export</a> will own the shipped snapshot contract and replace its current latest-folder behavior.</p>\n"
}
