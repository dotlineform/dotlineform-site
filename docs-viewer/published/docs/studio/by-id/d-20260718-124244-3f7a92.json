{
  "doc_id": "d-20260718-124244-3f7a92",
  "title": "Git",
  "added_date": "2026-07-18 12:42:44",
  "last_updated": "2026-07-18 12:57:56",
  "viewer_url": "/docs/?scope=studio&doc=d-20260718-124244-3f7a92",
  "parent_id": "d-20260718-114645-7b9d24",
  "summary": "Keep Core Refactor branches, worktrees, commits, synchronization, integration, and recovery explicit without making Git the user's maintenance burden.",
  "ui_status": "proposed",
  "content_html": "<h1>Git</h1>\n<h2>Purpose</h2>\n<p>Keep Git mechanics safe, visible, and lightweight while the user concentrates on product and architectural decisions. The sibling <a href=\"/docs/?scope=studio&amp;doc=d-20260718-114201-4e7a93\">Working Method</a> owns environment, scope-data, documentation, and concurrent-change rules.</p>\n<h2>Responsibilities</h2>\n<p>Codex may handle the operational Git work when explicitly asked to activate or advance it:</p>\n<ul>\n<li>inspect dirty state and attribute changes before grouping them;</li>\n<li>create and verify branches and worktrees;</li>\n<li>propose and make focused commits without sweeping in unrelated files;</li>\n<li>keep <code>main</code> and the refactor branch current at agreed checkpoints;</li>\n<li>account for watcher-generated output with its source changes;</li>\n<li>inspect and resolve conflicts through the current owners;</li>\n<li>maintain a draft pull request when useful;</li>\n<li>prepare integration, restart, recovery, and closeout steps;</li>\n<li>remove completed worktrees and branches after approval.</li>\n</ul>\n<p>The user retains decisions about the core boundary, delivery scope, discovered work, proposal constraints, migrations, product behavior, and final integration. This delegation is not blanket authorization for commits, pushes, merges, migration, deletion, or branch removal; perform those state-changing steps only after the relevant request or approval.</p>\n<h2>Status Reporting</h2>\n<p>At meaningful points, report only the state needed for the next decision:</p>\n<pre><code class=\"language-text\">main: clean / current\nrefactor: 3 commits ahead; 1 main commit not yet integrated\nactive delivery: application-root contract\nconflicts: none\nnext safe action: merge main into refactor\n</code></pre>\n<p>Include exact commit identities, dirty files, checks, or recovery details when they materially affect the action. Do not turn routine Git maintenance into a running command transcript.</p>\n<h2>Branch And Worktree Model</h2>\n<p>Keep the current checkout on <code>main</code> as the stable application. Create a sibling worktree on <code>feature/docs-viewer-core</code> only when the first implementation delivery is ready.</p>\n<p>A worktree has its own branch, index, and tracked files but shares Git history and objects with the repository. Merge branches and commits, not folders. Bring completed <code>main</code> changes into the refactor regularly; feature changes enter <code>main</code> only at validated integration points.</p>\n<p>Planning and coordination documents remain canonical on <code>main</code>. Merge their committed updates into the refactor branch; do not maintain competing authoritative versions.</p>\n<h2>Worktree Creation Gate</h2>\n<p>Create the long-lived branch and worktree only when:</p>\n<ul>\n<li>the main-owned planning baseline is committed and readable through the stable Docs Viewer;</li>\n<li>Core Definition Baseline v1 has been accepted and its remaining uncertainty is bounded;</li>\n<li>the <a href=\"/docs/?scope=studio&amp;doc=d-20260718-125756-a41c70\">Docs Viewer Source Documentation And Linting Pilot Delivery</a> is complete and its focused commands are available;</li>\n<li>Transition Architecture identifies the smallest safe first slice;</li>\n<li>the first implementation delivery has an exact outcome, current and target owners, checks, and recovery point;</li>\n<li>affected proposals and concurrent work have been assessed in both directions;</li>\n<li>unrelated dirty files on <code>main</code> are separated or explicitly accounted for;</li>\n<li>the exact baseline commit, branch name, and sibling path are recorded;</li>\n<li>the main-to-refactor integration rhythm is agreed;</li>\n<li>isolated ports, external data, logs/caches, allowed services, and remote-write policy are decided;</li>\n<li>implementation work is ready to begin immediately.</li>\n</ul>\n<p>If pre-delivery analysis needs write-based experiments, use a disposable spike branch or worktree with one explicit question and a discard-or-promote decision. Do not silently turn it into the long-lived refactor branch.</p>\n<h2>Activation</h2>\n<p>After the gate passes:</p>\n<ol>\n<li>verify the baseline commit and current dirty state;</li>\n<li>create the branch and sibling worktree;</li>\n<li>verify its branch, head, and clean initial state before editing;</li>\n<li>create its untracked <code>.env.local</code> and isolated data roots;</li>\n<li>confirm that it cannot write to real external-local or production remote targets;</li>\n<li>start only the services needed by the active delivery.</li>\n</ol>\n<p>Proposed command shape:</p>\n<pre><code class=\"language-bash\">git worktree add &lt;sibling-path&gt; -b feature/docs-viewer-core &lt;baseline-ref&gt;\n</code></pre>\n<p>Resolve and verify both placeholders before execution.</p>\n<h2>Maintenance</h2>\n<ul>\n<li>Begin a delivery or integration step by checking both worktrees and their divergence.</li>\n<li>Keep commits focused on one completed responsibility or explicit checkpoint.</li>\n<li>Do not use compatibility aliases to make old and new branches appear synchronized.</li>\n<li>Merge <code>main</code> into the refactor at controlled intervals while conflict context is fresh.</li>\n<li>Resolve source and configuration first, then regenerate rather than hand-merging generated payloads.</li>\n<li>Treat a conflict as an ownership question before treating it as a text-editing problem.</li>\n<li>Do not make a branch history look clean by hiding unresolved behavior or unrelated user changes.</li>\n</ul>\n<h2>Integration</h2>\n<p>Before feature changes enter <code>main</code>:</p>\n<ul>\n<li>verify the exact <code>main</code> and feature heads and all uncommitted files;</li>\n<li>complete the delivery's declared <a href=\"/docs/?scope=studio&amp;doc=d-20260718-123734-6d84c1\">Testing</a> evidence;</li>\n<li>confirm documentation and generated outputs follow their canonical source;</li>\n<li>assess app-data, config, external-scope, and open-proposal effects;</li>\n<li>record the recovery point and affected-service restart procedure.</li>\n</ul>\n<p>Stop normal services only when the controlled merge, migration, or restart requires it. Each intermediate feature-to-<code>main</code> integration must be independently complete; it is not synchronization for its own sake.</p>\n<h2>Closeout</h2>\n<p>After the Core Refactor is integrated and verified, account for remaining commits and temporary branches, then remove the worktree and feature branch only after explicit approval. Report what was removed, the retained recovery point, and whether any temporary data roots or remote test targets remain.</p>\n<h2>Status</h2>\n<p>Proposed. No long-lived Core Refactor branch or worktree exists yet, and Git maintenance has not been activated for an implementation delivery.</p>\n"
}
