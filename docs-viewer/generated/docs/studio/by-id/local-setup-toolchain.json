{
  "doc_id": "local-setup-toolchain",
  "title": "Local Setup Toolchain",
  "added_date": "2026-05-19",
  "last_updated": "2026-05-19",
  "source_path": "local-setup-toolchain.md",
  "viewer_url": "/docs/?scope=studio&doc=local-setup-toolchain",
  "parent_id": "local-setup",
  "content_html": "<h1>Local Setup Toolchain</h1>\n<h2>Current repo/runtime summary</h2>\n<p>Current versions in use:</p>\n<ul>\n<li>Python: <code>3.12.7</code></li>\n<li>Python package: <code>openpyxl 3.1.5</code></li>\n<li>Ruby: <code>3.1.6</code></li>\n<li>Bundler: <code>2.6.9</code></li>\n<li>Jekyll: <code>3.10.0</code></li>\n<li><code>github-pages</code> gem: <code>232</code></li>\n</ul>\n<p>Current external tools used by the Python/media workflow:</p>\n<ul>\n<li>macOS <code>sips</code></li>\n<li><code>ffmpeg</code></li>\n<li><code>heif-convert</code> from <code>libheif</code></li>\n</ul>\n<p>The Python scripts themselves are lightweight:</p>\n<ul>\n<li>non-stdlib Python dependency is currently just <code>openpyxl</code></li>\n<li>image/srcset generation also depends on external command-line tools, not extra Python packages</li>\n<li>the Tag Studio local write server uses only stdlib modules plus repo-local helpers</li>\n</ul>\n<p>That summary is now incomplete for feature-specific Python dependencies because the Docs HTML import feature adds a pinned parser/sanitizer stack in <code>requirements.txt</code>.\nUse <a href=\"/docs/?scope=studio&amp;doc=runtime-dependencies\">Runtime Dependencies</a> as the current dependency-role reference.</p>\n<h2>Fresh macOS install</h2>\n<p>Recommended order on a fresh macOS 24 system:</p>\n<ol>\n<li>Install Apple command line tools if they are not already present:</li>\n</ol>\n<pre><code class=\"language-bash\">xcode-select --install\n</code></pre>\n<ol start=\"2\">\n<li>Install the non-Python command-line dependencies with Homebrew:</li>\n</ol>\n<pre><code class=\"language-bash\">brew install rbenv ruby-build ffmpeg libheif\nbrew install --cask miniconda\n</code></pre>\n<ol start=\"3\">\n<li>Initialize Conda for your shell, then open a new shell:</li>\n</ol>\n<pre><code class=\"language-bash\">conda init zsh\n</code></pre>\n<p>If you use <code>bash</code> instead of the default macOS <code>zsh</code>, run:</p>\n<pre><code class=\"language-bash\">conda init bash\n</code></pre>\n<ol start=\"4\">\n<li>Create and activate a dedicated Python environment:</li>\n</ol>\n<pre><code class=\"language-bash\">conda create -n dotlineform python=3.12.7 openpyxl=3.1.5\nconda activate dotlineform\n</code></pre>\n<ol start=\"5\">\n<li>Install and select the repo's Ruby version:</li>\n</ol>\n<pre><code class=\"language-bash\">rbenv install 3.1.6\nrbenv local 3.1.6\n</code></pre>\n<ol start=\"6\">\n<li>Install the pinned Bundler version and the Ruby gems:</li>\n</ol>\n<pre><code class=\"language-bash\">gem install bundler:2.6.9\nbundle _2.6.9_ install\n</code></pre>\n<p>At that point the local script/Jekyll toolchain should be in place.</p>\n<h2>Version checks</h2>\n<p>Check that the active Python is the expected one:</p>\n<pre><code class=\"language-bash\">which python3\npython3 -V\npython3 -c &quot;import sys, openpyxl; print(sys.version); print('openpyxl', openpyxl.__version__)&quot;\n</code></pre>\n<p>Expected current values:</p>\n<ul>\n<li><code>python3 -V</code> -&gt; <code>Python 3.12.7</code></li>\n<li><code>openpyxl</code> -&gt; <code>3.1.5</code></li>\n</ul>\n<p>Check the media tools:</p>\n<pre><code class=\"language-bash\">ffmpeg -version | head -n 1\nheif-convert --version\nsips --help | head -n 1\n</code></pre>\n<p>Check the Ruby/Jekyll toolchain:</p>\n<pre><code class=\"language-bash\">ruby -v\nbundle -v\nbundle exec jekyll -v\n</code></pre>\n<p>Expected current values:</p>\n<ul>\n<li><code>ruby -v</code> -&gt; Ruby <code>3.1.6</code></li>\n<li><code>bundle -v</code> -&gt; Bundler <code>2.6.9</code></li>\n<li><code>bundle exec jekyll -v</code> -&gt; Jekyll <code>3.10.0</code></li>\n</ul>\n<p>If you want to confirm the repo's pinned Ruby/Bundler versions directly:</p>\n<pre><code class=\"language-bash\">cat .ruby-version\ntail -n 3 Gemfile.lock\n</code></pre>\n<h2>Switching to the correct version</h2>\n<h3>Python</h3>\n<p>The important requirement is that <code>python3</code> resolves to an interpreter that has <code>openpyxl 3.1.5</code> installed.</p>\n<p>Activate the repo environment:</p>\n<pre><code class=\"language-bash\">conda activate dotlineform\n</code></pre>\n<p>See available Conda environments:</p>\n<pre><code class=\"language-bash\">conda info --envs\n</code></pre>\n<p>If <code>python3</code> still points somewhere unexpected, check:</p>\n<pre><code class=\"language-bash\">which python3\npython3 -V\n</code></pre>\n<p>The repo scripts are normally run via <code>./scripts/...</code>, so the active shell <code>python3</code> matters because the script shebangs use <code>/usr/bin/env python3</code>.</p>\n<h3>Ruby</h3>\n<p>Use <code>rbenv</code> to ensure the repo-local Ruby is selected:</p>\n<pre><code class=\"language-bash\">rbenv versions\nrbenv local 3.1.6\nrbenv version\nrbenv which ruby\n</code></pre>\n<p>If Bundler reports the wrong version, install and invoke the pinned one:</p>\n<pre><code class=\"language-bash\">gem install bundler:2.6.9\nbundle _2.6.9_ -v\n</code></pre>\n<p>If local verification accidentally picks up <code>/usr/bin/ruby</code>, fix the shell setup first rather than working around it repeatedly.</p>\n"
}
