{
  "scope": "studio",
  "doc_id": "data-flow",
  "title": "Data Flow",
  "added_date": "2026-03-31",
  "last_updated": "2026-05-11 14:10",
  "parent_id": "catalogue",
  "hidden": false,
  "viewable": true,
  "source_path": "data-flow.md",
  "viewer_url": "/docs/?scope=studio&doc=data-flow",
  "content_html": "<h1>Data Flow</h1>\n<p>This document describes the current runtime data flow for the public catalogue.</p>\n<p>It focuses on which generated artifacts each route reads at runtime. It does not try to document every field in those payloads.</p>\n<p>For the catalogue artifact contracts themselves, use <a href=\"/docs/?scope=studio&amp;doc=data-models-catalogue\">Catalogue Scope</a>.</p>\n<p>Current public browsing routes covered here:</p>\n<ul>\n<li><code>/series/</code></li>\n<li><code>/series/&lt;series_id&gt;/</code></li>\n<li><code>/works/&lt;work_id&gt;/</code></li>\n<li><code>/work_details/&lt;detail_uid&gt;/</code></li>\n<li><code>/moments/&lt;moment_id&gt;/</code></li>\n</ul>\n<p>The main live rebuild path for these artifacts is the scoped JSON build flow described in <a href=\"/docs/?scope=studio&amp;doc=scripts-build-catalogue-json\">Scoped JSON Catalogue Build</a>.</p>\n<h2>Overview</h2>\n<p>Current generated JSON files involved in this flow:</p>\n<ul>\n<li><code>assets/data/series_index.json</code></li>\n<li><code>assets/data/moments_index.json</code></li>\n<li><code>assets/data/works_index.json</code></li>\n<li><code>assets/series/index/&lt;series_id&gt;.json</code></li>\n<li><code>assets/works/index/&lt;work_id&gt;.json</code></li>\n<li><code>assets/moments/index/&lt;moment_id&gt;.json</code></li>\n</ul>\n<h2>1. Catalogue Index</h2>\n<p>User-facing step:</p>\n<ul>\n<li><code>/series/</code></li>\n<li>lists series in <code>works</code> mode</li>\n<li>lists moments in <code>moments</code> mode</li>\n</ul>\n<p>Current JSON used:</p>\n<ul>\n<li><code>assets/data/series_index.json</code></li>\n<li><code>assets/data/moments_index.json</code></li>\n</ul>\n<p>Template:</p>\n<ul>\n<li><code>series/index.md</code></li>\n</ul>\n<p>How the page uses them:</p>\n<ul>\n<li><code>works</code> mode is built from <code>series_index.json</code></li>\n<li><code>moments</code> mode is built from <code>moments_index.json</code></li>\n<li>moment card URLs still come from <code>site.moments</code> URLs embedded into the page at build time</li>\n</ul>\n<p>This route does not read per-series, per-work, or per-moment JSON records.</p>\n<h2>2. Series Page</h2>\n<p>User-facing step:</p>\n<ul>\n<li><code>/series/&lt;series_id&gt;/</code></li>\n<li>shows a grid of all works in the series</li>\n<li>shows optional series prose below the grid</li>\n</ul>\n<p>Current JSON used:</p>\n<ul>\n<li><code>assets/data/series_index.json</code></li>\n<li><code>assets/data/works_index.json</code></li>\n<li><code>assets/series/index/&lt;series_id&gt;.json</code></li>\n</ul>\n<p>Template:</p>\n<ul>\n<li><code>_layouts/series.html</code></li>\n</ul>\n<p>How the page uses them:</p>\n<ul>\n<li><code>series_index.json</code> answers membership and ordering</li>\n<li><code>works_index.json</code> answers lightweight work-card metadata without fetching one JSON file per work</li>\n<li><code>assets/series/index/&lt;series_id&gt;.json</code> supplies prose content for the lower content block</li>\n<li>the page does not fetch per-work JSON for the series grid</li>\n</ul>\n<h2>3. Work Page</h2>\n<p>User-facing step:</p>\n<ul>\n<li><code>/works/&lt;work_id&gt;/</code></li>\n<li>shows the work</li>\n<li>shows one grid per detail section</li>\n</ul>\n<p>Current JSON used:</p>\n<ul>\n<li><code>assets/works/index/&lt;work_id&gt;.json</code></li>\n<li><code>assets/data/series_index.json</code></li>\n</ul>\n<p>Template:</p>\n<ul>\n<li><code>_layouts/work.html</code></li>\n</ul>\n<p>What <code>series_index.json</code> provides on the work page:</p>\n<ul>\n<li>series context when the work is opened with <code>?series=&lt;series_id&gt;</code></li>\n<li>previous and next work navigation inside that series</li>\n<li>series title used for the inline series link and back-link labeling</li>\n<li>no-context back-link fallback to the work's primary series when no explicit return source is present</li>\n</ul>\n<p>The work page is work-local by design. Detail sections, detail links, prose, downloads, and published links all come from the per-work record.</p>\n<h2>4. Detail Page</h2>\n<p>User-facing step:</p>\n<ul>\n<li><code>/work_details/&lt;detail_uid&gt;/</code></li>\n<li>shows a single detail image</li>\n</ul>\n<p>Current JSON used:</p>\n<ul>\n<li><code>assets/works/index/&lt;work_id&gt;.json</code></li>\n</ul>\n<p>Template:</p>\n<ul>\n<li><code>_layouts/work_details.html</code></li>\n</ul>\n<p>How the page works:</p>\n<ul>\n<li>it derives <code>work_id</code> from the <code>detail_uid</code> route prefix</li>\n<li>it fetches <code>assets/works/index/&lt;work_id&gt;.json</code></li>\n<li>it finds the matching <code>detail_uid</code> in that work payload</li>\n</ul>\n<p>This route does not use a global detail index.</p>\n<h2>5. Moment Page</h2>\n<p>User-facing step:</p>\n<ul>\n<li><code>/moments/&lt;moment_id&gt;/</code></li>\n<li>shows one moment plus its prose body</li>\n</ul>\n<p>Current JSON used:</p>\n<ul>\n<li><code>assets/moments/index/&lt;moment_id&gt;.json</code></li>\n</ul>\n<p>Template and runtime:</p>\n<ul>\n<li><code>_layouts/moment.html</code></li>\n<li><code>assets/js/moment.js</code></li>\n</ul>\n<p>The moment page is moment-local. It does not read <code>moments_index.json</code> at runtime.</p>\n<h2>Search Boundary</h2>\n<p>Catalogue search is a separate surface:</p>\n<ul>\n<li><code>/catalogue/search/</code></li>\n</ul>\n<p>It reads:</p>\n<ul>\n<li><code>assets/data/search/catalogue/index.json</code></li>\n</ul>\n<p>Search behavior, ranking, and index structure are documented in the <a href=\"/docs/?scope=studio&amp;doc=search\">Search</a> section rather than repeated here.</p>\n<p>The catalogue artifact families themselves are documented in <a href=\"/docs/?scope=studio&amp;doc=data-models-catalogue\">Catalogue Scope</a>.</p>\n<h2>Current State</h2>\n<p>The implemented data flow is now:</p>\n<ol>\n<li>\n<p><code>/series/</code></p>\n<ul>\n<li>uses <code>assets/data/series_index.json</code></li>\n<li>uses <code>assets/data/moments_index.json</code></li>\n</ul>\n</li>\n<li>\n<p><code>/series/&lt;series_id&gt;/</code></p>\n<ul>\n<li>uses <code>assets/data/series_index.json</code></li>\n<li>uses <code>assets/data/works_index.json</code></li>\n<li>uses <code>assets/series/index/&lt;series_id&gt;.json</code></li>\n</ul>\n</li>\n<li>\n<p><code>/works/&lt;work_id&gt;/</code></p>\n<ul>\n<li>uses <code>assets/works/index/&lt;work_id&gt;.json</code></li>\n<li>uses <code>assets/data/series_index.json</code> for series context</li>\n</ul>\n</li>\n<li>\n<p><code>/work_details/&lt;detail_uid&gt;/</code></p>\n<ul>\n<li>derives <code>work_id</code> from the <code>detail_uid</code> route prefix</li>\n<li>uses <code>assets/works/index/&lt;work_id&gt;.json</code></li>\n</ul>\n</li>\n<li>\n<p><code>/moments/&lt;moment_id&gt;/</code></p>\n<ul>\n<li>uses <code>assets/moments/index/&lt;moment_id&gt;.json</code></li>\n</ul>\n</li>\n<li>\n<p><code>/catalogue/search/</code></p>\n<ul>\n<li>uses <code>assets/data/search/catalogue/index.json</code></li>\n</ul>\n</li>\n</ol>\n"
}
