{
  "doc_id": "search-docs-viewer-infrastructure",
  "title": "Docs Viewer Search",
  "added_date": "2026-06-02",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=search-docs-viewer-infrastructure",
  "parent_id": "search",
  "content_html": "<h1>Docs Viewer Search</h1>\n<p>Docs Viewer Search provides inline document lookup for every configured Docs Viewer scope.</p>\n<h2>Execution Path</h2>\n<ol>\n<li><code>docs-viewer/config/scopes/docs_scopes.json</code> identifies the scope source and generated/published search paths.</li>\n<li><code>docs-viewer/build/build_search.py</code> reads source front matter and writes a scope-specific static index.</li>\n<li>Docs Viewer route config supplies the index URL and expected schema.</li>\n<li>the collection provider loads the index for the active route/scope</li>\n<li><code>docs-viewer-search.js</code> normalizes and ranks entries</li>\n<li><code>docs-viewer-search-controller.js</code> owns query/history state, loading, paging, and result rendering</li>\n</ol>\n<p>Public scopes publish their indexes to <code>site/assets/data/search/</code>; local scopes keep local generated output or serve it through the Docs Viewer service.</p>\n<h2>Capability Summary</h2>\n<ul>\n<li>indexes viewable documents only</li>\n<li>searches document id, title, parent title, and update metadata</li>\n<li>preserves parent context for display and ranking</li>\n<li>updates create/change/delete targets without rebuilding an unrelated scope</li>\n<li>supports inline query history and incremental result display</li>\n<li>shares one runtime across local, public, and external-local scopes</li>\n</ul>\n<p>Document body Markdown and rendered HTML are not indexed. Search is a navigation index, not full-text retrieval.</p>\n<h2>Configuration Boundary</h2>\n<ul>\n<li>source and filesystem publication: <code>docs_scopes.json</code></li>\n<li>local runtime URLs and expected schema: <code>docs-viewer/config/defaults/docs-viewer-config.json</code></li>\n<li>public route URLs: <code>site/docs-viewer/config/routes/docs-viewer-public-routes.json</code></li>\n<li>local external reads: Docs Viewer service routes from the configured external data root</li>\n</ul>\n<p>Keep filesystem paths out of browser config. A scope's type and publish paths determine whether its index is local or public.</p>\n<h2>Extension Method</h2>\n<p>Adding a normal scope should be configuration work, not a new builder/runtime branch. Add code only when the new scope requires a genuinely different source shape or retrieval method.</p>\n<p>For new searchable metadata, update source parsing, entry construction, normalization/ranking, and focused tests together. Adding body content would materially change payload size and relevance behaviour and should be treated as a separate feature.</p>\n<h2>Weak Spots</h2>\n<ul>\n<li>Search records use front matter directly while document payload generation performs richer rendering; both builders must continue to agree on identity and visibility.</li>\n<li>Parent-title changes affect child entries and require correct affected-id expansion by the watcher.</li>\n<li>Runtime config retains both legacy and nested search URL fields.</li>\n<li>Ranking policy is embedded in JavaScript rather than a registry or schema.</li>\n<li>A large document corpus may outgrow client-side static-index loading.</li>\n</ul>\n"
}
