{
  "doc_id": "site-request-report-imports",
  "title": "Imports Report",
  "added_date": "2026-06-09",
  "last_updated": "2026-06-09",
  "source_path": "site-request-report-imports.md",
  "viewer_url": "/docs/?scope=studio&doc=site-request-report-imports",
  "parent_id": "site-request-admin-checks-reports",
  "ui_status": "draft",
  "content_html": "<h1>Imports Report</h1>\n<p>This document describes a possible future report for <a href=\"/docs/?scope=studio&amp;doc=site-request-admin-checks-reports\">Admin Checks Reports</a>.</p>\n<h2>Purpose</h2>\n<p>The <code>imports</code> report would provide dependency-reference evidence for files selected by a checks run.</p>\n<p>Primary review question:</p>\n<pre><code class=\"language-text\">Which selected files depend on files outside their configured target boundary?\n</code></pre>\n<p>Secondary evidence:</p>\n<ul>\n<li>which selected files import or reference other local modules</li>\n<li>which selected files are imported by other files</li>\n<li>which selected files have unresolved local references</li>\n<li>which dependency references should appear in a later file-profile summary</li>\n</ul>\n<h2>Inputs</h2>\n<ul>\n<li>selected scope, family, area, and route filters from the run manifest</li>\n<li>selected files resolved through <code>target_map_resolver.py</code></li>\n<li>resolved target-map data for selected and referenced files</li>\n<li>language-specific parser or scanner rules for JavaScript, Python, CSS, and JSON/config references where practical</li>\n</ul>\n<p>Possible options:</p>\n<table>\n<thead>\n<tr>\n<th>Option</th>\n<th>Default</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>limit</code></td>\n<td><code>20</code></td>\n<td>Maximum files shown per Markdown section.</td>\n</tr>\n<tr>\n<td><code>include_reverse_refs</code></td>\n<td><code>true</code></td>\n<td>Include files outside the selected set that reference selected files.</td>\n</tr>\n<tr>\n<td><code>include_external_packages</code></td>\n<td><code>false</code></td>\n<td>Include package imports, not only repo-local references.</td>\n</tr>\n<tr>\n<td><code>unresolved_limit</code></td>\n<td><code>20</code></td>\n<td>Maximum unresolved references shown in Markdown.</td>\n</tr>\n</tbody>\n</table>\n<h2>Output</h2>\n<p>Artifacts:</p>\n<pre><code class=\"language-text\">var/admin/checks/&lt;run-id&gt;/imports/\n  report.json\n  report.md\n  report.csv\n</code></pre>\n<p>Required JSON fields:</p>\n<ul>\n<li><code>path</code></li>\n<li><code>language</code></li>\n<li><code>imports[]</code></li>\n<li><code>local_imports[]</code></li>\n<li><code>external_imports[]</code></li>\n<li><code>reverse_refs[]</code></li>\n<li><code>unresolved_refs[]</code></li>\n<li><code>cross_target_refs[]</code></li>\n<li><code>import_count</code></li>\n<li><code>local_import_count</code></li>\n<li><code>reverse_ref_count</code></li>\n<li><code>unresolved_ref_count</code></li>\n<li><code>cross_target_ref_count</code></li>\n</ul>\n<p>The stable join key for file-level consumers should be repo-relative <code>path</code>.</p>\n<p>Required per-reference fields:</p>\n<ul>\n<li><code>source_path</code></li>\n<li><code>target_path</code> when resolved</li>\n<li><code>raw_reference</code></li>\n<li><code>reference_kind</code></li>\n<li><code>source_targets</code></li>\n<li><code>target_targets</code></li>\n<li><code>target_match</code></li>\n</ul>\n<p><code>target_match</code> should be one of:</p>\n<ul>\n<li><code>same-target</code></li>\n<li><code>shared-target</code></li>\n<li><code>cross-family</code></li>\n<li><code>cross-area</code></li>\n<li><code>cross-route</code></li>\n<li><code>external-package</code></li>\n<li><code>unresolved</code></li>\n</ul>\n<h2>Markdown Shape</h2>\n<p>The Markdown should answer the review question with bounded aligned <code>text</code> blocks, not a full dependency table.</p>\n<p>Sections:</p>\n<ul>\n<li>summary counts</li>\n<li>files with cross-target references</li>\n<li>files with many reverse references</li>\n<li>unresolved local references</li>\n</ul>\n<p>Example:</p>\n<pre><code class=\"language-text\">Files with cross-target references\nFile                              Cross refs  Main target\n--------------------------------  ----------  ----------------\ndocs-viewer-management.js                  7  runtime-js / management\n</code></pre>\n<h2>Calculation Method</h2>\n<p>The first implementation should prefer structured parsers where practical and conservative regex extraction where parser support is not yet worth the cost.</p>\n<p>Likely source forms:</p>\n<ul>\n<li>Python <code>import</code> and <code>from ... import ...</code></li>\n<li>JavaScript <code>import</code>, <code>export ... from</code>, and dynamic import strings</li>\n<li>CSS <code>@import</code> and <code>url(...)</code> only when the target resolves to a selected code/config asset</li>\n<li>local JSON/config references where they are part of route, module, or generated-data wiring</li>\n</ul>\n<p>The report should resolve references to repo-relative paths when possible and leave unresolved references visible as evidence rather than hiding them.\nIt should compare source and target membership using the same target-map resolver data rather than inventing a separate ownership model.</p>\n<h2>Verification</h2>\n<p>Focused tests should cover:</p>\n<ul>\n<li>JavaScript static imports and dynamic import strings</li>\n<li>Python imports that resolve to repo files</li>\n<li>unresolved local references</li>\n<li>reverse references from outside the selected set</li>\n<li>cross-family, cross-area, and cross-route classification</li>\n<li>Markdown output with no wide Markdown tables</li>\n<li>option validation through <code>admin-checks-reports.json</code></li>\n</ul>\n<h2>Dependency Use</h2>\n<p><code>file-profile</code> or other compound reports could depend on <code>imports</code> to show local dependencies, reverse references, and cross-boundary references for one selected file.</p>\n<p>The <code>imports</code> report should remain independent and reusable.\nIt should not know about the file-profile report.</p>\n<h2>Non-Goals</h2>\n<ul>\n<li>no complete language-server implementation</li>\n<li>no bundler-specific graph unless a later request requires it</li>\n<li>no automatic risk judgment from a dependency edge</li>\n<li>no browser-provided parser rules or arbitrary scan patterns</li>\n</ul>\n"
}
