{
  "doc_id": "d-20260715-094411-599c3d",
  "title": "Document Identity Architecture",
  "added_date": "2026-07-15 09:44:11",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-094411-599c3d",
  "parent_id": "d-20260715-094411-1c5fa7",
  "summary": "Proposed generator, creation/import ownership, migration planner, reference rewrite, and validation structure for immutable document identity.",
  "ui_status": "proposed",
  "content_html": "<h1>Document Identity Architecture</h1>\n<p>This is proposed structure for DI-01. The code remains current authority until the delivery ships.</p>\n<h2>Current Starting Point</h2>\n<p>Docs Viewer already treats <code>doc_id</code> as a stable lookup key across hierarchy, URLs, generated payloads, semantic references, exports, reports, and reviewed updates. Metadata changes preserve it, and the live watcher does not assume that source filename equals <code>doc_id</code>.</p>\n<p>The remaining semantic coupling is concentrated at creation boundaries:</p>\n<ul>\n<li><code>docs_source_model.ensure_unique_stem()</code> derives a new identifier from title;</li>\n<li><code>docs_management_mutations.plan_create()</code> uses that identifier as both <code>doc_id</code> and filename;</li>\n<li>ordinary import previews derive proposed identifiers from staged filenames or titles;</li>\n<li>import collision handling exposes replacement title-like IDs;</li>\n<li>Docs search currently includes <code>doc_id</code> in general search terms.</li>\n</ul>\n<h2>Proposed Ownership</h2>\n<h3>Identity Allocation</h3>\n<p><code>docs_source_model.py</code> owns one generator used by every local new-document boundary.</p>\n<p>The generator:</p>\n<ol>\n<li>receives or creates the same second-resolution timestamp written to <code>added_date</code>;</li>\n<li>formats <code>d-YYYYMMDD-HHMMSS-</code>;</li>\n<li>appends six hexadecimal characters from a standard cryptographic random source;</li>\n<li>checks the result against current identities and source filenames;</li>\n<li>retries on collision;</li>\n<li>returns the ID without writing.</li>\n</ol>\n<p>Planning remains write-free. Apply writes the already planned ID; it must not silently generate a different one after confirmation.</p>\n<h3>Creation Boundaries</h3>\n<ul>\n<li>New Document allocates one ID and uses it as the Markdown filename.</li>\n<li>Ordinary single-source and collection imports allocate local IDs for genuinely new documents.</li>\n<li>Overwrite and returned-package update workflows preserve the selected canonical target ID.</li>\n<li>Import media may use the new ID when naming newly materialized assets, but existing media filenames are not identity and are not renamed by the migration.</li>\n<li>Titles remain independently editable metadata.</li>\n</ul>\n<h3>Added-Date Normalization</h3>\n<p>The migration planner normalizes every repository-owned document to <code>YYYY-MM-DD HH:MM:SS</code> and records an evidence method:</p>\n<ul>\n<li><code>front-matter-time</code>;</li>\n<li><code>git-time-on-matching-date</code>;</li>\n<li><code>midnight-default</code>.</li>\n</ul>\n<p>Current minute-only timestamps gain <code>:00</code>. Date-only metadata uses a matching Git addition time when available and otherwise keeps its date at midnight.</p>\n<h3>Migration Planner</h3>\n<p>A focused one-off planner scans configured repository-owned scope and sub-scope sources and produces an ignored review artifact under <code>var/docs/document-identity/</code>.</p>\n<p>Each mapping row contains:</p>\n<ul>\n<li>scope and sub-scope where relevant;</li>\n<li>source path and title;</li>\n<li>old and new <code>doc_id</code>;</li>\n<li>original and normalized <code>added_date</code>;</li>\n<li>timestamp evidence method;</li>\n<li>old and new source filename;</li>\n<li>parent and reference effects.</li>\n</ul>\n<p>The map is keyed by scope plus old identity. It must be stable between dry-run review and apply.</p>\n<p>Before apply, validate:</p>\n<ul>\n<li>every code and destination filename is unique and safe;</li>\n<li>every current parent resolves through the map;</li>\n<li>every configured default, non-loadable, and manage-only identity resolves;</li>\n<li>every recognized Docs Viewer <code>doc</code> or <code>subdoc</code> link can be rewritten;</li>\n<li>no source write or destination path escapes its configured root;</li>\n<li>external-local sources are excluded from DI-01.</li>\n</ul>\n<h3>Apply And Rebuild</h3>\n<p>Apply rewrites canonical source and config from the reviewed map:</p>\n<pre><code class=\"language-text\">reviewed identity map\n  -&gt; front-matter doc_id and parent_id\n  -&gt; source filename moves\n  -&gt; Docs Viewer doc/subdoc links\n  -&gt; configured identity references and fixtures\n  -&gt; full affected-scope docs builds\n  -&gt; full affected-scope search builds\n  -&gt; broken-link and old-identity scans\n</code></pre>\n<p>Generated payloads are rebuilt from source and stale old-ID files are removed. They are not migrated as authority.</p>\n<h3>Search</h3>\n<p>General Docs search should use title, parent title, and useful document metadata without tokenizing opaque IDs. Exact full-ID matching may be retained as a separate diagnostic path or exact token.</p>\n<h2>External And Historical Boundaries</h2>\n<ul>\n<li>Configured external-local scopes require a separate delivery because their source, workspace, backup, and lifecycle ownership differs.</li>\n<li>Pre-migration staged packages, review sessions, exports, bookmarks, and copied URLs may contain old IDs. Historical artifacts are not rewritten; active work is recreated or repaired deliberately.</li>\n<li>Public inbound links change once. No permanent alias registry is introduced.</li>\n<li>Git history and the migration map provide recovery if a source or reference mapping is wrong.</li>\n</ul>\n<h2>Pressure Points</h2>\n<ul>\n<li>Current validators accept slightly different character sets; the selected format fits their common safe subset, but allocation should have one canonical validator.</li>\n<li>Import workflows distinguish create, overwrite, and returned canonical identity in several adapters; the boundary must be explicit rather than inferred from one generic record shape.</li>\n<li>A mass rename may exceed targeted watcher assumptions, so implementation should suppress incidental rebuilds and perform deliberate full follow-through after canonical source is coherent.</li>\n<li>Search currently derives terms directly from <code>doc_id</code>; changing relevance must retain any genuinely useful exact diagnostic lookup.</li>\n<li>Front-matter and Git dates are provenance signals, not guaranteed historical truth.</li>\n</ul>\n<h2>Retirement</h2>\n<p>After DI-01, stable ownership and weak spots move into the retained concept/current overview. Delete this proposal once it no longer helps choose or implement unresolved structure.</p>\n"
}
