{
  "doc_id": "d-20260715-222129-8253ee",
  "title": "Recent Documents",
  "added_date": "2026-07-15 22:21:29",
  "last_updated": "2026-07-16",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-222129-8253ee",
  "parent_id": "d-20260714-234030-1e0af6",
  "summary": "Feature snapshot for one Recent view whose ordering follows catalogue or changing-document context without a user choice.",
  "ui_status": "proposed",
  "content_html": "<h1>Recent Documents</h1>\n<h2>Outcome</h2>\n<p>Docs Viewer has one <strong>Recent</strong> view whose meaning follows the route context rather than a user-selected mode.</p>\n<ul>\n<li>public <code>/moments/</code> is catalogue-like and shows works ordered by canonical <code>added_date</code>, newest first;</li>\n<li>public <code>/library/</code> and <code>/analysis/</code> are changing document collections and show documents ordered by canonical <code>last_updated</code>, newest first;</li>\n<li><code>/docs/</code> is always editorial and orders every public, local, or external-local scope by canonical <code>last_updated</code>, newest first.</li>\n</ul>\n<p>The same scope can therefore mean recently added at <code>/moments/</code> and recently edited at <code>/docs/?scope=moments</code>. The control remains simply <strong>Recent</strong>; there is no Added/Edited toggle or parallel user-facing view. New documents appear naturally in either ordering because creation is also their first update.</p>\n<p>Any route that enables Recent declares an allowlisted <code>added</code> or <code>edited</code> basis in its route policy. Moments is the first Docs Viewer <code>added</code> case, not a hard-coded route-id or scope-id exception. A future catalogue-like Docs Viewer route can adopt the same behavior through configuration without adding another application branch.</p>\n<p>The view keeps the current scope, visibility rules, result limit, <code>recent-results</code> host, and displayed-document behavior. Ordering comes from canonical document metadata, not the opaque <code>doc_id</code> or live Git history. Catalogue-owned <code>/series/</code> follows the same recently-added principle as public Moments but remains outside this Docs Viewer delivery. Docs Review has no Recent feature unless it later develops a distinct review-package need.</p>\n<h2>Delivery</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260715-225512-80198b\">Recent Documents Delivery</a> owns the complete timestamp amendment and context-sensitive Recent-view change. It is one proposed delivery because trustworthy update timestamps and the views that consume them solve the same recent-document problem.</p>\n<h2>Current Boundary</h2>\n<p>The current route feature, provider, payload, and controller expose recently added documents through <code>docs_recently_added_v1</code>, ordered by <code>added_date</code> in every context. <code>last_updated</code> is not yet a reliable activity timestamp: precision and automatic maintenance vary by write path.</p>\n<p>The delivery first makes <code>last_updated</code> trustworthy, then replaces the current contract with one generically named Recent projection at each served context. Do not add date fields to <code>index-tree.json</code> solely to support this view or couple it to the search payload.</p>\n<h2>Durable Home</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260605-125108-c68916\">Generated Data Providers</a> owns the generated payload and provider contract after implementation. The durable document-metadata owner records the timestamp contract. Ordinary control placement and rendering remain self-documented in the current view/control code.</p>\n"
}
