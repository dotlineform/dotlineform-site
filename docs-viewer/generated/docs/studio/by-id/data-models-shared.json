{
  "doc_id": "data-models-shared",
  "title": "Shared Artifact Patterns",
  "added_date": "2026-04-19",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=data-models-shared",
  "parent_id": "architecture",
  "content_html": "<h1>Shared Artifact Patterns</h1>\n<h2>Why Artifacts Are Split</h2>\n<p>The public site and Docs Viewer repeatedly separate stable routing, lightweight discovery, record-local content, and search:</p>\n<pre><code class=\"language-text\">canonical source\n  -&gt; stable route shell\n  -&gt; lightweight index/tree/recent payload\n  -&gt; per-record by-id payload\n  -&gt; separate search projection\n</code></pre>\n<p>This avoids a single large runtime dump, keeps URLs stable, and lets list/detail/search consumers load only their contract.</p>\n<h2>Pattern</h2>\n<h3>Route Shell</h3>\n<p>Tracked public HTML provides a stable URL and boot boundary. It owns presentation/runtime installation, not canonical record content.</p>\n<h3>Index Or Tree</h3>\n<p>Generated list/navigation payloads carry only the identity, ordering, hierarchy, labels, and small metadata needed for discovery. Exact shapes are domain-owned; a Docs Viewer tree is not a catalogue index schema.</p>\n<h3>Per-Record Payload</h3>\n<p>Generated by-id/per-record JSON carries heavier record-local structure and rendered HTML. Consumers fetch it after selecting an identity.</p>\n<h3>Search Projection</h3>\n<p>Search has its own normalized/ranked artifact because discovery text and fields are not the same as navigation or detail contracts.</p>\n<h2>Common Choices</h2>\n<ul>\n<li>a small header/schema/version/freshness/count block where the consumer needs it;</li>\n<li>object maps for canonical ID lookup;</li>\n<li>arrays when order/ranking is part of the contract;</li>\n<li>build-time rendered HTML when the browser should not parse Markdown;</li>\n<li>source-specific generated roots for public versus local/management data.</li>\n</ul>\n<p>These are tendencies, not one universal JSON schema. Read the owning builder/config/tests for exact fields.</p>\n<h2>Ownership Rules</h2>\n<ul>\n<li>canonical source and checked config are edited;</li>\n<li>builders own projections and skip/write behavior;</li>\n<li>public artifacts live under <code>site/</code>; local Docs Viewer generated reads live under their configured generated root;</li>\n<li>Data Sharing reads canonical Docs Viewer source context, not generated docs payloads;</li>\n<li>search reads its declared source artifacts and remains a separate projection;</li>\n<li>route consumers should not infer authority from whichever payload is easiest to fetch.</li>\n</ul>\n<h2>Assurance</h2>\n<p>Each domain should provide:</p>\n<ul>\n<li>one focused source/model doc;</li>\n<li>one builder/projection contract;</li>\n<li>schema/fixture checks for public/browser-visible fields;</li>\n<li>cross-reference/audit checks where artifacts join;</li>\n<li>a clear rebuild path from canonical source.</li>\n</ul>\n<p><a href=\"/docs/?scope=studio&amp;doc=data-models-projection-contract\">Projection Contract</a> explains the audit boundary. App/domain overviews point to their exact builders.</p>\n<h2>Weak Spots</h2>\n<ul>\n<li>One conceptual entity spans several artifacts, so partial rebuilds can leave temporary disagreement.</li>\n<li>Similar headers/IDs across domains can look like a shared schema when they are not.</li>\n<li>Legacy/compatibility fields in generated payloads can survive after consumers disappear.</li>\n<li>Central inventory pages go stale quickly; use builders/config/search to discover exact current files.</li>\n</ul>\n"
}
