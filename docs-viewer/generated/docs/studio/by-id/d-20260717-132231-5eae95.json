{
  "doc_id": "d-20260717-132231-5eae95",
  "title": "Docs Viewer Source And Publication Model Refactor",
  "added_date": "2026-07-17 13:22:31",
  "last_updated": "2026-07-17 15:00:57",
  "viewer_url": "/docs/?scope=studio&doc=d-20260717-132231-5eae95",
  "parent_id": "d-20260714-234030-1e0af6",
  "summary": "Establish role-first source, build, and published contracts with location-neutral workflows, then align scope structure and storage routing.",
  "ui_status": "planned",
  "content_html": "<h1>Docs Viewer Source And Publication Model Refactor</h1>\n<h2>Outcome</h2>\n<p>Docs Viewer uses one coherent source-to-published model before format-specific builders or further media features are added. Source, build, published artifact, storage location, public exposure, and scope ownership are separate contracts; build, import, inventory, and publish workflows are location-neutral. The explicit document, media, and sub-scope folder structure plus repository/external-local/R2 routing follow from that model rather than defining it.</p>\n<p>Every configured scope therefore has clear registered roots for canonical document/build source and published media. Documents and sub-scopes have visible boundaries; media that needs building keeps one source; already usable media has no Docs Viewer source and is stored once at its configured published location.</p>\n<p>The Docs Viewer-owned repository and external-local output workspaces are renamed from <code>generated/</code> to <code>published/</code>. Public <code>site/assets/</code> snapshots, R2 objects, and logical service routes retain their independent conventional paths.</p>\n<pre><code class=\"language-text\">source/&lt;scope&gt;/\n  documents/\n    *.md\n  media/\n    mermaid/\n      *.mmd\n  sub-scopes/\n    &lt;sub-scope&gt;/\n      documents/\n      media/\n\n&lt;configured published scope root&gt;/\n  media/\n    img/\n    files/\n    html/\n  sub-scopes/\n    &lt;sub-scope&gt;/\n      media/\n</code></pre>\n<p>Only media types the scope uses need to exist. New source or published types are added when they acquire a real validation, rendering, serving, build, or storage contract.</p>\n<p>External-local scopes use the same <code>documents/</code>, <code>media/</code>, and <code>sub-scopes/</code> shape beneath <code>$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/source/&lt;scope&gt;/</code>. That physical scope folder contains both source and published roles, so every external-local media type is held beneath its own scope's <code>media/</code> directory. Repository/R2 selection does not apply to them, and the parent path name does not make ready media into source.</p>\n<h2>Feature Documents</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260717-142549-d66485\">Source, Build, Published, And Location Concept</a> is the complete durable-ready semantic contract for source, build, published artifacts, location, public exposure, ownership, and the initial repo/R2 policy.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260717-132231-8cfc4d\">Docs Viewer Source And Publication Model Refactor Delivery</a> owns the cutover and verification sequence.</li>\n</ul>\n<h2>Ownership Model</h2>\n<ul>\n<li>A scope owns resources through all of its registered roots: document/build source, repository-published media, R2 prefixes, external-local media, document/search payloads, and public projections. Ownership is not restricted to containment beneath a path named <code>source/</code>.</li>\n<li>A document is the conceptual entity identified by <code>doc_id</code>. Its canonical Markdown file is the <strong>document source</strong>, its built JSON/HTML is the <strong>document payload</strong>, and the readable result is the <strong>rendered document</strong> or <strong>document view</strong>.</li>\n<li><code>documents/</code> contains one flat set of canonical Markdown files. It contains no document hierarchy and no structural subdirectories.</li>\n<li>Source <code>media/</code> contains canonical media inputs that require a build, such as Mermaid <code>.mmd</code>. Directly usable images, files, HTML, SVG, video, and similar bytes have no Docs Viewer source and belong only in their registered published media root.</li>\n<li>Published media types describe stable formats or handling boundaries, not changeable behaviour. HTML stays under <code>media/html/</code> whether it is static or interactive; sandboxing and script execution are code concerns.</li>\n<li>Mermaid source belongs under source <code>media/mermaid/</code>. Whether rendered manually or later by Docs Viewer, its SVG goes directly to the published <code>media/img/</code> target and is not also retained as source.</li>\n<li><code>sub-scopes/</code> makes nested collection ownership explicit. A sub-scope repeats the same document/media shape without appearing to be a folder inside the parent document collection.</li>\n<li>External Asset Collections remain independently owned roots outside the scope's registered source and published-media trees.</li>\n</ul>\n<p>Containment beneath any registered scope root supplies implicit ownership and discoverability. A generated inventory can make that concrete by recording media identity, document references, producer, backing, served path, and unreferenced assets.</p>\n<h2>Build Direction</h2>\n<p>A full Docs Viewer build treats source as read-only:</p>\n<ol>\n<li>inventory document source, build-source media, and existing published media;</li>\n<li>run any Docs Viewer-owned format builders directly into the configured published media root;</li>\n<li>render document payloads against the published media inventory;</li>\n<li>publish document/search projections where required.</li>\n</ol>\n<p>Images, files, HTML, manual renders, and other already usable media have no copy/build stage: import or manual placement writes them directly to the published root. Manual and automated production may target the same published identity, but two producers must never silently claim it.</p>\n<p>Build, import, inventory, and publish workflows resolve source and published locations through one common location boundary. Repository paths, external workspace paths, R2 authentication/object keys, temporary local staging, and provider capability checks belong in location helpers; the main workflows do not branch by provider.</p>\n<h2>Repository And R2 Policy</h2>\n<p>For repo-backed scopes, media references use logical scope/type/filename identity independently of physical storage. Repository or R2 backing is selected per media type so changing a decision requires a config path change, moving the bytes, and rebuilding document payloads—not rewriting document source.</p>\n<p>The selected backing is the one authoritative and served byte location:</p>\n<ul>\n<li>R2-backed media that needs no build is only the published object at <code>docs/&lt;scope&gt;/&lt;type&gt;/&lt;filename&gt;</code>; it is not also source;</li>\n<li>repository-backed media for a public scope is published once beneath <code>site/assets/data/docs/scopes/&lt;scope&gt;/media/&lt;type&gt;/</code>;</li>\n<li>repository-backed media without a public projection is stored once beneath its configured published scope root at <code>media/&lt;type&gt;/</code>; a current physical path named <code>generated</code> does not change that role;</li>\n<li>public projection status affects the repository path that can serve the bytes, but it does not decide whether the type uses repository or R2 backing.</li>\n</ul>\n<p>External-local scopes are deliberately simpler: all media types stay inside the external scope folder and follow that scope's create, rename, and delete lifecycle. They are never redirected into the repository or R2 by this policy.</p>\n<p>Current decisions:</p>\n<ul>\n<li>repository-first for ordinary Library and Analysis images, stored directly in their tracked public scope media roots;</li>\n<li>keep Moments images in R2 because they are part of the established Catalogue/Moments media system;</li>\n<li>keep downloadable files such as PDF, Word, archives, and similar externally sourced binaries in R2;</li>\n<li>keep directly usable SVG and HTML as single repository-published artifacts, and keep Mermaid <code>.mmd</code> as repository build source whose SVG targets the published image root;</li>\n<li>prefer R2 for large video, audio, datasets, or other types whose size makes repository storage impractical;</li>\n<li>keep every external-local media type in its external scope folder regardless of type or size;</li>\n<li>choose R2 for practical size and provenance reasons, not because a suffix belongs there in principle.</li>\n</ul>\n<p>R2-backed and repository-published media remains scope-owned through registered roots and inventory. Neither requires a redundant second Docs Viewer copy. Existing stable R2/cache behavior remains unchanged; this feature adds no purge, fingerprint, or versioning requirement.</p>\n<h2>Migration Boundary</h2>\n<p>This refactor includes the consequential source, config, build, and publication changes:</p>\n<ul>\n<li>move every normal scope's Markdown into <code>documents/</code>;</li>\n<li>move Analysis Tags into <code>sub-scopes/tags/documents/</code>;</li>\n<li>move the Library coincidence-salience HTML asset from its separate site path into <code>site/assets/data/docs/scopes/library/media/html/</code>, making the registered published location its only copy;</li>\n<li>inventory actual Docs Viewer R2 objects and their document references before moving or removing anything;</li>\n<li>move referenced Library and Analysis images from R2 directly into <code>site/assets/data/docs/scopes/&lt;scope&gt;/media/img/</code> and keep their logical references stable;</li>\n<li>leave Moments images and public downloadable files in R2 unless the inventory exposes a specific exception;</li>\n<li>preserve the same <code>documents/</code>, <code>media/</code>, and <code>sub-scopes/</code> structure for external-local scopes while keeping all of their media under the external source root;</li>\n<li>rename repository and external-local Docs Viewer output workspaces from <code>generated/</code> to <code>published/</code>, while leaving <code>site/assets/</code>, R2, and logical service routes independent;</li>\n<li>make registered config paths the sole topology authority by removing literal generated-root assumptions from validation, lifecycle, serving, manifests, browser config, and builders;</li>\n<li>replace scope-wide media storage modes with explicit type-level source/build/published roles and location mappings;</li>\n<li>update source loading, building, search, imports, watcher behavior, lifecycle, media inventory, serving, and validation to use the new roots without compatibility aliases or duplicate media copies.</li>\n</ul>\n<h2>Durable Home</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260423-000000-8fd731\">Source Organisation</a> will receive the complete concept section and own the shipped structure and build model. <a href=\"/docs/?scope=studio&amp;doc=d-20260514-184303-7914e2\">Media And Asset Handling</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260515-000000-f43740\">Scope Lifecycle</a>, and <a href=\"/docs/?scope=studio&amp;doc=d-20260623-221357-a97b66\">Embedded Detail Documents</a> receive only the focused current-state consequences they own after the refactor ships.</p>\n"
}
