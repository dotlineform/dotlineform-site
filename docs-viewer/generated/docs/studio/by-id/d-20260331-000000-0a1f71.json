{
  "doc_id": "d-20260331-000000-0a1f71",
  "title": "Srcset Builder",
  "added_date": "2026-03-31 00:00:00",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260331-000000-0a1f71",
  "parent_id": "d-20260423-000000-d015e6",
  "content_html": "<h1>Srcset Builder</h1>\n<h2>Purpose</h2>\n<p><code>studio/services/media/make_srcset_images.py</code> converts staged source images into the configured primary and thumbnail variants.</p>\n<p>The normal Work/detail path is the scoped catalogue build, which resolves source images, calls the derivative builder, copies public thumbnails, and reports readiness. Use the standalone command for explicit media maintenance or debugging, not as a second catalogue workflow.</p>\n<h2>Command Shape</h2>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 studio/services/media/make_srcset_images.py INPUT_DIR OUTPUT_DIR JOBS\n</code></pre>\n<p>Omitting the directories uses the configured Work staging/output roots. <code>--dry-run</code> previews derivative writes and source cleanup.</p>\n<p>Optional selected-ID and success-ID manifest paths plus the default worker count are read through names defined in <code>_data/pipeline.json</code>. Run <code>--help</code> and inspect <code>make_srcset_images.py</code> for the exact current argument/environment contract.</p>\n<h2>Ownership Boundary</h2>\n<p>The builder owns image decoding, scaling/cropping, encoding, variant filenames, parallel execution, and staged-source cleanup. It does not:</p>\n<ul>\n<li>read or write canonical catalogue records;</li>\n<li>select publication scope;</li>\n<li>generate public JSON or search;</li>\n<li>copy final assets into R2;</li>\n<li>promote confirmed media versions.</li>\n</ul>\n<p>Those responsibilities belong to catalogue build planning and <a href=\"/docs/?scope=studio&amp;doc=d-20260508-093841-3e27b1\">Publish Media To R2</a>.</p>\n<p>Variant widths, suffixes, subdirectories, format, codec, quality, and compression come from the shared pipeline config. Changing them is a pipeline/public-runtime change, not a local flag tweak.</p>\n"
}
