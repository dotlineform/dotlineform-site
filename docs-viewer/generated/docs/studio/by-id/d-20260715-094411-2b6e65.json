{
  "doc_id": "d-20260715-094411-2b6e65",
  "title": "Document Identity",
  "added_date": "2026-07-15 09:44:11",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-094411-2b6e65",
  "parent_id": "d-20260424-000000-50b63f",
  "summary": "Immutable document codes, timestamp provenance, creation boundaries, and the distinction between identity and human navigation.",
  "content_html": "<h1>Document Identity</h1>\n<h2>Why It Exists</h2>\n<p>A document's identity should survive changes to its title, meaning, hierarchy, feature ownership, and source organisation.</p>\n<p>Titles provide human navigation through the index, search, source view, and editor integrations. <code>doc_id</code> exists for durable references and machine contracts; putting mutable meaning into it causes link churn without improving discovery.</p>\n<h2>Identity Contract</h2>\n<p>New local documents use:</p>\n<pre><code class=\"language-text\">d-YYYYMMDD-HHMMSS-xxxxxx\n</code></pre>\n<ul>\n<li><code>d-</code> identifies a document code.</li>\n<li>The date and time match the document's second-resolution <code>added_date</code>.</li>\n<li>Six lowercase hexadecimal characters separate same-second and parallel creations.</li>\n<li>Allocation checks current identities before use; it does not require a central sequence or registry.</li>\n<li>The source Markdown filename matches the immutable code.</li>\n<li>Runtime lookup remains scope-local, although the shape makes accidental cross-scope collisions unlikely.</li>\n<li>Generated-data builds and generated document reads reject IDs outside this format.</li>\n</ul>\n<p>The identifier is not a title, slug, access token, security boundary, document type, app owner, hierarchy path, or promise about current content.</p>\n<p><code>docs-viewer/services/docs_document_identity.py</code> owns the format, timestamp, validation, and allocation primitives. Creation workflows own when an identity is allocated.</p>\n<h2>Creation And Update Rules</h2>\n<ul>\n<li>New Document, ordinary new imports, collection records that become new local documents, and the first document in a new scope allocate a local identity.</li>\n<li>Scope identity stays human-readable; its first document identity does not derive from the scope name.</li>\n<li>Import filenames, external record IDs, and titles are source context. A new local document records a different source ID as provenance rather than adopting it.</li>\n<li>Overwrite and returned-package update workflows preserve the selected canonical document identity, filename, <code>added_date</code>, and placement rules.</li>\n<li>Rename changes <code>title</code>; move changes <code>parent_id</code>; neither changes identity or filename.</li>\n<li>Duplicate allocates a new identity. Delete removes the identity.</li>\n<li>Planning stays write-free. Multi-step preview/apply workflows carry the planned identity into apply rather than silently allocating another after confirmation.</li>\n</ul>\n<p>Repository-owned and external-local scopes use the same identity contract and allocator. Migrating an external-local scope remains an explicit, selected operation because its source and generated files sit outside the repository boundary.</p>\n<p>The external migration plan:</p>\n<ul>\n<li>records configured workspace markers rather than machine-specific absolute paths;</li>\n<li>rewrites hierarchy, scope defaults, filenames, and known viewer links together;</li>\n<li>can consume an earlier migration map when external documents link into an already-migrated scope;</li>\n<li>retains no runtime aliases after verification and rebuild.</li>\n</ul>\n<p>The Notes scope was migrated after the repository corpus. Its date-only front matter used midnight because repository Git history is not evidence for external source files.</p>\n<h2>Date Provenance</h2>\n<p>New documents write <code>added_date</code> and <code>last_updated</code> as:</p>\n<pre><code class=\"language-yaml\">added_date: YYYY-MM-DD HH:MM:SS\n</code></pre>\n<p>The repository migration normalized existing <code>added_date</code> values by preserving explicit times, using Git addition time when a date-only value matched the Git date, and otherwise using midnight. Deliberately invented historical dates were retained.</p>\n<p>That migration was a provenance normalization, not a claim of perfect historical certainty. If a future legacy source has no usable date, assign an explicit conventional value rather than deriving identity from unstable filesystem metadata.</p>\n<h2>Search And Display</h2>\n<p>Titles and parent titles remain the visible navigation model. Search indexes the complete opaque ID as one exact lookup token for URLs, logs, reports, and diagnostics; date and random-suffix fragments do not enter ordinary relevance terms.</p>\n<p>No compatibility aliases, legacy-format lookup, or redirect registry were retained after migration. The migration command can read old IDs only while applying a reviewed rewrite plan; it is not part of runtime document resolution. Git history and the ignored reviewed migration maps are the repair surface for one-off mistakes.</p>\n<p>Docs Review package-record identities, package identities, semantic-reference target IDs, and reference slugs have separate owners and formats. They are not document identity exceptions. Docs Import maps a package create to a new local identity and an overwrite to the existing canonical identity.</p>\n<h2>Extension Rule</h2>\n<p>When adding a workflow that can create documents:</p>\n<ol>\n<li>decide whether the operation creates a genuinely new local document or updates an existing canonical one;</li>\n<li>allocate only for create, using the shared identity owner and the same timestamp written to <code>added_date</code>;</li>\n<li>preserve canonical identity for update;</li>\n<li>keep source-specific IDs in provenance;</li>\n<li>prove preview/apply identity stability and filename agreement in focused tests.</li>\n</ol>\n<p>Generated payloads, search entries, links, exports, logs, and reports consume source identity. They do not allocate or redefine it. Exact adapters and validators remain code/test authority.</p>\n"
}
