{
  "doc_id": "d-20260715-094411-2b6e65",
  "title": "Document Identity Concept",
  "added_date": "2026-07-15 09:44:11",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-094411-2b6e65",
  "parent_id": "d-20260715-094411-1c5fa7",
  "summary": "Durable purpose and mental model for Docs Viewer document identity, initially retained as the active feature concept.",
  "ui_status": "ready",
  "content_html": "<h1>Document Identity Concept</h1>\n<h2>Purpose</h2>\n<p>A document's identity should survive changes to its title, meaning, hierarchy, feature ownership, and source organisation.</p>\n<p>Docs Viewer now provides the useful navigation layer: index titles, search, source view, management tools, and VS Code integration. A title-like <code>doc_id</code> and filename no longer improve discovery enough to justify the link churn and semantic drift they create.</p>\n<h2>Identity Contract</h2>\n<p>Every newly allocated local identity uses:</p>\n<pre><code class=\"language-text\">d-YYYYMMDD-HHMMSS-xxxxxx\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-yaml\">doc_id: d-20260715-094411-2b6e65\ntitle: Document Identity Concept\n</code></pre>\n<ul>\n<li><code>d-</code> identifies a document code.</li>\n<li>The date and time record identity issuance to second precision.</li>\n<li>The six lowercase hexadecimal characters prevent same-second and parallel-creation collisions.</li>\n<li>The complete code is validated against current scope identities before use.</li>\n<li>The source Markdown filename matches the immutable code.</li>\n<li>Identity is scope-local in current runtime contracts, even though the generated shape makes accidental cross-scope collisions unlikely.</li>\n</ul>\n<p>The identifier is not a title, slug, access token, security boundary, document type, app owner, hierarchy path, or promise about current content.</p>\n<h2>Added Date</h2>\n<p><code>added_date</code> is useful provenance and should have one complete shape:</p>\n<pre><code class=\"language-yaml\">added_date: YYYY-MM-DD HH:MM:SS\n</code></pre>\n<p>For the current repository corpus:</p>\n<ol>\n<li>preserve an explicit front-matter time and add <code>:00</code> when only seconds are missing;</li>\n<li>when front matter has only a date and Git first added the current document on that date, use the Git time;</li>\n<li>otherwise preserve the chosen date and use <code>00:00:00</code>;</li>\n<li>retain deliberately invented dates, including the Moments dates, rather than replacing them with later repository-import times.</li>\n</ol>\n<p>The migration plan records whether each normalized timestamp came from front matter, matching-date Git history, or the midnight default. Exact historical certainty is not required; consistent useful provenance is.</p>\n<p>Every current repository document already has a date. If a future legacy source genuinely has none, assign an explicit conventional date such as <code>1900-01-01 00:00:00</code> rather than hiding uncertainty behind filesystem metadata.</p>\n<p>The document code derives its timestamp component from the normalized <code>added_date</code>. The random suffix remains necessary because several documents may share the same second.</p>\n<h2>Editing Consequences</h2>\n<ul>\n<li>Rename changes <code>title</code>, never <code>doc_id</code> or source filename.</li>\n<li>Move changes <code>parent_id</code>, never identity.</li>\n<li>Duplicate allocates a new identity.</li>\n<li>Delete removes the identity and allows genuinely meaningful inbound links to fail visibly.</li>\n<li>Ordinary HTML, Markdown, text, document, or media-wrapper imports allocate a new local identity independent of the staged filename or title.</li>\n<li>A returned package updating an existing canonical document preserves that document's supplied identity.</li>\n<li>A package record that becomes a new local document receives a local identity; any external identifier is provenance, not the local <code>doc_id</code>.</li>\n</ul>\n<p>No compatibility aliases or redirects are retained after the migration. The reviewed migration map and Git history are the repair surface for one-off mistakes.</p>\n<h2>Search And Display</h2>\n<p>Titles and parent titles remain the human navigation model. Opaque IDs should not contribute fragmented date and random-suffix tokens to ordinary relevance scoring. Exact-ID lookup may remain available for logs, URLs, reports, and diagnostics.</p>\n<h2>Lasting Invariants</h2>\n<ul>\n<li>Allocate once; never rename an identity.</li>\n<li>Keep identity free of mutable meaning.</li>\n<li>Display titles to people and codes to contracts.</li>\n<li>Preserve canonical identity through review and update workflows.</li>\n<li>Allocate local identity at the boundary where a genuinely new local document is created.</li>\n<li>Keep the generator centralized and the format consistent across creation paths.</li>\n<li>Treat generated payloads, search entries, references, and exports as consumers of source identity, not alternate owners.</li>\n</ul>\n<h2>Non-Goals</h2>\n<ul>\n<li>a global document registry or central sequence;</li>\n<li>meaningful or memorable URLs;</li>\n<li>aliases for previous title-derived IDs;</li>\n<li>rewriting Git history, completed logs, or historical packages;</li>\n<li>renaming unrelated media merely because its filename contains an old document slug;</li>\n<li>using predictability or obscurity as authorization.</li>\n</ul>\n<h2>Durable Role</h2>\n<p>After implementation, this document becomes the current Document Identity overview. Remove resolved proposal wording, keep the stable contract and extension rules, title it <code>Document Identity</code>, and reparent it under the durable Docs Viewer documentation without changing its <code>doc_id</code>.</p>\n"
}
