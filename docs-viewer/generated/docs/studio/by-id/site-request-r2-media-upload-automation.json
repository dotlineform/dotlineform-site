{
  "doc_id": "site-request-r2-media-upload-automation",
  "title": "R2 Media Upload Automation Request",
  "added_date": "2026-05-07",
  "last_updated": "2026-07-13",
  "viewer_url": "/docs/?scope=studio&doc=site-request-r2-media-upload-automation",
  "parent_id": "change-requests",
  "ui_status": "done",
  "content_html": "<h1>R2 Media Upload Automation Request</h1>\n<p>Status:</p>\n<ul>\n<li>the publisher is available through the CLI and a local Work editor action</li>\n<li><code>.env.local</code> and a bucket-scoped R2 account token are configured locally</li>\n<li>catalogue media staging is external under <code>$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/media/</code></li>\n<li>dry-run, unchanged comparison, forced replacement, and direct R2 verification have been completed for a catalogue work</li>\n<li>cache-safe public primary URLs now use the confirmed canonical <code>media_version</code></li>\n<li>the publisher now promotes <code>media_version</code> only after a complete successful R2 primary-set write and rebuilds the affected per-work JSON</li>\n<li>all existing media-bearing works and work details are backfilled to <code>media_version: 1</code></li>\n<li>stale catalogue <code>--kind moments</code> support has been removed from the publisher</li>\n<li>Work <code>Save</code> continues into R2 preview/confirmation when media publishing is eligible and remains enabled for a pending retry after cancellation or failure</li>\n<li>new Work-detail sections publish all newly created detail primary sets automatically; replacement of an existing individual detail remains CLI-only</li>\n<li>confirmed section and Work deletes clean exact R2 primary variants after the canonical/local delete, with manual-attention warnings instead of rollback or retry state</li>\n<li>the Docs Viewer media follow-up is implemented across public R2, repo-backed local, and external-local scopes</li>\n</ul>\n<h2>Summary</h2>\n<p>This request tracks adding a script-managed publishing step for media assets that currently need to be copied to Cloudflare R2 manually.</p>\n<p>The first supported use case is catalogue primary images generated by the local media pipeline.\nThe same credential and upload architecture now supports Docs Viewer images and files without a second security model.</p>\n<p>The upload, Work editor integration, cache-safe catalogue URL-version boundary, and Docs Viewer scope-aware media workflow are implemented.\nSuccessful complete-set uploads promote the canonical version and regenerate the affected per-work public payload; failed or partial attempts leave the confirmed version unchanged for a direct rerun.</p>\n<p>Implemented entrypoint:</p>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 studio/services/media/publish_media_to_r2.py --scope catalogue --kind works --id 01007\n</code></pre>\n<h2>Problem</h2>\n<p>The catalogue media pipeline stages source images, generates primary derivatives, and copies thumbnails into repo-owned public asset folders.\nPrimary derivatives remain under <code>$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/media/</code> as a manual handoff point for remote R2 publishing.</p>\n<p>That leaves a recurring manual step:</p>\n<ul>\n<li>find the generated primary derivatives</li>\n<li>upload them to the correct R2 bucket prefix</li>\n<li>avoid missing one size variant</li>\n<li>avoid overwriting the wrong remote object</li>\n<li>keep local logs clear enough to know what was published</li>\n</ul>\n<p>This is manageable while changes are small, but it is easy to forget and will become more awkward once docs imports can create image assets too.</p>\n<p>Overwriting the remote object is not sufficient to make the replacement visible immediately.\nCloudflare may continue serving the previous custom-domain response from its edge cache until that cache entry expires, even though the new object is already present in R2.</p>\n<h2>Goals</h2>\n<p>Add a project-local script that can publish approved media assets to R2.</p>\n<p>The workflow should:</p>\n<ul>\n<li>read R2 credentials from the local environment or a local-only secret file, never from tracked repo files</li>\n<li>support a dry-run preview before upload</li>\n<li>upload catalogue primary derivatives from the existing staging tree</li>\n<li>report skipped, uploaded, unchanged, and failed objects clearly</li>\n<li>avoid logging secrets or full credential-bearing URLs</li>\n<li>produce deterministic object keys</li>\n<li>make successful primary-image replacements visible through a deterministic media-version query</li>\n<li>keep the confirmed public media version separate from staged or incompletely uploaded media</li>\n<li>publish scope-owned Docs Viewer media through the same server-owned R2 boundary</li>\n</ul>\n<h2>Original Catalogue-Milestone Non-Goals</h2>\n<p>The original catalogue milestone did not:</p>\n<ul>\n<li>upload canonical source images</li>\n<li>delete remote R2 objects broadly or implicitly; exact-id primary-variant deletion is supported through <code>--delete</code></li>\n<li>expose R2 credentials to browser-side Studio code</li>\n<li>require credentials in generated docs, public JSON, or tracked config</li>\n<li>replace the existing local thumbnail copy behavior</li>\n<li>reduce the current CDN TTLs or add routine cache-purge API calls</li>\n<li>publish <code>/moments/</code> media through the catalogue adapter; Moments is now owned by the Docs Viewer scope</li>\n<li>add docs image uploading before the docs asset import model is settled</li>\n</ul>\n<h2>Credential Model</h2>\n<p>R2 credentials should be provided through environment variables that match the cloud/runtime convention already documented for the repo:</p>\n<ul>\n<li><code>R2_ACCOUNT_ID</code></li>\n<li><code>R2_ACCESS_KEY_ID</code></li>\n<li><code>R2_SECRET_ACCESS_KEY</code></li>\n<li><code>R2_BUCKET</code></li>\n<li><code>R2_ENDPOINT</code></li>\n</ul>\n<p>Local setup uses the gitignored <code>.env.local</code> file for those same R2 variables.\nThe file must be loaded only by CLI scripts or local server-side wrappers.\nThe script should fail closed when credentials are missing, with setup guidance that names the missing variables but never prints existing secret values.</p>\n<p>The R2 API key should be scoped as narrowly as Cloudflare allows.\nPreferred permissions:</p>\n<ul>\n<li>object read/write for the target bucket</li>\n<li>object list for preview and unchanged checks</li>\n<li>no account-wide permissions beyond what R2 S3-compatible access requires</li>\n<li>delete permission only when exact-id remote cleanup is intentionally used</li>\n</ul>\n<h2>Proposed Architecture</h2>\n<p>Introduce a media publisher module with a small registry of source adapters and remote destinations.</p>\n<p>Conceptual shape:</p>\n<pre><code class=\"language-text\">media publisher\n  credentials provider\n  R2 client\n  source adapter registry\n    catalogue primary image adapter\n    docs asset adapter\n  upload planner\n  upload executor\n  report writer\n</code></pre>\n<p>The script should separate:</p>\n<ul>\n<li>source discovery</li>\n<li>object-key mapping</li>\n<li>local/remote comparison</li>\n<li>dry-run reporting</li>\n<li>actual upload</li>\n<li>result logging</li>\n</ul>\n<p>That separation matters because catalogue primary images and Docs assets have different source roots, but share credentials, safety checks, and upload behavior.</p>\n<h2>External Media Workspace</h2>\n<p>Catalogue media staging uses one fixed external root:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/media/\n</code></pre>\n<p>The entire former <code>var/catalogue/media/</code> working tree is projected beneath that root, including staged source copies, generated primary and thumbnail variants, and staged work files.\nThere is no repo-local fallback or migration read path.\nThese artifacts are rebuildable, potentially large, and untracked, so keeping them inside the checkout provided no source-control benefit and allowed forgotten primary files to inflate the repository working directory.</p>\n<p><code>studio/shared/python/external_workspace_paths.py</code> owns the common <code>DOTLINEFORM_PROJECTS_BASE_DIR</code> resolution and confinement contract used by external workspaces.\nDomain adapters declare their fixed child roots: Catalogue uses <code>catalogue/media</code>, Data Sharing uses <code>data-sharing</code>, and Docs Viewer uses <code>docs-viewer</code> plus <code>docs-export</code> for static exports.\nThose adapters preserve domain-specific messages and configuration, but do not repeat base-directory or confinement logic.</p>\n<p>Local Studio previews request <code>/studio/media/catalogue/...</code>.\nThe local Studio server maps that URL directly to the allowlisted external catalogue-media root; it does not copy the image back into the repository or expose the rest of the projects directory.</p>\n<h2>First Use Case: Catalogue Primary Images</h2>\n<p>The catalogue adapter should discover generated primary derivatives under:</p>\n<ul>\n<li><code>$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/media/works/srcset_images/primary/</code></li>\n<li><code>$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/media/work_details/srcset_images/primary/</code></li>\n</ul>\n<p>Initial scope flags should allow targeted uploads such as:</p>\n<ul>\n<li>all catalogue primaries</li>\n<li>one work id</li>\n<li>one work-detail id</li>\n<li>one media kind</li>\n</ul>\n<p>The object-key mapping should match the public URL conventions already used by catalogue pages.\nThe implemented mappings are:</p>\n<ul>\n<li><code>works/img/&lt;work_id&gt;-primary-&lt;width&gt;.webp</code></li>\n<li><code>work_details/img/&lt;detail_uid&gt;-primary-&lt;width&gt;.webp</code></li>\n</ul>\n<p>The upload planner should verify that expected size variants exist for each selected item before upload, or clearly report partial sets.\nPartial uploads should either be blocked by default or require an explicit flag.</p>\n<h2>Moments Scope Boundary</h2>\n<p><code>/moments/</code> was part of the catalogue media model when this request was first written.\nIt has since become a Docs Viewer scope, so Moments media belongs with the implemented docs-owned asset workflow rather than the catalogue R2 adapter.</p>\n<p>The stale <code>--kind moments</code> publisher path has been removed.\nIt is not part of the catalogue media-version contract.</p>\n<h2>Delivery Cache And Media Version</h2>\n<p>The public R2 custom domain and GitHub Pages have independent cache behaviour.</p>\n<p>For R2 primary images, the live custom-domain response was observed with <code>Cache-Control: max-age=14400</code>.\nAfter a primary object was replaced successfully, the unversioned URL continued to return the old cached image while the same URL with a new query value returned the replacement.\nThe current Cloudflare cache key therefore distinguishes the version query, which gives the catalogue a simple cache-safe replacement contract without changing TTLs or adding purge credentials.</p>\n<p>Public catalogue primary URLs should use:</p>\n<pre><code class=\"language-text\">https://media.dotlineform.com/works/img/00008-primary-1200.webp?v=2\n</code></pre>\n<p>The version contract is:</p>\n<ul>\n<li><code>media_version</code> is a positive monotonically increasing integer for the independently replaceable primary-image set</li>\n<li>existing media-bearing works and work details are backfilled to <code>media_version: 1</code>; an empty string is not a valid steady state</li>\n<li>the confirmed version is stored in the canonical record and emitted in <code>site/assets/works/index/&lt;work_id&gt;.json</code></li>\n<li>work-detail media can carry its own version in the detail entry nested within that same per-work payload</li>\n<li>all widths in one primary set use the same version</li>\n<li>ordinary metadata or prose saves do not increment it</li>\n<li>selecting and generating replacement media makes Studio display the next candidate version as <code>confirmed + 1</code></li>\n<li>Save persists the source selection and staged derivatives but leaves the confirmed version unchanged</li>\n<li>before writing to R2, the publisher verifies that the complete required local variant set exists</li>\n<li>after every required R2 variant uploads successfully, the publisher promotes the candidate version in canonical data and rebuilds the affected per-work public JSON</li>\n<li>a failed, partial, or not-yet-run upload leaves the confirmed version number unchanged, reports the failure clearly, and is recovered by rerunning the publisher</li>\n<li>public primary <code>src</code> and <code>srcset</code> composition always appends the confirmed version as <code>?v=&lt;media_version&gt;</code></li>\n<li>aggregate lookup, search, recent, and series indexes do not need <code>media_version</code> because they render repo-owned thumbnails rather than R2 primary images</li>\n</ul>\n<p>The query is a cache-key version, not an archive of the corresponding R2 object.\nThe workflow does not add rollback, temporary-object, or atomic multi-object publishing machinery; if a rare mid-upload failure occurs, the operator reruns the reported upload.\n<code>studio/services/catalogue/catalogue_media_version.py</code> owns the narrow promotion seam.\nIt advances the canonical work or nested work-detail version only when remote bytes changed, then rebuilds the owning <code>site/assets/works/index/&lt;work_id&gt;.json</code> without regenerating local media or refreshing unrelated source dimensions.\nAn all-unchanged rerun rebuilds the current confirmed version without incrementing it, which also provides the recovery path when an earlier public-JSON rebuild failed after canonical promotion.</p>\n<p>Catalogue thumbnails are committed under <code>site/assets/...</code> and served separately by GitHub Pages through its CDN.\nThe live thumbnail response was observed with <code>Cache-Control: max-age=600</code>, so an overwritten thumbnail may remain stale for about ten minutes after the regenerated file is committed and deployed.\nThat short delay is acceptable: thumbnails remain unversioned, and the version field does not need to be copied into aggregate indexes solely for thumbnail cache busting.</p>\n<h2>Docs Viewer Media Follow-Up</h2>\n<p>Docs Viewer should use one scope-aware media policy rather than one physical storage location for every scope.\nThe storage labels describe application publication behavior, not a guarantee that bytes never exist in third-party infrastructure: the repository is public, and <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code> may itself be synchronized by iCloud or another filesystem provider. There is currently no sensitive Docs Viewer material that requires a stronger security boundary.</p>\n<p>The agreed defaults are:</p>\n<table>\n<thead>\n<tr>\n<th>Docs Viewer scope</th>\n<th>Ordinary images and files</th>\n<th>Application publication behavior</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>public scopes such as Library, Analysis, and Moments</td>\n<td>Cloudflare R2 under <code>docs/&lt;scope&gt;/...</code></td>\n<td>published through the shared media origin</td>\n</tr>\n<tr>\n<td>repo-backed local scopes such as Studio and temporary technical documentation</td>\n<td>repo-owned assets when needed</td>\n<td>retained with the repository and not intentionally copied to R2 by the Docs media workflow</td>\n</tr>\n<tr>\n<td>external-local scopes</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/...</code></td>\n<td>served only through the local Docs Viewer service and not copied into the repo, public site, or R2</td>\n</tr>\n</tbody>\n</table>\n<p>Thumbs and interactive HTML remain separate asset classes because they have their own rendering and sandbox contracts.\nThey must still follow the active scope's publication boundary: an external-local scope must not place either class in a repo/public location if support is added later.</p>\n<h3>Public Scope R2 Contract</h3>\n<p>Public Docs Viewer scopes should use the existing literal token form:</p>\n<pre><code>&#91;&#91;media:docs/&lt;scope&gt;/img/&lt;filename&gt;&#93;&#93;\n&#91;&#91;media:docs/&lt;scope&gt;/files/&lt;filename&gt;&#93;&#93;</code></pre>\n<p>The builder already resolves those tokens through the configured shared media base, and Moments already uses this convention for manually copied R2 images.\nLibrary and Analysis can use the same object-key family so public scopes do not need scope-specific media URL logic.</p>\n<p>Automated Docs uploads should:</p>\n<ul>\n<li>activate the reserved <code>r2_upload</code> Docs Import storage mode</li>\n<li>reuse the existing server-owned R2 credentials and client rather than exposing credentials to browser code</li>\n<li>upload only the media plans owned by the current import/apply action</li>\n<li>use stable deterministic object keys derived from the scope, media class, and chosen filename</li>\n<li>commit a source link to new remote media only after every required upload for that import record succeeds</li>\n<li>report safe scope/file identities without local absolute paths, object credentials, signed URLs, or checksums</li>\n</ul>\n<p>Docs Viewer cache invalidation is explicitly deferred.\nThe first implementation should not add fingerprinted filenames, canonical media versions, version query parameters, purge calls, or an asset registry.\nReplacing an existing R2 object may therefore remain stale at the delivery edge until its normal cache entry expires; that delay is acceptable at the current scale.\nIf one replacement needs to appear immediately, the operator can use a new filename and update the document token manually.\nIf a public scope such as Analysis later needs frequent reliable replacements, cache-safe versioning should be designed as a separate Docs asset-management capability rather than added implicitly to the uploader.</p>\n<p>Automatic remote deletion is not part of the first Docs slice.\nDocs assets may be shared across documents, so document deletion does not prove that an R2 object is unreferenced. A later reference/orphan report can identify cleanup candidates without adding rollback or ownership machinery to the initial uploader.</p>\n<h3>External-Local Media Contract</h3>\n<p>External-local media should be co-located with its external source and generated data under the configured projects base:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/img/\n$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/files/\n</code></pre>\n<p>This requires a new Docs Import storage mode, provisionally <code>external_assets</code>, that:</p>\n<ul>\n<li>derives the target root from the existing marker-rooted external scope configuration rather than accepting an arbitrary absolute path</li>\n<li>copies imported media into the allowlisted scope media root</li>\n<li>emits a scope-relative media reference rather than an R2 token or <code>/assets/...</code> URL</li>\n<li>serves media through a confined local Docs Viewer service route</li>\n<li>keeps absolute filesystem paths out of generated payloads and browser configuration</li>\n<li>rejects <code>r2_upload</code> and public repo-asset destinations for an external-local scope</li>\n</ul>\n<p>The external-local rule is about keeping the application workflow local to that scope. It does not claim that the underlying filesystem is physically offline or unsynchronized.</p>\n<h3>Existing Media And Migration</h3>\n<p>There is very little existing Docs Viewer media:</p>\n<ul>\n<li>Moments media is already manually stored in R2 and already uses the target token convention</li>\n<li>only a small amount of other public-scope media exists</li>\n<li>Studio and other local scopes currently have no media to migrate</li>\n</ul>\n<p>No bulk migration phase is required.\nExisting public media can remain in place or be normalized as it is next touched, while new public imports use R2 by default once the adapter is implemented.\nExisting repo-held technical-documentation references may remain repo-owned.</p>\n<p>The implemented sequence is:</p>\n<ol>\n<li><code>external_assets</code> is validated as the only external-local mode, and repo/R2 destinations are rejected there</li>\n<li><code>/docs/media/&lt;scope&gt;/&lt;class&gt;/&lt;filename&gt;</code> serves confined repo-backed and external-local media through the local service</li>\n<li><code>r2_upload</code> publishes a complete import record's media set under stable scope-owned keys before its Markdown source is committed</li>\n<li><code>--scope docs --docs-scope &lt;scope&gt; --kind img|files --staged-filename &lt;filename&gt;</code> publishes one manually staged public Docs asset</li>\n<li>cache versioning, automatic remote deletion, rollback, and migration automation remain intentionally out of scope</li>\n</ol>\n<h2>CLI Shape</h2>\n<p>Candidate script:</p>\n<pre><code class=\"language-text\">$HOME/miniconda3/bin/python3 studio/services/media/publish_media_to_r2.py --scope catalogue --kind works --id 01007\n$HOME/miniconda3/bin/python3 studio/services/media/publish_media_to_r2.py --scope catalogue --all --write\n</code></pre>\n<p>The default should be dry-run.\nWrite mode should require an explicit flag such as <code>--write</code>.</p>\n<p>Useful options:</p>\n<ul>\n<li><code>--scope catalogue</code></li>\n<li><code>--scope docs</code></li>\n<li><code>--kind works|work_details</code></li>\n<li><code>--kind img|files</code> with <code>--scope docs</code></li>\n<li><code>--id &lt;id&gt;</code></li>\n<li><code>--docs-scope &lt;scope&gt;</code></li>\n<li><code>--staged-filename &lt;filename&gt;</code></li>\n<li><code>--all</code></li>\n<li><code>--changed-only</code></li>\n<li><code>--force</code></li>\n<li><code>--write</code></li>\n<li><code>--report-json &lt;path&gt;</code></li>\n</ul>\n<h2>Upload Safety</h2>\n<p>The script should:</p>\n<ul>\n<li>read only from allowlisted local source roots</li>\n<li>refuse path traversal and symlink escapes</li>\n<li>compare local file size and checksum or ETag-compatible digest where practical</li>\n<li>skip unchanged remote objects by default</li>\n<li>require <code>--force</code> to overwrite changed remote objects when remote comparison finds a conflict</li>\n<li>use concise catalogue logs that show ids, local relative paths, object keys, and statuses</li>\n<li>keep Docs reports narrower by omitting absolute paths, object keys, checksums, ETags, and credentials</li>\n<li>never print secret values</li>\n<li>never include signed URLs in logs</li>\n</ul>\n<h2>Studio Integration</h2>\n<p>The Work editor uses <code>Save</code> as the sole media-publish entrypoint.\nSave runs its existing workflow first, then evaluates the unchanged media-publish eligibility contract: single saved published Work, ready local primary set, no unsaved source changes, available service, and idle route.\nAn ineligible result ends the composed workflow quietly.\nAn eligible result continues into R2 preview and confirmation and marks media publishing pending.\nCancellation, a blocked preview, or failure leaves that state in place, so Save remains enabled and its no-source-change path can retry the eligibility handoff.\nSuccessful publishing, loading another record, or leaving single-record mode clears the pending state.\nIt uses a server-owned preview/apply flow:</p>\n<ol>\n<li><code>POST /studio/api/catalogue/media-publish-preview</code> checks the expected confirmed media version, validates the complete local width set, and compares the variants with R2.</li>\n<li>Studio shows one terse mandatory <code>Publish R2 media?</code> or <code>Replace R2 media?</code> confirmation. Changed remote objects use the explicit <code>Replace media</code> action; detailed width and version counts stay out of this routine modal.</li>\n<li>Preview returns an opaque fingerprint of the exact local/remote comparison. <code>POST /studio/api/catalogue/media-publish-apply</code> repeats that comparison and rejects local-byte or remote-state drift before any upload.</li>\n<li>Apply performs the upload through the same media-owned publisher used by the CLI. Forced replacement also requires <code>confirm_overwrite: true</code> at the service boundary.</li>\n<li>A complete result promotes or retains the confirmed canonical version, rebuilds focused public work JSON, records the action in Studio Activity, and returns the refreshed Work record.</li>\n<li>The editor reloads the focused record and preview so the confirmed <code>?v=&lt;media_version&gt;</code> URL is immediately visible.</li>\n</ol>\n<p>The browser sends only the work id, expected media version, opaque preview fingerprint, explicit overwrite decision, and Studio Activity context.\nIt never receives raw R2 credentials, credential-bearing URLs, local paths, object keys, or checksums; <code>.env.local</code> is read only by the Local Studio service and media publisher.</p>\n<p>Existing Work-detail replacement continues to use the CLI because there is intentionally no individual detail mutation action.\nThe integrated Work editor does own aggregate detail-section creation and deletion:</p>\n<ul>\n<li>section creation writes and builds every selected detail first, then publishes those exact detail ids in one server-owned R2 run</li>\n<li>section deletion completes its canonical/public/local aggregate delete first, then removes every affected detail primary set from R2</li>\n<li>Work deletion cascades through the Work primary set and every remaining dependent detail primary set</li>\n<li>missing remote objects count as successful cleanup</li>\n<li>a rare remote failure leaves canonical creation/deletion complete and returns only safe affected ids for a manual Cyberduck or CLI follow-up; there is no rollback, pending job, or automated retry state</li>\n</ul>\n<h2>Documentation Requirements</h2>\n<p>Implementation should update:</p>\n<ul>\n<li>script reference docs for the new publisher</li>\n<li>local setup docs for R2 credential setup</li>\n<li>cloud environment docs for secret-store expectations</li>\n<li>catalogue media docs to replace the current manual R2 handoff note</li>\n<li>docs media handling and image guides for the enabled scope-aware workflow</li>\n</ul>\n<p>Documentation must keep examples project-local and avoid user-specific paths.</p>\n<h2>Acceptance Criteria</h2>\n<p>The first catalogue upload milestone is implemented:</p>\n<ul>\n<li>dry-run lists catalogue primary derivatives that would upload to R2</li>\n<li>write mode uploads selected catalogue primary derivatives only when <code>--write</code> is passed</li>\n<li>remote delete mode removes selected catalogue primary derivatives only when <code>--delete --write</code> is passed with an exact kind and id</li>\n<li>missing credentials fail with a clear non-secret error</li>\n<li>unchanged remote files are skipped by default</li>\n<li>changed remote files require <code>--force</code></li>\n<li>object keys are deterministic and documented in <a href=\"/docs/?scope=studio&amp;doc=scripts-publish-media-to-r2\">Publish Media To R2</a></li>\n<li>logs and optional JSON reports avoid secrets and signed URLs</li>\n<li>tests cover credential loading, source-root allowlisting, object-key mapping, dry-run planning, and overwrite behavior</li>\n</ul>\n<p>The cache-safe delivery follow-up is complete:</p>\n<ul>\n<li>existing media-bearing works and work details are backfilled to <code>media_version: 1</code></li>\n<li>the confirmed <code>media_version</code> is emitted in the appropriate per-work JSON only</li>\n<li>public R2 primary <code>src</code> and <code>srcset</code> URLs append that confirmed version</li>\n<li>Studio displays the current confirmed version and a candidate next version when replacement media is staged</li>\n<li>Save does not advance the confirmed version</li>\n<li>the publisher validates a complete local variant set before upload</li>\n<li>a complete successful upload advances the canonical version and rebuilds the affected per-work JSON</li>\n<li>a failed or partial upload reports failure without advancing the confirmed version and can be rerun directly</li>\n<li>stale catalogue support for <code>--kind moments</code> has been removed</li>\n</ul>\n<p>The Local Studio integration is complete for Works:</p>\n<ul>\n<li>a published Work with a ready complete local primary set is eligible for media publishing after Save</li>\n<li>Save automatically continues into the media-publish workflow when eligible and exits quietly when it is not</li>\n<li>there is no separate media-publish button; pending cancellation and failure retries keep Save enabled until publishing succeeds or the record context changes</li>\n<li>unsaved, unpublished, unavailable, or incomplete Work media cannot be applied from the editor</li>\n<li>preview is write-free and confirmation is mandatory</li>\n<li>apply revalidates the opaque preview fingerprint and stops before upload when local bytes or remote state drift</li>\n<li>changed remote objects require explicit replacement confirmation</li>\n<li>the local service owns credential reads, R2 comparison, upload, version promotion, and public JSON regeneration</li>\n<li>the browser response omits credentials, signed URLs, local paths, object keys, and checksums</li>\n<li>successful apply refreshes the editor to the confirmed media version and writes a unified Studio Activity row</li>\n<li>new detail-section creation publishes every created detail primary set after the complete local build succeeds</li>\n<li>detail-section deletion removes every affected detail primary set after canonical/public/local deletion</li>\n<li>Work deletion also cleans all remaining dependent detail primary sets without requiring the user to delete sections first</li>\n<li>existing individual detail replacement remains CLI-only</li>\n</ul>\n<p>The Docs Viewer media follow-up is complete:</p>\n<ul>\n<li>public Library, Analysis, and Moments scopes use <code>r2_upload</code> with deterministic <code>docs/&lt;scope&gt;/&lt;class&gt;/&lt;filename&gt;</code> keys</li>\n<li>one import record is fully preflighted, and its source link is committed only after every required upload succeeds or is confirmed unchanged</li>\n<li>remote comparison conflicts block the import record; ordinary Docs Import never treats document overwrite confirmation as media overwrite authority</li>\n<li>R2 credentials and remote object details remain server-side, while browser/result payloads expose only safe scope, class, filename, size, and status fields</li>\n<li>repo-backed local scopes store imported media under their docs source <code>media/</code> tree and use the confined local media route</li>\n<li>external-local scopes store media under <code>$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/...</code> and cannot select R2 or repo-public destinations</li>\n<li>the local route rejects traversal/symlink escape and does not serve interactive HTML through the ordinary media boundary</li>\n<li>the CLI supports a dry-run-first exact public Docs scope/class/filename upload from the shared import-staging drop-zone</li>\n<li>Docs media cache versioning, automatic deletion, and an asset registry remain deferred</li>\n</ul>\n<h2>Benefits</h2>\n<ul>\n<li>removes the manual R2 copy step for catalogue primary images</li>\n<li>reduces the chance of missing or mismatched primary image variants</li>\n<li>keeps one secure credential boundary across catalogue and Docs publishing</li>\n<li>gives Docs image publishing an exact shared runner instead of a separate ad hoc script</li>\n</ul>\n<h2>Risks</h2>\n<ul>\n<li>incorrect object-key mapping could overwrite or publish media in the wrong R2 location</li>\n<li>overly broad R2 credentials would increase blast radius if leaked</li>\n<li>remote comparison behavior may differ from local checksum expectations</li>\n<li>promoting a version before all variants are uploaded could cache an incomplete replacement under the new public URL</li>\n<li>GitHub-hosted thumbnails may briefly lag behind the versioned R2 primary after a deployment</li>\n<li>Docs R2 replacements can remain edge-cached until expiry; immediate replacement still requires a new filename and source-token update</li>\n</ul>\n<h2>Related References</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=scripts-build-catalogue-json\">Build Catalogue JSON</a></li>\n<li><a href=\"/docs/?scope=studio&amp;doc=scripts-catalogue-write-server\">Catalogue Write Server</a></li>\n<li><a href=\"/docs/?scope=studio&amp;doc=scripts-cloud-environments\">Cloud Environments</a></li>\n<li><a href=\"/docs/?scope=studio&amp;doc=user-guide-docs-images\">Docs Images</a></li>\n</ul>\n"
}
