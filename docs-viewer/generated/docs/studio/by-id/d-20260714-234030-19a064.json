{
  "doc_id": "d-20260714-234030-19a064",
  "title": "External Asset Collections Architecture",
  "added_date": "2026-07-14 23:40:30",
  "last_updated": "2026-07-14",
  "viewer_url": "/docs/?scope=studio&doc=d-20260714-234030-19a064",
  "parent_id": "d-20260714-194217-b2bdd7",
  "summary": "Proposed ownership, execution, extension, and safety boundaries for External Asset Collections.",
  "ui_status": "proposed",
  "content_html": "<h1>External Asset Collections Architecture</h1>\n<h2>Architectural Outcome</h2>\n<p>External Asset Collections should extend the existing scope lifecycle, capabilities, local-service, and report boundaries without creating a second document system or a general asset application.</p>\n<p>This document records the proposed structure and its known pressure points. Exact current behavior remains authoritative in the linked code and focused tests.</p>\n<h2>Ownership And Storage</h2>\n<p>An enabled scope uses the derived root:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/\n</code></pre>\n<ul>\n<li>Scope config records capability state and the report document id, not a resolved absolute path.</li>\n<li>The collection is associated with the scope but remains user-owned data.</li>\n<li>Manifest state must distinguish it from lifecycle-owned source and generated files.</li>\n<li>Delete preview names the preserved collection; delete apply never removes it.</li>\n<li>Rename must not silently relocate an enabled collection. Blocking rename is the first safe contract.</li>\n</ul>\n<p>Repo-managed attachments in <code>docs-viewer/source/&lt;scope&gt;/media/</code> remain separate. Existing <code>import_media_storage.storage_mode: external_assets</code> continues to describe where Docs Import materializes attachments for external-local scopes; it does not itself enable collection inventory.</p>\n<h2>Execution Path</h2>\n<pre><code class=\"language-text\">scope config\n  -&gt; management capability projection\n  -&gt; report metadata selects scope_assets\n  -&gt; local report loader requests confined inventory\n  -&gt; local service derives the root and scans metadata\n  -&gt; report renders relative paths and reference state\n</code></pre>\n<p>The initial scan should be synchronous and metadata-only. It preserves nested relative paths, avoids symlink following, skips minimal platform noise, and reports partial warnings without making unreadable entries fatal.</p>\n<p>One report-backed source document exposes the inventory. Asset-specific loading, state, and rendering remain in a focused report owner rather than the shared document controller.</p>\n<h2>Local File Boundary</h2>\n<p>Nested collection links use a separate loopback-service route from the existing flat <code>/docs/media/&lt;scope&gt;/&lt;img-or-files&gt;/&lt;filename&gt;</code> import-media route.</p>\n<p>The service must:</p>\n<ul>\n<li>require a configured local scope with the capability enabled</li>\n<li>derive and confine the root on the server</li>\n<li>reject absolute paths, traversal, malformed encoding, and symlink escape</li>\n<li>expose relative paths only</li>\n<li>apply <code>nosniff</code> and a narrow inline MIME policy</li>\n<li>force executable, interactive, unknown, and ordinary HTML content to download</li>\n</ul>\n<p>Public route shells and browser config receive no index, local root, or service URL.</p>\n<h2>Extension Method</h2>\n<p>Use existing owners:</p>\n<ul>\n<li>scope config — enabled state and report document id</li>\n<li>scope lifecycle and manifest — create, rename, delete, and preservation consequences</li>\n<li>management capabilities — service support and per-scope enabled state</li>\n<li>report metadata registry — selection of the <code>scope_assets</code> presentation</li>\n<li>focused local service — confined inventory and nested file reads</li>\n</ul>\n<p>Filesystem inventory is runtime data, not another registry. Do not add a plugin, action, or general asset registry framework.</p>\n<h2>Current Code Pressure Points</h2>\n<ul>\n<li><code>docs-viewer/services/docs_media_storage.py</code> couples the derived external media root to external-local import storage.</li>\n<li><code>docs-viewer/services/docs_scope_manifest.py</code> primarily models lifecycle-owned file records.</li>\n<li><code>docs-viewer/services/docs_scope_delete.py</code> and <code>docs-viewer/services/docs_scope_rename.py</code> currently give external media different preservation consequences.</li>\n<li>The existing <code>/docs/media/</code> contract is flat and classed as <code>img|files</code>.</li>\n<li>Project State has a proven metadata traversal, but its walker is mixed with catalogue-specific filtering and cannot be reused directly.</li>\n</ul>\n<p>These are inspection starting points, not an exhaustive implementation list. A delivery should update only the owners required for its outcome.</p>\n<h2>Known Weak Spots</h2>\n<ul>\n<li>The same derived physical root may contain import-managed attachments and user-managed project files; capability and ownership state must remove that ambiguity.</li>\n<li>A derived scope-id path conflicts with preserving files across rename; the first release should block rather than hide a relocation policy.</li>\n<li>Large-tree responsiveness is unknown. Measure Processing and I Ching before adding caches, jobs, hashes, or previews.</li>\n<li>Reference correlation may initially understand only current Docs media and asset routes; any compact authoring token is a separate design decision.</li>\n</ul>\n<h2>Delivery Boundary</h2>\n<p>The feature parent owns its delivery boundaries. The <a href=\"/docs/?scope=studio&amp;doc=d-20260714-234030-1e0af6\">Docs Viewer Roadmap</a> records only suggested feature order. Each delivery must complete one improvement to this architecture and transfer shipped behavior to the named durable owner.</p>\n"
}
