{
  "doc_id": "d-20260614-201632-79c9cb",
  "title": "Record List And Actions",
  "added_date": "2026-06-14 20:16:32",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260614-201632-79c9cb",
  "parent_id": "d-20260505-181739-bb5223",
  "content_html": "<h1>Record List And Actions</h1>\n<p>Use <code>RecordList</code> for a compact row list with fixed columns and optional single-row selection. Use <code>RecordListActions</code> when buttons outside the list act on its current selection.</p>\n<h2>Authority</h2>\n<ul>\n<li>behaviour: <code>shared/frontend/js/record-list.js</code></li>\n<li>styles: <code>shared/frontend/css/record-list.css</code></li>\n<li>focused tests: search <code>tests/</code> for <code>record-list</code></li>\n</ul>\n<p>Current consumers are in the Catalogue Work and Series adapters. Find the exact set by searching for imports from <code>/shared/frontend/js/record-list.js</code>.</p>\n<h2>Stable Structure</h2>\n<p><code>createRecordList(root, options)</code> receives route-normalized records and column definitions. It owns:</p>\n<ul>\n<li>header, row, cell, empty, link, and image rendering</li>\n<li>safe URL handling for link and image cells</li>\n<li>optional single-row selection</li>\n<li>pointer and keyboard selection</li>\n<li>focus boundaries and optional clear-on-blur behaviour</li>\n<li>selection events and update/destroy lifecycle</li>\n</ul>\n<p><code>createRecordListActions(root, options)</code> receives a list controller and action definitions. It owns button rendering, selection-dependent disabled state, action events, and registration of the toolbar as a focus boundary.</p>\n<p>The route owns:</p>\n<ul>\n<li>mapping domain records into rows and columns</li>\n<li>deciding which actions exist and when they are available</li>\n<li>modals, confirmation, validation, mutations, dirty state, saves, and server calls</li>\n<li>re-rendering from canonical route state after an action</li>\n</ul>\n<p>The component never mutates a route record.</p>\n<h2>Extension Boundary</h2>\n<p>Add a shared column type or interaction only when more than one domain can use the same behaviour. Keep domain filtering, row ordering, edit forms, destructive previews, and save logic in route adapters.</p>\n<p>For exact option names, callback payloads, supported cell types, and controller methods, read the exported factory functions. Copying that inventory into this page makes it drift without improving discovery.</p>\n<h2>Method And Weak Spots</h2>\n<ul>\n<li>Selection is deliberately single-row. Use <a href=\"/docs/?scope=studio&amp;doc=d-20260627-144244-b9cdf0\">Selectable List</a> for multi-selection or tree selection.</li>\n<li>The action toolbar is separate so list focus and workflow actions can evolve independently, but adapters must keep the toolbar and list lifecycle synchronized.</li>\n<li>Column definitions are flexible but untyped. A bad accessor or width is found at runtime.</li>\n<li>Record/action lists are display-and-command primitives, not inline editors. Add route-owned editing first; only generalize it after a second real use appears.</li>\n</ul>\n<h2>Verification</h2>\n<p>Protect the durable boundaries:</p>\n<ul>\n<li>safe text, link, and image rendering</li>\n<li>empty and header modes</li>\n<li>click and keyboard selection</li>\n<li>selection event and action payloads</li>\n<li>disabled actions before selection</li>\n<li>focus-boundary behaviour</li>\n<li>update and destroy cleanup</li>\n</ul>\n<p>Consumer tests should cover record mapping and resulting mutations. Visual fit, toolbar placement, and modal flow remain route-level manual checks.</p>\n"
}
