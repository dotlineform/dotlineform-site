{
  "doc_id": "d-20260715-181845-524859",
  "title": "Document Visualisations Concept",
  "added_date": "2026-07-15 18:18:45",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-181845-524859",
  "parent_id": "d-20260715-181845-0158f9",
  "summary": "Concept separating authored documentation diagrams from a data-driven Docs Viewer graph of semantic and catalogue relationships.",
  "ui_status": "proposed",
  "content_html": "<h1>Document Visualisations Concept</h1>\n<h2>Why Consider It</h2>\n<p>High-level diagrams could make execution paths, ownership, and public/manage/server boundaries easier to understand than prose or text art.</p>\n<p>There is also a concrete data-driven destination: a Docs Viewer panel that visualises the network formed by document semantic references, catalogue Works, and Series. <code>/analysis/</code> may eventually publish or extend that view.</p>\n<p>These needs should not be collapsed into one renderer merely because both produce graphics.</p>\n<h2>Authored-Diagram Decision</h2>\n<p>Use D2 as the default source language for authored Studio architecture and workflow diagrams.</p>\n<p>D2 source is the durable authority. The D2 CLI renders it to SVG before Docs Viewer reads the document; Docs Viewer displays the SVG as an ordinary image and does not parse D2 or load a diagram runtime in the browser.</p>\n<p>This is a default for maintainable technical diagrams, not a ban on ordinary images, inline SVG, or bespoke interactive assets where those are the better fit. It also has no bearing on the future D3 semantic graph: D2 produces authored figures, while D3 would render live repository data in a dedicated panel.</p>\n<h2>Three Different Capabilities</h2>\n<h3>Authored Diagrams</h3>\n<p>Text in Markdown describes a flowchart, sequence, state, relationship, or architecture map. The diagram changes when the author edits that text.</p>\n<p>This is the likely fit for Docs Viewer architecture and workflow documentation.</p>\n<h3>Build-Rendered Figures</h3>\n<p>A command turns diagram source into SVG or another static asset during a docs or site build. Readers receive the result without downloading the rendering library.</p>\n<p>This improves runtime independence and export behavior, but adds a local and CI build dependency plus generated-asset ownership.</p>\n<h3>Data-Driven Visualisations</h3>\n<p>A specification or browser module reads generated data and produces charts, plots, networks, or interactive exploration. The data owner drives the result; Markdown may explain or host it but is not the data source.</p>\n<p>This is the long-term fit for the semantic relationship graph and possible public <code>/analysis/</code> use. It should use a focused panel or visualisation owner rather than arbitrary executable code embedded in Markdown.</p>\n<p>These capabilities may share presentation styles, but they do not need one renderer, source format, or delivery.</p>\n<h2>Semantic Relationship Graph</h2>\n<p>The intended graph is a projection over relationships the repository already owns:</p>\n<ul>\n<li>semantic-reference artifacts provide document-to-Work, document-to-Series, document-to-Moment, and future registry-backed edges;</li>\n<li>catalogue data owns Work-to-Series membership and canonical Work/Series identity;</li>\n<li>generated browser-safe records provide titles, routes, and compact display metadata;</li>\n<li>a Docs Viewer panel owns graph layout, filtering, selection, and navigation.</li>\n</ul>\n<p>Likely initial node types are documents, Works, and Series. Moments, tags, and other semantic kinds can join only through their existing registry and data owners.</p>\n<p>D3 is a strong candidate because this is an interactive network/index view rather than a conventional chart. The visualisation would consume a bounded graph projection shaped for the panel; it should not make the browser reconstruct canonical relationships from unrelated payloads or create a second semantic-reference model.</p>\n<p>The graph is initially a read and navigation surface. Editing tokens, catalogue membership, or canonical records remains in the existing authoring applications.</p>\n<h2>Consequences Of Choosing D2</h2>\n<ul>\n<li><strong>New local tool:</strong> machines that author or render diagrams need the standalone D2 CLI. Ordinary Docs Viewer readers do not.</li>\n<li><strong>Pinned, offline build:</strong> the repository must name a supported D2 version and provide one project command that checks it. A docs build must never download or update D2 implicitly.</li>\n<li><strong>No Node consequence:</strong> D2 does not require <code>package.json</code>, npm, or browser JavaScript. A future D3 delivery may create a JavaScript dependency for its own reasons.</li>\n<li><strong>Build-rendered SVG:</strong> SVG is the default output. It stays sharp at different sizes and avoids the headless-browser dependency used by D2's PNG export.</li>\n<li><strong>Deliberately narrow format:</strong> the first Docs Viewer contract is one D2 board rendered to SVG. D2 can also produce compositions, presentations, animation, PNG, PDF, and other outputs; those capabilities do not become Docs Viewer requirements merely because the CLI provides them.</li>\n<li><strong>Two owned artifacts:</strong> reviewable <code>.d2</code> source is canonical; SVG is derived. Both need explicit Docs Viewer-owned locations, and Studio-only diagrams must not drift into the public <code>site/</code> tree.</li>\n<li><strong>Normal Markdown image:</strong> documents refer to the rendered SVG through the existing media path rather than injecting generated SVG markup. The surrounding heading, caption, and prose carry the accessible explanation.</li>\n<li><strong>Reproducible rendering:</strong> layout engine, theme, padding, fonts, and other material options belong in the project command rather than ad hoc author commands.</li>\n<li><strong>Failure by exit status:</strong> validation must use the CLI result, not merely the presence of an SVG, because D2 can leave partial output after a render error.</li>\n<li><strong>Watcher/build work:</strong> the implementation must decide when changed <code>.d2</code> sources are rendered, how stale SVG is detected, and how a diagram error is reported without corrupting unrelated document output.</li>\n<li><strong>Publication remains explicit:</strong> Studio diagrams remain local. If a public scope later adopts D2, its rendered SVG must enter that scope's normal asset and manual publication gate; the D2 executable still does not ship to the public site.</li>\n<li><strong>Static export:</strong> exports should carry the rendered SVG and retain the <code>.d2</code> source only in complete source-and-asset packages where canonical dependencies are expected.</li>\n</ul>\n<p>The <a href=\"https://d2lang.com/tour/man/\">D2 CLI</a> and <a href=\"https://d2lang.com/tour/exports/\">export documentation</a> are the upstream authority for command and format behavior. The repository's eventual wrapper and pinned version will be the authority for this project.</p>\n<h2>Invariants</h2>\n<ul>\n<li>Markdown remains readable and editable without a graphical renderer.</li>\n<li>A broken diagram does not prevent the rest of its document from rendering.</li>\n<li>Ordinary documents do not load a visualisation dependency they do not use.</li>\n<li>Public routes receive no local-only renderer, service, or data source by accident.</li>\n<li>Data-driven visualisations preserve the canonical data owner's authority.</li>\n<li>The semantic graph consumes existing reference and catalogue identities rather than defining parallel nodes or edges.</li>\n<li>Markdown does not become a route for arbitrary JavaScript execution.</li>\n<li>Versions are pinned and generated results are reproducible enough to review through source control and the publication gate.</li>\n</ul>\n<h2>Questions Before A Delivery</h2>\n<ol>\n<li>Which two or three current documents should prove the D2 convention?</li>\n<li>Where should canonical <code>.d2</code> sources and derived Studio SVGs live?</li>\n<li>Should D2 rendering be part of the normal docs build, a preceding focused command, or both through one shared service?</li>\n<li>How should the pinned version be installed and checked across local and GitHub workflow environments?</li>\n<li>Which layout, theme, sizing, and accessibility defaults are enough for the first delivery?</li>\n<li>For the semantic graph, what question should the first view answer that the current reference report cannot?</li>\n<li>Which node and edge types make a bounded useful first graph, and how large is the real dataset?</li>\n<li>Should the graph begin as a local Docs Viewer panel before any public <code>/analysis/</code> promotion?</li>\n</ol>\n<h2>Separate Delivery Candidates</h2>\n<ul>\n<li>Authored documentation diagrams: install and pin D2, define the <code>.d2</code>-to-SVG ownership and build contract, and prove it on a small set of architecture/workflow documents.</li>\n<li>Semantic relationship graph: prove one read-only Docs Viewer panel over an explicit generated graph projection, initially limited to the node and edge types needed for a useful index view.</li>\n</ul>\n<p>These should remain separate roadmap deliveries even if both eventually use repository-level JavaScript tooling.</p>\n<p>When those answers point to one complete result, add an architecture document if needed and promote one bounded delivery into the <a href=\"/docs/?scope=studio&amp;doc=d-20260714-234030-1e0af6\">Docs Viewer Roadmap</a>.</p>\n"
}
