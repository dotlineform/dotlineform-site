{
  "doc_id": "runtime-dependencies",
  "title": "Runtime Dependencies",
  "added_date": "2026-04-23",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=runtime-dependencies",
  "parent_id": "dev-home",
  "content_html": "<h1>Runtime Dependencies</h1>\n<h2>Authorities</h2>\n<ul>\n<li><code>requirements.txt</code> — pinned Python package baseline.</li>\n<li><code>.codex/setup.sh</code> — cloud bootstrap and optional system-package installation.</li>\n<li><code>.devcontainer/Dockerfile</code> / <code>devcontainer.json</code> — Codespaces image and environment.</li>\n<li>owning scripts — exact external commands a workflow invokes.</li>\n</ul>\n<p>Read those files for current versions and exhaustive package lists. This page explains dependency classes and change method.</p>\n<h2>Python Baseline</h2>\n<p>The pinned environment currently supports:</p>\n<ul>\n<li>spreadsheet/catalogue I/O;</li>\n<li>HTML parsing and sanitization for Docs Import;</li>\n<li>raster conversion for document/media import;</li>\n<li>Python Markdown rendering for docs/catalogue prose;</li>\n<li>pytest collection.</li>\n</ul>\n<p>All packages in <code>requirements.txt</code> are installed for local/cloud parity even when a specific task uses only part of the stack. “Feature-specific use” is not the same as “optional install” once pinned in the baseline.</p>\n<h2>External Tools</h2>\n<ul>\n<li><code>ffmpeg</code> and <code>heif-convert</code> are workflow-specific media tools.</li>\n<li><code>sips</code> is a macOS-only convenience used by some local image paths.</li>\n<li>Git/GitHub CLI and compiler/build packages support bootstrap/development rather than app runtime.</li>\n</ul>\n<p>Docs/search/static validation does not require media tools. A media workflow must check its actual command dependencies and source roots before running.</p>\n<h2>Runtime Boundaries</h2>\n<p>Public preview, Studio, Admin, Analytics, and Docs Viewer are separate Python-backed services/runners. Sharing one interpreter/dependency set does not merge their routes or ownership. <code>bin/local-all</code> supervises siblings; it is not a combined app server.</p>\n<h2>Local Versus Cloud</h2>\n<ul>\n<li>Local commands follow the configured Miniconda interpreter convention.</li>\n<li>Cloud/Codespaces bootstrap creates/reuses <code>.venv</code> and installs <code>requirements.txt</code>.</li>\n<li>Codespaces includes media tools in the image; Codex setup installs them only when requested.</li>\n<li>Private/external source roots and publishing credentials remain environment/secret-store concerns, not dependency setup.</li>\n</ul>\n<h2>Change Method</h2>\n<h3>Python Package</h3>\n<ol>\n<li>Prove the owning workflow needs it and an existing dependency/standard library is insufficient.</li>\n<li>Pin it in <code>requirements.txt</code>.</li>\n<li>Update bootstrap only if installation needs system support.</li>\n<li>Run <code>pip check</code> plus the focused consumer test.</li>\n<li>Update this page only if a dependency class or bootstrap expectation changed.</li>\n</ol>\n<h3>System Tool</h3>\n<p>Document/check it in the owning workflow and add it to cloud/container setup only when that environment must run the workflow. Do not make all tasks depend on media tooling.</p>\n<h2>Weak Spots</h2>\n<ul>\n<li>One requirements file installs feature-specific packages everywhere and will grow without periodic review.</li>\n<li>Local Miniconda and cloud <code>.venv</code> may use different Python patch/system-library versions.</li>\n<li>Command-line dependencies are discovered from scripts rather than one machine-readable registry.</li>\n<li>A successful bootstrap does not prove private media/workspace availability or publish credentials.</li>\n</ul>\n"
}
