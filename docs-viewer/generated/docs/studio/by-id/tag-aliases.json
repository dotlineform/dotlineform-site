{
  "doc_id": "tag-aliases",
  "title": "Tag Aliases",
  "added_date": "2026-03-31",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=tag-aliases",
  "parent_id": "analytics",
  "content_html": "<h1>Tag Aliases</h1>\n<h2>Capability</h2>\n<p><code>/analytics/tag-aliases/</code> maintains shorthand that resolves to canonical tags: browse/filter, import, create, edit, delete, promote an alias to a canonical tag, and demote a canonical tag into an alias.</p>\n<p>Aliases preserve useful language without making every phrase canonical vocabulary. They can also be recorded as historical selection context on assignment rows while canonical tag IDs remain the actual assignment identity.</p>\n<h2>Workflow</h2>\n<pre><code class=\"language-text\">load aliases + registry + assignment context\n  -&gt; edit/import or choose promote/demote\n  -&gt; validate alias key and canonical targets\n  -&gt; preview cross-file impact where required\n  -&gt; confirm\n  -&gt; server planner + atomic apply\n  -&gt; reload + activity evidence\n</code></pre>\n<p>Direct server writes are preferred. When the local service is unavailable, supported operations return manual patch snippets. Those snippets describe work for a maintainer; they do not become canonical state automatically.</p>\n<h2>Durable Rules</h2>\n<ul>\n<li>alias keys are slug-safe;</li>\n<li>an alias targets at least one canonical tag;</li>\n<li>one alias cannot target two tags from the same group;</li>\n<li>target IDs must exist in the canonical registry;</li>\n<li>promotion creates a canonical tag and reconciles the alias/assignment references defined by the planner;</li>\n<li>demotion removes a canonical identity only through an explicit replacement plan.</li>\n</ul>\n<p>Limits and exact request shapes live in <code>tag_source_model.py</code> and focused tests.</p>\n<h2>Code Map</h2>\n<ul>\n<li>template/controller: <code>tag-aliases.html</code>, <code>tag-aliases.js</code></li>\n<li>state/render/modal/import/save modules: <code>analytics-app/app/frontend/js/tag-aliases-*</code></li>\n<li>HTTP boundary: <code>tag_write_api/aliases.py</code>, <code>tag_write_api/promotions.py</code></li>\n<li>planners: <code>tag_alias_mutations.py</code>, <code>tag_promotion_mutations.py</code></li>\n<li>canonical source: <code>analytics-app/data/canonical/tag-aliases.json</code></li>\n</ul>\n<h2>Weak Spots And Change Method</h2>\n<ul>\n<li>Promotion/demotion spans vocabulary, aliases, and assignments, making it the highest-risk tag mutation family.</li>\n<li>Patch fallback can express guidance but does not share server validation, activity, atomicity, or rollback.</li>\n<li>Alias history in assignments is deliberately secondary metadata and can be mistaken for current resolution authority.</li>\n</ul>\n<p>Start changes in the planner/model and its cross-file tests. Update route docs only for a durable workflow/ownership change, not for modal layout.</p>\n"
}
