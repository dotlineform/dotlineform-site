{
  "doc_id": "local-setup-recovery",
  "title": "Local Setup Recovery",
  "added_date": "2026-05-19",
  "last_updated": "2026-05-19",
  "source_path": "local-setup-recovery.md",
  "viewer_url": "/docs/?scope=studio&doc=local-setup-recovery",
  "parent_id": "local-setup",
  "content_html": "<h1>Local Setup Recovery</h1>\n<h2>Recovery after Xcode / Command Line Tools updates</h2>\n<p>Major macOS, Xcode, or Command Line Tools updates can disturb local shell startup behavior and tool resolution. The most common symptoms are:</p>\n<ul>\n<li><code>brew</code>, <code>conda</code>, <code>python3</code> suddenly not found</li>\n<li>the shell stops loading exports from <code>.bashrc</code>, <code>.bash_profile</code>, or <code>.zshrc</code></li>\n<li>previously working <code>ffmpeg</code> or <code>heif-convert</code> commands disappear from <code>PATH</code></li>\n</ul>\n<p>Recommended recovery checklist:</p>\n<ol>\n<li>Confirm the shell you are actually using:</li>\n</ol>\n<pre><code class=\"language-bash\">echo &quot;$SHELL&quot;\nps -p $$ -o command=\n</code></pre>\n<ol start=\"2\">\n<li>Re-open the terminal, then check whether the expected tools resolve:</li>\n</ol>\n<pre><code class=\"language-bash\">which brew\nwhich python3\nwhich conda\n</code></pre>\n<ol start=\"3\">\n<li>If Homebrew is missing from <code>PATH</code>, restore the standard shell init line and reload the shell:</li>\n</ol>\n<pre><code class=\"language-bash\">eval &quot;$(/opt/homebrew/bin/brew shellenv)&quot;\n</code></pre>\n<p>For persistence, that line should usually live in your shell startup file.</p>\n<ol start=\"4\">\n<li>If <code>conda</code> no longer initializes correctly, re-run:</li>\n</ol>\n<pre><code class=\"language-bash\">conda init zsh\n</code></pre>\n<p>Or for <code>bash</code>:</p>\n<pre><code class=\"language-bash\">conda init bash\n</code></pre>\n<p>Then open a new shell and reactivate the environment:</p>\n<pre><code class=\"language-bash\">conda activate dotlineform\n</code></pre>\n<ol start=\"7\">\n<li>If the update affected the Apple developer tools themselves, re-run:</li>\n</ol>\n<pre><code class=\"language-bash\">xcode-select --install\n</code></pre>\n<p>If the CLT path looks wrong, check:</p>\n<pre><code class=\"language-bash\">xcode-select -p\n</code></pre>\n<ol start=\"8\">\n<li>If <code>ffmpeg</code> or <code>heif-convert</code> disappeared after the update, verify the Homebrew installs:</li>\n</ol>\n<pre><code class=\"language-bash\">brew list --versions ffmpeg libheif\nffmpeg -version | head -n 1\nheif-convert --version\n</code></pre>\n<h3>Shell startup file note</h3>\n<p>On macOS, login-shell behavior often causes confusion:</p>\n<ul>\n<li><code>zsh</code> usually reads <code>~/.zshrc</code></li>\n<li><code>bash</code> often reads <code>~/.bash_profile</code> for login shells, not <code>~/.bashrc</code></li>\n</ul>\n<p>If you keep your exports in <code>~/.bashrc</code>, make sure <code>~/.bash_profile</code> sources it:</p>\n<pre><code class=\"language-bash\">if [ -f ~/.bashrc ]; then\n  . ~/.bashrc\nfi\n</code></pre>\n<p>That is one of the most likely fixes if a system update makes it look like your env vars or init hooks have vanished.</p>\n<h3>Fast post-update validation</h3>\n<p>After any Xcode/CLT update, this is a good minimal smoke test:</p>\n<pre><code class=\"language-bash\">which python3\npython3 -V\npython3 -c &quot;import openpyxl; print(openpyxl.__version__)&quot;\nffmpeg -version | head -n 1\nheif-convert --version\n$HOME/miniconda3/bin/python3 admin-app/checks/audit_site_consistency.py --strict\n</code></pre>\n"
}
