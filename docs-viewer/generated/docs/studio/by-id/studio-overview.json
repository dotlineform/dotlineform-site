{
  "doc_id": "studio-overview",
  "title": "Studio Overview",
  "added_date": "2026-07-15",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=studio-overview",
  "parent_id": "studio",
  "summary": "Local Studio capabilities, execution path, authority boundaries, extension method, and known weak spots.",
  "content_html": "<h1>Studio Overview</h1>\n<h2>What Studio Is</h2>\n<p>Studio is the local catalogue authoring and maintenance app. It provides a browser shell for catalogue workflows and a loopback Python service for validated reads, writes, builds, publication plans, media operations, and project reports.</p>\n<p>Studio is not part of the deployed public site. Analytics, Admin, Docs Viewer, and their write authority are sibling local apps rather than Studio route modules.</p>\n<h2>Capabilities</h2>\n<p>Studio currently supports:</p>\n<ul>\n<li>browsing catalogue works and drafts</li>\n<li>creating and editing works, work-detail sections, and series</li>\n<li>bulk work import through preview and confirmed apply</li>\n<li>reviewing the catalogue field registry</li>\n<li>generating and opening the Project State report</li>\n<li>previewing and applying catalogue deletes, builds, publication, and media publication</li>\n<li>linking to public catalogue results through the configured public-preview base</li>\n</ul>\n<p>The checked-in route registry is the exact authority for mounted browser pages. The catalogue API and service tests are the authority for available mutations.</p>\n<h2>Execution Path</h2>\n<pre><code class=\"language-text\">browser requests /studio/... route\n  -&gt; Studio app server serves studio-shell.html\n  -&gt; studio-app.js loads /studio/runtime-config.json\n  -&gt; app.routes resolves the route\n  -&gt; configured HTML template is mounted\n  -&gt; configured route script boots\n  -&gt; static fallback read or /studio/api/catalogue/... request\n  -&gt; catalogue API validates and delegates\n  -&gt; focused catalogue service reads or changes canonical data\n  -&gt; required generated catalogue, search, media, or public output follows\n</code></pre>\n<p>The browser may use checked-in or generated JSON for safe fallback reads. Writes never use those browser paths as authority; they go through named local endpoints and server-side validation.</p>\n<h2>Authority Boundaries</h2>\n<h3>Routes And Browser Boot</h3>\n<ul>\n<li><code>studio/app/frontend/config/studio-config.json</code> owns source route records and browser-safe data paths.</li>\n<li><code>studio/app/server/studio/studio_app_config.py</code> validates the route registry and injects runtime endpoints, site bases, media settings, pipeline values, and the resolved view list.</li>\n<li><code>studio/app/frontend/studio-shell.html</code> is the stable outer shell.</li>\n<li><code>studio-app.js</code> composes the shell; the configured route template owns stable route markup; the route script owns behavior and dynamic state.</li>\n</ul>\n<h3>Reads And Writes</h3>\n<ul>\n<li><code>studio-data.js</code> and route-specific clients own browser reads and response shaping.</li>\n<li><code>studio-transport.js</code> and focused editor service clients own browser transport.</li>\n<li><code>studio_catalogue_api.py</code> is the narrow HTTP adapter for catalogue reads and writes.</li>\n<li><code>studio/services/catalogue/</code> owns validation, plans, source mutation, build follow-through, publication, media, activity, and generated catalogue behavior.</li>\n</ul>\n<h3>Data</h3>\n<ul>\n<li><code>studio/data/config/</code> owns checked-in Studio domain config.</li>\n<li><code>studio/data/canonical/</code> owns canonical catalogue records.</li>\n<li><code>studio/data/generated/</code> owns rebuildable local lookup and activity projections.</li>\n<li><code>site/</code> owns tracked public output; it is not a Studio source tree.</li>\n<li>external project and media roots come from environment-backed workspace helpers, never browser config absolute paths.</li>\n</ul>\n<h2>Configuration And Extension</h2>\n<p>To add or change a Studio route:</p>\n<ol>\n<li>add or update its <code>app.routes</code> record</li>\n<li>provide the configured template and route script</li>\n<li>let server validation reject missing or unsupported fields</li>\n<li>add the focused browser/service behavior the route actually needs</li>\n<li>verify route boot and ready-state integration</li>\n</ol>\n<p>To add a catalogue operation:</p>\n<ol>\n<li>define the focused catalogue service or extend its current owner</li>\n<li>expose a narrow API adapter path only when browser access is needed</li>\n<li>project the endpoint through runtime config when the browser must discover it</li>\n<li>keep mutation validation and file authority on the server</li>\n<li>update the workflow’s durable owner and focused tests</li>\n</ol>\n<p>Do not use route config, UI visibility, or a browser data path as proof of write capability.</p>\n<h2>Sibling-App Boundaries</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=docs-viewer\">Docs Viewer</a> owns <code>/docs/</code>, documentation management, Docs Import, review, and scope lifecycle.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=analytics\">Analytics</a> owns analytics and Data Sharing browser routes and APIs.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=admin\">Admin</a> owns operational checks, audits, and admin reports.</li>\n<li>The public site owns deployed reading routes and tracked public assets.</li>\n</ul>\n<p>Studio may link to these surfaces. It does not inherit their route registries, frontend modules, service endpoints, or authority.</p>\n<h2>Known Weak Spots</h2>\n<ul>\n<li>Runtime config is assembled from checked-in Studio config, Python-owned endpoint/media constants, environment, and <code>_data/pipeline.json</code>; there is no single source file showing the final payload.</li>\n<li>The route registry is copied into <code>app.runtime.views</code> for current helpers and tests, so two runtime projections describe the same routes.</li>\n<li><code>studio_app_config.py</code> retains one route compatibility key for catalogue field registry lookup.</li>\n<li>Static fallback reads and server-backed reads coexist. A route change must confirm which path is authoritative for that workflow.</li>\n<li>The catalogue API adapter still imports a broad catalogue service surface; exact domain ownership lives below it rather than in the adapter.</li>\n<li>Some older workflow docs describe retired route modules or repeat module inventories and need consolidation against the current registry.</li>\n</ul>\n<h2>Where To Look First</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=studio-runtime\">Studio Runtime</a> — browser shell and route execution boundary.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=local-studio-app\">Local Studio App</a> — server and runtime-config boundary.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=local-studio-routes\">Local Studio Routes</a> — exact mounted route inventory.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=local-studio-apis\">Local Studio APIs</a> — exact endpoint groups and adapters.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=studio-config-and-save-flow\">Config And Save Flow</a> — current read/write and build workflow.</li>\n<li><code>studio/tests/python/</code> and <code>studio/tests/smoke/</code> — executable authority for service and route integration.</li>\n</ul>\n"
}
