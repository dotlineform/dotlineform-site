{
  "doc_id": "scripts-catalogue-write-server-build-lookup",
  "title": "Catalogue Build And Lookup Refresh",
  "added_date": "2026-05-19",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=scripts-catalogue-write-server-build-lookup",
  "parent_id": "scripts-catalogue-write-server",
  "content_html": "<h1>Catalogue Build And Lookup Refresh</h1>\n<h2>Two Different Derived Outputs</h2>\n<p>Catalogue source writes can affect two families of derived data:</p>\n<ul>\n<li><strong>Studio lookups</strong> make search, focused editors, and related-record summaries fast.</li>\n<li><strong>Public builds</strong> produce site JSON, pages, search entries, thumbnails, and staged primary media.</li>\n</ul>\n<p>They share canonical source but have different planners and failure boundaries. A successful source save can therefore report lookup refresh, public build, and media outcomes separately.</p>\n<h2>Studio Lookup Refresh</h2>\n<p>Generated lookup files live under <code>studio/data/generated/catalogue-lookup/</code>. Browser-safe paths are registered in <code>studio/app/frontend/config/studio-config.json</code>; focused Work and detail reads are service projections rather than generated per-record files.</p>\n<p>After a canonical write:</p>\n<ol>\n<li>the catalogue field registry determines whether changed fields affect <code>studio-lookup</code>;</li>\n<li>serializer dependencies in <code>studio/services/catalogue/catalogue_lookup.py</code> identify the affected payloads;</li>\n<li><code>catalogue_lookup_refresh.py</code> writes a single record, a targeted related set, or the full lookup family;</li>\n<li>the mutation response reports the chosen refresh mode.</li>\n</ol>\n<p>A Work change can affect Work search, its focused projection, Series member summaries, and detail context. A Series change can affect Series search, its focused projection, and related Work summaries. That cross-record embedding is why the planner cannot assume one source record means one generated file.</p>\n<p>Bulk, import, create, delete, and mixed dependency changes may use a full refresh when a narrower dependency set is not proven. Full refresh is a safe fallback, not the desired default for every mutation.</p>\n<h2>Scoped Public Build</h2>\n<p><code>POST /studio/api/catalogue/build-preview</code> plans without writing. <code>build-apply</code> executes the same validated scope.</p>\n<p>The planner can select:</p>\n<ul>\n<li>focused Work or Series public JSON and pages;</li>\n<li>affected aggregate indexes and catalogue search;</li>\n<li>local Work and detail media derivatives;</li>\n<li>media-only regeneration when no source or public metadata write is required.</li>\n</ul>\n<p><code>studio/services/catalogue/catalogue_build_scopes.py</code> owns record scope. <code>catalogue_build_field_plan.py</code> maps changed fields to artifact families. <code>catalogue_build_media.py</code> owns source-image resolution and local derivative work. <code>catalogue_build_commands.py</code> owns generator/search command shapes. <code>catalogue_json_build.py</code> is the CLI and orchestration boundary.</p>\n<p>Draft Work and Series source saves do not create public output. Publication requirements are checked before a public scope can run.</p>\n<h2>Media Boundary</h2>\n<p>Local build media is not remote publication:</p>\n<ul>\n<li>source images resolve below the configured projects root;</li>\n<li>primary and thumbnail derivatives are generated below <code>$DOTLINEFORM_PROJECTS_BASE_DIR/catalogue/media/</code>;</li>\n<li>public thumbnails are copied into repo-owned asset folders;</li>\n<li>primary variants remain staged for the separate R2 publishing workflow;</li>\n<li>scoped build does not advance a confirmed media version.</li>\n</ul>\n<p>The Work editor may compose source save, public build, and remote media publication into one user action, but each result remains independently visible and retryable.</p>\n<h2>Extension And Weak Spots</h2>\n<ul>\n<li>Add lookup dependency knowledge beside the serializer that consumes the field, not in another hand-maintained inventory.</li>\n<li>Add public artifact impact through the catalogue field registry and build planner.</li>\n<li>Keep targeted writes targeted; only global validation, import, or genuinely cross-record changes should normalize the whole corpus by default.</li>\n<li>The distinction between generated list files and service-only focused projections is easy to blur. <code>studio-config.json</code>, <code>studio_catalogue_api.py</code>, and <code>catalogue_lookup.py</code> together define the current read boundary.</li>\n<li>The wider catalogue CLI still supports Moment generation and thumbnail maintenance, but there is no active Moment editor route or Moment mutation API in Local Studio. Do not infer an app capability from a generator option.</li>\n</ul>\n<p>Tests for lookup planning, field-registry verification, build scopes, and media plans are the exact behavioral authority.</p>\n"
}
