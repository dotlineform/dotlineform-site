{
  "doc_id": "scripts-cloud-environments",
  "title": "Cloud Environments",
  "added_date": "2026-04-14",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=scripts-cloud-environments",
  "parent_id": "dev-home",
  "content_html": "<h1>Cloud Environments</h1>\n<h2>Authorities</h2>\n<ul>\n<li><code>.codex/setup.sh</code> — shared bootstrap entrypoint.</li>\n<li><code>.devcontainer/</code> — Codespaces base image, packages, editor settings, and environment.</li>\n<li><code>requirements.txt</code> — pinned Python packages.</li>\n<li>platform settings — agent network access, environment variables, and secrets.</li>\n</ul>\n<p>Platform behavior can change independently of this repo; verify it in the current environment rather than preserving service assumptions here.</p>\n<h2>Bootstrap Flow</h2>\n<pre><code class=\"language-text\">repo checkout\n  -&gt; optional apt/system packages\n  -&gt; create/reuse .venv\n  -&gt; install requirements.txt\n  -&gt; optionally persist venv PATH/SITE_PYTHON/PYTHON_BIN\n  -&gt; print environment diagnostics\n</code></pre>\n<p>Setup does not build docs/catalogue/search, validate the site, run profiles, or publish anything.</p>\n<pre><code class=\"language-bash\">bash .codex/setup.sh\n</code></pre>\n<h2>Setup Flags</h2>\n<ul>\n<li><code>SETUP_SKIP_APT=1</code> — skip system-package installation.</li>\n<li><code>FORCE_APT_PACKAGES=1</code> — install even when baseline commands appear present.</li>\n<li><code>SETUP_INSTALL_MEDIA_PACKAGES=1</code> — require/install <code>ffmpeg</code> and libheif tools.</li>\n<li><code>SETUP_PERSIST_AGENT_ENV=0</code> — do not append the venv environment block to <code>~/.bashrc</code>.</li>\n</ul>\n<p>The script is the exact behavior authority; keep this list only because these flags are its operator interface.</p>\n<h2>Codespaces</h2>\n<p>The devcontainer uses an Ubuntu base, installs Python/build/GitHub/media tooling, delegates post-create to <code>.codex/setup.sh</code>, and selects the repo <code>.venv</code> interpreter.</p>\n<p><code>devcontainer.json</code> owns current container environment values, including projects/media roots and srcset concurrency. They are development defaults, not portable production config.</p>\n<h2>Runtime Modes</h2>\n<h3>Normal Docs/Code Work</h3>\n<p>Use baseline setup, then <code>.venv/bin/python</code> for checks.</p>\n<h3>Media Work</h3>\n<p>Enable/install media packages and verify the required external source root exists. Dependencies cannot create private media.</p>\n<h3>Publish-Sensitive Work</h3>\n<p>Bootstrap first, then run explicit validation such as <code>bin/site-validate</code> and the focused check profile. Setup success is not deploy evidence.</p>\n<h2>Environment And Secrets</h2>\n<ul>\n<li><code>DOTLINEFORM_PROJECTS_BASE_DIR</code> is required for external Data Sharing/Docs scope/media workflows that use it.</li>\n<li>Other workspace/concurrency defaults are owned by current runner/container config.</li>\n<li>R2 credentials belong in the platform secret store and must never be committed or written into docs output/logs.</li>\n<li><code>.env.local</code> is local-machine configuration; cloud sessions use platform environment/secrets.</li>\n</ul>\n<p>Use the minimum network access required. Dependency installation and agent web access are separate platform concerns.</p>\n<h2>Verification</h2>\n<pre><code class=\"language-bash\">.venv/bin/python -m pip check\n.venv/bin/python admin-app/commands/run_checks.py --list\n.venv/bin/python admin-app/commands/run_checks.py --profile quick\nSITE_PYTHON=&quot;$PWD/.venv/bin/python&quot; bin/site-validate\n</code></pre>\n<p>Use dry-run generators by default unless the task explicitly owns writes.</p>\n<h2>Weak Spots</h2>\n<ul>\n<li>Cached environments can retain old packages/setup behavior; reset cache after material bootstrap changes.</li>\n<li>Cloud/local system libraries and Python patch versions can differ.</li>\n<li>Persisting an absolute repo venv path into <code>.bashrc</code> assumes the checkout path remains stable.</li>\n<li>Container environment defaults can look like application config even though they exist only for that workspace.</li>\n<li>Broad agent network access increases prompt-injection/exfiltration risk and should not be enabled by habit.</li>\n</ul>\n"
}
