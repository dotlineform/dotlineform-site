{
  "doc_id": "d-20260612-153500-7eadc8",
  "title": "GitHub Actions",
  "added_date": "2026-06-12 15:35:00",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260612-153500-7eadc8",
  "parent_id": "d-20260419-000000-d2e47b",
  "content_html": "<h1>GitHub Actions</h1>\n<h2>Current Workflow</h2>\n<p><code>.github/workflows/public-site.yml</code> is the checked public-site validation/deployment workflow. The YAML is the authority; GitHub repository variables, Pages settings, runs, artifacts, and live deployment state are external and must be inspected live when they matter.</p>\n<h2>Trigger And Jobs</h2>\n<p>Current triggers:</p>\n<ul>\n<li><code>pull_request</code> when the workflow, <code>bin/site-validate</code>, <code>site/**</code>, or <code>site-tools/**</code> changes;</li>\n<li>manual <code>workflow_dispatch</code>.</li>\n</ul>\n<p>Current flow:</p>\n<pre><code class=\"language-text\">checkout\n  -&gt; Python 3.12\n  -&gt; python site-tools/site_validate.py\n  -&gt; configure Pages\n  -&gt; upload tracked site/ as Pages artifact\n  -&gt; evaluate deploy gate\n  -&gt; optional deploy-pages job\n</code></pre>\n<p>The workflow uploads <code>site/</code> directly. There is no site build/copy step.</p>\n<h2>Manual Production Gate</h2>\n<p>Production deployment is intentionally manual. The normal release sequence is:</p>\n<pre><code class=\"language-text\">change checked locally\n  -&gt; preview site/ locally\n  -&gt; merge/update main\n  -&gt; manually dispatch Public site on main\n  -&gt; GitHub validates the exact tracked artifact\n  -&gt; gated Pages deployment\n</code></pre>\n<p>Typical CLI trigger:</p>\n<pre><code class=\"language-bash\">gh workflow run public-site.yml --ref main\n</code></pre>\n<p>Inspect the resulting run before treating the site as deployed.</p>\n<p>Deployment requires all of:</p>\n<ul>\n<li>ref is <code>refs/heads/main</code>;</li>\n<li>repository variable <code>PUBLIC_SITE_PAGES_DEPLOY_ENABLED</code> is exactly <code>true</code>;</li>\n<li>event is <code>push</code> or <code>workflow_dispatch</code>;</li>\n<li>validate job succeeded.</li>\n</ul>\n<p>Repository variables and Pages source/environment settings are not tracked files. Changing them or triggering a live deployment is an external state change requiring explicit user intent.</p>\n<p>The manual dispatch is the deliberate gate between local preview and the live <code>www</code> site. Do not add an automatic push trigger unless the release policy is intentionally changed.</p>\n<h2>Workflow Cleanup Note</h2>\n<p>The deploy-gate shell still accepts <code>push</code>, but the workflow deliberately has no <code>push</code> trigger. That branch is currently unreachable residue, not missing deployment capability.</p>\n<p>It can be removed in a focused workflow cleanup so the YAML expresses the manual policy directly. Until then, ordinary pushes do not start this workflow. Whether the live site was actually deployed by a manual run must be checked on GitHub, not inferred from this document.</p>\n<h2>Debugging</h2>\n<ol>\n<li>Inspect the current workflow run and failing step.</li>\n<li>Reproduce validation locally with:</li>\n</ol>\n<pre><code class=\"language-bash\">bin/site-validate\n</code></pre>\n<ol start=\"3\">\n<li>Separate repo validation failures from GitHub-only permissions, Pages configuration, environment protection, artifact, or deployment failures.</li>\n<li>Fix the owning source/workflow and rerun only with publishing authority.</li>\n</ol>\n<h2>Change Method</h2>\n<ul>\n<li>Keep pull-request triggers narrow to public artifact/validator ownership and production deployment manual.</li>\n<li>Pin/update actions deliberately and review their release/migration notes.</li>\n<li>Preserve least-privilege default permissions; grant Pages/id-token only to deploy.</li>\n<li>Keep concurrency/cancellation intentional.</li>\n<li>Test YAML/validation locally where possible, then inspect a real run.</li>\n<li>Update this page only when the workflow architecture, gate, or operator method changes—not for run IDs or current external status.</li>\n</ul>\n<h2>Weak Spots</h2>\n<ul>\n<li>Pull requests currently configure/upload a Pages artifact even though they cannot deploy; this may be more work/permission surface than validation needs.</li>\n<li>The unreachable <code>push</code> branch in the deploy-gate script obscures the deliberate manual-only policy.</li>\n<li>The deployment variable is one gate among external Pages/environment settings and does not prove readiness alone.</li>\n<li>Action versions and GitHub Pages requirements can change; the YAML must be periodically verified against current GitHub behavior.</li>\n</ul>\n"
}
