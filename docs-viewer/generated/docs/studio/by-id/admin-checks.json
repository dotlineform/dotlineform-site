{
  "doc_id": "admin-checks",
  "title": "Checks",
  "added_date": "2026-06-08",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=admin-checks",
  "parent_id": "admin",
  "content_html": "<h1>Admin Checks</h1>\n<h2>What Checks Does</h2>\n<p><code>/admin/checks/</code> runs allowlisted evidence reports against a configured slice of the repository and stores ignored local artifacts for review.</p>\n<p>Implemented reports:</p>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=admin-checks-report-files\">Files Report</a> — counts and sizes for selected source files.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=admin-checks-report-target-map\">Target Map Report</a> — ownership, shared-dependency, boundary, and config-drift evidence.</li>\n</ul>\n<p>Future reports remain roadmap deliveries until their producer, registry entry, artifact contract, UI support, and tests are complete.</p>\n<h2>Targeting Model</h2>\n<pre><code class=\"language-text\">required scope\n  -&gt; optional technical families\n  -&gt; optional workflow areas\n  -&gt; optional mapped routes\n  -&gt; explicit shared dependencies\n  -&gt; selected source files\n</code></pre>\n<p>Filters intersect. Scope is the safety boundary; the other layers describe different ways to narrow within it.</p>\n<ul>\n<li><strong>Family</strong>: technical layer such as runtime JavaScript, services, config, or tests.</li>\n<li><strong>Area</strong>: workflow concept such as search, management, import/export, or catalogue.</li>\n<li><strong>Route</strong>: reviewed user-facing surface and its owned implementation files.</li>\n</ul>\n<p>Markdown source is excluded from report input. Generated payloads, canonical data, dependencies, caches, and local run output are excluded according to scope policy.</p>\n<h2>Run Workflow</h2>\n<ol>\n<li>The browser loads safe projected targets and report metadata.</li>\n<li>The user selects one report, scope, and optional filters.</li>\n<li>The API turns that selection into a validated run request using report defaults.</li>\n<li><code>run_reports.py</code> resolves the target set and writes a manifest/plan.</li>\n<li>It invokes each registered report by argv list without a shell.</li>\n<li>Reports write JSON plus focused Markdown; tabular reports may also write CSV.</li>\n<li>The route displays summaries/reports and can delete one confined run snapshot.</li>\n</ol>\n<p>Dry-run planning does not create artifacts. Write runs live below <code>var/admin/checks/&lt;run-id&gt;/</code>.</p>\n<h2>Configuration And Authority</h2>\n<ul>\n<li><code>admin-checks.json</code> — scopes, families, areas, routes, exclusions, and shared dependencies.</li>\n<li><code>admin-checks-reports.json</code> — report IDs, producer scripts, defaults, allowed options, and artifact types.</li>\n<li><code>admin_checks_config.py</code> — schema and request validation.</li>\n<li><code>target_map_resolver.py</code> — the only target matching/resolution implementation.</li>\n<li><code>run_reports.py</code> — planning, subprocess execution, run artifacts, and summaries.</li>\n<li><code>reports/</code> — focused evidence producers.</li>\n<li><code>admin_checks_api.py</code> — browser-safe projection and confined run reads/deletes.</li>\n</ul>\n<p>See <a href=\"/docs/?scope=studio&amp;doc=admin-checks-config\">Checks Config And Targeting</a> for extension rules and <a href=\"/docs/?scope=studio&amp;doc=admin-checks-target-map-architecture\">Target Map Architecture</a> for audit-versus-report ownership.</p>\n<h2>API Boundary</h2>\n<p>The <code>/admin/api/checks/</code> group exposes health, safe report/target metadata, recent runs, run creation, focused summary/report reads, and deletion of one validated run ID.</p>\n<p>The browser cannot provide command strings, script paths, environment, working directories, output roots, arbitrary filesystem paths, or unconstrained report options. IDs and resolved paths are validated before read/delete.</p>\n<h2>Artifact Boundary</h2>\n<p>A write run contains:</p>\n<ul>\n<li>manifest and command records explaining the plan and execution;</li>\n<li>machine-readable run summary;</li>\n<li>compact human-readable run summary;</li>\n<li>one directory per report with JSON, Markdown, and optional CSV.</li>\n</ul>\n<p>JSON is the exhaustive evidence. Markdown answers immediate review questions. CSV is for complete tabular inspection. Do not force all evidence into a wide Markdown document.</p>\n<h2>Extension And Weak Spots</h2>\n<p>Add a report as one finishable delivery: producer, registry, validated options, artifacts, API/UI visibility, tests, and durable report summary.</p>\n<p>Known pressure points:</p>\n<ul>\n<li>the target map mixes deterministic discovery with judgment-heavy route/area ownership;</li>\n<li>inventory-only routes can go stale and are not selectable evidence targets;</li>\n<li>current scope/family mappings include historical paths, so the maintenance audit must be treated as normal upkeep;</li>\n<li>one broad config file maps the whole repository and can become an attractive but misleading second architecture inventory;</li>\n<li>reports should produce decisions, not accumulate metrics because they are easy to count.</li>\n</ul>\n<p>Focused verification is the <code>admin-checks</code> profile plus report/config/API tests under <code>admin-app/tests/python/</code>.</p>\n"
}
