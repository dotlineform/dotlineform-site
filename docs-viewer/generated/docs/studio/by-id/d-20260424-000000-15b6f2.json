{
  "doc_id": "d-20260424-000000-15b6f2",
  "title": "Studio Runtime",
  "added_date": "2026-04-24 00:00:00",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260424-000000-15b6f2",
  "parent_id": "d-20260423-000000-d015e6",
  "summary": "Browser shell, route registry, template, route-script, shared-helper, and sibling-app runtime boundaries for Local Studio.",
  "content_html": "<h1>Studio Runtime</h1>\n<h2>Purpose</h2>\n<p>This document owns the Local Studio browser execution model. The <a href=\"/docs/?scope=studio&amp;doc=d-20260602-111803-6d22ef\">route inventory</a> owns exact mounted pages; <a href=\"/docs/?scope=studio&amp;doc=d-20260522-080600-a2b102\">Local Studio App</a> owns the Python server; <a href=\"/docs/?scope=studio&amp;doc=d-20260602-111803-019594\">Local Studio APIs</a> owns endpoint inventory.</p>\n<h2>Browser Boot</h2>\n<pre><code class=\"language-text\">studio-shell.html\n  -&gt; studio-app.js loads runtime config\n  -&gt; studio-route-registry.js resolves window.location\n  -&gt; studio-route-templates.js fetches the configured HTML template\n  -&gt; Studio shell mounts shared chrome and route markup\n  -&gt; configured route script imports and boots\n  -&gt; route script owns ready/busy state, reads, writes, and dynamic UI\n</code></pre>\n<p>The Python server serves the same outer shell for every registered Studio page. It does not render route body markup or choose route behavior.</p>\n<h2>Ownership Layers</h2>\n<table>\n<thead>\n<tr>\n<th>layer</th>\n<th>owner</th>\n<th>responsibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>source registry</td>\n<td><code>studio-config.json</code> <code>app.routes</code></td>\n<td>route identity, path, template, script, shell type, nav flag, ready-state id</td>\n</tr>\n<tr>\n<td>runtime validation</td>\n<td><code>studio_app_config.py</code></td>\n<td>reject invalid routes and add local runtime values</td>\n</tr>\n<tr>\n<td>outer document</td>\n<td><code>studio-shell.html</code></td>\n<td>config and asset-version metadata plus <code>#studioApp</code> mount</td>\n</tr>\n<tr>\n<td>app composition</td>\n<td><code>studio-app.js</code></td>\n<td>config load, route resolution, shared header/title, template mount, route import</td>\n</tr>\n<tr>\n<td>stable route DOM</td>\n<td><code>studio/app/frontend/routes/*.html</code></td>\n<td>page roots, panels, forms, static controls, and dynamic mounts</td>\n</tr>\n<tr>\n<td>route behavior</td>\n<td>configured route script</td>\n<td>data loading, events, dynamic rendering, modals, service calls, ready/busy projection</td>\n</tr>\n<tr>\n<td>shared browser behavior</td>\n<td>focused <code>studio-*</code> and catalogue helpers</td>\n<td>config, routing, templates, theme, navigation, transport, data, editor primitives</td>\n</tr>\n</tbody>\n</table>\n<p>Stable markup belongs in route templates. Repeated records, API results, previews, status text, and other data-dependent UI belong in JavaScript.</p>\n<h2>Route Registry Contract</h2>\n<p>Every active Studio route uses <code>shell_type: html-template</code> and declares:</p>\n<ul>\n<li><code>label</code></li>\n<li><code>title</code></li>\n<li><code>path</code></li>\n<li><code>template</code></li>\n<li><code>script</code></li>\n<li><code>nav</code></li>\n<li><code>ready_state_route_id</code></li>\n</ul>\n<p>Server validation checks required fields, duplicate paths, supported shell type, <code>/studio/</code> containment, and template/script existence. The browser normalizes the validated record but does not widen it.</p>\n<p>The server also copies route records into <code>app.runtime.views</code>. Current navigation helpers and smoke tests still consume that projection, but <code>app.routes</code> remains the source registry.</p>\n<h2>Shared Runtime Boundaries</h2>\n<ul>\n<li><code>studio-config.js</code> loads the runtime payload and exposes route, data-path, and code-owned text accessors.</li>\n<li><code>studio-route-state.js</code> owns <code>data-studio-ready</code> and <code>data-studio-busy</code> projection.</li>\n<li><code>studio-data.js</code> owns shared JSON loading and selected static/server read routing.</li>\n<li><code>studio-transport.js</code> owns the current catalogue endpoint constants, health probes, and JSON transport.</li>\n<li><code>studio-navigation.js</code> owns Studio route URLs, public-preview URLs, and modal event dispatch.</li>\n<li>focused catalogue editor modules own editor state, selection, fields, actions, modals, media, and services.</li>\n</ul>\n<p>The route shell remains orchestration. Do not add domain workflows to <code>studio-app.js</code> or Python-rendered HTML.</p>\n<h2>Sibling Apps</h2>\n<p>Studio does not host Docs Viewer, Analytics, Data Sharing, or Admin routes. It may build links to those apps or the public site, but it does not receive their config registries, runtime modules, APIs, or write capability.</p>\n<p>The public site likewise does not load the Studio runtime. Public catalogue links use the runtime-configured public-preview base and focused URL helpers.</p>\n<h2>Extension Method</h2>\n<p>For a new route:</p>\n<ol>\n<li>create the stable HTML template and focused route script</li>\n<li>add one validated <code>app.routes</code> record</li>\n<li>keep domain reads/writes behind existing focused browser and service owners</li>\n<li>project ready/busy state through the shared route-state helper</li>\n<li>add a route-boot smoke only for the durable shell/module/service boundary</li>\n</ol>\n<p>For shared browser behavior, add or extend the smallest owner. Do not turn the route registry into executable policy or add another shell type without a real rendering model.</p>\n<h2>Weak Spots</h2>\n<ul>\n<li><code>app.routes</code> and <code>app.runtime.views</code> duplicate the same route records at runtime.</li>\n<li><code>studio-transport.js</code> hardcodes catalogue endpoints even though runtime config also projects a partial catalogue service map.</li>\n<li>One catalogue field-registry compatibility key remains in server route validation.</li>\n<li>Static and server-backed read helpers coexist; each workflow must state which is authoritative and what happens when the service is unavailable.</li>\n</ul>\n<p>Exact code ownership for catalogue routes belongs in their workflow documents and modules, not another runtime module inventory here.</p>\n"
}
