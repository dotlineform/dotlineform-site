{
  "doc_id": "d-20260607-222033-ce5515",
  "title": "Source Mutation Endpoints",
  "added_date": "2026-06-07 22:20:33",
  "last_updated": "2026-07-27 20:43:41",
  "viewer_url": "/docs/?scope=studio&doc=d-20260607-222033-ce5515",
  "parent_id": "d-20260607-222033-647b52",
  "content_html": "<h1>Docs Viewer Source Mutation Endpoints</h1>\n<p>Source mutation endpoints rewrite source Markdown front matter or delete source Markdown files. They do not move files on disk except when deleting a confirmed source doc.</p>\n<p>Returned source paths are repo-relative for repo-backed scopes and relative to the fixed external Docs Viewer root for external-local scopes. User-specific absolute workspace roots are not exposed.</p>\n<p>Metadata editing uses one explicit managed target:</p>\n<ul>\n<li>parent document: <code>{ scope, doc_id }</code></li>\n<li>configured child document: <code>{ scope, sub_scope, doc_id }</code></li>\n</ul>\n<p>The local <code>GET /docs/metadata</code> read and <code>POST /docs/update-metadata</code> write use\nthe same target. They resolve only a configured parent or one-level child root,\nverify source front-matter identity, and reject missing, unlisted, mismatched,\nor path-escaping targets. Full metadata hydration stays local and does not\nexpand public manifests or compact public by-ID payloads.</p>\n<h2><code>POST /docs/update-metadata</code></h2>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;doc_id&quot;: &quot;docs-viewer&quot;,\n  &quot;title&quot;: &quot;Docs Viewer&quot;,\n  &quot;summary&quot;: &quot;Runtime and management docs.&quot;,\n  &quot;date&quot;: &quot;2026-07-27&quot;,\n  &quot;date_display&quot;: &quot;27 July 2026&quot;,\n  &quot;ui_status&quot;: &quot;done&quot;,\n  &quot;viewable&quot;: true,\n  &quot;parent_id&quot;: &quot;docs-viewer-overview&quot;\n}\n</code></pre>\n<p>For a configured sub-scope document, the request adds <code>sub_scope</code> and omits\n<code>parent_id</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;analysis&quot;,\n  &quot;sub_scope&quot;: &quot;tags&quot;,\n  &quot;doc_id&quot;: &quot;d-20260624-204534-e3de0b&quot;,\n  &quot;title&quot;: &quot;ordered&quot;,\n  &quot;summary&quot;: &quot;A compositional form tag.&quot;,\n  &quot;date&quot;: &quot;&quot;,\n  &quot;date_display&quot;: &quot;&quot;,\n  &quot;ui_status&quot;: &quot;done&quot;,\n  &quot;viewable&quot;: true\n}\n</code></pre>\n<p>Actions:</p>\n<ul>\n<li>updates supported front matter fields only</li>\n<li>preserves body content, filename, <code>doc_id</code>, and <code>added_date</code></li>\n<li>refreshes <code>last_updated</code> when a real change is written</li>\n<li>removes blank <code>summary</code> and <code>ui_status</code> fields</li>\n<li>validates non-blank <code>parent_id</code> in the same scope</li>\n<li>rejects parent cycles and descendant-parent moves</li>\n<li>rejects <code>parent_id</code> for a sub-scope target, without writing</li>\n<li>rebuilds targeted docs payloads for affected docs</li>\n<li>runs targeted docs-search updates when search-affecting fields change</li>\n<li>owns the manage UI's one-document <code>viewable</code> mutation through the <strong>non-viewable</strong> checkbox</li>\n<li>does not rewrite ancestors or descendants; a non-viewable ancestor still prevents public discovery</li>\n</ul>\n<p>For a sub-scope target, the rebuild refreshes its by-ID payload, local\nmanagement inventory, public-eligibility manifest, and configured child search\nprojection. It does not rebuild or insert the document into the parent index,\nRecent, or global search. The browser remounts the selected parent report and\nretains the existing <code>subdoc</code> route.</p>\n<p>Returned data includes the normalized target, change flags, source path,\nrebuild diagnostics, summary text, and <code>dry_run</code>.</p>\n<p>There is no separate single or bulk viewability endpoint. Manage UI viewability changes use this one-document metadata mutation without automatic ancestor or descendant writes.</p>\n<h2><code>POST /docs/move</code></h2>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;doc_id&quot;: &quot;docs-viewer&quot;,\n  &quot;parent_id&quot;: &quot;scripts&quot;\n}\n</code></pre>\n<p>Actions:</p>\n<ul>\n<li>writes only the moved doc's <code>parent_id</code> and <code>last_updated</code></li>\n<li>preserves source filename, body, title, <code>doc_id</code>, and <code>added_date</code></li>\n<li>accepts blank <code>parent_id</code> for root-level placement</li>\n<li>validates non-blank parent ids inside the same scope</li>\n<li>rejects moving a doc under itself or a descendant</li>\n<li>skips writes and rebuilds for no-op moves</li>\n<li>rebuilds the moved doc payload and updates search for the moved doc plus descendants</li>\n</ul>\n<p>Returned data includes previous and next parent ids, affected search ids, rebuild diagnostics, summary text, and <code>dry_run</code>.</p>\n<h2><code>POST /docs/delete-preview</code></h2>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;doc_ids&quot;: [&quot;research-note-a&quot;, &quot;research-note-b&quot;]\n}\n</code></pre>\n<p>Actions:</p>\n<ul>\n<li>requires one or more explicit checked document ids; the displayed, highlighted, or context-clicked document is never a fallback</li>\n<li>reduces checked descendants beneath checked ancestors to effective roots</li>\n<li>resolves the union of those roots and all descendants once, in deterministic tree order</li>\n<li>returns a permanent-deletion warning, total document count, and the exact document records and source paths that would be removed</li>\n<li>distinguishes checked documents from additional unselected descendants in the warning</li>\n<li>reports whether the delete set contains the optional configured default</li>\n<li>does not delete files</li>\n</ul>\n<p>Returned data includes <code>allowed</code>, <code>blockers</code>, <code>warnings</code>, requested and effective-root ids and counts, <code>delete_count</code>, <code>additional_descendant_count</code>, <code>delete_doc_ids</code>, and <code>delete_documents</code>.</p>\n<h2><code>POST /docs/delete-apply</code></h2>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;doc_ids&quot;: [&quot;research-note-a&quot;, &quot;research-note-b&quot;],\n  &quot;confirm&quot;: true\n}\n</code></pre>\n<p>Actions:</p>\n<ul>\n<li>requires <code>confirm: true</code></li>\n<li>re-runs delete preview validation</li>\n<li>deletes the checked Markdown source files and all additional descendant source files in one unioned mutation</li>\n<li>clears <code>default_doc_id</code> when any deleted doc was the optional configured default</li>\n<li>opens the first remaining loadable root in the browser without making it the new default</li>\n<li>rebuilds targeted generated docs payloads and docs search for every deleted id</li>\n<li>writes watcher-suppression markers for every deleted source path</li>\n</ul>\n<p>Returned data includes requested and effective-root ids and counts, <code>deleted_doc_ids</code>, deletion counts and paths, rebuild diagnostics, summary text, and <code>dry_run</code>.</p>\n"
}
