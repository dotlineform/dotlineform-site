{
  "doc_id": "d-20260812-153836-02802c",
  "title": "External Media Workspace Concept",
  "added_date": "2026-08-12 15:38:36",
  "last_updated": "2026-08-12 16:05:00",
  "viewer_url": "/docs/?scope=studio&doc=d-20260812-153836-02802c",
  "parent_id": "d-20260812-153836-b38d98",
  "summary": "separate retained originals, private managed media, and public media projections through one external scope-namespaced workspace",
  "ui_status": "proposed",
  "content_html": "<h1>External Media Workspace Concept</h1>\n<h2>Problem</h2>\n<p>Analysis currently writes repository-backed media directly into the tracked public <code>site/</code> tree, while other scopes may keep ready media inside their Docs Viewer scope container or in R2. That collapses three different roles: a retained authorial original, a private managed artifact used by Docs Viewer, and a public projection.</p>\n<p>Moving repository-backed media into <code>docs-viewer/scopes/&lt;scope&gt;/published/media/</code> would separate private from public behavior, but a public scope would then keep two copies of the same ready bytes in Git: one private and one under <code>site/</code>. The simpler model keeps all managed media outside the repository and reserves repository media bytes for the public projection only.</p>\n<h2>Three Media Roles</h2>\n<table>\n<thead>\n<tr>\n<th>role</th>\n<th>owner and lifetime</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Retained original</td>\n<td>Author-owned file beneath a configured Projects-relative source root such as <code>analysis/</code> or <code>processing/</code>. Add never moves, rewrites, or deletes it.</td>\n</tr>\n<tr>\n<td>Managed media</td>\n<td>Scope-owned consumer-ready artifact in the external Docs Viewer media workspace. Manage, inventory, build relationships, Copy, and future media management use it. It survives unpublish and document deletion.</td>\n</tr>\n<tr>\n<td>Public projection</td>\n<td>Replaceable byte-for-byte or derived copy under tracked <code>site/</code> or R2. It exists only through explicit Publish for a public scope.</td>\n</tr>\n</tbody>\n</table>\n<p>The managed artifact is deliberately distinct from the retained original. A raster image or file may be byte-identical, while sanitized SVG or rendered output may differ. The separate managed copy gives Docs Viewer a stable scope-owned identity even if the author later reorganises the original source tree.</p>\n<h2>Folder Model</h2>\n<p>The external workspace is rooted beneath the configured Projects base. The exact path is frozen at EMW-1.0, with this as the proposed shape:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/\n  docs-viewer/\n    media/\n      &lt;scope&gt;/\n        img/\n        svg/\n        files/\n        html/\n        build-source/\n          mermaid/\n        media-source-evidence.json\n</code></pre>\n<p>Each parent scope has one exact namespace. Media types remain separate logical subdirectories and identities remain flat within each type unless another feature deliberately changes that contract. Sub-scopes use the parent scope namespace; there is no inferred sub-scope media ownership.</p>\n<p>A repository-backed scope container becomes document-centred:</p>\n<pre><code class=\"language-text\">docs-viewer/scopes/&lt;scope&gt;/\n  source/\n    documents/\n    sub-scopes/&lt;sub-scope&gt;/documents/\n  published/\n    documents/\n    search/\n</code></pre>\n<p>An external-local document scope keeps the same document/search roles beneath its external scope root, but its media uses the shared external media workspace rather than being nested inside that document container. Repository and external-local scope containers therefore carry documents and their document/search projections, not ordinary media bytes or media build sources.</p>\n<h2>Stable Identity And URLs</h2>\n<p>Canonical Markdown continues to store one provider-neutral reference:</p>\n<pre><code>&#91;&#91;media:docs/analysis/img/algorithm.jpeg&#93;&#93;</code></pre>\n<p>The logical identity is:</p>\n<pre><code class=\"language-text\">scope + media type + relative identity\n</code></pre>\n<p>Physical roots and served URLs are projections of that identity:</p>\n<table>\n<thead>\n<tr>\n<th>surface</th>\n<th>example</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Managed file</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/analysis/img/algorithm.jpeg</code></td>\n</tr>\n<tr>\n<td>Manage URL</td>\n<td><code>/docs/media/analysis/img/algorithm.jpeg</code></td>\n</tr>\n<tr>\n<td>Public repository file</td>\n<td><code>site/assets/data/docs/scopes/analysis/media/img/algorithm.jpeg</code></td>\n</tr>\n<tr>\n<td>Public URL</td>\n<td><code>/assets/data/docs/scopes/analysis/media/img/algorithm.jpeg</code></td>\n</tr>\n<tr>\n<td>Public R2 key</td>\n<td>configured from the same <code>analysis + img + algorithm.jpeg</code> identity</td>\n</tr>\n</tbody>\n</table>\n<p>Manage and public payloads may therefore contain different rendered URLs without changing canonical Markdown. A raw physical path or public URL is never the authored ownership identity.</p>\n<h2>Add And Manage Workflow</h2>\n<pre><code class=\"language-text\">analysis/algorithm.jpeg\n        ↓ Add image\nexternal Docs Viewer media/analysis/img/algorithm.jpeg\n        ↓ local Manage serving\n/docs/media/analysis/img/algorithm.jpeg\n</code></pre>\n<ol>\n<li>Add image or Add file selects an eligible original beneath the scope's configured source root.</li>\n<li>Preview computes the stable logical identity and any existing collision without changing public storage.</li>\n<li>Apply copies or derives the managed artifact in the external scope namespace and verifies the result.</li>\n<li>Successful apply records the exact source association beside that namespace and inserts the logical token into canonical Markdown.</li>\n<li>Manage resolves the token through the local media route backed by the external workspace.</li>\n</ol>\n<p>Scopes without a configured retained-original root may continue to select from import staging, but their successful Add result still enters the same external managed workspace. Document and package Import follow the same destination rule for materialised media.</p>\n<p>A document-only build does not need to copy or publish media. When the external workspace is unavailable, media Add, inventory, serving, Copy, build-source work, and public Publish fail or report unavailable at their own boundary rather than silently falling back to repository or public bytes.</p>\n<h2>Build Sources And Editorial Copy</h2>\n<p>Standalone Mermaid <code>.mmd</code> is a registered media build source, not a document. It moves from <code>&lt;scope source&gt;/media/mermaid/</code> into the external namespace's <code>build-source/mermaid/</code>; its consumer-ready SVG lives in <code>svg/</code>. Inline Mermaid fenced inside Markdown remains document source and keeps its separate public projection contract.</p>\n<p>Editorial Copy transfers exact managed media identities between the source and target scope namespaces through the existing provider-neutral media plan. It carries recorded source evidence when present, preserves an existing target evidence row, performs no filename inference, and does not publish the target scope publicly.</p>\n<h2>Explicit Public Publish</h2>\n<p>For a public scope, Publish owns documents and media as one reconciled projection:</p>\n<pre><code class=\"language-text\">external Docs Viewer media/analysis/img/algorithm.jpeg\n        ↓ explicit Publish\nsite/assets/data/docs/scopes/analysis/media/img/algorithm.jpeg\n        or configured R2 projection\n</code></pre>\n<p>Publish derives the exact required media set from logical references across the complete publishable parent and sub-scope document set. A media identity referenced by several public documents is projected once and remains public until no included document references it.</p>\n<p>Preview reports missing managed media, changed public bytes, new public bytes, and public objects that would become unreferenced. Missing required managed media blocks confirmation rather than publishing a known broken document set.</p>\n<p>Apply writes and verifies required public media before exposing documents that reference it, then projects documents/search and removes exact stale public document/media projections last. A failure never removes or rewrites the external managed media. Repository and R2 mechanics stay behind their existing location adapters; ordinary Publish orchestration does not branch on provider.</p>\n<p>The public target policy remains configuration-owned. This concept does not decide whether every media type uses one provider or whether a current type-level split remains temporarily necessary; it requires only that the target is a public projection rather than the private managed authority.</p>\n<h2>Unpublish, Delete, And Orphans</h2>\n<p>Setting <code>publishable: false</code> changes eligibility for the next scope-wide Publish. That Publish removes the document projection and removes a public media projection only when the identity is absent from every remaining publishable document. The external managed artifact, retained original, and source-evidence row remain.</p>\n<p>Ordinary document Delete likewise has no authority to delete managed media. Immediate document-publication cleanup must not infer exclusive media ownership or delete shared media. The next exact scope-wide Publish may reconcile now-unreferenced public projections; future Scope Media Management may expose the managed artifact as unreferenced, but deletion remains a separate explicit action.</p>\n<h2>Migration Principles</h2>\n<ul>\n<li>Inventory every configured scope, media type, logical identity, build source, evidence row, current provider, and public reference before changing configuration.</li>\n<li>Copy current managed bytes from repository, external-local, <code>site/</code>, or R2 into the new external namespace and byte-verify them before cutover.</li>\n<li>Preserve currently valid <code>site/</code> and R2 public projections until the first media-aware Publish reconciles them.</li>\n<li>Cut over each exact configuration once; do not add path aliases, fallback reads, dual writes, or provider searches.</li>\n<li>Remove obsolete repository private-media directories and tracked markers only after their external targets are verified and all active owners use the new location.</li>\n<li>Preserve logical tokens and document source unchanged.</li>\n</ul>\n<h2>Boundaries</h2>\n<ul>\n<li>The external media workspace is not itself a backup promise; backup and synchronization remain user-owned policies.</li>\n<li>No shared physical object or deduplication across scope identities.</li>\n<li>No automatic import of arbitrary files from retained-original trees.</li>\n<li>No automatic managed-media deletion, orphan cleanup, source repair, or public publication during Add or Copy.</li>\n<li>No provider selector in ordinary authoring UI.</li>\n<li>No nested identity migration, asset registry, checksum catalogue, content-addressing, CDN cache invalidation, or background publication worker.</li>\n<li>No change to public document eligibility, lineage, or the meaning of <code>publishable</code>.</li>\n</ul>\n<p>The <a href=\"/docs/?scope=studio&amp;doc=d-20260812-153836-b38d98\">feature parent</a> owns delivery navigation. <a href=\"/docs/?scope=studio&amp;doc=d-20260514-184303-7914e2\">Media And Asset Handling</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260423-000000-8fd731\">Source Organisation</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260331-000000-f992b9\">Public Scopes</a>, and <a href=\"/docs/?scope=studio&amp;doc=d-20260719-123719-fb7565\">Document Diagrams</a> remain the shipped authorities until EMW-1 completes and transfers the new contract.</p>\n"
}
