{
  "doc_id": "d-20260719-123719-fb7565",
  "title": "Document Diagrams",
  "added_date": "2026-07-19 12:37:19",
  "last_updated": "2026-08-13 00:00:00",
  "viewer_url": "/docs/?scope=studio&doc=d-20260719-123719-fb7565",
  "parent_id": "d-20260424-000000-50b63f",
  "summary": "Author, build, publish, render, inspect, and extend scope-owned Mermaid diagrams in Docs Viewer.",
  "content_html": "<h1>Document Diagrams</h1>\n<h2>Purpose</h2>\n<p>Docs Viewer uses Mermaid for authored architecture and workflow diagrams that benefit from durable, reviewable source. It supports two source contracts:</p>\n<ul>\n<li>canonical <code>.mmd</code> plus a same-basename sanitized SVG for reusable or independently managed diagram media;</li>\n<li>one accessible fenced <code>mermaid</code> block for a diagram that belongs only to its document.</li>\n</ul>\n<p>Studio uses the accessible fenced block as its working default for Mermaid-authored, document-owned diagrams. Choose canonical <code>.mmd</code> plus persistent SVG when the diagram needs an independently managed media identity or stable served URL. Finished SVG remains the direct-media option for authored or imported vectors, and imported local documents retain that media contract until an explicitly selected migration chooses a different canonical source.</p>\n<p>The fenced block remains canonical Markdown. Managed local readers render it in memory. Configured public preparation derives a validated light and dark SVG pair, replaces the fence only in the public payload copy, and ships neither source nor Mermaid runtime to public browsers.</p>\n<p>Finished SVG and Mermaid source share the source editor's <strong>Add image…</strong> action but keep different contracts. A staged <code>.svg</code> is already-finished vector media; a staged <code>.mmd</code> is retained as canonical source and rendered before its SVG token is returned.</p>\n<p>The static-media workflow is shipped for every scope. Fenced Mermaid rendering is shipped for both managed-local scope types, and fenced Mermaid public projection is shipped for configured public scopes.</p>\n<h2>Dependency And Registration</h2>\n<p>The feature-local Node package root is <code>docs-viewer/build/mermaid/</code>:</p>\n<ul>\n<li><code>package.json</code> pins the direct Mermaid CLI dependency;</li>\n<li><code>package-lock.json</code> is the resolved dependency authority;</li>\n<li><code>mermaid-config.json</code> owns shared renderer settings;</li>\n<li><code>node_modules/</code> is local, ignored installation output.</li>\n</ul>\n<p>Install the locked dependency tree from the repository root:</p>\n<pre><code class=\"language-bash\">npm ci --prefix docs-viewer/build/mermaid\n</code></pre>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260423-204839-111501\">Runtime Dependencies</a> owns Node package-root policy. <a href=\"/docs/?scope=studio&amp;doc=d-20260519-210908-2f8f00\">Local Setup Toolchain</a> owns the supported Node/npm baseline and environment checks.</p>\n<p>Registration has two layers:</p>\n<ol>\n<li><code>docs-viewer/build/docs_builder/media_builds.py</code> allowlists the code-owned <code>mermaid</code> producer implemented by <code>docs-viewer/services/docs_mermaid_media.py</code>.</li>\n<li>Each participating scope opts in through reciprocal external build-source and managed-media records in <code>docs-viewer/config/scopes/docs_scopes.json</code>.</li>\n</ol>\n<pre><code class=\"language-json\">&quot;media&quot;: {\n  &quot;build_sources&quot;: {\n    &quot;mermaid&quot;: {\n      &quot;producer&quot;: &quot;mermaid&quot;,\n      &quot;publishes_to&quot;: &quot;svg&quot;\n    }\n  }\n}\n</code></pre>\n<p>The same scope's <code>media.types.svg</code> record must declare:</p>\n<pre><code class=\"language-json\">&quot;build_inputs&quot;: [&quot;mermaid&quot;]\n</code></pre>\n<p>The build source derives to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/build-source/mermaid/</code>; the SVG derives to the same scope's managed <code>svg</code> directory. Sub-scopes share their parent scope's media namespace.</p>\n<h2>Author Workflow</h2>\n<p>For initial intake through Docs Viewer:</p>\n<ol>\n<li>save an accessible <code>.mmd</code> as a direct child of shared import staging;</li>\n<li>open the target document's Markdown source editor and choose <strong>Add image…</strong>;</li>\n<li>select the Mermaid source and provide useful Markdown alt text;</li>\n<li>allow the service to render in temporary storage, publish and verify canonical source plus SVG, and insert the SVG token;</li>\n<li>review the dirty Markdown buffer and choose <strong>Rebuild doc</strong>.</li>\n</ol>\n<p>A render or publication failure inserts no token and writes no document source. Staged input remains in place after success.</p>\n<p>For later edits, view the rendered document and open Document Info. Its manage-only <strong>Diagrams</strong> section lists verified diagram-title links. Choosing one delegates the logical source identity to VS Code without exposing a physical path. Save the canonical <code>.mmd</code>; Docs Watcher renders only that changed identity. The Markdown token and search index do not change.</p>\n<p>Public routes never show these source links. Markdown source mode retains its semantic-token interface rather than becoming a Mermaid editor.</p>\n<h2>Repository Workflow</h2>\n<p>Repository authors and Codex can work directly:</p>\n<ol>\n<li>write canonical source to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/build-source/mermaid/&lt;diagram-name&gt;.mmd</code>;</li>\n<li>add the same-basename SVG token to the owning Markdown document;</li>\n<li>run the targeted document build:</li>\n</ol>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 docs-viewer/build/build_docs.py --scope &lt;scope&gt; --only-doc-ids &lt;doc-id&gt; --write\n</code></pre>\n<p>The producer runs before the selected document payload is rendered. A full Docs build renders every configured Mermaid identity in the scope. The watcher, staged intake, targeted builds, and full builds all call the same registered producer.</p>\n<h2>Source And Publication Contract</h2>\n<p>Use lowercase kebab-case identities:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/build-source/mermaid/&lt;diagram-name&gt;.mmd\n$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/&lt;scope&gt;/svg/&lt;diagram-name&gt;.svg\n</code></pre>\n<p>Markdown uses the scope's ordinary SVG media token:</p>\n<pre><code>![Useful diagram alt text](&#91;&#91;media:docs/&lt;scope&gt;/svg/&lt;diagram-name&gt;.svg&#93;&#93;)</code></pre>\n<p>Each scope owns its source and derived SVG. Do not share canonical source across scopes, edit generated SVG by hand, or register an unversioned global <code>mmdc</code> executable.</p>\n<p>Persistent <code>.mmd</code> and managed SVG remain private in the external media workspace. A public scope's confirmed Publish copies a referenced SVG to its configured repository or R2 projection, retains it while any publishable parent or sub-scope document references the identity, and removes the public copy only after the complete prospective reference set no longer uses it. Publish never ships the registered <code>.mmd</code> source.</p>\n<p>The repository-pinned persistent-media renderer uses a neutral theme, white background, strict security, <code>htmlLabels: false</code>, and an explicit 1200 by 800 browser viewport. Every persistent <code>.mmd</code> source requires non-empty Mermaid <code>accTitle</code> and <code>accDescr</code>; every Markdown reference requires useful alt text. The shared SVG sanitizer must preserve a responsive <code>viewBox</code>, accessibility metadata, and visible content before publication succeeds.</p>\n<h2>Code Authority And Extension</h2>\n<ul>\n<li>dependency and renderer settings: <code>docs-viewer/build/mermaid/</code></li>\n<li>producer implementation: <code>docs-viewer/services/docs_mermaid_media.py</code></li>\n<li>producer registry and full/targeted invocation: <code>docs-viewer/build/docs_builder/media_builds.py</code></li>\n<li>scope activation and locations: <code>docs-viewer/config/scopes/docs_scopes.json</code></li>\n<li>staged author intake: <code>docs-viewer/services/docs_staged_media_service.py</code></li>\n<li>rendered source discovery/opening: <code>docs-viewer/services/docs_diagram_source_service.py</code></li>\n<li>changed-identity rebuild: <code>docs-viewer/services/docs_live_rebuild_watcher.py</code></li>\n<li>public fence planner and manifest contract: <code>docs-viewer/services/docs_public_mermaid_projection.py</code></li>\n<li>public pair producer and build entrypoint: <code>docs-viewer/services/docs_public_mermaid_producer.py</code>, <code>docs-viewer/build/build_public_mermaid_projection.py</code></li>\n<li>public payload projection and publish boundary: <code>docs-viewer/services/docs_public_mermaid_payload.py</code>, <code>docs-viewer/services/docs_publish_gate.py</code></li>\n<li>public theme selection: <code>site/docs-viewer/runtime/js/public/docs-viewer-public-themed-diagrams.js</code></li>\n<li>exact diagram target, presentation, and temporary-resource lifecycle: <code>site/docs-viewer/runtime/js/shared/docs-viewer-diagram-detail.js</code></li>\n<li>public projection contracts: <code>test_docs_public_mermaid_projection.py</code>, <code>test_docs_public_mermaid_producer.py</code>, <code>test_docs_public_mermaid_payload.py</code>, and <code>test_docs_publish_gate.py</code></li>\n<li>external-local browser loading boundary: <code>docs_viewer_external_inline_mermaid_route.py</code></li>\n<li>focused contracts: <code>docs-viewer/tests/python/test_docs_mermaid_media.py</code>, <code>test_docs_staged_media_service.py</code>, <code>test_docs_diagram_source_service.py</code>, and <code>test_docs_live_rebuild_watcher.py</code></li>\n</ul>\n<p>To add another build-source type, register one code-owned producer and managed target, add the reciprocal <code>media.types.&lt;type&gt;.build_inputs</code> declaration, and test full builds, targeted builds, intake, watcher invalidation, materialisation verification, and reader-runtime exclusion together.</p>\n<h2>Examples</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260512-142027-54b354\">Configuration And Extension Points</a> uses inline Mermaid to demonstrate configuration, executable ownership, generated projection, and runtime capability.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260424-000000-fc8b5d\">Docs Import</a> uses inline Mermaid to demonstrate document intake, embedded media, source-editor media, publication, source writing, and rebuild boundaries.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260720-100017-e094d8\">Scope Media Management Concept</a> uses three inline Mermaid diagrams for its model and workflows.</li>\n</ul>\n<h2>Fenced Document Workflow</h2>\n<p>Author a fenced <code>mermaid</code> block directly in the document when the diagram belongs only to that document and does not need its own managed media identity. Authored non-empty <code>accTitle</code> and <code>accDescr</code> remain preferred and authoritative. In a managed reader either directive may be omitted: the adapter supplies only the missing SVG accessibility text from the exact mounted document title and diagram ordinal, retains any authored value, and emits a console warning without changing canonical Markdown. Configured public projection remains strict and requires both authored directives before preparation. Canonical Markdown, the managed payload, Markdown export, and static HTML export retain the fence and its source.</p>\n<p>In either configured managed local scope type, the manage reader finds those fences after the ordinary document mount, lazily loads the checked Mermaid <code>11.16.0</code> browser asset once for the application session, and replaces each successful fence with an accessible inline SVG host. Missing authored accessibility directives do not block rendering. Several fences render sequentially. Navigation invalidates late results, and a parser, renderer, or runtime failure retains its source with a concise visible status while technical detail stays in the console.</p>\n<p>For a configured public projection, first make the ordinary Docs payload current, then prepare the complete theme pairs:</p>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 docs-viewer/build/build_public_mermaid_projection.py --scope &lt;scope&gt; --write\n</code></pre>\n<p>Add <code>--sub-scope &lt;sub-scope&gt;</code> when the configured public sub-scope owns the document. The command uses the pinned renderer, shared sanitizer, and configured working-documents artifact adapter. It writes only projection-owned light/dark pairs plus <code>.publish/public-mermaid-projection/manifest.json</code>. Both variants must render, sanitize, preserve matching accessibility metadata and geometry, publish, and byte-verify before the family enters that manifest.</p>\n<p>Run this preparation before the existing public publish status, confirmation, and apply flow. The publish gate does not render Mermaid. It revalidates the prepared manifest and exact SVG bytes, matches each family to the current document fence and source digest, replaces only the public payload copy, and publishes only consumed variants. A missing, partial, unsafe, or stale family blocks projection. The manifest remains private, and public routes receive neither Mermaid source nor the managed adapter or vendor runtime.</p>\n<p>The active managed-local scope type remains the in-browser rendering rule: both <code>local</code> and <code>local_external</code> are eligible. External is a storage provider distinction, not a different ownership model, and no separate external-local trust capability is configured. An eligible document without a Mermaid fence loads no browser asset.</p>\n<p>Runtime authority is <code>docs-viewer/runtime/js/management/docs-viewer-inline-mermaid.js</code>, composed only by the manage entrypoint and invoked through the ordinary document controller. The checked browser asset is owned beside other local runtime code at <code>docs-viewer/runtime/vendor/mermaid/11.16.0/</code>; the Docs Viewer service preserves its stable browser URL. Neither file lives in the public site tree or public runtime manifest. Shared content CSS owns the bounded neutral host and failure treatment.</p>\n<h2>Reader Detail View</h2>\n<p>Supported rendered diagrams receive one overlaid <strong>Open diagram</strong> button while the diagram body remains ordinary document content. The button requests the shared <code>content-detail</code> main view with one exact mount-bound diagram target. The diagram adapter moves the accepted persistent image or managed-inline host into the expanded viewport and <strong>Back to document</strong> restores that same presentation and invocation focus.</p>\n<p>A configured persistent SVG retains its stable served URL. A projected public Mermaid image retains the active light or dark variant and refreshes through the public theme owner. A successfully rendered managed-local inline Mermaid host retains the adapter-owned direct SVG, including its responsive <code>viewBox</code>, visible presentation, title, and description. Theme refresh while the detail view is active updates the moved presentation through those existing owners.</p>\n<p>The existing <code>main-view</code> toolbar contributes <strong>Open in new tab</strong> only while the active adapter supplies a valid target. Persistent diagrams use their stable served URL, public-themed diagrams use the active prepared variant, and managed-inline diagrams use a temporary standalone <code>image/svg+xml</code> Blob. Theme refresh replaces the relevant toolbar target, and Back, navigation, document replacement, and adapter release revoke temporary resources before ownership ends.</p>\n<p>The shared adapter accepts generated persistent and themed markers plus the managed-inline adapter's successful host. Public readers compose the shared adapter for persistent and themed projections; Manage additionally composes inline Mermaid rendering and resource creation; Review retains its returned-package composition. The component button and icon-only toolbar link use the same pill and external-open glyph language, remain native controls with <strong>Open diagram</strong> or <strong>Open in new tab</strong> accessible labels and tooltips, and keep visible keyboard focus. The expanded viewport provides scalable keyboard-focusable inspection at supported desktop widths and in both themes. Public projected diagrams own their separate narrow-width acceptance.</p>\n"
}
