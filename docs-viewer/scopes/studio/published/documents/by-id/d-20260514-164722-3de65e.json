{
  "doc_id": "d-20260514-164722-3de65e",
  "title": "Docs Import Architecture",
  "added_date": "2026-05-14 16:47:22",
  "last_updated": "2026-08-07 22:27:41",
  "viewer_url": "/docs/?scope=studio&doc=d-20260514-164722-3de65e",
  "parent_id": "d-20260424-000000-50b63f",
  "summary": "Configuration, registries, source confinement, execution paths, extension methods, and weak spots for Docs Viewer imports.",
  "content_html": "<h1>Docs Import Architecture</h1>\n<p>Docs Import is a management workflow that reads user-selected material beneath the configured Projects base, normalizes it into document intent, and writes canonical Docs Viewer source through the local service.</p>\n<p>This document owns the stable architecture and extension method. <a href=\"/docs/?scope=studio&amp;doc=d-20260424-000000-fc8b5d\">Docs Import</a> owns the operator workflow. <a href=\"/docs/?scope=studio&amp;doc=d-20260719-182348-492a48\">HTML Import Mapping</a> owns the intended semantic-HTML conversion rules, and <a href=\"/docs/?scope=studio&amp;doc=d-20260719-182348-f326fd\">Word Import Style Mapping</a> owns Word-style semantics. Exact fields, endpoint payloads, and tests remain code-owned.</p>\n<h2>Execution Paths</h2>\n<pre><code class=\"language-text\">Projects-relative source directory\n    (default: data-sharing/import-staging)\n          |\n          v\nserver listing + source classification\n          |\n          +---------------- document source or Markdown package\n          |                              |\n          |                              v\n          |                    preview / conversion / validation\n          |                              |\n          |                              v\n          |                       ImportContent(replace)\n          |                              |\n          |                              v\n          |                    plan one document -&gt; media -&gt; source write\n          |                                              |\n          |                                              v\n          |                                        rebuild + result\n          |\n          +---------------- trusted collection\n                                         |\n                  +----------------------+----------------------+\n                  |                                             |\n          returned JSON/JSONL                         edited review folder\n                  |                                             |\n        package intake -&gt; ImportContent              provenance -&gt; ImportContent\n                  |                                             |\n                  +----------------------+----------------------+\n                                         |\n                              write-free complete collection plan\n                                         |\n                              whole-collection confirmation\n                                         |\n                                         v\n                               reread and recompute server plan\n                                         |\n                           +-------------+-------------+\n                           |                           |\n                     ordered apply              atomic apply\n                           |                           |\n                           +-------------+-------------+\n                                         |\n                                 one rebuild -&gt; result\n</code></pre>\n<p>The app-level browser workflow consumes a body-free server candidate projection, retains one server-accepted Projects-relative source marker, freezes one validated display collection for ordinary sources, and confirms complete collection plans. Trusted collection candidates carry their server-resolved manifest target instead. The browser never receives a physical path and does not own confinement, classification, converted source, hierarchy plans, media plans, collection target inference, or write authority.</p>\n<h2>Configuration And Registries</h2>\n<h3>Source Directory And Staging Gate</h3>\n<p><code>source_directory</code> is the canonical POSIX marker for one real directory beneath <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code>. <code>studio/shared/python/projects_directories.py</code> validates every requested segment without following symlinks, confirms final containment, and lists only safe direct-child directories. The exact root marker <code>.</code> is navigable but not selectable; nested accepted directories are selectable. Responses and diagnostics use only relative markers.</p>\n<p>Import staging remains the default:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/data-sharing/import-staging/\n</code></pre>\n<p>This is a shared user drop-zone, not a Docs Import inbox. Ordinary document sources may be selected from another accepted nested Projects directory. Candidate inventory and every preview/apply request bind the same validated <code>{source_directory, staged_filename}</code> pair, and the service resolves the direct child again on each request. Nested filenames, stale or moved entries, traversal, symlinks, and changed classification fail before import.</p>\n<p>Trusted returned packages and edited review-source folders remain staging-only. A registered recognizer sees the untouched direct child first; outside staging, a recognized trusted source is projected as blocked and cannot fall through to ordinary handling. The browser-local remembered marker is convenience state only: Import revalidates it on every session, clears an invalid value before validating staging, and never derives a source from UI context.</p>\n<h3>Source Classification</h3>\n<p>Classification is code-owned and deliberately closed:</p>\n<ul>\n<li><code>SOURCE_FORMAT_BY_SUFFIX</code> in <code>docs-viewer/services/docs_import_common.py</code> maps Word <code>.docx</code>, HTML, Markdown, and text suffixes to a document <code>source_format</code>.</li>\n<li>A direct-child directory is classified separately as <code>markdown_package</code> after package validation.</li>\n<li>Supported trusted document-package JSON/JSONL is classified under the retained serialized source-format id <code>data_sharing_documents</code> from trusted export metadata. Unregistered JSON/JSONL is not a Docs Import source.</li>\n<li>A complete edited Docs Review source folder is classified as <code>edited_review_sources</code> by strict provenance, retained-manifest, and trusted metadata validation before Markdown-package classification.</li>\n<li><code>generate_import_preview()</code> in <code>docs_import_preview.py</code> performs explicit format dispatch.</li>\n</ul>\n<p>The browser receives the resulting <code>source_format</code>; it does not recreate suffix or package rules. Registered collection recognizers receive untouched candidates before generic Markdown front-matter stripping. This registry is an allowlist, not a plugin loader.</p>\n<h3>Target Collection And Media Policy</h3>\n<p><code>docs-viewer/config/scopes/docs_scopes.json</code> defines top-level source roots, configured one-level sub-scopes, and each published media type's location. The shared managed-target resolver accepts only <code>{ scope }</code> or <code>{ scope, sub_scope }</code>, rejects nested or unconfigured children, and returns the confined source root used throughout the run. <code>docs_scope_config.py</code> validates and normalizes repository, external-local, or R2 locations. Import media services resolve the selected type through the common artifact-location adapter.</p>\n<p>Configured sub-scopes own documents rather than a second media tree, so ordinary child imports reuse the parent scope's media policy. Source-format registration does not decide where media is stored.</p>\n<p>Trusted collection Import is a separate, fail-closed policy. A\nsub-scope omitting <code>supports_return_import</code> is not eligible. An explicit <code>true</code>\nonly makes a returned package or edited review-source folder import-capable\nwhen its package profile also supports Docs Review and return Import.\nAnalysis/Tags is the first opted-in collection.</p>\n<h2>The Core Contracts</h2>\n<table>\n<thead>\n<tr>\n<th>contract</th>\n<th>owner</th>\n<th>purpose</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>confined Projects directory navigation</td>\n<td><code>studio/shared/python/projects_directories.py</code></td>\n<td>Validate canonical relative markers and list safe direct-child directories without import policy.</td>\n</tr>\n<tr>\n<td>document-source allowlist and format mapping</td>\n<td><code>docs_import_common.py</code></td>\n<td>Classify ordinary document sources.</td>\n</tr>\n<tr>\n<td>exact source inventory and dispatch</td>\n<td><code>docs_import_source_service.py</code></td>\n<td>Resolve the selected directory/direct-child pair, run recognizers first, and keep trusted sources staging-only.</td>\n</tr>\n<tr>\n<td>conversion and preview validation</td>\n<td><code>docs_import_preview.py</code></td>\n<td>Produce Markdown and media intent without a source write.</td>\n</tr>\n<tr>\n<td>body-free candidate projection</td>\n<td><code>docs_import_candidate_projection.py</code></td>\n<td>Project candidate kind, action state, exact target mode, safe diagnostics, and display label after recognizer-first classification.</td>\n</tr>\n<tr>\n<td>exact collection adoption</td>\n<td><code>docs_management_document_target.py</code>, <code>docs_management_import_service.py</code></td>\n<td>Resolve one configured parent or child source root without fallback.</td>\n</tr>\n<tr>\n<td>Word semantic-HTML adapter</td>\n<td><code>docs_import_docx.py</code></td>\n<td>Apply the repository Mammoth style/image policy without gaining write authority.</td>\n</tr>\n<tr>\n<td>trusted documents-package intake</td>\n<td><code>docs_import_document_package.py</code>, <code>docs_import_document_package_content.py</code></td>\n<td>Verify wrapper provenance and emit wrapper-neutral records.</td>\n</tr>\n<tr>\n<td>edited review-source intake</td>\n<td><code>docs_import_review_source_folder.py</code>, <code>docs_import_review_source_collection.py</code></td>\n<td>Validate a complete reviewed folder and emit overwrite-only normalized records.</td>\n</tr>\n<tr>\n<td>wrapper-neutral document intent</td>\n<td><code>docs_import_content.py</code></td>\n<td>Represent one document independently of its trusted transport wrapper.</td>\n</tr>\n<tr>\n<td>per-document plan and apply</td>\n<td><code>docs_import_document.py</code></td>\n<td>Validate a create/overwrite and perform its media/source mutation.</td>\n</tr>\n<tr>\n<td>write-free collection plan</td>\n<td><code>docs_import_collection_plan.py</code></td>\n<td>Resolve all records, collisions, parents, warnings, and blockers.</td>\n</tr>\n<tr>\n<td>whole-package confirmation, ordered apply, and result</td>\n<td><code>docs_import_collection_apply.py</code>, <code>docs_import_collection_result.py</code></td>\n<td>Revalidate the server-issued package plan, mutate, rebuild, and return the grouped result.</td>\n</tr>\n<tr>\n<td>HTTP-facing request adoption</td>\n<td><code>docs_management_import_service.py</code></td>\n<td>Require the explicit source identity and exact managed destination before service dispatch.</td>\n</tr>\n<tr>\n<td>app Import lifecycle and exact result refresh</td>\n<td><code>docs-viewer-management-import-controller.js</code>, <code>docs-viewer-management-import-result.js</code>, <code>docs-viewer-management.js</code></td>\n<td>Own the single entrypoint, frozen ordinary context, validated result route, and exact mounted-owner refresh.</td>\n</tr>\n<tr>\n<td>shared folder navigation</td>\n<td><code>shared/frontend/js/folder-picker.js</code></td>\n<td>Render app-neutral lazy breadcrumb, parent, direct-child, keyboard, and selectable-marker behavior through injected load/submit callbacks.</td>\n</tr>\n<tr>\n<td>Docs Import source preference and fallback</td>\n<td><code>docs-viewer/runtime/js/import/docs-import-source-selection.js</code></td>\n<td>Revalidate the local marker, clear invalid state before staging fallback, and refresh exact candidates without owning destination.</td>\n</tr>\n<tr>\n<td>browser candidate, Review, confirmation, and result UI</td>\n<td><code>docs-viewer/runtime/js/import/</code></td>\n<td>Validate the candidate projection, dispatch existing ordinary/collection owners, hand off one reviewable row, and present explicit destination links.</td>\n</tr>\n</tbody>\n</table>\n<p>These are navigation pointers, not a promise that every helper will remain in the same file.</p>\n<h2>Normalized Document Intent</h2>\n<p><code>ImportContent</code> is the seam between source-specific adapters and document planning. It carries source identity, title, allowed front matter, hierarchy, diagnostics, and one explicit content intent:</p>\n<ul>\n<li><code>replace</code>: convert supplied Markdown, HTML, or plain text and use it as the target body</li>\n<li><code>preserve-existing</code>: update allowed metadata or hierarchy while retaining the current body</li>\n<li><code>empty-new</code>: create a structural document with no fabricated body</li>\n</ul>\n<p>Ordinary document-source imports produce one <code>replace</code> content record for a newly allocated document after preview. Collection adapters may produce any of the three intents and may target existing documents explicitly. This prevents collection planning from inventing replacement content for hierarchy-only records and lets future trusted wrappers reuse document planning without reusing Data Sharing provenance.</p>\n<p>Trusted collection adapters may supply permitted <code>title</code>, <code>parent_id</code>,\n<code>summary</code>, and <code>publishable</code> intent when the exact destination supports Publish. The edited review-source adapter is\noverwrite-only and maps body, title, and summary; parent and publishability are\neligible only where the destination contract permits them. Local destinations reject publication metadata. Configured child hierarchy, group,\nstatus, and publication state remain destination-owned. For an unclaimed ordinary\nMarkdown source, a well-formed leading front-matter block is removed after\nregistered collection recognition; only scalar <code>title</code> can act as a fallback\nand all other fields are ignored with diagnostics. Canonical formatting, local\nidentity, timestamps, target paths, collision targets, and default publishability\nremain service-owned.</p>\n<p>The body-free candidate listing is context-independent. Returned packages and\nedited review-source folders are recognized from untouched staged candidates\nand retain one exact resolved <code>{ scope }</code> or <code>{ scope, sub_scope }</code> target.\nOrdinary rows declare display-context routing without carrying a target.\nMissing legacy capabilities, invalid <code>false</code> / <code>true</code>, mismatched collections,\nretired physical names, and unsafe provenance fail closed.</p>\n<h2>Ordinary Source Path</h2>\n<p>An ordinary source follows one-document orchestration:</p>\n<ol>\n<li>Resolve and classify one exact <code>{source_directory, staged_filename}</code> direct child, then adopt one validated <code>{ scope }</code> or <code>{ scope, sub_scope }</code> collection.</li>\n<li>Convert it to Markdown and validate it through the shared Python renderer.</li>\n<li>Plan media and any role-marked interactive companions.</li>\n<li>Allocate a fresh immutable local identity by checking only its candidate source path; do not load or parse target-scope documents.</li>\n<li>Convert the accepted preview to <code>ImportContent</code>, plan the new document, materialize media, atomically create source, and invoke the owning parent or confined child rebuild boundary.</li>\n</ol>\n<p>The format converter never writes source or performs a rebuild. The Word adapter passes Mammoth's semantic HTML unchanged into the shared HTML converter; temporary HTML and base64 image bodies remain server-side while the public preview contains canonical Markdown and safe media-plan metadata. Interactive-companion replacement confirmation and endpoint authorization stay outside the converter.</p>\n<p>Immediately before a replacement body is written, the per-document owner applies the shared Markdown blank-line rule: a line containing only Unicode whitespace becomes empty outside fenced code and explicit <code>&lt;pre&gt;</code> blocks. This contains hidden no-break spaces from exported notes without trimming prose, Markdown hard-break spaces, or literal examples.</p>\n<p>For a sub-scope display, the report publishes the mounted collection to the\nmanage host; it does not own an Import control or workflow. The app-level\nmodal freezes that collection only for ordinary candidates and sends\n<code>sub_scope</code> on every attempt. Trusted candidates ignore display context and\nretain their manifest target. A successful response supplies a prevalidated\n<code>viewer_url</code> and exact target. If that same child collection is mounted, the\nhost invokes its document- or collection-refresh callback; otherwise it leaves\nthe current route fixed and presents the result link. Public reports, checked\nIDs, URL text, displayed documents, and the selected parent are not\ndestination sources.</p>\n<h2>Collection Path</h2>\n<p>A collection source first passes its trusted wrapper owner: returned JSON/JSONL\nuses document-package intake, while an edited review-source folder validates\nthe complete retained package and trusted sidecar contract. The generic\ncollection planner then processes every normalized record without exposing\nbodies or generated source to the browser.</p>\n<p>The plan resolves:</p>\n<ul>\n<li>create or collision state</li>\n<li>stable planned local identities for creates, including package-parent remapping</li>\n<li>server-planned create/overwrite actions</li>\n<li>record errors that block the whole package</li>\n<li>existing and package-supplied parent chains</li>\n<li>hierarchy blockers and cycles</li>\n<li>safe media summaries and unmapped asset warnings</li>\n</ul>\n<p>Confirmed apply rereads the selected collection, recomputes the plan using the\nserver-issued receipts, and checks package or folder identity and planned\nactions against current state. The grouped result is returned directly; apply\ndoes not create a separate result document or result-specific log.</p>\n<p>Returned-package apply is synchronous batch orchestration, not a transaction or\njob system. Earlier writes survive a later source-write failure; later records\nbecome not attempted. R2 publication failure blocks that record's source write\nso a new media token is not committed without its media. Generation failure\nremains separate from successful canonical mutations.</p>\n<p>Edited review-source apply selects the other shared apply mode: overwrite-only\nplans are completely prepared, exact canonical source snapshots are rechecked\ninside one parent or child mutation boundary, and a write or rebuild failure\nrestores the complete selected source set and its projection. This atomicity is\na wrapper policy supplied through the common apply owner, not a generic promise\nfor all collection formats.</p>\n<h2>Extension Methods</h2>\n<h3>Add An Ordinary Document Format</h3>\n<ol>\n<li>Add a code-owned allowlist entry and stable <code>source_format</code>.</li>\n<li>Add a preview builder that returns normalized Markdown, warnings, validation, and optional media plans.</li>\n<li>Dispatch it from <code>generate_import_preview()</code>.</li>\n<li>Reuse <code>ImportContent</code>, fresh identity allocation, per-document create, writes, and rebuilds.</li>\n<li>Add focused listing, preview, repeated-create, media, and service tests.</li>\n</ol>\n<p>Do not add standalone media here. Image and opaque-download intake belongs to the source editor's staged-media service. Do not add a document format by extending only the browser suffix list or by giving a converter direct write access.</p>\n<h3>Add A Collection Wrapper</h3>\n<ol>\n<li>Require an explicit, trusted, versioned wrapper contract.</li>\n<li>Classify it through trusted package registration before ordinary document dispatch.</li>\n<li>Normalize each record to <code>ImportContent</code> with explicit content intent.</li>\n<li>Reuse the generic collection planner, whole-package confirmation, per-document apply, rebuild, and result contracts.</li>\n<li>Add wrapper-specific UI only when its package confirmation genuinely differs.</li>\n</ol>\n<p>A collection adapter should own provenance and schema interpretation. It should not fork document mutation rules.</p>\n<h3>Add Or Change A Published Media Location</h3>\n<p>Register the media type's provider, confined path, served prefix, and required capabilities, then extend the focused location adapter only if the provider itself is new. Do not add publication policy to the document source mapping or a format converter.</p>\n<h2>Why This Structure Exists</h2>\n<ul>\n<li>A closed server registry makes accepted local files inspectable and testable.</li>\n<li>Wrapper-neutral <code>ImportContent</code> separates provenance from canonical mutation.</li>\n<li>Write-free planning makes collisions, hierarchy, and unsafe input visible before collection mutation.</li>\n<li>Replanning on confirmation prevents the browser from applying a stale plan.</li>\n<li>Shared per-document apply keeps ordinary and collection imports on the same source-formatting and mutation boundary.</li>\n<li>Media publication before source write prevents new R2 tokens from pointing at unpublished record-owned media.</li>\n<li>Revalidating the exact source pair on every request prevents a remembered or displayed marker from becoming filesystem authority.</li>\n</ul>\n<h2>Weak Spots</h2>\n<ul>\n<li>“Registry” is only partly accurate: <code>SOURCE_FORMAT_BY_SUFFIX</code> covers ordinary document suffixes, while Markdown packages and trusted collections have separate classification paths.</li>\n<li>Format metadata and preview dispatch are separate code structures, so a new ordinary format requires coordinated edits rather than one self-contained registration.</li>\n<li>The single-source and collection workflows share one endpoint and modal but have different orchestration and failure semantics.</li>\n<li>Returned-package apply is synchronous and partially durable; large packages\nhave no progress, resume, rollback, or background job. Edited review-source\nfolders have atomic source/rebuild rollback but still no job controls.</li>\n<li>Role-marked interactive companions are discovered across the selected source directory during an ordinary import rather than explicitly associated with one selected document.</li>\n<li>Embedded-media publication spans scope config, artifact-location adapters, the shared SVG sanitizer, and focused media services. Looking only at the document source mapping gives an incomplete picture of import behavior.</li>\n<li>Returned Data Sharing packages and edited Docs Review source folders now\nprove two wrapper policies through the shared normalized planning/apply seam.</li>\n</ul>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260514-184303-7914e2\">Media Handling</a> owns storage paths and materialization rules. <a href=\"/docs/?scope=studio&amp;doc=d-20260607-222033-647b52\">Docs Management Endpoints</a> owns the HTTP surface rather than this architecture summary.</p>\n"
}
