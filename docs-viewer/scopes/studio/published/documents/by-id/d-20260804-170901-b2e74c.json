{
  "doc_id": "d-20260804-170901-b2e74c",
  "title": "TDL-0 — Tag Document Metadata Authoring Refactor Delivery",
  "added_date": "2026-08-04 17:09:01",
  "last_updated": "2026-08-14 16:17:28",
  "viewer_url": "/docs/?scope=studio&doc=d-20260804-170901-b2e74c",
  "parent_id": "d-20260804-160200-fa7bb7",
  "summary": "move Tag document group editing out of the generic metadata modal into a configured Tag-owned modal and read-only Info projection",
  "ui_status": "done",
  "content_html": "<h1>TDL-0 — Tag Document Metadata Authoring Refactor Delivery</h1>\n<h2>Status And Purpose</h2>\n<p>This delivery moved Analysis Tags document <code>group</code> editing out of the app-level <strong>Edit metadata</strong> modal so that the shared modal contains only document-common front matter.</p>\n<p>Before this delivery, the generic modal had a dedicated <code>group</code> field populated from sub-scope configuration. Its ownership was wrong: <code>group</code> belongs to the registered Tag-document customisation, not to every document.</p>\n<h2>Complete Outcome</h2>\n<p>On the local Manage route, Info remains read-only and displays all metadata projected by the exact active document's resolved scope/sub-scope configuration. For the current Analysis Tags registration, that includes the document's configured <code>group</code> state beneath the common document summary. Public Info receives no Manage customisation data or controls.</p>\n<p>A resolved <code>sub_scope_customisation</code> that declares <code>DocsSubScopeAssignableFieldGroup(group_id=&quot;tag_fields&quot;, ...)</code> may expose a focused Tag-document fields action and modal. <code>analysis/tags</code> is the current configuration registrant with that declaration. The server resolves it with <code>sub_scope_customisation_assignable_field_groups()</code> and the browser checks its projected ID with <code>hasDocsViewerAssignableFieldGroup()</code>; the current ordered group choices remain in validated customisation settings.</p>\n<p>The modal initially edits only document <code>group</code>, using the current configured choice order and exact source value. It owns confirmation, cancellation, exact <code>{scope, sub_scope, doc_id}</code> target, source revision, mutation feedback, and clear/unassigned behavior. TDL-1 later extends the same registered field group and modal with optional canonical <code>tag_id</code> selection while Info remains read-only.</p>\n<p>After the migration, <strong>Edit metadata</strong> no longer contains, hydrates, renders, validates, or submits <code>group</code>. Together with SSP-0's Projects extraction, this restores the target boundary: <strong>Edit metadata</strong> owns common document fields, while configured sub-scope workflows own their fields.</p>\n<p>Document <code>group</code> and canonical Tag Registry <code>group</code> remain independent fields. Existing create-time seeding stays create-only. Editing a Tag document's group through this modal never updates, compares, repairs, or triggers a build from the Registry group, and editing the Registry never rewrites document front matter.</p>\n<h2>Awareness And Registration Boundary</h2>\n<ul>\n<li><strong>Edit metadata</strong> remains document-agnostic and knows only common front matter.</li>\n<li><strong>Info</strong> resolves the active collection's registered metadata display contribution and renders its values read-only.</li>\n<li>The focused Tag-document modal resolves the active collection's declared assignable Tag-document fields and their choices.</li>\n<li>Current availability in <code>analysis/tags</code> is a configuration fact, not a hard-coded route or collection-name condition.</li>\n<li><code>analysis/tags</code> public presentation as Concepts does not change its technical collection identity or expose Manage authoring capability publicly.</li>\n</ul>\n<h2>Delivery Discipline</h2>\n<p>TDL-0 consumes SSC-1's <code>DocsSubScopeAssignableFieldGroup</code>, exact-target, metadata, browser-composition, and access-safe capability projection, then reuses the current Analysis Tags group choices, management modal shell, revision receipts, atomic source mutation, and targeted sub-scope rebuild. The exact definition and projected <code>tag_fields</code> group own availability and accepted fields. It does not introduce a generic field-schema editor, editable Info panel, Tag Registry synchronization, generic cross-collection mutation service, public management contribution, or Tag association behavior.</p>\n<p>TDL-0.0 is a short scope-and-spec sanity gate. Each implementation stage ends with focused evidence and an independent code/diff review before the next stage begins.</p>\n<h2>Delivery Steps</h2>\n<h3>TDL-0.0 — Confirm Scope And Delivery</h3>\n<ul>\n<li>[x] Confirm the shipped <code>sub_scope_customisation</code>, <code>DocsSubScopeAssignableFieldGroup</code>, exact-target, metadata-aspect, and access-safe browser capability contracts; then establish <code>tag_fields</code> through the Analysis Tags definition rather than treating it as already shipped.</li>\n<li>[x] Confirm the likely blast radius across hosted-view context, active sub-scope target projection, Analysis Tags customisation, metadata modal shell/workflow, focused management modal, group mutation, targeted rebuild, and current group/report tests.</li>\n<li>[x] Confirm the deliverables: read-only configured group display, capability-selected Tag-document modal, exact revision-safe group update, generic <code>group</code> control retirement, TDL-1 extension seam, focused evidence, and durable Info/management/customisation documentation transfer.</li>\n<li>[x] Stop for review if the outcome requires inline Info editing, literal collection-name checks, Registry/document group synchronization, a generic field-schema framework, public authoring controls, or early <code>tag_id</code> association work.</li>\n</ul>\n<p><strong>Checkpoint:</strong> a concise sanity note confirms implementation fit, broad owners, blast radius, deliverables, risks, exact-target behavior, public isolation, and the TDL-1 handoff. Stop before implementation approval.</p>\n<h4>TDL-0.0 Sanity Note — 11 August 2026</h4>\n<p>The current implementation fits the delivery without a new framework. <code>analysis_tags</code> has the correct exact collection identity, document-group choices, private manifest, and browser, but no metadata aspect or assignable field group. SSC-1's <code>plan_assign_field_group()</code> service and the shipped Projects modal/Info contribution provide the accepted capability-selected, exact-target, revision-safe pattern; TDL-0 should add <code>tag_fields</code> through that seam and keep the generic metadata modal as the temporary old owner until TDL-0.2 removes it.</p>\n<p>The broad owners are the Analysis Tags customisation definition and validation, hosted Manage context and Info composition, the generic metadata modal, the focused assign-field-group mutation/modal workflow, targeted sub-scope rebuild, and their existing service, module, route, and public-isolation contracts. Report Blocks and intentional child-document exclusion do not expand this surface. The repaired Analysis-only public location seam is a completed prerequisite regression fix, not a TDL-0 implementation dependency.</p>\n<p>The bounded result remains read-only configured group display, a capability-selected Tag-document modal, exact revision-safe group mutation, retirement of generic <code>group</code> editing, and a clean TDL-1 extension point for <code>tag_id</code>. Primary risks are duplicate field ownership during cutover, hard-coded collection checks, leaking private metadata or controls publicly, and accidentally coupling Registry group to document group. Those conditions remain stop signals. That gate stopped before implementation; TDL-0.1 was subsequently approved and completed below.</p>\n<h3>TDL-0.1 — Add Read-Only Configured Tag Metadata</h3>\n<ul>\n<li>[x] Project the exact active managed-document target and its registered Tag-document metadata into Info without reparsing Markdown or hydrating a write service in the view.</li>\n<li>[x] Render configured document group and unassigned/unavailable evidence read-only while preserving the compact common metadata summary.</li>\n<li>[x] Project Tag-document fields action availability from the resolved assignable field declaration; prove the current Analysis Tags registration enables it while other collections and public routes remain action-free.</li>\n<li>[x] Preserve outside-context Info view selection and the shared shell's view-agnostic lifecycle.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused exact-target, wrong-target, group-state, lifecycle, action-capability, other-collection, and public-isolation evidence passes. An independent code/diff review confirms Info remains a passive configured projection. Stop before TDL-0.2.</p>\n<h4>TDL-0.1 Checkpoint Note — 11 August 2026</h4>\n<p>The Analysis Tags registration now owns a read-only metadata aspect for its validated <code>group</code> field and declares <code>tag_fields(group)</code> through the existing assignable-field contract. Its Manage-only browser descriptor projects that capability; public descriptors remain absent. The existing report host passes its already-loaded private manifest data to the registered contribution, which accepts only the exact configured collection and exact active document target. Info renders the resulting generic field as assigned, unassigned, or unavailable and carries the capability-derived Tag action flag without adding an editor, modal, service hydration, or write path.</p>\n<p>Focused evidence covers normalized server metadata and browser configuration, exact and wrong targets, all three group states, capability present and absent, Manage detail versus list selection, and public isolation. Fifty-six focused Python tests, the sub-scope report module smoke, the shared metadata Info-view smoke, and changed-source Python/JavaScript lint pass. The checkpoint diff keeps the shared Info renderer passive and leaves the generic metadata <code>group</code> owner unchanged for the atomic TDL-0.2 cutover. Stop before TDL-0.2.</p>\n<h3>TDL-0.2 — Move Group Editing Into The Tag-Owned Modal</h3>\n<ul>\n<li>[x] Add the focused Tag-document fields modal with configured group choices, exact target and source revision, confirmation, cancellation, clear behavior, and contained status/errors.</li>\n<li>[x] Add a capability-restricted mutation that resolves the exact sub-scope configuration, accepts only its declared Tag-document fields and values, preserves revision conflict and atomic replacement, and triggers the exact required rebuild without a collection-name branch.</li>\n<li>[x] Remove the dedicated group field, choice projection, hydration, validation, and payload handling from <strong>Edit metadata</strong> after focused equivalence evidence confirms the new owner.</li>\n<li>[x] Preserve a bounded extension seam for TDL-1 to add optional <code>tag_id</code> selection to the registered Tag-document field group without changing Info or the generic modal.</li>\n<li>[x] Preserve the independence of document group and Registry group across reads, writes, builds, reports, and failures.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused modal, configured choices, clear, cancel, stale revision, invalid choice, exact-target, atomic-write, targeted-build, generic-metadata independence, Registry independence, and public-boundary evidence passes. An independent code/diff review confirms the Tag customisation is the only group-editing owner. Stop before TDL-0.3.</p>\n<h4>TDL-0.2 Checkpoint Note — 11 August 2026</h4>\n<p>The Analysis Tags contribution now renders a <strong>Tag fields</strong> action only when the resolved browser descriptor declares <code>tag_fields</code>. Its focused modal rereads the exact managed-document metadata and source revision, preserves configured group order, supports <strong>No group</strong>, cancel, contained service feedback, and exact assignment through the existing <code>/docs/assign-field-group</code> service. A successful assignment refreshes the same detail target. The registered Analysis Tags metadata normalizer accepts exactly the declared <code>group</code> field and configured values; the generic assignment planner retains exact target, revision conflict, atomic source replacement, and sub-scope rebuild ownership without a collection-name branch. The same declared field group remains the bounded TDL-1 seam for adding <code>tag_id</code>.</p>\n<p>The generic metadata response, shell, modal composition, hydration, choices, request payload, mutation planner, response, and log no longer own <code>group</code>; the server rejects an attempted generic <code>group</code> update. Focused update and clear plans write only the exact Tag document, while invalid and stale requests produce no write. No Tag Registry source, service, report, or build path participates in the read, assignment, rebuild, or failure flow, so document <code>group</code> and Registry <code>group</code> remain independent.</p>\n<p>Focused evidence covers assigned and clear writes, configured and invalid choices, cancel, unavailable service, exact and wrong targets, stale revisions, generic-metadata rejection, one-document atomic replacement, sub-scope rebuild receipts, refreshed Manage manifest/detail state, capability absence, and the public read-only boundary. Sixty-three focused Python tests, the sub-scope report, modal-focus, managed sub-scope route, and metadata Info-view smokes, plus changed-source Python and JavaScript lint pass. The checkpoint diff review confirms the registered Tag customisation is the only document-group editing owner. Stop before TDL-0.3.</p>\n<h3>TDL-0.3 — Verify And Transfer</h3>\n<ul>\n<li>[x] Run focused service, browser-module, route-boundary, source-lint, generated-doc, search, and whitespace checks proportional to the changed contracts.</li>\n<li>[x] Verify rendered-document and Markdown-source contexts, assigned/unassigned group, unavailable management service, stale revision, clear/cancel behavior, no-contribution collections, and public read-only presentation.</li>\n<li>[x] Perform the independent final code/delivery review and reconcile completion claims with exact evidence and omissions.</li>\n<li>[x] Update <a href=\"/docs/?scope=studio&amp;doc=d-20260623-000000-c99cef\">Info Panel</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260519-202931-66c794\">Management Operations</a>, and the current sub-scope customisation owner with the shipped read-only projection, configured authoring action, mutation, group independence, and public-isolation rules.</li>\n<li>[x] Update TDL-1 to consume the accepted Tag-document field contribution before adding <code>tag_id</code> meaning and associations.</li>\n</ul>\n<h4>TDL-0.3 Closeout Note — 11 August 2026</h4>\n<p>Final evidence retains the accepted sixty-three focused Python tests and four browser smokes from TDL-0.2: sub-scope report modules, modal focus, the managed sub-scope route, and metadata Info view. Together they exercise exact and wrong targets, rendered-document and Markdown-source Info context, assigned, unassigned, invalid, and unavailable group state, configured choices, missing services, cancel and clear, stale revision, exact atomic write and rebuild receipts, generic-metadata rejection, no-contribution collections, detail refresh, and the public read-only boundary. Focused Python and JavaScript lint for every changed implementation and test file passes.</p>\n<p>The durable <a href=\"/docs/?scope=studio&amp;doc=d-20260623-000000-c99cef\">Info Panel</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260519-202931-66c794\">Management Operations</a>, and <a href=\"/docs/?scope=studio&amp;doc=d-20260731-155053-3929e2\">Sub-Scope Customisation Architecture</a> owners now state the passive Info, capability-selected authoring, strict mutation, generic-metadata exclusion, Registry independence, and public-isolation contracts. TDL-1 now consumes the exact group-only declaration, normalizer, private projection, modal, and Info seams that it must extend together. Studio document and search builders report no pending writes after watcher regeneration and no document warnings; whitespace inspection passes.</p>\n<p>The final bounded diff review found no collection-name availability branch, duplicate group editor, compatibility reader, public management import, Registry coupling, or early <code>tag_id</code> association work. The full <code>source-lint</code> profile recorded six passing source boundaries, including Docs Viewer production, but its unchanged tests boundary reports two pre-existing unused imports in <code>docs-viewer/tests/python/test_build_docs_cli.py</code>; the TDL-0 changed-test lint remains green, so this unrelated finding was not absorbed into the delivery. No broad tactile, visual, mobile, or screen-reader audit is claimed. TDL-1 implementation remains unstarted.</p>\n<h2>Completion Evidence</h2>\n<ul>\n<li><strong>Edit metadata</strong> contains and submits only common document fields; its dedicated <code>group</code> control has retired.</li>\n<li>Info displays the exact active collection's registered metadata read-only, including Analysis Tags document group.</li>\n<li>The current Analysis Tags configuration exposes the Tag-document fields modal through its declared assignable group without literal collection-name availability logic.</li>\n<li>Document group updates are exact-target, revision-safe, atomic, choice-validated, and independently rebuilt.</li>\n<li>Document <code>group</code> and Registry <code>group</code> retain independent owners and mutation paths.</li>\n<li>Public routes receive neither private group metadata nor Tag-document authoring capability.</li>\n<li>TDL-1 can add optional <code>tag_id</code> to the same Tag-owned modal and read-only Info projection without changing shared surfaces.</li>\n</ul>\n"
}
