{
  "doc_id": "d-20260715-181845-524859",
  "title": "Document Diagrams Concept",
  "added_date": "2026-07-15 18:18:45",
  "last_updated": "2026-07-19 12:15:22",
  "viewer_url": "/docs/?scope=studio&doc=d-20260715-181845-524859",
  "parent_id": "d-20260715-181845-0158f9",
  "summary": "Repository-built Mermaid diagrams for Docs Viewer documents.",
  "ui_status": "done",
  "content_html": "<h1>Document Diagrams Concept</h1>\n<h2>Purpose</h2>\n<p>High-level diagrams make execution paths, ownership, and public/manage/server boundaries easier to understand than prose or text art.</p>\n<h2>Decision</h2>\n<p>Use Mermaid as the default source language for authored architecture and workflow diagrams in any Docs Viewer scope.</p>\n<p>Mermaid source is the durable authority. A repository-owned Docs media producer renders configured <code>.mmd</code> source to static SVG. Docs Viewer displays the result as an ordinary image and does not parse Mermaid or load a diagram runtime in the browser.</p>\n<p>Finished SVG and Mermaid source share <strong>Add image…</strong> but take different service paths. A staged <code>.svg</code> is sanitized and published as finished vector media; a staged <code>.mmd</code> is retained as canonical build source and passed through the registered Mermaid producer before its SVG token is returned. An unprocessed <code>.mmd</code> beside a manually imported SVG is not a supported workflow.</p>\n<h2>Viewer Author Workflow</h2>\n<ol>\n<li>Open the target document's source editor and choose <strong>Add image…</strong>, not global <strong>Import</strong>.</li>\n<li>Select a staged <code>.mmd</code> file and enter useful diagram alt text.</li>\n<li>The action copies the canonical source into the current scope's configured <code>media/mermaid/</code> location and invokes the Mermaid producer for that source.</li>\n<li>After successful SVG publication and byte verification, the action inserts this form at the current selection or caret:</li>\n</ol>\n<pre><code>![Diagram alt text](&#91;&#91;media:docs/&lt;scope&gt;/svg/&lt;diagram-name&gt;.svg&#93;&#93;)</code></pre>\n<ol start=\"5\">\n<li><strong>Rebuild doc</strong> commits the dirty Markdown buffer and its targeted document payload. Returning to the displayed document shows the published SVG.</li>\n</ol>\n<p>A render or publication failure inserts no token. The interactive action renders the selected source immediately; a later full Docs build runs the same producer over all configured Mermaid source so the derived SVG remains reproducible.</p>\n<h2>Editing Boundary</h2>\n<p>Mermaid CLI is the repository renderer, not the human editing interface. This feature does not surface or edit raw <code>.mmd</code> inside Docs Viewer. The document source editor remains a Markdown editor; it does not host an embedded Mermaid editor or live diagram preview.</p>\n<p>Human authors create diagrams in an external Mermaid-aware editor and save the initial <code>.mmd</code> into staging for <strong>Add image…</strong>. A browser editor still requires an explicit save back to a local file; a local-file editor such as VS Code with Mermaid support is the direct-save fit. Codex and repository authors can edit canonical <code>.mmd</code> directly.</p>\n<p>External-editor preview is authoring assistance. The repository-pinned Mermaid version and producer output remain authoritative.</p>\n<h2>Ongoing Edit Workflow</h2>\n<p>After initial intake, the scope-owned <code>.mmd</code> is edited in place rather than staged again:</p>\n<ol>\n<li>view the rendered document and open Document Info;</li>\n<li>under <strong>Diagrams</strong>, choose the diagram-title link corresponding to the rendered SVG to open its Mermaid source in VS Code;</li>\n<li>save the <code>.mmd</code> in its scope-owned <code>media/mermaid/</code> location;</li>\n<li>Docs Watcher detects the configured Mermaid-source change and runs the producer for that identity;</li>\n<li>reload the displayed document to see the updated SVG at its existing token.</li>\n</ol>\n<p>Rendered Document Info lists only scope-confined Mermaid sources that correspond to the saved document's SVG tokens and registered <code>mermaid</code> to <code>svg</code> build relationship. A standalone SVG has no canonical <code>.mmd</code> and does not appear. Multiple diagrams become separate labelled links; no toolbar action, caret inference, arbitrary path, or source picker is required.</p>\n<p>These links are manage-only and expose logical identities rather than physical paths. The server revalidates the document reference, configured same-basename Mermaid source, containment, and file existence before delegating to VS Code. In Markdown source mode the info surface remains the semantic-token picker and does not show diagram links.</p>\n<p>The Markdown token does not change, so a normal diagram edit does not require <strong>Add image…</strong>, <strong>Rebuild doc</strong>, or a docs-search rebuild.</p>\n<h2>Repository And Codex Workflow</h2>\n<p>Repository authors and Codex do not drive <strong>Add image…</strong> or use staging. They:</p>\n<ol>\n<li>write the canonical <code>.mmd</code> directly to the scope's configured <code>media/mermaid/</code> source location;</li>\n<li>add the SVG media token directly to the owning Markdown document;</li>\n<li>run the targeted Docs builder for the owning document:</li>\n</ol>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 docs-viewer/build/build_docs.py --scope &lt;scope&gt; --only-doc-ids &lt;doc-id&gt; --write\n</code></pre>\n<p>The Mermaid delivery extends targeted builds so they render and publish Mermaid outputs referenced by the selected document before rendering its payload. The normal targeted search build follows when the document prose changed. A full Docs build continues to process all configured Mermaid source. No separate diagram-conversion CLI is required.</p>\n<h2>Repository Build Contract</h2>\n<ul>\n<li>Own the Node boundary beneath <code>docs-viewer/build/mermaid/</code>: exact dependency and scripts in <code>package.json</code>, resolved dependency graph in <code>package-lock.json</code>, and shared render settings in <code>mermaid-config.json</code>.</li>\n<li>Pin <code>@mermaid-js/mermaid-cli</code> exactly to <code>11.16.0</code>; invoke that local executable and never an unversioned global <code>mmdc</code> installation.</li>\n<li>Register Mermaid in each participating scope's <code>source.build_media</code> configuration.</li>\n<li>Read canonical <code>.mmd</code> files from the configured <code>media/mermaid/</code> source location.</li>\n<li>Render each source to same-basename SVG directly in the configured published <code>svg</code> location.</li>\n<li>Support targeted rendering for <strong>Add image…</strong> and Mermaid outputs referenced by <code>--only-doc-ids</code>, plus complete rendering during a full Docs build, through the same producer.</li>\n<li>Extend Docs Watcher beyond Markdown documents to configured Mermaid source and run only the changed diagram identity on save.</li>\n<li>Render with the neutral theme, white background, strict security, <code>htmlLabels: false</code>, and an explicit 1200 by 800 browser viewport. Preserve the SVG <code>viewBox</code> and do not add Markdown dimensions by default.</li>\n<li>Require useful Mermaid <code>accTitle</code> and <code>accDescr</code> source plus useful Markdown alt text. Disabling HTML labels keeps generated text in ordinary SVG elements that survive the shared sanitizer.</li>\n<li>Report invalid source or a failed render as a build failure rather than silently retaining or accepting an unrelated SVG.</li>\n</ul>\n<p>The <a href=\"https://github.com/mermaid-js/mermaid-cli\">Mermaid CLI</a> is the upstream command authority. The pinned repository version, producer, and render configuration become the project authority.</p>\n<h2>Reader Boundary</h2>\n<p>Readers receive static SVG. Mermaid JavaScript, Node, npm, Puppeteer, source files, and build commands do not ship to Docs Viewer routes or the public site.</p>\n<h2>Suggested Repository Convention</h2>\n<p>Use the existing configured source and published-media roots:</p>\n<pre><code class=\"language-text\">&lt;scope source root&gt;/media/mermaid/&lt;diagram-name&gt;.mmd\n&lt;configured published svg root&gt;/&lt;diagram-name&gt;.svg\n</code></pre>\n<ul>\n<li>For a repo-backed scope the source maps to <code>docs-viewer/source/&lt;scope&gt;/media/mermaid/</code>.</li>\n<li>The producer writes the SVG to the scope's configured <code>svg</code> location. Current public scopes keep this class in the repository even when raster or download media uses R2.</li>\n</ul>\n<p>Markdown uses the same logical token in every scope: <code>[[media:docs/&lt;scope&gt;/svg/&lt;diagram-name&gt;.svg]]</code>.</p>\n<p>Additional conventions:</p>\n<ul>\n<li>use lowercase kebab-case names describing the diagram, not the rendering tool;</li>\n<li>keep one <code>.mmd</code> source and one <code>.svg</code> output with the same basename;</li>\n<li>do not edit the generated SVG by hand;</li>\n<li>render with the repository's explicit background, theme, dimensions, and accessibility options;</li>\n<li>include useful surrounding prose and alt text; the SVG is not the sole statement of the document's meaning;</li>\n<li>if two scopes need the same diagram, each scope owns its own <code>.mmd</code> source and derived SVG rather than depending on another scope's media.</li>\n</ul>\n<h2>Invariants</h2>\n<ul>\n<li>Markdown remains readable and editable without a graphical renderer.</li>\n<li>Every canonical <code>.mmd</code> source has one build path to its derived SVG.</li>\n<li>A broken diagram fails visibly at the media-producer boundary.</li>\n<li>Ordinary documents do not load a visualisation dependency they do not use.</li>\n<li>Public routes receive no build-time renderer or source file by accident.</li>\n<li>The supported tool version and material render options are pinned, making generated results reproducible enough to review through source control and the publication gate.</li>\n</ul>\n<h2>Shipped Proof And Boundaries</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260512-142027-54b354\">Configuration And Extension Points</a> contains the source/configuration/build/published proof diagram.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260424-000000-fc8b5d\">Docs Import</a> contains the document-intake, embedded-media, and source-editor-media proof diagram.</li>\n<li>A full Docs build initially renders every configured Mermaid source. Targeted document builds, staged intake, and Docs Watcher render only the relevant diagram identities. No fingerprint or render cache is added until measured volume justifies it.</li>\n<li>Rendered Document Info lists each Mermaid-backed SVG as a manage-only diagram-title link that opens its source in VS Code. Markdown source mode retains the semantic-token picker, and Mermaid editing is not embedded in Docs Viewer.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260718-214206-27df44\">Mermaid Document Diagrams Delivery</a> retains the implementation and verification record.</li>\n</ul>\n"
}
