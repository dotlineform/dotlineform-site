{
  "doc_id": "d-20260720-174842-4c2f6a",
  "title": "DSO-0 — Behavior And Dependency Findings",
  "added_date": "2026-07-20 17:48:42",
  "last_updated": "2026-07-20 18:14:09",
  "viewer_url": "/docs/?scope=studio&doc=d-20260720-174842-4c2f6a",
  "parent_id": "d-20260718-155350-c84e62",
  "summary": "Freeze the current document-package behavior, dependency graph, retirement boundary, atomic-return gaps, and proposed Docs Viewer route surface before implementation.",
  "ui_status": "completed",
  "content_html": "<h1>DSO-0 — Behavior And Dependency Findings</h1>\n<h2>Review Gate</h2>\n<p>This is the temporary audit record for <a href=\"/docs/?scope=studio&amp;doc=d-20260720-172819-93d6b2\">Document Sharing Ownership Cutover Delivery</a>. It records the accepted DSO-0 findings; code and runtime configuration remain unchanged.</p>\n<p>The target surface and contract corrections were accepted on 2026-07-20. The cutover has no discovery blocker; DSO-1 may begin when separately requested.</p>\n<h2>Headline Findings</h2>\n<ul>\n<li>The live document capability has two prepare profiles: <code>document-content</code> and export-only <code>document-tree</code>. <code>document-full-source</code> is not enabled and remains a potential later feature owned by Docs Viewer, outside this cutover.</li>\n<li>Most document behavior already lives in Docs Viewer services. Analytics supplies the route shell/API gateway, while <code>data-sharing/</code> supplies generic dispatch, a thin documents adapter, tag behavior, workspace paths, and returned metadata lookup.</li>\n<li>Trusted export metadata already stores <code>selected_doc_ids</code>, so complete-set validation can be added without changing the external package format or workspace location.</li>\n<li>Current returned parsing does not compare the returned identity set with <code>selected_doc_ids</code>. Missing and extra records are not detected as a package-level mismatch; duplicate and malformed identities are warnings or skipped rows in some paths.</li>\n<li>The current browser renders a returned-row selector, but its review and apply requests omit <code>record_indices</code>. The visible selection therefore does not control those actions; they operate on all parsed rows.</li>\n<li>Docs Import, Docs Review, management capability reporting, and staged-media services also import the generic Data Sharing workspace helper. That helper must move and be retargeted before <code>data-sharing/</code> can be deleted.</li>\n<li>The Data Sharing schema directory is empty apart from <code>.gitkeep</code>, and <code>services/package_io.py</code> only exposes its unused path. There is no generic schema implementation to migrate.</li>\n</ul>\n<h2>Current Prepare Contract</h2>\n<table>\n<thead>\n<tr>\n<th>Profile</th>\n<th>Current behavior</th>\n<th>Return path</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>document-content</code></td>\n<td>JSONL by default; JSON supported. Exports document identity, relationships, summaries, headings, and rendered-derived Markdown or plain text. Content is capped at 50,000 characters per document with paragraph-boundary truncation.</td>\n<td>Supported</td>\n</tr>\n<tr>\n<td><code>document-tree</code></td>\n<td>JSON-only nested document IDs and titles for the selected subtree.</td>\n<td>Export-only</td>\n</tr>\n</tbody>\n</table>\n<p>Both profiles use explicit document IDs, declare descendant and non-viewable inclusion, preserve source order, and reject unknown selected IDs. The current engine expands descendants automatically for <code>document-tree</code>; <code>document-content</code> exports the explicit IDs supplied by the route. The route also supports select-all. <code>document-content</code> is compact rendered-derived content; it is not source-faithful Markdown.</p>\n<p>The prepare result reports <code>ok</code>, dry-run/write state, <code>export_id</code>, scope/profile/format, output paths, selected/exported IDs, counts, warnings/errors, and whether output was written.</p>\n<h2>Package Identity And Workspace</h2>\n<p>The initial cutover must preserve these physical paths:</p>\n<table>\n<thead>\n<tr>\n<th>Artifact</th>\n<th>Current and retained path</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Outbound package and context sidecar</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/data-sharing/exports/</code></td>\n</tr>\n<tr>\n<td>Manually returned package staging</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/data-sharing/import-staging/</code></td>\n</tr>\n<tr>\n<td>Markdown review evidence and validated Docs Review projections</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/data-sharing/import-preview/</code></td>\n</tr>\n<tr>\n<td>Trusted internal export metadata</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/data-sharing/meta/</code></td>\n</tr>\n</tbody>\n</table>\n<p>The initial cutover must also preserve current serialized identifiers even where they retain Data Sharing terminology:</p>\n<ul>\n<li>export IDs: <code>ds_YYYYMMDDTHHMMSSZ</code>;</li>\n<li>returned package schema: <code>data_sharing_returned_package_v1</code>;</li>\n<li>internal metadata schema: <code>data_sharing_export_meta_v1</code>;</li>\n<li>JSONL routing row: <code>record_type: data_sharing_header</code>.</li>\n</ul>\n<p>JSON packages carry an envelope with <code>export_id</code> and <code>records</code>. JSONL packages carry the routing row first. Prepare also writes an external context sidecar and a trusted internal metadata sidecar. The internal sidecar records profile, scope, format, return eligibility, selected IDs, source update values, counts, and config checksum.</p>\n<h2>Current Returned Workflow</h2>\n<ol>\n<li>The service lists <code>.json</code> and <code>.jsonl</code> files under <code>import-staging/</code>, excludes sidecars, resolves <code>export_id</code>, and requires matching trusted internal metadata.</li>\n<li>Export-only or unknown profiles are blocked before becoming actionable.</li>\n<li>Inspect parses all records, normalizes document fields, annotates current source state, and returns counts, issues, records, and review rows.</li>\n<li>The current review menu offers:\n<ul>\n<li><strong>Content</strong> — validates and atomically publishes an immutable text-only package for Docs Review;</li>\n<li><strong>Summaries</strong> — writes Markdown review evidence under <code>import-preview/</code>;</li>\n<li><strong>Hierarchy</strong> — writes the same form of Markdown review evidence with hierarchy-oriented status copy.</li>\n</ul>\n</li>\n<li>Apply offers summary or hierarchy preflight, confirmation, source writes, and targeted Docs Viewer rebuilds.</li>\n<li>Docs Import separately consumes supported staged document packages through its collection planning/apply boundary.</li>\n</ol>\n<p>Summary and hierarchy apply accept <code>record_indices</code>; omission means all records. The browser omits the field. Content publication intentionally validates the full staged file. Current narrow apply treats unchanged or missing candidate fields as skips/no-ops and prevents writes when it finds a missing canonical target, but duplicate or malformed rows can be skipped while otherwise valid rows remain actionable.</p>\n<h2>Atomic Return Correction</h2>\n<p>The accepted product contract is stricter than current behavior. DSO-1 must add one shared package-level validator before inspect, review, publication, apply, or Docs Import handoff can become actionable.</p>\n<p>The validator should require:</p>\n<ul>\n<li>trusted metadata for a return-enabled profile;</li>\n<li>a non-empty <code>selected_doc_ids</code> list in that metadata;</li>\n<li>exactly one base-valid returned record for every prepared document ID;</li>\n<li>no returned ID outside the prepared set;</li>\n<li>no duplicate or missing document ID;</li>\n<li>valid package format, profile, scope, routing identity, and required base fields.</li>\n</ul>\n<p>Prepared and returned ID <strong>sets</strong> must match; row order is not identity and may change. Optional action fields may remain sparse: an omitted supported field or an unchanged value is a valid no-op, while a present invalid value blocks that action for the whole package. The cutover should not invent a universal source-freshness policy; existing action-specific current-source checks remain authoritative.</p>\n<p><code>record_indices</code>, selected-record reports, and <code>selectable</code> review-row semantics should be removed from browser requests, service requests, configuration, results, and tests. Inspection can still return ordered rows and row diagnostics, but the only actionable unit is the package.</p>\n<p>The external guidance for <code>document-content</code> should explicitly require one returned record per prepared ID. Its existing sparse-field guidance can remain.</p>\n<h2>Current Dependency Trace</h2>\n<pre><code class=\"language-text\">Analytics prepare/returned route shells\n  -&gt; Analytics config, transport, route state, modal, activity, CSS\n  -&gt; /analytics/api/data-sharing/*\n  -&gt; Analytics Data Sharing API and gateway\n  -&gt; generic registry + workflow dispatch\n  -&gt; thin documents adapter\n  -&gt; Docs Viewer export/returned/package services\n  -&gt; Docs Viewer source, write/rebuild, Docs Import, and Docs Review owners\n\nShared path + metadata helpers\n  -&gt; document packages\n  -&gt; Docs Import and staged media\n  -&gt; Docs Review sessions/packages\n  -&gt; Docs Viewer management capability reporting\n</code></pre>\n<p>The target removes the middle three generic layers:</p>\n<pre><code class=\"language-text\">Docs Viewer package routes\n  -&gt; fixed Docs Viewer package client\n  -&gt; direct document-package service\n  -&gt; existing Docs Viewer, Docs Import, and Docs Review owners\n</code></pre>\n<h2>Move Or Absorb</h2>\n<table>\n<thead>\n<tr>\n<th>Current owner</th>\n<th>DSO action</th>\n<th>Target owner</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>data-sharing/adapters/documents/config/prepare-profiles.json</code></td>\n<td>Move without adding profiles</td>\n<td><code>docs-viewer/config/document-packages/profiles.json</code></td>\n</tr>\n<tr>\n<td>Thin documents adapter wrappers and family dispatcher</td>\n<td>Absorb behavior; do not preserve adapter abstractions</td>\n<td><code>docs-viewer/services/docs_document_packages/</code></td>\n</tr>\n<tr>\n<td><code>docs-viewer/services/docs_data_sharing/</code>, <code>docs_export*</code>, and <code>docs_returned_import*</code></td>\n<td>Re-home and remove adapter/subset terminology</td>\n<td><code>docs-viewer/services/docs_document_packages/</code></td>\n</tr>\n<tr>\n<td><code>data-sharing/services/paths.py</code></td>\n<td>Move first and retarget all Docs Viewer/Import/Review consumers</td>\n<td><code>docs_document_packages.workspace</code></td>\n</tr>\n<tr>\n<td><code>data-sharing/services/returned_metadata.py</code></td>\n<td>Move and make document-specific</td>\n<td><code>docs_document_packages.metadata</code></td>\n</tr>\n<tr>\n<td><code>docs_import_data_sharing_*</code> and the compact/full-source content normalizer</td>\n<td>Keep under Docs Import, rename package boundary, and call the Docs Viewer metadata/parser owner</td>\n<td>Docs Import document-package modules</td>\n</tr>\n<tr>\n<td>Docs Review package/session services</td>\n<td>Keep in place; retarget workspace imports only</td>\n<td>Existing Docs Review owners</td>\n</tr>\n<tr>\n<td>Analytics document browser modules and route HTML</td>\n<td>Move useful document-only behavior, remove domain/app/row-selection state, and replace Analytics dependencies</td>\n<td><code>docs-viewer/runtime/js/packages/</code> and Docs Viewer route shells</td>\n</tr>\n<tr>\n<td>Data Sharing CSS in <code>analytics.css</code></td>\n<td>Extract only the required route presentation</td>\n<td>Docs Viewer-owned package CSS</td>\n</tr>\n<tr>\n<td>Document activity emission</td>\n<td>Retarget route/endpoint context and log source</td>\n<td>Docs Viewer document-package service</td>\n</tr>\n</tbody>\n</table>\n<p>This is a final integrated-only Docs Viewer boundary. Do not create <code>docs-viewer/data-sharing/</code>, adapter aliases, or an Analytics proxy.</p>\n<h2>Retire Rather Than Move</h2>\n<ul>\n<li>all <code>data-sharing/adapters/tags/</code> code, tag profiles, returned parsing/apply, and tag package tests;</li>\n<li>the tag records in <code>data-sharing/config/adapters.json</code>;</li>\n<li>generic registry, registry schema, dispatch, and workflow modules under <code>data-sharing/services/</code> and <code>data-sharing/workflows/</code>;</li>\n<li>unused <code>data-sharing/services/package_io.py</code> and the empty <code>data-sharing/schemas/</code> directory;</li>\n<li>Analytics Data Sharing server gateway, resolver, endpoint composition, and neutral route constants;</li>\n<li>Analytics app/domain selection, generic capability projection, tag-specific browser branches, returned-row selection UI, and Data Sharing route shells after the document UI has moved;</li>\n<li>Analytics home/config links, route checks, smoke coverage, runner labels, and active activity-contract entries that describe the retired routes;</li>\n<li>current generic/tag tests after equivalent document contracts are protected under Docs Viewer.</li>\n</ul>\n<p>Retiring active route/activity configuration does not require deleting historical activity records. Any display metadata still needed to render old records should be classified as historical data, not left as an active route or capability.</p>\n<h2>Proposed Frozen Docs Viewer Surface</h2>\n<h3>Browser Routes</h3>\n<table>\n<thead>\n<tr>\n<th>Workflow</th>\n<th>Route</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Prepare</td>\n<td><code>/docs/packages/prepare/</code></td>\n</tr>\n<tr>\n<td>Returned Packages</td>\n<td><code>/docs/packages/returned/</code></td>\n</tr>\n</tbody>\n</table>\n<h3>Same-Origin Service Endpoints</h3>\n<table>\n<thead>\n<tr>\n<th>Method</th>\n<th>Endpoint</th>\n<th>Responsibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>GET</code></td>\n<td><code>/docs/packages/config</code></td>\n<td>Fixed browser-safe scopes, profiles, actions, and workspace availability</td>\n</tr>\n<tr>\n<td><code>GET</code></td>\n<td><code>/docs/packages/documents</code></td>\n<td>Selectable documents for one <code>scope</code></td>\n</tr>\n<tr>\n<td><code>GET</code></td>\n<td><code>/docs/packages/returned</code></td>\n<td>Actionable and blocked document packages from staging</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/docs/packages/prepare</code></td>\n<td>Dry-run or write one selected document package</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/docs/packages/context</code></td>\n<td>Validate or save external profile context</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/docs/packages/returned/inspect</code></td>\n<td>Parse and validate the complete returned package for inspection</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/docs/packages/returned/review</code></td>\n<td>Produce current Content, Summaries, or Hierarchy review result for the complete package</td>\n</tr>\n<tr>\n<td><code>POST</code></td>\n<td><code>/docs/packages/returned/apply</code></td>\n<td>Preflight or confirm one supported whole-package apply action</td>\n</tr>\n</tbody>\n</table>\n<p>Do not add a package-specific health endpoint. The existing <code>/health</code> and <code>/capabilities</code> endpoints should report service and <code>document_packages</code> availability.</p>\n<p>Requests should use direct document terms: <code>scope</code>, <code>doc_ids</code>, <code>select_all</code>, <code>profile_id</code>, <code>target_format</code>, <code>content_format</code>, <code>staged_filename</code>, <code>review_action</code>, <code>apply_action</code>, <code>dry_run</code>, and <code>confirm</code>. They should not accept <code>app</code>, <code>data_domain</code>, <code>adapter_id</code>, generic <code>operation</code>, nested adapter selection, or <code>record_indices</code>.</p>\n<p>The old Analytics pages and APIs are removed at cutover with no redirect, compatibility endpoint, or dual route.</p>\n<h2>Fixture And Baseline Evidence</h2>\n<p>There is no checked-in canonical document-package fixture corpus. Current Python tests synthesize source documents, JSON/JSONL packages, and trusted metadata in temporary workspaces. The Analytics browser smoke stubs the Data Sharing API rather than exercising a real returned package through the full service.</p>\n<p>DSO-0 baseline on 2026-07-20:</p>\n<ul>\n<li>67 focused Docs Viewer export, returned parsing, review publication, summary/hierarchy apply, and package-content tests passed;</li>\n<li>39 Analytics API, generic registry/dispatch, adapter, and tag prepare tests passed;</li>\n<li>tests ran with isolated writable <code>DOTLINEFORM_PROJECTS_BASE_DIR</code> roots.</li>\n</ul>\n<p>DSO-1/DSO-4 should establish a small document-owned fixture matrix covering:</p>\n<ul>\n<li><code>document-content</code> JSONL and JSON plus matching metadata/context;</li>\n<li>export-only <code>document-tree</code>;</li>\n<li>valid exact-set return with reordered rows;</li>\n<li>missing, extra, duplicate, malformed, or mismatched records blocking the package;</li>\n<li>sparse valid action fields and unchanged no-ops;</li>\n<li>Docs Review publication and summary/hierarchy preflight/confirmed apply.</li>\n</ul>\n<h2>Accepted Decisions</h2>\n<p>The following freezes were accepted on 2026-07-20:</p>\n<ol>\n<li>accept <code>/docs/packages/prepare/</code> and <code>/docs/packages/returned/</code> as the two browser routes;</li>\n<li>accept the eight direct endpoints above and the absence of a package health endpoint;</li>\n<li>accept exact set equality with order-insensitive identity and action-specific sparse fields;</li>\n<li>accept <code>docs-viewer/config/document-packages/</code> and <code>docs-viewer/services/docs_document_packages/</code> as the final integrated-only owners;</li>\n<li>accept creation of a small document-owned package fixture matrix during implementation;</li>\n<li>keep source-freshness handling action-specific during this cutover.</li>\n</ol>\n<p><code>document-full-source</code> remains a potential later Docs Viewer feature. Existing lower-level normalization does not make it a live profile, and DSO does not move or enable it.</p>\n<p>No DSO-1 implementation has started.</p>\n"
}
