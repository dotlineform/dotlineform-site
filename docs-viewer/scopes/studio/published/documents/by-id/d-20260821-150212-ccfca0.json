{
  "doc_id": "d-20260821-150212-ccfca0",
  "title": "iPad Docs Viewer — Installation Proof Results",
  "added_date": "2026-08-21 15:02:12",
  "last_updated": "2026-08-21 15:02:12",
  "viewer_url": "/docs/?scope=studio&doc=d-20260821-150212-ccfca0",
  "parent_id": "d-20260808-205414-91040a",
  "content_html": "<h1>iPad Docs Viewer — Installation Proof Results</h1>\n<p><strong>Test date:</strong> 20 August 2026<br />\n<strong>Source procedure:</strong> <code>d-20260808-205416-9f6d6c</code> — <em>iPad Docs Viewer Installation And Proof</em><br />\n<strong>Target:</strong> iPad + iSH + Safari, using the dotlineform repository and iCloud-backed external workspaces.</p>\n<h2>Overall result so far</h2>\n<p>The core iPad working slice is viable, with important platform constraints and a few installation/runtime deviations from the original procedure.</p>\n<p>Confirmed working:</p>\n<ul>\n<li>iSH can mount the real iCloud <code>dotlineform</code> projects base and access its expected children.</li>\n<li>The mount point can be named <code>/mnt/dotlineform</code>, matching the real folder name.</li>\n<li>The iOS-backed mount persisted across iSH relaunches during this test.</li>\n<li>Git can clone the repository into iSH local storage and create the disposable <code>ipad-proof</code> branch.</li>\n<li>A Python virtual environment can be created and the required Docs Viewer Python imports can be installed under the stock iSH Alpine environment.</li>\n<li>Docs Viewer starts successfully and <code>/health</code> reports management, generated reads, and review enabled.</li>\n<li>The full Docs Viewer Manage UI loads in Safari.</li>\n<li>Repository-backed documents load.</li>\n<li>A configured <code>local_external</code> scope loads from the mounted iCloud workspace.</li>\n<li>The Edit Metadata modal opens.</li>\n<li>A metadata change (<code>ui_status</code>) was successfully saved, although with noticeable latency.</li>\n<li>Safari and iSH can be used side-by-side; with iSH kept visible, Docs Viewer continues responding without repeated restarts.</li>\n</ul>\n<p>Important constraints:</p>\n<ul>\n<li>Stock iSH currently uses Alpine 3.14.3, not Alpine 3.23.</li>\n<li>Stock Alpine 3.14 provides Python 3.9.16, not Python 3.12.</li>\n<li><code>py3-markdown-it-py</code> is not available from the pinned Alpine 3.14 repositories.</li>\n<li><code>bleach==6.3.0</code> is not compatible with Python 3.9, so the proof used <code>bleach==6.2.0</code>.</li>\n<li><code>.env.local</code> is not applied early enough for external workspace resolution during service imports; the file must currently be exported into the shell before starting Docs Viewer.</li>\n<li>iSH must remain visible/active while Safari is making localhost requests. Full-screen Safari commonly leaves new Docs Viewer requests stalled until iSH becomes visible again.</li>\n<li>In one observed longer/deeper background interval, the Python process survived but the HTTP listener on port 8776 was lost. That instance required a service restart.</li>\n</ul>\n<h2>1. iSH runtime baseline</h2>\n<p>Observed:</p>\n<pre><code class=\"language-text\">Alpine: 3.14.3\nPython: 3.9.16\nGit: 2.32.7\n</code></pre>\n<p>Pinned repositories:</p>\n<pre><code class=\"language-text\">http://apk.ish.app/v3.14-2023-05-19/main\nhttp://apk.ish.app/v3.14-2023-05-19/community\n</code></pre>\n<p>This means the original document assumption that Alpine 3.23 / Python 3.12 would be the initial environment was not true for a fresh iSH installation.</p>\n<p>The stock iSH environment was therefore used as the proof baseline instead of upgrading Alpine.</p>\n<h2>2. External projects mount</h2>\n<p>The real iCloud projects base is named <code>dotlineform</code>, so the Linux-side mount was changed from the document's proposed <code>/mnt/dlf-projects</code> to:</p>\n<pre><code class=\"language-sh\">mkdir -p /mnt/dotlineform\nmount -t ios . /mnt/dotlineform\n</code></pre>\n<p>The folder picker was used to select the real iCloud <code>dotlineform</code> folder.</p>\n<p>Expected/confirmed shape:</p>\n<pre><code class=\"language-text\">/mnt/dotlineform/\n├── docs-viewer/\n├── data-sharing/\n└── projects/\n</code></pre>\n<p>Verification:</p>\n<pre><code class=\"language-sh\">test -d /mnt/dotlineform/docs-viewer\ntest -d /mnt/dotlineform/data-sharing\ntest -d /mnt/dotlineform/projects\nfind /mnt/dotlineform -maxdepth 1 -type d -print\n</code></pre>\n<p>During this test, the mount persisted across iSH relaunches and did not need to be manually recreated.</p>\n<p>If it is ever absent after a restart, remount with:</p>\n<pre><code class=\"language-sh\">mount -t ios . /mnt/dotlineform\n</code></pre>\n<p>and select the same iCloud folder.</p>\n<h2>3. Alpine packages</h2>\n<p>The original package list included:</p>\n<pre><code class=\"language-text\">py3-markdown-it-py\n</code></pre>\n<p>which does not exist in the pinned Alpine 3.14 repositories.</p>\n<p>The working Alpine package install was therefore:</p>\n<pre><code class=\"language-sh\">apk update\n\napk add bash git ca-certificates python3 py3-pip py3-virtualenv   py3-lxml py3-beautifulsoup4 py3-pillow\n</code></pre>\n<p>The first install attempt suffered network failures for some packages, including <code>expat</code>, <code>nghttp2-libs</code>, and <code>libcurl</code>.</p>\n<p>Most requested packages nevertheless survived the interruption.</p>\n<p>The missing runtime dependency was repaired with:</p>\n<pre><code class=\"language-sh\">apk add expat\n</code></pre>\n<p>which also installed the remaining missing network libraries.</p>\n<p>Final result:</p>\n<pre><code class=\"language-text\">OK: 118 MiB in 84 packages\n</code></pre>\n<h2>4. Python virtual environment</h2>\n<p>Initial virtualenv creation failed because Python's <code>pyexpat</code> extension could not load:</p>\n<pre><code class=\"language-text\">ImportError: Error loading shared library libexpat.so.1\n</code></pre>\n<p>After installing <code>expat</code>, virtualenv creation succeeded:</p>\n<pre><code class=\"language-sh\">mkdir -p /root/venvs\n\nvirtualenv --system-site-packages /root/venvs/dotlineform\n</code></pre>\n<p>Activate with:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\n</code></pre>\n<p>Observed:</p>\n<pre><code class=\"language-text\">Python 3.9.16\npip 21.0.1\n</code></pre>\n<p>The pip upgrade warning was ignored for the proof.</p>\n<h2>5. Python package compatibility changes</h2>\n<p>Because Alpine 3.14 does not provide <code>py3-markdown-it-py</code>, <code>markdown-it-py</code> was installed with pip.</p>\n<p>Because Python 3.9 cannot use <code>bleach==6.3.0</code>, the proof used Bleach 6.2.0.</p>\n<p>Working command:</p>\n<pre><code class=\"language-sh\">python3 -m pip install   mammoth==1.12.0   bleach==6.2.0   openpyxl==3.1.5   markdown-it-py==3.0.0\n\npython3 -m pip check\n</code></pre>\n<p>Installed pip-layer versions:</p>\n<pre><code class=\"language-text\">mammoth        1.12.0\nbleach         6.2.0\nopenpyxl       3.1.5\nmarkdown-it-py 3.0.0\n</code></pre>\n<p>System Alpine packages supplied the native dependencies such as lxml and Pillow.</p>\n<h2>6. Git checkout</h2>\n<p>The repository was kept in iSH local Linux storage rather than iCloud:</p>\n<pre><code class=\"language-sh\">mkdir -p /root/src\ncd /root/src\n\ngit clone --depth 1 https://github.com/dotlineform/dotlineform-site.git\ncd dotlineform-site\n\ngit status --short\ngit switch -c ipad-proof\n</code></pre>\n<p>Results:</p>\n<ul>\n<li>shallow clone succeeded;</li>\n<li>initial <code>git status --short</code> was clean;</li>\n<li>disposable <code>ipad-proof</code> branch was created successfully.</li>\n</ul>\n<h2>7. Device-local environment</h2>\n<p><code>.env.local</code> was created in the iSH clone:</p>\n<pre><code class=\"language-text\">/root/src/dotlineform-site/.env.local\n</code></pre>\n<p>Contents:</p>\n<pre><code class=\"language-sh\">DOTLINEFORM_PROJECTS_BASE_DIR=/mnt/dotlineform\nDOCS_VIEWER_MANAGEMENT_ENABLED=1\nDOCS_VIEWER_REVIEW_ENABLED=1\n</code></pre>\n<p>Verification passed:</p>\n<pre><code class=\"language-sh\">test -f .env.local\ntest -d /mnt/dotlineform/docs-viewer\ngit status --short\n</code></pre>\n<p><code>.env.local</code> remained ignored by Git.</p>\n<h3>Current startup-order issue</h3>\n<p>Starting Docs Viewer directly initially failed with:</p>\n<pre><code class=\"language-text\">ValueError: DOTLINEFORM_PROJECTS_BASE_DIR is required for external local Docs Viewer scopes\n</code></pre>\n<p>The file existed, but the external workspace resolver was being imported before <code>.env.local</code> had been applied to <code>os.environ</code>.</p>\n<p>Current proof workaround:</p>\n<pre><code class=\"language-sh\">cd /root/src/dotlineform-site\n\nset -a\n. ./.env.local\nset +a\n</code></pre>\n<p>Confirm:</p>\n<pre><code class=\"language-sh\">printf '%s\n' &quot;$DOTLINEFORM_PROJECTS_BASE_DIR&quot;\n</code></pre>\n<p>Expected:</p>\n<pre><code class=\"language-text\">/mnt/dotlineform\n</code></pre>\n<p>This should be treated as a candidate repository fix rather than a permanent manual requirement.</p>\n<h2>8. Starting Docs Viewer</h2>\n<p>After reopening iSH, shell-session state must be recreated:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\ncd /root/src/dotlineform-site\n\nset -a\n. ./.env.local\nset +a\n\npython3 docs-viewer/services/docs_viewer_service.py\n</code></pre>\n<p>Successful health response:</p>\n<pre><code class=\"language-json\">{\n  &quot;generated_reads_enabled&quot;: true,\n  &quot;management_enabled&quot;: true,\n  &quot;ok&quot;: true,\n  &quot;review_enabled&quot;: true,\n  &quot;service&quot;: &quot;docs_viewer&quot;\n}\n</code></pre>\n<p>Safari URLs:</p>\n<pre><code class=\"language-text\">http://127.0.0.1:8776/health\nhttp://127.0.0.1:8776/docs/\n</code></pre>\n<p>The full Docs Viewer UI loaded successfully.</p>\n<p>Initial index/document loading could take roughly a minute. Later loads were often faster.</p>\n<h2>9. Docs Viewer functionality tested</h2>\n<p>Confirmed:</p>\n<ul>\n<li>repository-backed Docs Viewer index loaded;</li>\n<li>repository-backed documents loaded;</li>\n<li>configured local-external scope loaded;</li>\n<li>Edit Metadata modal opened;</li>\n<li><code>ui_status</code> metadata was changed and saved successfully;</li>\n<li>the generated UI refreshed after the write;</li>\n<li>Safari and iSH could be used together in the iPad windowed/multitasking layout.</li>\n</ul>\n<p>Not yet fully proven:</p>\n<ul>\n<li>formal search/read gate across all required scopes;</li>\n<li>full repository-backed create/delete proof;</li>\n<li>full external create/restart/Mac-sync/delete proof;</li>\n<li>import/review/media proof;</li>\n<li>optional static-site preview;</li>\n<li>complete lifecycle recovery matrix.</li>\n</ul>\n<h2>10. iSH focus and cursor behaviour</h2>\n<p>A hollow iSH caret by itself is not evidence that the shell has frozen.</p>\n<p>Observed behaviour:</p>\n<ul>\n<li>when iSH is visible but does not have keyboard focus, the caret becomes hollow;</li>\n<li>when iSH regains focus, the caret normally becomes solid again;</li>\n<li>while this normal focus transition occurs, no restart is needed.</li>\n</ul>\n<p>Treat the state as a real failure only if iSH regains focus but keyboard interaction still does not recover.</p>\n<h2>11. Safari full-screen versus iSH-visible operation</h2>\n<p>This was the most important platform finding.</p>\n<h3>Working mode</h3>\n<p>With Safari and iSH visible side-by-side:</p>\n<pre><code class=\"language-text\">iSH visible\n→ Python service continues running\n→ Safari localhost requests complete\n→ document navigation and managed actions work\n</code></pre>\n<h3>Full-screen Safari</h3>\n<p>When Safari becomes full screen and iSH is no longer visible:</p>\n<pre><code class=\"language-text\">Safari full screen\n→ iSH is suspended or heavily deprioritised\n→ existing Safari content remains visible\n→ new localhost requests may stall\n→ making iSH visible again allows requests to continue\n</code></pre>\n<p>This means the practical working mode is currently:</p>\n<pre><code class=\"language-text\">Safari + iSH side-by-side\n</code></pre>\n<p>rather than relying on a hidden/background iSH process.</p>\n<p>A useful working rhythm is:</p>\n<ol>\n<li>keep Safari and iSH windowed while navigating/editing;</li>\n<li>make Safari full screen only after the required document has already loaded;</li>\n<li>restore the split/windowed layout before requesting a different document or performing a managed action.</li>\n</ol>\n<h2>12. Lost-listener failure</h2>\n<p>One deeper lifecycle failure was captured in detail.</p>\n<p>Safari <code>/health</code> stopped responding.</p>\n<p>From a second iSH window:</p>\n<pre><code class=\"language-sh\">wget -qO- http://127.0.0.1:8776/health\n</code></pre>\n<p>returned:</p>\n<pre><code class=\"language-text\">Connection refused\n</code></pre>\n<p>But:</p>\n<pre><code class=\"language-sh\">ps | grep '[d]ocs_viewer_service.py'\n</code></pre>\n<p>still showed the Python service process.</p>\n<p>Process state:</p>\n<pre><code class=\"language-text\">42 RW   python3 docs-viewer/services/docs_viewer_service.py\n</code></pre>\n<p><code>netstat</code> was not useful under iSH because <code>/proc/net/tcp</code> and <code>/proc/net/tcp6</code> were unavailable.</p>\n<p><code>/proc/42/fd</code> showed:</p>\n<pre><code class=\"language-text\">0 -&gt; /dev/pts/0\n1 -&gt; /dev/pts/0\n2 -&gt; /dev/pts/0\n3 -&gt; anon_inode:[unknown]\n</code></pre>\n<p>The decisive test was whether another process could bind port 8776:</p>\n<pre><code class=\"language-sh\">python3 -c &quot;import socket; s=socket.socket(); s.bind(('127.0.0.1',8776)); print('port 8776 is free'); s.close()&quot;\n</code></pre>\n<p>Result:</p>\n<pre><code class=\"language-text\">port 8776 is free\n</code></pre>\n<p>Therefore:</p>\n<ul>\n<li>the original Python process still existed;</li>\n<li>it was no longer listening on port 8776;</li>\n<li>the HTTP listener had been released/lost;</li>\n<li>merely bringing iSH back into focus could not recover that instance;</li>\n<li>the Docs Viewer service had to be restarted.</li>\n</ul>\n<p>This is distinct from the ordinary short background-suspension behaviour, where requests merely wait and then continue when iSH becomes visible again.</p>\n<h2>13. BrokenPipeError</h2>\n<p>One server-side message was observed:</p>\n<pre><code class=\"language-text\">BrokenPipeError: [Errno 32] Broken pipe\n</code></pre>\n<p>This occurred while Safari was making/cancelling localhost requests.</p>\n<p>The Docs Viewer continued working afterwards.</p>\n<p>For the proof, this should be recorded but not treated as a failure unless it correlates with missing or failed user-visible operations.</p>\n<h2>14. Search and Recent observations</h2>\n<p>At one point Search and Recent immediately displayed:</p>\n<pre><code class=\"language-text\">Load failed\n</code></pre>\n<p>At the same time <code>/health</code> was no longer responding and the service listener was later confirmed lost.</p>\n<p>Therefore that specific failure was not established as a Search/Recent implementation bug; it occurred while the localhost service itself was unavailable.</p>\n<p>A separate UI issue was observed:</p>\n<ul>\n<li>the iPad Safari search field did not expose an obvious <code>×</code> clear button;</li>\n<li>the current UI relies on the browser-native behaviour of <code>input type=&quot;search&quot;</code>.</li>\n</ul>\n<p>An explicit application-level clear control may be preferable for iPad/mobile use.</p>\n<h2>15. Recovery commands</h2>\n<h3>A. Normal iSH reopen</h3>\n<p>If the mount has persisted:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\ncd /root/src/dotlineform-site\n\nset -a\n. ./.env.local\nset +a\n</code></pre>\n<p>Then start Docs Viewer:</p>\n<pre><code class=\"language-sh\">python3 docs-viewer/services/docs_viewer_service.py\n</code></pre>\n<h3>B. Check whether the iCloud mount is still available</h3>\n<pre><code class=\"language-sh\">test -d /mnt/dotlineform/docs-viewer\n</code></pre>\n<p>If that succeeds, no remount is required.</p>\n<p>If it fails:</p>\n<pre><code class=\"language-sh\">mount -t ios . /mnt/dotlineform\n</code></pre>\n<p>and select the real iCloud <code>dotlineform</code> folder again.</p>\n<h3>C. Check whether Docs Viewer is responding</h3>\n<p>From a second iSH window:</p>\n<pre><code class=\"language-sh\">wget -qO- http://127.0.0.1:8776/health\n</code></pre>\n<p>Expected: JSON with <code>&quot;ok&quot;: true</code>.</p>\n<h3>D. Check whether the service process still exists</h3>\n<pre><code class=\"language-sh\">ps | grep '[d]ocs_viewer_service.py'\n</code></pre>\n<h3>E. Distinguish &quot;temporarily suspended&quot; from &quot;listener lost&quot;</h3>\n<p>First make iSH visible again and retry:</p>\n<pre><code class=\"language-sh\">wget -qO- http://127.0.0.1:8776/health\n</code></pre>\n<p>If it responds after iSH becomes active, the service was only suspended/deprioritised.</p>\n<p>If it still returns <code>Connection refused</code>, check whether port 8776 is free:</p>\n<pre><code class=\"language-sh\">python3 -c &quot;import socket; s=socket.socket(); s.bind(('127.0.0.1',8776)); print('port 8776 is free'); s.close()&quot;\n</code></pre>\n<p>If the command prints:</p>\n<pre><code class=\"language-text\">port 8776 is free\n</code></pre>\n<p>while <code>ps</code> still shows <code>docs_viewer_service.py</code>, the server process is stale and has lost its listener.</p>\n<h3>F. Restart a stale Docs Viewer process</h3>\n<p>Find the PID:</p>\n<pre><code class=\"language-sh\">ps | grep '[d]ocs_viewer_service.py'\n</code></pre>\n<p>Kill it:</p>\n<pre><code class=\"language-sh\">kill &lt;PID&gt;\n</code></pre>\n<p>Confirm it has gone:</p>\n<pre><code class=\"language-sh\">ps | grep '[d]ocs_viewer_service.py'\n</code></pre>\n<p>Then restart:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\ncd /root/src/dotlineform-site\n\nset -a\n. ./.env.local\nset +a\n\npython3 docs-viewer/services/docs_viewer_service.py\n</code></pre>\n<p>Test from the second iSH window:</p>\n<pre><code class=\"language-sh\">wget -qO- http://127.0.0.1:8776/health\n</code></pre>\n<h2>16. Practical operating procedure</h2>\n<p>For the current proof environment:</p>\n<pre><code class=\"language-text\">1. Open iSH.\n2. Confirm /mnt/dotlineform is still available.\n3. Activate the dotlineform virtualenv.\n4. cd to /root/src/dotlineform-site.\n5. Export .env.local into the shell.\n6. Start docs_viewer_service.py.\n7. Open Safari beside iSH rather than hiding iSH.\n8. Use http://127.0.0.1:8776/docs/.\n9. If a request stalls, make sure iSH is visible.\n10. If requests return connection refused, check /health and the service process.\n11. If the process exists but port 8776 is free, kill and restart the service.\n</code></pre>\n<h2>17. Candidate follow-up changes</h2>\n<p>The proof has identified several bounded improvements worth considering after the test is complete:</p>\n<ol>\n<li>\n<p><strong>Early <code>.env.local</code> application</strong></p>\n<ul>\n<li>Ensure <code>DOTLINEFORM_PROJECTS_BASE_DIR</code> is available before modules resolve external workspaces.</li>\n</ul>\n</li>\n<li>\n<p><strong>Explicit iPad dependency profile</strong></p>\n<ul>\n<li>Document/test the stock iSH Alpine 3.14 / Python 3.9 path, or deliberately adopt a newer supported iSH filesystem if required.</li>\n<li>Current tested divergence:\n<ul>\n<li><code>bleach==6.2.0</code></li>\n<li><code>markdown-it-py==3.0.0</code> via pip</li>\n<li>native lxml/Pillow from Alpine.</li>\n</ul>\n</li>\n</ul>\n</li>\n<li>\n<p><strong>iPad launcher/helper</strong></p>\n<ul>\n<li>Reduce the repeated activation, directory, environment-export, and service-start commands once the proof stabilises.</li>\n</ul>\n</li>\n<li>\n<p><strong>Mobile search clear control</strong></p>\n<ul>\n<li>Add an explicit clear button instead of relying solely on Safari's native <code>type=&quot;search&quot;</code> affordance.</li>\n</ul>\n</li>\n<li>\n<p><strong>Responsive CSS fixes</strong></p>\n<ul>\n<li>Address the already-visible narrow-window layout issues in the Docs Viewer management UI.</li>\n</ul>\n</li>\n<li>\n<p><strong>Lifecycle documentation</strong></p>\n<ul>\n<li>State clearly that iSH must remain visible/active for reliable localhost service operation on iPad.</li>\n<li>Document the difference between recoverable suspension and the observed lost-listener state.</li>\n</ul>\n</li>\n</ol>\n<h2>Current assessment</h2>\n<p>The central feasibility claim has been demonstrated: Docs Viewer can run locally on the iPad through iSH, access repository-backed and iCloud-backed documents, and perform at least one real managed metadata write.</p>\n<p>The principal constraint is not basic Python/Git/File Provider compatibility. It is iPadOS lifecycle behaviour around a localhost server hosted by iSH.</p>\n<p>At present, the most reliable working mode is to keep iSH visible alongside Safari. Short suspension appears recoverable; a deeper suspension can leave the Python process alive without its HTTP listener, in which case the service must be restarted.</p>\n"
}
