{
  "doc_id": "d-20260331-154730-b019e0",
  "title": "Catalogue Media Pipeline Config",
  "added_date": "2026-03-31 15:47:30",
  "last_updated": "2026-08-13 12:01:22",
  "viewer_url": "/docs/?scope=studio&doc=d-20260331-154730-b019e0",
  "parent_id": "d-20260423-000000-d015e6",
  "content_html": "<h1>Catalogue Media Pipeline Config</h1>\n<h2>What It Owns</h2>\n<p><code>_data/pipeline.json</code> is the shared checked configuration for catalogue source-media roots, external staging subpaths, image variant naming, and encoding defaults.</p>\n<p>It exists because source discovery, derivative generation, public templates, R2 publication, and consistency checks must agree on the same path and variant policy.</p>\n<p>Main consumers load it through <code>studio/shared/python/pipeline_config.py</code>, including catalogue generation/build media, the standalone derivative builder, R2 publishing, project-media selection, and site consistency checks.</p>\n<h2>Configuration Flow</h2>\n<pre><code class=\"language-text\">_data/pipeline.json\n  -&gt; pipeline_config.py validates and exposes named values\n  -&gt; catalogue/media tools resolve paths below DOTLINEFORM_PROJECTS_BASE_DIR\n  -&gt; generators and public shells use matching variant conventions\n</code></pre>\n<p>The external <code>catalogue/media</code> staging root has no repo-local fallback. Environment variables provide machine-specific base locations and secrets; pipeline JSON provides checked relative policy.</p>\n<h2>Change Boundary</h2>\n<p>Keep here:</p>\n<ul>\n<li>relative source/staging/output subpaths shared across tools;</li>\n<li>primary and thumbnail widths, suffixes, and output directories;</li>\n<li>encoding format, codec, quality, preset, and compression defaults;</li>\n<li>environment variable names shared by pipeline helpers;</li>\n<li>configured workbook and source-root paths used by catalogue operations.</li>\n</ul>\n<p>Do not put browser routes, UI text, credentials, or generated payload paths here when another registry owns them.</p>\n<h2>Derivative Builder</h2>\n<p><code>studio/services/media/make_srcset_images.py</code> converts staged source images into the configured primary and thumbnail variants. The normal Work and detail path is the scoped catalogue build, which resolves source images, calls this builder, copies public thumbnails, and reports readiness. Use the standalone command for explicit media maintenance or debugging, not as a second catalogue workflow:</p>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 studio/services/media/make_srcset_images.py INPUT_DIR OUTPUT_DIR JOBS\n</code></pre>\n<p>Omitting the directories uses the configured Work staging and output roots. <code>--dry-run</code> previews derivative writes and staged-source cleanup. Optional selected-ID and success-ID manifests plus the default worker count use names defined in <code>_data/pipeline.json</code>; run <code>--help</code> for the exact current argument and environment contract.</p>\n<p>The builder owns image decoding, scaling and cropping, encoding, variant filenames, parallel execution, and staged-source cleanup. It does not read or write canonical catalogue records, select publication scope, generate public JSON or search, copy final assets to R2, or promote confirmed media versions. Those responsibilities remain with catalogue build planning and <a href=\"/docs/?scope=studio&amp;doc=d-20260508-093841-3e27b1\">Publish Media To R2</a>.</p>\n<h2>Why Changes Are Broad</h2>\n<p>A variant-policy change can affect source staging, derivative filenames, public HTML attributes, R2 object expectations, cache behavior, audits, and regeneration requirements. Treat it as a pipeline contract change and verify all active consumers instead of updating only the image builder.</p>\n<p>The config file and <code>pipeline_config.py</code> are the exact authority for current values; this page deliberately does not copy the full JSON inventory.</p>\n"
}
