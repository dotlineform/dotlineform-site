{
  "doc_id": "d-20260729-151446-09489e",
  "title": "Tag Link Management Delivery",
  "added_date": "2026-07-29 15:14:46",
  "last_updated": "2026-07-29 20:56:21",
  "viewer_url": "/docs/?scope=studio&doc=d-20260729-151446-09489e",
  "parent_id": "d-20260726-211842-cc8f6c",
  "summary": "narrow New Tag preflight, migrate tags to canonical public document URLs, and deliver shared published-document search plus Tag Registry link management",
  "ui_status": "review",
  "content_html": "<h1>Tag Link Management Delivery</h1>\n<h2>Target Result</h2>\n<p>Tag Registry stores an ordered array of optional one-way canonical document\nlocations for each tag. Tag Registry and Edit Tag present current document\ntitles, Edit Tag can search published document titles and add a selected\nlocation through a reusable picker or remove an association, and the registry\nno longer stores or presents a tag <code>description</code>. Tag Editor searches published\nAnalysis locations only. Report context appears in those picker results when\npresent; scope context appears only for consumers configured with more than one\nscope.</p>\n<p>The relationship and independent-lifecycle boundary remains owned by\n<a href=\"/docs/?scope=studio&amp;doc=d-20260726-211842-cc8f6c\">Tag-document linking</a>.</p>\n<p>Before that UI is implemented, New Tag stops auditing every existing registry\nassociation. Its job remains one atomic result: validate the submitted tag,\nallocate and create one fresh Analysis <code>tags</code> document, append the new linked\nregistry row, rebuild the child projection, and report the outcome. The later\nschema cutover records that freshly created canonical location at <code>doc_url[0]</code>.</p>\n<h2>Status And Order</h2>\n<p><strong>Current state:</strong> TLM-0 through TLM-2 are accepted, and TLM-3 is implemented\nand ready for review. TLM-4 association mutation and presentation, and TLM-5\ncloseout, have not started.</p>\n<p><strong>Review rule:</strong> TLM-1 records an already accepted planning decision, not\nshipped behavior. Complete implementation checkpoints TLM-0 and TLM-2 through\nTLM-5 in order, record evidence beneath each checkpoint, and stop for review\nbefore starting the next.</p>\n<h2>Frozen Data Contract</h2>\n<p>The next registry schema uses <code>doc_url</code> as an ordered array of canonical\nsame-origin Docs Viewer locations:</p>\n<pre><code class=\"language-json\">{\n  &quot;doc_url&quot;: [\n    &quot;/analysis/?doc=d-20260624-213316-478639&amp;subdoc=d-20260727-225608-63967a&quot;,\n    &quot;/docs/?scope=studio&amp;doc=d-20260729-151446-09489e&quot;\n  ]\n}\n</code></pre>\n<ul>\n<li><code>doc_url: []</code> means the tag is not linked. New Tag writes its newly created\nAnalysis <code>tags</code> location at index <code>0</code>; later picker selections append in\ncommit order.</li>\n<li>The URL is the association identity because it preserves the chosen report\ncontext. The same source document may therefore appear more than once through\ndifferent canonical report URLs.</li>\n<li>The same canonical URL cannot occur twice within one tag, but any number of\ntags may reference it. Add and remove operations identify the complete URL\nrather than relying on a mutable array index.</li>\n<li>A canonical URL is a normalized repository-relative viewer route produced by\nDocs ownership. It includes the configured viewer base and scope behavior,\nthe exact report-host document where applicable, and the exact displayed\ndocument. It excludes fragments, transient filters, and arbitrary query\nstate unless a later contract deliberately makes them part of location\nidentity.</li>\n<li>It never stores a scheme or host. The same root-relative value resolves\nagainst the local origin while working locally and the public origin after\npublication.</li>\n<li>Each scope uses its configured canonical viewer base. Analysis locations use\n<code>/analysis/?doc=&lt;report-doc-id&gt;&amp;subdoc=&lt;document-id&gt;</code>; they do not store a\nlocal <code>/docs/?scope=analysis...</code> alternative. A scope whose canonical base is\n<code>/docs/</code> still uses that configured route.</li>\n<li>New writes accept only a URL returned by the supported document-location\nprovider. They do not accept free-form, external, or merely parseable URLs.</li>\n<li>Stale URLs remain stored, visible, and removable. No title match, selected\nparent, displayed document, or alternative report may silently replace them.</li>\n</ul>\n<p>The registry does <strong>not</strong> store document or report titles. A shared\ndocument-location provider resolves each stored URL to current presentation\ndata such as\n<code>{url, scope_id, document_title, report_title, available}</code>. Picker results\nrepresent actual report placements, not just documents: the same source\ndocument may appear as separate choices such as <code>bird-nerve — All Tags</code> and\n<code>bird-nerve — Made-up Tags</code>.</p>\n<p>The provider searches public projections, not management or source\ncollections. Its initial coverage is published Analysis and Library documents\nplus their configured published sub-scopes. The picker adapter requires a\nnon-empty consumer-supplied <code>scopeIds</code> allowlist and has no implicit\nall-supported-scopes default. Tag Editor supplies <code>[&quot;analysis&quot;]</code>. The provider\nloads and returns only records from the requested supported scopes. Top-level\npublic search records and sub-scope placement records contribute one searchable\nresult per canonical URL. Hidden, manage-only, local-only, and unpublished\ndocuments are absent, so every new selection has a public route that also\nresolves against the local origin during local work.</p>\n<p>Provider records always retain <code>scope_id</code>. A picker configured with one scope\nomits the redundant scope label; a picker configured with more than one shows\nit. Report context remains visible when present because distinct report\nplacements can have distinct canonical URLs. Scope-label visibility follows\nthe configured allowlist length, not the scopes represented by the current\nquery results.</p>\n<p>The picker commits one reusable result such as\n<code>{url, scope_id, document_title, report_title}</code>. Consumer adapters determine\nhow it is written:</p>\n<ul>\n<li>Tag Registry stores <code>url</code> in <code>doc_url[]</code> and Tag Editor renders it as the\ntitle link.</li>\n<li>Ordinary document authoring inserts <code>url</code> as the destination of a Markdown\nlink.</li>\n<li>A future document-capable Semantic Token adapter uses <code>url</code> as the generated\ntarget-lookup <code>href</code>; Semantic Token source continues to store its registered\ntoken identity rather than embedding a raw URL in token syntax.</li>\n</ul>\n<p>These are three uses of the same selected canonical URL, not three picker or\nroute contracts. Plain-link and Semantic Token authoring remain later\nconsumers; this delivery proves the shared result through Tag Registry first.</p>\n<h2>Frozen Interaction Contract</h2>\n<ul>\n<li>Edit Tag shows <code>doc_url[]</code> as a selectable title-only list in array order.\nRows retain their exact URL internally; identical visible titles need no\nextra disambiguation after selection.</li>\n<li><strong>Add document</strong> opens the picker with <code>scopeIds: [&quot;analysis&quot;]</code>. The picker\nsearches published Analysis titles, omits the redundant Analysis scope label,\nand shows report context when present so the user can choose the intended\ncanonical location. Committing a result appends its URL and returns to Edit\nTag. A later consumer configured with several scopes shows scope context too.</li>\n<li>The picker excludes an exact URL already present in the draft. A different\nURL with the same document title remains a valid result.</li>\n<li>Selecting a linked-document row enables <strong>Remove link</strong>. Removal changes only\nthe modal draft; it does not delete the document or mutate canonical data.</li>\n<li>Edit Tag <strong>Confirm</strong> writes the complete revised <code>doc_url[]</code> with the other\naccepted tag edits in one atomic registry mutation. Cancel discards all draft\nadditions and removals.</li>\n<li>A URL that no longer resolves is shown as <code>Unavailable document</code>, remains\nselectable, and can be removed. The stored URL is not repaired or replaced.</li>\n<li>The Tag Registry table replaces <strong>description</strong> with <strong>documents</strong>. It renders\ndocument titles as comma-separated links in <code>doc_url[]</code> order and renders\n<code>—</code> for an empty array. Equal titles remain equal; no report label is added.</li>\n<li><strong>Remove link</strong> is deliberately distinct from Tag or document deletion.</li>\n</ul>\n<p>This is the final pre-implementation interaction decision. Ordinary-width and\nnarrow-width fit, tactile selection behavior, and final wording are reviewed\nagainst the working UI rather than designed further in the abstract.</p>\n<p>The schema cutover retires the tag-row <code>description</code> field. This does not alter\nalias descriptions or the independent contents of existing linked documents.</p>\n<h2>TLM-0 Prerequisite Contract</h2>\n<p>Before TLM-0, the creation planner called <code>registry_document_ids</code>, which\nvalidated the complete registry as though New Tag owned association integrity.\nIt rejected creation when any existing row had a missing, duplicate,\nnon-immutable, or non-resolving <code>doc_id</code>.</p>\n<p>That is wider than the creation responsibility:</p>\n<pre><code class=\"language-text\">submitted New Tag\n  -&gt; validate new tag_id, scalar group, and description\n  -&gt; reject an existing tag_id\n  -&gt; allocate one fresh document identity\n  -&gt; create one new document and linked row atomically\n  -&gt; rebuild the exact Analysis/tags projection\n\nexisting registry associations\n  -&gt; preserve unchanged\n  -&gt; do not audit or repair during New Tag\n</code></pre>\n<p>TLM-0 is deliberately limited to that current-schema prerequisite.\n<code>description</code> removal and <code>doc_url[]</code> creation start after its review gate.</p>\n<p>New Tag retains the guards required for its own write:</p>\n<ul>\n<li>require the supported registry version and a registry <code>tags</code> array;</li>\n<li>validate the submitted fields and reject an existing <code>tag_id</code>;</li>\n<li>resolve the configured direct-child document root and confine the new path;</li>\n<li>allocate the immutable ID directly from the document timestamp and\ncryptographic token, without supplying registry or source identities; and</li>\n<li>preserve the existing atomic Registry/document write, targeted rebuild,\ncompensation, activity, response, and manual-patch contracts.</li>\n</ul>\n<p>The planner does not inspect any existing registry <code>doc_id</code>, inventory document\nIDs for allocation, compare the generated ID with registry rows, or preflight\nthe destination's existence. <code>write_text_atomic_new</code> is the sole collision\nboundary: it refuses to overwrite a real destination if the generated identity\ndoes collide. That create-only write is filesystem safety, not registry\nassociation validation. Focused association audit and repair belong to the\nlater link-management workflow.</p>\n<h2>Delivery Tracker</h2>\n<h3>[x] TLM-0 — Narrow New Tag To Its Own Write</h3>\n<ul>\n<li>[x] Remove <code>registry_document_ids</code> and all other existing-association reads\nfrom document-ID allocation and row creation.</li>\n<li>[x] Stop inventorying Analysis document IDs or checking destination existence\nbefore the exclusive-create write.</li>\n<li>[x] Keep validation of the new tag, new row, new immutable document ID,\nconfigured source root, confined path, and atomic create-only destination.</li>\n<li>[x] Prove creation succeeds while unrelated existing rows include:\n<ul>\n<li>no <code>doc_id</code>;</li>\n<li>a shared <code>doc_id</code>;</li>\n<li>a stale <code>doc_id</code>; and</li>\n<li>a malformed <code>doc_id</code>.</li>\n</ul>\n</li>\n<li>[x] Preserve duplicate-new-<code>tag_id</code>, invalid submitted field, destination\ncollision at exclusive create, write/rebuild failure, compensation, response,\nactivity, and manual-patch behavior.</li>\n<li>[x] Remove helpers and tests whose only purpose was whole-registry or\nwhole-collection document-ID preflight.</li>\n<li>[x] Run focused creation/planner/API tests and the narrow maintained Tag\nRegistry smoke boundary.</li>\n<li>[x] Record exact tested and untested surfaces and inspect the final diff.</li>\n</ul>\n<p><strong>Checkpoint / review gate:</strong> New Tag creates one correct linked tag/document\npair without certifying unrelated associations. No links UI, association\nmutation, repair, or presentation change starts before user review.</p>\n<p>Ready for review. Stop here before TLM-2.</p>\n<h4>TLM-0 Evidence</h4>\n<ul>\n<li>New Tag now validates the supported v4 container and submitted row, resolves\nthe configured Analysis <code>tags</code> source root, allocates directly from the new\ndocument timestamp and cryptographic token, and plans only the new row and\nconfined destination. It no longer reads existing registry associations or\ninventories Analysis documents.</li>\n<li>Focused creation proves a successful commit while unrelated rows are\nunlinked, share one resolving <code>doc_id</code>, hold a stale immutable <code>doc_id</code>, and\nhold a malformed legacy value. Focused row creation also permits a shared\n<code>doc_id</code> while retaining duplicate-new-<code>tag_id</code> and submitted-field guards.</li>\n<li>Destination existence is not checked during planning. The atomic\n<code>write_text_atomic_new</code> call is the collision boundary and preserves an\nexisting destination byte-for-byte. Registry-byte revision checking,\nRegistry/document compensation, targeted child rebuild, response, and\nactivity paths are unchanged.</li>\n<li>Focused Python evidence passed 22 tests across\n<code>test_tag_document_creation.py</code>, <code>test_tag_registry_mutations.py</code>, and\n<code>test_studio_tag_workflows.py</code>. This covers planning, real child projection,\nexclusive-create collision, Registry-write and rebuild compensation,\ndry-run/API response, invalid input, and retry evidence.</li>\n<li>A write-free plan against the current canonical v4 Registry produced one\nconfined, absent destination and left the Registry byte-for-byte unchanged.</li>\n<li>Maintained <code>studio-tag-smoke</code> passed all 11 checks, including the fixture\ncreate API, linked source, targeted rebuild, response/activity boundary, and\nroute modules. Run evidence:\n<code>var/admin/test-runs/tag-link-management-tlm0-localhost/summary.md</code>. The first\nsandboxed run could not bind its fixture servers to <code>127.0.0.1</code>; its ten\nfailures were environment failures, and the localhost-permitted rerun is the\nproduct evidence.</li>\n<li>Explicit Python lint passed for the four changed implementation/test files,\nand <code>git diff --check</code> passed. The final source and generated diff were\ninspected after rebuilding this delivery document.</li>\n<li>No live canonical tag or Analysis document was written. Manual patch source\nand UI modules were unchanged and covered only by maintained source/module\nchecks, not a direct fallback interaction. No links UI, schema v5 migration,\nassociation mutation, repair, provider/picker, browser interaction, or\nordinary/narrow visual review was attempted.</li>\n</ul>\n<h3>[x] TLM-1 — Freeze The Cutover And Interaction</h3>\n<ul>\n<li>[x] Use one-way <code>tag_registry_v4</code> to <code>tag_registry_v5</code> migration with no\ndual-schema compatibility path.</li>\n<li>[x] Support published Analysis and Library documents plus their configured\npublished sub-scopes behind a required consumer scope allowlist; Tag Editor\nrequests Analysis only. Exclude Studio, manage-only, local-only, hidden, and\nunpublished documents.</li>\n<li>[x] Use one shared title-search picker whose records retain scope/report\ncontext and commit one canonical URL plus presentation data. Omit scope from\none-scope presentation while retaining report context.</li>\n<li>[x] Keep the Edit Tag linked-document list title-only and permit equal titles.</li>\n<li>[x] Use draft <strong>Add document</strong> and <strong>Remove link</strong> actions, then commit the\ncomplete URL array only on Edit Tag confirmation.</li>\n<li>[x] Replace the registry description column with comma-separated document\ntitle links and an empty-state dash.</li>\n<li>[x] Keep stale URLs visible as <code>Unavailable document</code> and removable without\nrepair.</li>\n</ul>\n<p><strong>Checkpoint accepted:</strong> the persistence, search, modal, table, and downstream\nconsumer contracts are frozen. Visual and tactile acceptance waits for the\nworking UI.</p>\n<h3>[x] TLM-2 — Cut Over The Registry And New Tag</h3>\n<p>Start only after TLM-0 is accepted.</p>\n<ul>\n<li>[x] Add <code>tag_registry_v5</code> validation for ordered canonical <code>doc_url</code> arrays.</li>\n<li>[x] Migrate each nonblank v4 scalar to its current canonical Analysis <code>tags</code>\nreport URL; migrate a missing or blank scalar to <code>[]</code>.</li>\n<li>[x] Remove tag-row <code>description</code> during the same guarded migration while\npreserving row order, <code>tag_id</code>, scalar <code>group</code>, timestamps, policy, aliases,\nassignments, documents, and document content.</li>\n<li>[x] Reject noncanonical, unsupported, external, and within-tag duplicate new\nURLs without rejecting shared URLs across different tags.</li>\n<li>[x] Remove <code>description</code> from New Tag input, validation, registry writes,\ndirect API and manual-patch contracts. A new document starts from its tag\ntitle without a duplicated registry description.</li>\n<li>[x] Record the freshly created Analysis <code>tags</code> canonical URL at <code>doc_url[0]</code>\nwithout reading existing URL arrays for uniqueness or stale-link checks.</li>\n<li>[x] Preserve TLM-0's exclusive-create collision boundary, atomic registry and\ndocument write, rebuild, compensation, activity, and failure behavior.</li>\n<li>[x] Remove v4-only readers and writers after the migration; do not retain a\ncompatibility alias or silent scalar/URL normalization in the live app.</li>\n<li>[x] Reconcile the canonical migration and run focused schema, migration,\ncreation, API, rollback, and manual-patch evidence.</li>\n</ul>\n<p><strong>Checkpoint / review gate:</strong> canonical v5 data and New Tag are accepted before\nassociation mutation or browser picker integration.</p>\n<p>Checkpoint accepted.</p>\n<h4>TLM-2 Evidence</h4>\n<ul>\n<li>The one-way migration replaced all 246 nonblank v4 <code>doc_id</code> scalars with\n<code>/analysis/?doc=d-20260624-213316-478639&amp;subdoc=&lt;doc-id&gt;</code> at <code>doc_url[0]</code>.\nIt removed all 246 tag-row descriptions and scalar IDs while preserving\nordered <code>tag_id</code>, scalar group, row timestamp, and policy values. The\nmigration also covers blank or missing legacy IDs as <code>[]</code>; the applied input\ncontained none.</li>\n<li>Registry reconciliation reports 246 ordered rows and 246 document URLs.\nAliases remain 70 entries with 71 targets, assignments remain eight\nreferences, their source fingerprints remain\n<code>9bdcd5ac7df75719ba791480ff45612137ce1832e5c0595eaf8e23a376701158</code>\nand\n<code>08c0dba8bf923be376f1b8186290a00b1a2bdb7c74ad811f896eb1f430b72d5d</code>,\nand no Analysis <code>tags</code> document source changed.</li>\n<li>Live Registry loading now requires <code>tag_registry_v5</code> and exact row fields.\nOrdered arrays accept supported canonical Analysis, Library, and configured\nlocal Docs routes. Validation rejects external, fragment-bearing,\nunsupported, malformed, and within-row duplicate URLs while permitting the\nsame exact URL on different tags. Historical v1-to-v4, label-retirement, and\ndocument-bootstrap utilities remain explicitly pinned one-time tools rather\nthan live v4 compatibility.</li>\n<li>Docs Viewer owns canonical sub-scope location generation from configured\nviewer base/scope behavior and the exact report-host source. Migration,\nfirst-class New Tag creation, Studio runtime config, and manual patch output\nconsume that owner. A new tag appends its fresh URL without inspecting other\nrows' URL arrays.</li>\n<li>New Tag no longer renders, validates, sends, stores, or manually patches a\ndescription. Direct creation rejects a supplied description. Its new\nAnalysis source contains front matter plus <code># &lt;tag-id&gt;</code> only.</li>\n<li>The existing exclusive-create destination guard, revision check, atomic\nRegistry/document write, exact child rebuild, compensation, activity, and\nresponse behavior remain covered. Focused schema/migration/mutation tests\nalso cover empty arrays, shared cross-tag URLs, invalid URLs, rollback, and\nreal sub-scope projection.</li>\n<li>All 287 Studio Python tests passed with an isolated writable catalogue media\nbase. Maintained <code>studio-tag-smoke</code> passed all 11 checks, including Studio\nsource lint, fixture APIs, actual route readiness, New Tag input retirement,\nmanual-patch modules, linked-source creation, rebuild, compensation-adjacent\nboundaries, and activity. Run evidence:\n<code>var/admin/test-runs/tag-link-management-tlm2-final-4-localhost/summary.md</code>.\nThe immediately preceding sandboxed attempt allowed source lint but blocked\nall ten localhost fixtures; the localhost-permitted rerun is the product\nevidence.</li>\n<li><code>git diff --check</code> passed and the final canonical migration was validated by\nthe same command in read-only mode. No TLM-3 provider/picker, link add/remove,\nor association mutation was implemented. The existing Edit Tag description\nand Registry-table presentation are deliberately left for TLM-4, so no\nrealistic link-management or narrow-width visual review was attempted.</li>\n</ul>\n<h3>[x] TLM-3 — Deliver The Shared Document Location Provider And Picker</h3>\n<ul>\n<li>[x] Add the reusable local-authoring document-location picker under\n<code>shared/frontend/</code>, using the existing shared search-list behavior rather\nthan creating another listbox implementation.</li>\n<li>[x] Keep the picker independent of a route shell so Tag Registry and later\nlocal authoring consumers can provide their own adapters.</li>\n<li>[x] Require each consumer to supply a non-empty <code>scopeIds</code> allowlist. Keep\nsupported-collection policy, durable form state, and writes with the\nconsuming app; keep the low-level shared search-list record-only. The picker\npasses the allowlist to the provider and commits one exact canonical URL plus\npresentation data; it does not serialize Tag Registry values, Markdown links,\nor Semantic Tokens itself.</li>\n<li>[x] Add a compact public document-location search projection and provider for\npublished Analysis and Library documents plus their published sub-scopes.\nLoad and return only requested supported scopes, fail closed for empty or\nunsupported scope requests, and emit one canonical root-relative URL per\nactual placement with its document, scope, and report presentation data.</li>\n<li>[x] Build from public search/index and sub-scope placement ownership only.\nDo not read manage manifests, local published workspaces, source front matter,\nor local Docs APIs in the picker.</li>\n<li>[x] Permit one source document to appear in several provider records when\ndistinct report-host documents surface it. Do not construct a report/document\ncross-product or assume one report host per sub-scope.</li>\n<li>[x] Search document titles case-insensitively as the user types, rank exact\nand prefix matches ahead of other title matches, and preserve distinct\nplacements with duplicate titles.</li>\n<li>[x] Support asynchronous loading, keyboard/pointer commit, empty results, and\nprovider failure. Retain scope on every record; show it only when\n<code>scopeIds.length &gt; 1</code>. Show report context when present. Do not vary\nscope-label visibility from the current result set.</li>\n<li>[x] Exclude exact URLs already attached to the edited tag from selectable\nresults while preserving them in the existing-link list.</li>\n<li>[x] Add pure component/provider, public-projection, and exact route/module\nwiring evidence.</li>\n</ul>\n<p><strong>Checkpoint / review gate:</strong> approve provider results and picker behavior\nbefore allowing the picker to mutate Tag Registry.</p>\n<p>Ready for review. Stop here before TLM-4.</p>\n<h4>TLM-3 Evidence</h4>\n<ul>\n<li>Docs Viewer now owns deterministic\n<code>docs_document_locations_v1</code> projections beside the public Analysis and\nLibrary search indexes. Records contain only canonical URL, scope ID,\ndocument title, and report title; they are derived from public search\nrecords, public report payloads, and the matching public sub-scope manifest.\nSource documents, manage manifests, and local APIs are outside the builder.</li>\n<li>The checked-in public projections contain 250 Analysis locations: two\ntop-level documents and 248 currently published <code>Concepts</code> placements. The\nLibrary projection contains 14 top-level locations. The TLM-2-created\n<code>bird-nerve</code> source remains absent because its public sub-scope projection has\nnot been published; TLM-3 did not publish or otherwise promote that document.</li>\n<li>Publication confirm/apply now includes the location projection for supported\nscopes, using the exact parent, search, and sub-scope bytes eligible for the\nsame publication. The standalone builder exists only to reconcile the\nprojection from the already published <code>site/</code> artifact and is idempotent for\nboth current scopes.</li>\n<li>The shared provider requires a non-empty Analysis/Library <code>scopeIds</code>\nallowlist, fetches only those per-scope indexes, validates schemas and\ncanonical URLs, caches successful scope loads, and fails closed for malformed\nor unsupported data. Title search is case-insensitive with exact, prefix,\nthen containing-match rank while retaining equal-title report placements.</li>\n<li>The shared picker adapts the existing search-list primitive and returns only\n<code>{url, scope_id, document_title, report_title}</code>. It supports dynamic exact-URL\nexclusions, keyboard and pointer commit, empty and failure states, report\ncontext, and scope context based on configured allowlist length. The generic\nsearch list now ignores stale asynchronous responses after later input,\nclose, commit, or teardown, and cannot commit an earlier match while a newer\nrequest is pending.</li>\n<li>Focused Python evidence passed 41 tests across the location builder,\npublish gate, builder bootstrap, Studio asset/runtime route, and Admin target\nmap/config boundaries. After final review narrowed publication inputs to\nactually placed public sub-scopes, the affected builder/publish slice passed\nall 10 tests. Explicit Python and JavaScript source lint passed, as did the\ncomplete maintained <code>shared</code> and <code>docs-viewer</code> lint scopes. The\nprojection-contract audit passed, both generated indexes rebuilt to an\nunchanged result, and <code>git diff --check</code> passed.</li>\n<li>The focused Playwright module smoke loaded the real Analysis and Library\nprojections through Studio-compatible routes, then proved requested-scope\nloading, ranking, duplicate placements, exact exclusion, stale resolution,\none- and multi-scope presentation, keyboard and pointer commit, empty and\nfailure states, invalid commit rejection, and stale asynchronous response\nsuppression.</li>\n<li>Maintained <code>studio-tag-smoke</code> passed all 11 checks, including the complete\nStudio source lint boundary, fixture tag APIs, local routes, the real\nprovider/picker module and data paths, and retained Tag Editor readiness.\nRun evidence:\n<code>var/admin/test-runs/tag-link-management-tlm3-final-7-localhost/summary.md</code>.</li>\n<li>TLM-4 remains intentionally absent: the picker is not mounted in Edit Tag,\nno Registry URL draft is displayed or changed, no description/table\npresentation was altered, and no ordinary- or narrow-width Tag Registry\nvisual review was attempted.</li>\n</ul>\n<h3>[ ] TLM-4 — Integrate Tag Link Management</h3>\n<ul>\n<li>[ ] Add focused atomic append/remove operations using explicit <code>tag_id</code> plus\nthe complete draft <code>doc_url[]</code>; confirmation changes only the associations\nand accepted tag fields and never deletes a document.</li>\n<li>[ ] Preserve unrelated tag rows, URL order, aliases, assignments,\ndocuments, and document content.</li>\n<li>[ ] Replace the Tag Registry description heading, cells, and sort projection\nwith <strong>documents</strong>, rendering current titles as comma-separated links in URL\norder and <code>—</code> for no links.</li>\n<li>[ ] Remove tag-description controls and state from New Tag and Edit Tag.</li>\n<li>[ ] Render zero, one, or many title-only selectable linked-document rows in\nEdit Tag. Equal titles remain unchanged and rows retain exact URL identity.</li>\n<li>[ ] Add <strong>Add document</strong> to open the shared picker with\n<code>scopeIds: [&quot;analysis&quot;]</code> and append one committed URL to the draft. Omit the\nredundant scope label and retain report context in picker results.</li>\n<li>[ ] Add <strong>Remove link</strong>, disabled without a selection, to remove the selected\nexact URL from the draft without deleting a document.</li>\n<li>[ ] Commit the complete URL draft atomically on Edit Tag confirmation and\ndiscard it on cancel.</li>\n<li>[ ] Render an unresolved URL as <code>Unavailable document</code> in both surfaces while\nkeeping it selectable, linked to its exact stored URL, and removable.</li>\n<li>[ ] Never accept free-form IDs or URLs and never infer a location from the\ndisplayed document, selected tree row, title, group, or an alternative route.</li>\n<li>[ ] Add focused planner, API, atomic-write, activity, failure, retry,\nfrontend-domain, and route-module evidence before realistic UI review.</li>\n<li>[ ] Review realistic linked, unlinked, multi-link, shared, duplicate-title,\nmulti-report, search-no-match, provider-failure, and stale examples at\nordinary and narrow widths.</li>\n</ul>\n<p><strong>Checkpoint / review gate:</strong> user acceptance of the actual title list,\npicker, remove interaction, and state language before closeout.</p>\n<h3>[ ] TLM-5 — Integration And Closeout</h3>\n<ul>\n<li>[ ] Run the accumulated focused service, API, route-module, and maintained Tag\nRegistry checks chosen by the accepted interaction.</li>\n<li>[ ] Verify migration, optional, ordered, shared, stale, duplicate-title,\nmulti-report, and standalone-document behavior without implying\nsynchronization or exclusive ownership.</li>\n<li>[ ] Inspect the final source and generated diff independently.</li>\n<li>[ ] Move only stable shipped behavior and remaining weak spots into Tag\nRegistry and Tag Source Data.</li>\n<li>[ ] Close or split any unapproved follow-on rather than widening this\ndelivery.</li>\n</ul>\n<h2>In Scope</h2>\n<ul>\n<li>Removing complete-registry association validation from New Tag.</li>\n<li>Relying on generated identity plus the atomic exclusive-create write, without\nregistry or document-inventory uniqueness preflight.</li>\n<li>Evidence-led discovery of the actual link-management UI need.</li>\n<li>An ordered <code>doc_url</code> array of canonical cross-scope document locations.</li>\n<li>A guarded v4-to-v5 migration and tag-description retirement.</li>\n<li>Live document and report-title resolution without registry title duplication.</li>\n<li>One reusable, provider-driven published-document location picker with a\nrequired consumer scope allowlist.</li>\n<li>Provider coverage over public Analysis, Library, and published sub-scope\nprojections, with Analysis-only title search in Tag Editor.</li>\n<li>Edit Tag title-only listing plus draft exact-URL append and remove.</li>\n<li>Tag Registry comma-separated document-title links.</li>\n<li>Optional, multiple, shared, resolving, stale, and unlinked relationship\nstates.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>Synchronizing tag group with document content or metadata.</li>\n<li>Making tags own document deletion or documents own tag deletion.</li>\n<li>Inferring, deduplicating, or automatically repairing associations.</li>\n<li>Reordering document URLs.</li>\n<li>Editing document titles or content from Tag Registry.</li>\n<li>Bulk link mutation.</li>\n<li>Automatically supporting every configured Docs scope or sub-scope.</li>\n<li>An implicit all-supported-scopes picker mode.</li>\n<li>Persisting fragments, temporary filters, or other incidental viewer state.</li>\n<li>Searching source, manage-only, hidden, local-only, or unpublished documents.</li>\n<li>Adding authoring controls to the live public site.</li>\n<li>Integrating the picker into a second local authoring workflow in this\ndelivery.</li>\n<li>Redesigning the Analysis Concepts collection or generic sub-scope report.</li>\n<li>Tag semantic-token implementation.</li>\n<li>Renaming <code>tag_id</code>, adding a Concept identity model, or changing alias and\nassignment semantics.</li>\n</ul>\n<h2>Starting Authority</h2>\n<ul>\n<li>creation planner and transaction:\n<code>studio/services/tags/tag_document_creation.py</code></li>\n<li>focused row creation:\n<code>studio/services/tags/tag_registry_mutations.py</code></li>\n<li>API boundary:\n<code>studio/app/server/studio/studio_tag_api/registry.py</code></li>\n<li>current Tag Registry route and modules:\n<code>studio/app/frontend/routes/tag-registry.html</code> and\n<code>studio/app/frontend/js/tag-registry-*</code></li>\n<li>current local search-list interaction reference:\n<code>shared/frontend/js/search-list.js</code></li>\n<li>public scope configuration, search, sub-scope manifests, and canonical route\nprojection:\n<code>site/docs-viewer/config/defaults/docs-viewer-public-config.json</code>,\n<code>site/assets/data/search/</code>, <code>site/assets/data/docs/scopes/</code>,\n<code>site/docs-viewer/runtime/js/shared/docs-viewer-configured-scope-provider.js</code>,\nand <code>docs-viewer/build/docs_builder/sub_scope.py</code></li>\n<li>canonical registry:\n<code>studio/data/canonical/tags/tag-registry.json</code></li>\n<li>Analysis document source:\n<code>docs-viewer/scopes/analysis/source/sub-scopes/tags/documents/</code></li>\n<li>focused creation, registry, workflow, and route tests:\n<code>studio/tests/python/test_tag_document_creation.py</code>,\n<code>test_tag_registry_mutations.py</code>, <code>test_studio_tag_workflows.py</code>, and the\nmaintained Tag Registry smoke checks</li>\n</ul>\n<p>These are inspection starting points. The picker remains local, app-neutral\nauthoring behavior; Tag transport and mutation remain Studio-owned, while its\nsearch inputs, public placement data, and canonical URLs remain Docs\nViewer-owned.</p>\n<h2>Completion Gate</h2>\n<p>Complete only when the New Tag prerequisite is independently accepted, the\nv5 migration and description retirement are reconciled, the shared provider\nand picker have a real Tag Registry consumer, the exact inspect/add/remove\nworkflow is shipped and realistically reviewed, and durable owners describe\nthe resulting current behavior without turning URL order or bootstrap output\ninto ownership or one-to-one relationship invariants.</p>\n"
}
