{
  "doc_id": "d-20260802-163320-9015b0",
  "title": "TE-1 — Source Editor Directive Actions Delivery",
  "added_date": "2026-08-02 16:33:20",
  "last_updated": "2026-08-02 17:14:31",
  "viewer_url": "/docs/?scope=studio&doc=d-20260802-163320-9015b0",
  "parent_id": "d-20260802-150245-4e7f85",
  "summary": "first add a contextual Directives menu to the existing Manage Source toolbar for exact-table insertion of two harmless authoring comments",
  "ui_status": "proposed",
  "content_html": "<h1>TE-1 — Source Editor Directive Actions Delivery</h1>\n<h2>Status And Purpose</h2>\n<p>Proposed as <strong>TE-1</strong>, the first <a href=\"/docs/?scope=studio&amp;doc=d-20260802-150245-4e7f85\">Table Enhancements</a> delivery. It has no dependency on <a href=\"/docs/?scope=studio&amp;doc=d-20260802-162149-3ba013\">TE-2</a> or <a href=\"/docs/?scope=studio&amp;doc=d-20260802-160430-34cb3b\">TE-7</a>: inserted comments remain harmless and visually inert until a later consumer chooses to recognise them. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-160429-69a5f6\">Authoring Directives Concept And Examples</a> owns the source forms, exact-table, and no-op boundaries implemented by this delivery.</p>\n<p>TE-1 adds one authoring convenience to the existing Manage Source mode: a <strong>Directives</strong> button, presented in the style and interaction model of the existing Actions controls, opens the two maintained table comment templates. Choosing an item inserts the comment on its own line immediately before the exact Markdown table identified by the captured caret or selection.</p>\n<h2>Complete Outcome</h2>\n<p>With the caret or selection inside exactly one supported Markdown table, the <strong>Directives</strong> menu offers:</p>\n<ul>\n<li><strong>Enable table detail</strong>, inserting <code>&lt;!-- dotlineform:table-detail --&gt;</code>; and</li>\n<li><strong>Publish table as linked SVG</strong>, inserting <code>&lt;!-- dotlineform:table-publish format=svg id=table-name --&gt;</code> and selecting only <code>table-name</code> for immediate editing.</li>\n</ul>\n<p>The editor returns focus to the source textarea and marks the existing buffer dirty through the ordinary input path. It does not save or rebuild automatically and shows no insertion confirmation message. Before TE-2 or TE-7 ships, Save/Rebuild simply treats the inserted text as an ordinary HTML comment and the table renders normally.</p>\n<p>The control appears wherever the existing Manage Source editor is available; it is not filtered by build or publication capability. A command is disabled when the captured source cannot identify exactly one eligible table, when the buffer changed after capture, or when that table already has one of the exact maintained comments immediately associated with it. The initial delivery leaves removal, malformed comments, and attribute editing to ordinary source editing.</p>\n<h2>Existing Seams And Expected Size</h2>\n<p>The current Source editor already exposes main-view controls through the view registry and contribution maps. Its context adapter can capture a selection with the current buffer revision, replace a verified range with <code>textarea.setRangeText</code>, select a verified inserted range, return focus, dispatch the normal input path, and project busy/disabled control state. The existing Index and sub-scope Actions controls provide the menu interaction and accessibility precedent; TE-1 reuses their presentation behavior without adopting selection-mode document targeting.</p>\n<p>The likely production owners are <code>docs-viewer-action-definitions.js</code>, <code>docs-viewer-management-hosted-views.js</code>, <code>docs-viewer-manage.js</code>, <code>source-editor/source-editor.js</code>, one bounded <code>source-editor/directive-actions.js</code> module, and <code>docs-viewer-source-editor.css</code> or the existing Manage menu CSS. Focused pure/module tests should own table-range recognition, insertion text/ranges, already-marked state, revision rejection, focus/dirty integration, and menu wiring.</p>\n<p>The current indicative shape is 5–7 touched production files, approximately 240–400 JavaScript/CSS lines and 260–450 focused test lines. TE-1.0 must replace this estimate with exact files and rough diffs after tracing the current control/menu renderers and ordinary Markdown table behavior. If the work requires a new editor framework, server round trip for every caret move, general directive registry, modal workflow, custom undo manager, or materially exceeds the estimate, stop for review.</p>\n<h2>Interaction Contract</h2>\n<p>The button is one Source-mode main-view control. Its popup is keyboard accessible, closes through the same outside-click, Escape, focus-return, route-change, and disabled-state behavior as the existing Actions menus, and does not introduce a second toolbar or inline table chrome.</p>\n<p>The source caret or selection is authoritative. A collapsed caret anywhere in the exact table source range may identify that table; a non-collapsed selection must intersect one table and no other Markdown block. The action never falls back to the active rendered table, table occurrence, nearby heading, document title, previous invocation, or first table in the document.</p>\n<p>The captured buffer revision and table range are revalidated immediately before insertion. The comment is inserted at the table’s source start with exactly one separating newline according to the immediate-next-table form frozen by TE-1 itself. The SVG placeholder selection is computed from the inserted text rather than found later by searching the document.</p>\n<p>The browser UI may conservatively decline Markdown it cannot identify exactly. It only needs enough source awareness to place one maintained comment safely; it neither parses rendered HTML nor predicts whether a later build consumer will act. Unknown or hand-edited directive text is ordinary source and does not need browser validation.</p>\n<p>The edit uses the existing textarea <code>setRangeText</code> and input-event seam. TE-1 does not use deprecated <code>execCommand</code>, synthesize keyboard events, intercept the clipboard, maintain a fake undo stack, or claim a native Undo guarantee. Any actual browser undo behavior is recorded at TE-1.0 and accepted as native textarea behavior rather than emulated.</p>\n<h2>Rough Delivery Steps</h2>\n<h3>TE-1.0 — Trace And Freeze The Source Action</h3>\n<ul>\n<li>[ ] Trace Source-mode control registration/rendering/dispatch, Actions menu lifecycle and accessibility, adapter capture/range replacement, dirty revision handling, focus behavior, route cleanup, and current focused module tests.</li>\n<li>[ ] Freeze the smallest browser-side source range contract needed to place a comment before one ordinary Markdown pipe table.</li>\n<li>[ ] Freeze the control label/icon, menu labels and order, already-marked behavior, blank-line insertion normalization, SVG placeholder range, and no-status-message behavior.</li>\n<li>[ ] Record actual browser-native Undo behavior for the existing <code>setRangeText</code> seam without adding <code>execCommand</code> or a synthetic history.</li>\n<li>[ ] Record exact production/test files, reused control/menu/adapter seams, and rough diffs for TE-1.1 and TE-1.2 before implementation approval.</li>\n</ul>\n<p><strong>Checkpoint:</strong> a static source fixture demonstrates a caret above, inside, and below a table; a selection intersecting one table; a selection spanning two tables; ordinary comments; existing exact and malformed directive comments; changed buffer revision; and SVG placeholder selection. The expected enabled state and exact inserted source/range are recorded. Stop before implementation.</p>\n<h3>TE-1.1 — Add The Contextual Directives Control</h3>\n<ul>\n<li>[ ] Register one Manage Source main-view <strong>Directives</strong> control and reuse the established Actions-style popup lifecycle, keyboard behavior, focus return, and route cleanup.</li>\n<li>[ ] Supply the two bounded maintained comment templates without consulting build or publication capability.</li>\n<li>[ ] Recompute menu item state from the current adapter capture whenever the menu opens and disable ambiguous, already-marked, busy, unloaded, or stale actions.</li>\n<li>[ ] Keep the menu independent of Index checked-document selection, rendered-table identity, and Publish state.</li>\n</ul>\n<h3>TE-1.2 — Locate The Exact Table And Insert Source</h3>\n<ul>\n<li>[ ] Implement pure source helpers that locate exactly one supported Markdown table from the captured caret/selection and detect its immediately associated maintained directives.</li>\n<li>[ ] Insert the chosen comment immediately before the verified table through one revision-bound adapter edit, preserve surrounding source bytes apart from the required newline, and dispatch the existing dirty/input path.</li>\n<li>[ ] For the SVG form, select the exact inserted <code>table-name</code> placeholder; otherwise place the caret after the inserted directive and return focus to the textarea.</li>\n<li>[ ] Refuse stale or ambiguous captures without modifying source, saving, rebuilding, opening a modal, or displaying a success message.</li>\n</ul>\n<h3>TE-1.3 — Verify And Transfer</h3>\n<ul>\n<li>[ ] Run focused pure/module checks for source ranges, table boundaries, insertion bytes, placeholder selection, already-marked tables, stale revisions, control registration, menu lifecycle, and dirty-state integration.</li>\n<li>[ ] Manually verify keyboard and pointer opening, item activation, Escape/outside-click closure, focus return, placeholder replacement, narrow toolbar fit, route changes, and the observed native Undo behavior in the supported browser.</li>\n<li>[ ] Run targeted lint, generated Docs/search, internal-link, JSON, and whitespace checks.</li>\n<li>[ ] Update the concise Source editor and authoring syntax authorities, then hand the UI to the user for tactile review without changing the TE-2/TE-7 build contracts.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>recognising, validating, projecting, or otherwise acting on a directive during a build;</li>\n<li>a generic directive/plugin registry, user-configurable menu, or automatic discovery of arbitrary comments;</li>\n<li>inserting a directive without one exact eligible Markdown table at the captured caret or selection;</li>\n<li>editing attributes after insertion, generating a semantic SVG ID, removing/reordering directives, or converting one directive into another;</li>\n<li>parsing rendered HTML, selecting a table by title/occurrence, or adding controls to rendered tables;</li>\n<li>save, rebuild, Publish, conversion progress, or success/status messages;</li>\n<li><code>execCommand</code>, clipboard manipulation, synthetic key events, fake undo, or a custom editor history;</li>\n<li>a modal, second source toolbar, parallel editor model, rich-text editing, or inline table editor; or</li>\n<li>sub-scope-specific behavior beyond using the same exact target and Source editor contracts.</li>\n</ul>\n<p>The <a href=\"/docs/?scope=studio&amp;doc=d-20260802-150245-4e7f85\">Table Enhancements feature</a> owns delivery order. TE-1 is complete when the two comments can be inserted safely through the existing Source control model while canonical Markdown, ordinary rendering, transfer behavior, Content Detail, SVG preparation, and Publish remain unchanged.</p>\n"
}
