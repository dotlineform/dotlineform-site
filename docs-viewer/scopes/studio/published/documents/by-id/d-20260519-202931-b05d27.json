{
  "doc_id": "d-20260519-202931-b05d27",
  "title": "Catalogue Indexes And Payloads",
  "added_date": "2026-05-19 20:29:31",
  "last_updated": "2026-08-13 12:01:22",
  "viewer_url": "/docs/?scope=studio&doc=d-20260519-202931-b05d27",
  "parent_id": "d-20260401-000000-a11bf3",
  "content_html": "<h1>Catalogue Indexes And Payloads</h1>\n<h2>Public Read Models</h2>\n<table>\n<thead>\n<tr>\n<th>Artifact</th>\n<th>Purpose</th>\n<th>Main consumers</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>site/assets/data/series_index.json</code></td>\n<td>lean published Series list identity, title/year fields, primary Work, and optional single-Work destination</td>\n<td>unselected <code>/series/</code></td>\n</tr>\n<tr>\n<td><code>site/assets/data/recent_index.json</code></td>\n<td>publication snapshots with resolved public <code>href</code> destinations</td>\n<td><code>/recent/</code></td>\n</tr>\n<tr>\n<td><code>site/assets/series/index/&lt;series_id&gt;.json</code></td>\n<td>selected Series metadata, optional rendered prose, derived public document URLs, and ordered lightweight member Works</td>\n<td>selected Series and exact Work navigation</td>\n</tr>\n<tr>\n<td><code>site/assets/works/index/&lt;work_id&gt;.json</code></td>\n<td>selected Work metadata, optional rendered prose, downloads, links, grouped details, and derived public document URLs</td>\n<td>Work and Work-detail runtimes</td>\n</tr>\n<tr>\n<td><code>site/assets/data/search/catalogue/index.json</code></td>\n<td>flat search records for current catalogue kinds</td>\n<td><code>/catalogue/search/</code></td>\n</tr>\n</tbody>\n</table>\n<p>All public Catalogue projections contain published records only. The former flat Works aggregate is retired; a future flat Works report must be a separate, explicitly owned projection.</p>\n<p>These are generated payloads. Their schema builders under <code>studio/services/catalogue/catalogue_generation_*.py</code> and the consuming route modules are the exact authority.</p>\n<h2>Published Document URLs</h2>\n<p><code>work_record_v4</code> and <code>series_record_v3</code> include <code>doc_url</code> as a deterministic array of public document URL strings. Empty arrays are ordinary output. The field is derived by joining exact current-public Docs identities and locations to each document's final canonical <code>work_id</code> or <code>series_id</code> declaration; it is not authored in canonical Catalogue source, inferred from Work membership, Series membership, titles, routes, filenames, rendered links, or Semantic Token mentions.</p>\n<p>Work subjects contribute only to the exact Work payload, and Series subjects contribute only to the exact Series payload. The public Work and Series routes do not consume this field yet; presentation remains a separate Catalogue delivery.</p>\n<p>After a successful Docs Publish, the follow-through compares the complete current derived projection with existing non-empty Work/Series payload evidence and writes only exact changed by-ID targets. <code>catalogue_generation_records.py</code> owns the shared complete Work and Series payload finalizers used by normal generation, cleanup, and this follow-through; the exact multi-payload write is atomic. A derivation or write failure leaves Docs publication successful, retains the prior complete Catalogue payloads, and returns explicit stale evidence without retrying or rolling back publication. Aggregate Catalogue indexes, search, media, canonical Catalogue source, authored links, and public route presentation are not part of this follow-through.</p>\n<h2>Why There Are Lean And Exact Files</h2>\n<p>The Series list needs many small records; selected routes need one exact rich record.</p>\n<ul>\n<li>The unselected Series grid reads only <code>series_index.json</code>.</li>\n<li>A selected Series reads its exact Series payload and ordered lightweight member Works.</li>\n<li>A selected Work loads one Work payload containing its local detail sections.</li>\n<li>Exact Work-to-Series navigation reads the exact relevant Series payload rather than a global membership map.</li>\n<li>A selected detail derives its parent Work from <code>detail_uid</code> and reuses that Work payload rather than requiring a global detail index.</li>\n<li>Recent owns frozen snapshot destinations; Catalogue Search generation reads canonical source and the browser reads only its Search projection.</li>\n<li>Search uses its own flattened representation instead of forcing list or selected-record payloads to carry search-only text.</li>\n</ul>\n<h2>Studio Read Models</h2>\n<p>Studio also uses <code>studio/data/generated/catalogue-lookup/</code> for Work and Series search plus Series-focused list data. Rich focused Work/detail payloads are built by the catalogue service at read time.</p>\n<p>This distinction is intentional:</p>\n<ul>\n<li>generated list data is cheap to serve and cache;</li>\n<li>service projections can combine current canonical data and generated context without creating another per-record file family;</li>\n<li>the browser still has one logical configuration map in <code>studio-config.json</code>.</li>\n</ul>\n<h2>Publication And Versioning</h2>\n<p>Public indexes include published runtime data and stable content versions needed by their consumers. Per-Work payloads carry confirmed media versions where primary remote media can be independently replaced. Aggregate indexes generally point to repo-owned thumbnails and do not need those primary-media version fields.</p>\n<p>Recent entries are publication snapshots rather than a normalized view of current titles. That is why recent history remains a separate artifact.</p>\n<h2>Retired Moments Boundary</h2>\n<p>There is no current public Moments route, Docs Viewer scope, Local Studio catalogue source, or editor family. The current catalogue search index contains Series and Works.</p>\n<p>Do not restore retired Moment catalogue payloads merely because historical delivery notes or generic fixtures still mention them. Follow the current route and scope registries.</p>\n<h2>Change Rule</h2>\n<ul>\n<li>unselected Series-list context: change the lean Series serializer;</li>\n<li>selected Series content: change the Series record serializer;</li>\n<li>selected Work or detail content: change the Work record/section serializer;</li>\n<li>Recent snapshot or destination context: change the Recent serializer;</li>\n<li>search-visible content: change the catalogue search builder;</li>\n<li>Studio-only editing/search context: change the lookup or focused service projection.</li>\n</ul>\n<p>Then update the field registry so build and lookup planning selects the affected artifact family.</p>\n"
}
