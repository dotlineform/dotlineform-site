{
  "doc_id": "d-20260813-165124-16b0ab",
  "title": "All-Scope Full-Text Search Baseline",
  "added_date": "2026-08-13 16:51:24",
  "last_updated": "2026-08-14 15:19:32",
  "viewer_url": "/docs/?scope=studio&doc=d-20260813-165124-16b0ab",
  "parent_id": "d-20260715-092247-6bbb5e",
  "summary": "give every Docs Viewer scope Studio's current full-text field policy before considering further search improvements",
  "content_html": "<h1>All-Scope Full-Text Search Baseline</h1>\n<h2>Purpose</h2>\n<p>Every configured Docs Viewer scope should use the full-text Search baseline already shipped for Studio. Today all scopes share one v2 index producer and browser query path, but Analysis, Notes, Processing, and Dotlineform still emit metadata-only postings. That exception is no longer useful.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260715-084611-c2fa6f\">SM-1 — Studio Full-Text Search Delivery</a> established the reusable extraction and ranking contract. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-225547-cd82d8\">DSI-2 — Scope-Level Sub-Scope Search Delivery</a> added eligible visibly hosted sub-scope documents to the same atomic scope index. The combined parent and sub-scope corpora now provide enough content to make full text the common baseline before any further Search improvement is considered.</p>\n<p>This is a policy alignment, not a new search feature or a separate Analysis experiment.</p>\n<h2>Common Baseline</h2>\n<p>Every configured Docs Viewer scope adopts exactly:</p>\n<pre><code class=\"language-json\">&quot;search_fields&quot;: [&quot;title&quot;, &quot;heading&quot;, &quot;body&quot;, &quot;code&quot;]\n</code></pre>\n<p>The existing shared behavior remains unchanged:</p>\n<ul>\n<li>title matches rank above heading matches, which rank above body and authored-code matches;</li>\n<li><code>doc_id</code>, URL, update date, parent identity, parent title, sub-scope identity, and collection title remain structural or display metadata and do not emit postings;</li>\n<li>visible headings, prose, inline code, and fenced code come from the shared Markdown extraction boundary;</li>\n<li>front matter, report declarations, link destinations, media paths, raw configuration, scripts, styles, and excluded documents do not contribute searchable terms;</li>\n<li>each scope owns one complete atomic index containing its eligible parent documents and eligible visibly hosted sub-scope documents; and</li>\n<li>exact <code>{scope, sub_scope, doc_id}</code> identity and the active-route resolver continue to own child navigation.</li>\n</ul>\n<p>There is no scope-name branch, second schema, alternate tokenizer, independent child index, or browser-side result merger. New Docs Viewer scopes should start from this same field policy unless a deliberate exception is approved.</p>\n<h2>Delivery</h2>\n<ol>\n<li>Change Analysis, Notes, Processing, and Dotlineform to the same <code>search_fields</code> policy as Studio.</li>\n<li>Explicitly rebuild the complete working Search index for every configured scope through the existing builder or Manage Rebuild control. Ordinary document saves remain Docs-only.</li>\n<li>Confirm every generated index declares only <code>title</code>, <code>heading</code>, <code>body</code>, and <code>code</code> posting fields and retains exact parent/sub-scope membership and routing identity.</li>\n<li>Run a small representative title, heading, prose, and code query set for each scope, including at least one visibly hosted sub-scope result where that scope has one.</li>\n<li>Inspect document, term, posting, generation-time, and raw-byte counts for obvious regressions. This alignment does not introduce per-scope payload budgets or optimization work.</li>\n<li>Preview and explicitly Publish the Analysis Search delta through the existing public Publish boundary. The public projection must not expose excluded documents or vocabulary.</li>\n</ol>\n<p>The baseline is complete when all configured working indexes and the accepted Analysis public projection use the same full-text field policy. Local and external-local scopes require no public projection step.</p>\n<h2>Deliberate Omissions</h2>\n<p>This baseline does not change relevance weights, prefix behavior, snippets, highlighting, phrase search, stemming, synonyms, typo handling, query language, section results, deep links, cross-scope Search, or Search presentation. It does not change parent Index or Recent composition.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260602-160839-4ebe27\">Catalogue Search</a> remains a separate domain-owned system and is not a Docs Viewer scope.</p>\n<p>Search improvements should be considered only after this common baseline is implemented, rebuilt, published where required, and accepted.</p>\n<h2>Relationships</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260602-160839-6d3cbb\">Docs Viewer Search</a> and <a href=\"/docs/?scope=studio&amp;doc=d-20260813-214350-4c7a21\">Docs Viewer Search Index</a> remain the durable current-state authorities. SM-1 owns the shared full-text extraction and ranking implementation. DSI-2 owns combined parent/sub-scope corpus membership and exact child routing. This document owns only the remaining all-scope field-policy alignment.</p>\n"
}
