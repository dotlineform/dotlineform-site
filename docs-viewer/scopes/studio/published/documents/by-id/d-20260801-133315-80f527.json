{
  "doc_id": "d-20260801-133315-80f527",
  "title": "Projects Architecture",
  "added_date": "2026-08-01 13:33:15",
  "last_updated": "2026-08-02 20:07:37",
  "viewer_url": "/docs/?scope=studio&doc=d-20260801-133315-80f527",
  "parent_id": "d-20260801-095819-30103c",
  "summary": "define Project document metadata, local folder-link projection, authority boundaries, and integration with the dedicated Project State feature",
  "ui_status": "proposed",
  "content_html": "<h1>Projects Architecture</h1>\n<h2>Purpose</h2>\n<p>This document is the implementation reference for Project documents, their optional folder links, and the boundary between Docs Viewer, Studio, and local folders. The derived relationship model now has its own owner: <a href=\"/docs/?scope=studio&amp;doc=d-20260802-121105-c574f9\">Project State Concept And Architecture</a>.</p>\n<p>The governing flow is:</p>\n<pre><code class=\"language-text\">Project source document -&gt; Projects customisation -&gt; namespaced folder_path projection\nbody path paste --------&gt; dlf-local link ---------&gt; confined Manage activation\n\nfolders + Projects Manage manifest + canonical Works/Series\n    -&gt; Project State generation\n    -&gt; saved Doc/Folder/Series report + folder resolver lookup\n</code></pre>\n<p>No arrow grants mutation authority over an input.</p>\n<h2>Authorities</h2>\n<table>\n<thead>\n<tr>\n<th>concern</th>\n<th>authority</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>local path containment and existence</td>\n<td>exact targets confined below <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code></td>\n</tr>\n<tr>\n<td>Project discovery and seed inventory</td>\n<td>configured discovery roots, initially <code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code></td>\n</tr>\n<tr>\n<td>Work source-path snapshot</td>\n<td>canonical Studio Work record</td>\n</tr>\n<tr>\n<td>Work-to-Series membership</td>\n<td>canonical Studio catalogue</td>\n</tr>\n<tr>\n<td>Project notes and collection membership</td>\n<td>document source in <code>dotlineform/projects</code></td>\n</tr>\n<tr>\n<td>literal convenience target</td>\n<td>optional Project document <code>folder_path</code> front matter</td>\n</tr>\n<tr>\n<td>normalized manifest projection and Projects controls</td>\n<td>registered Manage-only Projects sub-scope customisation</td>\n</tr>\n<tr>\n<td>folder relationship model, saved report, and Project lookup</td>\n<td>Project State producer</td>\n</tr>\n<tr>\n<td>Project resolver parsing/rendering and semantic usage</td>\n<td><a href=\"/docs/?scope=studio&amp;doc=d-20260802-174339-53877d\">Resolver Tokens Architecture</a> plus existing Semantic Tokens owners</td>\n</tr>\n<tr>\n<td>current image-level project audit and its retirement</td>\n<td>SSP-7</td>\n</tr>\n</tbody>\n</table>\n<h2>Studio Boundary</h2>\n<p><code>folder_path</code> and <code>project_folder</code> are not aliases. <code>folder_path</code> belongs only to Project document source and the namespaced Docs Viewer Manage manifest. PRJ-1 local links and PRJ-2 metadata/seeding add no Studio field, UI control, read path, write path, or service dependency.</p>\n<p>Project State has one explicit integration seam because Studio owns historical Work folder and Series evidence. The server-side post-commit observer reads exact committed Work before/after records and writes derived Docs Viewer state only. It never writes <code>folder_path</code>, rewrites <code>project_folder</code>, changes the catalogue transaction, or makes a successful Work commit contingent on follow-through.</p>\n<p>SSP-7 may read canonical Work media fields for its audit but likewise has no Studio mutation authority.</p>\n<h2>Project Document Data Contract</h2>\n<p><code>folder_path</code> is an optional normalized POSIX path relative to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code>:</p>\n<pre><code class=\"language-yaml\">folder_path: &quot;projects/3 symbols&quot;\n</code></pre>\n<p>The configured <code>dotlineform/projects</code> collection selects the Projects customisation. Its controlled projector adds the value only to the exact row's namespaced local management data:</p>\n<pre><code class=\"language-json\">{\n  &quot;doc_id&quot;: &quot;d-example&quot;,\n  &quot;title&quot;: &quot;3 symbols&quot;,\n  &quot;customisation&quot;: {\n    &quot;folder_path&quot;: &quot;projects/3 symbols&quot;\n  }\n}\n</code></pre>\n<p>The default manifest fields, public manifest, and public browser descriptor do not gain <code>folder_path</code>. Missing, malformed, duplicate, or non-resolving values remain reportable evidence and never alter collection membership.</p>\n<p>The Projects Edit Metadata contribution reads and writes the field only for an explicit validated <code>{scope, sub_scope, doc_id}</code> target. Creation and the one-off Apple Notes migration use the same normalizer and exact-target document writer.</p>\n<h2>Local Path Contract</h2>\n<p>The shared parser accepts local <code>file:</code> URLs, absolute POSIX paths with ordinary spaces, and absolute POSIX paths whose characters are backslash-escaped for shell display.</p>\n<p>Input is parsed as data. There is no shell evaluation, environment-variable or home expansion, command substitution, relative-path resolution, remote file authority, or non-<code>file:</code> scheme.</p>\n<p>Normalization strips the configured root and produces one decoded base-relative POSIX target plus an encoded link representation. Body Markdown uses <code>dlf-local:projects/3%20symbols</code>; structured metadata stores only <code>projects/3 symbols</code>. The Projects feature supplies <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code> as its allowed root. Metadata may save a non-existing contained target; activation requires the target to exist.</p>\n<p>A local Open in Finder service receives the exact relative target, resolves and reconfines it below the configured base root, and never falls back to a displayed document, parent collection, title, or URL text from a different target.</p>\n<p>Containment and discovery are separate policies. Links and <code>folder_path</code> may target any contained folder. Automatic Project candidate discovery scans only configured Project discovery roots, initially <code>projects/</code>; it does not recurse through Docs Viewer scopes, configuration, or every other base-root directory.</p>\n<p>PRJ-1 renders <code>dlf-local:</code> links as clickable only in local Manage. Public projection recognizes the marker and retains a safe inert label or placeholder while removing the marker and target. General redaction of absolute paths authored as prose, raw HTML, or code is a separate public-build concern.</p>\n<h2>Project State Integration</h2>\n<p>Projects supplies one input to Project State: the exact <code>dotlineform/projects</code> Manage manifest with its namespaced <code>folder_path</code> values. It does not own the normalized relationship schema, report source, resolver lookup, refresh policy, or stale-generation behavior.</p>\n<p>Project State owns the folder-centred relationship:</p>\n<pre><code class=\"language-text\">Project documents[] &lt;- folder_path -&gt; Series[]\n</code></pre>\n<p>Works remain historical derivation evidence. Individual Works are neither Project State report results nor initial Project resolver subjects/results. The saved report and lookup are generated from the same versioned row model, but downstream Resolver Tokens do not depend on the Markdown layout.</p>\n<p>Project-document create/delete and <code>folder_path</code> changes may trigger bounded Project State follow-through after the canonical Docs Viewer mutation. Failure does not undo or repeat that document mutation.</p>\n<h2>Project Media Audit Integration</h2>\n<p>The current Studio Project State report is actually an image-level audit. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-121106-15ea55\">SSP-7 — Project Media Audit Delivery</a> preserves that useful behavior as a separate generated Docs Viewer document and owns retirement of <code>/studio/project-state/</code>, its APIs/activity, generator placement, opener, and <code>var/studio/reports/project-state.md</code>.</p>\n<p>Project Media Audit does not write or enrich Project State rows. Project State does not inherit image-extension policy, image inventories, missing-image exceptions, or audit repair semantics.</p>\n<h2>Failure And Freshness</h2>\n<p>Project document, Studio Work, Project State, and Project Media Audit writes retain separate commit boundaries. A derived follow-through failure never rolls back or retries its source mutation. The prior complete derived generation remains available and reports stale state.</p>\n<p>Relationship freshness and rendered Resolver Token freshness are separate:</p>\n<pre><code class=\"language-text\">input mutation -&gt; Project State generation\ndocument rebuild -&gt; rendered Resolver Token output\n</code></pre>\n<p>Project State does not silently rebuild every document containing a Resolver Token. A dependent-document refresh requires its own explicit usage-owned workflow.</p>\n<h2>Architectural Invariants</h2>\n<ul>\n<li>There is no canonical Project-folder registry.</li>\n<li>Folder identity is explicit normalized path evidence, never inferred from title, Series name, filename, or UI context.</li>\n<li><code>folder_path</code> remains Docs Viewer-only and never becomes a Studio field or alias.</li>\n<li>Project document membership is independent of folder existence and <code>folder_path</code> validity.</li>\n<li>Local activation is exact, contained, and Manage-only.</li>\n<li>Project State has one dedicated data-model owner and one complete generated generation.</li>\n<li>Project Media Audit remains a separate read-only capability.</li>\n<li>Resolver expansion is one-hop and never refreshes Project State.</li>\n<li>No derived relationship or audit result authorizes mutation of a Work, document, Series, or folder.</li>\n</ul>\n<p>The <a href=\"/docs/?scope=studio&amp;doc=d-20260801-133314-870275\">Projects Concept</a> defines the wider three-world model. The <a href=\"/docs/?scope=studio&amp;doc=d-20260801-095819-30103c\">feature parent</a> owns delivery order. <a href=\"/docs/?scope=studio&amp;doc=d-20260801-150013-f26b87\">Local File And Folder Links Concept</a> owns reusable path and activation behavior.</p>\n"
}
