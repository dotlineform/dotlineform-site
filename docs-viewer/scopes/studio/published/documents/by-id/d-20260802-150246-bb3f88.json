{
  "doc_id": "d-20260802-150246-bb3f88",
  "title": "Content Detail View Concept And Architecture",
  "added_date": "2026-08-02 15:02:46",
  "last_updated": "2026-08-04 11:18:11",
  "viewer_url": "/docs/?scope=studio&doc=d-20260802-150246-bb3f88",
  "parent_id": "d-20260802-150245-4e7f85",
  "summary": "define one expanded main-panel view for exact static-source presentations, including table and diagram adapters, ordinary toolbar ownership, and flash-free document-directed entry",
  "ui_status": "in-progress",
  "content_html": "<h1>Content Detail View Concept And Architecture</h1>\n<h2>Purpose</h2>\n<p>Markdown tables and diagrams belong to documents, but some need substantially more space and interaction than flowing prose can provide. Content Detail View gives one exact presentation the main panel without leaving Docs Viewer or creating a second shell model.</p>\n<p>The architectural commitment is stronger than a visual preference: this is a registered main hosted view, not a full-screen modal disguised as a view. It participates in the current panel state, view lifecycle, control registry, main-view toolbar, route composition, and public/manage ownership boundaries.</p>\n<h2>Static Source Boundary</h2>\n<p>“Static source” means that the content belongs to the saved document rather than to a live report or application query. Its presentation may still be generated or rendered:</p>\n<table>\n<thead>\n<tr>\n<th>source presentation</th>\n<th>qualifies</th>\n<th>reason</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Markdown table</td>\n<td>yes</td>\n<td>authored document source renders a semantic HTML table</td>\n</tr>\n<tr>\n<td>configured SVG media token</td>\n<td>yes</td>\n<td>the document owns a stable presentation reference</td>\n</tr>\n<tr>\n<td>authored Mermaid fence rendered in the browser</td>\n<td>yes</td>\n<td>saved Mermaid source owns the presentation even though SVG is produced at mount time</td>\n</tr>\n<tr>\n<td>authored Mermaid projected to themed public SVG</td>\n<td>yes</td>\n<td>the public image is a projection of document-owned source</td>\n</tr>\n<tr>\n<td>dynamic Docs Viewer report list</td>\n<td>no</td>\n<td>the mounted report component owns live data, controls, and lifecycle</td>\n</tr>\n<tr>\n<td><code>docs_subscope</code> list/detail surface</td>\n<td>no</td>\n<td>the report owns collection state and selected detail presentation</td>\n</tr>\n</tbody>\n</table>\n<p>The table target scan occurs over explicitly marked tables in the rendered document content from the current payload before dynamic document extras mount. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-162149-3ba013\">TE-2</a> consumes the authoring comment and projects <code>data-docs-content-detail=&quot;table&quot;</code> onto the exact semantic table. Diagram adapters register only their already-supported rendered surfaces through the owning diagram lifecycle. Unmarked tables, report rows, and later component DOM are therefore not inferred as content-detail targets.</p>\n<h2>Existing Model Extension</h2>\n<p>Docs Viewer already has index, main, and info panels; registered main views for rendered documents, search, and Recent; hosted lifecycle; and the stable <code>main-view</code> toolbar surface. The feature adds:</p>\n<ul>\n<li>one <code>content-detail</code> main view definition;</li>\n<li>one transient exact target context supplied when that view is requested;</li>\n<li>one expanded-main layout state owned and projected by the existing panel layout;</li>\n<li>view-owned Back and presentation-action controls on the existing <code>main-view</code> surface; and</li>\n<li>focused table and diagram target adapters supplied through normal app composition.</li>\n</ul>\n<p>It does not add a panel, toolbar mount, modal host, route shell, or global detail-state service.</p>\n<pre><code class=\"language-text\">existing Docs Viewer shell\n  index panel\n  main panel\n    rendered-document view\n      exact table/diagram control\n        -&gt; request content-detail with validated target\n    content-detail view\n      existing main-view toolbar: Back | presentation actions\n      expanded main-panel presentation\n  info panel\n</code></pre>\n<h2>Expanded Main Layout</h2>\n<p>The detail view uses the <code>expanded-main</code> main-panel layout state because changing only the document content measure would leave the index and info rails consuming the viewport. Its behavior is:</p>\n<ul>\n<li>the existing main panel occupies the app's available width and usable viewport height;</li>\n<li>index and info presentation are temporarily absent from the projected layout without deleting their prior state;</li>\n<li>the top bar and <code>main-view</code> toolbar remain part of the ordinary shell;</li>\n<li>leaving Content Detail View restores the prior index state, info state, rendered-document view, scroll position, and invocation focus where still valid; and</li>\n<li>document or scope navigation invalidates the transient target and returns through the normal route workflow.</li>\n</ul>\n<p>This is an extension of panel layout projection. Individual table or diagram adapters must not toggle shell classes, hide panels, or own restoration independently.</p>\n<h2>Exact Target Context</h2>\n<p>One request identifies the current rendered presentation rather than searching again from titles, text, URLs, or arbitrary DOM:</p>\n<pre><code class=\"language-json\">{\n  &quot;documentTarget&quot;: {\n    &quot;scope&quot;: &quot;studio&quot;,\n    &quot;subScope&quot;: &quot;&quot;,\n    &quot;docId&quot;: &quot;d-example&quot;\n  },\n  &quot;documentMountGeneration&quot;: 42,\n  &quot;kind&quot;: &quot;table&quot;,\n  &quot;adapterTargetId&quot;: &quot;table-0&quot;,\n  &quot;occurrence&quot;: 1\n}\n</code></pre>\n<p>The representation is adapter-owned and immutable for a request. <code>adapterTargetId</code> is an opaque key into adapter-owned state; it has meaning only within the named document mount. The invariant is that the target belongs to the current document mount, has a supported content kind, and becomes invalid when that mount is replaced. <code>occurrence</code> is ephemeral presentation context for labels such as <strong>Table 2</strong>, not the mechanism used to search for the table or a persistent table identity.</p>\n<p>The hosted-view request contract carries the narrow immutable target context into lifecycle context while request reasons distinguish Back from document navigation and failure cleanup. It does not expose broad runtime state or create a second view registry. The view selects its explicitly composed table or diagram adapter from the exact target <code>kind</code>; a generic adapter registry requires separate evidence.</p>\n<h2>Table Registration And Presentation Construction</h2>\n<p>The table adapter queries only <code>table[data-docs-content-detail=&quot;table&quot;]</code> in the accepted rendered-document content before dynamic document extras mount. It does not parse authoring comments, measure table geometry, or decide whether a table is large. For each marked table it creates one record in a registry owned by that document mount:</p>\n<pre><code class=\"language-text\">document mount generation 42\n  table-0 -&gt; exact rendered &lt;table&gt; element\n  table-1 -&gt; exact rendered &lt;table&gt; element\n  table-2 -&gt; exact rendered &lt;table&gt; element\n</code></pre>\n<p>The adapter adds a component-level pill control using the SVG expand icon and an accessible <strong>Open table N</strong> label while it holds the exact element. The button closes over or otherwise carries the opaque record key; clicking it does not rescan by heading, cell text, occurrence, URL, selected document, or currently displayed DOM. Document replacement disposes the registry and makes every old key fail its mount-generation check.</p>\n<p><code>content-detail</code> owns the generic hosted-view lifecycle, expanded shell, toolbar surface, and presentation mount. It does not understand table rows or construct table markup. After validating the target, it asks the active table adapter to mount the presentation for that record.</p>\n<p>The initial table adapter constructs the presentation from the already-rendered semantic DOM, not from Markdown or an HTML string. The expected shape is:</p>\n<pre><code class=\"language-text\">content-detail body\n  bounded table viewport\n    cloned semantic &lt;table&gt;\n</code></pre>\n<p>The adapter clones the accepted table, removes document-only controls and transient markers, applies the small presentation class/label set, and strips or remaps duplicate IDs while preserving valid <code>headers</code>, ARIA, link, and text relationships. The original rendered document remains intact for <strong>Back to document</strong>. The registry retains the exact accepted element until entry, and the adapter clones it only after revalidating the current document target, mount generation, kind, and opaque record. It does not independently regenerate cell meaning.</p>\n<p>Most improvement comes from context and CSS rather than new HTML: the expanded panel supplies space; the viewport supplies unified horizontal and vertical scrolling; and table styles supply natural widths up to a <code>36ch</code> default maximum per column, wrapping for long text/links/code, sticky header cells, sticky first-column cells, and the top-left intersection layer. Manage may extend the mounted presentation through the table adapter's optional mount, activate, and release lifecycle; TE-3 uses that seam for adapter-owned resize handles and a transient <code>&lt;colgroup&gt;</code> on the detail clone without changing the source table or generic view.</p>\n<h2>Main-View Toolbar</h2>\n<p>Controls that act on Content Detail View belong to the existing <code>main-view</code> surface with <code>ownerViewId: &quot;content-detail&quot;</code>. Eligibility and live state may additionally hide controls that do not apply to the active content kind.</p>\n<p>Shared controls include:</p>\n<ul>\n<li><strong>Back to document</strong> — returns through the main-view coordinator;</li>\n<li>a concise presentation label such as <strong>Table 2</strong> or the diagram title; and</li>\n<li>presentation-specific controls contributed by the focused adapter.</li>\n</ul>\n<p>TE-2 gives a static table only Back and its presentation label. TE-3 contributes an icon-only <strong>Copy table</strong> button and <strong>Reset widths</strong> from the Manage entrypoint; the copy button uses the normal toolbar icon-button treatment, a code-owned inline overlapping-rectangles SVG, and <strong>Copy table</strong> as its accessible label and tooltip. A resize handle inside a header cell remains a component control. TE-4 contributes an icon-only pill link using the table detail control's external-open glyph, <strong>Open in new tab</strong> as its accessible label and tooltip, and the active adapter's stable URL, public theme variant, or temporary SVG resource.</p>\n<p>Later successful copy or reset actions require no status message. Clipboard failure remains explicit through the existing status/error surface.</p>\n<h2>Table Presentation Example</h2>\n<p>An author marks a table in canonical Markdown:</p>\n<pre><code class=\"language-markdown\">&lt;!-- dotlineform:table-detail --&gt;\n\n| Project | Folder | Series |\n| --- | --- | --- |\n| Nerve | projects/nerve | Series 105 |\n</code></pre>\n<p>When the table-detail consumer recognises the exact adjacent comment, the Docs build projects the narrow eligibility marker onto the same semantic table; removing the ordinary invisible comment from rendered HTML is unnecessary. The rendered document then adds the pill-shaped expand icon control with its accessible table label. Invoking it changes the active main view:</p>\n<pre><code class=\"language-text\">main-view toolbar\n  Back to document     Table 2\n\ncontent-detail body\n  +-------------------------------------------------------------+\n  | sticky header                                               |\n  +----------------+----------------------+---------------------+\n  | sticky column  | horizontally and vertically scrollable     |\n  |                | table body                                 |\n  |                |                                            |\n  +----------------+----------------------+---------------------+\n</code></pre>\n<p>The initial table contract is:</p>\n<ul>\n<li>require the projected table-detail marker rather than applying a size or overflow heuristic;</li>\n<li>use the semantic HTML table produced from Markdown;</li>\n<li>use natural content width below a <code>36ch</code> default maximum per column rather than forcing either the table into the prose measure or every row onto one unwrapped line;</li>\n<li>allow long text, links, and inline code to wrap inside that default maximum;</li>\n<li>use one bounded viewport for horizontal and vertical scrolling;</li>\n<li>keep the header row and first column visible, including the top-left intersection;</li>\n<li>keep the static presentation free of resizing, export, and persisted table state; and</li>\n<li>preserve ordinary links and text selection.</li>\n</ul>\n<p>The first delivery uses bounded natural sizing, full available width, sticky context, and unified scrolling before adding user-controlled widths or other tools.</p>\n<h2>Dedicated Table Documents And Default Entry</h2>\n<p>A large table may live in an ordinary child document while its parent retains a summary table and a normal document link such as <strong>More…</strong>. TE-5 proposes one public-safe scalar front-matter hint for a dedicated document:</p>\n<pre><code class=\"language-yaml\">viewer_content_detail: table\n</code></pre>\n<p>The exact field name is confirmed in TE-5.0. Its initial contract supports only a document with exactly one authored Markdown table and implicitly projects the same table-detail eligibility marker without requiring a redundant comment. The hint is projected into document-index metadata as well as the document payload so the router knows the intended initial presentation before a cold payload fetch or cached render begins. Child status is conventional organisation, not a runtime requirement, and the parent link needs no special component or table target.</p>\n<p>The initial contract applies only to indexed scope documents. A sub-scope document remains embedded detail owned by its <code>docs_subscope</code> report: it is absent from the parent index, the parent report remains the selected Docs Viewer document, and its <code>subdoc</code> payload renders after the report mounts. Sub-scope content is not intrinsically ineligible, but supporting it would require a separate report-owned hint projection, preflight, exact <code>{scope, sub_scope, doc_id}</code> handoff, and report-state restoration design.</p>\n<p>The hint must not be implemented as an after-render click. Navigation first projects the final expanded-main layout and an inert pending content state. The existing Markdown pipeline then supplies <code>content_html</code> to a non-visible preparation mount, where the TE-2 table adapter registers the exact rendered table. One coordinated commit mounts that target in <code>content-detail</code> and reveals it; the ordinary rendered document is never visible in between and the outer panel geometry does not resize after content appears.</p>\n<p><strong>Back to document</strong> reveals the already-prepared ordinary document without refetching it and records a suppression decision for the current document visit so the default does not immediately reopen. A later fresh navigation or reload may apply the authored default again. Missing or ambiguous targets fail explicitly and atomically to the ordinary document rather than leaving a blank or partially expanded view.</p>\n<p>This remains presentation of rendered HTML, not a Markdown reparse, direct table URL, persistent table identity, dynamic report, or second document renderer.</p>\n<h2>Managed Table Tools</h2>\n<p>TE-3 extends the same active table presentation through Manage composition. It adds pointer and keyboard-accessible header-boundary resizing, session-local Reset, and <strong>Copy table</strong> on the existing main-view toolbar. The shared adapter owns the optional presentation-extension lifecycle; Manage owns the tool implementation, transient widths, toolbar contributions, clipboard request, and failure projection. Back, navigation, target replacement, and disposal release the extension state. TE-3 does not register a second view or replace the shared table renderer.</p>\n<p><strong>Copy table</strong> writes plain-text TSV for direct spreadsheet paste: header cells are included, cells use tab delimiters, rows use line feeds, cell-internal whitespace and line breaks normalize to spaces, empty cells retain their positions, and links contribute their visible labels. Serialization never reads Markdown source or a report data model; it uses the exact table presentation accepted by the view. Persistent widths, downloaded table files, and public promotion remain outside TE-3.</p>\n<h2>Diagram Presentation Example</h2>\n<p>The existing persistent and inline diagram wrappers already identify supported diagram surfaces. TE-4 changes their primary detail action from an external target to an exact <code>content-detail</code> request:</p>\n<pre><code class=\"language-text\">rendered document diagram\n  Open diagram\n    -&gt; content-detail(kind=diagram)\n       -&gt; expanded main panel\n       -&gt; Back to document\n       -&gt; optional Open in new tab\n</code></pre>\n<p>Persistent SVG continues to use its stable served target. Inline Mermaid continues to use the adapter-owned rendered SVG and temporary-resource lifecycle. Public themed Mermaid continues to use its active light or dark projection. Content Detail View changes presentation ownership; it does not add another Mermaid renderer, source format, media identity, or publication path.</p>\n<h2>Access And Composition</h2>\n<p>The main-view and panel-layout primitive should remain public-safe because it carries presentation and no management authority. Each route composition still decides which target adapters and controls it imports:</p>\n<ul>\n<li>TE-2 explicitly composes the static table view in public and Manage readers through a public-safe shared module and public import-boundary evidence;</li>\n<li>TE-3 contributes resizing and <strong>Copy table</strong> only from the Manage entrypoint unless a later public promotion is separately approved;</li>\n<li>TE-4 explicitly composes the shared diagram adapter in public and Manage readers while Manage retains inline Mermaid rendering and temporary-resource authority; and</li>\n<li>Review remains excluded until its returned-package rendering and navigation context are deliberately assessed.</li>\n</ul>\n<p>Registering or showing a detail presentation never grants document, clipboard, source, filesystem, or management authority. The public static view reads only the already-rendered public table. Clipboard access exists only when TE-3 contributes the explicit Copy action.</p>\n<h2>State And Failure</h2>\n<ul>\n<li>Only one Content Detail target is active; nested detail views are unsupported.</li>\n<li>A stale mount generation, missing presentation, unsupported kind, or unavailable adapter rejects entry and leaves the rendered document usable.</li>\n<li>Failure while mounting the detail view returns to the rendered document with an explicit error; it does not navigate away or fall back to an arbitrary presentation.</li>\n<li>Back and normal document navigation release adapter-owned resources and transient width/scroll state.</li>\n<li>Source rebuild or theme re-render invalidates or refreshes the target only through the owning document/diagram lifecycle; the content-detail view does not rescan the document.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>a modal, overlay, popup window, secondary app shell, or independent navigation stack;</li>\n<li>editing Markdown, table cells, diagrams, or canonical source from the detail view;</li>\n<li>spreadsheet formulas, sorting, filtering, grouping, row selection, charts, or virtualized data grids;</li>\n<li>persistent column widths or stable table IDs;</li>\n<li>automatic table eligibility based on dimensions, overflow, row count, headings, or viewport;</li>\n<li>default Content Detail presentation for documents with zero, multiple, dynamic, or unsupported targets;</li>\n<li>dynamic report lists, report toolbars, search results, Recent, or sub-scope collection and embedded-detail presentation;</li>\n<li>arbitrary raw page tables or SVG outside the accepted current document mount; or</li>\n<li>a general content-plugin registry before another proven content kind needs it.</li>\n</ul>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260528-224754-964108\">Panel Hosts and Control Surfaces</a> owns the existing lifecycle and control architecture. <a href=\"/docs/?scope=studio&amp;doc=d-20260719-123719-fb7565\">Document Diagrams</a> owns diagram source/rendering contracts. The <a href=\"/docs/?scope=studio&amp;doc=d-20260802-150245-4e7f85\">Table Enhancements feature</a> owns delivery order.</p>\n"
}
