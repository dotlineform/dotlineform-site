{
  "doc_id": "d-20260813-171341-8bfa80",
  "title": "DSI-1 — Docs Viewer Search Index v2 Delivery",
  "added_date": "2026-08-13 17:13:41",
  "last_updated": "2026-08-13 19:52:25",
  "viewer_url": "/docs/?scope=studio&doc=d-20260813-171341-8bfa80",
  "parent_id": "d-20260715-092247-6bbb5e",
  "summary": "migrate every Docs Viewer scope to one field-aware inverted index and one browser query path before Studio full-text adoption",
  "ui_status": "in-progress",
  "content_html": "<h1>DSI-1 — Docs Viewer Search Index v2 Delivery</h1>\n<h2>Status</h2>\n<p>In progress. DSI-1.2 is complete and parked at the code-review gate. Every configured runtime and generated search index now uses v2; DSI-1.3 has not begun.</p>\n<h2>Complete Result</h2>\n<p>Every configured Docs Viewer scope uses one versioned inverted-index structure and one browser query and ranking path. Each scope still controls which source fields are searchable, but index structure is not a scope-specific choice. The runtime retains no v1 reader, format fallback, or Studio-only engine.</p>\n<p>DSI-1 completes before <a href=\"/docs/?scope=studio&amp;doc=d-20260715-084611-c2fa6f\">SM-1 — Studio Full-Text Search Delivery</a>, which then adds headings, body prose, and authored code only to Studio's field policy.</p>\n<h2>Boundary</h2>\n<p>DSI-1 includes the common document table and field-aware postings shape, shared tokenizer contract, per-scope field-policy seam, whole-index search rebuild behavior, one browser query/ranking path, coordinated migration of every configured Docs Viewer search index, and the metadata-only Analysis public projection.</p>\n<p>It preserves each scope's accepted retrieval fields during the structural migration. It does not add body-text retrieval to any scope, change Catalogue Search, retain a dual-format transition reader, introduce an incremental postings cache, or redesign search results.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260813-161942-b1e9e1\">Studio Full-Text Search Index</a> explains the inverted structure. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-163802-5ca41f\">Docs Viewer Search Tokenizer</a> owns term semantics. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-165124-16b0ab\">Analysis Full-Text Search Extension</a> distinguishes this metadata-only schema migration from later public body-text adoption.</p>\n<h2>Delivery Steps</h2>\n<p>The verification tables are initial ceilings, not quotas. Confirm or reduce the relevant table after inspecting that step's exact owners; expand it only when implementation reveals a new credible risk and the step is updated before proceeding.</p>\n<h3>DSI-1.0 — Readiness And Sequence</h3>\n<ul>\n<li>[x] Confirm the current Docs Viewer builder, browser query path, scope-policy owner, targeted-search orchestration, configured scope set, and Analysis public output at broad-owner level.</li>\n<li>[x] Confirm each scope's accepted searchable fields as the migration baseline without freezing final code, fixtures, schema names, or file inventory.</li>\n<li>[x] Confirm that every currently served scope can cut over together without a retained v1 reader or unavailable external-scope gap.</li>\n<li>[x] Confirm DSI-1.1 can implement the shared transformation/query contract without changing active generated output, and DSI-1.2 can perform the coordinated producer/consumer cutover.</li>\n<li>[x] Record only genuine ownership, migration, public-projection, or sequencing stop conditions, then stop for implementation approval.</li>\n</ul>\n<p>No executable verification is budgeted for this read-only step. Source/config/document inspection is sufficient; prototypes, generated deltas, test changes, and a recognisable production diff belong to DSI-1.1 or DSI-1.2.</p>\n<p><strong>Gate:</strong> Review the readiness result and linear sequence before any production or test implementation begins.</p>\n<p><strong>Record:</strong> Completed 2026-08-13. <code>build_search.py</code> is the single producer and currently hard-codes one v1 metadata policy for every scope; <code>docs_scopes.json</code> and <code>docs_scope_config.py</code> own scope identity, membership exclusions, routes, and output locations but have no explicit search-field policy. <code>docs-viewer-search.js</code> is the single browser normalization/ranking owner and <code>docs-viewer-search-controller.js</code> is its shared loader and UI caller. <code>docs_write_rebuild.py</code> and <code>docs_live_rebuild_watcher.py</code> own targeted search execution, while mutation plans supply affected identities.</p>\n<p>The configured set is Studio, Analysis, Notes, Processing, and dotlineform. All five source roots and working search indexes were available during readiness, including both external-local roots. All five currently accept the same retrieval fields: title, parent title, update metadata, and exact full document identity; identity fragments are not ordinary terms. The proposed minimal stop-word list remains suitable for representative metadata across the five scopes, and lifecycle-bearing words such as <code>before</code>, <code>after</code>, and <code>while</code> remain indexed.</p>\n<p>Analysis working search is copied unchanged to <code>site/assets/data/search/analysis/index.json</code>. The public publish gate is format-agnostic, but <code>docs_document_location_projection.py</code> and <code>docs_public_delete_cleanup.py</code> currently read or filter <code>entries</code>; DSI-1.2 must move those identity-only consumers to the v2 document table and remap postings during public deletion rather than retain <code>entries</code> compatibility.</p>\n<p>The linear sequence remains valid. DSI-1.1 can add the explicit scope policy plus pure Python transformation/tokenizer and pure JavaScript tokenizer/query contract behind the inactive v1 producer/consumer, using one shared fixture. The expected review shape is roughly 250-350 production/config lines and 200-300 focused fixture/test lines across the existing scope-config, builder, and shared runtime owners; material expansion beyond that shape requires another review. DSI-1.2 then switches the producer, browser loader/query caller, expected schema, whole-index orchestration, Analysis auxiliary consumers, all five working indexes, and the tracked Analysis public index together, with no v1 reader.</p>\n<p>Two sequencing conditions are explicit. Exact full-ID lookup is retained as structural identity handling outside ordinary tokenizer terms despite the broader full-text concept's statement that <code>doc_id</code> is not searchable. Both external-local roots must still be available at DSI-1.2; otherwise the all-scope cutover stops rather than leaving a v1 scope or adding a fallback reader. Gate outcome: ready for DSI-1.1 implementation approval; no production code, tests, prototypes, or v2 outputs were changed or run. The routine docs watcher regenerated only this delivery record's existing Studio document and v1 search projections.</p>\n<h3>DSI-1.1 — Shared V2 Transformation And Query Contract</h3>\n<ul>\n<li>[x] Add one explicit Docs Viewer search-field policy seam that can preserve each configured scope's accepted retrieval fields.</li>\n<li>[x] Implement the shared tokenizer contract and deterministic document-table/field-postings transformation.</li>\n<li>[x] Implement one pure postings-based browser query, ranking, and tie-breaking path against the v2 structure.</li>\n<li>[x] Make the v2 search builder treat a search-relevant document change as a whole-index operation while preserving skip-on-identical-output behavior.</li>\n<li>[x] Keep the active v1 producer/consumer seam unchanged until the coordinated DSI-1.2 cutover.</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>contract or credible risk</th>\n<th>lowest adequate evidence</th>\n<th>permanent test change?</th>\n<th>why this evidence is needed</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>build-time and query-time token normalization diverge</td>\n<td>one shared representative fixture exercised by focused Python and direct JavaScript module assertions</td>\n<td>extend focused checks; no browser test</td>\n<td>catches terms that can be emitted but never queried, or queried but never emitted</td>\n</tr>\n<tr>\n<td>document positions, field postings, intersection, rank, and tie-breaking are nondeterministic</td>\n<td>focused pure builder and query assertions over a small multi-document corpus</td>\n<td>extend the current builder test and the smallest available pure runtime check</td>\n<td>proves the common data and ranking contract without route choreography</td>\n</tr>\n<tr>\n<td>a targeted v2 request patches postings or leaves stale removal state</td>\n<td>focused builder behavior covering edit, create, delete, and explicit whole-index fallback</td>\n<td>extend the current search-builder test</td>\n<td>protects the global postings invariant without an incremental-cache suite</td>\n</tr>\n</tbody>\n</table>\n<p><strong>Gate:</strong> The shared v2 owners and focused evidence are reviewable, but no configured runtime or generated index has cut over. Stop before DSI-1.2.</p>\n<p><strong>Record:</strong> Completed 2026-08-13. <code>docs_scope_config.py</code> now normalizes one flat <code>search_fields</code> tuple and all five configured scopes explicitly retain <code>title</code>, <code>parent_title</code>, <code>identity</code>, and <code>last_updated</code>. <code>build_search.py</code> contains inactive v2 tokenizer, deterministic document-table/postings transformation, and whole-index build methods. <code>docs-viewer-search.js</code> contains the matching inactive browser tokenizer, postings intersection, ranking, and deterministic tie-breaking exports. One shared JSON fixture drives focused Python and direct JavaScript assertions.</p>\n<p>The first implementation draft reached 500 production/config lines and was paused at the size-review condition. On user review it was reduced to 311 production/config insertions and 215 focused fixture/test lines: no inactive loader or validation framework, no generic policy registry, and no second query owner. Exact identity and update metadata remain explicit fields outside ordinary tokenizer terms. A v2 request carrying changed document IDs always reconstructs the complete index; edit, create, delete, deterministic ordering, and identical-version skipping are covered without adding an incremental cache.</p>\n<p>Focused evidence passed: <code>pytest -q docs-viewer/tests/python/test_docs_scope_config.py docs-viewer/tests/python/test_build_search_python.py</code> reported 59 passed; <code>node --experimental-default-type=module docs-viewer/tests/js/docs_viewer_search_v2_contract.mjs</code> reported the JavaScript contract OK; explicit Python and JavaScript lint passed; Python <code>py_compile</code> passed; all five configured policies loaded through an isolated writable Projects base; and <code>git diff --check</code> passed. A direct all-scope read against the real external workspace was unavailable under the sandbox's write-capability check, so DSI-1.2 retains its readiness requirement to confirm both external-local roots before cutover.</p>\n<p>The active CLI still calls the v1 producer and <code>docs-viewer-search-controller.js</code> still calls the v1 runtime functions. No watcher/write-service orchestration, public projection consumer, browser controller, generated search index, or Catalogue Search owner changed. Gate outcome: DSI-1.1 is ready for review; stop before DSI-1.2.</p>\n<h3>DSI-1.2 — Atomic Runtime And All-Scope Cutover</h3>\n<ul>\n<li>[x] Wire the shared v2 transformation into the Docs Viewer search builder and the single browser query path.</li>\n<li>[x] Update expected schemas and scope policy without hard-coded Studio or Analysis query branches.</li>\n<li>[x] Convert existing targeted search orchestration to an explicit whole-index v2 search rebuild while leaving safe document-payload builds targeted.</li>\n<li>[x] Regenerate every configured Docs Viewer search index and the tracked metadata-only Analysis public projection in the same cutover.</li>\n<li>[x] Remove the v1 reader and obsolete <code>entries</code>, <code>search_terms</code>, and <code>search_text</code> production handling rather than retaining compatibility code.</li>\n<li>[x] Confirm Catalogue Search production and generated files remain outside the change.</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>contract or credible risk</th>\n<th>lowest adequate evidence</th>\n<th>permanent test change?</th>\n<th>why this evidence is needed</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>configured scopes emit different structures or lose approved metadata retrieval</td>\n<td>focused builder/config tests plus exact JSON/schema assertions for the regenerated scope indexes</td>\n<td>update existing builder/config contracts</td>\n<td>proves one structure and preserves scope-owned field policy</td>\n</tr>\n<tr>\n<td>watcher or write-service orchestration rebuilds too little or rebuilds every document payload</td>\n<td>focused existing orchestration tests for targeted docs plus whole v2 search</td>\n<td>update existing watcher/write-service checks only where behavior changes</td>\n<td>protects the intended split between targeted document output and global search output</td>\n</tr>\n<tr>\n<td>Analysis publishes excluded identities or new body vocabulary</td>\n<td>exact public-projection comparison and the narrowest existing publication-boundary assertion</td>\n<td>extend an existing projection contract only if it owns this payload</td>\n<td>protects the public corpus boundary introduced by the schema migration</td>\n</tr>\n<tr>\n<td>shared runtime cannot load and query v2 on local and public routes</td>\n<td>direct module/payload integration plus one temporary manual check of <code>/docs/</code> and <code>/analysis/</code></td>\n<td>no permanent browser test unless a browser-only failure is discovered and separately justified</td>\n<td>proves the producer/consumer agreement without adding UI choreography coverage</td>\n</tr>\n<tr>\n<td>Catalogue Search is changed by the Docs Viewer migration</td>\n<td>bounded path/diff inspection</td>\n<td>none</td>\n<td>ownership isolation is visible directly and needs no synthetic test</td>\n</tr>\n</tbody>\n</table>\n<p><strong>Gate:</strong> All configured Docs Viewer indexes and consumers use v2, focused evidence passes, and no v1 reader remains. Stop for the explicit code-review step.</p>\n<p><strong>Record:</strong> Completed 2026-08-13. <code>build_search.py</code> now emits only <code>docs_viewer_search_index_v2</code>; the shared browser controller loads that schema directly and calls the sole postings query path. The old entries normalizer, v1 scoring, targeted search CLI, and <code>search_terms</code>/<code>search_text</code> production handling were removed. Browser config declares the common schema plus <code>rebuild_policy: whole_index</code> for every scope, while watcher and write-service flows still select document payloads narrowly and run one complete search build when affected identities are non-empty.</p>\n<p>Studio, Analysis, Notes, Processing, and dotlineform were rebuilt together after both external-local roots passed live preflight. Their <code>(documents, terms, bytes)</code> measurements were respectively <code>(236, 630, 140666)</code>, <code>(17, 66, 10538)</code>, <code>(244, 833, 174908)</code>, <code>(137, 486, 87038)</code>, and <code>(9, 33, 4887)</code>. Analysis working search was copied byte-for-byte to the tracked public index; its identity-only location projection rebuilt to 266 records. The only added cutover complexity was remapping v2 postings positions when immediate public Delete removes an Analysis document.</p>\n<p>Focused evidence passed: 185 Python checks across the builder/config, orchestration, Analysis projection/Delete/Publish, generated-read, move/transfer, and generated-contract owners; the shared direct JavaScript contract; focused Python and JavaScript lint; and an actual-payload module check for Studio and Analysis. Temporary local route checks returned <code>DSI-1 — Docs Viewer Search Index v2 Delivery</code> for <code>/docs/?scope=studio&amp;q=search+index</code> and <code>Analysis</code> plus <code>Concepts</code> for <code>/analysis/?q=analysis</code>, with no console errors. The five generated search-policy records matched the builder exactly, Analysis working/public indexes matched byte-for-byte, and bounded diff inspection found no Catalogue production or generated path. Gate outcome: DSI-1.2 is complete and stopped for the explicit DSI-1.3 code review.</p>\n<h3>DSI-1.3 — Code Review</h3>\n<ul>\n<li>[ ] Review the bounded production, config, test, documentation, and generated diff independently after DSI-1.2 evidence.</li>\n<li>[ ] Check specifically for a hidden second query path, scope-name branching, duplicated tokenizer rules, compatibility residue, stale targeted-postings behavior, public Analysis leakage, and Catalogue ownership drift.</li>\n<li>[ ] Review the test diff for duplicated cases, unnecessary fixtures or frameworks, browser choreography, broad-profile expansion, and checks that cannot name a unique regression.</li>\n<li>[ ] Record every finding and its resolution or explicitly accepted omission.</li>\n<li>[ ] Rerun only the verification-budget rows affected by review changes, then stop for review approval.</li>\n</ul>\n<p>No additional evidence category is budgeted here. A review finding may require updating the affected implementation step's table before new tests or broader checks are added.</p>\n<p><strong>Gate:</strong> Present the actual code-review findings, resolutions, final bounded diff, and remaining risks before closeout.</p>\n<p><strong>Record:</strong> Pending. Add findings by severity, resolutions, affected reruns, and the gate outcome.</p>\n<h3>DSI-1.4 — Closeout And Durable Transfer</h3>\n<ul>\n<li>[ ] Confirm the complete result and every completion claim against the accepted implementation and review records.</li>\n<li>[ ] Update <a href=\"/docs/?scope=studio&amp;doc=d-20260602-160839-6d3cbb\">Docs Viewer Search</a> with the shipped v2 schema, field policy, build behavior, and browser query path.</li>\n<li>[ ] Update <a href=\"/docs/?scope=studio&amp;doc=d-20260331-000000-5dcf32\">Search</a> only if its Docs Viewer/Catalogue authority map changed materially.</li>\n<li>[ ] Mark DSI-1 complete, make SM-1 eligible for readiness, and record remaining Analysis full-text and other scope-field decisions as separate outcomes.</li>\n<li>[ ] Present the retain-or-retire recommendation for temporary search concept and delivery documents without deleting them.</li>\n</ul>\n<p>No implementation retest is automatic at closeout. Run only documentation projection checks made necessary by the closeout edits.</p>\n<p><strong>Gate:</strong> Stop for explicit closeout approval.</p>\n<p><strong>Record:</strong> Pending. Add durable destinations, status changes, named omissions, closeout-only evidence, and the gate outcome.</p>\n"
}
