{
  "doc_id": "d-20260810-173738-703ecd",
  "title": "Report Capability and Composition",
  "added_date": "2026-08-10 17:37:38",
  "last_updated": "2026-08-11 11:48:40",
  "viewer_url": "/docs/?scope=studio&doc=d-20260810-173738-703ecd",
  "parent_id": "d-20260513-105343-6de36b",
  "summary": "durable module, data, contribution, and stylesheet boundaries for shared, public, and local Docs Viewer reports",
  "content_html": "<h1>Report Capability and Composition</h1>\n<h2>Purpose</h2>\n<p>This document owns the durable boundary between genuinely shared report capability, public report composition, and local-only report composition.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260513-105343-6de36b\">Reports</a> owns report metadata, the registry, current report behavior, and the implemented report inventory. Individual feature documents own changes to a report or its presentation. They reference this document rather than redefining the public/local boundary.</p>\n<p>The governing rule is:</p>\n<blockquote>\n<p>Public reports are a separate module graph, not local reports with capabilities switched off.</p>\n</blockquote>\n<p>That graph cannot be determined from a module's path alone. Public safety, public composition, and public adoption are separate decisions and must be stated separately.</p>\n<h2>Current Position</h2>\n<p>The current public report registry and executable loader allowlist expose only <code>docs_subscope</code>. Its shared report reads a public manifest and exact by-ID payloads, owns collection membership and detail validation, and imports no management module.</p>\n<p>Manage loads the same shared sub-scope report and explicitly supplies management contributions. Those contributions own private inventory presentation, selection, actions, source and metadata bridges, packages, and collection-specific browser customisations.</p>\n<p>The public sub-scope customisation loader registry is empty. <code>analysis_tags</code> and <code>dotlineform_projects</code> have Manage-only browser contributions. <code>analysis_works</code> has service-owned subject and lineage aspects but no browser report contribution.</p>\n<p>A report block's <code>access: public</code> remains access intent. It does not project metadata, install a loader, publish data, move CSS, or make a module executable.</p>\n<p>Expanded Report View currently demonstrates the distinction directly. Its adapter is public-safe, publicly addressable, and listed in the public runtime inventory, but it is absent from the public entrypoint's transitive import graph and its selectors are absent from the public-loaded shared report stylesheet. Manage imports the exact adapter and loads its selectors from the local report stylesheet. The capability is therefore composed only by Manage and is not publicly downloaded. No report currently returns its <code>expandedPresentation</code> handle, so it is adopted nowhere; the sole executable public report remains <code>docs_subscope</code>.</p>\n<h2>Safety, Composition, And Adoption</h2>\n<p>These states are independent:</p>\n<ul>\n<li><strong>Public-safe</strong> means an implementation and its transitive dependencies can execute over public browser data without importing local services, management modules, private paths, write authority, or management CSS. Safety is a property of the implementation; it does not cause the browser to load anything.</li>\n<li><strong>Publicly composed</strong> means a public entrypoint or one of its transitive imports loads and wires the implementation, or a public route shell loads a stylesheet containing its selectors. Composed JavaScript is downloaded and evaluated according to normal module and cache behavior even when no report uses it. Composed CSS contributes bytes to the loaded stylesheet even when none of its selectors match.</li>\n<li><strong>Publicly adopted</strong> means at least one explicitly promoted public report, contribution, or route actually invokes the composed capability. Report adoption additionally requires the public data, metadata, loader, and behavior contracts owned below.</li>\n</ul>\n<p>Physical location is not composition evidence. A JavaScript file under <code>site/docs-viewer/runtime/js/shared/</code> is publicly addressable and eligible for a public graph, but it is not downloaded merely because it exists there. Its public composition is determined by the transitive import graph rooted at the public entrypoint. A file under <code>docs-viewer/</code> may be local-only, but Manage can also import site-owned shared modules through its explicit runtime mapping. For CSS, the equivalent evidence is the ordered stylesheet list in the route shell and the selectors contained by each loaded file.</p>\n<p>Public safety is a prerequisite for public composition, not authorization for it. A capability with no named current public adopter should remain absent from the public import graph and public-loaded stylesheets. It may remain implemented in a public-safe owner and be composed only by Manage. A later public report adopts it through an explicit promotion slice that adds public composition and verifies the complete import, stylesheet, data, metadata, and loader boundary.</p>\n<p>The same distinction applies locally. A public-safe shared implementation can be composed by Manage without becoming publicly composed, and a local report can adopt that implementation without creating a public report or predicting that one will ever exist.</p>\n<h2>Runtime Layers</h2>\n<h3>Shared report capability</h3>\n<p>A capability implementation can be public-safe and reusable before both shells compose it. It is genuinely shared in runtime composition only when the same implementation is both composed and useful in public and local routes.</p>\n<p>Shared report code may own:</p>\n<ul>\n<li>exact report mount, loading, error, refresh, and unmount lifecycle;</li>\n<li>accessible list, table, detail, and expanded presentation structure;</li>\n<li>read-only filtering, sorting, paging, browser-local presentation state, and report-owned route state;</li>\n<li>exact collection, row, and detail identity validation;</li>\n<li>stable column identity and a representation-owned layout surface;</li>\n<li>detached contribution hosts and narrow lifecycle notifications; and</li>\n<li>adapters over explicit browser-safe read models.</li>\n</ul>\n<p>Shared report code must not import or resolve local services, source paths, filesystem targets, private Studio lookups, mutation clients, selection actions, package workflows, or management CSS.</p>\n<p>Shared capability does not require one generic report engine. A report may keep a focused shared module when its data, presentation, and lifecycle are specific to that report.</p>\n<p>Keeping a public-safe implementation in a site-owned shared module does not require the public entrypoint to import it. Manage may be its only current composer and adopter.</p>\n<h3>Public composition</h3>\n<p>Public composition owns:</p>\n<ul>\n<li>the public report entrypoint and executable loader allowlist;</li>\n<li>explicitly promoted public report metadata;</li>\n<li>published or otherwise explicitly public browser projections;</li>\n<li>public-only adapters and read-only report contributions; and</li>\n<li>the shared report stylesheet plus any explicitly promoted report stylesheet.</li>\n</ul>\n<p>Public composition is observable runtime work, not merely permission. Static imports, eagerly reached dynamic imports, and route-loaded stylesheets make their code or selectors part of the public download. Optional handles and unmatched selectors can prevent behavior, but they do not undo composition.</p>\n<p>Public modules may perform complex read-only presentation over public data. Complexity, row count, sorting, filtering, paging, deep links, or browser-local state do not make a report local. Data authority and dependencies do.</p>\n<p>Public composition must not import from local report, management, package, import, review, or local-service module roots. A missing control, false capability flag, hidden element, or unavailable service is not a module boundary.</p>\n<h3>Local-only composition</h3>\n<p>Local composition owns:</p>\n<ul>\n<li>the full local report loader registry;</li>\n<li>private manifests, generated-data readers, and Studio or Catalogue lookups;</li>\n<li>local report implementations with no public data contract;</li>\n<li>management contributions and collection-specific customisations;</li>\n<li>selection, source, metadata, create, copy, delete, publishability, package, filesystem, and other local actions; and</li>\n<li>local report and management stylesheets.</li>\n</ul>\n<p>A report can be local-only while remaining read-only. The Works report, for example, is local because it composes private Projects inventory and Studio lookups, not because it writes data.</p>\n<h2>Dependency Direction</h2>\n<pre><code class=\"language-text\">Public entrypoint\n  -&gt; public loader registry\n    -&gt; shared report module\n      -&gt; public projection\n\nManage entrypoint\n  -&gt; local loader registry\n    -&gt; shared report module\n    + management contribution\n      -&gt; private data, local services, and actions\n</code></pre>\n<p>Dependencies point inward toward shared read-only code. Shared modules do not import their local callers. Manage may supply a narrow contribution or adapter to a shared module; public composition cannot receive a management contribution because the corresponding module and loader are absent from its graph.</p>\n<h2>Data Boundary</h2>\n<p>Public reports read only data deliberately present in the public browser contract, such as public report metadata, public scope configuration, published manifests, and public by-ID payloads.</p>\n<p>Manage may replace a public read adapter with an exact private adapter while retaining shared presentation and identity validation. Private records must not leak into the public projection merely because the renderer can display them.</p>\n<p>Public report promotion therefore requires a data decision as well as a module decision. Reusing a local report renderer is insufficient when its input is a private manifest, local endpoint, source file, credential, or repository path.</p>\n<h2>Customisation And Contribution</h2>\n<p><code>sub_scope_customisation</code> is a collection-level service definition. Depending on its registered aspects, it may own manifest projection, document groups, validation, metadata, import front matter, assignable fields, subject fields, transfer, lineage, or browser composition.</p>\n<p>A <strong>report contribution</strong> is only the optional browser-composition aspect. The terms are not interchangeable.</p>\n<ul>\n<li>A <strong>public report contribution</strong> is a separately allowlisted read-only module over public projection data.</li>\n<li>A <strong>management report contribution</strong> is a local module that may compose private data and local actions.</li>\n<li>A customisation with no browser-composition aspect contributes nothing to report rendering.</li>\n<li>Declaring browser access in service configuration does not create an executable browser loader.</li>\n</ul>\n<p>The current browser exposure is:</p>\n<table>\n<thead>\n<tr>\n<th>Customisation</th>\n<th>Browser report contribution</th>\n<th>Other registered ownership</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>analysis_tags</code></td>\n<td>Manage only</td>\n<td>groups, manifest projection, transfer</td>\n</tr>\n<tr>\n<td><code>dotlineform_projects</code></td>\n<td>Manage only</td>\n<td>manifest projection, metadata, import, subject, lineage</td>\n</tr>\n<tr>\n<td><code>analysis_works</code></td>\n<td>none</td>\n<td>subject, lineage</td>\n</tr>\n<tr>\n<td>public customisations</td>\n<td>none currently</td>\n<td>none</td>\n</tr>\n</tbody>\n</table>\n<h2>Stylesheet Ownership</h2>\n<p>Styles follow the same composition boundary as JavaScript.</p>\n<ul>\n<li><code>site/docs-viewer/static/css/docs-viewer-reports.css</code> contains genuinely shared report shell, sub-scope list/detail, search, link, and public responsive presentation.</li>\n<li><code>docs-viewer/static/css/docs-viewer-local-reports.css</code> contains controls, layouts, and report-specific selectors used only by local report modules.</li>\n<li><code>docs-viewer/static/css/docs-viewer-manage.css</code> contains management shell, selection, action, and management-contribution presentation.</li>\n</ul>\n<p>The Manage shell loads shared report CSS, then local report CSS, then management CSS. Public route shells load only shared report CSS.</p>\n<p>When a local report is promoted, its required presentation is reviewed deliberately. Genuinely shared selectors move to shared CSS; public-specific selectors receive a public owner. Local implementation CSS is not preloaded publicly in anticipation of possible promotion.</p>\n<p>Putting selectors in <code>docs-viewer-reports.css</code> publicly composes them because public shells load that complete file. Selectors for a public-safe capability with only local adopters instead belong in a stylesheet that only Manage loads, or in a separately loadable public-safe stylesheet that remains absent from public shells until adoption.</p>\n<h2>New Report Decision</h2>\n<p>Before implementing a report, decide:</p>\n<ol>\n<li>Is its data already public, deliberately projectable, or private/local?</li>\n<li>Is the complete report public-capable, local-only, or a shared read-only core with separate public and local adapters?</li>\n<li>Does it need a public contribution, a management contribution, neither, or both as separate modules?</li>\n<li>Which capabilities are merely public-safe, which are composed by each shell now, and which named reports actually adopt them?</li>\n<li>Is the content a stable rectangular dataset requiring semantic table structure, or a list, group, summary, or composite better represented with list/section semantics and CSS Grid?</li>\n<li>Which module and stylesheet own each report-specific behavior, and which entrypoint or shell causes each one to be downloaded?</li>\n<li>Does the result remain an inspection surface, or has it become a workflow that belongs outside the report mechanism?</li>\n</ol>\n<p>An existing report may move from local-only to shared/public composition when its data and dependencies become public-safe. That is an explicit promotion slice, not a registry edit or an access-flag change.</p>\n<h2>Promotion Requirements</h2>\n<p>Promoting a report or contribution to public requires all of the following:</p>\n<ul>\n<li>an explicit public data contract and projection;</li>\n<li>a public-safe metadata projection;</li>\n<li>a public executable loader entry;</li>\n<li>a public or shared implementation with no local imports;</li>\n<li>deliberately owned public/shared CSS;</li>\n<li>contained unavailable behavior when promotion is incomplete; and</li>\n<li>focused verification of imports, requested data, route state, and absence of management UI or services.</li>\n</ul>\n<p>Promoting a reusable capability to public composition is a smaller but still explicit decision: name the current public adopter, add the public import and stylesheet edges, and verify that the public graph contains no local dependency. Public-safe code without a public adopter remains uncomposed.</p>\n<h2>Verification</h2>\n<p>Changes to this boundary should verify, in proportion to the slice:</p>\n<ul>\n<li>projected public report ids have matching public loader entries;</li>\n<li>projected public customisation ids have matching public contribution loaders and manifest data;</li>\n<li>public entrypoints and their transitive report imports contain no local management dependency;</li>\n<li>every publicly composed optional capability has a named current public adopter, or is recorded explicitly as accepted application-level public behavior rather than inferred from its path;</li>\n<li>public report requests remain within public projections and never request private inventory or local APIs;</li>\n<li>public route shells do not load local report or management CSS;</li>\n<li>Manage loads shared, local-report, and management CSS in that order; and</li>\n<li>the affected report still passes its focused behavior smoke.</li>\n</ul>\n<h2>Current Module Owners</h2>\n<ul>\n<li><code>site/docs-viewer/runtime/js/reports/docs-viewer-public-reports.js</code></li>\n<li><code>site/docs-viewer/runtime/js/public/docs-viewer-public-document-reports.js</code></li>\n<li><code>site/docs-viewer/runtime/js/shared/docs-subscope-report.js</code></li>\n<li><code>site/docs-viewer/runtime/js/shared/docs-subscope-customisation-registry.js</code></li>\n<li><code>docs-viewer/runtime/js/reports/docs-viewer-reports.js</code></li>\n<li><code>docs-viewer/runtime/js/management/docs-viewer-management-document-reports.js</code></li>\n<li><code>docs-viewer/runtime/js/management/docs-viewer-management-subscope-customisation-registry.js</code></li>\n<li><code>site/docs-viewer/static/css/docs-viewer-reports.css</code></li>\n<li><code>docs-viewer/static/css/docs-viewer-local-reports.css</code></li>\n<li><code>docs-viewer/static/css/docs-viewer-manage.css</code></li>\n</ul>\n"
}
