{
  "doc_id": "d-20260808-160019-084f3d",
  "title": "SSP-8.1 — Superseded Nullable-State Cutover Evidence",
  "added_date": "2026-08-08 16:00:19",
  "last_updated": "2026-08-08 16:57:51",
  "viewer_url": "/docs/?scope=studio&doc=d-20260808-160019-084f3d",
  "parent_id": "d-20260808-153226-a067d9",
  "summary": "retain evidence for the committed nullable-state v2 cutover that later product review superseded with a Working-owned lineage model",
  "ui_status": "stopped",
  "content_html": "<h1>SSP-8.1 — Superseded Nullable-State Cutover Evidence</h1>\n<h2>Outcome</h2>\n<p>This document retains implementation evidence for commit <code>8319b2a02</code>. Subsequent product review rejected its independently nullable Working, Editorial, and Published target. It is not the current SSP-8 contract and must not be extended.</p>\n<p>The current direction is <a href=\"/docs/?scope=studio&amp;doc=d-20260808-153226-a067d9\">SSP-8 — Revised Model</a>: the Working <code>doc_id</code> owns a set of current Editorial children, Published is an optional URL attribute of a current Editorial child, deleting Working ends lineage, and <code>lineage_id</code> plus the orphan-lineage report are removed. <a href=\"/docs/?scope=studio&amp;doc=d-20260808-165751-1f159a\">DPD-1 — Immediate Public Projection Cleanup On Document Delete</a> must deliver the Docs Viewer-wide Delete behavior before that replacement proceeds.</p>\n<p>The private canonical table currently has the committed interim <code>docs_document_publication_lineage_v2</code> contract. Each row has an immutable branch ID and independently optional <strong>Working</strong>, <strong>Editorial</strong>, and <strong>Published</strong> states. The evidence below describes that committed implementation only.</p>\n<p>The known SSP-7 review row was migrated deliberately to Working-only. Its prior workflow Editorial Delete is represented by <code>editorial: null</code>; no filesystem check, unavailable marker, or inferred history was used.</p>\n<h2>Frozen Row Contract</h2>\n<pre><code class=\"language-json\">{\n  &quot;lineage_id&quot;: &quot;sha256:&lt;64 lowercase hexadecimal characters&gt;&quot;,\n  &quot;working&quot;: {&quot;scope&quot;: &quot;...&quot;, &quot;sub_scope&quot;: &quot;...&quot;, &quot;doc_id&quot;: &quot;...&quot;},\n  &quot;editorial&quot;: {&quot;scope&quot;: &quot;...&quot;, &quot;sub_scope&quot;: &quot;...&quot;, &quot;doc_id&quot;: &quot;...&quot;},\n  &quot;created_at&quot;: &quot;YYYY-MM-DDTHH:MM:SSZ&quot;,\n  &quot;last_copied_at&quot;: &quot;YYYY-MM-DDTHH:MM:SSZ&quot;,\n  &quot;published&quot;: {&quot;public_url&quot;: &quot;...&quot;}\n}\n</code></pre>\n<p>Each of <code>working</code>, <code>editorial</code>, and <code>published</code> may independently be <code>null</code>, but a retained row must contain at least one state. Published carries only its current exact <code>{public_url}</code> evidence; it does not duplicate Editorial identity or add a presence marker.</p>\n<p>For New Copy, <code>lineage_id</code> is <code>sha256:</code> plus the SHA-256 digest of the whitespace-free, key-sorted JSON object containing the initial exact <code>working</code> and <code>editorial</code> identity objects. That ID never changes when a later action removes a state. It therefore preserves one exact branch identity through Working-only, Editorial-only, Published-only, and mixed-state records while allowing one Working document to own zero-to-many New branches. Rows are persisted in <code>lineage_id</code> order.</p>\n<p><code>created_at</code> records the initial New Copy and <code>last_copied_at</code> records the latest New or exact Replace. Copy New creates Working plus Editorial with no Published evidence. Replace requires both current exact states, retains the branch ID and every other state, and updates only <code>last_copied_at</code>.</p>\n<h2>Clean Cut And Current Consumers</h2>\n<p>The canonical JSON, strict reader/writer, Copy fixtures, Publish fixtures, and Projects projection fixtures all moved together to v2. A v1 table now fails schema validation; there is no dual reader or migration-at-runtime path.</p>\n<p>Only the minimum read adaptations needed by the current actions were made:</p>\n<ul>\n<li>Copy planning reads branches containing the exact current Working and Editorial states; New and Replace continue to use their existing action and response contracts.</li>\n<li>Publish reconciliation reads nullable Editorial state safely and leaves a row without Editorial unchanged. SSP-8.2 still owns the revised post-Delete Publish behavior.</li>\n<li>Projects joins only a current Working-plus-Editorial branch into its existing <code>publication_targets</code> projection. A Working-only branch produces no unavailable Editorial target.</li>\n</ul>\n<p>No Delete lineage mutation, highest-state report change, new report, filesystem scan, rollback, retry, repair, or public lineage projection is included.</p>\n<h2>Evidence</h2>\n<ul>\n<li>Focused lineage, Copy planning/apply, Projects build, and Publish checks: <strong>72 passed</strong>.</li>\n<li>Optional-state coverage: Working-only, Editorial-only, Published-only, deterministic ordering, all-empty rejection, duplicate-ID rejection, and stable SHA-256 input contract.</li>\n<li>Mutation continuity: New creates one exact branch; Replace retains its ID, creation evidence, and Published evidence; Publish sets and clears Published for current Editorial while safely retaining a Working-only row.</li>\n<li>Canonical check: the migrated table loads as one Working-only row and renders byte-for-byte identically.</li>\n<li>Targeted <code>dotlineform/projects</code> rebuild: <strong>241 documents</strong>, <strong>0 document writes</strong>, <strong>0 removals</strong>, <strong>0 warnings</strong>; the Manage manifest follow-through produced no tracked generated delta.</li>\n<li>Targeted Studio delivery-doc and search rebuilds confirmed both changed documents in their by-ID and search projections with <strong>0 warnings</strong> and no full-search fallback.</li>\n<li>Maintained Docs Viewer source lint, maintained test-source lint, focused Python syntax compilation, and <code>git diff --check</code>: passed.</li>\n</ul>\n<h2>Review And Gate</h2>\n<p>The final consumer inventory found one table owner and only the existing Copy, Publish, and Projects readers. The active implementation contains no v1 schema reader, source/publication field alias, presence boolean, filesystem reconciliation, inferred state, generic provenance layer, or public exposure.</p>\n<p>SSP-8.1 is superseded and stopped. Do not add Delete or reporting behavior to the nullable-state model. The next implementation approval belongs to DPD-1.0; SSP-8 resumes later with a clean Working-owned replacement.</p>\n"
}
