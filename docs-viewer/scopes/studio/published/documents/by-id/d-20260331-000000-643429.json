{
  "doc_id": "d-20260331-000000-643429",
  "title": "Tag Editor",
  "added_date": "2026-03-31 00:00:00",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260331-000000-643429",
  "parent_id": "d-20260506-181900-d08dbe",
  "content_html": "<h1>Tag Editor</h1>\n<h2>Capability</h2>\n<p><code>/analytics/series-tag-editor/?series=&lt;series_id&gt;</code> edits one series assignment and its per-work override deltas.</p>\n<p>Series tags are inherited by every work in the series. A work row stores only additional tags; the service removes duplicates of inherited series tags. Empty work overrides are removed unless the workflow explicitly keeps the row.</p>\n<h2>Execution Path</h2>\n<pre><code class=\"language-text\">series query ID\n  -&gt; load public series/work indexes\n  -&gt; load registry, aliases, groups, assignments\n  -&gt; build series + work editor state\n  -&gt; tag/weight/alias-assisted changes\n  -&gt; one Save action\n     -&gt; direct allowlisted API writes when safe\n     -&gt; otherwise stage one versioned offline series snapshot\n</code></pre>\n<p>The header/media area is catalogue context. Canonical writes are limited to Analytics assignments.</p>\n<h2>Save Modes</h2>\n<h3>Direct</h3>\n<p>When the Local Analytics service is reachable and the series has no staged offline row, Save posts the changed series row and work deltas. Multiple writes share one initiating activity context. The server validates IDs, weights, membership, inheritance, and canonical source before atomic replacement.</p>\n<h3>Offline</h3>\n<p>When the service is unavailable—or the same series already has staged work—Save records a base snapshot plus staged row in browser storage. It does not silently switch back to direct writes while unresolved staged work exists. Reconciliation happens through <a href=\"/docs/?scope=studio&amp;doc=d-20260331-000000-974bf0\">Series Tags</a>.</p>\n<p>If a direct save fails, the editor reports the failure and requires another explicit Save to stage offline.</p>\n<h2>Durable Rules</h2>\n<ul>\n<li>series ID is required and slug-safe; work IDs use the catalogue identity contract;</li>\n<li>manual weights are the fixed values enforced by <code>tag_source_model.py</code>;</li>\n<li>one tag ID appears at most once in a row;</li>\n<li>aliases help resolve input and may preserve historical context, but canonical tag ID remains authoritative;</li>\n<li>effective work tags are derived, not stored as a complete copy.</li>\n</ul>\n<h2>Code Map</h2>\n<ul>\n<li>page/context boot: <code>series-tag-editor-page.js</code></li>\n<li>editor coordinator: <code>analytics-tag-editor.js</code></li>\n<li>domain/state/render/suggestion modules: <code>analytics-tag-editor-*</code></li>\n<li>save policy: <code>analytics-tag-editor-save-controller.js</code>, <code>analytics-tag-editor-save.js</code></li>\n<li>offline format: <code>tag-assignments-offline*.js</code></li>\n<li>server planner: <code>tag_assignment_service.py</code></li>\n</ul>\n<p>The route root follows shared Analytics ready/busy state; selected-work and editor/empty modes are route-level state, not persistence authority.</p>\n<h2>Weak Spots</h2>\n<ul>\n<li>The editor coordinates catalogue context, tag search, inheritance, weights, direct writes, and offline staging; module splits reduce file size but not conceptual breadth.</li>\n<li>Public catalogue indexes are a generated dependency and can disagree with canonical assignment state.</li>\n<li>Offline and direct modes deliberately gate each other, so save-mode bugs can strand valid staged work or bypass conflict review.</li>\n</ul>\n"
}
