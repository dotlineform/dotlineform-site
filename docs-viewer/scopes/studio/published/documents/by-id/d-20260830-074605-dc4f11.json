{
  "doc_id": "d-20260830-074605-dc4f11",
  "title": "Scope, Data, And Site Foundations",
  "added_date": "2026-08-30 07:46:05",
  "last_updated": "2026-08-30 15:11:55",
  "viewer_url": "/docs/?scope=studio&doc=d-20260830-074605-dc4f11",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "Separate runtime projection, external scope Build and Publish, repository deployment, explicit Git commit and push, and public Pages deployment through three ordered foundation stages.",
  "ui_status": "in-progress",
  "content_html": "<h1>Scope, Data, And Site Foundations</h1>\n<h2>Purpose</h2>\n<p>Make code, private document data, consumer-ready published data, and the deployed public site separate ownership layers. These changes are useful repository foundations in their own right and are delivered through the Studio Roadmap even though they also prepare for the Swift application described in <a href=\"/docs/?scope=app&amp;doc=d-20260829-212459-c17eb8\">App Architecture And Migration Approach</a>.</p>\n<p>The target repository contains code and the exact tracked <code>site/</code> deployment snapshot. <code>site/</code> directly owns public-only static website artifacts and contains deployed projections of shared runtime and public document data. It is not the authority for shared runtime code or for scope source, generated output, or published output.</p>\n<h2>Stage 1 — Runtime Ownership And Site Projection</h2>\n<p>Move canonical public/shared Docs Viewer runtime code outside <code>site/</code>. Add one deterministic internal development projection that copies the deployable runtime code assets into tracked <code>site/</code> whenever the runtime changes. Canonical and projected changes are reviewed and committed together, while local Docs Viewer routes read canonical runtime directly.</p>\n<p>This is a code-development process, not a Docs Viewer user action. It does not introduce Update Site, Publish, or Deploy controls and does not move document data. The GitHub Pages workflow remains a validator and deployer of the tracked <code>site/</code> snapshot; it does not generate or commit runtime changes.</p>\n<p>This stage is first because it removes the immediate code/deployment ambiguity without anticipating the final scope model. It also creates a clean runtime source for later WebKit and Swift work.</p>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260830-074606-881007\">Docs Viewer Runtime Ownership And Site Projection Delivery</a> is the completed first delivery.</li>\n</ul>\n<h2>Stage 2 — External Scope Build And Publish Model</h2>\n<p>Give every in-scope Docs Viewer scope the same external, application-independent storage model. Storage provider and public exposure become separate configuration concerns rather than different scope types.</p>\n<p>Each scope owns:</p>\n<ul>\n<li>canonical Markdown and source media;</li>\n<li>replaceable generated document, search, and media output created by Build; and</li>\n<li>an accepted, consumer-ready published snapshot promoted through Publish.</li>\n</ul>\n<p>Manage mode can read generated output for working review and published output for accepted-output review. Swift and other application consumers read published output directly without requiring <code>site/</code>. Published payloads therefore remain consumer-neutral and do not encode GitHub Pages or dotlineform.com as their owner.</p>\n<p>Publish owns document eligibility. A document with <code>publishable: false</code> remains available in generated working output but does not enter the published snapshot and therefore cannot be deployed. Every consumer of published output sees the same accepted document set.</p>\n<p>Analysis is the only scope eligible for document deployment into the public site. Studio and the other private scopes use the same Build and Publish lifecycle without becoming publicly deployable. Before repository-backed canonical Markdown leaves Git, this stage must establish the minimum recoverable snapshot boundary. The <code>processing/</code> Java/Processing code project remains separate and this feature does not define its development lifecycle. The <code>processing</code> Docs Viewer scope participates in the same external storage, Build, and Publish migration as the other Docs Viewer scopes.</p>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260830-151155-8eadce\">External Scope Build And Publish Delivery</a> is the planned Stage 2 delivery.</li>\n</ul>\n<h2>Stage 3 — Document Deployment And Pages Release</h2>\n<p>Introduce two separate deployment operations for document content, with an explicit ordinary Git boundary between them:</p>\n<ul>\n<li><strong>Deploy Repo</strong> copies the complete accepted published Analysis output into tracked <code>site/</code> without applying another document-eligibility rule. The resulting repository projection can be reviewed immediately through the local site preview server.</li>\n<li><strong>Commit and push</strong> remains an explicit manual Git action outside the document lifecycle. Deploy Repo does not perform it implicitly.</li>\n<li><strong>Deploy Public</strong> validates and deploys the committed and pushed <code>site/</code> snapshot to GitHub Pages through the existing manually triggered workflow.</li>\n</ul>\n<p>Deploy Repo does not rebuild or publish a scope, commit or push changes, or deploy GitHub Pages. Deploy Public does not read external scopes, rebuild content, publish content, alter <code>site/</code>, or commit changes; it deploys only the committed snapshot it receives. These remain independent manual operations. A future application may gain explicit repository, commit, push, and GitHub Action capabilities without collapsing their boundaries or making them automatic.</p>\n<p>The current local workflow will normally compose Publish and Deploy Repo in one capability-based Publish modal, with both selected by default. Publish remains usable without Deploy Repo where repository capability is unavailable. <a href=\"/docs/?scope=studio&amp;doc=d-20260830-092438-f0c9d2\">Publish And Deploy Model</a> defines the detailed current behaviour, target ownership, modal composition, capability progression, and partial-failure handling.</p>\n<p>Public-only static HTML, CSS, JavaScript, images, and other website artifacts remain maintained directly in <code>site/</code>. They are not duplicated into another source tree merely so a build can copy them back. No canonical scope source, replaceable generated output, non-Analysis scope data, or private configuration is deployed.</p>\n<h2>Lifecycle Vocabulary And Order</h2>\n<p>The user-facing document lifecycle is:</p>\n<ol>\n<li><strong>Build:</strong> canonical source to replaceable generated output for working review in Manage mode.</li>\n<li><strong>Publish:</strong> generated output to the accepted, consumer-ready scope-local published snapshot for published review and direct application consumption.</li>\n<li><strong>Deploy Repo:</strong> accepted published Analysis output to the tracked <code>site/</code> document projection for local public-site preview.</li>\n<li><strong>Deploy Public:</strong> the validated committed and pushed <code>site/</code> snapshot to GitHub Pages.</li>\n</ol>\n<p>The explicit Git commit and push occurs between Deploy Repo and Deploy Public but is not itself a Docs Viewer document-lifecycle operation.</p>\n<p>Runtime projection is deliberately outside that lifecycle:</p>\n<pre><code class=\"language-text\">runtime code change\n  -&gt; internal runtime projection\n  -&gt; tracked site runtime\n\nscope source\n  -&gt; Build\n  -&gt; generated\n  -&gt; Publish\n  -&gt; published -----&gt; Swift or another direct consumer\n  -&gt; Deploy Repo\n  -&gt; tracked site document projection\n  -&gt; explicit Git commit and push\n  -&gt; committed repository snapshot\n  -&gt; Deploy Public\n  -&gt; dotlineform.com\n</code></pre>\n<p>Stage 1 establishes runtime ownership. Stage 2 establishes consumer-ready scope output. Stage 3 deploys only accepted Analysis output and keeps local preview separate from public release.</p>\n<p>The feature does not itself implement the Swift application, migrate Catalogue data, introduce document version history beyond the required recovery boundary, or absorb the <code>processing/</code> code project into the website lifecycle.</p>\n"
}
