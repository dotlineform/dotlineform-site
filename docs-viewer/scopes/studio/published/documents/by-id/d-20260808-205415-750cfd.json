{
  "doc_id": "d-20260808-205415-750cfd",
  "title": "iPad Docs Viewer Concept And Feasibility",
  "added_date": "2026-08-08 20:54:15",
  "last_updated": "2026-08-08 20:54:15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260808-205415-750cfd",
  "parent_id": "d-20260808-205414-91040a",
  "summary": "define the iPad runtime stack, expected Docs Viewer working slice, iCloud boundary, known exclusions, and configuration approach",
  "ui_status": "proposed",
  "content_html": "<h1>iPad Docs Viewer Concept And Feasibility</h1>\n<h2>Feasibility Claim</h2>\n<p>The current evidence supports a serious real-device proof: most Docs Viewer manage behavior should be able to run locally on iPad through a Linux environment, with Safari as the UI and an iCloud Drive folder mounted as <code>DOTLINEFORM_PROJECTS_BASE_DIR</code>. This is a feasibility hypothesis, not a confirmed platform contract, until <a href=\"/docs/?scope=studio&amp;doc=d-20260808-205416-9f6d6c\">iPad Docs Viewer Installation And Proof</a> passes on the target iPad.</p>\n<p>The likely result is a useful session-based Docs Viewer with external-scope access, not unchanged macOS parity. The application already has a loopback Python service, browser-managed UI, generated static products, explicit external-local paths, and subprocess-based rebuilds. Those are a better fit for <a href=\"https://ish.app/\">iSH</a> than for a native Python editor sandbox.</p>\n<h2>Proposed Stack</h2>\n<table>\n<thead>\n<tr>\n<th>component</th>\n<th>role</th>\n<th>status</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>iPadOS Files and iCloud Drive</td>\n<td>expose the existing external projects base and keep its contents available locally</td>\n<td>required</td>\n</tr>\n<tr>\n<td>iSH</td>\n<td>provide local Alpine Linux, Python, child processes, sockets, Git, native packages, and the iOS File Provider mount</td>\n<td>required runtime</td>\n</tr>\n<tr>\n<td>Safari</td>\n<td>open the manage UI on <code>http://127.0.0.1:8776/docs/</code></td>\n<td>required UI</td>\n</tr>\n<tr>\n<td>Git in iSH</td>\n<td>clone and operate the proof checkout in iSH's Linux filesystem</td>\n<td>baseline source-control path</td>\n</tr>\n<tr>\n<td>Working Copy</td>\n<td>optional Git UI if it can safely access the same checkout without moving the runtime clone onto a less reliable File Provider path</td>\n<td>optional after the baseline gate</td>\n</tr>\n<tr>\n<td>Textastic or another Files-aware editor</td>\n<td>optional direct source editor for folders the app can access</td>\n<td>optional convenience</td>\n</tr>\n<tr>\n<td>Codespaces</td>\n<td>remote fallback for repo-local development and checks</td>\n<td>complementary, not an external-scope solution</td>\n</tr>\n</tbody>\n</table>\n<p>The baseline keeps the Git checkout inside iSH and mounts only the external project tree from iCloud. This avoids putting <code>.git</code>, the Python environment, caches, and frequent generated writes on a File Provider filesystem while still giving Docs Viewer the external root it actually needs.</p>\n<pre><code class=\"language-text\">Safari\n  -&gt; http://127.0.0.1:8776/docs/\n      -&gt; Python Docs Viewer service in iSH\n          -&gt; repository checkout and repo-backed scopes in iSH storage\n          -&gt; /mnt/dlf-projects\n              -&gt; Files provider\n                  -&gt; iCloud Drive external scopes and workspaces\n</code></pre>\n<p><a href=\"https://workingcopyapp.com/\">Working Copy</a> can clone, inspect, commit, and push repositories on iPad, and <a href=\"https://www.textasticapp.com/v10/manual/managing_files/local_files_icloud.html\">Textastic external folders</a> can open folders exposed through Files. Neither is required to prove Docs Viewer. A shared clone between those apps and iSH is an additional File Provider behavior to verify rather than an assumed part of the stack.</p>\n<h2>Why iSH Is The Baseline</h2>\n<p>Docs Viewer uses <code>ThreadingHTTPServer</code>, loopback sockets, subprocesses for document and search rebuilds, compiled Python dependencies such as lxml and Pillow, and ordinary repository paths. iSH supplies an emulated x86 Linux userspace with Alpine packages and supports mounting a folder selected from Files through <code>mount -t ios</code>.</p>\n<p>Native iPad Python IDEs and shell apps remain useful for focused scripts or editing, but they are not interchangeable with this runtime. For example, <a href=\"https://github.com/holzschu/a-shell\">a-Shell</a> documents WebAssembly limits including no sockets or forks and permits <code>pip</code> installation only for pure-Python packages. Any alternative app must prove the same required sockets, subprocess, native-extension, path, and Files-provider behavior before replacing iSH in the baseline.</p>\n<h2>Current Repository Fit</h2>\n<p>The current integrated service is a Python <code>ThreadingHTTPServer</code> started from <code>docs-viewer/services/docs_viewer_service.py</code>. It serves the manage shell and tracked browser assets directly from the checkout. Managed saves run the document and search builders as child Python processes through <code>docs_write_rebuild.py</code>. The local manage route does not need Jekyll or Ruby.</p>\n<p>External-local scope paths are resolved below <code>DOTLINEFORM_PROJECTS_BASE_DIR</code>; the same base also owns adjacent <code>data-sharing</code> and <code>projects</code> workspaces used by integrated workflows. iSH can ask the Files picker to mount the exact iCloud parent at a stable Linux mount point, as described by the <a href=\"https://github.com/ish-app/ish/wiki/Mounting-other-file-providers\">iSH File Provider mount guide</a>.</p>\n<p>This fit is for the current integrated checkout. It does not prove the package boundary proposed by Standalone Docs Viewer and should not be described as the standalone application.</p>\n<h2>Expected Working Slice</h2>\n<table>\n<thead>\n<tr>\n<th>capability</th>\n<th>expectation before device proof</th>\n<th>qualification</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>service health and manage shell</td>\n<td>should work</td>\n<td>Python service and static browser assets are self-contained in the checkout</td>\n</tr>\n<tr>\n<td>repository-backed Studio and Processing scopes</td>\n<td>should work</td>\n<td>normal generated products must be present or rebuildable</td>\n</tr>\n<tr>\n<td>configured iCloud-backed external scopes and sub-scopes</td>\n<td>should work</td>\n<td>depends on complete local download and correct File Provider mount behavior</td>\n</tr>\n<tr>\n<td>index, document rendering, recent activity, and search</td>\n<td>should work</td>\n<td>generated reads and browser assets are ordinary local files</td>\n</tr>\n<tr>\n<td>Source editor save and managed document/search rebuild</td>\n<td>should work</td>\n<td>subprocess execution is supported; atomic writes and renames across the iCloud mount must pass the gate</td>\n</tr>\n<tr>\n<td>Markdown and HTML import</td>\n<td>should work</td>\n<td>ordinary Python parsing and source writes</td>\n</tr>\n<tr>\n<td>DOCX import</td>\n<td>expected with Mammoth installed</td>\n<td>must pass one real import including any extracted image behavior used by the test document</td>\n</tr>\n<tr>\n<td>JPEG and PNG document-package media</td>\n<td>expected with Pillow installed</td>\n<td>HEIF, video, and macOS-specific media tools are not implied</td>\n</tr>\n<tr>\n<td>Docs Review and document-package workflows</td>\n<td>expected with the integrated workspaces mounted</td>\n<td>the proof must use disposable packages and must not infer success from the route merely loading</td>\n</tr>\n<tr>\n<td>inline Mermaid rendering in Manage</td>\n<td>should work</td>\n<td>it uses the tracked browser Mermaid bundle rather than server-side Chromium</td>\n</tr>\n<tr>\n<td>tracked public-site preview</td>\n<td>should work through a second Python server</td>\n<td>this previews <code>site/</code>; it is not a Jekyll rebuild</td>\n</tr>\n<tr>\n<td>Git status, diff, commit, fetch, and push</td>\n<td>should work through Git in iSH</td>\n<td>remote authentication and a Working Copy UI are separate concerns</td>\n</tr>\n</tbody>\n</table>\n<h2>Known Or Likely Exclusions</h2>\n<ul>\n<li><strong>Open Source and local-folder activation:</strong> current source-open actions invoke platform applications, and <code>docs_local_links.py</code> deliberately enables local-folder activation only on Darwin. Local targets can still be authored and validated, but iSH cannot claim Finder-style reveal/open behavior.</li>\n<li><strong>Server-produced Mermaid SVG:</strong> public projection and related build paths require the checked Node <code>mmdc</code> toolchain, Puppeteer, and Chromium. Installing that stack inside iSH is not part of the baseline and is likely impractical. Browser-side Manage rendering remains separate.</li>\n<li><strong>Always-on service:</strong> iPadOS may suspend iSH when it is backgrounded or the device is locked. The feature promises a foreground or multitasking session that can be restarted safely, not a persistent daemon.</li>\n<li><strong>Complete desktop toolchain:</strong> Jekyll production builds, the full repository test/lint toolchain, macOS <code>sips</code>, HEIF conversion, video workflows, and other platform utilities are outside the first proof.</li>\n<li><strong>Unproved public and remote publication paths:</strong> static preview can be tested locally, but credentialed R2 publication and other network side effects require a later explicitly authorized proof.</li>\n</ul>\n<p>These exclusions are honest platform boundaries, not automatic reasons to reject the feature. A device proof fails only when the required working slice is unreliable or unsafe for external data.</p>\n<h2>iCloud Data Boundary</h2>\n<p>The selected Files folder must be the exact parent represented by <code>DOTLINEFORM_PROJECTS_BASE_DIR</code>, normally containing <code>docs-viewer/</code>, <code>data-sharing/</code>, and <code>projects/</code>. Mark the required tree <strong>Keep Downloaded</strong> before the proof. Placeholder-only files, partial downloads, or an accidentally selected child folder invalidate the test.</p>\n<p>The service writes source and generated files atomically and may use temporary siblings and renames. The gate therefore tests the actual iSH-to-Files-to-iCloud path rather than only confirming that Python can list a mounted directory. It must prove write, rebuild, restart, Mac-side synchronization, and cleanup with a disposable external document.</p>\n<p>Do not edit the same proof document concurrently on the Mac and iPad. iCloud remains synchronization transport, not locking, Git, or transaction coordination. A conflict copy, resurrected temporary file, partial generated tree, or lost rename is a platform blocker until understood.</p>\n<h2>Configuration Decision</h2>\n<p>An iPad-specific environment file is useful operationally, but a new tracked <code>env.ipad</code> contract is not needed for the first proof. The repository already ignores and loads clone-local <code>.env.local</code>, which is the correct place for the device-specific iSH mount path and local capability switches. It must contain no credentials and must remain untracked.</p>\n<p>The current loaders do not read <code>env.ipad</code> automatically. Adding one now would create a second configuration convention without a launcher, precedence rule, ignore rule, or test. If repeated iPad use later demonstrates a need for named profiles, add a bounded delivery for an explicit non-secret example, untracked active file, launcher, precedence contract, and focused verification. Until then, use <code>.env.local</code> and keep the installation guide authoritative for its minimal iPad values.</p>\n<h2>Success Meaning</h2>\n<p>Passing the proof establishes that the current integrated Docs Viewer is practically usable on the tested iPad for the required local and iCloud-backed scope slice, with recorded platform exclusions. It does not establish support for every iPad model, every iPadOS or iSH release, unattended background operation, the future standalone composition, or full desktop development parity.</p>\n"
}
