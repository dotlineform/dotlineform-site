{
  "doc_id": "d-20260821-162454-96e752",
  "title": "Pandoc EPUB Export from Docs Viewer HTML",
  "added_date": "2026-08-21 16:24:54",
  "last_updated": "2026-08-21 16:24:54",
  "viewer_url": "/docs/?scope=studio&doc=d-20260821-162454-96e752",
  "parent_id": "d-20260428-000000-f5ff18",
  "content_html": "<h1>Pandoc EPUB Export from Docs Viewer HTML</h1>\n<h2>Purpose</h2>\n<p>This document outlines a simpler EPUB export approach using <strong>Pandoc</strong> rather than building EPUB structure directly in Python.</p>\n<p>The key point is that the Docs Viewer already produces HTML. Markdown does not need to be introduced into the EPUB pipeline.</p>\n<p>The intended architecture is:</p>\n<pre><code class=\"language-text\">Docs Viewer generated HTML\n          │\n          ▼\nsmall HTML preparation step\n          │\n          ▼\nPandoc\n          │\n          ▼\nEPUB 3\n          │\n          ▼\nEPUBCheck\n</code></pre>\n<p>Pandoc is responsible for understanding EPUB packaging.</p>\n<p>The preparation step is responsible only for translating the existing Docs Viewer HTML collection into a form Pandoc can convert reliably.</p>\n<hr />\n<h2>Why Pandoc is attractive</h2>\n<p>A direct EbookLib implementation would require the project to understand and construct EPUB-specific concepts such as:</p>\n<ul>\n<li>package manifests;</li>\n<li>navigation documents;</li>\n<li>spine order;</li>\n<li>XHTML content files;</li>\n<li>asset registration;</li>\n<li>EPUB metadata;</li>\n<li>final EPUB packaging.</li>\n</ul>\n<p>Pandoc already implements these concerns.</p>\n<p>This gives a much cleaner division of responsibility:</p>\n<pre><code class=\"language-text\">Docs Viewer / small Python helper\n    understands Docs Viewer documents\n\nPandoc\n    understands EPUB\n</code></pre>\n<p>This is closer to the original goal of a simple external snapshot exporter.</p>\n<hr />\n<h2>Existing input</h2>\n<p>The current HTML snapshot already resembles:</p>\n<pre><code class=\"language-text\">studio-docs/\n├── index.html\n├── styles.css\n└── docs/\n    ├── d-....html\n    ├── d-....html\n    └── ...\n</code></pre>\n<p>The index contains the complete nested document hierarchy and links to the individual document files.</p>\n<p>The current export contains more than 200 documents, so the process must be deterministic and automated rather than assembled manually.</p>\n<hr />\n<h2>Pandoc can accept HTML directly</h2>\n<p>Pandoc supports HTML as an input format and EPUB 3 as an output format.</p>\n<p>A basic single-file conversion is therefore:</p>\n<pre><code class=\"language-bash\">pandoc   document.html   --from=html   --to=epub3   --output=document.epub\n</code></pre>\n<p>There is no Markdown stage:</p>\n<pre><code class=\"language-text\">HTML → Pandoc → EPUB\n</code></pre>\n<hr />\n<h2>Pandoc can accept multiple HTML files</h2>\n<p>Pandoc also accepts multiple input files:</p>\n<pre><code class=\"language-bash\">pandoc   docs/a.html   docs/b.html   docs/c.html   --from=html   --to=epub3   --output=studio-docs.epub\n</code></pre>\n<p>By default, Pandoc concatenates multiple input files before parsing them.</p>\n<p>This is potentially useful, but there are two important issues for the Docs Viewer export:</p>\n<ol>\n<li>the exported files may each be complete standalone HTML documents rather than HTML fragments;</li>\n<li>the existing document hierarchy is not encoded simply by the order of the input files.</li>\n</ol>\n<p>These issues need to be tested rather than assuming that a raw <code>docs/*.html</code> invocation will produce the intended book.</p>\n<hr />\n<h2>Important: <code>--file-scope</code> is probably not appropriate</h2>\n<p>Pandoc provides:</p>\n<pre><code class=\"language-bash\">--file-scope\n</code></pre>\n<p>which parses each input file independently before combining the resulting documents.</p>\n<p>That initially appears attractive for a collection of standalone HTML documents.</p>\n<p>However, Pandoc explicitly documents an important limitation:</p>\n<blockquote>\n<p>cross-file footnotes and links do not work when <code>--file-scope</code> is enabled.</p>\n</blockquote>\n<p>Pandoc also prefixes identifiers from separate files to avoid collisions.</p>\n<p>Because cross-document links are important in the Docs Viewer snapshot, <code>--file-scope</code> should <strong>not</strong> be adopted as the default solution without a specific proof that all required links survive.</p>\n<p>The safer working assumption is therefore:</p>\n<pre><code class=\"language-text\">do not rely on --file-scope\n</code></pre>\n<p>Instead, prepare one coherent HTML publication before passing it to Pandoc.</p>\n<hr />\n<h2>Recommended pipeline</h2>\n<p>The likely robust implementation is:</p>\n<pre><code class=\"language-text\">existing HTML export\n├── index.html\n├── styles.css\n└── docs/*.html\n          │\n          ▼\nsmall Python preparation script\n          │\n          ├── read index.html\n          ├── recover hierarchy and ordering\n          ├── read document HTML\n          ├── extract document content\n          ├── assign stable document anchors\n          ├── represent hierarchy with headings\n          ├── rewrite cross-document links\n          └── write temporary book.html\n          │\n          ▼\nbook.html\n          │\n          ▼\nPandoc\n          │\n          ▼\nstudio-docs.epub\n          │\n          ▼\nEPUBCheck\n</code></pre>\n<p>The temporary <code>book.html</code> is generated output. It is not a new canonical source format.</p>\n<hr />\n<h2>The Python step should remain small</h2>\n<p>The preparation script should not know anything about EPUB package internals.</p>\n<p>It should only understand the HTML export.</p>\n<p>Conceptually:</p>\n<pre><code class=\"language-python\">index = load_index()\ntree = parse_document_tree(index)\n\nfor document in tree:\n    html = load_document(document)\n    content = extract_content(html)\n    content = rewrite_links(content)\n\n    append_to_book(\n        document=document,\n        content=content,\n    )\n\nwrite_book_html()\nrun_pandoc()\nrun_epubcheck()\n</code></pre>\n<p>This is very different from implementing an EPUB generator.</p>\n<hr />\n<h2>Key issue 1: document hierarchy</h2>\n<p>The existing <code>index.html</code> contains a nested tree such as:</p>\n<pre><code class=\"language-text\">Architecture\n├── Data Models Diagrams\n│   ├── Catalogue Data Model Map\n│   ├── Cross-System Relationship Map\n│   └── Docs Viewer Projection Map\n├── Knowledge System Vision\n└── ...\n</code></pre>\n<p>Pandoc's generated table of contents is based primarily on the heading hierarchy in its input document.</p>\n<p>It will not automatically interpret the nested <code>&lt;ul&gt;</code> in the existing index as the desired EPUB contents structure.</p>\n<p>The preparation stage therefore needs to convert tree depth into document heading depth.</p>\n<p>For example:</p>\n<pre><code class=\"language-html\">&lt;h1 id=&quot;d-architecture&quot;&gt;Architecture&lt;/h1&gt;\n\n...\n\n&lt;h2 id=&quot;d-data-models&quot;&gt;Data Models Diagrams&lt;/h2&gt;\n\n...\n\n&lt;h3 id=&quot;d-catalogue-map&quot;&gt;Catalogue Data Model Map&lt;/h3&gt;\n\n...\n</code></pre>\n<p>Pandoc can then generate a nested EPUB table of contents from that hierarchy.</p>\n<hr />\n<h2>Key issue 2: document headings versus internal headings</h2>\n<p>Each exported document may already have its own heading structure:</p>\n<pre><code class=\"language-html\">&lt;h1&gt;Document title&lt;/h1&gt;\n&lt;h2&gt;Section&lt;/h2&gt;\n&lt;h3&gt;Subsection&lt;/h3&gt;\n</code></pre>\n<p>But the same document may sit at depth 2 or 3 in the Docs Viewer tree.</p>\n<p>If the hierarchy is represented directly through heading levels, the exporter must decide how the internal headings are shifted.</p>\n<p>For example, a document placed at level 3 might need:</p>\n<pre><code class=\"language-html\">&lt;h3&gt;Document title&lt;/h3&gt;\n&lt;h4&gt;Section&lt;/h4&gt;\n&lt;h5&gt;Subsection&lt;/h5&gt;\n</code></pre>\n<p>This needs an explicit rule.</p>\n<p>Possible approach:</p>\n<pre><code class=\"language-text\">document tree depth\n    determines document title heading\n\ninternal heading levels\n    are shifted relative to the document title\n</code></pre>\n<p>This transformation should be mechanical and tested.</p>\n<hr />\n<h2>Key issue 3: cross-document links</h2>\n<p>Current HTML links may look like:</p>\n<pre><code class=\"language-html\">&lt;a href=&quot;d-20260802-205910-533be0.html&quot;&gt;\n    Catalogue Data Model Map\n&lt;/a&gt;\n</code></pre>\n<p>or, depending on location:</p>\n<pre><code class=\"language-html\">&lt;a href=&quot;docs/d-20260802-205910-533be0.html&quot;&gt;\n</code></pre>\n<p>If the preparation step produces one logical HTML document, these can be rewritten to stable fragment links:</p>\n<pre><code class=\"language-html\">&lt;a href=&quot;#d-20260802-205910-533be0&quot;&gt;\n</code></pre>\n<p>and the destination document title receives:</p>\n<pre><code class=\"language-html\">&lt;h2 id=&quot;d-20260802-205910-533be0&quot;&gt;\n    Catalogue Data Model Map\n&lt;/h2&gt;\n</code></pre>\n<p>This is preferable to trying to predict the internal filenames Pandoc will create inside the EPUB.</p>\n<p>Pandoc can then adjust internal references when it splits the logical publication into EPUB content files.</p>\n<hr />\n<h2>Preserve fragment links</h2>\n<p>A source link might point to a section inside another document:</p>\n<pre><code class=\"language-text\">d-document.html#validation\n</code></pre>\n<p>The preparation step should preserve the fragment relationship.</p>\n<p>A possible transformed identifier scheme is:</p>\n<pre><code class=\"language-text\">#d-document--validation\n</code></pre>\n<p>This avoids collisions between common section IDs such as:</p>\n<pre><code class=\"language-text\">#overview\n#testing\n#architecture\n</code></pre>\n<p>which may occur in many documents.</p>\n<p>A deterministic identifier namespace is worth establishing early.</p>\n<hr />\n<h2>Key issue 4: EPUB splitting</h2>\n<p>Pandoc's:</p>\n<pre><code class=\"language-bash\">--split-level=N\n</code></pre>\n<p>controls the heading level at which the EPUB is internally divided into separate content files.</p>\n<p>The default is level 1.</p>\n<p>For example:</p>\n<pre><code class=\"language-bash\">--split-level=2\n</code></pre>\n<p>may be useful if top-level sections contain many documents.</p>\n<p>This option does <strong>not</strong> define what users see as the hierarchy. It affects the internal composition of the EPUB.</p>\n<p>Therefore keep these concepts separate:</p>\n<pre><code class=\"language-text\">heading hierarchy\n    → table of contents structure\n\n--split-level\n    → EPUB internal file boundaries\n</code></pre>\n<p>The correct split level should be determined empirically once the full snapshot is tested in Apple Books.</p>\n<hr />\n<h2>Key issue 5: CSS</h2>\n<p>Pandoc supplies basic EPUB styling by default.</p>\n<p>A project-specific stylesheet can be supplied using:</p>\n<pre><code class=\"language-bash\">--css=epub.css\n</code></pre>\n<p>The existing <code>styles.css</code> should probably not be imported wholesale.</p>\n<p>The Docs Viewer stylesheet contains application presentation as well as document presentation.</p>\n<p>Create a dedicated EPUB stylesheet containing only semantic document rules such as:</p>\n<pre><code class=\"language-text\">body\nheadings\nparagraphs\nlists\nlinks\ntables\ncode\npre\nblockquote\nfigure\nfigcaption\nimages\n</code></pre>\n<p>Avoid:</p>\n<pre><code class=\"language-text\">toolbars\nbuttons\npanels\nmenus\nhover states\nmanage-mode UI\napplication layout\n</code></pre>\n<p>The EPUB should look like a publication, not a frozen browser application.</p>\n<hr />\n<h2>Key issue 6: images and other media</h2>\n<p>Pandoc's EPUB writer normally includes media referenced by supported media elements in the generated EPUB, producing a self-contained publication.</p>\n<p>This is useful for the snapshot requirement.</p>\n<p>Typical local assets should therefore become part of the EPUB:</p>\n<pre><code class=\"language-text\">generated SVG diagram\nlocal image\nillustration\ndocument figure\n</code></pre>\n<p>External links can remain external when they genuinely reference external information.</p>\n<p>An explicit policy is useful:</p>\n<pre><code class=\"language-text\">required snapshot asset\n    → embed in EPUB\n\nexternal reference\n    → preserve external link\n</code></pre>\n<p>If Pandoc has difficulty locating local assets, resource lookup paths need to be considered.</p>\n<hr />\n<h2>Key issue 7: relative asset paths</h2>\n<p>The existing HTML documents may reference resources relative to their own location:</p>\n<pre><code class=\"language-text\">docs/document.html\n    ↳ ../assets/image.svg\n</code></pre>\n<p>Once content is extracted into <code>book.html</code>, those paths may no longer resolve correctly.</p>\n<p>The preparation step should either:</p>\n<ol>\n<li>rewrite asset paths relative to the temporary publication root; or</li>\n<li>arrange the temporary file so existing relative paths remain valid.</li>\n</ol>\n<p>Pandoc has resource-search options, but path rewriting should still be deterministic rather than relying on the process working directory accidentally matching the export structure.</p>\n<hr />\n<h2>Key issue 8: JavaScript</h2>\n<p>The EPUB snapshot should not attempt to carry the Docs Viewer runtime into the book.</p>\n<p>Remove or statically replace:</p>\n<pre><code class=\"language-text\">application scripts\ndynamic document loading\nruntime JSON fetching\nmanagement controls\ninteractive navigation\n</code></pre>\n<p>Where JavaScript currently produces a visual result, prefer a pre-rendered static equivalent.</p>\n<p>For example:</p>\n<pre><code class=\"language-text\">Mermaid source\n    ↓ build stage\nSVG\n    ↓ EPUB export\nembedded SVG\n</code></pre>\n<p>rather than:</p>\n<pre><code class=\"language-text\">Mermaid JavaScript runtime inside EPUB\n</code></pre>\n<hr />\n<h2>Key issue 9: HTML conversion can be lossy</h2>\n<p>Pandoc parses source formats into its internal document model before writing the target format.</p>\n<p>Its own documentation cautions that conversions from richer formats such as HTML may lose constructs that do not map cleanly into that model.</p>\n<p>This should not be treated as a reason not to use Pandoc.</p>\n<p>Instead, test the actual semantic HTML used by the Docs Viewer.</p>\n<p>Representative cases should include:</p>\n<ul>\n<li>headings;</li>\n<li>paragraphs;</li>\n<li>emphasis;</li>\n<li>links;</li>\n<li>lists;</li>\n<li>nested lists;</li>\n<li>code blocks;</li>\n<li>tables;</li>\n<li>block quotes;</li>\n<li>images;</li>\n<li>SVG diagrams;</li>\n<li>figures/captions;</li>\n<li>any custom semantic tokens rendered into HTML.</li>\n</ul>\n<p>The proof should identify unsupported cases before a production exporter is built.</p>\n<hr />\n<h2>Key issue 10: metadata</h2>\n<p>Pandoc can add normal EPUB metadata including:</p>\n<pre><code class=\"language-text\">title\ncreator\ndate\nlanguage\nidentifier\ndescription\n</code></pre>\n<p>A snapshot should have a deterministic identity rather than relying on an automatically generated identifier.</p>\n<p>For example:</p>\n<pre><code class=\"language-text\">Title: Studio Docs\nIdentifier: studio-docs-2026-08-16\nDate: 2026-08-16\nLanguage: en-GB\n</code></pre>\n<p>A metadata file or command-line options can provide this without modifying the input HTML.</p>\n<hr />\n<h2>Recommended command shape</h2>\n<p>After preparation, a command might eventually resemble:</p>\n<pre><code class=\"language-bash\">pandoc   build/epub/book.html   --from=html   --to=epub3   --toc   --toc-depth=6   --split-level=2   --css=build/epub/epub.css   --metadata-file=build/epub/metadata.yaml   --output=dist/studio-docs-2026-08-16.epub\n</code></pre>\n<p>The precise <code>toc-depth</code> and <code>split-level</code> values should come from testing.</p>\n<p>They should not be treated as fixed architectural decisions yet.</p>\n<hr />\n<h2>A Pandoc defaults file may be cleaner</h2>\n<p>Once the proof is stable, static command options can move into a Pandoc defaults file.</p>\n<p>Conceptually:</p>\n<pre><code class=\"language-yaml\">from: html\nto: epub3\ntoc: true\ntoc-depth: 6\nsplit-level: 2\n\ncss:\n  - epub.css\n</code></pre>\n<p>The export script can then concentrate on:</p>\n<pre><code class=\"language-text\">prepare HTML\nprepare metadata\ncall Pandoc\nvalidate result\n</code></pre>\n<p>rather than constructing a long command line.</p>\n<hr />\n<h2>Validation</h2>\n<p>The generated EPUB should be checked with <strong>EPUBCheck</strong>.</p>\n<p>EPUBCheck is the W3C EPUB conformance checker.</p>\n<p>The pipeline should become:</p>\n<pre><code class=\"language-text\">prepare HTML\n    ↓\nPandoc\n    ↓\nEPUB\n    ↓\nEPUBCheck\n    ↓\nsnapshot complete\n</code></pre>\n<p>A conforming EPUB is not guaranteed merely because Pandoc successfully produced a file.</p>\n<p>Any EPUBCheck errors should fail the snapshot build.</p>\n<hr />\n<h2>Suggested proof of concept</h2>\n<p>Do not begin with the complete document collection.</p>\n<p>Choose approximately 5–10 representative documents that include:</p>\n<ul>\n<li>at least three navigation depths;</li>\n<li>links between documents;</li>\n<li>a link to a subsection in another document;</li>\n<li>code;</li>\n<li>a table;</li>\n<li>an image;</li>\n<li>an SVG diagram if used;</li>\n<li>ordinary external links.</li>\n</ul>\n<p>The proof should answer:</p>\n<pre><code class=\"language-text\">Can the existing HTML content survive Pandoc conversion?\n\nDoes the hierarchy appear correctly in Apple Books?\n\nDo cross-document links work?\n\nDo fragment links work?\n\nAre local images/SVGs embedded?\n\nDoes the CSS produce a readable publication?\n\nDoes EPUBCheck pass?\n</code></pre>\n<p>Only then scale up to all documents.</p>\n<hr />\n<h2>Suggested implementation stages</h2>\n<h3>Stage 1 — raw Pandoc experiment</h3>\n<p>Try a small number of existing HTML files directly.</p>\n<p>Purpose:</p>\n<ul>\n<li>understand how Pandoc parses the current generated HTML;</li>\n<li>identify whether standalone document wrappers are a problem;</li>\n<li>inspect styling and media behaviour.</li>\n</ul>\n<p>Do not build infrastructure yet.</p>\n<h3>Stage 2 — minimal preparation script</h3>\n<p>Add only what the proof demonstrates is necessary:</p>\n<ul>\n<li>extract document body/main content;</li>\n<li>establish document IDs;</li>\n<li>rewrite internal links;</li>\n<li>establish hierarchy headings.</li>\n</ul>\n<p>Produce:</p>\n<pre><code class=\"language-text\">book.html\n</code></pre>\n<p>Then pass that to Pandoc.</p>\n<h3>Stage 3 — asset and style handling</h3>\n<p>Add:</p>\n<ul>\n<li>EPUB-specific CSS;</li>\n<li>image/SVG path handling;</li>\n<li>validation for missing local assets.</li>\n</ul>\n<h3>Stage 4 — complete hierarchy</h3>\n<p>Use the existing <code>index.html</code> tree to generate:</p>\n<ul>\n<li>full document order;</li>\n<li>correct heading depth;</li>\n<li>deterministic navigation.</li>\n</ul>\n<h3>Stage 5 — production snapshot</h3>\n<p>Add:</p>\n<ul>\n<li>metadata;</li>\n<li>timestamp/version naming;</li>\n<li>EPUBCheck;</li>\n<li>concise export reporting.</li>\n</ul>\n<hr />\n<h2>Expected production pipeline</h2>\n<p>The likely final pipeline remains small:</p>\n<pre><code class=\"language-text\">Docs Viewer HTML export\n          │\n          ▼\nprepare_epub.py\n          │\n          ├── parse index hierarchy\n          ├── extract document content\n          ├── normalize heading levels\n          ├── rewrite internal links\n          ├── normalize asset paths\n          └── produce book.html\n          │\n          ▼\nPandoc\n          │\n          ▼\nstudio-docs-YYYY-MM-DD.epub\n          │\n          ▼\nEPUBCheck\n</code></pre>\n<p>Pandoc remains the EPUB implementation.</p>\n<p>Python remains only the adapter between the Docs Viewer HTML structure and Pandoc's logical-document model.</p>\n<hr />\n<h2>Recommended position</h2>\n<p>Pandoc should be tested before implementing a dedicated EPUB library integration.</p>\n<p>The existing system already has the hard part — the generated documents and their hierarchy.</p>\n<p>The export problem is therefore not primarily:</p>\n<blockquote>\n<p>How do we construct an EPUB?</p>\n</blockquote>\n<p>Pandoc already solves that.</p>\n<p>The actual project-specific problem is:</p>\n<blockquote>\n<p>How do we present the existing HTML collection to Pandoc as one coherent hierarchical publication while preserving document identity, links and assets?</p>\n</blockquote>\n<p>A small HTML preparation step is likely sufficient.</p>\n<p>This keeps EPUB export simple, local and disposable: if the publication format or tool changes later, no EPUB-specific model becomes embedded deeply in the Docs Viewer architecture.</p>\n<hr />\n<h2>References</h2>\n<p>Implementation assumptions in this note were checked against the current Pandoc User's Guide and EPUB documentation in August 2026, including:</p>\n<ul>\n<li>multiple input-file behaviour;</li>\n<li><code>--file-scope</code>;</li>\n<li>EPUB <code>--split-level</code>;</li>\n<li>EPUB CSS handling;</li>\n<li>linked-media embedding;</li>\n<li>EPUB metadata;</li>\n<li>Pandoc's HTML conversion model.</li>\n</ul>\n<p>Final output should be checked with the W3C EPUBCheck conformance checker.</p>\n"
}
