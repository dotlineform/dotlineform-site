{
  "doc_id": "d-20260723-203332-3aba41",
  "title": "Recommended JSDoc Approach for the Repository",
  "added_date": "2026-07-23 20:33:32",
  "last_updated": "2026-07-23 20:35:05",
  "viewer_url": "/docs/?scope=studio&doc=d-20260723-203332-3aba41",
  "parent_id": "d-20260713-000000-d9bad2",
  "ui_status": "research",
  "content_html": "<h1>Recommended JSDoc Approach for the Repository</h1>\n<blockquote>\n<p>Research doc: not indicative of current implementation.</p>\n</blockquote>\n<h2>Purpose</h2>\n<p>This guide explains how JSDoc comments relate to JavaScript syntax and recommends a practical way to introduce JSDoc into the repository.</p>\n<p>The objective is not to add comments everywhere. It is to make important JavaScript interfaces, data structures, responsibilities, and domain rules easier for:</p>\n<ul>\n<li>developers to understand;</li>\n<li>VS Code to inspect;</li>\n<li>coding agents to retrieve and interpret;</li>\n<li>linting and documentation tools to validate;</li>\n<li>future maintainers to change safely.</li>\n</ul>\n<hr />\n<h2>1. What <code>/** ... */</code> means</h2>\n<p>JavaScript has block-comment syntax:</p>\n<pre><code class=\"language-js\">/*\n * This is an ordinary block comment.\n */\n</code></pre>\n<p>A JSDoc comment uses:</p>\n<pre><code class=\"language-js\">/**\n * This is a documentation comment.\n */\n</code></pre>\n<p>The JavaScript engine does not assign any special meaning to the extra <code>*</code>.</p>\n<p>To JavaScript, both forms are comments and are ignored at runtime:</p>\n<pre><code class=\"language-text\">/*   JavaScript block-comment syntax\n/**  documentation-comment convention recognised by tools\n</code></pre>\n<p>The distinction exists because external tools recognise comments beginning with <code>/**</code> as structured documentation.</p>\n<p>These tools can include:</p>\n<ul>\n<li>JSDoc;</li>\n<li>VS Code;</li>\n<li>the TypeScript language service;</li>\n<li>documentation generators;</li>\n<li>ESLint plugins;</li>\n<li>coding agents;</li>\n<li>static-analysis tools.</li>\n</ul>\n<p>For example:</p>\n<pre><code class=\"language-js\">/*\n * Ordinary implementation comment.\n */\nfunction resetCache() {\n  // ...\n}\n</code></pre>\n<pre><code class=\"language-js\">/**\n * Clears the in-memory work index without deleting generated files.\n */\nfunction resetWorkIndexCache() {\n  // ...\n}\n</code></pre>\n<p>Both are valid JavaScript comments. The second can be associated with the function as documentation.</p>\n<hr />\n<h2>2. What JSDoc provides</h2>\n<p>JSDoc provides a standard structure for documenting JavaScript.</p>\n<p>Common tags include:</p>\n<pre><code class=\"language-js\">@param\n@returns\n@throws\n@typedef\n@property\n@template\n@deprecated\n@example\n@module\n</code></pre>\n<p>A typical function comment might be:</p>\n<pre><code class=\"language-js\">/**\n * Builds the URL for an existing work-image derivative.\n *\n * This function selects a URL. It does not generate or upload image files.\n *\n * @param {string} workId - Canonical work identifier.\n * @param {800 | 1200 | 1600 | 2400} width - Required derivative width.\n * @param {MediaConfig} config - Local or remote media configuration.\n * @returns {string} Absolute or site-relative image URL.\n */\nexport function buildWorkImageUrl(workId, width, config) {\n  return `${config.mediaBase}${config.mediaPrefix}/${workId}-primary-${width}.webp`;\n}\n</code></pre>\n<p>JSDoc-aware tools can use this information to provide:</p>\n<ul>\n<li>hover documentation;</li>\n<li>parameter hints;</li>\n<li>type checking;</li>\n<li>autocomplete;</li>\n<li>navigation;</li>\n<li>generated API documentation;</li>\n<li>more useful repository search results.</li>\n</ul>\n<hr />\n<h2>3. What JSDoc does not provide</h2>\n<p>JSDoc does not automatically discover the important meaning of the code.</p>\n<p>It does not independently know that:</p>\n<pre><code class=\"language-text\">This function selects an existing derivative.\nIt does not generate the image.\n</code></pre>\n<p>That information must be supplied by:</p>\n<ul>\n<li>the developer;</li>\n<li>a coding agent inspecting the implementation;</li>\n<li>tests;</li>\n<li>architecture documentation;</li>\n<li>domain knowledge.</li>\n</ul>\n<p>An editor may generate a mechanical skeleton:</p>\n<pre><code class=\"language-js\">/**\n *\n * @param {*} workId\n * @param {*} width\n * @param {*} config\n */\n</code></pre>\n<p>However, that skeleton does not explain:</p>\n<ul>\n<li>the function’s responsibility;</li>\n<li>the meaning of each parameter;</li>\n<li>allowed values;</li>\n<li>side effects;</li>\n<li>invariants;</li>\n<li>privacy rules;</li>\n<li>ordering rules;</li>\n<li>what the function deliberately does not do.</li>\n</ul>\n<p>The useful part of JSDoc is therefore authored semantic content, not merely the comment markers.</p>\n<hr />\n<h2>4. Recommended division of responsibility</h2>\n<p>A practical division of responsibility is:</p>\n<pre><code class=\"language-text\">JSDoc and editor tooling\n    provide the structured comment format\n\nCodex or another coding agent\n    drafts documentation from code, tests, and surrounding modules\n\nDeveloper\n    confirms architectural intent and domain rules\n\nTests and linting\n    provide executable checks and consistency\n</code></pre>\n<p>Codex can draft JSDoc efficiently, but it should not add generic comments to every function without review.</p>\n<p>The important questions are:</p>\n<ul>\n<li>Is the description accurate?</li>\n<li>Does it explain something not obvious from the code?</li>\n<li>Does it identify subsystem boundaries?</li>\n<li>Does it state important constraints?</li>\n<li>Is it likely to remain stable?</li>\n<li>Is the function important enough to document?</li>\n</ul>\n<hr />\n<h2>5. Recommended scope</h2>\n<p>JSDoc should initially be added selectively.</p>\n<h3>Document first</h3>\n<p>Prioritise:</p>\n<ul>\n<li>exported functions;</li>\n<li>shared utility modules;</li>\n<li>configuration objects;</li>\n<li>public classes and methods;</li>\n<li>callbacks with non-obvious contracts;</li>\n<li>functions that transform canonical data;</li>\n<li>functions with side effects;</li>\n<li>code handling public versus private data;</li>\n<li>code with important ordering behaviour;</li>\n<li>module boundaries;</li>\n<li>complex data structures;</li>\n<li>values with constrained options.</li>\n</ul>\n<h3>Usually do not document</h3>\n<p>Avoid routine JSDoc for:</p>\n<ul>\n<li>obvious one-line helpers;</li>\n<li>simple local variables;</li>\n<li>trivial getters;</li>\n<li>implementation details already made clear by names;</li>\n<li>generated files;</li>\n<li>abandoned or deprecated code that should instead be removed;</li>\n<li>every private helper by default.</li>\n</ul>\n<p>Weak example:</p>\n<pre><code class=\"language-js\">/**\n * Returns the title.\n *\n * @param {object} work - The work.\n * @returns {string} The title.\n */\nfunction getTitle(work) {\n  return work.title;\n}\n</code></pre>\n<p>This adds little information.</p>\n<p>Better use of documentation:</p>\n<pre><code class=\"language-js\">/**\n * Returns the public display title for a work.\n *\n * Untitled works use the configured fallback label. Private catalogue notes\n * must never be used as display text.\n *\n * @param {WorkRecord} work - Canonical work record.\n * @param {string} fallback - Label used when no public title is available.\n * @returns {string} Public display title.\n */\nfunction getWorkDisplayTitle(work, fallback) {\n  return work.title?.trim() || fallback;\n}\n</code></pre>\n<hr />\n<h2>6. Recommended comment content</h2>\n<p>A useful JSDoc comment should explain some combination of the following.</p>\n<h3>Purpose</h3>\n<pre><code class=\"language-js\">/**\n * Builds the lightweight work entry used by the client-side search index.\n */\n</code></pre>\n<h3>Responsibility boundary</h3>\n<pre><code class=\"language-js\">/**\n * Resolves derivative URLs at runtime.\n *\n * This module does not create, upload, or validate image files.\n */\n</code></pre>\n<h3>Parameter meaning</h3>\n<pre><code class=\"language-js\">/**\n * @param {string[]} seriesIds\n *   Canonical series identifiers in editorial order.\n */\n</code></pre>\n<h3>Return contract</h3>\n<pre><code class=\"language-js\">/**\n * @returns {Promise&lt;WorkRecord | null&gt;}\n *   The public work record, or `null` when the work does not exist.\n */\n</code></pre>\n<h3>Side effects</h3>\n<pre><code class=\"language-js\">/**\n * Writes the updated route configuration to local storage.\n *\n * @returns {void}\n */\n</code></pre>\n<h3>Exceptions</h3>\n<pre><code class=\"language-js\">/**\n * @throws {TypeError} If the supplied score is not an integer from 0 to 5.\n */\n</code></pre>\n<h3>Invariants</h3>\n<pre><code class=\"language-js\">/**\n * Work IDs are stable strings and must never be parsed as numbers.\n */\n</code></pre>\n<h3>Semantic distinctions</h3>\n<pre><code class=\"language-js\">/**\n * A null score means unscored and is excluded from statistics.\n * Zero means explicitly absent and is included.\n */\n</code></pre>\n<h3>Ordering rules</h3>\n<pre><code class=\"language-js\">/**\n * Preserves editorial order. Do not sort the returned IDs lexically.\n */\n</code></pre>\n<h3>Privacy rules</h3>\n<pre><code class=\"language-js\">/**\n * Returns a public work record.\n *\n * Internal notes and storage-location metadata are excluded.\n */\n</code></pre>\n<hr />\n<h2>7. Module-level documentation</h2>\n<p>Important files should begin with a short statement of responsibility.</p>\n<pre><code class=\"language-js\">/**\n * Work image URL utilities.\n *\n * Maps canonical work IDs and derivative widths to local or remote URLs.\n * Image generation and upload are handled by the offline media pipeline.\n *\n * @module work-image-urls\n */\n</code></pre>\n<p>This is particularly useful where several parts of the repository deal with the same general concept.</p>\n<p>For example:</p>\n<pre><code class=\"language-text\">processing/\n    creates artwork or source visual assets\n\nscripts/images/\n    generates derivatives\n\nassets/js/media/\n    selects image URLs at runtime\n\nR2\n    stores primary derivatives and attachments\n</code></pre>\n<p>A module-level comment helps prevent an agent from confusing those responsibilities.</p>\n<hr />\n<h2>8. Shared object types with <code>@typedef</code></h2>\n<p>JSDoc can define reusable object structures.</p>\n<pre><code class=\"language-js\">/**\n * @typedef {object} MediaConfig\n * @property {string} mediaBase\n *   Empty for repository-local assets or an absolute remote origin.\n * @property {string} mediaPrefix\n *   Path prefix below the origin.\n * @property {boolean} useRemotePrimaries\n *   Whether primary derivatives are served from remote storage.\n */\n</code></pre>\n<p>The type can then be reused:</p>\n<pre><code class=\"language-js\">/**\n * Returns the configured media root.\n *\n * @param {MediaConfig} config\n * @returns {string}\n */\nexport function getMediaRoot(config) {\n  return `${config.mediaBase}${config.mediaPrefix}`;\n}\n</code></pre>\n<p>Another example:</p>\n<pre><code class=\"language-js\">/**\n * @typedef {object} WorkSummary\n * @property {string} work_id\n * @property {string | null} title\n * @property {number | null} year\n * @property {string[]} series_ids\n * @property {number} details_count\n * @property {boolean} has_details\n */\n</code></pre>\n<p>This gives developers and agents a searchable declaration of the data model.</p>\n<hr />\n<h2>9. Constrained values</h2>\n<p>JSDoc can record allowed values.</p>\n<pre><code class=\"language-js\">/**\n * @typedef {0 | 1 | 2 | 3 | 4 | 5} DimensionScore\n */\n</code></pre>\n<pre><code class=\"language-js\">/**\n * @typedef {&quot;unipolar&quot; | &quot;bipolar&quot; | &quot;axis2d&quot; | &quot;vector&quot;} DimensionShape\n */\n</code></pre>\n<pre><code class=\"language-js\">/**\n * @param {DimensionScore | null} score\n * @returns {boolean}\n */\nexport function includeScoreInStatistics(score) {\n  return score !== null;\n}\n</code></pre>\n<p>This helps VS Code and agents distinguish valid values from arbitrary numbers or strings.</p>\n<hr />\n<h2>10. Type checking without converting to TypeScript</h2>\n<p>JSDoc can provide meaningful type checking while retaining <code>.js</code> files.</p>\n<p>A file can opt into TypeScript-style checking with:</p>\n<pre><code class=\"language-js\">// @ts-check\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-js\">// @ts-check\n\n/**\n * @param {number} a\n * @param {number} b\n * @returns {number}\n */\nexport function add(a, b) {\n  return a + b;\n}\n</code></pre>\n<p>An incorrect call may then be reported by VS Code:</p>\n<pre><code class=\"language-js\">add(&quot;1&quot;, 2);\n</code></pre>\n<p>For project-wide checking, a <code>jsconfig.json</code> can be introduced later:</p>\n<pre><code class=\"language-json\">{\n  &quot;compilerOptions&quot;: {\n    &quot;checkJs&quot;: true,\n    &quot;allowJs&quot;: true,\n    &quot;module&quot;: &quot;ESNext&quot;,\n    &quot;target&quot;: &quot;ES2022&quot;,\n    &quot;strict&quot;: false\n  },\n  &quot;exclude&quot;: [\n    &quot;_site&quot;,\n    &quot;node_modules&quot;,\n    &quot;vendor&quot;,\n    &quot;assets/generated&quot;\n  ]\n}\n</code></pre>\n<p>A conservative approach is preferable:</p>\n<ol>\n<li>use JSDoc without project-wide strict checking;</li>\n<li>add <code>// @ts-check</code> to selected modules;</li>\n<li>resolve genuine problems;</li>\n<li>expand gradually;</li>\n<li>enable broader <code>checkJs</code> only when the repository is ready.</li>\n</ol>\n<hr />\n<h2>11. Relationship with ESLint</h2>\n<p>JSDoc and ESLint solve different problems.</p>\n<p>JSDoc contributes:</p>\n<ul>\n<li>intent;</li>\n<li>contracts;</li>\n<li>parameter and return information;</li>\n<li>domain terminology;</li>\n<li>object shapes;</li>\n<li>responsibility boundaries.</li>\n</ul>\n<p>ESLint contributes:</p>\n<ul>\n<li>consistency;</li>\n<li>unused-code detection;</li>\n<li>undefined-name detection;</li>\n<li>suspicious-pattern detection;</li>\n<li>clearer module structure;</li>\n<li>reduced repository noise.</li>\n</ul>\n<p>Together:</p>\n<pre><code class=\"language-text\">JSDoc\n    explains what the code means\n\nESLint\n    helps ensure the code is mechanically coherent\n</code></pre>\n<p>An ESLint configuration should not initially require JSDoc on every function. That would encourage low-value comments.</p>\n<p>Doc-comment enforcement can be added later for selected public APIs if necessary.</p>\n<hr />\n<h2>12. Relationship with tests</h2>\n<p>Important behaviour should not exist only in comments.</p>\n<p>For example:</p>\n<pre><code class=\"language-js\">/**\n * Private notes must never be included in public work JSON.\n */\n</code></pre>\n<p>should ideally be supported by a test:</p>\n<pre><code class=\"language-js\">test(&quot;public work records exclude private notes&quot;, () =&gt; {\n  const source = {\n    work_id: &quot;0001&quot;,\n    title: &quot;Example&quot;,\n    notes_private: &quot;Internal only&quot;,\n  };\n\n  const result = buildPublicWorkRecord(source);\n\n  expect(result).not.toHaveProperty(&quot;notes_private&quot;);\n});\n</code></pre>\n<p>A useful rule is:</p>\n<pre><code class=\"language-text\">JSDoc explains the rule.\nTests verify the rule.\n</code></pre>\n<p>For especially important data structures, schema validation can add another layer.</p>\n<hr />\n<h2>13. Recommended rollout</h2>\n<h3>Stage 1: identify important modules</h3>\n<p>Start with modules that:</p>\n<ul>\n<li>are used widely;</li>\n<li>define shared data structures;</li>\n<li>sit at subsystem boundaries;</li>\n<li>contain domain-specific behaviour;</li>\n<li>are regularly changed by Codex.</li>\n</ul>\n<p>Possible initial areas include:</p>\n<ul>\n<li>media URL handling;</li>\n<li>search indexing and result shaping;</li>\n<li>work and series data loading;</li>\n<li>route configuration;</li>\n<li>tag and dimension models;</li>\n<li>generated JSON consumers.</li>\n</ul>\n<h3>Stage 2: add module-level comments</h3>\n<p>Add a concise responsibility statement to each selected file.</p>\n<pre><code class=\"language-js\">/**\n * Client-side work data loading and cache management.\n *\n * Loads generated public JSON. It does not modify canonical source data.\n */\n</code></pre>\n<h3>Stage 3: document exported functions</h3>\n<p>For each exported function, document:</p>\n<ul>\n<li>purpose;</li>\n<li>parameters;</li>\n<li>return value;</li>\n<li>side effects;</li>\n<li>exceptions;</li>\n<li>important rules.</li>\n</ul>\n<h3>Stage 4: define shared object types</h3>\n<p>Add <code>@typedef</code> declarations for important structures such as:</p>\n<ul>\n<li><code>WorkSummary</code>;</li>\n<li><code>WorkRecord</code>;</li>\n<li><code>SeriesRecord</code>;</li>\n<li><code>MediaConfig</code>;</li>\n<li><code>SearchResult</code>;</li>\n<li><code>RouteConfig</code>;</li>\n<li><code>DimensionDefinition</code>.</li>\n</ul>\n<h3>Stage 5: introduce selective type checking</h3>\n<p>Use:</p>\n<pre><code class=\"language-js\">// @ts-check\n</code></pre>\n<p>in stable, well-documented modules.</p>\n<p>Do not enable strict project-wide checking until the initial errors have been assessed.</p>\n<h3>Stage 6: connect checks to the normal workflow</h3>\n<p>A standard validation sequence might be:</p>\n<pre><code class=\"language-bash\">npx eslint .\nnpm test\n</code></pre>\n<p>Later:</p>\n<pre><code class=\"language-bash\">npx tsc --noEmit --allowJs --checkJs\n</code></pre>\n<p>or an equivalent project command can be added if JSDoc-based checking becomes part of the repository workflow.</p>\n<hr />\n<h2>14. Suggested Codex task</h2>\n<p>A useful initial Codex instruction would be:</p>\n<pre><code class=\"language-markdown\">Introduce JSDoc selectively in the JavaScript codebase.\n\nPrioritise:\n\n- module-level responsibility comments;\n- exported functions;\n- shared configuration and data structures;\n- non-obvious domain rules;\n- side effects and subsystem boundaries.\n\nDo not add comments that merely restate the implementation.\n\nUse `@typedef` for important shared object structures and reference those\ntypes from function parameters and return values.\n\nWhere documentation intent cannot be inferred confidently from code, tests,\nor existing architecture documents, flag it for review rather than inventing\na rule.\n\nDo not enable strict project-wide JSDoc enforcement in this change.\n</code></pre>\n<p>A more focused first pass might be:</p>\n<pre><code class=\"language-markdown\">Add JSDoc to the media URL and derivative-selection modules.\n\nMake clear which functions:\n\n- select existing image URLs;\n- generate image derivatives;\n- upload media;\n- handle repository-local thumbnails;\n- handle remote primary images.\n\nDo not change runtime behaviour.\n</code></pre>\n<hr />\n<h2>15. Review checklist</h2>\n<p>When reviewing generated JSDoc, check:</p>\n<ul>\n<li>Does the comment explain purpose rather than syntax?</li>\n<li>Are parameter descriptions specific?</li>\n<li>Are nullable values correctly represented?</li>\n<li>Are allowed values constrained where useful?</li>\n<li>Are side effects stated?</li>\n<li>Are important exceptions stated?</li>\n<li>Are privacy rules represented accurately?</li>\n<li>Are ordering rules explicit?</li>\n<li>Does the comment identify what the module does not do?</li>\n<li>Does the documentation agree with tests and current behaviour?</li>\n<li>Is the comment concise enough to remain maintainable?</li>\n<li>Would removing the comment lose useful information?</li>\n</ul>\n<p>If the answer to the final question is no, the comment may not be necessary.</p>\n<hr />\n<h2>16. Recommended repository policy</h2>\n<pre><code class=\"language-markdown\">## JavaScript documentation policy\n\n- Use `/** ... */` for structured JSDoc comments associated with modules,\n  types, exported functions, classes, and important interfaces.\n- Remember that `/** ... */` remains ordinary JavaScript comment syntax at\n  runtime; external tools give it its documentation meaning.\n- Document purpose, contracts, side effects, invariants, exclusions, and\n  responsibility boundaries.\n- Do not add comments that merely restate obvious code.\n- Use `@typedef` for shared object structures and constrained values.\n- Prefer tests or validation for critical behavioural rules.\n- Keep documentation current in the same change as the implementation.\n- Introduce JSDoc and type checking gradually rather than enforcing comments\n  on every function immediately.\n</code></pre>\n<hr />\n<h2>Conclusion</h2>\n<p>The extra <code>*</code> in <code>/** ... */</code> is not special JavaScript runtime syntax. It is a documentation convention built on ordinary block comments.</p>\n<p>JSDoc provides the structure and tooling, but the meaningful description still has to be authored.</p>\n<p>The recommended approach is therefore selective:</p>\n<pre><code class=\"language-text\">module responsibilities\n+ exported function contracts\n+ shared typedefs\n+ domain rules\n+ selective type checking\n+ ESLint\n+ tests\n</code></pre>\n<p>This should improve both human maintenance and coding-agent context without filling the repository with repetitive comments.</p>\n"
}
