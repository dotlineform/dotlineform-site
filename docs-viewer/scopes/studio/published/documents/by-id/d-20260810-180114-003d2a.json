{
  "doc_id": "d-20260810-180114-003d2a",
  "title": "Report Blocks",
  "added_date": "2026-08-10 18:01:14",
  "last_updated": "2026-08-10 18:26:10",
  "viewer_url": "/docs/?scope=studio&doc=d-20260810-180114-003d2a",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "replace document-level report front matter with one exact block token while preserving report-host identity and access boundaries",
  "ui_status": "done",
  "content_html": "<h1>Report Blocks</h1>\n<h2>Purpose</h2>\n<p>Replace document-level report assignment front matter with one report block in the authored Markdown body. The block positions the document's report exactly among its surrounding content while retaining the document as the sole report host.</p>\n<p>This feature changes report assignment and placement. It does not turn a document into a container for multiple or parameterised report instances.</p>\n<h2>Current Starting Point</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260513-105343-6de36b\">Reports</a> owns the current report registry, metadata, implementations, inventory, and front-matter report-host model. A source document currently declares <code>viewer_report</code> and optional <code>viewer_report_scope</code>, <code>viewer_report_access</code>, <code>viewer_report_preset</code>, and <code>viewer_report_subscope</code> fields. The browser normalises that singular document-level descriptor and appends one asynchronously mounted report after the rendered Markdown content.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260810-173738-703ecd\">Report Capability and Composition</a> owns the durable shared, public, local-only, data, contribution, and stylesheet boundaries. A report block does not alter those boundaries or promote a local report.</p>\n<p>The <code>docs_subscope</code> report-host declaration is additionally consumed by sub-scope lifecycle, document-location, publication, deletion, and transfer services. Its exact parent document remains the route and lifecycle host for the configured child collection.</p>\n<p>No implementation is authorised by this feature document. <a href=\"/docs/?scope=studio&amp;doc=d-20260810-182610-c274f0\">RB-1 — Report Blocks Cutover Delivery</a> is proposed and not started.</p>\n<h2>Feature Contract</h2>\n<ul>\n<li>A document contains zero or one report block. More than one report block in one document is invalid source and fails validation; blocks are never merged, ordered as instances, or resolved by first match.</li>\n<li>The report block is a block-level authored component at one exact Markdown position. It is not an inline link, a semantic-token occurrence, or an instruction to infer placement from neighbouring content.</li>\n<li>The document's immutable <code>doc_id</code> remains the report-host identity. There is no separate report-instance id or instance registry.</li>\n<li>The block selects one registered report and may express only the existing closed, code-owned assignment context: access intent, scope, preset, and sub-scope where applicable. The exact source syntax remains to be fixed at the first delivery sanity check.</li>\n<li>A preset remains a registered report variation. It is not an arbitrary parameter envelope.</li>\n<li>Report blocks do not accept free-form arguments, JSON configuration, initial filters, query expressions, column choices, or report-specific ad hoc attributes.</li>\n<li>Filtering, sorting, paging, selection, refresh state, and other interactions remain report-owned browser or route state. They are not persisted in the report block.</li>\n<li>Report identity, access, preset, scope, and sub-scope are validated against code-owned contracts. The block cannot name a loader path, module URL, service endpoint, data file, or stylesheet.</li>\n</ul>\n<h2>Source And Projection Boundary</h2>\n<p>One report-source owner parses and validates the authored block, then exposes one normalised document-level report descriptor to builders and services. Report consumers do not independently scan raw Markdown or interpret token strings.</p>\n<p>The docs builder replaces the valid block with one inert report host at that exact content position and emits the normalised descriptor in the document payload. Browser composition mounts the allowed report into that generated host; it does not append another report root or treat arbitrary authored HTML as executable report configuration.</p>\n<p>Manage projection may retain public or local report descriptors according to the existing local loader and access rules. Public projection emits only the report assignment and host allowed by the existing public metadata, loader, data, and module boundaries. <code>access: public</code> remains intent rather than promotion, and a local-only block cannot make local code or data available publicly.</p>\n<p>Source workflows that copy, import, review, export, or rewrite a complete document must treat the report block as structural source. The implementation sanity check must preserve or deliberately replace each current report-host behavior; the block must not bypass report validation merely because it is stored in the Markdown body.</p>\n<h2>Sub-Scope Host Boundary</h2>\n<p>A <code>docs_subscope</code> block selects one exact configured sub-scope and remains constrained to one parent report-host document. Lifecycle configuration continues to record the immutable host <code>doc_id</code>, and child routes continue to compose through that document plus the exact <code>subdoc</code> target.</p>\n<p>Sub-scope lifecycle, document-location, publication, deletion, and transfer services consume the same normalised report descriptor as the builder. They do not retain private front-matter readers or add their own report-token parser.</p>\n<p>Creating a sub-scope writes its ordinary parent document with the report block at the canonical body position. Deleting or validating a lifecycle-owned sub-scope verifies the recorded host against the normalised block declaration before mutation. This feature does not change child collection ownership, public eligibility, manifests, customisations, or report contributions.</p>\n<h2>Explicit Cutover</h2>\n<p>The implementation uses one source contract after migration:</p>\n<ul>\n<li>convert every current report-host source document from the five <code>viewer_report*</code> front-matter fields to one report block;</li>\n<li>update lifecycle-created report-host source and every builder or service consumer to use the normalised block descriptor;</li>\n<li>remove the retired front-matter fields, payload construction paths, and direct service readers; and</li>\n<li>reject a document that mixes retired report front matter with a report block.</li>\n</ul>\n<p>There is no permanent dual reader, compatibility alias, precedence rule, or silent fallback between front matter and report blocks. A bounded migration may recognise the old fields only inside an explicit conversion step whose completion removes that recognition.</p>\n<h2>Boundaries</h2>\n<p>This feature does not:</p>\n<ul>\n<li>support multiple reports in one document;</li>\n<li>introduce report parameters, saved filters, initial query state, dashboards, or user-defined report composition;</li>\n<li>merge report blocks into the semantic-token registry, semantic usage indexes, or Broken Links;</li>\n<li>add a generic report engine, arbitrary plugin loader, or report-specific source grammar;</li>\n<li>change report data authority, public promotion, loader allowlists, management contributions, or stylesheet ownership;</li>\n<li>implement Expanded Report View, horizontal scrolling, semantic-table conversion, or manual column widths; or</li>\n<li>make report-host documents transferable to a new collection without an explicit existing or newly approved transfer contract.</li>\n</ul>\n<p>Multiple or parameterised report instances would require a separate feature covering parameter schemas, data-query authority, route-state namespacing, lifecycle ownership, and public validation. This feature deliberately creates no latent instance mechanism for that possibility.</p>\n<h2>Relationship To Expanded Report View</h2>\n<p>This feature is delivered before <a href=\"/docs/?scope=studio&amp;doc=d-20260810-165949-ee4d35\">Expanded Report View</a>. It establishes the one exact live report host and authored restoration position that the expanded presentation will rehost.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260810-181816-dc9891\">Catalogue Works Report Migration</a> follows this feature and uses the new source contract for its local report host. Catalogue Works is then available as the motivating wide tabular report for Expanded Report View; neither later feature broadens Report Blocks into a parameter or multiple-instance mechanism.</p>\n<p>Because a document can host at most one report, the expanded target remains document-owned. It retains the exact scope and <code>doc_id</code> plus the normalised report identity and preset or collection context needed for validation; it does not introduce <code>report_instance_id</code> or instance-prefixed state.</p>\n<h2>Proposed Delivery</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260810-182610-c274f0\">RB-1 — Report Blocks Cutover Delivery</a> owns one complete source, projection, runtime, service, and report-host migration. It begins with a contract and footprint sanity gate, adds the pure shared source parser without changing current product behavior, performs one atomic product/source cutover with no completed dual-reader state, and finishes with cross-scope verification plus durable documentation transfer.</p>\n<p>RB-1 is proposed and does not grant permission to begin its sanity check or implementation. Its checkpoints stop for review before the parser, before the atomic cutover, and before closeout. Catalogue Works Report Migration remains outside RB-1 and begins only after explicit RB-1 acceptance and closeout.</p>\n<h2>Success Meaning</h2>\n<p>The feature succeeds when every report is assigned and positioned by one valid report block, every document has at most one report, the immutable document remains the exact host across public and Manage routes, and all builders and services consume one normalised descriptor. Current reports and sub-scope routes retain their data, access, lifecycle, and interaction behavior; retired report front matter and direct readers are absent. Expanded Report View can then target and rehost that one exact live report without inventing instance identity.</p>\n"
}
