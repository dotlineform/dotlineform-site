{
  "doc_id": "d-20260422-174736-9bd0fc",
  "title": "Local Runners",
  "added_date": "2026-04-22 17:47:36",
  "last_updated": "2026-07-15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260422-174736-9bd0fc",
  "parent_id": "d-20260413-000000-d314b7",
  "content_html": "<h1>Local Runners</h1>\n<h2>Choose A Runner</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Starts</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>bin/local-studio</code></td>\n<td>Local Studio and the docs live-rebuild watcher</td>\n</tr>\n<tr>\n<td><code>bin/local-admin</code></td>\n<td>Local Admin</td>\n</tr>\n<tr>\n<td><code>bin/local-analytics</code></td>\n<td>Local Analytics and Data Sharing routes</td>\n</tr>\n<tr>\n<td><code>docs-viewer/bin/docs-viewer</code></td>\n<td>Docs Viewer manage service and Docs Review when enabled</td>\n</tr>\n<tr>\n<td><code>bin/site-preview</code></td>\n<td>checked-in public <code>site/</code> preview</td>\n</tr>\n<tr>\n<td><code>bin/local-all</code></td>\n<td>all enabled services above under one supervisor</td>\n</tr>\n</tbody>\n</table>\n<p>Run only the owner you are changing. Use <code>bin/local-all</code> when a workflow crosses app boundaries or Studio links need both the public preview and Docs Viewer.</p>\n<h2>Default Boundaries</h2>\n<table>\n<thead>\n<tr>\n<th>Surface</th>\n<th>Default URL</th>\n<th>Owner</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Public preview</td>\n<td><code>http://127.0.0.1:4000/</code></td>\n<td><code>bin/site-preview</code></td>\n</tr>\n<tr>\n<td>Studio</td>\n<td><code>http://127.0.0.1:8765/studio/</code></td>\n<td><code>studio_app_server.py</code></td>\n</tr>\n<tr>\n<td>Analytics</td>\n<td><code>http://127.0.0.1:8766/analytics/</code></td>\n<td>Analytics app</td>\n</tr>\n<tr>\n<td>Admin</td>\n<td><code>http://127.0.0.1:8768/admin/</code></td>\n<td>Admin app</td>\n</tr>\n<tr>\n<td>Docs Viewer</td>\n<td><code>http://127.0.0.1:8776/docs/</code></td>\n<td>Docs Viewer service</td>\n</tr>\n</tbody>\n</table>\n<p>Each app owns its own pages and APIs. <code>bin/local-all</code> supervises siblings; it does not merge their routes into Studio or add compatibility aliases.</p>\n<h2>Studio Runner</h2>\n<p><code>bin/local-studio</code>:</p>\n<ol>\n<li>changes to the repo root and loads <code>.env.local</code> when present;</li>\n<li>chooses <code>$HOME/miniconda3/bin/python3</code> when available;</li>\n<li>verifies the configured Studio port before starting;</li>\n<li>starts <code>studio/app/server/studio/studio_app_server.py</code> when enabled;</li>\n<li>starts <code>docs-viewer/services/docs_live_rebuild_watcher.py</code> when enabled;</li>\n<li>stops both children on interrupt, termination, or runner exit.</li>\n</ol>\n<p>The watcher rebuilds generated Docs Viewer documents and search payloads after source Markdown changes. It is not the Docs Viewer web service. Studio catalogue writes refresh their own derived lookups; the runner does not perform startup catalogue or docs rebuilds.</p>\n<h2>All-Services Runner</h2>\n<p><code>bin/local-all</code> loads the same local environment, then preflights enabled host/port bindings for conflicts and availability before starting any child.</p>\n<p>It starts the public preview, Studio runner, Admin, Analytics, and Docs Viewer in that order. If any child exits, the supervisor identifies it, stops the remaining children, and returns a failing status for an unexpected clean exit or the child's error status.</p>\n<p>This fail-together behavior is useful for an integrated local session; it is not a production orchestration contract.</p>\n<h2>Configuration</h2>\n<p>Local overrides belong in <code>.env.local</code>. The main variable families are:</p>\n<ul>\n<li><code>STUDIO_APP_*</code>, <code>ADMIN_APP_*</code>, and <code>ANALYTICS_APP_*</code> for enablement, host, port, and access logging;</li>\n<li><code>SITE_*</code> for public preview enablement and binding;</li>\n<li><code>DOCS_VIEWER_*</code> for the Docs Viewer binding/base URL and optional review surface;</li>\n<li><code>DOCS_WATCH_*</code> for watcher enablement and timing.</li>\n</ul>\n<p>The runner scripts are the authority for exact names and defaults. Avoid copying the full variable inventory into app documentation; it changes whenever local services are split or renamed.</p>\n<p>Successful requests are quiet by default, while shared local HTTP logging retains useful 4xx/5xx service, method, path, and status context. Enable an app's access-log flag only when request-level detail is needed.</p>\n<h2>Public Preview And Validation</h2>\n<p><code>bin/site-preview</code> serves the checked-in <code>site/</code> tree. <code>bin/site-validate</code> checks that deploy-root contract without copying or generating files. Neither starts Studio services.</p>\n<p>Studio public links use the configured preview base, so the preview must be running when testing those cross-host links.</p>\n<h2>Extension And Weak Spots</h2>\n<ul>\n<li>Add a new long-running app as its own runner first, then make <code>local-all</code> supervise it.</li>\n<li>Keep port preflight and shutdown behavior symmetrical when adding a child.</li>\n<li>Keep app routes with their owner; a runner should not provide old-path aliases.</li>\n<li><code>local-studio</code> starting the cross-scope docs watcher is operationally convenient but couples documentation rebuild activity to Studio sessions.</li>\n<li>Environment settings are spread across sibling runners rather than described by one checked schema; inspect the scripts before changing cross-runner defaults.</li>\n</ul>\n"
}
