{
  "doc_id": "d-20260726-185814-c5e581",
  "title": "Target Lookup",
  "added_date": "2026-07-26 18:58:14",
  "last_updated": "2026-08-09 18:03:47",
  "viewer_url": "/docs/?scope=studio&doc=d-20260726-185814-c5e581",
  "parent_id": "d-20260725-153656-516b61",
  "content_html": "<h1>Target Lookup</h1>\n<pre><code>docs-viewer/data/generated/semantic-tokens/target-lookup.json\n\n</code></pre>\n<h2>Purpose</h2>\n<p>The <strong>Add catalogue token</strong> and <strong>Add Catalogue image</strong> search modals are its main UI consumers, but it has a broader purpose.</p>\n<p><code>target-lookup.json</code> is the shared manage-side projection of targets currently usable by Semantic Tokens:</p>\n<ul>\n<li><strong>Add catalogue token</strong> searches current text-link targets across Works and Series.</li>\n<li><strong>Add Catalogue image</strong> searches only those same rows carrying a valid <code>image</code> projection.</li>\n<li>The Info view reads it for the existing target’s Title, link, and metadata.</li>\n<li>The document builder requires an exact row with a workable <code>href</code> before rendering a text occurrence as a link, and also requires <code>image.src</code> before rendering a visual occurrence.</li>\n<li>Docs Broken Links uses it to distinguish missing targets, missing destinations, and visual targets missing an image projection.</li>\n</ul>\n<p>The Semantic Tokens report does <strong>not</strong> read it directly; it reads the generated usage index.</p>\n<p><strong>the target lookup describes what can be linked; the usage index records what has been linked in documents.</strong></p>\n<h2>Row Contract</h2>\n<p>The current file is <code>docs_semantic_token_target_lookup_v2</code>. Every row retains the existing semantic identity, title, destination, and compact metadata. An optional image projection adds only one browser-safe field:</p>\n<pre><code class=\"language-json\">{\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;work&quot;,\n  &quot;target_id&quot;: &quot;00638&quot;,\n  &quot;title&quot;: &quot;3 symbols&quot;,\n  &quot;href&quot;: &quot;/works/?work=00638&quot;,\n  &quot;meta&quot;: [&quot;2007&quot;, &quot;3 symbols&quot;],\n  &quot;image&quot;: {\n    &quot;src&quot;: &quot;https://media.dotlineform.com/works/img/00638-primary-1600.webp?v=1&quot;\n  }\n}\n</code></pre>\n<p><code>image.src</code> is either root-relative or HTTPS. The projection contains no canonical record, local filesystem path, provider credential, source filename, media version field, dimensions, or unrelated media metadata.</p>\n<p>For a published Work, the builder emits the configured public 1600-pixel primary WebP URL only when the Work has complete current media data.</p>\n<p>For a published Series, the builder copies the image projection from only the exact published Work named by <code>primary_work_id</code>, and only when that Work belongs to the Series. The row remains a Series target with its Series destination. Missing media, an absent or invalid primary Work, or invalid Series membership removes only <code>image</code>; it does not infer another member or remove an otherwise valid text target.</p>\n<p>A visual occurrence whose exact row later lacks <code>image</code> remains literal source, contributes no resolved usage occurrence, and is reported by Docs Broken Links as <code>missing_image</code>. Text occurrences for the same row continue to use its title and destination normally.</p>\n<h2>Build</h2>\n<p><code>target-lookup.json</code> is built by SemanticTargetLookupBuilder, exposed through:</p>\n<pre><code class=\"language-bash\">python docs-viewer/build/build_semantic_target_lookup.py --write\n</code></pre>\n<p>The Catalogue JSON build runs this as its “Build Semantic Target Lookup” step,\nand Catalogue Work/Series save-and-build workflows refresh it automatically.\nDocs scope builds only read the lookup.</p>\n<p>The input flow is:</p>\n<pre><code class=\"language-text\">Semantic-token registry\n    + canonical Works\n    + canonical Series\n    -&gt; SemanticTargetLookupBuilder\n    -&gt; target-lookup.json\n</code></pre>\n<p>Specifically:</p>\n<ul>\n<li><strong>registry.json</strong> (<code>docs-viewer/config/semantic-tokens/registry.json</code>) declares the <code>catalogue</code> family, target types, ID normalization, and lookup adapters. It controls the shape, but does not contain target records.</li>\n<li>Works come from <code>studio/data/canonical/catalogue/works.json</code>.</li>\n<li>Series come from <code>studio/data/canonical/catalogue/series.json</code>.</li>\n</ul>\n<p>The builder includes only <code>published</code> records with a valid normalized ID, Title, and generated destination. Work metadata includes its date and first Series title.</p>\n<p>Destinations are generated by the builder:</p>\n<ul>\n<li>Work → <code>/works/?work=&lt;id&gt;</code></li>\n<li>Series → <code>/series/?series=&lt;id&gt;</code></li>\n</ul>\n"
}
