{
  "doc_id": "d-20260726-185814-c5e581",
  "title": "Target Lookup",
  "added_date": "2026-07-26 18:58:14",
  "last_updated": "2026-08-09 12:06:07",
  "viewer_url": "/docs/?scope=studio&doc=d-20260726-185814-c5e581",
  "parent_id": "d-20260725-153656-516b61",
  "content_html": "<h1>Target Lookup</h1>\n<pre><code>docs-viewer/data/generated/semantic-tokens/target-lookup.json\n\n</code></pre>\n<h2>Purpose</h2>\n<p>The <strong>Add catalogue token</strong> search modal is its main UI consumer, but it has a broader purpose.</p>\n<p><code>target-lookup.json</code> is the shared manage-side projection of targets currently usable by Semantic Tokens:</p>\n<ul>\n<li>The Add modal searches it across Works and Series.</li>\n<li>The Info view reads it for the existing target’s Title, link, and metadata.</li>\n<li>The document builder requires an exact row with a workable <code>href</code> before rendering a token as a link and adding it to the usage index.</li>\n<li>Docs Broken Links uses it to distinguish missing targets from targets lacking a destination.</li>\n</ul>\n<p>The Semantic Tokens report does <strong>not</strong> read it directly; it reads the generated usage index.</p>\n<p><strong>the target lookup describes what can be linked; the usage index records what has been linked in documents.</strong></p>\n<h2>Build</h2>\n<p><code>target-lookup.json</code> is built by SemanticTargetLookupBuilder, exposed through:</p>\n<pre><code class=\"language-bash\">python docs-viewer/build/build_semantic_target_lookup.py --write\n</code></pre>\n<p>The Catalogue JSON build runs this as its “Build Semantic Target Lookup” step,\nand Catalogue Work/Series save-and-build workflows refresh it automatically.\nDocs scope builds only read the lookup.</p>\n<p>The input flow is:</p>\n<pre><code class=\"language-text\">Semantic-token registry\n    + canonical Works\n    + canonical Series\n    -&gt; SemanticTargetLookupBuilder\n    -&gt; target-lookup.json\n</code></pre>\n<p>Specifically:</p>\n<ul>\n<li><strong>registry.json</strong> (<code>docs-viewer/config/semantic-tokens/registry.json</code>) declares the <code>catalogue</code> family, target types, ID normalization, and lookup adapters. It controls the shape, but does not contain target records.</li>\n<li>Works come from <code>studio/data/canonical/catalogue/works.json</code>.</li>\n<li>Series come from <code>studio/data/canonical/catalogue/series.json</code>.</li>\n</ul>\n<p>The builder includes only <code>published</code> records with a valid normalized ID, Title, and generated destination. Work metadata includes its date and first Series title.</p>\n<p>Destinations are generated by the builder:</p>\n<ul>\n<li>Work → <code>/works/?work=&lt;id&gt;</code></li>\n<li>Series → <code>/series/?series=&lt;id&gt;</code></li>\n</ul>\n"
}
