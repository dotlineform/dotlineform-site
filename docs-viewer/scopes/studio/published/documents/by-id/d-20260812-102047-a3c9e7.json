{
  "doc_id": "d-20260812-102047-a3c9e7",
  "title": "Media Sources",
  "added_date": "2026-08-12 10:20:47",
  "last_updated": "2026-08-12 13:25:22",
  "viewer_url": "/docs/?scope=studio&doc=d-20260812-102047-a3c9e7",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "let Analysis, Dotlineform, and Processing add media from durable source folders while retaining simple source evidence",
  "ui_status": "in-progress",
  "content_html": "<h1>Media Sources</h1>\n<h2>Purpose</h2>\n<p>Help authors retain an identifiable original for manually added Docs Viewer media before a copy or derived artifact is published. A published repository, external-local, or R2 object is a delivery artifact, not an automatic backup of its source.</p>\n<p>The current <strong>Add image</strong> and <strong>Add file</strong> workflows select direct-child files from the shared import-staging drop-zone. They publish or derive the configured media artifact but do not establish a durable relationship back to an organised original. Authors can preserve originals separately, but Docs Viewer neither requires that step nor helps verify it later.</p>\n<h2>Proposed Model</h2>\n<p>Each participating scope may configure one external-local <strong>media source root</strong>. More than one scope may deliberately share the same root. The source root is distinct from the scope's one published-media location defined by <a href=\"/docs/?scope=studio&amp;doc=d-20260812-095531-6fd2b1\">Media Locations</a>.</p>\n<table>\n<thead>\n<tr>\n<th>role</th>\n<th>responsibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Media source</td>\n<td>Retained original selected through a scope's configured source root. It remains unchanged after intake and is the material included in the author's real backup or sync policy.</td>\n</tr>\n<tr>\n<td>Published media</td>\n<td>Consumer-ready copy or derived artifact written to the scope's configured repository, external-local, or R2 published-media root.</td>\n</tr>\n<tr>\n<td>Import staging</td>\n<td>Shared transient drop-zone for document and package imports. A scope with a configured media source does not require authors to copy manually added media through it.</td>\n</tr>\n<tr>\n<td>Backup</td>\n<td>An external policy covering the media source root. Docs Viewer can expose source evidence but does not claim that local, repository, or R2 storage is itself a backup.</td>\n</tr>\n</tbody>\n</table>\n<p>The source-editor workflow becomes:</p>\n<pre><code class=\"language-text\">save original in the configured media source root\n    -&gt; choose Add image or Add file\n    -&gt; select an eligible source from that root\n    -&gt; preview the published identity and any transformation\n    -&gt; retain the original and publish or verify the output\n    -&gt; insert the logical media reference\n</code></pre>\n<p>Ordinary authoring does not choose a published storage provider. The configured source root answers where the original is retained; Media Locations independently answers where consumer-ready media is published.</p>\n<h2>Initial Scope Adoption</h2>\n<p>Analysis, Dotlineform, and Processing adopt the source workflow up front:</p>\n<table>\n<thead>\n<tr>\n<th>scope</th>\n<th>configured media source root</th>\n<th>current published-media outcome</th>\n<th>later location question</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Analysis</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/analysis/</code></td>\n<td>Continue the current configured repository locations for <code>img</code>, <code>svg</code>, and <code>html</code> and R2 location for <code>files</code>; Media Sources does not move providers.</td>\n<td>Media Locations may later move the complete published root to one provider.</td>\n</tr>\n<tr>\n<td>Dotlineform</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/analysis/</code></td>\n<td>Continue to publish media inside the external-local Dotlineform scope.</td>\n<td>Copy may later carry the document, referenced media, and any recorded source evidence into Analysis.</td>\n</tr>\n<tr>\n<td>Processing</td>\n<td><code>$DOTLINEFORM_PROJECTS_BASE_DIR/processing/</code></td>\n<td>Continue to publish against the current local Processing scope contract.</td>\n<td>Large generated images make R2 plausible, but whether they ultimately belong to Analysis or a separate public Processing scope is deliberately undecided.</td>\n</tr>\n</tbody>\n</table>\n<p>These roots sit alongside the Studio Catalogue Works source root at <code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code>. <strong>Add image</strong> and <strong>Add file</strong> use the active scope's configured media source root rather than require a duplicate in <code>data-sharing/import-staging</code>.</p>\n<p>Sharing <code>analysis/</code> is intentional. An author may add an original to a working document in <code>dotlineform/projects</code> before editorial Copy, or add the same original directly to an Analysis document. Dotlineform is an optional working gate for Analysis, not a second originals store. The media-source folder name is explicit configuration and is not derived from <code>scope_id</code>, so this model creates no <code>$DOTLINEFORM_PROJECTS_BASE_DIR/dotlineform/</code> requirement and gives no folder-layout reason to rename the Dotlineform scope.</p>\n<p>Authors may save media directly in the scope root or organise originals in any confined subfolder beneath it. Source subfolders do not become published subfolders: repository or R2 media keeps the existing flat per-type filename identity. This follows the Studio Catalogue Works separation between source organisation and delivery identity, keeps private organisation out of public URLs, and allows source files to be reorganised without rewriting document references. Duplicate basenames from different source folders remain one published-identity collision and must be shown by the existing preview/confirmation boundary.</p>\n<p>Processing's source workflow does not decide its future public scope, route, or provider. The exact Processing source evidence remains useful whichever published-media ownership is deliberately adopted later. Its external media source root is for Docs Viewer media originals; it is not the Processing Java project, a generated-output directory, or a reason to apply website lifecycle assumptions to that project.</p>\n<p>All three adoptions preserve current media handling:</p>\n<ul>\n<li>raster images and opaque files publish byte-identical copies;</li>\n<li>standalone SVG retains its original while publishing the sanitized SVG;</li>\n<li>Mermaid retains its existing explicit source-to-rendered-SVG contract;</li>\n<li>source files are not consumed, moved, renamed, rewritten, or automatically deleted; and</li>\n<li>changing or removing a source does not silently replace or delete the published artifact.</li>\n</ul>\n<h2>Source Evidence</h2>\n<p>Publishing should save one small provider-neutral row for the published media identity. The first contract needs only the configured source-root identity and exact Projects-relative source path, such as <code>analysis/reference/portrait.jpg</code> or <code>processing/studies/study-01.png</code>. Absolute local paths do not belong in browser payloads or portable records.</p>\n<p>When a Dotlineform document is copied into Analysis, Copy carries an exact source-evidence row for each transferred media identity when one exists. It does not search for, infer, or fabricate an original. Existing media and media introduced through workflows that have not recorded an original truthfully remain <strong>no source recorded</strong> until a later explicit association capability is designed.</p>\n<p>This deliberately small evidence allows <a href=\"/docs/?scope=studio&amp;doc=d-20260720-100017-9855dd\">Scope Media Management</a> to distinguish:</p>\n<ul>\n<li>source recorded and currently present;</li>\n<li>no source recorded;</li>\n<li>recorded source missing or its source root unavailable; and</li>\n<li>published artifact present or missing.</li>\n</ul>\n<p>The first outcome does not hash originals, compare source and published bytes, track source changes, monitor folders, or continuously verify provenance. Those capabilities should be added only if the report demonstrates a practical need. These are inspection states, not automatic mutation authority.</p>\n<h2>Folder Selection Direction</h2>\n<p>The two source-editor media modals replace their staged-file dropdowns with accessible, single-selection file lists. Each modal displays its current source folder and a <strong>Choose folder…</strong> button. Image and file actions filter the same folder through their existing supported-format rules.</p>\n<p><strong>Choose folder…</strong> hands off to the existing app-neutral folder-picker in a dedicated modal. Navigation is rooted at the active scope's configured <code>analysis/</code> or <code>processing/</code> directory: authors may choose that root or navigate through descendants, but neither the browser nor the service can move above it. Dotlineform and Analysis therefore open the same <code>analysis/</code> tree. Choosing a folder returns to the media modal, displays the selected Projects-relative folder, refreshes the eligible direct-child file list, clears any selection and derived label from the former folder, and focuses the refreshed list. Cancelling returns without changing the current folder or selection.</p>\n<p>An empty initial or selected folder still opens the media modal and keeps <strong>Choose folder…</strong> available. Preview and apply submit the scope, selected Projects-relative directory, and filename; the server recomputes and confines the exact source beneath the configured root. A scope that has not adopted Media Sources retains its current import-staging workflow until it receives an explicit source-root configuration.</p>\n<p>Folder selection reuses the existing Projects-relative confinement and app-neutral picker responsibilities rather than accepting arbitrary absolute paths or browser-submitted filesystem authority. The shared picker needs a lower-root option and the Docs Viewer service must enforce the same root; an initial breadcrumb or browser restriction alone is insufficient.</p>\n<p>Remaining UI details include whether the current subfolder is remembered per source root between modal sessions, which minimal file metadata appears in list rows, and whether the media modal also needs an explicit refresh control for files added while it is open. These details should be settled by trying the workflow rather than freezing a larger browser contract in advance.</p>\n<h2>Delivery Order And Stance</h2>\n<p>This feature comes first in the current media-workflow delivery sequence. The read-only Scope Media report follows it so the report can use real source evidence; a later <a href=\"/docs/?scope=studio&amp;doc=d-20260812-114603-9706aa\">Media Publication Check</a> may then reuse the report's simple byte aggregation.</p>\n<p>The specification is provisional and should change when a simpler UI makes the authoring workflow clearer. The delivery should reuse the existing Add image/Add file preview and publication owners, add only the source-folder seam and small evidence record, and avoid a second media intake framework.</p>\n<p>Verification should stay at the lowest useful layer: focused source-path confinement and evidence-record service cases, plus manual review of the folder/list interaction. Do not add a broad smoke profile or permanent browser choreography tests merely to exercise modal steps, focus, copy, or layout.</p>\n<h2>Delivery Steps</h2>\n<h3>MS-1 — Durable Media Source Selection</h3>\n<p>MS-1 delivers the complete first outcome: Analysis and Dotlineform add media from their shared <code>analysis/</code> source root, Processing adds media from <code>processing/</code>, successful Add image/Add file publication records the small source association, and Dotlineform-to-Analysis Copy carries recorded evidence with transferred media. Other scopes retain import staging, and ordinary document/package Import remains unchanged.</p>\n<p>The steps below are provisional checkpoints, not a frozen implementation design. Each checkpoint stops for review. If trying the UI reveals a simpler workflow, revise the later steps rather than preserving an awkward specification.</p>\n<h4>MS-1.0 — Confirm UI And Implementation Fit</h4>\n<ul>\n<li>[x] Inspect the current Add image/Add file modal, staged-media service, folder picker, scope configuration, media publication, and document Copy seams at broad owner level.</li>\n<li>[x] Try the smallest useful modal shape: current source folder, <strong>Choose folder…</strong>, one single-selection file list, and the existing publication/presentation controls.</li>\n<li>[x] Confirm how the picker can be confined beneath a configured lower root without exposing arbitrary filesystem paths to the browser.</li>\n<li>[x] Confirm one small source-evidence owner and the minimum root-identity plus relative-path record needed by Add, Copy, and the later report.</li>\n<li>[x] Record the proposed changed owners, reused seams, approximate production/test footprint, focused checks, and any decision that remains genuinely UI-dependent.</li>\n<li>[x] Stop if the result requires a second media intake service, a generic filesystem browser, a new modal framework, source verification, a migration/backfill system, or broad browser-smoke expansion.</li>\n</ul>\n<p><strong>Checkpoint:</strong> review one concise MS-1.0 sanity record and the tried UI direction. Do not begin MS-1.1 without explicit approval.</p>\n<p><strong>MS-1.0 sanity record — 12 August 2026:</strong> The current compact Add image and Add file modal, provider-neutral staged-media preview/apply service, app-neutral folder picker, Projects-relative directory confinement, strict scope configuration, media transfer plan/apply, and current configured media providers fit the delivery. The accepted modal direction is one source-folder row with <strong>Choose folder…</strong>, a native radio-row file list showing filename and byte size, and the unchanged label, caption, placement, collision-review, and insertion controls below it. The initial folder is the configured root; choosing another folder clears selection and derived label, an empty folder keeps the modal usable, and the first implementation adds neither persisted folder memory nor a separate refresh control. Row density and whether byte size remains the sole secondary value stay open to manual review in MS-1.1.</p>\n<p>The configured source root is one canonical Projects-relative directory marker on <code>DocsScopeConfig</code>: <code>analysis</code> for Analysis and Dotlineform and <code>processing</code> for Processing. The shared Projects-directory resolver and folder picker gain a lower-root argument; both reject navigation above it, while the browser receives only Projects-relative markers. The existing staged-media list/preview/apply owner resolves either that configured directory or the unchanged import-staging fallback for a non-adopter, preserves all current format and publication behavior, and records evidence only after successful publish or verification. Ordinary document/package Import remains on its existing unrestricted Projects picker and import-staging contracts.</p>\n<p>One new focused evidence owner stores a private canonical <code>docs_media_source_evidence_v1</code> table at each scope source root. Its exact scope-owned row is <code>{media_type, identity, source_root, source_path}</code>: <code>source_root</code> and <code>source_path</code> are canonical Projects-relative markers, and no absolute path, timestamp, hash, document identity, or public projection is added. Copy reads exact source and target rows through the existing media plan. If the same target media bytes already exist, an existing target row wins; otherwise an existing source row fills an unrecorded target. Missing source evidence stays unrecorded. This is deterministic and adds no prompt, merge history, inference, or backfill.</p>\n<p>The proposed production owners are <code>docs_scopes.json</code> and <code>docs_scope_config.py</code>; <code>projects_directories.py</code> and the shared <code>folder-picker.js</code>; the existing staged-media service, management read/client/source-adapter, source-editor media module, and its CSS; one new focused evidence module; and the existing document-transfer plan/apply modules. MS-1.1 is approximately 350–450 production lines and 250–350 focused test lines; MS-1.2 is approximately 100–150 production lines and 120–180 focused test lines. Focused checks are the existing scope-config, Projects-directory, staged-media, and document-transfer Python files, the smallest existing source-editor module check needed for request wiring, changed Python/JavaScript lint, and <code>git diff --check</code>; no broad profile or new browser choreography test is proposed.</p>\n<p>The fit does not require a second intake service, generic filesystem browser, new modal framework, source scanning or verification, migration/backfill, provider movement, or Processing-project lifecycle change. The current Analysis <code>files</code> provider is R2, contrary to the earlier table wording; the adoption table now records that current fact and leaves any provider unification to Media Locations.</p>\n<h4>MS-1.1 — Add From Configured Source Folders</h4>\n<ul>\n<li>[ ] Configure Analysis and Dotlineform with the shared <code>$DOTLINEFORM_PROJECTS_BASE_DIR/analysis/</code> media source root and Processing with <code>$DOTLINEFORM_PROJECTS_BASE_DIR/processing/</code>.</li>\n<li>[ ] Extend the existing folder-picker/read seam only as needed to choose a confined root or descendant and return eligible direct-child files.</li>\n<li>[ ] Replace the Add image/Add file dropdowns with the accepted file-list UI, preserving current type filtering, labels, captions, collision preview, publication, and source-buffer insertion behavior.</li>\n<li>[ ] Keep import staging unchanged for scopes without a configured media source and for ordinary document/package Import.</li>\n<li>[ ] After successful media publication, record the exact source-root identity and root-relative source path for the published media identity. Do not infer, hash, move, rename, or rewrite the original.</li>\n<li>[ ] Cover source-root resolution, confinement, eligible listing, and evidence persistence with small focused service cases. Review the folder/list interaction manually rather than encoding its choreography in browser tests.</li>\n</ul>\n<p><strong>Checkpoint:</strong> Add image and Add file work from the configured roots for the three adopters, other scopes retain their current workflow, and focused checks pass. Stop for UI and diff review before MS-1.2.</p>\n<h4>MS-1.2 — Carry Evidence Through Editorial Copy</h4>\n<ul>\n<li>[ ] Extend the existing exact Dotlineform-to-Analysis Copy plan so each transferred media identity carries its source-evidence row when one exists.</li>\n<li>[ ] Preserve <strong>no source recorded</strong> when evidence does not exist; do not search the source tree or infer a match from the published filename.</li>\n<li>[ ] Reuse the existing media planning, stale-preview, New/Replace, and target-rebuild boundaries without adding a second transfer path or publication action.</li>\n<li>[ ] Use one deterministic, no-prompt rule for an already-recorded target identity, chosen at MS-1.0 after inspecting the current collision/reuse behavior. Do not add evidence history, merge UI, rollback, or retained recovery copies.</li>\n<li>[ ] Add only focused transfer-service cases for evidence present, evidence absent, and the accepted existing-target rule. Do not add a browser smoke for Copy confirmation choreography.</li>\n</ul>\n<p><strong>Checkpoint:</strong> Copy retains exact recorded source evidence alongside transferred media without changing document lineage, <code>publishable</code>, or public Publish ownership. Stop for focused review before closeout.</p>\n<h4>MS-1.3 — Verify And Transfer Durable Ownership</h4>\n<ul>\n<li>[ ] Manually review Add image/Add file from the Analysis/Dotlineform shared root and the Processing root, including root, subfolder, empty-folder, cancel, and refresh behavior that the accepted UI exposes.</li>\n<li>[ ] Run only the focused service/module checks selected by the implemented seams, changed-source lint, and <code>git diff --check</code>; reuse an existing public/private boundary check if one is genuinely affected.</li>\n<li>[ ] Do not run a broad profile or add a permanent browser smoke merely for extra evidence.</li>\n<li>[ ] Transfer shipped behavior and remaining weak spots to the existing Media And Asset Handling, Source Organisation, and Copy authorities without retaining speculative implementation detail.</li>\n<li>[ ] Present the feature/delivery closeout list and stop for explicit retirement or retention decisions. Do not begin Scope Media Report implementation from this closeout.</li>\n</ul>\n<p><strong>Completion gate:</strong> the three configured scopes use durable source folders through a simple Add UI, successful publication records small source evidence, editorial Copy carries it when present, historical media remains truthfully unrecorded, and later report/publication-check work remains unstarted.</p>\n<h2>Boundaries</h2>\n<ul>\n<li>This feature does not implement backup, versioning, synchronization, R2 export, or disaster recovery.</li>\n<li>It does not change the published-media defaults or provider migration owned by Media Locations.</li>\n<li>It does not make import staging a source archive or search for originals across multiple folders.</li>\n<li>It does not change ordinary document/package Import in the first delivery; the durable source-folder choice applies to <strong>Add image</strong> and <strong>Add file</strong>.</li>\n<li>It does not automatically republish when a source changes.</li>\n<li>It does not delete source originals when media, documents, or scopes are deleted.</li>\n<li>It does not require every imported or generated document asset to have recorded source evidence; the first outcome concerns manually selected <strong>Add image</strong> and <strong>Add file</strong> media.</li>\n<li>It adds no checksum catalogue, background verification, automatic source repair, rollback, retry framework, or retained rollback copy.</li>\n<li>Scopes other than Analysis, Dotlineform, and Processing do not adopt a media source root in the first outcome. Their occasional <strong>Add image</strong> and <strong>Add file</strong> use continues through import staging.</li>\n</ul>\n<p>MS-1 is in progress. MS-1.0 is complete and parked at its review gate; MS-1.1 has not started and requires explicit approval.</p>\n"
}
