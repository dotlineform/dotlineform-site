{
  "doc_id": "d-20260802-002213-b7d4e2",
  "title": "TST-1 — Tag Semantic Token Delivery",
  "added_date": "2026-08-02 00:22:13",
  "last_updated": "2026-08-02 11:09:26",
  "viewer_url": "/docs/?scope=studio&doc=d-20260802-002213-b7d4e2",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "add a separate Tag semantic family that links selected text through the Tag Registry's doc_url[0] association",
  "content_html": "<h1>TST-1 — Tag Semantic Token Delivery</h1>\n<h2>Status And Purpose</h2>\n<p>Proposed as <strong>TST-1</strong>, a standalone delivery associated with <a href=\"/docs/?scope=studio&amp;doc=d-20260725-153656-516b61\">Semantic Tokens</a>. It is not active and does not authorize implementation.</p>\n<p>The delivery adds Tag as a second Semantic Tokens family after the Tag Registry acquired canonical document associations. It keeps Tag separate from Catalogue: a Work, Series, or Moment resolves to one concrete catalogue destination, while a Tag resolves to its first associated document.</p>\n<h2>Complete Outcome</h2>\n<p>In Manage Source, an author selects meaningful text, chooses <strong>Add tag token</strong>, searches canonical Tags and aliases, and selects one exact canonical <code>tag_id</code>. The built document converts the authored text to the Tag's current associated document link:</p>\n<pre><code class=\"language-text\">[Nerve]\n</code></pre>\n<p>The source records stable Tag identity rather than copying document URLs. A normal rebuild resolves the current <code>doc_url[0]</code> projection, and records one Tag-token occurrence in the scope usage index. The public document contains ordinary resolved links and no Tag Registry, target lookup, or client-side semantic resolver.</p>\n<h2>Fixed Product Direction</h2>\n<h3>Separate Family And Action</h3>\n<ul>\n<li>Tag is a separate Semantic Tokens family with its own <strong>Add tag token</strong> action, discovery modal, parser contribution, and Info view.</li>\n<li>Tag is not added to <strong>Add catalogue token</strong> search. Catalogue continues to combine only Work, Series, and Moment.</li>\n<li>The Tag family reuses the generic source selection, guarded range mutation, modal lifecycle, token registry, target lookup, build projection, usage index, report, and Broken Links seams.</li>\n<li>The canonical identity is <code>tag_id</code>. Aliases are discovery inputs only and always resolve to a selected canonical Tag.</li>\n<li>Group and current association titles are distinguishing lookup context; they are not copied into source identity.</li>\n</ul>\n<h3>Associations Are The Target</h3>\n<ul>\n<li>A Tag target is its <code>doc_url[0]</code> association. There is no separate Tag landing page.</li>\n<li>The Tag Registry remains authoritative for canonical Tag identity, group, aliases, and association order. Semantic Tokens consume a focused generated target projection and do not inspect or mutate canonical Tag JSON directly.</li>\n<li>Document-location projection supplies current public-safe titles and URLs. A token never infers an association from document title, content, current route, report host, or neighbouring source.</li>\n</ul>\n<h2>Wrapped Selection</h2>\n<p>The action replaces the captured selection with a token containing the occurrence text:</p>\n<pre><code class=\"language-markdown\">[[tag:tag:nerve|nerve]]\n</code></pre>\n<p>The builder renders:</p>\n<pre><code class=\"language-text\">[Nerve]\n</code></pre>\n<p>The token owns both the visible occurrence text and Tag identity; its Info view can edit the occurrence text while keeping identity readonly.</p>\n<h2>Link Resolution</h2>\n<ul>\n<li>A recognized Tag token resolves only the canonical Tag row's exact <code>doc_url[0]</code> association.</li>\n<li>The occurrence Title stored in the token is the rendered anchor label. The associated document title may appear as discovery or Info context but does not replace the authored Title.</li>\n<li><code>doc_url[1:]</code> remains canonical Tag Registry association data for later analysis and other consumers. TST-1 does not enumerate, copy, validate, or publish those later associations.</li>\n<li>There is no fallback from an empty, stale, unpublished, or otherwise unusable <code>doc_url[0]</code> to a later array entry. The token follows the same tolerant Semantic Tokens rule as Catalogue: unresolved source remains ordinary text, creates no resolved usage row, and can be diagnosed by Docs Broken Links.</li>\n<li>Public output is resolved at build time into one ordinary anchor. Public readers receive no Tag Registry, target lookup, usage index, or resolution runtime.</li>\n</ul>\n<h2>Tag Registry Ordering</h2>\n<p>Association order becomes user-visible because <code>doc_url[0]</code> is the Semantic Token link target. TST-1 therefore includes an explicit way to change <code>doc_url[]</code> order in <strong>Edit Tag</strong>.</p>\n<ul>\n<li>Reordering edits the complete local association draft and is committed by the existing atomic Tag mutation boundary.</li>\n<li>The UI must expose deliberate accessible ordering controls. TST-1.0 decides between move-up/move-down controls and another equally explicit interaction; drag-only ordering is insufficient.</li>\n<li>Removing, adding, and reordering remain independent draft operations until Save. Close discards all of them.</li>\n<li>No automatic title sort, availability sort, recently-used ranking, or semantic-token usage may rewrite canonical order.</li>\n<li>Moving a URL into position zero deliberately changes the Tag Semantic Token destination. It does not transfer document lifecycle ownership to Tag Registry or synchronize a document with Tag identity.</li>\n</ul>\n<h2>Target And Usage Projection</h2>\n<p>The current Semantic Tokens target row already assumes one workable <code>href</code>. Tag can reuse that shape without a multi-target schema: the focused Tag projection derives one <code>href</code> from the exact <code>doc_url[0]</code> association.</p>\n<p>A proposed Tag target row contains:</p>\n<pre><code class=\"language-json\">{\n  &quot;family&quot;: &quot;tag&quot;,\n  &quot;target_type&quot;: &quot;tag&quot;,\n  &quot;target_id&quot;: &quot;nerve&quot;,\n  &quot;title&quot;: &quot;nerve&quot;,\n  &quot;href&quot;: &quot;/analysis/?doc=...&amp;subdoc=...&quot;,\n  &quot;meta&quot;: [&quot;subject&quot;, &quot;Nerve&quot;]\n}\n</code></pre>\n<p>The exact field names and unavailable-record representation remain a TST-1.0 freeze decision. Required semantics are:</p>\n<ul>\n<li>one lookup row per canonical Tag with a currently workable <code>doc_url[0]</code>;</li>\n<li>one safe <code>href</code> resolved from that exact stored URL;</li>\n<li>canonical aliases available to discovery without becoming target identity;</li>\n<li>the canonical group and associated document title available only as compact lookup context where useful;</li>\n<li>one ordinary resolved usage occurrence per authored Tag token; and</li>\n<li>exact source document and source range for report navigation and targeted build preservation.</li>\n</ul>\n<p>The Semantic Tokens report treats Tag like another resolved family occurrence and links back to its source document. Docs Broken Links remains the read-only diagnosis owner for unknown Tags and unusable <code>doc_url[0]</code> targets.</p>\n<h2>Delivery Discipline</h2>\n<p>TST-1 is one complete second-family vertical slice over the existing single-target Semantic Tokens contract. It should add a focused Tag lookup adapter and family-owned UI without introducing multi-target schema, rendering, or report behavior. It must not turn the registry into executable plugins, combine Tag and Catalogue search, add a generic graph, create a public Tag application, or move Tag Registry authority into Docs Viewer.</p>\n<p>TST-1.0 must record the exact proposed files, reused seams, and rough production/test diff per later checkpoint. Any proposed expansion of the existing single-<code>href</code> contract is a pause-and-review signal because this delivery does not require it.</p>\n<h2>Likely Authority To Inspect</h2>\n<p>This is a routing map for TST-1.0, not a frozen file-change list.</p>\n<table>\n<thead>\n<tr>\n<th>boundary</th>\n<th>likely current owners</th>\n<th>delivery pressure</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>family definition and browser registry</td>\n<td><code>docs-viewer/config/semantic-tokens/registry.json</code> and <code>source-editor/semantic-token-registry.js</code></td>\n<td>register Tag without adding it to Catalogue discovery</td>\n</tr>\n<tr>\n<td>canonical Tag identity and associations</td>\n<td><code>studio/data/canonical/tags/</code> and <code>studio/services/tags/</code></td>\n<td>project canonical IDs, aliases, group, and exact <code>doc_url[0]</code> without transferring authority</td>\n</tr>\n<tr>\n<td>document association editing</td>\n<td><code>studio/app/frontend/js/tag-registry-*</code></td>\n<td>add accessible draft reordering while preserving complete-array atomic Save</td>\n</tr>\n<tr>\n<td>target lookup</td>\n<td><code>docs_builder/semantic_target_lookup.py</code> and Semantic Token target adapters</td>\n<td>derive the existing single safe <code>href</code> from exact <code>doc_url[0]</code> with no later-entry fallback</td>\n</tr>\n<tr>\n<td>source authoring and Info</td>\n<td>generic Source contribution/range seams plus new Tag-owned modules beside <code>catalogue-token-*</code></td>\n<td>wrap the captured selection and keep Tag identity readonly</td>\n</tr>\n<tr>\n<td>build and usage</td>\n<td><code>docs_builder/semantic_tokens.py</code> and <code>semantic_token_artifacts.py</code></td>\n<td>reuse ordinary single-link rendering and one resolved occurrence</td>\n</tr>\n<tr>\n<td>reports and diagnosis</td>\n<td>Semantic Tokens report and Docs Broken Links semantic checks</td>\n<td>show the Tag occurrence and diagnose unknown or unavailable first targets</td>\n</tr>\n<tr>\n<td>assurance</td>\n<td>shared grammar fixtures, focused Python contracts, and browser module smoke</td>\n<td>prove the Tag family while protecting Catalogue behavior and rejecting multi-association projection</td>\n</tr>\n</tbody>\n</table>\n<h2>Delivery Steps</h2>\n<h3>TST-1.0 — Trace And Freeze The Tag Family</h3>\n<ul>\n<li>[ ] Trace current Tag Registry ordering, aliases, document-location resolution, Semantic Token grammar, target lookup, Source contributions, builder projection, usage preservation, report, Broken Links, and public isolation.</li>\n<li>[ ] Freeze wrapped-selection syntax, occurrence Title handling, canonical Tag identity, alias discovery, group/document context, and Info update/removal behavior.</li>\n<li>[ ] Freeze exact <code>doc_url[0]</code> projection, empty and unavailable first-target behavior, the absence of fallback to later associations, and the ordinary resolved usage row.</li>\n<li>[ ] Freeze Edit Tag reordering interaction and complete-draft Save behaviour.</li>\n<li>[ ] Record the exact files, reused seams, rough production/test diff, and any proposed expansion before implementation approval.</li>\n</ul>\n<p><strong>Checkpoint:</strong> reviewed fixtures cover collapsed and non-empty selections, punctuation, canonical and alias discovery, one and several stored associations, reordered position zero, no association, stale or unpublished position zero with a workable later URL, duplicate visible titles, targeted rebuild, usage rows, and public output. They prove that only <code>doc_url[0]</code> participates and that later associations remain untouched Registry data. Stop before implementation.</p>\n<h3>TST-1.1 — Tag Lookup And Single-Link Projection</h3>\n<ul>\n<li>[ ] Add the focused Tag target generator over canonical Registry, aliases, and the public-safe document-location projection for exact <code>doc_url[0]</code>.</li>\n<li>[ ] Register the Tag family in the existing single-<code>href</code> target schema and shared consumers.</li>\n<li>[ ] Implement tolerant single-anchor build rendering for wrapped Tag tokens and the frozen unavailable-first-target states.</li>\n<li>[ ] Generate one ordinary resolved usage occurrence while preserving untouched documents during targeted builds.</li>\n<li>[ ] Keep Tag canonical data, aliases, target lookup, and usage data out of public output.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused lookup, parser, builder, usage, targeted-build, and public-isolation evidence passes for the frozen fixtures. Stop for approval before TST-1.2.</p>\n<h3>TST-1.2 — Authoring, Info, And Target Ordering</h3>\n<ul>\n<li>[ ] Add the separate <strong>Add tag token</strong> Source action and Tag discovery modal with canonical ID, alias, group, and first-associated-document context.</li>\n<li>[ ] Insert the wrapped token through captured, revision-guarded Source mutation only.</li>\n<li>[ ] Add the family-specific Info view with immutable identity, current target context, permitted occurrence editing, and exact-range removal.</li>\n<li>[ ] Add accessible association reordering to the Edit Tag draft without changing its atomic complete-array mutation boundary.</li>\n<li>[ ] Preserve validation, unavailable-target, and stale-range errors without adding redundant successful-mutation status messages.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused module and route evidence passes, and manual acceptance adds a Tag token from selected prose, rebuilds one ordinary link, moves another associated document into position zero, saves, rebuilds, and observes the link target change. Stop for approval before TST-1.3.</p>\n<h3>TST-1.3 — Reports, Regression, And Transfer</h3>\n<ul>\n<li>[ ] Extend the Semantic Tokens report and Docs Broken Links diagnosis for the frozen Tag occurrence and association states.</li>\n<li>[ ] Prove Catalogue Work, Series, and Moment insertion, Info, rendering, usage, and unresolved behaviour are unchanged.</li>\n<li>[ ] Verify canonical and alias discovery, zero/one/many stored associations, reordered position zero, unavailable first targets, later-entry non-participation, source edits, targeted rebuild preservation, public links, and public isolation.</li>\n<li>[ ] Run focused Python, browser-module, lint, whitespace, generated docs, and search checks, then perform the mandatory final diff and omission review.</li>\n<li>[ ] Transfer shipped behaviour to the concise Semantic Tokens, Tag Registry, Source Editor UI, Architecture, Target Lookup, and Usage Index owners.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>combining Tag targets with Catalogue discovery or the Catalogue toolbar action;</li>\n<li>rendering, projecting, or reporting <code>doc_url[1:]</code> from a Tag Semantic Token;</li>\n<li>falling back to a later association when <code>doc_url[0]</code> is unavailable;</li>\n<li>a multi-target Semantic Token schema, link group, display limit, or overflow state;</li>\n<li>automatic Tag-document creation, repair, deletion, or synchronization;</li>\n<li>deriving associations from document titles, content, source proximity, or usage rows;</li>\n<li>recursive relationship resolution or Resolver Token behaviour;</li>\n<li>a public Tag registry, target lookup, usage index, or client-side resolver;</li>\n<li>changing document lifecycle because a URL is added, removed, or reordered.</li>\n</ul>\n"
}
