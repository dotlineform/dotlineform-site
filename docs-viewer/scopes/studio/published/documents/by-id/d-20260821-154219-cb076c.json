{
  "doc_id": "d-20260821-154219-cb076c",
  "title": "iPad Lightweight Workflow",
  "added_date": "2026-08-21 15:42:19",
  "last_updated": "2026-08-21 16:21:30",
  "viewer_url": "/docs/?scope=studio&doc=d-20260821-154219-cb076c",
  "parent_id": "d-20260808-205414-91040a",
  "summary": "iPad Lightweight Workflow: Background iSH, Shortcuts, CLI Services, and an Inbox",
  "content_html": "<h1>iPad Lightweight Workflow</h1>\n<p><strong>Date:</strong> 20 August 2026<br />\n<strong>Related proof:</strong> <code>d-20260808-205416-9f6d6c</code> — <em>iPad Docs Viewer Installation And Proof</em></p>\n<h2>Purpose</h2>\n<ul>\n<li>Background iSH,</li>\n<li>Shortcuts,</li>\n<li>CLI Services,</li>\n<li>Inbox for ChatGPT</li>\n</ul>\n<p>The iPad does not need to reproduce the full desktop Studio environment.</p>\n<p>A more useful role is:</p>\n<blockquote>\n<p><strong>capture → convert → ingest → package → sync</strong></p>\n</blockquote>\n<p>with more complex organisation, curation, development, and publication left for the desktop.</p>\n<p>This reduces mobile UI requirements while making the iPad a practical low-friction entry point into the dotlineform knowledge system.</p>\n<p>It also creates another route for sharing material with ChatGPT: once material is ingested into the Git repository and pushed to GitHub, it can be inspected directly through the connected GitHub integration without separately uploading the same document into a chat.</p>\n<h2>1. Background iSH</h2>\n<p>The first iPad proof established that Docs Viewer can run locally in iSH and be used through Safari.</p>\n<p>The main lifecycle constraint is that iPadOS can suspend or heavily deprioritise iSH when Safari is full-screen.</p>\n<p>Observed behaviour:</p>\n<pre><code class=\"language-text\">iSH visible\n→ Python service continues running\n→ Safari localhost requests complete\n→ Docs Viewer navigation and managed actions work\n\nSafari full-screen\n→ iSH may stop executing\n→ existing Safari content remains visible\n→ new localhost requests may stall\n→ restoring iSH to a visible window normally allows them to continue\n</code></pre>\n<p>A deeper suspension was also observed where the Python process survived but its HTTP listener on port <code>8776</code> was lost. In that state, the service had to be restarted.</p>\n<h3>iSH background-running workaround</h3>\n<p>The iSH project documents a background-running workaround based on keeping <code>/dev/location</code> active:</p>\n<pre><code class=\"language-sh\">cat /dev/location &gt; /dev/null &amp;\n</code></pre>\n<p>Source:</p>\n<ul>\n<li><a href=\"https://github.com/ish-app/ish/wiki/Running-in-background\">https://github.com/ish-app/ish/wiki/Running-in-background</a></li>\n</ul>\n<p>A cautious test procedure is:</p>\n<pre><code class=\"language-sh\">cat /dev/location &gt; /dev/null &amp;\necho $!\n</code></pre>\n<p>Record the PID printed by <code>echo $!</code>.</p>\n<p>Then test Docs Viewer with Safari fully full-screen:</p>\n<ol>\n<li>load several documents;</li>\n<li>use Search;</li>\n<li>use Recent;</li>\n<li>make a metadata change;</li>\n<li>leave Safari full-screen for several minutes;</li>\n<li>test <code>/health</code> again.</li>\n</ol>\n<p>Stop the background process afterwards with:</p>\n<pre><code class=\"language-sh\">kill &lt;PID&gt;\n</code></pre>\n<p>If this works reliably, full-screen Safari may become viable without keeping an iSH window visible.</p>\n<p>The iSH wiki also describes configuring this through OpenRC, but that should only be considered after the manual mechanism has proved useful and stable.</p>\n<h2>2. iSH Shortcuts Integration</h2>\n<p>iSH does not expose a direct Apple Shortcuts action, but the iSH project documents a workaround using SSH.</p>\n<p>Source:</p>\n<ul>\n<li><a href=\"https://github.com/ish-app/ish/wiki/Shortcuts-Integration-Workaround\">https://github.com/ish-app/ish/wiki/Shortcuts-Integration-Workaround</a></li>\n</ul>\n<p>Conceptually:</p>\n<pre><code class=\"language-text\">Apple Shortcut\n    ↓\nOpen iSH\n    ↓\nwait briefly for SSH\n    ↓\nRun Script over SSH\n    ↓\nexecute command inside iSH\n</code></pre>\n<p>This is relevant because many useful dotlineform operations do not require a large interactive UI.</p>\n<p>Examples:</p>\n<pre><code class=\"language-text\">Markdown → HTML\nDOCX / HTML / Markdown import\ncreate or return JSON document packages\nrebuild a scope\nvalidate generated outputs\ncheck service health\nrestart a local service\nstage media\ncopy or normalize files\n</code></pre>\n<p>For these tasks, Shortcuts can provide the native iPad interaction while iSH performs the underlying work.</p>\n<h2>3. Native iPad UI Outside iSH</h2>\n<p>A Linux shell inside iSH can provide command-line prompts and menus, but it is not the best place to implement native iPad file selection.</p>\n<p>Apple Shortcuts can provide:</p>\n<ul>\n<li>Choose File</li>\n<li>Share Sheet input</li>\n<li>folders in Files/iCloud</li>\n<li>prompts and menus</li>\n<li>notifications</li>\n<li>opening iSH</li>\n<li>SSH command execution</li>\n</ul>\n<p>This suggests a layered design:</p>\n<pre><code class=\"language-text\">Files / Share Sheet\n        ↓\nApple Shortcut\n        ↓\niSH command\n        ↓\nthin shell wrapper\n        ↓\nexisting Python service/library\n        ↓\ncanonical output\n</code></pre>\n<p>The Shortcut should handle the iPad-native interaction.</p>\n<p>The shell/Python layer should handle deterministic processing.</p>\n<h2>4. Thin Linux Wrappers Around Existing Python Services</h2>\n<p>The repository already contains service and library code for Docs Viewer operations.</p>\n<p>The iPad should not gain a second implementation of those operations.</p>\n<p>Instead, expose selected existing capabilities through a small command-line interface.</p>\n<p>Possible commands:</p>\n<pre><code class=\"language-sh\">dlf import document.docx --scope notes\ndlf import notes.md --scope studio\n\ndlf convert document.md --to html\n\ndlf package-export d-20260808-205416-9f6d6c\ndlf package-return returned-package.zip\n\ndlf rebuild notes\ndlf health\ndlf server-start\n</code></pre>\n<p>The shell command should be a thin entry point, for example:</p>\n<pre><code class=\"language-sh\">#!/bin/sh\nexec python3 /root/src/dotlineform-site/tooling/ipad/dlf.py &quot;$@&quot;\n</code></pre>\n<p>The Python dispatcher should then call existing repository service functions.</p>\n<p>The important rule is:</p>\n<blockquote>\n<p><strong>Do not reimplement import, rebuild, package, or mutation logic in the shell script or Shortcut.</strong></p>\n</blockquote>\n<p>Those layers should only provide orchestration.</p>\n<h2>5. CLI Versus HTTP Service</h2>\n<p>There are two possible CLI patterns.</p>\n<h3>Pattern A — CLI calls the running Docs Viewer HTTP API</h3>\n<pre><code class=\"language-text\">dlf import ...\n    ↓\nlocalhost:8776/docs/import-source\n    ↓\nexisting service\n</code></pre>\n<p>Advantages:</p>\n<ul>\n<li>almost identical to the browser path;</li>\n<li>minimal new integration code;</li>\n<li>all validation remains in one HTTP contract.</li>\n</ul>\n<p>Disadvantages:</p>\n<ul>\n<li>requires the long-running Docs Viewer service to be alive;</li>\n<li>inherits the iSH background/listener lifecycle problem.</li>\n</ul>\n<h3>Pattern B — CLI calls the underlying Python service directly</h3>\n<pre><code class=\"language-text\">dlf import ...\n    ↓\nPython management/import service\n    ↓\nexisting source write + rebuild pipeline\n</code></pre>\n<p>Advantages:</p>\n<ul>\n<li>one-shot operation;</li>\n<li>no dependency on Safari;</li>\n<li>no dependency on a persistent localhost listener;</li>\n<li>likely better for Shortcuts;</li>\n<li>easier to run as an isolated background task.</li>\n</ul>\n<p>Disadvantages:</p>\n<ul>\n<li>requires a deliberate supported CLI entry point into the service layer.</li>\n</ul>\n<p>For iPad automation, <strong>Pattern B is likely the stronger long-term design</strong>.</p>\n<p>The browser and CLI can still share the same underlying Python services.</p>\n<h2>6. Example: iPad Import Shortcut</h2>\n<p>A future Shortcut could look like:</p>\n<pre><code class=\"language-text\">Receive file from Share Sheet\n        ↓\nChoose destination scope\n        ↓\nCopy file into dotlineform staging/inbox\n        ↓\nOpen iSH\n        ↓\nRun Script over SSH\n        ↓\ndlf import &lt;file&gt; --scope &lt;scope&gt;\n        ↓\nshow result\n</code></pre>\n<p>Possible source apps include:</p>\n<ul>\n<li>Files</li>\n<li>Safari</li>\n<li>Mail</li>\n<li>ChatGPT downloads</li>\n<li>another document editor</li>\n<li>any app exposing a file through the Share Sheet</li>\n</ul>\n<p>This may be substantially easier on iPad than navigating a full import modal for every operation.</p>\n<p>The existing Docs Viewer import UI should still be tested first because it remains part of the formal iPad proof.</p>\n<p>The CLI/Shortcut route becomes justified when it clearly reduces friction or avoids lifecycle/UI limitations.</p>\n<h2>7. One-Shot Processing Is a Good iPad Fit</h2>\n<p>Many dotlineform operations are naturally bounded jobs:</p>\n<pre><code class=\"language-text\">input file\n    ↓\nconversion / validation\n    ↓\noutput file(s)\n    ↓\nfinish\n</code></pre>\n<p>Examples include:</p>\n<pre><code class=\"language-text\">Markdown → HTML\nHTML normalization\nDOCX → Markdown/HTML\npackage export\npackage return parsing\nJSON generation\nsearch/index rebuild\nsmall media conversion\nvalidation\n</code></pre>\n<p>A one-shot CLI job only needs iSH to remain active for the duration of the operation.</p>\n<p>This is a better match for iPadOS than unnecessarily keeping a local HTTP service alive all day.</p>\n<h2>8. Division of Work: iPad and Desktop</h2>\n<table>\n<thead>\n<tr>\n<th>iPad</th>\n<th>Desktop</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>capture files</td>\n<td>large-scale organisation</td>\n</tr>\n<tr>\n<td>import documents</td>\n<td>restructuring hierarchies</td>\n</tr>\n<tr>\n<td>Markdown → HTML</td>\n<td>bulk metadata editing</td>\n</tr>\n<tr>\n<td>package export/return</td>\n<td>detailed package review</td>\n</tr>\n<tr>\n<td>simple metadata edits</td>\n<td>complex metadata work</td>\n</tr>\n<tr>\n<td>quick rebuilds</td>\n<td>full test suite</td>\n</tr>\n<tr>\n<td>reading/searching</td>\n<td>development/refactoring</td>\n</tr>\n<tr>\n<td>staging files</td>\n<td>Git review and release work</td>\n</tr>\n<tr>\n<td>Share Sheet workflows</td>\n<td>publication</td>\n</tr>\n<tr>\n<td>lightweight Git operations</td>\n<td>heavy media/toolchain work</td>\n</tr>\n</tbody>\n</table>\n<p>The iPad therefore does not need a complete replica of Studio.</p>\n<p>It needs a small number of reliable entry points into the existing system.</p>\n<h2>9. The Inbox Concept</h2>\n<p>A dedicated <strong>Inbox</strong> is a useful extension of this model.</p>\n<p>The purpose would be to let material enter dotlineform without requiring an immediate decision about its final place in the hierarchy.</p>\n<pre><code class=\"language-text\">external document\n    ↓\nShare / Import\n    ↓\nInbox\n    ↓\nnormalized canonical document\n    ↓\nlater organisation on desktop\n</code></pre>\n<p>The guiding principle is:</p>\n<blockquote>\n<p><strong>Capture it, normalize it, identify it, and make it available. Organise it later.</strong></p>\n</blockquote>\n<p>Possible implementation forms include:</p>\n<pre><code class=\"language-text\">Docs Viewer scope:\n    inbox\n\nor\n\nDocs Viewer sub-scope:\n    studio / inbox\n</code></pre>\n<p>The exact location should follow the existing scope/sub-scope architecture rather than being added as an unrelated storage convention.</p>\n<h3>Minimal Inbox metadata</h3>\n<p>An Inbox item might initially need only:</p>\n<pre><code class=\"language-text\">doc_id\ntitle\nsource/import provenance\nadded date\nsource format\noptional source filename\nbasic ui_status\ncanonical body\n</code></pre>\n<p>It does not need complete classification at capture time.</p>\n<p>Later desktop work can add:</p>\n<pre><code class=\"language-text\">parent/hierarchy\ntags\nsemantic tokens\ncross-links\npublishability\nfinal scope/sub-scope\nadditional metadata\n</code></pre>\n<h2>10. Inbox as a Share Sheet Destination</h2>\n<p>The Inbox becomes especially useful when combined with Shortcuts.</p>\n<pre><code class=\"language-text\">File / webpage / document\n        ↓\nShare\n        ↓\n&quot;Send to dotlineform Inbox&quot;\n        ↓\nShortcut\n        ↓\niSH\n        ↓\ndlf inbox-add &lt;file&gt;\n        ↓\ncanonical Inbox document created\n</code></pre>\n<p>The Shortcut could optionally ask:</p>\n<pre><code class=\"language-text\">Title?\nKeep original file?\nTarget scope?\nAdd note?\n</code></pre>\n<p>But the default should remain low-friction.</p>\n<h2>11. Inbox and ChatGPT</h2>\n<p>The Inbox also creates a useful path for sharing documents with ChatGPT.</p>\n<h3>Local/private Inbox</h3>\n<pre><code class=\"language-text\">iPad\n    ↓\nDocs Viewer / CLI\n    ↓\niCloud-backed external Inbox\n</code></pre>\n<p>This is useful for private/local working material.</p>\n<p>It does <strong>not</strong> automatically make the material visible through the connected GitHub integration.</p>\n<h3>Repository Inbox</h3>\n<pre><code class=\"language-text\">iPad\n    ↓\nDocs Viewer / CLI\n    ↓\nrepository Inbox\n    ↓\ncommit + push\n    ↓\nGitHub\n    ↓\nChatGPT can inspect it through the connected repo\n</code></pre>\n<p>This removes the separate chat-upload step.</p>\n<p>A conversation could then be as simple as:</p>\n<blockquote>\n<p>“I just put the new research document in the Inbox. Review it.”</p>\n</blockquote>\n<p>ChatGPT can locate the repository material, inspect it, and work from the canonical version.</p>\n<h2>12. Import Versus Share</h2>\n<p>It may be useful to distinguish two explicit actions.</p>\n<h3>Import</h3>\n<pre><code class=\"language-text\">dlf import ...\n</code></pre>\n<p>Meaning:</p>\n<ul>\n<li>ingest into dotlineform;</li>\n<li>normalize and rebuild;</li>\n<li>remain local unless already in a repository-backed scope.</li>\n</ul>\n<h3>Share</h3>\n<p>Conceptually:</p>\n<pre><code class=\"language-text\">dlf share ...\n</code></pre>\n<p>Meaning:</p>\n<ol>\n<li>ingest;</li>\n<li>rebuild;</li>\n<li>show changed files;</li>\n<li>optionally commit;</li>\n<li>optionally push.</li>\n</ol>\n<p>Commit and push should remain explicit.</p>\n<p>A normal import should never silently publish material to GitHub.</p>\n<h2>13. GitHub as Shared Working Context</h2>\n<p>Once a document has been pushed to the repository, ChatGPT can use the connected GitHub integration to work with it directly.</p>\n<p>The connected GitHub capability also supports focused repository changes, including:</p>\n<ul>\n<li>reading and searching files;</li>\n<li>creating branches;</li>\n<li>creating text files;</li>\n<li>updating existing text files;</li>\n<li>deleting files;</li>\n<li>committing changes;</li>\n<li>opening pull requests;</li>\n<li>reviewing and commenting on pull requests.</li>\n</ul>\n<p>This makes a workflow possible where an Inbox item can move from capture to discussion to a concrete repository change without repeatedly moving copies between tools.</p>\n<pre><code class=\"language-text\">iPad Share Sheet\n    ↓\nInbox\n    ↓\nGitHub\n    ↓\nChatGPT discussion\n    ↓\nfocused branch\n    ↓\nimplementation/documentation change\n    ↓\ndraft pull request\n</code></pre>\n<p>Large cross-cutting refactors are still better suited to Codex or a full local checkout, but small bounded changes can be handled directly through the GitHub integration.</p>\n<h2>14. Possible <code>dlf</code> Command Surface</h2>\n<p>A future command might expose only operations that are useful outside the full UI:</p>\n<pre><code class=\"language-sh\">dlf health\n\ndlf inbox-add file.docx\ndlf inbox-add notes.md\n\ndlf import file.docx --scope notes\ndlf import file.html --scope studio\n\ndlf convert file.md --to html\n\ndlf rebuild studio\ndlf rebuild notes\n\ndlf package-export &lt;doc-id&gt;\ndlf package-return &lt;package&gt;\n\ndlf server-start\ndlf server-stop\ndlf server-status\n</code></pre>\n<p>This should remain deliberately small.</p>\n<p>It is not intended to replace Docs Viewer.</p>\n<h2>15. Three Interfaces to One Service Layer</h2>\n<pre><code class=\"language-text\">                    Python service layer\n                           │\n             ┌─────────────┼─────────────┐\n             │             │             │\n         Docs Viewer      CLI        Shortcuts\n          rich UI      thin wrapper   iPad-native\n</code></pre>\n<h3>Docs Viewer</h3>\n<p>Best for:</p>\n<ul>\n<li>browsing;</li>\n<li>reading;</li>\n<li>search;</li>\n<li>metadata;</li>\n<li>hierarchy;</li>\n<li>visual inspection;</li>\n<li>interactive management.</li>\n</ul>\n<h3>CLI</h3>\n<p>Best for:</p>\n<ul>\n<li>deterministic one-shot operations;</li>\n<li>scripts;</li>\n<li>debugging;</li>\n<li>automation;</li>\n<li>conversion;</li>\n<li>rebuilds.</li>\n</ul>\n<h3>Shortcuts</h3>\n<p>Best for:</p>\n<ul>\n<li>Share Sheet input;</li>\n<li>Files integration;</li>\n<li>file pickers;</li>\n<li>prompts;</li>\n<li>quick actions;</li>\n<li>launching/waking iSH;</li>\n<li>running a CLI operation from ordinary iPad workflows.</li>\n</ul>\n<p>No layer should duplicate the underlying business logic.</p>\n<h2>16. Immediate Next Experiments</h2>\n<h3>1. Background-running test</h3>\n<p>Run:</p>\n<pre><code class=\"language-sh\">cat /dev/location &gt; /dev/null &amp;\necho $!\n</code></pre>\n<p>Then test Docs Viewer in full-screen Safari.</p>\n<p>Determine whether:</p>\n<ul>\n<li>document navigation works;</li>\n<li>Search works;</li>\n<li>Recent works;</li>\n<li>managed writes work;</li>\n<li><code>/health</code> remains available after several minutes;</li>\n<li>the listener survives longer background periods.</li>\n</ul>\n<h3>2. Existing Docs Viewer import</h3>\n<p>Use the current Import UI to import one small disposable file.</p>\n<p>Preferably test:</p>\n<ol>\n<li>Markdown;</li>\n<li>HTML;</li>\n<li>later, a small DOCX with an embedded image.</li>\n</ol>\n<p>This establishes whether the existing rich UI is already sufficient on iPad.</p>\n<h3>3. Identify CLI-worthy friction</h3>\n<p>Do not build a CLI because it is theoretically attractive.</p>\n<p>Use the proof to identify operations that are actually awkward in the browser.</p>\n<p>Those are the first candidates for <code>dlf</code>.</p>\n<h3>4. Prototype an Inbox</h3>\n<p>Once import behaviour is understood, define the minimum Inbox contract:</p>\n<pre><code class=\"language-text\">where it lives\nwhat metadata is required\nwhat source formats it accepts\nhow items move out of it\nwhether it is repository-backed, external, or both\n</code></pre>\n<h2>17. Design Direction</h2>\n<p>The emerging iPad strategy is not:</p>\n<blockquote>\n<p>“Make the entire desktop Studio fit onto an iPad.”</p>\n</blockquote>\n<p>It is:</p>\n<blockquote>\n<p><strong>Give the iPad several low-friction ways to enter the same canonical service layer.</strong></p>\n</blockquote>\n<p>That allows the iPad to be useful for:</p>\n<pre><code class=\"language-text\">capture\nconversion\ningestion\nlight editing\npackaging\nsync\nsharing\n</code></pre>\n<p>while retaining the desktop for:</p>\n<pre><code class=\"language-text\">organisation\ncuration\ndevelopment\nbulk work\npublication\n</code></pre>\n<p>The Inbox can become the bridge between those two modes.</p>\n<p>It also provides a natural bridge into AI-assisted work: once an Inbox item reaches the repository, ChatGPT and Codex can work from the same canonical material without another manual upload step.</p>\n"
}
