{
  "doc_id": "d-20260821-180459-852999",
  "title": "iSH Docs Viewer Startup Script",
  "added_date": "2026-08-21 18:04:59",
  "last_updated": "2026-08-21 18:04:59",
  "viewer_url": "/docs/?scope=studio&doc=d-20260821-180459-852999",
  "parent_id": "d-20260808-205414-91040a",
  "content_html": "<h1>iSH Docs Viewer Startup Script</h1>\n<p><strong>Date:</strong> 21 August 2026</p>\n<h2>Purpose</h2>\n<p>The current iPad/iSH proof requires several manual startup steps:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\ncd /root/src/dotlineform-site\n\nset -a\n. ./.env.local\nset +a\n\npython3 docs-viewer/services/docs_viewer_service.py\n</code></pre>\n<p>A small device-local launcher removes this repetition while the <code>.env.local</code> startup-order issue is still unresolved.</p>\n<p>The launcher should:</p>\n<ol>\n<li>confirm that the iCloud-backed <code>dotlineform</code> mount is available;</li>\n<li>confirm that the iSH Python virtual environment exists;</li>\n<li>enter the repository;</li>\n<li>export <code>.env.local</code> into the process environment;</li>\n<li>start Docs Viewer using the virtual environment's Python directly.</li>\n</ol>\n<h2>Create the launcher</h2>\n<pre><code class=\"language-sh\">mkdir -p /root/bin\n\ncat &gt; /root/bin/dlf-docs &lt;&lt;'EOF'\n#!/bin/sh\nset -e\n\nREPO=/root/src/dotlineform-site\nPYTHON=/root/venvs/dotlineform/bin/python3\nMOUNT=/mnt/dotlineform\n\nif [ ! -d &quot;$MOUNT/docs-viewer&quot; ]; then\n    echo &quot;dotlineform mount is unavailable: $MOUNT&quot;\n    echo &quot;Remount with: mount -t ios . $MOUNT&quot;\n    exit 1\nfi\n\nif [ ! -x &quot;$PYTHON&quot; ]; then\n    echo &quot;dotlineform Python environment not found: $PYTHON&quot;\n    exit 1\nfi\n\ncd &quot;$REPO&quot;\n\nif [ ! -f .env.local ]; then\n    echo &quot;.env.local not found&quot;\n    exit 1\nfi\n\n# Temporary workaround until Docs Viewer loads .env.local early enough.\nset -a\n. ./.env.local\nset +a\n\necho &quot;Starting Docs Viewer...&quot;\nexec &quot;$PYTHON&quot; docs-viewer/services/docs_viewer_service.py\nEOF\n\nchmod +x /root/bin/dlf-docs\n</code></pre>\n<p>Run it with:</p>\n<pre><code class=\"language-sh\">/root/bin/dlf-docs\n</code></pre>\n<h2>Add <code>/root/bin</code> to PATH</h2>\n<p>Add once:</p>\n<pre><code class=\"language-sh\">echo 'export PATH=&quot;/root/bin:$PATH&quot;' &gt;&gt; ~/.profile\n. ~/.profile\n</code></pre>\n<p>Thereafter:</p>\n<pre><code class=\"language-sh\">dlf-docs\n</code></pre>\n<p>is enough to start Docs Viewer.</p>\n<h2>Why call the venv Python directly?</h2>\n<p>The launcher does not need:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\n</code></pre>\n<p>Activation is mostly a convenience for an interactive shell. For a launcher it is simpler and more deterministic to call:</p>\n<pre><code class=\"language-sh\">/root/venvs/dotlineform/bin/python3\n</code></pre>\n<p>directly.</p>\n<h2>Mount check</h2>\n<p>The external iCloud project tree is mounted at:</p>\n<pre><code class=\"language-text\">/mnt/dotlineform\n</code></pre>\n<p>with the expected shape:</p>\n<pre><code class=\"language-text\">/mnt/dotlineform/\n├── docs-viewer/\n├── data-sharing/\n└── projects/\n</code></pre>\n<p>If the mount is missing, the launcher exits and tells you to run:</p>\n<pre><code class=\"language-sh\">mount -t ios . /mnt/dotlineform\n</code></pre>\n<p>and select the real <code>dotlineform</code> folder again.</p>\n<p>The script deliberately does <strong>not</strong> remount automatically, because that would unexpectedly invoke the iPadOS folder picker from a startup command.</p>\n<h2>Temporary <code>.env.local</code> workaround</h2>\n<p>The iSH clone contains:</p>\n<pre><code class=\"language-text\">/root/src/dotlineform-site/.env.local\n</code></pre>\n<p>with values such as:</p>\n<pre><code class=\"language-sh\">DOTLINEFORM_PROJECTS_BASE_DIR=/mnt/dotlineform\nDOCS_VIEWER_MANAGEMENT_ENABLED=1\nDOCS_VIEWER_REVIEW_ENABLED=1\n</code></pre>\n<p>Starting the service directly currently fails unless these values have already entered <code>os.environ</code>, because some external workspace configuration is resolved during imports before Docs Viewer applies <code>.env.local</code>.</p>\n<p>The launcher therefore does:</p>\n<pre><code class=\"language-sh\">set -a\n. ./.env.local\nset +a\n</code></pre>\n<p>Once the repository startup-order issue is fixed, this block can be removed.</p>\n<h2>Expected startup</h2>\n<p>Running:</p>\n<pre><code class=\"language-sh\">dlf-docs\n</code></pre>\n<p>should eventually produce output similar to:</p>\n<pre><code class=\"language-text\">Docs Viewer service: http://127.0.0.1:8776/docs/\nDocs Viewer management: enabled\nDocs Review: enabled\nDocs Viewer generated reads: enabled\n</code></pre>\n<p>Safari:</p>\n<pre><code class=\"language-text\">http://127.0.0.1:8776/health\nhttp://127.0.0.1:8776/docs/\n</code></pre>\n<h2>Check health from a second iSH window</h2>\n<pre><code class=\"language-sh\">wget -qO- http://127.0.0.1:8776/health\n</code></pre>\n<p>Check for the process:</p>\n<pre><code class=\"language-sh\">ps | grep '[d]ocs_viewer_service.py'\n</code></pre>\n<h2>Recover a stale server</h2>\n<p>One lifecycle test produced a state where the Python process still existed but port <code>8776</code> was no longer listening.</p>\n<p>If:</p>\n<pre><code class=\"language-sh\">wget -qO- http://127.0.0.1:8776/health\n</code></pre>\n<p>returns <code>Connection refused</code>, check:</p>\n<pre><code class=\"language-sh\">ps | grep '[d]ocs_viewer_service.py'\n</code></pre>\n<p>Optionally confirm that the port is free:</p>\n<pre><code class=\"language-sh\">python3 -c &quot;import socket; s=socket.socket(); s.bind(('127.0.0.1',8776)); print('port 8776 is free'); s.close()&quot;\n</code></pre>\n<p>If the process exists but the port is free, kill the stale process:</p>\n<pre><code class=\"language-sh\">kill &lt;PID&gt;\n</code></pre>\n<p>Then restart with:</p>\n<pre><code class=\"language-sh\">dlf-docs\n</code></pre>\n<h2>Possible future <code>dlf</code> command</h2>\n<p>The launcher could later become part of a wider CLI:</p>\n<pre><code class=\"language-sh\">dlf docs\ndlf health\ndlf import ...\ndlf rebuild ...\ndlf package-export ...\ndlf package-return ...\n</code></pre>\n<p>That would fit the emerging iPad architecture:</p>\n<pre><code class=\"language-text\">Docs Viewer = rich interactive UI\nCLI         = deterministic one-shot operations\nShortcuts   = native iPad entry points / Share Sheet integration\n</code></pre>\n<p>For now, keep <code>dlf-docs</code> device-local. The <code>.env.local</code> workaround is temporary and the final iPad startup contract is still being established.</p>\n<p>The immediate goal is simply:</p>\n<blockquote>\n<p><strong>one reliable command to restore the Docs Viewer working environment after reopening iSH.</strong></p>\n</blockquote>\n"
}
