{
  "doc_id": "d-20260621-000000-baa17e",
  "title": "Documents Adapter Bridge",
  "added_date": "2026-06-21 00:00:00",
  "last_updated": "2026-07-20 19:09:50",
  "viewer_url": "/docs/?scope=studio&doc=d-20260621-000000-baa17e",
  "parent_id": "d-20260513-171600-a1eeb9",
  "content_html": "<h1>Documents Adapter Bridge</h1>\n<h2>Role</h2>\n<p>This is a temporary DSO-1 compatibility bridge for the existing Analytics browser routes. It translates legacy Data Sharing requests into the direct Docs Viewer document-package service and restores only the response annotations the old browser expects.</p>\n<p>The bridge does not own document selection, package creation, returned-package review, apply, source, or collection import. Those behaviors now live under <code>docs-viewer/services/docs_document_packages/</code>, while Docs Import owns create/overwrite/skip into a configured collection. DSO-3 deletes this bridge with the old routes; it is not a compatibility commitment.</p>\n<h2>Structure</h2>\n<pre><code class=\"language-text\">adapter.py             handler wiring\ncontext.py             scope/dependency/path validation\nprepare.py             legacy prepare delegation\nreturned.py            legacy list/review/apply delegation\nfamilies/documents.py  request/response translation only\n       |\ndocs-viewer/services/docs_document_packages/\n  direct service, source context, package, validation, review, apply, write/rebuild\n</code></pre>\n<h2>Source And Scope</h2>\n<p>The direct service uses <code>scope</code>. The bridge translates legacy <code>selection.docs_scope</code> while the old UI remains. Selectable records and export content come from source Markdown/front matter through Docs Viewer source/rendering context.</p>\n<p>Generated tree, by-id, search, Recent, report, or metadata payloads are never fallback canonical input.</p>\n<h2>Prepare</h2>\n<p><code>docs-viewer/config/document-packages/profiles.json</code> chooses the supported record shape, content/container formats, selection rules, field mappings, context, limits, and returned-import eligibility. The bridge converts legacy <code>config_id</code> and nested selection into direct <code>profile_id</code>, <code>doc_ids</code>, and <code>select_all</code> fields.</p>\n<h2>Returned Path</h2>\n<p>The Docs Viewer document-package service:</p>\n<ul>\n<li>lists staged files through trusted export metadata;</li>\n<li>parses returned rows against the metadata-selected profile;</li>\n<li>produces normalized review rows/issues;</li>\n<li>writes focused Markdown review evidence;</li>\n<li>for supported content packages, can publish an immutable validated Docs Review folder;</li>\n<li>previews/applies the configured summary or hierarchy action through Docs Viewer writes and focused docs/search rebuild follow-through.</li>\n</ul>\n<p>The validated review folder has <code>docs_review_validated_package_v1</code>, a safe metadata-derived ID, source Markdown projection, validation diagnostics, and generated read-only output. It is not a registered Docs Viewer scope and does not mutate canonical documents.</p>\n<h2>Apply Boundary</h2>\n<p>Current actions are intentionally narrow:</p>\n<ul>\n<li><code>summary_apply</code> updates <code>summary</code> only;</li>\n<li><code>hierarchy_apply</code> updates <code>parent_id</code> only.</li>\n</ul>\n<p>Both reparse the complete staged input, validate exact package identity and current source, support dry-run, require confirmation for writes, and use Docs Viewer write/rebuild dependencies. They do not accept returned-row selection, create missing documents, or apply returned body content.</p>\n<h2>Known Weak Spots</h2>\n<ul>\n<li><code>document-content</code> is reconstructed from rendered-derived Markdown/plain text, so source-only syntax/media constructs may be absent from the review projection.</li>\n<li><code>docs_import_document_package_content.py</code> still recognizes a lower-level <code>document-full-source</code> contract even though no enabled returned-import profile owns it.</li>\n<li>The old browser still depends on generic route/config/response shapes until DSO-2.</li>\n<li>Scope fallback exists only in the legacy translation path; direct requests require an explicit configured scope.</li>\n<li>Data Sharing apply and Docs Import are adjacent mutation paths; keep narrow field update distinct from collection import.</li>\n</ul>\n<h2>Extension Rule</h2>\n<p>Do not add document behavior to this bridge. New document-package behavior belongs to an explicit Docs Viewer profile/service contract, and generated Docs Viewer payloads must not become canonical inputs.</p>\n"
}
