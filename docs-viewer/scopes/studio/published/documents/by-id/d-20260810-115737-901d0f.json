{
  "doc_id": "d-20260810-115737-901d0f",
  "title": "Works Report",
  "added_date": "2026-08-10 11:57:37",
  "last_updated": "2026-08-10 12:35:51",
  "viewer_url": "/docs/?scope=studio&doc=d-20260810-115737-901d0f",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "show which published Series have any exact Series-level or member-Work-level Project documentation",
  "ui_status": "proposed",
  "content_html": "<h1>Works Report</h1>\n<h2>Purpose</h2>\n<p>Works is the published-Series-first complement to Project State and Projects. Its title follows the public-site vocabulary used by <code>analysis/works</code>; its row identity remains exact canonical <code>series_id</code>, not <code>work_id</code>.</p>\n<table>\n<thead>\n<tr>\n<th>report</th>\n<th>row authority</th>\n<th>question answered</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Project State</td>\n<td>immediate physical Project folders</td>\n<td>Which source folders have no current canonical Series relationship?</td>\n</tr>\n<tr>\n<td>Projects</td>\n<td>current <code>dotlineform/projects</code> documents</td>\n<td>What documentation exists?</td>\n</tr>\n<tr>\n<td>Works</td>\n<td>published canonical Series</td>\n<td>Which published Series have nothing written about them?</td>\n</tr>\n</tbody>\n</table>\n<h2>Complete Outcome</h2>\n<p>One local-only report in scope <code>dotlineform</code> shows one row for every published Series. The Series cell links to the exact Series page on the configured local site preview. The Docs cell links to every current <code>dotlineform/projects</code> document whose exact valid subject is either that Series or a published Work currently belonging to that Series. Each document retains the Series or Work subject icon that explains why it covers the row.</p>\n<p>A published Series is <strong>written about</strong> when its Docs collection is non-empty. One published Work-subject document is sufficient regardless of how many other member Work IDs have no document. Draft Works do not contribute coverage. A blank Docs cell means the published Series has nothing written about it at either level.</p>\n<h2>Delivery</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260810-115737-741620\">Works Report Concept And Architecture</a> owns the proposed row, browser inputs, join, presentation, and local-only boundaries. <a href=\"/docs/?scope=studio&amp;doc=d-20260810-115737-01cace\">WRK-1 — Published Series Documentation Coverage Report Delivery</a> owns the bounded implementation and verification path.</p>\n<p>No implementation has started. WRK-1 remains proposed and must begin with its sanity gate.</p>\n<h2>Feature Contract</h2>\n<ul>\n<li>The browser reads the Studio-owned Series search projection and filters exact records with <code>status: published</code>; documents and Work membership never create a Series row.</li>\n<li>The browser reads the Studio-owned Work search projection and uses only exact records with <code>status: published</code> when expanding Work subjects through <code>series_ids</code>.</li>\n<li>The browser reads the current private Projects Manage manifest for document title, exact target, and normalized <code>authoring_subject</code>; it does not require a second subject-association read.</li>\n<li>An exact Series subject reaches only its declared <code>series_id</code> row.</li>\n<li>An exact published Work subject reaches every published Series currently named in that Work's canonical <code>series_ids</code> membership. A draft or unknown Work reaches no row.</li>\n<li>Each document appears at most once in one Series row and retains its exact declared subject kind and identity.</li>\n<li>Folder, None, malformed, conflicting, and unknown subjects enter no row.</li>\n<li>Document titles remain presentation labels, not subject or membership evidence.</li>\n<li>Series and document links use their exact existing activation targets; neither is inferred from a title, filename, report host, selected row, or route state.</li>\n<li>Empty rows remain visible because absence is the report's useful result.</li>\n<li>The report is composed in the browser from current local read projections, is read-only and local, creates no persisted coverage product, and exposes no public report capability.</li>\n<li>Public Catalogue JSON is a downstream delivery projection, not publication authority or a Works report input. Projection drift belongs to a separate Catalogue audit.</li>\n<li>The existing Local Studio read boundary serves the two lookup projections; Works adds no report producer, versioned report response, Docs Viewer service method, or report endpoint.</li>\n</ul>\n<h2>Boundary</h2>\n<p>Works does not identify undocumented individual Work IDs, calculate a completeness percentage within a Series, treat every Work as requiring its own document, read semantic-token mentions, include Folder-subject documents, inspect editorial or public document copies, read public Catalogue aggregate or exact-record JSON, consume public <code>doc_url[]</code>, display documentation on public Catalogue pages, change Series membership, mutate document subjects, refresh Project State, or introduce a general relationship/reporting engine.</p>\n<p>The report title does not change the canonical distinction between Series and Works and does not make <code>analysis/works</code> its source collection. Coverage comes only from exact current subjects in <code>dotlineform/projects</code> and exact current Catalogue membership.</p>\n<p>Project State currently retains canonical Work/Series relationships without distinguishing draft from published records. WRK-1 does not silently change that completed report. If the folder-first question must be restricted to published Series, that is a separate Project State refinement with its own review boundary.</p>\n"
}
