{
  "doc_id": "d-20260830-092438-f0c9d2",
  "title": "Publish And Deploy Model",
  "added_date": "2026-08-30 09:24:38",
  "last_updated": "2026-08-30 10:01:41",
  "viewer_url": "/docs/?scope=studio&doc=d-20260830-092438-f0c9d2",
  "parent_id": "d-20260830-074605-dc4f11",
  "summary": "Separate consumer-neutral scope publication, repository deployment, explicit Git commit and push, and manual GitHub Pages deployment.",
  "ui_status": "proposed",
  "content_html": "<h1>Publish And Deploy Model</h1>\n<h2>Purpose</h2>\n<p>This concept defines the document lifecycle that follows the runtime-ownership work in <a href=\"/docs/?scope=studio&amp;doc=d-20260830-074605-dc4f11\">Scope, Data, And Site Foundations</a>. It records what the current Analysis <strong>Publish</strong> action actually does, preserves that behaviour during the Stage 1 runtime cutover, and separates consumer-neutral scope publication, repository deployment, Git commit and push, and public deployment in the target model.</p>\n<p>The separation is useful repository architecture whether or not a Swift application is built. It gives each operation a clear input, output, capability, and side-effect boundary. A future Swift application benefits by adopting those boundaries incrementally rather than requiring a different document lifecycle.</p>\n<h2>Current Analysis Publish Behaviour</h2>\n<p>The current Analysis <strong>Publish</strong> action combines publication with repository deployment. The Docs builders write the current reviewable document and Search output beneath <code>docs-viewer/scopes/analysis/published/</code>; Publish then promotes selected site-ready content into the tracked public projection beneath <code>site/assets/data/</code>.</p>\n<pre><code class=\"language-text\">current builder output\ndocs-viewer/scopes/analysis/published/\n        -&gt; current Publish\ntracked public projection\nsite/assets/data/\n        -&gt; explicit manual Git commit and push\ncommitted repository snapshot\n        -&gt; separate manual GitHub Action\ndotlineform.com\n</code></pre>\n<p>The button first calls <code>POST /docs/publish/confirm</code>. This is a read-only preview that compares the current Analysis output with its configured public projection and reports document, Search, media, and exclusion changes. Confirmation calls <code>POST /docs/publish/apply</code> with an explicit <code>confirm: true</code>.</p>\n<p>Apply performs the following work:</p>\n<ul>\n<li>project the Analysis document payloads into <code>site/assets/data/docs/scopes/analysis/</code>;</li>\n<li>project the configured <code>tags</code> and <code>works</code> sub-scopes into their corresponding Analysis site folders;</li>\n<li>include documents by default and omit roots and descendants with explicit <code>publishable: false</code>, removing only the exact currently public document files owned by those exclusions rather than treating arbitrary missing source files as deletions;</li>\n<li>omit Manage-only artifacts including management manifests, backlinks, references, semantic-token indexes, and publication preparation files;</li>\n<li>create public-safe document payloads by removing local-only report configuration and folder activation data, rewriting managed-media URLs, and using prepared public Mermaid variants;</li>\n<li>copy the existing Analysis Search index byte-for-byte to <code>site/assets/data/search/analysis/index.json</code> when it differs;</li>\n<li>generate the Analysis public document-location projection;</li>\n<li>reconcile referenced image, SVG, HTML, and file media against their configured tracked-site or R2 destinations, including removal of exact stale unreferenced public media;</li>\n<li>reconcile publication-lineage evidence; and</li>\n<li>refresh the exact Catalogue document URL projections as post-publication follow-through.</li>\n</ul>\n<p>Media status or application errors are reported without rolling back a completed document projection. The Search index is copied but is not rebuilt: Publish deploys whichever manually rebuilt Search output currently exists.</p>\n<p>Current Publish does not build documents, rebuild Search, update runtime code, commit repository changes, trigger the GitHub Pages workflow, or deploy dotlineform.com. The manually triggered GitHub Action separately validates and deploys the tracked <code>site/</code> snapshot.</p>\n<h2>Stage 1 Boundary</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260830-074606-881007\">Docs Viewer Runtime Ownership And Site Projection Delivery</a> does not change the current Analysis Publish behaviour, button, endpoints, service, configuration, data paths, publishability rules, or resulting <code>site/assets/data/</code> delta.</p>\n<p>The internal <code>site-code-update</code> manifest owns only projected runtime JavaScript and CSS. It must never own document, Search, media, scope configuration, or other <code>site/assets/data/</code> paths. Source-level runtime tests may move from projected <code>site/</code> paths to canonical runtime paths, but public projection assertions remain against <code>site/</code>; this is not a document Publish change.</p>\n<p>Renaming, splitting, or otherwise changing Analysis Publish during Stage 1 would cross into the later scope/data and document-deployment stages. Stage 1 closeout must therefore confirm that the current Publish preview and apply boundaries remain intact.</p>\n<h2>Target Lifecycle</h2>\n<p>The target model gives Build, Publish, Deploy Repo, the ordinary Git commit and push, and Deploy Public distinct owners and outputs:</p>\n<pre><code class=\"language-text\">canonical source\n        -&gt; Build\nreplaceable generated output\n        -&gt; Publish\naccepted external published snapshot -----&gt; Swift or another direct consumer\n        -&gt; Deploy Repo, Analysis only\ntracked site/assets/data/ projection\n        -&gt; explicit Git commit and push\ncommitted repository snapshot\n        -&gt; Deploy Public\nGitHub Pages and dotlineform.com\n</code></pre>\n<h3>Build</h3>\n<p>Build transforms canonical Markdown, metadata, and source media into replaceable generated document, Search, and media output. Generated output is the working-review surface in Manage mode. Rebuilding does not publish or deploy anything.</p>\n<p>Search remains a manual whole-index rebuild unless a later feature explicitly changes that contract.</p>\n<h3>Publish</h3>\n<p>Publish promotes generated output into the scope's accepted external published snapshot. Every in-scope Docs Viewer scope has this operation, regardless of whether it can be deployed publicly.</p>\n<p>Publish owns document eligibility. Documents are included by default; <code>publishable: false</code> keeps a document in generated working output but excludes it and its owned descendants from the accepted published snapshot. A document that is not in the published snapshot cannot be deployed.</p>\n<p>Published output is consumer-neutral. Manage mode can review it directly, and Swift or another application can consume it without requiring the repository, <code>site/</code>, a local web server, GitHub Pages, or dotlineform.com. Every consumer sees the same published document set. Publish performs the payload preparation required by those consumers but does not copy into <code>site/</code> or perform destination-specific deployment work.</p>\n<h3>Deploy Repo</h3>\n<p>Deploy Repo is available only for Analysis. It projects the accepted Analysis published snapshot into tracked <code>site/</code>, producing the exact data and media snapshot used by local public-site preview and a later public release.</p>\n<p>Deploy Repo copies the complete accepted published document set without applying another eligibility rule or producing a differently filtered edition. The repository projection can lag behind Swift, Manage's Published view, or another direct consumer until Deploy Repo is run, but after deployment it contains the same documents.</p>\n<p>The current Analysis Publish implementation combines the two future operations and is therefore the starting seam for their separation. Its publishability filtering and common consumer-ready payload preparation move to Publish. Its destination-specific responsibilities remain with Deploy Repo: materialising the accepted snapshot at the configured site paths, reconciling public media destinations, and performing follow-through that depends on the repository projection. The later delivery must classify the current URL, document-location, lineage, and Catalogue follow-through steps against that boundary without changing the shared published document set.</p>\n<p>Deploy Repo does not rebuild or publish the scope, commit or push repository changes, or deploy GitHub Pages. Its current implementation writes the local repository working tree. A future provider may perform the same repository-projection operation through Swift and GitHub capability, but repository commit remains an explicit subsequent action.</p>\n<h3>Commit And Push</h3>\n<p>The intended tracked <code>site/</code> delta is reviewed, committed, and pushed through the ordinary explicit Git workflow. This is not part of Build, Publish, or Deploy Repo and is never an implicit side effect of them.</p>\n<p>The current workflow leaves this as a manual user action. A future Swift application may acquire an explicit repository commit and push capability, but it must retain the same visible boundary rather than silently committing as part of Publish or Deploy Repo.</p>\n<h3>Deploy Public</h3>\n<p>Deploy Public is the existing manually triggered GitHub Pages workflow. It assumes that the intended <code>site/</code> state has already been committed and pushed. It validates and deploys that committed snapshot without reading external scopes, rebuilding documents or Search, publishing a scope, generating runtime, modifying <code>site/</code>, or committing changes.</p>\n<p>Deploy Repo, commit and push, and Deploy Public remain independent manual operations. Local preview can inspect an uncommitted or not-yet-public repository projection; Deploy Public releases only the committed snapshot supplied to the workflow. A future Swift application may explicitly perform the repository commit and trigger the GitHub Action, but those capabilities do not collapse the lifecycle boundaries or make either step automatic.</p>\n<h2>Capability-Based Workflow</h2>\n<p>The conceptual separation does not require extra friction in the current local workflow. The Publish modal composes the operations that the active environment can perform:</p>\n<pre><code class=\"language-text\">Publish changes\n\n[x] Publish\n[x] Deploy Repo\n</code></pre>\n<p>Publish and Deploy Repo are both selected by default when both capabilities are available. Deploy Repo is shown unchecked and disabled when the environment cannot write the repository projection. At least one available operation must be selected.</p>\n<p>The selections remain independent:</p>\n<ul>\n<li><strong>Publish only</strong> promotes generated output for every published-data consumer without updating <code>site/</code>.</li>\n<li><strong>Deploy Repo only</strong> copies an already-published Analysis snapshot into <code>site/</code>.</li>\n<li><strong>Publish and Deploy Repo</strong> is the normal current local workflow and runs Publish first, then deploys the exact published revision produced by that operation.</li>\n</ul>\n<p>The primary action label follows the selection: <strong>Publish</strong>, <strong>Deploy Repo</strong>, or <strong>Publish And Deploy</strong>. If Publish succeeds and Deploy Repo fails, the published snapshot remains valid while the repository projection is stale; the result reports that partial completion and permits Deploy Repo to be retried without republishing.</p>\n<p>Capability availability supports an incremental application path:</p>\n<table>\n<thead>\n<tr>\n<th>Environment</th>\n<th>Publish</th>\n<th>Deploy Repo</th>\n<th>Normal behaviour</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Current local Manage</td>\n<td>Available</td>\n<td>Available</td>\n<td>Publish and Deploy Repo together</td>\n</tr>\n<tr>\n<td>Read-only Swift</td>\n<td>Not exposed</td>\n<td>Not exposed</td>\n<td>Read the published snapshot</td>\n</tr>\n<tr>\n<td>Swift with Manage capability</td>\n<td>Available</td>\n<td>Unavailable</td>\n<td>Publish only</td>\n</tr>\n<tr>\n<td>Swift with repository and GitHub capability</td>\n<td>Available</td>\n<td>Available</td>\n<td>Publish and Deploy Repo, with separate explicit commit, push, and Deploy Public actions</td>\n</tr>\n</tbody>\n</table>\n<p>Deploy Public remains outside the Publish modal because it is a separate manual release action against committed repository state.</p>\n<h2>Migration Implications</h2>\n<p>The later scope/data and deployment deliveries need to:</p>\n<ul>\n<li>establish separate external <code>generated/</code> and <code>published/</code> ownership for every migrated scope;</li>\n<li>migrate the current builder output now named <code>published/</code> into the generated layer;</li>\n<li>introduce a genuine scope Publish operation that applies <code>publishable</code>, performs common consumer-ready payload preparation, and promotes generated output to the accepted external published snapshot;</li>\n<li>allow Manage mode to view generated and published output without treating either as a public-site projection;</li>\n<li>replace the current single-purpose Analysis Publish control with the capability-based Publish and Deploy Repo workflow;</li>\n<li>split the existing public projection service so Publish owns document eligibility and shared payload preparation, while Deploy Repo reads accepted external Analysis published output and applies no further document filtering;</li>\n<li>make Deploy Repo capability-dependent while keeping Publish available independently;</li>\n<li>keep destination-specific storage, transfer, and public-projection follow-through out of the consumer-neutral published payload;</li>\n<li>keep repository commit and push explicit and outside the document lifecycle; and</li>\n<li>preserve the existing manually triggered, non-mutating GitHub Pages release workflow as Deploy Public, with future application triggering treated as an explicit capability rather than automation.</li>\n</ul>\n<p>The precise recovery boundary required before canonical scope source leaves repository Git belongs to the external-scope delivery. Document version history beyond that minimum recovery requirement remains a separate future concern.</p>\n<h2>Settled Ownership</h2>\n<table>\n<thead>\n<tr>\n<th>Concern</th>\n<th>Owner</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Canonical Markdown and source media</td>\n<td>External scope source</td>\n</tr>\n<tr>\n<td>Replaceable working payloads</td>\n<td>Build and external generated output</td>\n</tr>\n<tr>\n<td>Accepted application-ready payloads and document eligibility</td>\n<td>Publish and external published output</td>\n</tr>\n<tr>\n<td>Site materialisation of the complete accepted Analysis snapshot</td>\n<td>Deploy Repo for Analysis</td>\n</tr>\n<tr>\n<td>Tracked public document and media snapshot</td>\n<td><code>site/</code></td>\n</tr>\n<tr>\n<td>Repository commit and push</td>\n<td>Explicit Git action</td>\n</tr>\n<tr>\n<td>Public release</td>\n<td>Deploy Public through the manual GitHub Pages workflow</td>\n</tr>\n<tr>\n<td>Canonical shared/public runtime code</td>\n<td>Repository code outside <code>site/</code></td>\n</tr>\n<tr>\n<td>Tracked public runtime projection</td>\n<td>Internal <code>site-code-update</code> development process</td>\n</tr>\n</tbody>\n</table>\n<p>Runtime projection and document deployment share <code>site/</code> as their destination but remain separate processes. Neither is an implicit side effect of the other.</p>\n"
}
