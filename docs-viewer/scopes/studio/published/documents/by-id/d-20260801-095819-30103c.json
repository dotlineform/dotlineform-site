{
  "doc_id": "d-20260801-095819-30103c",
  "title": "Projects",
  "added_date": "2026-08-01 09:58:19",
  "last_updated": "2026-08-01 10:55:58",
  "viewer_url": "/docs/?scope=studio&doc=d-20260801-095819-30103c",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "replace Studio Project State with three-way reconciliation of live folders, Project convenience links, and historical Work paths",
  "content_html": "<h1>Projects</h1>\n<h2>Purpose</h2>\n<p>Give current, historical, and prospective project folders an equally flexible notes layer, and replace the Studio Project State pipeline with a Docs Viewer reconciliation report while keeping Studio Works stable catalogue records.</p>\n<h2>Problem</h2>\n<p><code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code> contains the real source images and files behind the catalogue. Its roughly 200 direct folders are relatively stable working groupings, but they are not permanent identities. As projects evolve, folders can be renamed, moved, split, or consolidated.</p>\n<p>A Work's <code>project_folder</code>, optional <code>project_subfolder</code>, and <code>project_filename</code> record where its source was when the Work was created or its file was uploaded. That path is a useful historical snapshot. It is not a live pointer that should be silently rewritten whenever the working folder structure changes.</p>\n<p>The folder tree, Project documents, and catalogue snapshots can all drift independently and disagree legitimately:</p>\n<ul>\n<li>Works can refer to folders or files that no longer exist at the recorded path;</li>\n<li>a Project document can be created before its intended folder exists, as a note about what could happen;</li>\n<li>a Project document can remain after its folder has moved, been renamed, or ceased to exist;</li>\n<li>a physical folder can exist without a Project document or any catalogue representation;</li>\n<li>a renamed or moved folder can remain represented by older Work snapshots;</li>\n<li>several older folders can become one current project, or one project can split;</li>\n<li>a current folder can relate to no Series, one Series, or several Series; and</li>\n<li>two documents can accidentally declare the same folder URL.</li>\n</ul>\n<p>The problem is not to force these sources back into one shape. It is to make their current, prospective, and historical relationships visible and explicitly manageable.</p>\n<h2>Mental Model</h2>\n<table>\n<thead>\n<tr>\n<th>world</th>\n<th>character</th>\n<th>ownership</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Studio</td>\n<td>database: structured, managed, and stable</td>\n<td>strict canonical ownership of Works and their recorded source history</td>\n</tr>\n<tr>\n<td>Docs Viewer</td>\n<td>semi-structured notes and metadata</td>\n<td>owns its documents and projections, while allowing them to drift from external reality</td>\n</tr>\n<tr>\n<td>folders</td>\n<td>human working world: fluid and potentially chaotic</td>\n<td>no canonical identity or lifecycle contract</td>\n</tr>\n</tbody>\n</table>\n<p>The Project State report observes the disagreement between these worlds; it does not make any one of them manage the other two.</p>\n<ul>\n<li><strong>Project documents represent folders.</strong> Each document is a flexible notes record for one current, historical, or prospective folder and may store a literal local convenience link in source front matter, for example <code>folder_url: &quot;file:///path/to/projects/nerve&quot;</code>.</li>\n<li><strong>The URL is not identity or authority.</strong> It helps a person navigate and helps the report compare evidence, but it does not decide which documents exist, where Studio uploads media, or how any lifecycle proceeds.</li>\n<li><strong>Existence is not a precondition.</strong> A document may precede its folder, track a folder that exists now, or remain as evidence after that URL stops resolving.</li>\n<li><strong>Relationships are explicit.</strong> A stored URL declares the intended folder association; it is never inferred from a document title, folder label, selected Work, or Series name.</li>\n</ul>\n<p>The document, its stored URL, the physical folder, and catalogue path snapshots are separate records of reality. Like Analysis tag documents, Project documents may intentionally exist before or without the external thing they describe. Folder URLs should remain relatively stable so the notes stay understandable, but that is an authoring convention rather than a system-enforced canonical contract. Seeding documents from the current folder inventory is therefore a useful starting point, not a synchronization contract.</p>\n<h2>Desired Capability</h2>\n<p>The <code>dotlineform/projects</code> Docs Viewer sub-scope holds Project documents and ordinary document-management workflows. A registered Manage-only Projects customisation adds folder-specific metadata and controls to that sub-scope report without changing the shared default.</p>\n<p>A separate local Docs Viewer <strong>Project State</strong> report replaces the current Studio route and generated Markdown report. It reconciles three independent inputs:</p>\n<ol>\n<li>folders that currently exist below <code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code>, from a confined folder-only inventory;</li>\n<li>Project document rows and their customisation-owned <code>folder_url</code> values from the local sub-scope management manifest; and</li>\n<li>folders historically recorded by canonical Studio Works, plus the Series reached through those Works.</li>\n</ol>\n<p>The current-folder inventory is live evidence, not a canonical registry. It scans folder identity only and does not continue Studio's primary-image audit. The reconciliation report and document-seeding preview may consume the same confined inventory, but neither owns or synchronizes the folders.</p>\n<p>The three inputs have deliberately different meanings: filesystem folders are current physical evidence, manifest URLs are user-maintained convenience links, and Work folder fields are historical catalogue facts. The report compares them without promoting the manifest URL into a canonical identifier or operational instruction.</p>\n<p>The report should make these states reviewable:</p>\n<ul>\n<li>current folders with no Project document or historical Work reference;</li>\n<li>Project documents whose stored folder exists now;</li>\n<li>Project documents whose stored folder does not exist, without guessing whether the document is prospective, stale, or historical;</li>\n<li>Project documents with missing, malformed, out-of-root, or duplicate folder URLs;</li>\n<li>Work folder snapshots matching no current folder or Project document, one of them, or both;</li>\n<li>Project documents with no matching Work snapshot;</li>\n<li>incomplete Work folder snapshots;</li>\n<li>three-way agreement among a current folder, one Project document, and historical Works;</li>\n<li>unresolved moved, renamed, split, or consolidated history; and</li>\n<li>Project documents related through Works to more than one Series.</li>\n</ul>\n<p>Rows link to the exact Project document, relevant Works and Series, and the literal folder target where one is declared. A validated local action opens an existing target in macOS Finder. Because a browser may not activate a <code>file:</code> URL from an HTTP page, Docs Viewer may provide that action through its local management service; the service must reread the URL from the exact document target and confine it below the allowed projects root. Missing, prospective, historical, and unmatched paths remain visible evidence and never fall back to another folder.</p>\n<p>Bulk document seeding and later creation use a preview/apply management workflow. Creating a document does not require the folder to exist. The report remains an inspection surface rather than owning source writes or treating a mismatch as an instruction to repair it.</p>\n<h2>Path Entry and Editing</h2>\n<p>The ordinary input is likely to be a pathname copied or dragged from Finder or a terminal, not a hand-authored URL. For example:</p>\n<pre><code class=\"language-text\">/Users/dlf/Library/Mobile\\ Documents/com\\~apple\\~CloudDocs/dotlineform/projects/3\\ symbols\n</code></pre>\n<p>Docs Viewer should expose the same paste-and-normalize behavior in two explicit editing contexts:</p>\n<ul>\n<li><strong>Body content:</strong> paste or insert the path as an ordinary clickable local link. It remains document prose and does not declare the document's folder, enter the sub-scope manifest, or affect reconciliation.</li>\n<li><strong>Folder Link metadata:</strong> paste the path into the dedicated field. Saving writes normalized <code>folder_url</code> front matter and projects that structured value into the local Projects management manifest, where the reconciliation report can consume it.</li>\n</ul>\n<p>Both contexts should accept:</p>\n<ul>\n<li>a local <code>file:</code> URL;</li>\n<li>an absolute POSIX pathname containing ordinary spaces; or</li>\n<li>an absolute POSIX pathname whose characters have been backslash-escaped for shell display.</li>\n</ul>\n<p>The pasted value is data, never a shell expression. Docs Viewer performs constrained path decoding without shell evaluation, variable expansion, command substitution, or implicit home-directory expansion. It then previews the decoded path, confirms that it is below the configured projects root, and reports whether the folder currently exists. A body paste is inserted as a clickable link. A Folder Link paste stores one normalized URL form such as:</p>\n<pre><code class=\"language-yaml\">folder_url: &quot;file:///Users/dlf/Library/Mobile%20Documents/com~apple~CloudDocs/dotlineform/projects/3%20symbols&quot;\n</code></pre>\n<p>An in-root Folder Link may be saved when it does not yet exist; the document is then prospective and the report shows that state. Relative paths, non-local authorities, non-<code>file:</code> schemes, and targets outside the configured root are rejected.</p>\n<p>The front-matter field is the document's structured convenience link, not its identity or validity boundary. The registered Projects report customisation projects it into the exact document row's <code>customisation</code> namespace in local <code>manage-manifest.json</code>, for example:</p>\n<pre><code class=\"language-json\">{\n  &quot;doc_id&quot;: &quot;d-example&quot;,\n  &quot;title&quot;: &quot;3 symbols&quot;,\n  &quot;customisation&quot;: {\n    &quot;folder_url&quot;: &quot;file:///Users/dlf/Library/Mobile%20Documents/com~apple~CloudDocs/dotlineform/projects/3%20symbols&quot;\n  }\n}\n</code></pre>\n<p>The shared default row schema, ordinary manifest, and any public projection exclude it. The Projects sub-scope customisation derives its visible link and exact-document Open in Finder action from that namespaced field. The broader reconciliation report can consume the same custom-owned projection alongside filesystem and Studio evidence. Body links can still use the validated local opener, but they remain ordinary links and are never silently promoted into front matter.</p>\n<p>This is deliberately not a Semantic Tokens capability. Project documents are the notes records and their local management manifest is the reportable document inventory; <code>folder_url</code> is only optional convenience metadata on those records. Ordinary body links need no semantic identity, and the front-matter link needs no separate Project registry or target lookup.</p>\n<h2>Ownership</h2>\n<table>\n<thead>\n<tr>\n<th>concern</th>\n<th>authority</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>current-folder inventory and path existence</td>\n<td>confined Docs Viewer scan below <code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code></td>\n</tr>\n<tr>\n<td>Work source-path snapshot</td>\n<td>canonical Studio Work record</td>\n</tr>\n<tr>\n<td>Work-to-Series membership</td>\n<td>canonical Studio catalogue</td>\n</tr>\n<tr>\n<td>Project notes and document identity</td>\n<td>Project document source and membership in the <code>dotlineform/projects</code> Docs Viewer sub-scope</td>\n</tr>\n<tr>\n<td>literal <code>folder_url</code> convenience link</td>\n<td>optional Project document front matter</td>\n</tr>\n<tr>\n<td>pasted-path parsing, normalization, and validation</td>\n<td>exact-target Docs Viewer document creation and metadata editing</td>\n</tr>\n<tr>\n<td><code>folder_url</code> manifest projection and document-row controls</td>\n<td>registered Manage-only Projects sub-scope report customisation</td>\n</tr>\n<tr>\n<td>Project State reconciliation presentation</td>\n<td>dedicated local Docs Viewer report and services</td>\n</tr>\n</tbody>\n</table>\n<p>Studio may provide read-only catalogue evidence to the report. It does not own or resolve <code>folder_url</code>. Docs Viewer must not copy or become the mutation authority for Works or Series.</p>\n<h2>Invariants</h2>\n<ul>\n<li>Folder changes never automatically rewrite Work records.</li>\n<li>A Project document may store one literal local folder URL; neither the field nor its target must exist when the document is created.</li>\n<li>A folder may exist without a Project document, a Project document may exist without a folder, and a Work snapshot may exist without either.</li>\n<li>Work, folder, document, or Series lifecycle never automatically creates, renames, merges, repairs, or deletes any of the others.</li>\n<li>A missing recorded path is a reconciliation state, not proof that a Work or document is invalid or permission to repair it.</li>\n<li>Moved, renamed, split, and consolidated relationships require explicit user decisions.</li>\n<li>One folder can relate to many Works and Series. One document per folder is the useful authoring convention, but missing and duplicate URLs remain reportable drift rather than enforced cardinality.</li>\n<li>Projects customisation and reconciliation actions use the URL stored on the exact Project document and projected in its local management-manifest <code>customisation</code> row. They never synthesize it from a title or obtain it through a Studio-owned lookup or fallback.</li>\n<li>A body link has no reconciliation meaning, even when it points to the same folder as <code>folder_url</code>.</li>\n<li>No Project semantic-token family or separate canonical folder registry duplicates the Project document manifest.</li>\n<li>The default <code>docs_subscope</code> manifest and report contracts remain unchanged and folder-unaware.</li>\n<li>The Projects customisation is additive to document membership; it does not make the sub-scope report invent rows for folders that have no documents.</li>\n<li>Sub-scope membership comes from the documents themselves. A missing, changed, duplicate, or non-resolving <code>folder_url</code> never creates, hides, invalidates, renames, or deletes a document.</li>\n<li>Studio media selection and upload destinations never read <code>folder_url</code>; Studio continues to own its publishing workflow and recorded Work path fields.</li>\n<li>The Docs Viewer Project State report is read-only and never repairs documents, folders, Works, or Series.</li>\n<li>Project State has one report owner. The retired Studio route and Markdown generator do not remain as a second implementation or compatibility surface.</li>\n<li>The replacement report does not write <code>var/studio/reports/project-state.md</code> or another static report snapshot.</li>\n<li>The report's filesystem input enumerates folders, not primary images, and remains observation rather than a continuously synchronized source of Project identity.</li>\n<li>Reconciliation and seeding reuse the same current-folder inventory contract; the report is read-only and the seeding workflow remains explicit preview/apply.</li>\n<li>Pasted path text is parsed as inert data and never evaluated by a shell.</li>\n<li>Successful normalization writes only the exact Project document being created or edited and shows the normalized URL before commit.</li>\n<li><code>folder_url</code> is intentionally local source data. It may appear in local Docs Viewer management data but must not enter a public projection; activation remains confined below the configured projects root.</li>\n<li>Changing a document URL is an explicit notes-side edit and never moves a folder or rewrites catalogue data.</li>\n<li>The feature remains local-only unless a separate public-safe projection is deliberately designed.</li>\n<li>Findings are review prompts, not permission to clean up files or catalogue data.</li>\n</ul>\n<h2>Existing Baseline</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260427-223830-ad1ab0\">Project State</a> describes the current implementation. <code>studio/services/catalogue/project_state_report.py</code> scans project folders and primary images, compares them with Work <code>project_folder</code>, <code>project_subfolder</code>, and <code>project_filename</code> values, and writes <code>var/studio/reports/project-state.md</code>. <code>/studio/project-state/</code> provides the Studio UI for running that generator and opening its Markdown output through catalogue API endpoints.</p>\n<p>The Projects delivery retires that machinery rather than narrowing or sharing it: the Studio route, frontend, report/open endpoints, activity contract, Markdown writer/opener, generated report, and focused route/report tests are removed or rerouted to the Docs Viewer owner. Folder inventory survives only as one input to the new three-way Docs Viewer report and seeding preview. The image-level findings from the old scan do not survive as an implicit residual Studio report; any future image audit needs its own explicitly justified owner and contract. <code>IMAGE_EXTENSIONS</code> is also imported by the catalogue detail-section service today, so that reusable policy must move to an appropriate neutral owner before the old report module is deleted.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260513-105343-6de36b\">Docs Viewer Reports</a> owns the local report boundary. <a href=\"/docs/?scope=studio&amp;doc=d-20260731-155053-3929e2\">Sub-Scope Report Customisation Architecture</a> owns additive controls and validated collection or detail actions inside the existing Projects document report.</p>\n<p>The current generic sub-scope manifests do not project arbitrary document front matter, and <code>dotlineform/projects</code> does not yet select a registered customisation. Projects therefore needs a narrow Manage-only report customisation whose controlled projector reads <code>folder_url</code> and emits it as namespaced row data; this is planned behavior, not an existing contract. That customisation is sufficient for document-level folder links and supplies the Project-document input to reconciliation without changing the default report or introducing another registry.</p>\n"
}
