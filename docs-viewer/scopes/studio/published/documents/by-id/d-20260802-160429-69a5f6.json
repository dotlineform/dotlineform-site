{
  "doc_id": "d-20260802-160429-69a5f6",
  "title": "Authoring Directives Concept And Examples",
  "added_date": "2026-08-02 16:04:29",
  "last_updated": "2026-08-03 20:13:22",
  "viewer_url": "/docs/?scope=studio&doc=d-20260802-160429-69a5f6",
  "parent_id": "d-20260802-150245-4e7f85",
  "summary": "define optional namespaced comments that harmlessly no-op until an exact table-detail or linked-SVG consumer recognises them",
  "ui_status": "proposed",
  "content_html": "<h1>Authoring Directives Concept And Examples</h1>\n<h2>Purpose</h2>\n<p>Some presentation and publication decisions are deliberate authoring choices rather than facts a runtime can infer. An Authoring Directive records that choice next to the affected Markdown block as an ordinary namespaced HTML comment.</p>\n<p>The first consumer is Content Detail table eligibility and the second is table-to-SVG publication. A consumer acts only when it recognises the exact comment form and can associate it with the expected next table. Everything else is ignored and the Markdown renders normally. This is deliberately narrower than a validated Markdown extension language.</p>\n<h2>Illustrative Source</h2>\n<p>The first maintained source shape is:</p>\n<pre><code class=\"language-markdown\">&lt;!-- dotlineform:table-detail --&gt;\n\n| Project | Folder | Series |\n| --- | --- | --- |\n| Nerve | projects/nerve | Series 105 |\n</code></pre>\n<p>The second maintained source shape is:</p>\n<pre><code class=\"language-markdown\">&lt;!-- dotlineform:table-publish format=svg id=project-state --&gt;\n\n| Project | Folder | Series |\n| --- | --- | --- |\n| Nerve | projects/nerve | Series 105 |\n</code></pre>\n<p>Each comment applies only when the next non-blank source block is the expected Markdown table. Intervening prose, another comment, a fence, or another block leaves it inert. This also makes stacked or contradictory comments harmless: only the comment immediately associated with the table can be considered. The SVG directive’s <code>id</code> is a stable author-supplied key within the document for generated-artifact ownership; it is not a DOM ID, URL fragment, or Content Detail table identity.</p>\n<p>Working and exported Markdown retain the comment and table. Rendered HTML may also retain the ordinary invisible comment; comment removal is not a contract. When TE-2 exists, it adds a narrow marker to a table associated with the exact <code>table-detail</code> form. When TE-6 exists and has a complete current artifact, the public projection replaces the table associated with the exact <code>table-publish</code> form. Before those consumers exist, both comments do nothing.</p>\n<h2>Recognition And Harmless No-Op</h2>\n<p>Recognition belongs to each consumer rather than a central directive subsystem:</p>\n<ul>\n<li>comments outside the exact <code>dotlineform:</code> namespace remain ordinary comments and are never interpreted;</li>\n<li>a malformed, unknown, misplaced, duplicated, or unsupported <code>dotlineform:</code> comment is ignored without failing the build;</li>\n<li>the underlying Markdown table remains ordinary semantic HTML whenever no consumer acts;</li>\n<li>the table-detail consumer recognises only its maintained comment and projects <code>data-docs-content-detail=&quot;table&quot;</code> onto the associated semantic table;</li>\n<li>the Content Detail adapter queries that projected marker and never parses source comments or decides whether an unmarked table is large;</li>\n<li>the table-publication consumer recognises only its maintained comment, keeps local and Manage content semantic, and substitutes a complete current SVG only in the public projection; and</li>\n<li>no consumer needs to report comments it does not understand.</li>\n</ul>\n<p>The <code>dotlineform:</code> namespace prevents accidental collisions but does not make a comment valid or mandatory. A document can move through scopes that have no consumer for it; the comment remains source text and the table remains usable.</p>\n<h2>Table Authoring Outcomes</h2>\n<table>\n<thead>\n<tr>\n<th>authored state</th>\n<th>result when the relevant consumer exists</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>ordinary Markdown table</td>\n<td>semantic HTML table with no Content Detail control</td>\n</tr>\n<tr>\n<td>table with <code>table-detail</code> directive</td>\n<td>semantic HTML table carrying the narrow Content Detail eligibility marker</td>\n</tr>\n<tr>\n<td>table with <code>table-publish format=svg</code> directive</td>\n<td>linked SVG figure replaces that table in the public payload</td>\n</tr>\n</tbody>\n</table>\n<p>Both enhanced routes are explicit and opt-in. There is no size threshold, overflow measurement, automatic detection, or question at Publish time. Removing <code>table-detail</code> removes the detail control on the next build. Removing <code>table-publish</code> restores ordinary HTML publication on the next current build and publish. A typo has the same visible result as removing the comment: the table renders normally.</p>\n<p>A document using <code>viewer_content_detail: table</code> declares that its sole semantic table is the default presentation. Its build independently projects the same table-detail eligibility marker, so a redundant <code>table-detail</code> comment is unnecessary. If a table-publication candidate is incompatible with that document-level presentation, the SVG consumer leaves the semantic table unchanged rather than failing the build.</p>\n<h2>Prepared Projection Sequence</h2>\n<pre><code class=\"language-text\">canonical Markdown\n  directive + semantic table\n        |\n        v\nordinary Docs build/watch\n  recognise exact supported comments\n  render the ordinary semantic table\n  attempt SVG + manifest preparation\n        |\n        v\nexisting Publish preview/apply\n  use only complete current preparation\n  otherwise retain the semantic table\n  copy an SVG only when consumed\n        |\n        v\npublic document\n  linked SVG preview -&gt; raw SVG in a new tab\n</code></pre>\n<p>The exact preparation owner is confirmed against the current media-producer and public-projection seams in TE-6.0. The architectural invariant is that authoring builds may render, while the Publish gate remains renderer-free. Publish never uses missing, partial, unsafe, stale, or source-mismatched preparation; it simply retains the ordinary semantic table for that candidate.</p>\n<p>This follows the current public Mermaid boundary without requiring Mermaid’s light/dark family model. A table SVG should initially be one neutral, self-contained artifact unless visual testing proves a theme pair necessary.</p>\n<h2>Public Figure Contract</h2>\n<p>The projected result is conceptually:</p>\n<pre><code class=\"language-html\">&lt;figure class=&quot;docsTableSvg&quot;&gt;\n  &lt;a href=&quot;/path/project-state.svg&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;\n    &lt;img src=&quot;/path/project-state.svg&quot; alt=&quot;Project state table&quot;&gt;\n  &lt;/a&gt;\n  &lt;figcaption&gt;Project state&lt;/figcaption&gt;\n&lt;/figure&gt;\n</code></pre>\n<p>The preview fits the ordinary document measure; clicking it opens the raw SVG in a new tab for full-viewport inspection and browser zoom. The SVG uses a <code>viewBox</code>, selectable native text rather than outlined glyphs, safe links only if explicitly supported, and the configured public SVG sanitization and media role.</p>\n<p>The delivery must freeze how caption and alternative text are authored or derived for SVGs that are successfully projected. A large SVG table is not semantic HTML, so a useful adjacent summary or alternative description remains required. It must also decide whether table cell text remains in public Docs search or is replaced by caption/summary text; a search match that cannot be located in the visible document is not acceptable by accident.</p>\n<h2>Identity And Regeneration</h2>\n<p>Generated identity is derived from the immutable document ID plus the recognised comment’s document-local <code>id</code>. A prepared manifest also records the recognised comment, semantic rendered-table digest, renderer/config version, and output digest. Editing table content, comment options, rendering rules, or relevant style makes the preparation ineligible for reuse; the ordinary table remains the fallback.</p>\n<p>The exact public filename may be stable and versioned through existing media cache policy or content-addressed; TE-6.0 confirms that choice with stale-file cleanup. The source comment never contains the generated path.</p>\n<h2>Copy, Move, And Source Authoring</h2>\n<p>Copy and Move preserve the comment because they preserve canonical Markdown. A target consumer may recognise it or ignore it independently. No transfer workflow generates SVG or rewrites the comment.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260802-163320-9015b0\">TE-1 — Directive Insertion</a> is delivered first and adds one <strong>Directives</strong> control to the existing Manage Source main-view toolbar. It uses an Actions-style menu to list maintained source snippets and inserts the chosen predefined bytes at the captured insertion point. It is a typing shortcut, not the owner of directive recognition or validity.</p>\n<p>The detail command inserts the complete <code>&lt;!-- dotlineform:table-detail --&gt;</code> form. The SVG command inserts <code>&lt;!-- dotlineform:table-publish format=svg id=table-name --&gt;</code> and selects only <code>table-name</code> for immediate replacement in the source. The menu does not guess an ID, open a modal, save, rebuild, report a success message, locate a target table, or detect an existing directive. Removal and correction remain ordinary source editing.</p>\n<p>The UI is only a source-writing convenience. TE-1.0 may keep a blank-line or immediate-next-line pipe-table hint only if it is a trivial local check; neither is required and neither is validation. Save/Rebuild delegates recognition to the dedicated consumer, which ignores unsupported source quickly and cleanly. The implementation reuses the Source adapter’s revision-bound capture and <code>setRangeText</code> editing seam; it must not use <code>execCommand</code>, add a fake undo history, or promise undo behavior the native textarea does not provide.</p>\n<h2>No-Op And Safety Contract</h2>\n<ul>\n<li>unknown, malformed, misplaced, unsupported, or unassociated comments are ignored;</li>\n<li>an incomplete or unsafe SVG ID produces no artifact or substitution;</li>\n<li>duplicate generated identities are not consumed and their tables remain semantic;</li>\n<li>SVG rendering, sanitization, geometry, font, or media-publication failure produces no current consumable record for that table;</li>\n<li>stale preparation is never reused, but it does not block Publish;</li>\n<li>an incompatible document-level Content Detail hint leaves the semantic table unchanged; and</li>\n<li>an unmarked table is never converted because of width, row count, title, scope, or previous artifacts.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>executable directives, templates, macros, conditions, includes, variables, or arbitrary build commands;</li>\n<li>a central directive grammar, validator, capability registry, or diagnostics system merely because two bounded table consumers recognise adjacent comments;</li>\n<li>runtime comment scanning in public or Manage JavaScript;</li>\n<li>automatic conversion based on table size or viewport;</li>\n<li>changing canonical Markdown into an image reference;</li>\n<li>table editing, sorting, filtering, pagination, or SVG interaction beyond browser navigation and zoom;</li>\n<li>using a successfully projected SVG table as an accessible equivalent of semantic HTML without an explicit alternative-text/summary contract; or</li>\n<li>adding options, progress controls, or conversion decisions to Publish.</li>\n</ul>\n<p>The initial Source action does not validate directive meaning, edit attributes after insertion, remove directives, offer a directive-management modal, infer or locate a target table, detect duplicates, or expose unshipped directive ideas.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260523-222005-1085b2\">Projection Contract</a> remains the canonical/local/public authority. <a href=\"/docs/?scope=studio&amp;doc=d-20260719-123719-fb7565\">Document Diagrams</a> remains the nearest prepared-SVG precedent. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-150245-4e7f85\">Table Enhancements</a> owns the delivery sequence and unchanged semantic-table fallback.</p>\n"
}
