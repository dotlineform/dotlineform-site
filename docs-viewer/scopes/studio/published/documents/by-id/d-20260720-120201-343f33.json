{
  "doc_id": "d-20260720-120201-343f33",
  "title": "Diagram Detail View Delivery",
  "added_date": "2026-07-20 12:02:01",
  "last_updated": "2026-07-20 15:04:43",
  "viewer_url": "/docs/?scope=studio&doc=d-20260720-120201-343f33",
  "parent_id": "d-20260715-181845-0158f9",
  "summary": "Add one accessible open-in-new-tab detail control for published SVG diagrams and in-memory inline Mermaid diagrams.",
  "ui_status": "proposed",
  "content_html": "<h1>Diagram Detail View Delivery</h1>\n<h2>Status And Outcome</h2>\n<p>This is an <strong>active sibling delivery</strong> under Diagrams. It does not need a separate concept document. DDV-0 and DDV-1 are complete; DDV-2 begins after checkpoint review.</p>\n<p>A reader can use a small overlaid diagram-detail control to open a rendered diagram in a new browser tab for easier inspection and browser zoom. The same visible action works for both diagram forms while preserving their different storage contracts:</p>\n<ul>\n<li>a published SVG diagram opens its stable served SVG URL;</li>\n<li>a successfully rendered inline Mermaid diagram opens a temporary standalone SVG created from its in-memory SVG DOM.</li>\n</ul>\n<p>The control is labelled <strong>Open diagram in new tab</strong> for assistive technology and uses an expand/open-in-new visual treatment. The diagram body itself remains available for ordinary selection and any diagram-owned links rather than becoming one large click target.</p>\n<p>This delivery owns only the detail-view affordance. The shipped Mermaid producer remains responsible for canonical <code>.mmd</code> and published SVG, while <a href=\"/docs/?scope=studio&amp;doc=d-20260720-102658-7de33d\">Inline Mermaid Rendering</a> remains responsible for turning a local Markdown fence into an in-memory SVG.</p>\n<h2>Settled Placement And Visibility</h2>\n<p>The control is an HTML element outside the <code>&lt;svg&gt;</code> or persistent <code>&lt;img&gt;</code>. A shared HTML frame contains the existing diagram surface and one native new-tab link. The detail adapter may wrap or register the diagram, but it must not insert the control into Mermaid-generated SVG, include it in temporary standalone SVG serialization, or make the diagram body the trigger.</p>\n<p>Representative ownership shape:</p>\n<pre><code class=\"language-html\">&lt;div class=&quot;docsViewer__diagramFrame&quot;&gt;\n  &lt;div class=&quot;docsViewer__diagramViewport&quot;&gt;\n    &lt;!-- existing persistent image or adapter-owned inline SVG host --&gt;\n  &lt;/div&gt;\n  &lt;a class=&quot;docsViewer__diagramDetailControl&quot;\n     href=&quot;...&quot;\n     target=&quot;_blank&quot;\n     rel=&quot;noopener&quot;\n     aria-label=&quot;Open diagram in new tab&quot;&gt;…&lt;/a&gt;\n&lt;/div&gt;\n</code></pre>\n<p>The frame is the positioning container. The diagram viewport keeps its normal size and owns any horizontal scrolling. The compact control is absolutely positioned over the frame's top-right corner with a sufficient stacking level and its own quiet contrasting background. It remains pinned while a wide diagram scrolls underneath. Only the control's small box intercepts pointer input; the rest of the diagram remains selectable and any diagram-owned links remain operable.</p>\n<p>The control is permanently present and never creates a toolbar row, reserved top gap, or layout shift. On hover-capable fine-pointer devices it is visible but muted at rest. It becomes fully unmuted when any of these is true:</p>\n<ul>\n<li>the diagram frame is hovered;</li>\n<li>the control itself is hovered;</li>\n<li>the control has keyboard focus, or the frame matches <code>:focus-within</code>.</li>\n</ul>\n<p>The direct control-hover rule is explicit rather than relying only on frame hover. On touch or other non-hover input, the control remains fully unmuted because there is no discoverable hover state. Reveal styling changes opacity or color only; it must not toggle <code>display</code>, <code>visibility</code>, dimensions, positioning, padding, or document flow. A visible focus ring remains independent of the muted treatment, and reduced-motion preferences suppress any nonessential opacity transition.</p>\n<p>This accepts the possibility that the compact control temporarily overlays a small amount of diagram content. That bounded overlap is preferable to permanently shrinking the diagram, leaving unexplained whitespace, or moving the document when interaction begins.</p>\n<h2>Delivery Boundary</h2>\n<p>In scope:</p>\n<ul>\n<li>one shared rendered-diagram frame and absolutely overlaid detail-control contract;</li>\n<li>persistent SVG diagrams in ordinary rendered document content;</li>\n<li>inline Mermaid diagrams after their browser render succeeds;</li>\n<li>native link and keyboard behavior, a concise accessible label, focus visibility, stable overlay geometry, and responsive placement;</li>\n<li>stable served-URL navigation for persistent SVG;</li>\n<li>temporary Blob URL or equivalent standalone-SVG navigation for inline Mermaid;</li>\n<li>cleanup of temporary browser resources when document content is replaced;</li>\n<li>focused public, local, navigation-remount, accessibility, and failure checks.</li>\n</ul>\n<p>Out of scope:</p>\n<ul>\n<li>an in-page lightbox, modal, pan-and-zoom canvas, custom zoom toolbar, or fullscreen viewer;</li>\n<li>making the whole diagram clickable or intercepting links inside a diagram;</li>\n<li>a layout-flow toolbar, reserved empty control gutter, or hover/focus behavior that shifts document content;</li>\n<li>rasterizing a diagram to WebP, PNG, or another image format;</li>\n<li>persisting inline Mermaid output, allocating media identities, or adding it to Scope Media inventory;</li>\n<li>changing Mermaid authoring, rendering, sanitization, publication, source opening, or export behavior;</li>\n<li>adding the control to arbitrary icons, decorative SVG, report/detail embeds, source mode, or exported static HTML without an explicit host decision.</li>\n</ul>\n<h2>Likely Blast Radius</h2>\n<table>\n<thead>\n<tr>\n<th>boundary</th>\n<th>likely owner or evidence</th>\n<th>expected change</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Persistent diagram recognition</td>\n<td>document payload rendering or a focused post-mount adapter, active scope media configuration</td>\n<td>Identify eligible document SVG media explicitly; do not guess from a filename suffix or expose Mermaid producer mechanics in the UI.</td>\n</tr>\n<tr>\n<td>Shared diagram host</td>\n<td>new focused shared Docs Viewer module</td>\n<td>Wrap or register supported diagrams, add one HTML control outside the diagram DOM, avoid duplicate decoration after navigation or repeated mounts, and clean up owned resources.</td>\n</tr>\n<tr>\n<td>Published SVG navigation</td>\n<td>rendered <code>&lt;img&gt;</code> source and configured served SVG location</td>\n<td>Use the existing stable served URL directly with a new tab and <code>noopener</code>; do not fetch, copy, or reserialize persistent bytes.</td>\n</tr>\n<tr>\n<td>Inline Mermaid integration</td>\n<td>shipped inline-Mermaid adapter</td>\n<td>Register the successfully rendered SVG with the shared host and provide a temporary standalone-SVG target without creating media.</td>\n</tr>\n<tr>\n<td>Document mount</td>\n<td><code>site/docs-viewer/runtime/js/shared/docs-viewer-document-controller.js</code></td>\n<td>Invoke the detail adapter after ordinary document content mounts with explicit scope and host context.</td>\n</tr>\n<tr>\n<td>Presentation</td>\n<td>shared Docs Viewer content CSS and existing icon conventions</td>\n<td>Pin one compact control over the frame's top-right without changing layout; mute it at rest for mouse users, unmute it on frame hover, direct control hover, or focus, and keep it unmuted for non-hover input.</td>\n</tr>\n<tr>\n<td>Assurance</td>\n<td>focused browser-module tests and manual document review</td>\n<td>Prove eligibility, stable versus temporary targets, keyboard operation, navigation cleanup, multiple diagrams, popup behavior, and no export/runtime leakage.</td>\n</tr>\n</tbody>\n</table>\n<h2>Settled Eligibility And Host Contract</h2>\n<p>DDV-0 uses two existing source-aware boundaries and adds no media registry:</p>\n<ul>\n<li>a persistent diagram is a Markdown image whose semantic media token resolves through the active scope's configured <code>svg</code> published-media role; the content-token renderer will mark its generated <code>&lt;img&gt;</code> with <code>data-docs-viewer-diagram-kind=&quot;persistent-svg&quot;</code> before the configured served URL replaces the logical token;</li>\n<li>an inline diagram is the shipped adapter-owned <code>.docsViewer__diagram[data-docs-viewer-diagram-kind=&quot;inline-mermaid&quot;]</code> host after rendering succeeds.</li>\n</ul>\n<p>The persistent rule is based on the configured media role, not a filename suffix, producer name, provider, or URL pattern. Ordinary <code>img</code> media, direct HTML images, inline decorative SVG, icons, and arbitrary page SVG remain unmarked. Using a scope-owned <code>svg</code> semantic image token is the explicit authoring boundary for an eligible persistent document diagram; a future need to distinguish diagram and decorative uses inside that same role must introduce a separate authored contract rather than runtime guessing.</p>\n<p>The shared adapter will accept only those two markers inside the ordinary mounted document-content root. It will normalize either existing surface into this frame without moving the control into the image or SVG DOM:</p>\n<pre><code class=\"language-html\">&lt;div class=&quot;docsViewer__diagramFrame&quot;&gt;\n  &lt;div class=&quot;docsViewer__diagramViewport&quot;&gt;\n    &lt;!-- marked persistent &lt;img&gt;, or the marked inline host and its direct &lt;svg&gt; --&gt;\n  &lt;/div&gt;\n  &lt;a class=&quot;docsViewer__diagramDetailControl&quot; ...&gt;&lt;/a&gt;\n&lt;/div&gt;\n</code></pre>\n<p>The frame is <code>position: relative</code> and remains the surface's block-size container. The viewport retains the current surface and owns horizontal overflow; the absolutely positioned control contributes no intrinsic width, height, margin, padding, or flow content. The control is a sibling of the viewport, so inline standalone-SVG serialization can select only the adapter-owned SVG and can never include UI chrome.</p>\n<p>Persistent decoration is public-safe and appropriate for <code>local</code>, <code>local_external</code>, and <code>public</code> ordinary document routes: the generated image already contains the correct stable served URL, and the control uses that current URL without fetching, copying, or interpreting its provider. Inline registration remains limited to the existing successful <code>local</code> render. Reports, review/detail and embedded surfaces, source mode, and exports do not invoke the ordinary document detail adapter. A generated persistent marker may remain inert in exported HTML, but no frame, control, detail runtime, or temporary SVG resource is exported.</p>\n<p>This contract fits the existing payload renderer, shared document controller, and shipped inline host. It does not require a general media registry, browser config expansion, or broad Markdown-output refactor, so the DDV-0 stop condition is not triggered.</p>\n<h2>Delivery Tracker</h2>\n<p><strong>Current state:</strong> DDV-0 and DDV-1 complete. Configured SVG media tokens emit the persistent marker, and ordinary public, external-local, and local document mounts receive one shared accessible stable-URL detail control without management or inline-rendering dependencies.</p>\n<p><strong>Next checkpoint:</strong> DDV-2 — register successful inline Mermaid hosts with the shared adapter and own temporary standalone-SVG resource creation and cleanup. Do not begin until DDV-1 is reviewed.</p>\n<h2>Delivery Checkpoints</h2>\n<h3>DDV-0 — Eligibility And Host Contract</h3>\n<ul>\n<li>[x] Decide the smallest explicit marker or payload/runtime contract that distinguishes supported document diagrams from ordinary images and decorative SVG.</li>\n<li>[x] Confirm the same host shape can contain the current published-SVG <code>&lt;img&gt;</code> and the shipped inline Mermaid <code>&lt;svg&gt;</code> without changing either source contract.</li>\n<li>[x] Keep the detail control as an HTML sibling outside the SVG/image surface so standalone SVG serialization never includes UI chrome.</li>\n<li>[x] Confirm absolute top-right placement changes no diagram or document geometry and leaves only the compact control box above diagram content.</li>\n<li>[x] Confirm persistent SVG detail viewing is appropriate on local, external-local, and public document routes.</li>\n<li>[x] Keep reports, review/detail surfaces, embedded documents, source mode, and exports excluded unless their host context is explicitly added later.</li>\n<li>[x] Stop if reliable persistent-diagram recognition would require a general media registry or broad Markdown-output refactor.</li>\n</ul>\n<p>DDV-0 inspected the configured media projection, content-token renderer, shared ordinary document mount, static export path, existing image layout rules, and shipped inline Mermaid host. The smallest contract is one generated persistent marker beside the existing inline marker. DDV-1 can use the resolved <code>&lt;img&gt;</code> URL directly and compose a public-safe shared adapter; DDV-2 remains a separate temporary-resource slice.</p>\n<h3>DDV-1 — Persistent SVG Detail Control</h3>\n<ul>\n<li>[x] Decorate each eligible published SVG diagram after an ordinary document mount with one absolutely overlaid top-right <strong>Open diagram in new tab</strong> control.</li>\n<li>[x] Open the current stable served SVG URL directly; preserve absolute and same-origin configured media locations without provider-specific branching.</li>\n<li>[x] Use native keyboard-capable link behavior, visible focus, an accessible label, and safe new-tab relationship attributes.</li>\n<li>[x] Keep the control visibly muted at rest for fine-pointer hover devices, fully unmute it on frame hover, its own hover, or focus, and keep it fully unmuted when hover is unavailable.</li>\n<li>[x] Change only paint properties during interaction; never add/remove the control or change spacing, dimensions, positioning, or document flow.</li>\n<li>[x] Avoid duplicate controls across navigation, repeated mounts, or management-mode projection.</li>\n<li>[x] Verify the existing Configuration And Extension Points and Docs Import diagrams as proof fixtures.</li>\n</ul>\n<p>DDV-1 added the generated <code>persistent-svg</code> marker at the configured-media renderer, one site-owned shared read-only detail adapter, explicit public and manage composition, ordinary-document controller handoff, and shared overlay presentation. Docs Review does not compose the adapter. The native anchor retains the generated <code>src</code> string as its <code>href</code>, uses <code>_blank</code>, <code>noopener</code>, and the exact accessible label, and contains only an <code>aria-hidden</code> open-in-new icon. The frame and viewport wrap only the marked image; ordinary images, arbitrary SVG, inline Mermaid, and missing targets remain untouched. Repeated mounts add no duplicate frame or control.</p>\n<p>The Configuration And Extension Points and Docs Import payloads were rebuilt through one targeted dry-run/write pair and each contains its expected marker. Eleven focused builder/public-boundary tests, JavaScript and Python syntax checks, the diagram-detail module smoke, the adjacent inline-Mermaid smoke, router-module smoke, public Library/Analysis/Moments read-only smoke with an injected persistent SVG, site validation, generated-payload inspection, and <code>git diff --check</code> form the DDV-1 acceptance evidence. The focused browser smoke also proves stable same-origin and absolute targets, unchanged image geometry, one compact control, independent diagram semantics, focus visibility, and duplicate avoidance. Ruff was unavailable in the configured Python environment; no Ruff result is claimed.</p>\n<h3>DDV-2 — Inline Mermaid Detail Target</h3>\n<ul>\n<li>[ ] Depend on a successfully rendered inline Mermaid host rather than parsing the Markdown fence again.</li>\n<li>[ ] Serialize only the adapter-owned rendered SVG into a standalone <code>image/svg+xml</code> browser resource; do not accept arbitrary page SVG.</li>\n<li>[ ] Preserve the diagram's responsive <code>viewBox</code>, visible content, title/description accessibility metadata, and required standalone presentation.</li>\n<li>[ ] Open the temporary target from direct user activation and manage its lifetime without leaking one object URL per remount.</li>\n<li>[ ] Leave the rendered inline diagram usable when detail-target creation or opening fails.</li>\n</ul>\n<p>DDV-2 may ship only after inline Mermaid rendering provides the shared host contract. DDV-1 can ship independently.</p>\n<h3>DDV-3 — Integration And Closeout</h3>\n<ul>\n<li>[ ] Prove multiple persistent and inline diagrams receive one control each and keep distinct targets.</li>\n<li>[ ] Prove public routes add no inline Mermaid runtime while published SVG detail links continue to work.</li>\n<li>[ ] Check narrow width, light/dark themes, rest/frame-hover/direct-control-hover/focus/touch treatments, zero layout shift, keyboard and screen-reader labelling, browser zoom, navigation remount, and failed-open behavior manually.</li>\n<li>[ ] Add focused module/route contracts without testing ordinary visual choreography.</li>\n<li>[ ] Update Document Diagrams and the Diagrams feature parent with the shipped contract; do not add export promises.</li>\n</ul>\n<h2>Completion Evidence</h2>\n<ul>\n<li>A published Mermaid-backed SVG opens its stable configured SVG URL in a new tab from the overlaid control.</li>\n<li>A local inline Mermaid diagram opens a visually equivalent temporary standalone SVG without writing media or source files.</li>\n<li>The two forms share one accessible control and diagram-host contract while retaining distinct target creation.</li>\n<li>Diagram-owned links remain operable because the diagram body is not the detail trigger.</li>\n<li>Rest, hover, direct control hover, focus, and touch states change no diagram or document geometry and leave no unexplained control gap.</li>\n<li>Navigation and repeated mounts create no duplicate controls or abandoned temporary URLs.</li>\n<li>Public and external-local routes do not acquire the inline Mermaid renderer through this feature.</li>\n</ul>\n"
}
