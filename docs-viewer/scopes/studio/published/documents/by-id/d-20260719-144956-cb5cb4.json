{
  "doc_id": "d-20260719-144956-cb5cb4",
  "title": "Scope-First Storage Layout",
  "added_date": "2026-07-19 14:49:56",
  "last_updated": "2026-07-19 20:43:24",
  "viewer_url": "/docs/?scope=studio&doc=d-20260719-144956-cb5cb4",
  "parent_id": "d-20260714-234030-1e0af6",
  "summary": "Make each Docs Viewer scope the explicit ownership and lifecycle container, with source and published roles organised by artifact class beneath it independently of storage provider.",
  "ui_status": "active",
  "content_html": "<h1>Scope-First Storage Layout</h1>\n<h2>Purpose</h2>\n<p>Make the scope, rather than the artifact role, the outer ownership and lifecycle boundary for scope-owned Docs Viewer data, and give the artifact classes within each lifecycle role a consistent hierarchy.</p>\n<p>Each scope owns its canonical document and build source, local consumer-ready output, direct-published media, reproducible published output, and configured provider destinations. <code>source</code> and <code>published</code> describe how the scope treats an artifact; they do not own separate global pieces of every scope.</p>\n<p>The feature therefore contains two related structural changes: the scope moves above the lifecycle role, and published media moves out of the document-publication tree to become a sibling of <code>documents/</code> and <code>search/</code> beneath <code>published/</code>. Source media is likewise a sibling of source <code>documents/</code>.</p>\n<h2>Status</h2>\n<p>Active. External-local media configuration, creation, rename, serving, and tests now place published media beneath the external published-documents root. The existing Notes Mermaid-built and directly published SVGs have been moved and verified there, including a real Mermaid rebuild and local media-route resolution. The broader scope-first migration remains open in this feature.</p>\n<h2>Future State</h2>\n<p>The provider-neutral shape is scope-first and artifact-class-first within each lifecycle role. The maximal repository-backed scope below shows every currently supported stable directory. Individual scopes only materialize the branches they configure or generate.</p>\n<pre><code class=\"language-text\">docs-viewer/scopes/&lt;scope&gt;/\n├── source/\n│   ├── documents/\n│   ├── media/\n│   │   └── mermaid/\n│   └── sub-scopes/\n│       └── &lt;sub-scope&gt;/\n│           └── documents/\n└── published/\n    ├── documents/\n    │   ├── by-id/\n    │   ├── references/\n    │   │   ├── by-doc/\n    │   │   ├── by-target/\n    │   │   │   └── &lt;target-kind&gt;/\n    │   │   └── index.json\n    │   ├── sub-scopes/\n    │   │   └── &lt;sub-scope&gt;/\n    │   │       ├── by-id/\n    │   │       └── manifest.json\n    │   ├── .publish/\n    │   │   └── recent.json\n    │   ├── index-tree.json\n    │   └── recent.json\n    ├── search/\n    │   ├── sub-scopes/\n    │   │   └── &lt;sub-scope&gt;/\n    │   │       └── index.json\n    │   └── index.json\n    └── media/\n        ├── files/\n        ├── html/\n        ├── img/\n        └── svg/\n</code></pre>\n<p><code>mermaid/</code> is the only currently registered source-media build type. <code>files/</code>, <code>html/</code>, <code>img/</code>, and <code>svg/</code> are the currently supported published-media types; their provider and served-path configuration still determines whether and how each branch is exposed. Generated semantic-reference artifacts remain within <code>published/documents/references/</code>, because they are part of the document publication rather than canonical source or general media. <code>.publish/</code> is a persistent builder-owned helper for the selected public <code>recent.json</code> snapshot, not a separate lifecycle role or general build cache.</p>\n<p>Application-level data that no scope owns uses a separate data boundary:</p>\n<pre><code class=\"language-text\">docs-viewer/data/\n└── generated/\n    └── semantic-references/\n        └── target-lookup.json\n</code></pre>\n<p>The semantic-reference target lookup combines catalogue and configured-scope inputs for the manage-only source editor, so assigning it to one scope would be false ownership. <code>generated/</code> is accurate here as provenance for a reproducible application-level artifact; it is not an alternative name for scope publication. The cutover materializes only <code>data/generated/</code>. Authored configuration stays in <code>config/</code>, scope-owned artifacts stay in <code>scopes/</code>, and machine-local logs, caches, or staging remain outside this tracked application-data boundary. Other <code>data/</code> classes should be added only when a concrete non-scope dataset establishes their ownership and lifecycle.</p>\n<p>Sub-scopes are one level deep. A configured <code>&lt;sub-scope&gt;/documents/</code> is a leaf source-document set; there is no recursive child collection or builder support for a sub-scope of a sub-scope. The current lifecycle code mechanically materializes an unused nested <code>sub-scopes/</code> directory beneath a new sub-scope source, but that directory is not a supported content location. The future-state contract omits it, and the migration should stop creating it.</p>\n<p>For an external-local scope the same contract resolves beneath the external workspace, for example:</p>\n<pre><code class=\"language-text\">$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/scopes/notes/\n├── source/\n└── published/\n</code></pre>\n<p>The enclosing <code>&lt;scope&gt;</code> container is the create, rename, copy, backup, and delete boundary through the configured provider adapter. Its source and published children must not overlap.</p>\n<p>A future provider-native source location must preserve the same ordering rather than recreate a provider-specific role-first tree:</p>\n<pre><code class=\"language-text\">&lt;configured provider root&gt;/scopes/&lt;scope&gt;/source/...\n</code></pre>\n<p>For example, adding R2 source support would map canonical source identities beneath that scope-first object-key root. This feature establishes the location contract but does not add R2 source support.</p>\n<p>Public exposure remains an independent provider concern. A public scope can keep its source and working publication beneath its canonical scope root while registered public projections and remote media remain at their deployment destinations:</p>\n<pre><code class=\"language-text\">site/assets/data/docs/scopes/&lt;scope&gt;/...\nsite/assets/data/search/&lt;scope&gt;/...\nR2: docs/&lt;scope&gt;/...\n</code></pre>\n<p>The scope owns those identities and location declarations through configuration; the repository deploy root or remote provider owns their physical storage. The feature does not introduce duplicate local copies of public assets.</p>\n<p>Build remains an optional transformation between source and published rather than a persistent directory role. Public URLs, document identities, media tokens, provider policy, and management/public exposure remain unchanged.</p>\n<h2>Feature Boundary</h2>\n<ul>\n<li>Give every scope an explicit configured scope root independently of storage provider.</li>\n<li>Place Docs Viewer-owned source and working published data beneath that root using the same role names for every scope.</li>\n<li>Organise each role by artifact class, with media beside documents rather than physically nested inside the document-publication root.</li>\n<li>Keep configured sub-scopes one level deep and stop materializing an unsupported descendant <code>sub-scopes/</code> directory beneath them.</li>\n<li>Make scope create, rename, delete, configuration validation, builders, media publication, and serving resolve paths through that provider-neutral boundary.</li>\n<li>Keep public projections and R2 objects at their configured provider destinations and lifecycle-manage them as registered scope-owned outputs.</li>\n<li>Move the repository-wide semantic-reference target lookup to <code>docs-viewer/data/generated/semantic-references/target-lookup.json</code> and update its registered manage-only browser URL.</li>\n<li>Keep <code>docs-viewer/data/</code> for explicitly owned non-scope application data rather than using it as a general miscellaneous-data root.</li>\n<li>Move existing local bytes through one verified migration without compatibility aliases or duplicate canonical locations.</li>\n<li>Do not add R2 source support, a persistent build cache, or a different imported-versus-built SVG identity policy as part of the folder move.</li>\n</ul>\n<h2>Success</h2>\n<ul>\n<li>A scope's repository or external-local source and working publication resolve from one explicit scope container; configured public projections and R2 media remain independent destinations.</li>\n<li>Documents, search, references, source build inputs, and every supported published-media type have one explicit place in the scope hierarchy.</li>\n<li>Source contains only canonical source material; published contains consumer-ready artifacts.</li>\n<li>Direct-published media is not mistaken for reproducible build output and is preserved by lifecycle operations.</li>\n<li>Public projection paths and remote object keys remain stable.</li>\n<li>Configuration rejects source/published overlap and no longer needs to treat a source root as the scope container.</li>\n<li>The role-first top-level <code>source/</code> and <code>published/</code> trees are gone; the application-level target lookup is reproducible beneath <code>data/generated/</code>.</li>\n</ul>\n<h2>Delivery</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260719-202836-1769ad\">Scope-First Storage Cutover Delivery</a> owns the configuration, resolver, lifecycle, consumer, byte-migration, verification, and closeout work for the remaining scope-first cutover.</p>\n<h2>Current State</h2>\n<p>The current repository is mostly organised by artifact lifecycle role first:</p>\n<p><code>&lt;workspace&gt; -&gt; source | published -&gt; &lt;scope&gt; -&gt; content</code></p>\n<p>The paths encode several independent dimensions:</p>\n<ul>\n<li><strong>artifact lifecycle role</strong>: <code>source</code> or <code>published</code>; build is an optional transformation between them, not currently a persistent storage role; the lifecycle itself is the set of operations that create, rename, copy, back up, or delete a scope.</li>\n<li><strong>scope</strong>: ownership such as Studio, Analysis, or Notes;</li>\n<li><strong>artifact type</strong>: documents, search, or a media type;</li>\n<li><strong>storage provider</strong>: repository, external-local, or R2;</li>\n<li><strong>exposure</strong>: management-only or public.</li>\n</ul>\n<p>Within those dimensions, three locations need to be distinguished:</p>\n<ul>\n<li><strong>source</strong>: canonical Markdown and registered build inputs such as Mermaid <code>.mmd</code> files;</li>\n<li><strong>working publication</strong>: consumer-ready document payloads, search indexes, and locally published media used by the management viewer;</li>\n<li><strong>public projection</strong>: the selected public document and search payloads tracked beneath <code>site/assets/</code>.</li>\n</ul>\n<p>Public exposure and physical storage are configured independently. Published media can be stored in the repository, in the external-local workspace, or in R2.</p>\n<h3>Public scopes</h3>\n<p>Analysis is a representative public scope.</p>\n<h4>Source</h4>\n<ul>\n<li>documents: <code>docs-viewer/source/analysis/documents</code></li>\n<li>registered Mermaid source: <code>docs-viewer/source/analysis/media/mermaid</code></li>\n<li>scope root: <code>docs-viewer/source/analysis</code></li>\n</ul>\n<p>The source tree also contains materialised media directories. Their presence does not make them the configured published destinations.</p>\n<h4>Working publication</h4>\n<ul>\n<li>documents: <code>docs-viewer/published/docs/analysis/by-id</code></li>\n<li>search: <code>docs-viewer/published/search/analysis/index.json</code></li>\n</ul>\n<p>The extra <code>docs/</code> level distinguishes document payloads from the sibling <code>search/</code> publication tree.</p>\n<h4>Public projection</h4>\n<ul>\n<li>documents: <code>site/assets/data/docs/scopes/analysis/by-id</code></li>\n<li>search: <code>site/assets/data/search/analysis/index.json</code></li>\n</ul>\n<p>The public projection is distinct from working publication and can contain a selected subset of the working documents.</p>\n<p><code>site/assets/data/docs/scopes/</code> is the namespace for Docs Viewer scope-owned public data. The parent <code>site/assets/data/docs/</code> can also contain document-domain data that is not owned by one Docs Viewer scope; <code>public-reports.json</code> is a current example. The <code>scopes/</code> level is therefore an intentional ownership boundary, not merely an extra copy of the scope name.</p>\n<h4>Published media</h4>\n<p>Analysis media does not have one physical root:</p>\n<ul>\n<li>images: <code>site/assets/data/docs/scopes/analysis/media/img</code></li>\n<li>SVG: <code>site/assets/data/docs/scopes/analysis/media/svg</code></li>\n<li>files: R2 object prefix <code>docs/analysis/files</code></li>\n</ul>\n<p>Repository-hosted public media sits alongside the public scope projection, while configured R2 media remains outside the repository.</p>\n<h3>Local scopes (in-repo)</h3>\n<p>Studio is a representative repository-backed management scope.</p>\n<h4>Source</h4>\n<ul>\n<li>documents: <code>docs-viewer/source/studio/documents</code></li>\n<li>registered Mermaid source: <code>docs-viewer/source/studio/media/mermaid</code></li>\n<li>scope root: <code>docs-viewer/source/studio</code></li>\n</ul>\n<h4>Working publication</h4>\n<ul>\n<li>documents: <code>docs-viewer/published/docs/studio/by-id</code></li>\n<li>search: <code>docs-viewer/published/search/studio/index.json</code></li>\n<li>media: <code>docs-viewer/published/docs/studio/media</code></li>\n</ul>\n<p>Studio has no public projection. Its source and published media are physically separate.</p>\n<h3>External-local scopes</h3>\n<p>Notes is the current external-local scope. The paths below are relative to <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code>.</p>\n<h4>Source</h4>\n<ul>\n<li>documents: <code>docs-viewer/source/notes/documents</code></li>\n<li>Mermaid source: <code>docs-viewer/source/notes/media/mermaid</code></li>\n<li>configured source root: <code>docs-viewer/source/notes</code></li>\n</ul>\n<h4>Working publication</h4>\n<ul>\n<li>documents: <code>docs-viewer/published/docs/notes/by-id</code></li>\n<li>search: <code>docs-viewer/published/search/notes/index.json</code></li>\n<li>images: <code>docs-viewer/published/docs/notes/media/img</code></li>\n<li>SVG: <code>docs-viewer/published/docs/notes/media/svg</code></li>\n<li>files: <code>docs-viewer/published/docs/notes/media/files</code></li>\n</ul>\n<p>External-local source and published roles are now separate. The validator confines external-local media beneath the scope's published media root. Rename moves that media with <code>published/docs/&lt;scope&gt;</code>, while deletion removes the same manifest-owned published root.</p>\n<h3>Shared SVG namespace</h3>\n<p>Imported SVG and Mermaid-built SVG publish into the same configured <code>published.media.svg</code> namespace. For Notes that is <code>docs-viewer/published/docs/notes/media/svg</code>; for Studio it is <code>docs-viewer/published/docs/studio/media/svg</code>.</p>\n<p>Mermaid output uses the source basename deterministically: <code>&lt;name&gt;.mmd</code> publishes as <code>&lt;name&gt;.svg</code>. A directly imported <code>&lt;name&gt;.svg</code> can therefore target the same identity. Interactive staging reports changed-byte collisions and requires replacement confirmation, while an ordinary Mermaid rebuild publishes its deterministic output by replacement. The collision concern applies to every Mermaid-enabled scope and is independent of the scope-first storage change.</p>\n<h3>Build and temporary locations</h3>\n<p>There is no persistent or scope-owned <code>build/&lt;scope&gt;</code> artifact tree in the current implementation:</p>\n<ul>\n<li>document and search builders calculate their write plans and write consumer-ready payloads directly to the configured published locations;</li>\n<li>public scopes can retain a builder-owned <code>.publish/recent.json</code> beneath their working document publication so the selected public recent-documents snapshot remains distinct from the management snapshot;</li>\n<li>Mermaid renders intermediate SVG into an operating-system temporary directory, sanitizes it, and then writes the verified SVG to the configured published media location;</li>\n<li>provider adapters can stage remote artifacts in operating-system temporary directories;</li>\n<li>local publication and lifecycle operations can use short-lived sibling <code>.tmp</code> files for atomic replacement.</li>\n</ul>\n<p><code>.publish/</code> is persistent and structurally stable enough to appear in the full hierarchy, but it remains an internal publication helper rather than a lifecycle role or general build cache. The other temporary paths are implementation details and are removed or replaced during the operation, so they are not stable enough to count as filesystem roles. <code>docs-viewer/build/</code> is structurally stable, but it contains builder code and the Mermaid toolchain rather than per-scope build artifacts. The persistent Data Sharing import and review staging roots are workflow inputs and review state, not Docs Viewer build output.</p>\n<h3>Repository-wide generated data</h3>\n<p>The semantic-reference target lookup is generated at <code>docs-viewer/published/semantic-references/target-lookup.json</code>. It combines catalogue records with configured Docs Viewer scope output and serves the manage-only semantic-token picker. It is not owned by one scope, so its move to the application-level <code>data/generated/</code> boundary is separate from the scope-root byte moves but part of the same cutover.</p>\n<h3>Current-state conclusions</h3>\n<ul>\n<li>The repository currently uses a role-first <code>source/&lt;scope&gt;</code> and <code>published/&lt;artifact-type&gt;/&lt;scope&gt;</code> layout.</li>\n<li>Build is currently a transformation between source and published, without a persistent artifact tree of its own.</li>\n<li>Public scopes add a separate public projection and may split media across repository and R2 locations.</li>\n<li>Local in-repo scopes keep source and published media separate.</li>\n<li>External-local scopes now keep source separate from documents, search, and media in their external published roots.</li>\n<li>The semantic-reference target lookup is an active repository-wide published artifact rather than a scope-owned output.</li>\n<li>Source versus published is the durable semantic distinction; public versus management exposure is a separate concern.</li>\n</ul>\n"
}
