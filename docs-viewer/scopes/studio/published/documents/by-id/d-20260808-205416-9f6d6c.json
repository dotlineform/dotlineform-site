{
  "doc_id": "d-20260808-205416-9f6d6c",
  "title": "iPad Docs Viewer Installation And Proof",
  "added_date": "2026-08-08 20:54:16",
  "last_updated": "2026-08-08 20:54:16",
  "viewer_url": "/docs/?scope=studio&doc=d-20260808-205416-9f6d6c",
  "parent_id": "d-20260808-205414-91040a",
  "summary": "install the proposed iPad runtime, mount iCloud external scopes, start Docs Viewer, and record the real-device proof gate",
  "ui_status": "proposed",
  "content_html": "<h1>iPad Docs Viewer Installation And Proof</h1>\n<h2>Status</h2>\n<p>This is the proposed first-install and proof procedure. It has been reconciled with the current repository and app documentation but has not yet passed on the target iPad. Record exact deviations and evidence from the real device; do not silently rewrite a failed step into a claimed success.</p>\n<p>The gate uses a fresh shallow clone and disposable proof content. Do not run it against the only copy of uncommitted work or while the same external proof files are being edited on another device.</p>\n<h2>1. Install The Apps</h2>\n<p>Install <a href=\"https://ish.app/\">iSH</a>. Safari and Files are built into iPadOS. Working Copy is optional after the core proof, and a Files-aware editor such as Textastic is optional because Docs Viewer already contains its own Source editor.</p>\n<p>In Files, locate the exact iCloud Drive folder that corresponds to the Mac's <code>DOTLINEFORM_PROJECTS_BASE_DIR</code>. Confirm that it contains the expected <code>docs-viewer</code>, <code>data-sharing</code>, and <code>projects</code> children, then choose <strong>Keep Downloaded</strong> for the required tree and allow downloads to complete.</p>\n<h2>2. Confirm A Supported iSH Filesystem</h2>\n<p>In iSH, record the runtime baseline:</p>\n<pre><code class=\"language-sh\">uname -a\ncat /etc/alpine-release\ncat /etc/apk/repositories\n</code></pre>\n<p>The target is a currently supported Alpine x86 release with working <code>main</code> and <code>community</code> repositories and Python 3.12. Alpine 3.23 is the initial known candidate. If <code>apk update</code> reports end-of-life or missing repositories, install or upgrade to a current iSH Alpine filesystem through the supported iSH filesystem workflow before continuing; do not blindly redirect an old filesystem to a newer package branch.</p>\n<h2>3. Mount The External Projects Base</h2>\n<p>Create a stable mount point and invoke the iOS folder picker:</p>\n<pre><code class=\"language-sh\">mkdir -p /mnt/dlf-projects\nmount -t ios . /mnt/dlf-projects\n</code></pre>\n<p>Select the exact iCloud parent identified in Files. The source argument is intentionally <code>.</code>; iSH ignores it and uses the folder selected in the picker.</p>\n<p>Verify the selected boundary without changing it:</p>\n<pre><code class=\"language-sh\">test -d /mnt/dlf-projects/docs-viewer\ntest -d /mnt/dlf-projects/data-sharing\ntest -d /mnt/dlf-projects/projects\nfind /mnt/dlf-projects -maxdepth 1 -type d -print\n</code></pre>\n<p>Stop if one of the required children is absent, if files still show download placeholders, or if the mount resolves to a child inside the projects base. Remounting uses the same command after <code>umount /mnt/dlf-projects</code> when necessary.</p>\n<h2>4. Install The Runtime Dependencies</h2>\n<p>Install the shell, Git, Python, environment tooling, and Alpine builds of the native Python packages:</p>\n<pre><code class=\"language-sh\">apk update\napk add bash git ca-certificates python3 py3-pip py3-virtualenv py3-lxml py3-beautifulsoup4 py3-markdown-it-py py3-pillow\n</code></pre>\n<p>Create a virtual environment that can use those system packages, then install the remaining checked runtime dependencies:</p>\n<pre><code class=\"language-sh\">mkdir -p /root/venvs\nvirtualenv --system-site-packages /root/venvs/dotlineform\n. /root/venvs/dotlineform/bin/activate\npython3 -m pip install mammoth==1.12.0 bleach==6.3.0 openpyxl==3.1.5\npython3 -m pip check\n</code></pre>\n<p>Confirm that the imports needed by the initial working slice load:</p>\n<pre><code class=\"language-sh\">python3 -c &quot;import bleach, bs4, lxml, mammoth, markdown_it, openpyxl, PIL; print('Docs Viewer Python imports: ok')&quot;\n</code></pre>\n<p>Do not begin with <code>pip install -r requirements.txt</code>. The checked file pins desktop versions of native packages for the primary development environment, while iSH needs Alpine's x86 builds for lxml and Pillow. The proof receipt must record the actual Alpine and Python package versions. A later delivery may add an explicitly tested iPad dependency contract if the divergence proves stable and useful.</p>\n<h2>5. Clone Into iSH Storage</h2>\n<p>Keep the repository, <code>.git</code>, virtual environment, and caches in iSH's Linux filesystem rather than iCloud Drive:</p>\n<pre><code class=\"language-sh\">mkdir -p /root/src\ncd /root/src\ngit clone --depth 1 https://github.com/dotlineform/dotlineform-site.git\ncd dotlineform-site\ngit status --short\n</code></pre>\n<p>The first output from <code>git status --short</code> should be empty. Create a disposable proof branch before any managed write:</p>\n<pre><code class=\"language-sh\">git switch -c ipad-proof\n</code></pre>\n<p>A full-history clone can replace the shallow clone after the workflow is proven. Working Copy may be evaluated later as a GUI for the same checkout only if that sharing arrangement preserves service performance and Git/filesystem correctness; do not maintain two writable clones and assume they are one working tree.</p>\n<h2>6. Create The Device-Local Environment</h2>\n<p>Create <code>/root/src/dotlineform-site/.env.local</code> with these minimal non-secret values:</p>\n<pre><code class=\"language-sh\">DOTLINEFORM_PROJECTS_BASE_DIR=/mnt/dlf-projects\nDOCS_VIEWER_MANAGEMENT_ENABLED=1\nDOCS_VIEWER_REVIEW_ENABLED=1\n</code></pre>\n<p><code>.env.local</code> is already ignored and is loaded by the current repository runtime. Do not add credentials to it for the initial proof. Do not create <code>env.ipad</code>: the current service does not load that filename, and this guide should not imply a configuration contract that does not exist.</p>\n<p>Confirm the active path without printing unrelated environment values:</p>\n<pre><code class=\"language-sh\">test -f .env.local\ntest -d /mnt/dlf-projects/docs-viewer\ngit status --short\n</code></pre>\n<p>The Git status should remain empty because <code>.env.local</code> is ignored.</p>\n<h2>7. Start Docs Viewer</h2>\n<p>Activate the environment from the repository root and start the service in the foreground:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\ncd /root/src/dotlineform-site\npython3 docs-viewer/services/docs_viewer_service.py\n</code></pre>\n<p>Expected startup output includes the manage URL and enabled management, review, and generated-read capabilities. In Safari, open:</p>\n<pre><code class=\"language-text\">http://127.0.0.1:8776/health\nhttp://127.0.0.1:8776/docs/\n</code></pre>\n<p>The health response must report <code>ok: true</code>. Keep iSH active in a foreground or multitasking window while using Safari. If the server stops responding after iSH is backgrounded or the device is locked, return to iSH and restart it; record this lifecycle behavior in the receipt.</p>\n<h2>8. Optional Static Public Preview</h2>\n<p>After the manage gate passes, a second iSH window may serve the already tracked public site:</p>\n<pre><code class=\"language-sh\">. /root/venvs/dotlineform/bin/activate\ncd /root/src/dotlineform-site\npython3 site-tools/site_preview.py --host 127.0.0.1 --port 4000 --directory site\n</code></pre>\n<p>Open <code>http://127.0.0.1:4000/</code> in Safari. This proves only the tracked static preview. It does not run Jekyll, produce server-side Mermaid SVG, or authorize remote publication.</p>\n<h2>Proof Gate</h2>\n<p>Use a unique proof marker such as <code>IPAD-PROOF-YYYYMMDD-HHMM</code> in all disposable content so its source, payload, search entry, and synchronized copy can be identified exactly.</p>\n<h3>A. Runtime And Startup</h3>\n<ul>\n<li>[ ] Record iPad model, iPadOS version, iSH version/build, Alpine version, <code>uname -m</code>, Python version, installed dependency versions, Git commit, and whether the clone is shallow.</li>\n<li>[ ] Start the service without an import, bind, configuration, media-directory, or port error.</li>\n<li>[ ] Confirm <code>/health</code> reports management, generated reads, and review enabled.</li>\n<li>[ ] Load <code>/docs/</code> in Safari, refresh once, and confirm there are no blocking browser errors.</li>\n</ul>\n<h3>B. Read And Search</h3>\n<ul>\n<li>[ ] Open one repository-backed Studio document and one Processing document.</li>\n<li>[ ] Open one configured external scope and, where applicable, one external sub-scope from the iCloud mount.</li>\n<li>[ ] Search for a known unique term in a repository-backed document and a different term in an external document; open the exact results.</li>\n<li>[ ] Confirm recent activity, index hierarchy, document body, metadata, and ordinary local media load for the chosen records.</li>\n</ul>\n<h3>C. Repository-Backed Managed Write</h3>\n<ul>\n<li>[ ] On the disposable proof branch, create one clearly named Studio proof document through Manage, save the unique marker, and confirm the post-save document and search rebuilds succeed.</li>\n<li>[ ] Reload the document and find the marker through search.</li>\n<li>[ ] Use <code>git status --short</code> and <code>git diff --check</code> in iSH to confirm that only the expected source and generated products changed and that no <code>.env.local</code>, temporary, cache, or iCloud file entered Git.</li>\n<li>[ ] Delete the disposable document through its supported UI only after its evidence is captured; record any remaining generated diff rather than forcing the tree clean.</li>\n</ul>\n<h3>D. iCloud-Backed Managed Write And Synchronization</h3>\n<ul>\n<li>[ ] Create one clearly named document in a disposable or explicitly safe external collection, save the unique marker, and confirm its document and search rebuilds succeed.</li>\n<li>[ ] Stop and restart the service, then reopen the external document and its search result.</li>\n<li>[ ] In Files, confirm the exact source and generated products exist below the selected iCloud tree without conflict copies, leaked temporary files, or partial siblings.</li>\n<li>[ ] On the Mac, wait for iCloud synchronization and confirm the same marker and expected generated record arrive at the exact external scope path.</li>\n<li>[ ] Delete the proof document through Manage, allow synchronization to complete, and confirm the source, generated record, and search entry are removed on both devices without resurrection.</li>\n</ul>\n<h3>E. Import, Review, And Media</h3>\n<ul>\n<li>[ ] Import one disposable Markdown file and one HTML file; confirm their rendered bodies and saved sources.</li>\n<li>[ ] Import one small DOCX containing text and one ordinary embedded image; confirm Mammoth conversion, saved source, and the expected image result.</li>\n<li>[ ] Run one disposable document-package or Docs Review round trip using the mounted <code>data-sharing</code> workspace and confirm that exact package identity and routing are preserved.</li>\n<li>[ ] Exercise one JPEG or PNG package-media path and confirm Pillow can read and write the required output.</li>\n<li>[ ] Open a document containing inline Mermaid and confirm Manage renders it in Safari without invoking the server-side <code>mmdc</code> pipeline.</li>\n</ul>\n<h3>F. Platform Boundary And Session Recovery</h3>\n<ul>\n<li>[ ] Confirm local-folder target authoring or validation remains bounded, and record that Finder-style activation or reveal is unavailable rather than treating the button as working.</li>\n<li>[ ] Background iSH briefly, use Safari in multitasking, lock and unlock the device once, and record when the loopback service is suspended.</li>\n<li>[ ] After any suspension, restart the service and confirm the repository-backed and external proof documents remain consistent.</li>\n<li>[ ] Record static-site preview separately if tested; do not record Jekyll, server-side Mermaid production, HEIF/video tooling, full desktop checks, R2 publication, or Working Copy integration as passed unless each receives its own explicit evidence.</li>\n</ul>\n<h2>Pass Rule</h2>\n<p>The feature passes its initial feasibility gate only when all required A-E checks succeed and F shows restartable session behavior without data loss. The repository-backed and external managed-write checks are both mandatory. A route that merely loads, an iCloud directory that can only be read, or a save that cannot be confirmed after restart and Mac synchronization is not a pass.</p>\n<p>Known exclusions in the concept do not fail the gate when they remain contained and accurately presented. Unexpected source loss, conflict copies, partial or resurrected generated files, inability to run child rebuilds, unreliable loopback access while iSH is active, or inability to install/load required native Python packages blocks the working-slice claim.</p>\n<h2>Proof Receipt</h2>\n<p>Append one dated receipt to this document after the real-device run:</p>\n<pre><code class=\"language-text\">date and tester:\niPad model and iPadOS:\niSH version/build:\nAlpine, architecture, and Python:\ndependency versions:\nrepository commit and branch:\nexternal mount shape, with private absolute names redacted:\nA runtime/startup:\nB read/search:\nC repo-backed write:\nD iCloud-backed write/sync:\nE import/review/media:\nF lifecycle and exclusions:\nunexpected failures or workarounds:\noverall result: pass | blocked\ncandidate repository deliverables:\n</code></pre>\n<p>Do not add a delivery merely to record setup friction. Add one under <a href=\"/docs/?scope=studio&amp;doc=d-20260808-205414-91040a\">iPad Docs Viewer</a> only when the receipt supports a bounded repository change with an observable completion rule, such as a tested dependency profile, explicit launcher/profile contract, platform-safe action capability, or File Provider robustness fix.</p>\n"
}
