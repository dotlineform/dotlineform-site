{
  "doc_id": "d-20260804-182355-7ac901",
  "title": "SSC-1.0 — Contract And Cutover Sanity Note",
  "added_date": "2026-08-04 18:23:55",
  "last_updated": "2026-08-04 18:23:55",
  "viewer_url": "/docs/?scope=studio&doc=d-20260804-182355-7ac901",
  "parent_id": "d-20260804-172012-5d91bf",
  "summary": "accept the bounded sub-scope customisation aspect model, migration boundary, access isolation and implementation stop gate",
  "ui_status": "done",
  "content_html": "<h1>SSC-1.0 — Contract And Cutover Sanity Note</h1>\n<h2>Decision</h2>\n<p>The unified contract fits the current system without a second registry, generic schema language, public management projection, compatibility alias, or user-visible behaviour change. SSC-1.1 may perform the coordinated terminology cutover after explicit implementation approval. This note changes no runtime contract.</p>\n<p>The two current Projects-only collection-name checks in the Python definition and Manage browser factory are migration risks, not accepted availability owners. The configured exact sub-scope selects the registered definition; the resolved exact target then flows through its aspects. SSC-1 must retire those redundant name checks rather than preserve them behind the renamed contract.</p>\n<h2>Accepted Configuration</h2>\n<p>The configured sub-scope retains one optional extension property and no loose field or capability declarations:</p>\n<pre><code class=\"language-yaml\">sub_scope_customisation:\n  id: &lt;registered snake-case id&gt;\n  settings: &lt;definition-validated object&gt;\n</code></pre>\n<p>An absent property selects the canonical default report host, manifests, navigation, list/detail behavior, and Manage actions. The registry validates the ID, settings, and any collection prerequisites. Raw settings remain server-side.</p>\n<h2>Accepted Definition Aspects</h2>\n<p>The registered definition keeps identity, settings normalization, and any capability-based collection prerequisites at its root. Existing and reserved behavior is expressed through the following optional code-owned aspects:</p>\n<table>\n<thead>\n<tr>\n<th>aspect</th>\n<th>owner and boundary</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>manifest_projection</code></td>\n<td>projects strict root and row data for each enabled access without redefining membership</td>\n</tr>\n<tr>\n<td><code>document_groups</code></td>\n<td>owns the ordered document-group values used by current validation and metadata behavior</td>\n</tr>\n<tr>\n<td><code>source_validation</code></td>\n<td>validates custom source front matter during build, package, and mutation flows</td>\n</tr>\n<tr>\n<td><code>metadata</code></td>\n<td>pairs the exact-target read projection with its optional revision-safe update normalizer</td>\n</tr>\n<tr>\n<td><code>import_front_matter</code></td>\n<td>normalizes only custom create-time front matter owned by the definition</td>\n</tr>\n<tr>\n<td><code>browser_composition</code></td>\n<td>declares the enabled access set used for descriptor projection and exact Python/JavaScript registry agreement</td>\n</tr>\n<tr>\n<td><code>assignable_field_groups</code></td>\n<td>reserves code-owned focused-workflow declarations; current registrations contribute none until SSP-0 or TDL-0 owns them</td>\n</tr>\n<tr>\n<td><code>transfer</code></td>\n<td>reserves a code-owned field-ownership and compatibility contract; current registrations contribute none until the Copy delivery owns it</td>\n</tr>\n</tbody>\n</table>\n<p>The access set has one registered owner rather than separate public and Manage availability booleans. Manifest projection and browser descriptor emission consume that same set. Assignable field groups and transfer contracts are typed registration slots, not config arrays or data-driven form schemas. Later field-group values must reuse the owning definition's normalized values instead of duplicating choices.</p>\n<p>The generated manifest and metadata <code>customisation</code> envelopes remain contribution-data namespaces. The no-alias cutover renames the selected contract descriptor and report-named implementation vocabulary to <code>sub_scope_customisation</code> or <code>subScopeCustomisation</code>; it does not rename every contribution-data field merely because it contains custom data.</p>\n<h2>Migration Surface</h2>\n<p>The coordinated cutover covers:</p>\n<ul>\n<li>configured sub-scope records, normalized config types, strict errors, and the maintained group-seeding utility;</li>\n<li>the Python registry module, definition/config types, helper names, builders, source validation, metadata mutation/read, import, returned-package, and review-source callers;</li>\n<li>generated browser configuration and its checked-in default, while preserving default and custom manifest membership and data values;</li>\n<li>browser config normalization, shared report state, Manage report and modal composition, focused registries, factories, fixtures, and access-agreement checks;</li>\n<li>focused configuration, builder, metadata, import, package, runtime-boundary, migration, and module evidence; and</li>\n<li>active feature/delivery references, followed by the durable architecture rename and consumer transfer at SSC-1.3.</li>\n</ul>\n<p>The cutover is one source-and-consumer change. There is no dual key, fallback reader, compatibility property, or mixed generated payload.</p>\n<h2>Access Boundary</h2>\n<p><code>analysis_tags</code> and <code>dotlineform_projects</code> remain Manage-only. Their current public browser configuration omits the descriptor, public manifests retain the standard public-safe rows, the public registry has no loaders for them, and the public import graph has no edge to their Manage modules. Settings, metadata mutation authority, focused actions, and future assignable-field declarations remain local to Manage. Any later public contribution requires an explicit public access registration and matching public loader; it cannot arrive through a hidden control or runtime branch.</p>\n<h2>Risks And Stop Gate</h2>\n<p>The main risks are a partial vocabulary cutover, stale generated browser config, access declarations drifting from loader registries, custom metadata callers escaping the aspect owner, the current Projects name checks surviving, and future field values being declared twice. SSC-1.1 and SSC-1.2 must use focused exact-key, default, unknown-ID/settings, registry-agreement, exact-target, generated-payload, and public-isolation evidence to contain those risks.</p>\n<p>Stop and return for review if implementation requires a compatibility alias, parallel field registry, duplicated action flag, literal collection-name availability check, generic form engine, public management data or controls, changed report membership, or other user-visible behavior. SSC-1.1 has not started.</p>\n"
}
