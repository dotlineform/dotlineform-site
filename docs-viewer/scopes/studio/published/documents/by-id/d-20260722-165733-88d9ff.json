{
  "doc_id": "d-20260722-165733-88d9ff",
  "title": "Missing-Summary Package Preparation Delivery",
  "added_date": "2026-07-22 16:57:33",
  "last_updated": "2026-07-22 19:22:44",
  "viewer_url": "/docs/?scope=studio&doc=d-20260722-165733-88d9ff",
  "parent_id": "d-20260720-230623-c7e921",
  "ui_status": "complete",
  "content_html": "<h1>Missing-Summary Package Preparation Delivery</h1>\n<h2>Status</h2>\n<p><strong>Complete. MSP-P0 through MSP-P5 are shipped and verified.</strong></p>\n<p>This delivery captures four related changes:</p>\n<ol>\n<li>a missing-summary filter and live target count in the <strong>Prepare package</strong> modal;</li>\n<li>an <strong>Include non-viewable documents</strong> package choice, on by default;</li>\n<li>a generic <strong>Select all</strong> transition over the complete manage index;</li>\n<li>removal of the manage-index visibility filter and the separate <strong>Show</strong> viewability Action, followed by an audit and retirement of their unused dedicated workflow.</li>\n</ol>\n<p>The index selection owner assembles checked ids; the Package Prepare workflow interprets package-specific filters; <strong>Edit metadata</strong> owns changes to a document's viewability. Public projection continues to enforce viewability independently. The final review confirmed the shipped workflow and documentation; no checkpoint remains in this delivery.</p>\n<h2>Outcome</h2>\n<p>A user can always see and select every source document in the manage index, including non-viewable documents. They can optionally add descendants, limit the <strong>Document content</strong> package to documents whose current source metadata has no summary, and choose whether the package includes non-viewable documents. Before confirmation, the modal shows how many documents the current choices are expected to prepare.</p>\n<p>For the scope-wide case, the intended path is:</p>\n<ol>\n<li>choose <strong>Select</strong> in the manage index;</li>\n<li>choose <strong>Select all</strong>;</li>\n<li>choose <strong>Prepare package</strong> from <strong>Actions</strong>;</li>\n<li>use the <strong>Document content</strong> profile;</li>\n<li>enable <strong>Only documents missing summaries</strong>;</li>\n<li>leave <strong>Include non-viewable documents</strong> on, or turn it off to exclude them;</li>\n<li>confirm the displayed total and prepare the package.</li>\n</ol>\n<p>The raw checked count is only the index-selection starting point. The modal derives one final effective target after descendant expansion, both package filters, and any configured profile limit. That effective target is sent as explicit document ids; <strong>Select all</strong> does not switch Package Prepare to the service's separate <code>select_all</code> request mode.</p>\n<h2>Starting Point</h2>\n<ul>\n<li>The package engine already accepts <code>missing_summary_only</code> and skips a requested document with reason <code>has_summary</code> when its normalized source <code>summary</code> is non-empty.</li>\n<li>Both current profiles declare <code>supports_missing_summary_only: false</code>; the browser-safe profile projection does not expose the support or default flags.</li>\n<li>The direct Prepare service currently passes <code>missing_summary_only=None</code>, so a browser request cannot activate the engine filter.</li>\n<li>The package-document feed already includes source summary text, parent ids, and selection eligibility. The browser can therefore calculate an immediate count from the same loaded snapshot used for descendant expansion.</li>\n<li>None of the 512 current source documents across Studio, Library, Analysis and its sub-scope, Moments, Processing, and external Notes uses document-level <code>published</code> front matter. That older pipeline-inclusion field was removed from the Docs source model in May 2026, then unintentionally returned inside the July package source projection and its synthetic tests.</li>\n<li>The content profile declares <code>include_non_viewable: true</code>, but the package-document feed currently marks non-viewable documents as unselectable. The service treats the profile value as fixed rather than accepting a per-request choice.</li>\n<li>The index selection owner supports enter, toggle, Shift-range, clear, exit, and reload reconciliation, but no bulk membership transition.</li>\n<li>Manage mode currently defaults to showing non-viewable documents but exposes a <strong>Show non-viewable</strong> scope control that can remove them from the index and selection population.</li>\n<li>The separate <strong>Show</strong> Action is not a simple metadata shortcut: it can include non-viewable ancestors and optionally descendants through the bulk viewability endpoint. <strong>Edit metadata</strong> changes only the active document's own <code>viewable</code> value.</li>\n<li>Package Prepare sends explicit ids with <code>select_all: false</code> and keeps checkbox selection unchanged after cancel, success, or failure.</li>\n<li>The current export engine counts documents removed by selection filters as <code>skipped</code> outside <code>selected</code>. A large filtered-out population can therefore produce a <code>skipped</code> count larger than <code>selected</code>, even though those documents were deliberately excluded before preparation. This delivery replaces that reporting model for the direct Prepare workflow.</li>\n<li>Both profiles currently set <code>limits.max_documents</code> and <code>limits.max_total_chars</code> to <code>null</code>. The content profile separately truncates each document's content at 50,000 characters.</li>\n</ul>\n<p>Primary code owners are:</p>\n<ul>\n<li><code>docs-viewer/config/document-packages/profiles.json</code></li>\n<li><code>docs-viewer/services/docs_document_packages/{package,service,export_selection}.py</code></li>\n<li><code>docs-viewer/runtime/js/packages/document-package-prepare-{model,workflow}.js</code></li>\n<li><code>docs-viewer/runtime/js/management/docs-viewer-index-selection.js</code></li>\n<li><code>docs-viewer/runtime/js/management/docs-viewer-management-{actions,action-workflow,client,control-renderers,hosted-views,interactions}.js</code></li>\n<li><code>site/docs-viewer/runtime/js/shared/docs-viewer-document-index-state.js</code></li>\n<li><code>docs-viewer/services/docs_management_{routes,mutation_service,mutations}.py</code></li>\n</ul>\n<h2>Shipped Modal Contract</h2>\n<ul>\n<li>Show <strong>Only documents missing summaries</strong> immediately below <strong>Include descendants</strong>.</li>\n<li>Show it only when the selected profile explicitly supports the filter. The <strong>Document content</strong> profile supports it; the <strong>Document tree</strong> profile does not.</li>\n<li>Leave it unchecked by default so existing Package Prepare behavior does not change silently.</li>\n<li>Define a missing summary as empty or whitespace-only source <code>summary</code> metadata. Body content, headings, generated proposal fields, and published search text do not affect this decision.</li>\n<li>Show <strong>Include non-viewable documents</strong> below the missing-summary choice for the <strong>Document content</strong> profile and leave it checked by default. The tree profile keeps non-viewable documents included without offering this filter.</li>\n<li>Calculate in this order: checked-id eligibility, optional descendant expansion, non-viewable filtering when requested, missing-summary filtering, then any configured maximum document count.</li>\n<li>Display <code>Total documents to be prepared: N</code> after the choices and recalculate it whenever the profile or any checkbox changes.</li>\n<li>Describe deliberate filter exclusions before submission, for example <code>98 documents excluded because they already have summaries.</code> These exclusions do not become result <code>skipped</code> documents because they are outside the effective target.</li>\n<li>When non-viewable documents are included, disclose their count, for example <code>Includes 4 non-viewable documents.</code> When they are excluded, report how many were excluded before submission.</li>\n<li>Remove the initial <code>Prepare N checked documents</code> message. The raw checked count is not a package result and becomes irrelevant once modal choices alter the target.</li>\n<li>If the total is zero, disable <strong>Prepare package</strong> and explain which active filters excluded every targeted document.</li>\n<li>Keep per-profile option state. Switching profiles must not leak an unsupported filter choice into the tree profile.</li>\n<li>Send only the final effective target ids, plus explicit <code>missing_summary_only</code> and <code>include_non_viewable</code> choices so the service can revalidate those ids against current source. The service never broadens the snapshot by adding a document the modal excluded.</li>\n<li>Treat the live total as the result for the loaded source snapshot. The service remains authoritative if document summaries or hierarchy change before submission.</li>\n</ul>\n<h2>Result Contract</h2>\n<p>The existing result order remains:</p>\n<ol>\n<li><code>selected</code></li>\n<li><code>exported</code></li>\n<li><code>failed</code></li>\n<li><code>skipped</code></li>\n<li><code>truncated</code></li>\n</ol>\n<p><code>selected</code> is the effective post-modal target and must equal the displayed <strong>Total documents to be prepared</strong> for the submitted snapshot. It is not the number of manually checked index rows.</p>\n<p>The primary result invariant is:</p>\n<p><code>selected = exported + failed + skipped</code></p>\n<p><code>truncated</code> remains a subset of <code>exported</code>, so <code>truncated &lt;= exported</code>. Each failed or skipped count is therefore bounded by <code>selected</code> and can never be inflated by documents deliberately excluded in the modal.</p>\n<p>For example, 100 checked documents may expand to 120 with descendants and reduce to 10 after modal filters. The modal reports <strong>Total documents to be prepared: 10</strong> and sends those 10 ids. The expected result is <code>selected: 10</code>, <code>exported: 10</code>, <code>failed: 0</code>, and <code>skipped: 0</code>; the 110 deliberate exclusions remain pre-submit information only.</p>\n<p>The service revalidates the ten submitted targets without broadening them. If one no longer exists or no longer meets an active filter, a possible result is <code>selected: 10</code>, <code>exported: 9</code>, and <code>skipped: 1</code>. Record-building errors contribute to <code>failed</code> instead. Trusted written-package metadata and returned-package validation must continue to describe the exact prepared record set rather than the earlier raw checkbox population.</p>\n<h2>Shipped Select-All Contract</h2>\n<ul>\n<li>Add <strong>Select all</strong> to the active manage-index selection controls alongside the selected count, <strong>Clear</strong>, and <strong>Done</strong>.</li>\n<li>Use one pure bulk transition owned by <code>docs-viewer-index-selection.js</code>; do not reconstruct selection by reading checkbox DOM state.</li>\n<li>Select every document in the canonical manage-index population, including non-viewable documents and rows inside collapsed branches.</li>\n<li>Replace the selected-id set with the eligible-id set and clear the Shift-range anchor. Disable <strong>Select all</strong> when every eligible document is already checked.</li>\n<li>Keep selection browser-only and preserve the existing lifecycle: same-scope reload prunes ineligible ids, while leaving the owning scope, management context, or <code>index-tree</code> clears selection.</li>\n<li>A document's public viewability is not selection eligibility. Changing <code>viewable</code> does not prune that document from the checked set.</li>\n<li>Do not make the index aware of summaries or Package Prepare profiles. The generic control selects eligible documents; the modal owns the missing-summary filter.</li>\n<li>Do not change Action targeting, drag/drop, active-document highlighting, link navigation, or other singular controls.</li>\n</ul>\n<h2>Manage Viewability Contract</h2>\n<ul>\n<li>Manage mode always includes every source document in the index. Remove the <strong>Show non-viewable</strong> scope control, its browser state, visibility filtering, event handling, and selection reconciliation branch.</li>\n<li>Retain the non-viewable icon as the index indication for a document whose own <code>viewable</code> value is false.</li>\n<li>Public Docs Viewer continues to exclude a non-viewable document and the descendants gated by a non-viewable ancestor.</li>\n<li>Remove the separate <strong>Show</strong> Action and control. <strong>Edit metadata</strong> becomes the only UI for changing the active document's own <strong>non-viewable</strong> checkbox.</li>\n<li>Do not migrate automatic ancestor or descendant changes into <strong>Edit metadata</strong>. Its save remains one-document metadata editing.</li>\n<li>Explain in the metadata modal or its durable documentation that a non-viewable ancestor also prevents a document appearing publicly. Clearing one document's own checkbox does not override its ancestors.</li>\n<li>Retire the current ancestor confirmation and optional descendant choice with the <strong>Show</strong> Action rather than retaining an invisible parallel workflow.</li>\n<li>Audit the dedicated single and bulk viewability clients, endpoints, planners, routes, capability/config entries, tests, and documentation. Remove each item with no remaining real consumer, without aliases. If the audit finds an active consumer, document it and stop for a decision before retaining or changing that contract.</li>\n<li>Preserve <code>viewable</code> handling in the ordinary metadata mutation and in public build/runtime projection. This cleanup removes duplicate controls and dead transport, not the canonical metadata field or its public gate.</li>\n</ul>\n<p><code>viewable: false</code> is a public-display gate, not an external-sharing prohibition. The package checkbox is an explicit composition choice; a future requirement that some documents must never leave the local workspace would require a separate policy rather than overloading <code>viewable</code>.</p>\n<h2>Confirmed Legacy Cleanup</h2>\n<p>Document-level <code>published</code> is not part of the current Docs source contract. Remove it from <code>DocumentPackageSourceRecord</code>, source-context loading, export-document projection, the browser package feed, and synthetic fixtures/tests. Package selectability must not depend on it, and no source migration is required because the current corpus contains no instances.</p>\n<p>This retirement is narrowly about document front matter and package records. Scope configuration, generated-document locations, public projections, publication commands, and other artifact paths that use the word <code>published</code> are separate active concepts and remain unchanged.</p>\n<h2>Confirmed Package-Size Decision</h2>\n<p>Do not add or enforce a document-count or total-character package limit. The visible effective total and explicit confirmation are sufficient for the current workflow. Retain the existing 50,000-character per-document content truncation, and reconsider broader limits only if an observed scale or external-tool constraint creates a real need.</p>\n<h2>Delivery Checkpoints</h2>\n<h3>MSP-P0 — Complete The Product Contract</h3>\n<ul>\n<li>[x] Confirm the missing-summary filter is <strong>Document content</strong> only and off by default.</li>\n<li>[x] Include all source documents in the manage index and make public viewability independent from checkbox eligibility.</li>\n<li>[x] Keep <strong>Select all</strong> in this delivery and include non-viewable and collapsed documents from the canonical manage-index model.</li>\n<li>[x] Add <strong>Include non-viewable documents</strong> to the content profile, on by default, while keeping tree packages complete.</li>\n<li>[x] Remove <strong>Show non-viewable</strong> and make the manage index permanently complete.</li>\n<li>[x] Remove <strong>Show</strong> and make <strong>Edit metadata</strong> the only UI owner for changing one document's own viewability.</li>\n<li>[x] Include an audit and retirement of the dedicated viewability workflow and unused endpoints.</li>\n<li>[x] Make the post-modal effective target the only preparation total and block confirmation when it is empty.</li>\n<li>[x] Keep deliberate modal exclusions out of result <code>skipped</code>; require <code>selected = exported + failed + skipped</code> and <code>truncated &lt;= exported</code>.</li>\n<li>[x] Remove the legacy document-level <code>published</code> field from package records and eligibility; no source migration is needed.</li>\n<li>[x] Add no document-count or total-character package limit; retain the existing per-document content truncation.</li>\n</ul>\n<p>MSP-P0 is complete. Stop for explicit approval before changing runtime or service code.</p>\n<h3>MSP-P1 — Carry The Filter Through The Package Contract</h3>\n<ul>\n<li>[x] Enable missing-summary support only for the content profile and retain the current default-off behavior.</li>\n<li>[x] Expose browser-safe filter support, defaults, and any count-relevant limit fields through the profile projection.</li>\n<li>[x] Accept and type-check <code>missing_summary_only</code> and <code>include_non_viewable</code> in the direct Prepare service instead of forcing profile-only values.</li>\n<li>[x] Make non-viewable source documents selectable in the package feed and remove document-level <code>published</code> from package source records, projections, and synthetic fixtures.</li>\n<li>[x] Submit only the final effective target ids and revalidate those ids server-side without adding documents excluded from the modal snapshot.</li>\n<li>[x] Make response counts obey <code>selected = exported + failed + skipped</code> while preserving the exact prepared-record set in trusted metadata and whole-return validation.</li>\n<li>[x] Add focused profile, service, request, count-invariant, unsupported-choice, zero-result, and stale-source tests.</li>\n</ul>\n<p>MSP-P1 is complete. The focused package service, export, source-context, and loopback HTTP suite passes 54 tests. Stop for explicit approval before beginning MSP-P2.</p>\n<h3>MSP-P2 — Add The Modal Choices And Live Count</h3>\n<ul>\n<li>[x] Add the two conditional filters and total beneath <strong>Include descendants</strong>.</li>\n<li>[x] Recalculate after profile, descendant, missing-summary, or non-viewable changes using one pure selection-projection helper.</li>\n<li>[x] Remove the raw checked-count note and show the effective total as the only preparation count.</li>\n<li>[x] Show included/excluded non-viewable counts, deliberate summary exclusions, and the zero-target reason without reporting modal exclusions as result skips.</li>\n<li>[x] Keep profile switching, package-context editing, cancellation, selection preservation, and result ordering unchanged.</li>\n<li>[x] Prove count and request composition as pure/browser-module behavior; leave copy and tactile layout to user review.</li>\n</ul>\n<p>MSP-P2 is complete. The focused package, source-context, service, loopback HTTP, and static-asset suite passes 61 tests; the Prepare browser-module smoke also passes with no page errors. Tactile copy and layout review remains in MSP-P5. Stop for explicit approval before beginning MSP-P3.</p>\n<h3>MSP-P3 — Simplify The Manage Index And Add Select All</h3>\n<ul>\n<li>[x] Remove <strong>Show non-viewable</strong>, its state and event path, and manage-mode document visibility filtering while preserving public projection.</li>\n<li>[x] Add one bulk-selection transition and owner method using the complete manage-index population.</li>\n<li>[x] Project <strong>Select all</strong> through the existing index-view control owner.</li>\n<li>[x] Include non-viewable and collapsed rows, clear the range anchor, and retain existing scope/reload lifecycle reconciliation.</li>\n<li>[x] Preserve selected ids when their documents change viewability.</li>\n<li>[x] Prove that selection remains explicit browser state and no Action, route, drag, or displayed-document target changes implicitly.</li>\n</ul>\n<p>MSP-P3 is complete. The pure module contracts cover complete-set replacement, collapsed and non-viewable ids, anchor clearing, viewability-independent membership, lifecycle reconciliation, Action-target isolation, active-document identity, and drag preservation. The maintained <code>docs-viewer-smoke</code> profile passes all 13 checks, including the real manage route, selection lifecycle, Prepare workflow, tree-move projection, service boundary, and public read-only projection; evidence is in <code>var/admin/test-runs/missing-summary-package-msp-p3-final/summary.md</code>. Stop for explicit approval before beginning MSP-P4.</p>\n<h3>MSP-P4 — Consolidate Viewability Ownership And Retire Dead Paths</h3>\n<ul>\n<li>[x] Remove the <strong>Show</strong> Action, control, ancestor/descendant choice workflow, client wiring, and dedicated action-target references.</li>\n<li>[x] Keep the metadata checkbox as the only UI mutation owner and explain the non-viewable-ancestor effect.</li>\n<li>[x] Audit the single and bulk viewability endpoints, planners, service/config entries, tests, and docs for real consumers.</li>\n<li>[x] Remove every unowned dedicated viewability path without an alias; stop for a decision if an active external consumer is found.</li>\n<li>[x] Preserve ordinary metadata writes, the non-viewable icon, and public document/subtree gating.</li>\n</ul>\n<p>MSP-P4 is complete. The audit found no independent consumer of the dedicated single or bulk viewability APIs: the bulk client served only the retired <strong>Show</strong> workflow, while the single endpoint remained only in tests and documentation. Both endpoints, their routes, planners, config entries, tests, and docs are removed without aliases. <strong>Edit metadata</strong> remains the one-document mutation path and now explains the ancestor effect. The focused Python suite passes 36 tests; focused module and real-route smokes pass; and the maintained <code>docs-viewer-smoke</code> profile passes all 13 checks, including live 404 proof for both retired endpoints and public read-only projection. Evidence is in <code>var/admin/test-runs/missing-summary-package-msp-p4-final/summary.md</code>. Stop for explicit approval before beginning MSP-P5.</p>\n<h3>MSP-P5 — Verify And Document</h3>\n<ul>\n<li>[x] Run the smallest focused service, pure module, request/response, syntax, and whitespace checks that prove the changed contracts.</li>\n<li>[x] Perform manual modal and index-control review for effective-count clarity, exclusion explanations, zero state, failure/skip reporting, non-viewable disclosure, keyboard focus, and narrow-screen fit.</li>\n<li>[x] Update <a href=\"/docs/?scope=studio&amp;doc=d-20260722-151224-7b61c4\">Package Prepare</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260503-141500-98fa03\">Package Prepare Profiles</a>, metadata/viewability ownership, endpoints, and index-selection documentation with only the shipped behavior.</li>\n<li>[x] Close this delivery only when the modal projection and final service counts agree for unchanged source state.</li>\n</ul>\n<p>MSP-P5 is complete. The focused package, source-context, management, route, and configuration suite passes 89 tests with one expected skip; the focused Prepare and index module/route smokes pass; JSON, Python syntax, and repository whitespace checks pass. The maintained <code>docs-viewer-smoke</code> profile passes all 13 checks, with evidence in <code>var/admin/test-runs/missing-summary-package-msp-p5-final/summary.md</code>.</p>\n<p>Tactile review covered the normal layout and a 390 × 844 viewport, keyboard focus order, the disabled zero-target state, failure and ordered result counts, and included/excluded non-viewable disclosures. No horizontal overflow or console error remained. With 269 checked Studio documents, the missing-summary filter projected 174; the unchanged live service result reported <code>selected: 174</code>, <code>exported: 174</code>, <code>failed: 0</code>, <code>skipped: 0</code>, and <code>truncated: 0</code>. The modal and service counts therefore agree.</p>\n<h2>Explicitly Outside This Delivery</h2>\n<ul>\n<li>summary generation, automatic summary writes, or returned-package apply changes;</li>\n<li>summary-aware filtering or badges in the index itself;</li>\n<li>a second package document picker or private modal selection store;</li>\n<li>changing Package Prepare to service-level <code>select_all</code> mode;</li>\n<li>missing-summary filtering for the tree profile;</li>\n<li>selection persistence across scope, reload, or management exit;</li>\n<li>automatic ancestor or descendant viewability changes from <strong>Edit metadata</strong>;</li>\n<li>a new privacy, external-sharing permission, or data-classification field;</li>\n<li>a generic batch-action framework, preview service, rollback layer, or speculative package limits;</li>\n<li>changes to Export, Move, Delete, Returned Packages, or other selection consumers.</li>\n</ul>\n<h2>Completion Gates</h2>\n<ul>\n<li>the content profile alone exposes a default-off missing-summary choice;</li>\n<li>the content profile exposes a default-on non-viewable choice while tree packages remain complete;</li>\n<li>the modal removes the raw checked count and projects one effective target after eligibility, descendants, both filters, and configured limits;</li>\n<li>zero-result confirmation is blocked with a clear reason;</li>\n<li>the server receives only the effective explicit ids, revalidates them against current-source summary and viewability, and never broadens the submitted snapshot;</li>\n<li><code>selected</code> equals the submitted modal total, <code>selected = exported + failed + skipped</code>, and <code>truncated &lt;= exported</code>;</li>\n<li>included non-viewable documents and deliberate modal exclusions are explained before confirmation without inflating result <code>skipped</code>;</li>\n<li>no document-count or total-character package limit is introduced, while the existing per-document content truncation remains;</li>\n<li>manage mode always shows every source document, with the icon as its viewability indication and public filtering unchanged;</li>\n<li><strong>Select all</strong> uses the selection owner and the complete manage-index population without inspecting DOM visibility;</li>\n<li><strong>Edit metadata</strong> is the only viewability mutation UI, and the separate <strong>Show</strong> and <strong>Show non-viewable</strong> controls are absent;</li>\n<li>every dedicated viewability endpoint or workflow with no remaining consumer is removed without an alias;</li>\n<li>document-level <code>published</code> no longer exists in package records or eligibility, while active publication configuration and artifact paths remain unchanged;</li>\n<li>tree packages, selection lifecycle, Action targeting, and unrelated workflows retain their existing contracts.</li>\n</ul>\n<h2>Next Decision</h2>\n<p>This delivery is closed. Any future summary generation, package policy, or additional index-selection consumer requires its own feature decision and delivery boundary.</p>\n"
}
