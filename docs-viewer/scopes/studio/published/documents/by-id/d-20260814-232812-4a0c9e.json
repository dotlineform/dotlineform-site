{
  "doc_id": "d-20260814-232812-4a0c9e",
  "title": "Clone-Ready Docs Viewer",
  "added_date": "2026-08-14 23:28:12",
  "last_updated": "2026-08-14 23:28:12",
  "viewer_url": "/docs/?scope=studio&doc=d-20260814-232812-4a0c9e",
  "parent_id": "d-20260814-225646-8f7d2a",
  "summary": "define a coherent Docs Viewer composition that runs from a clean repository clone without private workspaces or adjacent applications",
  "ui_status": "proposed",
  "content_html": "<h1>Clone-Ready Docs Viewer</h1>\n<h2>Purpose</h2>\n<p>Make the repository checkout a real, documented Docs Viewer composition. After cloning the repository and installing its declared dependencies, someone can run one command and receive a coherent application using only data and services available in that clone.</p>\n<p>This is source-checkout portability, not a separately packaged product. It is one concrete motivation and proof scenario for <a href=\"/docs/?scope=studio&amp;doc=d-20260814-225646-8f7d2a\">Docs Viewer Core and Integration Architecture</a>.</p>\n<h2>Scenario</h2>\n<p>A person clones the repository because they want to inspect, develop, or understand Docs Viewer. They have no <code>.env.local</code>, private Projects workspace, Studio process, public-site preview, R2 credentials, document-package workspace, or Docs Review context. They follow short checked-in instructions and start Docs Viewer successfully.</p>\n<p>The resulting application exposes a deliberately selected clone-safe scope set and only the capabilities backed by that composition. It does not advertise private scopes, invent localhost destinations for absent applications, or fail while resolving integrations the person did not select.</p>\n<h2>Current Gap</h2>\n<p>The present checkout has no supported clean-clone run path. Its broad runner starts the public preview, Studio, Docs Viewer, and Review together; the direct launcher enters the same repository-integrated service; the tracked scope registry includes external-local scopes and an external managed-media workspace; optional package, Review, Studio, and full-registry dependencies are imported or resolved before a minimal application can start.</p>\n<p>The first missing external workspace can therefore prevent startup before the route exists. If startup dependencies are supplied, the Manage composition still receives Studio and public-preview origins whether those applications are running or not. The problem is not inadequate instructions alone; the selected application composition is not yet a clean-clone boundary.</p>\n<h2>Desired Experience</h2>\n<p>The checked-in instructions should produce this sequence:</p>\n<ol>\n<li>clone the repository;</li>\n<li>install the explicitly declared Docs Viewer runtime dependencies;</li>\n<li>run one project-local command; and</li>\n<li>open one reported local URL.</li>\n</ol>\n<p>No private path, credential, untracked config file, adjacent application, or public-site server is required. The command reports the selected composition, scopes, enabled integrations, and any required value that is genuinely missing. An unavailable optional integration does not affect startup.</p>\n<p>The clone-safe scope set must be deliberate. It may contain selected repository-owned documentation or a small example scope, but it must not be the incidental result of loading the complete dotlineform development registry. Every displayed scope, action, link adapter, report, and service has a resolvable owner in the selected composition.</p>\n<h2>Composition Model</h2>\n<p>Clone-ready and dotlineform development use the same runtime code, schemas, builders, services, and browser modules.</p>\n<pre><code class=\"language-text\">shared code + checked clone-safe composition\nshared code + dotlineform integrated composition + machine-local values\n</code></pre>\n<p>These are selections within one configuration contract, not two copied applications or complete configuration trees that can drift independently.</p>\n<ul>\n<li>immutable defaults and schemas remain shared;</li>\n<li>the composition chooses scope descriptors and optional integrations;</li>\n<li><code>.env.local</code> supplies only machine-specific values needed by the integrated composition;</li>\n<li>private scope registrations and data remain outside the clone-safe composition;</li>\n<li>scope-owned settings remain with their data; and</li>\n<li>capability output is derived from the components constructed at startup.</li>\n</ul>\n<h2>Document Workspace Boundary</h2>\n<p>The scenario must not depend on canonical document data living beneath the Docs Viewer application directory. A composition supplies exact source, generated-output, media, and scope-owned configuration locations.</p>\n<p>Repository documentation may live in an explicit content root or beside its owning project. Private or user workspaces remain external. Reproducible local output may use an application-local runtime area or a scope-owned output root. Public projections retain their public-site owner. <code>docs-viewer/</code> owns the application implementation rather than every workspace it can display.</p>\n<p>The exact relocation of current repository scopes is not part of this concept. The required architectural change is that Docs Viewer stops enforcing <code>docs-viewer/scopes/&lt;scope&gt;</code> as the meaning of a repository scope and resolves only the scope descriptors selected by the active composition.</p>\n<h2>Acceptance Boundary</h2>\n<p>The concept is realised when, in an environment without the dotlineform local configuration:</p>\n<ul>\n<li>one documented command starts Docs Viewer from a clean checkout;</li>\n<li>startup does not require <code>DOTLINEFORM_PROJECTS_BASE_DIR</code>, Studio, public preview, R2, packages, or Review;</li>\n<li>only clone-safe scopes and capabilities appear;</li>\n<li>absent integrations contribute no origins, routes, controls, loaders, or scope records;</li>\n<li>selected repository content and generated reads load successfully;</li>\n<li>the full dotlineform composition continues to use the same code with its explicit integrations and local values; and</li>\n<li>focused evidence proves composition selection and the first durable service/browser boundary without reproducing the complete application in tests.</li>\n</ul>\n<h2>Boundaries</h2>\n<ul>\n<li>No installer, bundled runtime, release archive, upgrade system, or general standalone product is introduced.</li>\n<li>No automatic discovery of arbitrary local folders or scopes is introduced.</li>\n<li>No private data or machine-specific paths are copied into the repository.</li>\n<li>No generic plug-in platform or universal composition language is required.</li>\n<li>No immediate bulk move of existing document workspaces is implied.</li>\n<li>No promise is made that the public site or Studio works unless the selected composition explicitly includes and configures them.</li>\n</ul>\n<h2>Status</h2>\n<p>Proposed concept. It motivates a future bounded delivery beneath the parent architecture feature; it does not authorize implementation or document relocation.</p>\n"
}
