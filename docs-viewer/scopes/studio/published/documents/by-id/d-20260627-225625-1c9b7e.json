{
  "doc_id": "d-20260627-225625-1c9b7e",
  "title": "Documents Returned Package Review",
  "added_date": "2026-06-27 22:56:25",
  "last_updated": "2026-07-20 19:09:50",
  "viewer_url": "/docs/?scope=studio&doc=d-20260627-225625-1c9b7e",
  "parent_id": "d-20260513-171600-a1eeb9",
  "content_html": "<h1>Documents Returned Package Review</h1>\n<h2>Boundary</h2>\n<p>This workflow takes a returned document package from <code>import-staging/</code>, proves its prepare-run identity through <a href=\"/docs/?scope=studio&amp;doc=d-20260627-113800-55dd63\">Export Metadata</a>, parses candidate rows, and produces evidence or a narrow confirmed mutation.</p>\n<p>It is not a general document importer and does not infer intent from filenames or fields.</p>\n<h2>Execution Path</h2>\n<pre><code class=\"language-text\">staged filename\n  -&gt; export_id + internal metadata\n  -&gt; profile/format/scope validation\n  -&gt; parse all returned rows\n  -&gt; compare with current Docs Viewer source\n  -&gt; normalized review rows + issues\n  -&gt; one of:\n       Markdown review evidence\n       immutable validated Docs Review projection\n       dry-run/confirmed summary or hierarchy apply\n</code></pre>\n<p>Returns may be sparse. Rich export context need not be echoed; an action reads only its candidate field. Omitted body content means no content change for narrow apply.</p>\n<h2>Review Outputs</h2>\n<h3>Record Review</h3>\n<p>Writes a focused Markdown snapshot under <code>import-preview/</code> for the complete validated package. It is evidence, never apply input.</p>\n<h3>Content / Docs Review Projection</h3>\n<p>For a supported <code>document-content</code> package, validates the complete staged file and atomically publishes an immutable folder under <code>import-preview/&lt;metadata-derived-id&gt;/</code> with:</p>\n<ul>\n<li><code>docs_review_validated_package_v1</code> manifest/provenance/diagnostics;</li>\n<li>one safe Markdown source projection per valid returned record;</li>\n<li>generated read-only Docs Review output.</li>\n</ul>\n<p>Allowed projected front matter is limited to title, parent, summary, and viewable. Package-local cycles/unsafe identities are fatal; a parent outside a compact returned selection is removed from the projection and recorded as a warning.</p>\n<p>The projection is rendered-derived text only. It is not registered as a normal Docs Viewer scope and does not mutate canonical source. Docs Import may later consume the associated staged package through an explicit collection plan.</p>\n<h2>Narrow Apply Actions</h2>\n<ul>\n<li><code>summary_apply</code> considers returned <code>summary</code> only.</li>\n<li><code>hierarchy_apply</code> considers returned <code>parent_id</code> only.</li>\n</ul>\n<p>Both reparse the complete staged input, validate current targets, plan without writing, require confirmation, then write through Docs Viewer source/rebuild helpers. Unknown/missing targets and invalid/self parents block the focused action.</p>\n<p>They do not apply body content, create documents, create missing parents, or accept unknown fields.</p>\n<h2>Atomic Return Contract</h2>\n<p>Prepare fixes the package document set. Returned rows are inspection-only: the direct document-package service rejects <code>record_indices</code>, and review, publication, apply, and Docs Import handoff operate on the complete package.</p>\n<p>Trusted export metadata must contain <code>selected_doc_ids</code>. Returned identities must equal that prepared set without regard to row order. Missing, extra, duplicate, malformed, or base-invalid rows block every downstream action; the external service must regenerate the package as a whole.</p>\n<p>Supported action fields may remain sparse. An omitted field or unchanged valid value is a no-op, while an invalid present value blocks that whole action. Existing action-specific current-source checks remain authoritative.</p>\n<h2>Assurance And Weak Spots</h2>\n<ul>\n<li>Metadata/profile identity is required before parsing; no row-shape fallback.</li>\n<li>Source writes have no Data Sharing backup artifact; use Git/external recovery.</li>\n<li>Apply-time parsing prevents an old preview object becoming write authority, but source freshness still needs action-specific comparison.</li>\n<li>Compact content can omit source-only constructs and media; replacing canonical content is a later explicit Docs Import decision.</li>\n<li>The lower-level full-source normalizer is not an enabled returned workflow.</li>\n</ul>\n<p>Exact skip/warning/error counts and field normalization belong to <code>docs_document_packages/apply_*</code>, <code>review.py</code>, <code>review_sources.py</code>, and their tests.</p>\n"
}
