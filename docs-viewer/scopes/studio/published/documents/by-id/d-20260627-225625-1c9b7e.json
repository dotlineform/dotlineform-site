{
  "doc_id": "d-20260627-225625-1c9b7e",
  "title": "Package Return Review",
  "added_date": "2026-06-27 22:56:25",
  "last_updated": "2026-07-22 22:40:23",
  "viewer_url": "/docs/?scope=studio&doc=d-20260627-225625-1c9b7e",
  "parent_id": "d-20260424-000000-50b63f",
  "content_html": "<h1>Package Return Review</h1>\n<h2>Boundary</h2>\n<p>This workflow takes a returned document package from <code>import-staging/</code>, proves its prepare-run identity through <a href=\"/docs/?scope=studio&amp;doc=d-20260627-113800-55dd63\">Export Metadata</a>, validates every returned row, and prepares one complete read-only Docs Review package.</p>\n<p>It does not mutate canonical documents. The normal Docs Viewer <strong>Import</strong> Action is the only returned-package apply path.</p>\n<h2>Execution Path</h2>\n<pre><code class=\"language-text\">staged filename\n  -&gt; export_id + internal metadata\n  -&gt; profile/format/scope validation\n  -&gt; parse all returned rows\n  -&gt; validate content and package-local hierarchy\n  -&gt; immutable validated Docs Review projection\n  -&gt; optional later whole-package Docs Viewer Import\n</code></pre>\n<p>The review list treats the staged package as stable for the modal interaction: the records found by its scan are the records prepared for review. It does not add changed-since-list tokens or snapshot reconciliation.</p>\n<h2>Docs Review Projection</h2>\n<p>For a supported <code>document-content</code> package, validates the complete staged file and atomically publishes an immutable folder under <code>import-preview/&lt;metadata-derived-id&gt;/</code> with:</p>\n<ul>\n<li><code>docs_review_validated_package_v1</code> manifest/provenance/diagnostics;</li>\n<li>one safe Markdown source projection per valid returned record;</li>\n<li>generated read-only Docs Review output.</li>\n</ul>\n<p>Allowed projected front matter is limited to title, parent, summary, and viewable. Package-local cycles/unsafe identities are fatal; a parent outside a compact returned selection is removed from the projection and recorded as a warning.</p>\n<p>The projection is rendered-derived text only. It is not registered as a normal Docs Viewer scope and does not mutate canonical source. Docs Review has no Import control or query-string handoff. To accept a returned package, return to Docs Viewer and choose the normal <strong>Import</strong> Action; it discovers the trusted staged package independently.</p>\n<h2>Atomic Return Contract</h2>\n<p>Prepare fixes the package document set. Returned records are atomic input: the direct document-package service rejects <code>record_indices</code>, and review, publication, and Docs Import operate on the complete package without row selection or skip.</p>\n<p>Trusted export metadata must contain <code>selected_doc_ids</code>. Returned identities must equal that prepared set without regard to row order. Missing, extra, duplicate, malformed, or base-invalid rows block every downstream action; the external service must regenerate the package as a whole.</p>\n<p>Docs Import plans each valid record as a create or overwrite and asks for one package-level confirmation. A malformed record, missing parent, or hierarchy cycle blocks the package; there is no record skip or partial-selection path. Returned content, summary, parent, and viewability values use the normalized import contract, while omitted fields preserve current canonical values on overwrite.</p>\n<h2>Assurance And Weak Spots</h2>\n<ul>\n<li>Metadata/profile identity is required before parsing; no row-shape fallback.</li>\n<li>Source writes have no document-package backup artifact; use Git/external recovery.</li>\n<li>Import confirmation revalidates the package identity and server-planned create/overwrite actions; the browser does not author target actions.</li>\n<li>Compact content can omit source-only constructs and media; inspect the generated review before confirming canonical import.</li>\n<li>The lower-level full-source normalizer is not an enabled returned workflow.</li>\n</ul>\n<p>Exact blockers and field normalization belong to <code>review_sources.py</code>, <code>docs_import_document_package_content.py</code>, <code>docs_import_collection_plan.py</code>, <code>docs_import_collection_apply.py</code>, and their tests.</p>\n"
}
