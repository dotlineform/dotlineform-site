{
  "doc_id": "d-20260804-160200-fa7bb7",
  "title": "Tag Document Linking",
  "added_date": "2026-08-04 16:02:00",
  "last_updated": "2026-08-11 19:42:26",
  "viewer_url": "/docs/?scope=studio&doc=d-20260804-160200-fa7bb7",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "own Tag document metadata authoring, ordered Tag associations, optional Registry primary overrides and Tag Semantic Tokens without coupling lifecycle",
  "ui_status": "in-progress",
  "content_html": "<h1>Tag Document Linking</h1>\n<h2>Purpose</h2>\n<p>Tag Document Linking owns the relationship between independently authored Tag documents, canonical Tag Registry identity, an optional primary override of the automatic document order, and Tag Semantic Token destinations. It is a Tag authoring and reference feature rather than part of Sub-Scope Publishing or the folder-centred Project State workflow.</p>\n<p>The feature restores the bounded Tag-document direction: documents declare exact canonical <code>tag_id</code>; a private derived association answers which documents describe each Tag in one stable exact-target order; the Tag Registry editor displays that association read-only and may store one optional <code>primary_document</code> override; and Tag Semantic Tokens use the valid selected primary or otherwise the first current association in that same deterministic order.</p>\n<p>It also owns the Tag-specific authoring surface needed to keep the app-level <strong>Edit metadata</strong> modal document-agnostic. Configured Tag-document fields appear read-only in Info and edit through a focused Tag-owned modal selected by the resolved sub-scope contribution rather than a literal Analysis Tags name check. <a href=\"/docs/?scope=studio&amp;doc=d-20260804-172012-5d91bf\">SSC-1</a> established that shared contribution as the single <code>sub_scope_customisation</code> workflow; this feature adds Tag-owned fields to it rather than defining another registry.</p>\n<h2>Current Implementation Baseline — 11 August 2026</h2>\n<p>SSC-1, TDL-0, and TDL-1 are shipped. Analysis Tags now registers <code>tag_fields(group, tag_id)</code>, passive exact-target Info projection, the focused revision-safe modal, normalized declaration states, a private deterministic <code>docs_tag_associations_v1</code> product, and the source-revalidated Studio Tag-delete guard. Report Blocks own the Analysis Tags host presentation, placed child documents remain intentionally absent from the parent index/recent/search products, and public eligibility is <code>publishable</code>; none of those boundaries alter the exact Tag-document ownership described here.</p>\n<p>The current <code>tag_registry_v5</code> snapshot has 247 canonical Tags, 246 stored document URLs, and one deliberately unlinked Tag (<code>aaa</code>). The Analysis public location projection resolves 245 of those URLs; <code>bird-nerve</code> retains one stale source target and must remain <strong>Unavailable document</strong> rather than being inferred or repaired. There are 249 managed Tag documents and 248 public Tag documents. No document has yet declared <code>tag_id</code>, so the shipped private association product is valid but empty; these live counts and legacy links are bounded TDL-2 migration evidence, not persistent schema invariants.</p>\n<p>The former Library public scope was retired after this plan was written. Its stale document-location allowlist entry made the Tag Registry's one shared lookup reject before any Analysis records could resolve, so every row appeared unavailable. The TDL-0.0 prerequisite repair makes the public producer, browser provider, Registry consumer, and URL validation Analysis-only; it does not change the feature model or start TDL-0.1.</p>\n<p>Private authoring associations and public linkability are separate products. Exact <code>{scope, sub_scope, doc_id}</code> identity remains usable for authoring and diagnosis even when no public-safe URL exists; public links are optional derived location descriptors and never replace exact identity.</p>\n<h2>Complete Feature Outcome</h2>\n<p>An <code>analysis/tags</code> document may declare one optional canonical <code>tag_id</code> alongside its existing group metadata. The declaration is explicit scalar identity and never inferred from title, group, body, route, selected row, or Registry state. Several documents may declare the same Tag. Missing, malformed, stale, unpublished, or unavailable relationships remain bounded document or association states and do not synchronize lifecycle.</p>\n<p>Document <code>group</code> moves out of <strong>Edit metadata</strong> into the registered Tag-document authoring modal before <code>tag_id</code> is added there. Info displays both fields read-only when supported. Document group remains independent from Tag Registry group after its create-time seed; neither editor synchronizes or repairs the other.</p>\n<p>A private <code>tag_id -&gt; documents[]</code> association projects exact Docs Viewer targets in stable ascending <code>{scope, sub_scope, doc_id}</code> order and current public-safe locations where available. Document front matter owns membership and the derived association owner owns this canonical order. The Tag Registry editor displays the complete current association list in that order but cannot add, remove, or reorder its documents; only editing or deleting document source changes membership. Tag Registry owns one optional exact <code>primary_document</code> using <code>{scope, sub_scope, doc_id}</code> whose runtime role is to override the automatic first-document destination.</p>\n<p>The only destructive integrity guard follows document-owned membership: Studio may delete a Tag only when current canonical document declarations produce no associations; otherwise <strong>Delete Tag</strong> lists the exact blocking documents and writes neither owner. Current, empty, or stale <code>primary_document</code> state does not affect that decision. Docs Viewer may delete a document or change or clear its <code>tag_id</code> without consulting primary selection, then rebuilds the association product without mutating Tag Registry. If that removes the selected primary, the stored identity becomes <strong>Unavailable document</strong> and Semantic Tokens use the automatic first-current-document rule. Neither owner cascades, silently repairs, or selects a replacement.</p>\n<p>The migration from <code>tag_registry_v5.doc_url[]</code> derives exact document declarations, reports unresolvable entries, validates the resulting association set, preserves the first exact target as <code>primary_document</code> only when it remains one of several current documents, and then retires the former Registry membership field. A Tag Semantic Token records canonical <code>tag_id</code>; at build time it uses an exact current selected primary when one exists, otherwise the first current association in canonical order. The chosen document must have a workable public-safe location; an unusable chosen destination does not cause a scan for another association.</p>\n<h2>Boundary</h2>\n<p>Tag Document Linking owns Tag document field authoring, ordered derived Tag associations, optional Registry primary overrides, the bounded migration, Tag Semantic Token authoring/resolution, and their reports and diagnosis. Docs Viewer remains canonical for document source and lifecycle; Tag Registry remains canonical for Tag identity, group, aliases, and the optional override.</p>\n<p>The feature does not participate in <code>dotlineform/projects</code>, Project State, folder/Work/Series authoring subjects, collection-aware editorial copy, public Work/Series reverse lookup, or catalogue-document coverage reporting. It does not create Tag documents automatically, synchronize Registry descriptions with documents, infer replacement primaries, cascade deletes, or add a public Tag application.</p>\n<h2>Deliveries</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260804-170901-b2e74c\">TDL-0 — Tag Document Metadata Authoring Refactor Delivery</a> follows SSC-1 and moves document group out of <strong>Edit metadata</strong> into configured read-only Info and a focused Tag-owned modal.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260804-160201-47af2d\">TDL-1 — Tag Document Association Delivery</a> establishes explicit <code>tag_id</code> declarations, the complete private reverse association, and the Studio Tag-delete association guard.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260802-002213-b7d4e2\">TDL-2 — Tag Primary Document And Semantic Token Delivery</a> replaces Registry membership editing with ordered read-only document pills, adds a replace-only optional scalar primary override with lifecycle-independent stale handling, and adds deterministic Tag Semantic Token resolution.</li>\n</ul>\n<h2>Delivery Discipline</h2>\n<p>The feature-level boundary is settled by separating Tag authoring and reference behavior from Sub-Scope Publishing. Each delivery <code>.0</code> step confirms current implementation fit, broad owners, blast radius, deliverables, risks, and stop conditions without reopening that separation or freezing an exact file plan. Focused evidence and an independent code/diff review remain required at every implementation checkpoint.</p>\n"
}
