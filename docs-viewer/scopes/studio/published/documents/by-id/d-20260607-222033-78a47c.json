{
  "doc_id": "d-20260607-222033-78a47c",
  "title": "Create And Import Endpoints",
  "added_date": "2026-06-07 22:20:33",
  "last_updated": "2026-08-07 08:52:47",
  "viewer_url": "/docs/?scope=studio&doc=d-20260607-222033-78a47c",
  "parent_id": "d-20260607-222033-647b52",
  "content_html": "<h1>Docs Viewer Create And Import Endpoints</h1>\n<h2><code>POST /docs/create</code></h2>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;title&quot;: &quot;New Doc&quot;,\n  &quot;parent_id&quot;: &quot;d-20260424-000000-50b63f&quot;\n}\n</code></pre>\n<p>Actions:</p>\n<ul>\n<li>validates <code>scope</code> against configured docs scopes</li>\n<li>defaults blank titles to <code>New Doc</code></li>\n<li>allocates an immutable <code>doc_id</code> and matching filename independently of the title</li>\n<li>validates <code>parent_id</code> inside the same scope when supplied</li>\n<li>writes a new Markdown source file under the configured scope root</li>\n<li>writes matching second-resolution <code>added_date</code> and <code>last_updated</code> values</li>\n<li>omits <code>viewable</code> for local-scope docs such as Studio, which makes them viewable by default</li>\n<li>writes <code>viewable: false</code> for public-scope docs such as Analysis, Library, and Moments</li>\n<li>rebuilds targeted generated docs payloads and targeted docs search for the new doc</li>\n<li>writes watcher-suppression markers for the new source file</li>\n</ul>\n<p>Returned data includes the created doc identity, source path, viewer URL, mutation flags, rebuild diagnostics, summary text, and <code>dry_run</code>.</p>\n<h2>Document Transfer Endpoints</h2>\n<h3><code>POST /docs/document-transfer-preview</code></h3>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;doc_ids&quot;: [\n    &quot;d-20260424-000000-50b63f&quot;\n  ],\n  &quot;target_scope&quot;: &quot;notes&quot;,\n  &quot;transfer_mode&quot;: &quot;copy&quot;,\n  &quot;include_descendants&quot;: false\n}\n</code></pre>\n<p>The preview is write-free. <code>doc_ids</code> is the exact checked set: there is no active-document or context-row fallback. Copy includes descendants only when requested and allocates fresh target identities. Move always expands the complete descendant closure, preserves identities, and requires writable source and target roots. Both modes remove parent/child overlap, retain included hierarchy, and place each effective root at the target scope root.</p>\n<p>Planning inventories referenced source-scope media once, attaches registered build sources such as Mermaid <code>.mmd</code> inputs for referenced SVG, classifies retained external dependencies, and blocks missing bytes, incompatible roles/builds, and differing target bytes. Inline Mermaid remains canonical document source and creates no media-plan item.</p>\n<p>The current browser request remains parent-scope-only. The planner itself resolves source and target as exact <code>{scope}</code> or <code>{scope, sub_scope}</code> collections, and its response records exact collection placement, target-default viewability, custom-metadata decisions, link decisions, blockers, warnings, and an <code>apply_plan</code> only when unblocked. Known inbound links from documents left in a parent source scope are title-based warnings rather than blockers: an explicit source-scope URL does not follow a moved identity automatically, but it can continue to work cross-scope after its <code>scope</code> value is updated. The apply plan is one bounded server-issued receipt containing exact collection identities, parent and collection configuration digests, document/media/build-source evidence, metadata and link decisions, parent-owned media roles, and the intended target rebuild owner. The browser returns it unchanged.</p>\n<h3><code>POST /docs/document-transfer-apply</code></h3>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;confirm&quot;: true,\n  &quot;apply_plan&quot;: {\n    &quot;schema_version&quot;: &quot;docs_document_transfer_apply_plan_v2&quot;,\n    &quot;mode&quot;: &quot;copy&quot;,\n    &quot;source&quot;: {\n      &quot;scope&quot;: &quot;studio&quot;\n    },\n    &quot;target&quot;: {\n      &quot;scope&quot;: &quot;notes&quot;\n    },\n    &quot;operation_timestamp&quot;: &quot;2026-07-24 15:20:00&quot;,\n    &quot;include_descendants&quot;: false,\n    &quot;requested_doc_ids&quot;: [\n      &quot;d-20260424-000000-50b63f&quot;\n    ],\n    &quot;source_parent_config_sha256&quot;: &quot;...&quot;,\n    &quot;source_collection_config_sha256&quot;: &quot;...&quot;,\n    &quot;target_parent_config_sha256&quot;: &quot;...&quot;,\n    &quot;target_collection_config_sha256&quot;: &quot;...&quot;,\n    &quot;media_owners&quot;: {\n      &quot;source&quot;: {&quot;scope&quot;: &quot;studio&quot;},\n      &quot;target&quot;: {&quot;scope&quot;: &quot;notes&quot;}\n    },\n    &quot;target_rebuild_owner&quot;: {&quot;scope&quot;: &quot;notes&quot;},\n    &quot;target_default_viewable&quot;: true,\n    &quot;documents&quot;: [\n      {\n        &quot;source_doc_id&quot;: &quot;d-20260424-000000-50b63f&quot;,\n        &quot;source_sha256&quot;: &quot;...&quot;,\n        &quot;target_doc_id&quot;: &quot;d-20260724-152000-a1b2c3&quot;,\n        &quot;target_parent_id&quot;: &quot;&quot;\n      }\n    ],\n    &quot;media&quot;: [],\n    &quot;custom_metadata&quot;: [],\n    &quot;link_decisions&quot;: []\n  }\n}\n</code></pre>\n<p>Apply requires <code>confirm: true</code>, rejects <code>dry_run</code>, restores the previewed plan, and revalidates every bounded document, media, build-source, target, metadata, link, and configuration fact before mutation. Existing target bytes are reused only when exact; no document or media overwrite is permitted. Copy accepts exact parent or configured child collection receipts, creates documents only under that target root, and routes follow-through to the existing parent or exact child rebuild owner.</p>\n<p>Copy transfers and verifies parent-owned media and registered build sources first, then creates fresh-identity documents. It applies the receipt's target-default viewability and custom-metadata decisions, preserves ordinary Folder/Work/Series fields without interpretation, rewrites only receipt-owned exact <code>doc</code> or <code>subdoc</code> links and planned media references, and rebuilds the exact target once. Its v2 result and activity payload identify exact <code>source</code> and <code>target</code> collection objects and return exact local target URLs. Move retains its parent-scope contract: it completes and rebuilds the target first, removes and rebuilds the exact source document set, then removes only transferred source media no longer referenced by remaining source documents.</p>\n<p>Failures after a write return HTTP <code>409</code> with the exact collection and observed target media, build-source, and document state. Partial creates remain confined to the receipt's target collection and are never reported as success. This generalized pair is the only current Copy/Move route contract. There is no hidden rollback, retry, compatibility route, or success activity before required follow-through completes. The retired <code>/docs/copy-subtree-preview</code> and <code>/docs/copy-subtree-apply</code> paths are not aliases.</p>\n<h2><code>GET /docs/import-source-files</code></h2>\n<p>Query parameters: none.</p>\n<p>Returned data:</p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;candidates&quot;: [\n    {\n      &quot;filename&quot;: &quot;example.md&quot;,\n      &quot;source_format&quot;: &quot;markdown&quot;,\n      &quot;candidate_kind&quot;: &quot;ordinary_document&quot;,\n      &quot;validation_state&quot;: &quot;ready&quot;,\n      &quot;target_mode&quot;: &quot;ordinary_context&quot;,\n      &quot;target&quot;: null,\n      &quot;supports_docs_review&quot;: false,\n      &quot;supports_return_import&quot;: false,\n      &quot;docs_review_enabled&quot;: false,\n      &quot;import_enabled&quot;: true,\n      &quot;diagnostics&quot;: []\n    },\n    {\n      &quot;filename&quot;: &quot;20260730-095512-document-content.jsonl&quot;,\n      &quot;source_format&quot;: &quot;data_sharing_documents&quot;,\n      &quot;candidate_kind&quot;: &quot;returned_package&quot;,\n      &quot;validation_state&quot;: &quot;ready&quot;,\n      &quot;target_mode&quot;: &quot;manifest_collection&quot;,\n      &quot;target&quot;: {\n        &quot;scope&quot;: &quot;analysis&quot;,\n        &quot;sub_scope&quot;: &quot;tags&quot;\n      },\n      &quot;target_label&quot;: &quot;Analysis / Tags&quot;,\n      &quot;scope&quot;: &quot;analysis&quot;,\n      &quot;sub_scope&quot;: &quot;tags&quot;,\n      &quot;supports_docs_review&quot;: true,\n      &quot;supports_return_import&quot;: true,\n      &quot;docs_review_enabled&quot;: true,\n      &quot;import_enabled&quot;: true,\n      &quot;diagnostics&quot;: [],\n      &quot;document_count&quot;: 2\n    }\n  ]\n}\n</code></pre>\n<p>Used for:</p>\n<ul>\n<li>rendering the context-independent app-level Import candidate list</li>\n<li>projecting candidate kind, exact target mode, effective Review/Import\nactions, safe disabled reasons, and display-only destination labels</li>\n</ul>\n<p>The retained <code>files</code> array remains a compatibility projection; the\nconsolidated browser consumes <code>candidates</code>. Neither projection exposes source\nbodies, returned records, trusted metadata payloads, or arbitrary paths.</p>\n<p>Supported staged source formats:</p>\n<ul>\n<li><code>html</code>: <code>.html</code>, <code>.htm</code></li>\n<li><code>docx</code>: <code>.docx</code></li>\n<li><code>markdown</code>: <code>.md</code>, <code>.markdown</code></li>\n<li><code>markdown_package</code>: direct child package folders containing exactly one Markdown file</li>\n<li><code>text</code>: <code>.txt</code></li>\n<li><code>data_sharing_documents</code>: trusted supported Data Sharing <code>.json</code> or <code>.jsonl</code> document collections</li>\n<li><code>edited_review_sources</code>: one complete trusted Docs Review <code>source/</code> folder</li>\n</ul>\n<p>Standalone raster, SVG, and downloadable files are excluded. They belong to the active source editor's <strong>Add image…</strong> and <strong>Add file…</strong> actions.</p>\n<p>Collection recognizers inspect untouched staged candidates before generic\nnormalization. A valid edited folder receives its retained review-folder label,\nexact collection provenance, and return-Import capability in the listing while\n<code>filename</code> remains the only staged request identity. Invalid claimed packages\nremain blocked candidates when a safe diagnostic is available; an individually\nselected review source is rejected before ordinary Markdown conversion.</p>\n<p>Trusted returned document packages are classified directly from their staged\nheader and internal export metadata. Preparing Docs Review never preselects an\nImport source.</p>\n<h2><code>POST /docs/import-source</code></h2>\n<p>Expected data:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;studio&quot;,\n  &quot;sub_scope&quot;: &quot;tags&quot;,\n  &quot;staged_filename&quot;: &quot;example.md&quot;,\n  &quot;include_prompt_meta&quot;: false,\n  &quot;confirm_interactive_html_overwrite&quot;: false,\n  &quot;preview_only&quot;: false\n}\n</code></pre>\n<p><code>sub_scope</code> is omitted for a parent-scope run and required for one fixed\nconfigured child destination.</p>\n<p>Actions:</p>\n<ul>\n<li>validates <code>scope</code> and resolves <code>staged_filename</code> as a direct child of the W0-configured shared import drop-zone</li>\n<li>parses staged HTML, Markdown, Markdown packages, text, and trusted document collections</li>\n<li>converts import output to Markdown source</li>\n<li>validates generated Markdown with the shared Docs Viewer Markdown renderer</li>\n<li>uses the staged filename/title as source context and always allocates a fresh local document identity</li>\n<li>does not load target-scope documents or infer document replacement from filenames or titles</li>\n<li>atomically creates a new source document and refuses a last-moment target-path conflict</li>\n<li>uses <code>confirm_interactive_html_overwrite: true</code> only to confirm replacement of a colliding role-marked interactive HTML companion</li>\n<li>sets <code>last_updated</code> when the new document is created</li>\n<li>materializes inline raster or sanitized SVG media, package media, and interactive HTML assets only during write operations</li>\n<li>supports <code>preview_only: true</code> for non-writing preview responses</li>\n<li>rebuilds targeted generated docs payloads and targeted docs search after successful writes</li>\n<li>logs import-source activity when a create actually writes</li>\n<li>dispatches trusted returned packages and edited review-source folders to\ntheir complete collection planner/apply owners before ordinary formats</li>\n</ul>\n<p>Returned data can include preview Markdown, allocated document identity,\noperation type, media plans, interactive HTML plans, written media records,\ncreated source path, rebuild diagnostics, summary text, <code>dry_run</code>, exact\n<code>target</code>, and a validated local <code>viewer_url</code>. Ordinary results link to the\nexact parent or child document. Collection results link to the exact owning\nscope or sub-scope report. The browser must not synthesize or broaden that\nroute.</p>\n<h2>Returned-Package Collection Import</h2>\n<p>Schema-aware Data Sharing JSON/JSONL collection planning and synchronous apply reuse <code>POST /docs/import-source</code>. The user selects the trusted server-listed staged package from the normal Docs Viewer Import Action.</p>\n<p>The endpoints now use the shared <code>$DOTLINEFORM_PROJECTS_BASE_DIR/data-sharing/import-staging/</code> drop-zone. Endpoint services resolve it through <code>configured_workspace_paths(repo_root).import_staging</code>, return marker-rooted paths, and report import unavailable through the W0 workspace capability contract when the root cannot be used. They do not fall back to repo-local staging.</p>\n<p>The service resolves a safe immutable staged-file identity through Data Sharing\nmetadata, detects supported package headers before the generic JSON/JSONL file\nfallback, and normalizes document records before shared validation, media\nplanning, source writes, and rebuild work. This returned-package path never\nuses the derived Docs Review <code>source/*.md</code> projection as content.</p>\n<p>Collection planning uses the ordinary request identity with <code>preview_only: true</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;library&quot;,\n  &quot;staged_filename&quot;: &quot;returned-documents.jsonl&quot;,\n  &quot;preview_only&quot;: true\n}\n</code></pre>\n<p>The body-free response covers every package record and includes package identity, server-planned create or overwrite actions, collisions, content intent, parent resolution, new-parent dependencies, media summaries, blockers, warnings, and confirmation readiness. Any invalid record or hierarchy error blocks the package. Planning never writes source or media and never invokes generation.</p>\n<p>Confirmed apply uses <code>preview_only: false</code>, explicit confirmation, and the package identity, planned local identities, and planned package actions returned by the server:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;library&quot;,\n  &quot;staged_filename&quot;: &quot;returned-documents.jsonl&quot;,\n  &quot;preview_only&quot;: false,\n  &quot;confirm&quot;: true,\n  &quot;export_id&quot;: &quot;ds_...&quot;,\n  &quot;source_sha256&quot;: &quot;...&quot;,\n  &quot;planned_identities&quot;: [\n    {\n      &quot;record_index&quot;: 0,\n      &quot;source_doc_id&quot;: &quot;package-doc&quot;,\n      &quot;doc_id&quot;: &quot;d-20260715-120000-a1b2c3&quot;,\n      &quot;added_date&quot;: &quot;2026-07-15 12:00:00&quot;\n    }\n  ],\n  &quot;planned_actions&quot;: [\n    {\n      &quot;record_index&quot;: 0,\n      &quot;action&quot;: &quot;overwrite&quot;,\n      &quot;doc_id&quot;: &quot;d-20260101-120000-b2c3d4&quot;,\n      &quot;target_doc_id&quot;: &quot;d-20260101-120000-b2c3d4&quot;\n    }\n  ]\n}\n</code></pre>\n<p><code>planned_actions</code> contains only server-issued <code>create</code> or <code>overwrite</code> actions for the complete valid package. Collection apply rejects record decisions, browser-authored target paths, generated source, and fields outside the request allowlist. <code>planned_identities</code> and <code>planned_actions</code> are confirmed-plan receipts, not browser allocation or selection surfaces.</p>\n<p>Apply rereads the staged package and recomputes the current plan. Changed package identity, server-planned actions, hierarchy validity, or blocker state returns <code>preview_only: true</code>, <code>reconfirmation_required: true</code>, and a refreshed plan without writes. If the confirmed plan remains valid, records are written synchronously in package order. A source-write failure preserves earlier writes, stops later writes, and marks remaining records not attempted; asset failures warn and continue. Docs and search generation runs once after mutation and is reported separately from source outcomes.</p>\n<p>The final response includes <code>outcome</code>, <code>source_mutation</code>, <code>generation</code>, ordered <code>records</code>, grouped and counted created/overwritten/failed/not-attempted records, warnings, and manual-copy instructions. The browser presents that result directly. Confirmed apply does not create a separate result document or result-specific log.</p>\n<h2>Edited Review-Source-Folder Import</h2>\n<p>The same endpoint accepts one server-listed <code>edited_review_sources</code> folder.\nPreview uses the normal collection request:</p>\n<pre><code class=\"language-json\">{\n  &quot;scope&quot;: &quot;analysis&quot;,\n  &quot;sub_scope&quot;: &quot;tags&quot;,\n  &quot;staged_filename&quot;: &quot;edited-tags-review&quot;,\n  &quot;preview_only&quot;: true\n}\n</code></pre>\n<p>The recognizer validates direct Markdown membership, strict review provenance,\nthe retained <code>docs_review_validated_package_v1</code> manifest, trusted export\nmetadata, <code>supports_return_import: true</code>, exact prepared <code>doc_id</code> membership,\nscope/sub-scope routing, and complete prepared <code>source_last_updated</code> evidence.\nThe browser-visible <code>display_name</code> is presentation only and is never accepted\nas <code>staged_filename</code>.</p>\n<p>The body-free preview is overwrite-only and contains the exact collection,\ndocument count, folder hashes, prepared versions, every canonical target,\nblockers, planned overwrite actions, and one <code>derived_markdown_fidelity</code>\nwarning. Creates, record decisions, nested source, standalone reviewed\nMarkdown, and cross-collection fallback are rejected.</p>\n<p>Confirmed apply submits <code>preview_only: false</code>, <code>confirm: true</code>, and the exact\n<code>export_id</code>, <code>source_sha256</code>, <code>trusted_metadata_sha256</code>,\n<code>planned_identities</code>, and <code>planned_actions</code> returned by preview. The service\nrereads the folder and metadata, replans against current canonical sources, and\nreturns a refreshed preview with <code>reconfirmation_required: true</code> if either\nsource side changed.</p>\n<p>An unchanged confirmed plan enters one atomic destination boundary. Parent\nscope apply snapshots every exact source and performs the targeted Docs/search\nrebuild. Child apply reuses the configured sub-scope snapshot and confined\nrebuild with no parent search rebuild. Any source-write or rebuild failure\nrestores every source in the selected collection set, rebuilds the restored\nprojection, and reports no successful overwrite.</p>\n<p>Only edited body, title, and summary advance canonical <code>last_updated</code>; unchanged\ndocuments preserve it. Review transport dates never replace canonical\n<code>added_date</code> or <code>last_updated</code>. Parent and visibility may apply only for a\nparent collection; configured child hierarchy, <code>group</code>, <code>ui_status</code>, and\nvisibility remain destination-owned.</p>\n"
}
