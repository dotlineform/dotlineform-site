{
  "doc_id": "d-20260810-181816-dc9891",
  "title": "Catalogue Works Report Migration",
  "added_date": "2026-08-10 18:18:16",
  "last_updated": "2026-08-10 21:47:08",
  "viewer_url": "/docs/?scope=studio&doc=d-20260810-181816-dc9891",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "replace the permanent read-only Studio Works route with a search-driven local Docs Viewer report over published Works",
  "ui_status": "draft",
  "content_html": "<h1>Catalogue Works Report Migration</h1>\n<h2>Purpose</h2>\n<p>Replace the permanent read-only <code>/studio/studio-works/</code> route with one local Docs Viewer <strong>Catalogue Works</strong> report. The report remains the complete inspection surface for published canonical Works, but it displays rows only after a search and leaves permanent Studio routes to focused editing and workflow responsibilities.</p>\n<p>This feature distinguishes Catalogue Works from the existing local Docs Viewer <a href=\"/docs/?scope=studio&amp;doc=d-20260810-115737-901d0f\">Works Report</a>, whose rows are published Series and whose purpose is Project-document coverage. The new report id is <code>catalogue_works</code>; it does not reuse or broaden <code>works</code>.</p>\n<h2>Current Starting Point</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260401-000000-ebf14a\">Catalogue Works</a> owns the current <code>/studio/studio-works/</code> route. The route is not server-rendered: its Studio JavaScript loads canonical Works and Series through the Local Studio Catalogue read API, augments storage context, filters to published records, constructs rows, sorts them, and preserves optional Series route state in the browser.</p>\n<p>The current page has no ordinary search or filter controls. It renders the complete published list by default, shows only the first value from plural <code>series_ids</code>, and its <strong>Copy series</strong> action copies an alphabetical list of published Series titles rather than the current Work table.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260418-000000-16bf7e\">Catalogue Drafts</a> remains a deliberate Studio route. It is a recovery and maintenance workflow for locating draft Works and Series and opening their exact editors. Draft records are not part of Catalogue Works reporting, and this feature does not move or reproduce Catalogue Drafts.</p>\n<p>No implementation is authorised by this feature document. <a href=\"/docs/?scope=studio&amp;doc=d-20260810-214708-b96106\">CWR-1 — Catalogue Works Report Migration Delivery</a> is proposed and not started.</p>\n<h2>Sequence</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260810-180114-003d2a\">Report Blocks</a> precedes this feature and supplies the single block-level report-host contract. Catalogue Works then becomes the motivating wide tabular report for <a href=\"/docs/?scope=studio&amp;doc=d-20260810-165949-ee4d35\">Expanded Report View</a>.</p>\n<p>The migration must work as an ordinary embedded report before Expanded Report View begins. This feature may declare the report's semantic presentation kind, stable columns, and expanded eligibility, but the expanded presentation and manual column-width behavior remain owned by Expanded Report View.</p>\n<h2>Report Contract</h2>\n<ul>\n<li>One ordinary document in the <code>studio</code> scope hosts the local-only <code>catalogue_works</code> report through one Report Block.</li>\n<li>The report population is every exact canonical Work with <code>status: published</code>. Draft, malformed, or unknown records do not enter the population and require no status column, cue, or filter.</li>\n<li>The report loads its complete current input but renders no rows for an empty search. There is no initial all-Works table and no implicit <strong>Show all</strong> mode.</li>\n<li>A non-empty search matches normalized Work id, title, and exact Series id or title. Search never infers identity from a partial title match when composing a target.</li>\n<li>Search results contain the current columns <strong>Work</strong>, <strong>Year</strong>, <strong>Title</strong>, <strong>Series</strong>, and <strong>Storage</strong>. Every exact <code>series_ids</code> membership is retained and presented; the report does not collapse a Work to its first Series.</li>\n<li>Column headings own accessible sorting for the current search result. Search and sort remain report-owned browser state and use report-prefixed URL parameters if persisted in the route.</li>\n<li>Work and Series links retain exact existing activation targets. The report does not edit catalogue data or reproduce Work or Series editor forms.</li>\n<li>Clearing the search removes the rows and returns to the contained search prompt rather than rendering the complete population.</li>\n<li>Empty matches, unavailable input, and invalid input use contained report states. A failed current read never falls back to public Catalogue JSON or an older result.</li>\n</ul>\n<h2>Semantic HTML Presentation</h2>\n<p>Catalogue Works is a stable rectangular dataset and uses a semantic HTML table with <code>thead</code>, <code>tbody</code>, column headers, rows, and cells. Sorting controls may live inside header cells, and Work or Series links remain ordinary interactive cell content.</p>\n<p>The report owns stable column ids and one ordered header/row correspondence. CSS owns the responsive embedded presentation but does not replace table semantics with a parallel Grid renderer. The ordinary report fits or reflows inside the document measure; horizontal scrolling and temporary manual widths arrive only through Expanded Report View.</p>\n<p>The initial report explicitly exposes <strong>Copy table</strong> after a current search has results. It copies the complete current matching table, including its headers and current row order, as tab-separated plain text suitable for spreadsheet paste. It does not copy the complete loaded population, hidden non-matches, an alphabetical Series list, action labels, or arbitrary interactive DOM outside the semantic table.</p>\n<h2>Frontend And Server Boundary</h2>\n<p>The Local Studio Catalogue service remains the authority for canonical Work and Series reads, validation, and exact record identity. The Docs Viewer report module owns search, filtering, sorting, table rendering, result count, clipboard projection, links, and contained browser states.</p>\n<p>The migration should reuse the existing browser-safe Local Studio Catalogue reads. It does not add a server-rendered page, report-specific endpoint, versioned report response, persisted result, or generated report Markdown merely to relocate the presentation. If a later filter needs data absent from the accepted browser read model, that filter must first receive an explicit Studio-owned projection rather than reading a repository path or public Catalogue payload directly.</p>\n<p><code>catalogue_works</code> is local-only because it depends on local Studio Catalogue authority and curator context. Its module and CSS belong to the local report graph and local report stylesheet; public report metadata, loaders, data, and CSS remain unchanged.</p>\n<h2>Route Retirement</h2>\n<p>The completed migration removes <code>/studio/studio-works/</code> rather than preserving a redirect, route alias, empty shell, or duplicate reader. The retirement includes its Studio route registration, template, JavaScript entry module, route-only text and CSS, runtime/config assertions, route smoke, Admin Checks entry, and current-route documentation. A bounded reference scan must update any exact inbound navigation to the new Docs Viewer report host before removal.</p>\n<p>Studio retains the Work and Series editor routes because their primary purpose is canonical mutation. Catalogue Drafts also remains in Studio as the explicit exception for recovery and maintenance: it finds unfinished canonical records and hands them to those focused editors rather than acting as a general read-only catalogue report.</p>\n<h2>Later Filter Adoption</h2>\n<p>Search and current-column sorting are part of the migration. Additional filters follow only after the migrated report and its accepted data inputs can be inspected in practice.</p>\n<p>Candidate filters include:</p>\n<ul>\n<li><strong>Medium</strong> — exact <code>medium_type</code> values are already canonical, but a binary digital/physical filter requires a separately approved classification rule. Values such as <code>digital print</code> demonstrate that the classification cannot be inferred safely from one word or from storage presence.</li>\n<li><strong>Storage</strong> — yes/no can derive from the presence of the canonical storage value once the migration confirms the accepted field and read-model ownership.</li>\n<li><strong>Tags</strong> — become eligible only after Works have an exact tag-assignment authority and an explicit local browser projection. This feature adds no inert tag control, placeholder field, speculative join, or preloaded CSS.</li>\n</ul>\n<p>Each adopted filter is code-owned, deterministic, and backed by an existing or deliberately added data contract. Filters narrow a non-empty search; they do not independently reveal the complete population or become parameters in the Report Block.</p>\n<h2>Boundaries</h2>\n<p>This feature does not:</p>\n<ul>\n<li>include draft Works or duplicate Catalogue Drafts;</li>\n<li>turn Catalogue Status into a Docs Viewer report or remove its Studio workflow;</li>\n<li>merge <code>catalogue_works</code> with the Series-grained documentation-coverage <code>works</code> report;</li>\n<li>add catalogue mutation, inline editing, bulk selection, publication, or repair actions to the report;</li>\n<li>introduce a generic Catalogue report engine, server-side search product, arbitrary query language, saved report, or report-parameter mechanism;</li>\n<li>read public Catalogue aggregates or exact public payloads as canonical input;</li>\n<li>add medium classification or Work tags before their exact authority is approved;</li>\n<li>implement Expanded Report View, horizontal scrolling, or manual column widths; or</li>\n<li>retain the retired Studio route as a compatibility surface.</li>\n</ul>\n<h2>Delivery</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260810-214708-b96106\">CWR-1 — Catalogue Works Report Migration Delivery</a> owns one concise implementation-fit checkpoint, one atomic report/route cutover, and verification plus durable ownership transfer. It is proposed and does not grant permission to begin its sanity check or implementation.</p>\n<p>The migration is independently complete without later filters or expanded presentation. Data-backed filters require separate approval after the report can be inspected in practice; Expanded Report View remains a later feature.</p>\n<h2>Success Meaning</h2>\n<p>The feature succeeds when every published canonical Work can be found through one search-driven local Docs Viewer report, every result preserves exact Work and plural Series identity in a semantic HTML table, and Copy table represents the current sorted search result. <code>/studio/studio-works/</code> and its dedicated presentation owners are absent, while Catalogue Drafts and focused catalogue editors remain Studio-owned. No public report capability, draft-report duplication, server report product, Report Block parameter, or Expanded Report implementation is introduced.</p>\n"
}
