{
  "doc_id": "d-20260801-190502-6927fb",
  "title": "IFS-1 — Docs Import Folder Source Selection Delivery",
  "added_date": "2026-08-01 19:05:02",
  "last_updated": "2026-08-02 11:00:44",
  "viewer_url": "/docs/?scope=studio&doc=d-20260801-190502-6927fb",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "let ordinary Docs Import browse a remembered folder beneath the Projects base without changing classification, targeting, or apply semantics",
  "content_html": "<h1>IFS-1 — Docs Import Folder Source Selection Delivery</h1>\n<h2>Status And Purpose</h2>\n<p>Proposed and parked as <strong>IFS-1</strong>, a future delivery related to <a href=\"/docs/?scope=studio&amp;doc=d-20260424-000000-fc8b5d\">Docs Import</a>.</p>\n<p>Ordinary Import currently lists only direct children of <code>$DOTLINEFORM_PROJECTS_BASE_DIR/data-sharing/import-staging/</code>. This delivery lets a user choose another folder beneath <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code>, list eligible ordinary sources there, and remember that relative folder between local browser sessions. Import staging remains the default.</p>\n<p>The feature removes unnecessary copying for occasional standalone imports. It does not add recursive discovery, batch apply, or a second import workflow.</p>\n<h2>Complete Outcome</h2>\n<p>The Manage-only app Import modal has one server-backed source-folder control. It begins at <code>data-sharing/import-staging/</code>, can browse to any validated real directory beneath <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code>, and lists the same eligible direct-child ordinary files and one-document Markdown-package folders already accepted by Import.</p>\n<p>The browser stores only the chosen base-relative directory for the next local session. The server re-resolves and revalidates it whenever the modal opens or the selection changes. A missing, unreadable, symlinked, or out-of-root value falls back safely to import staging.</p>\n<p>Selecting a different source directory changes only candidate discovery and source provenance. Existing recognizer-first classification, frozen ordinary destination, conversion, media, create-only identity, source-write, rebuild, and exact-result contracts remain unchanged.</p>\n<h2>Relationship To PRJ-2</h2>\n<p>IFS-1 is not a prerequisite for <a href=\"/docs/?scope=studio&amp;doc=d-20260801-133317-e8eb39\">PRJ-2 — Projects Customisation And Apple Notes Migration Delivery</a>. If it shipped first, its base-relative source locator could remove one small migration-specific adapter and make individual manual imports less awkward. It would not provide corpus inventory, multi-note folder handling, <code>folder_path</code> mapping, stable whole-plan identities, resumable apply, or the final reconciliation report.</p>\n<p>PRJ-2 therefore remains a separate one-off migration and should not wait for IFS-1. The later IFS-1 implementation may reuse an already-shipped confined base-relative locator if PRJ-2 establishes one cleanly.</p>\n<h2>Fixed Contracts</h2>\n<ul>\n<li>IFS-1 extends ordinary standalone-source discovery only. It does not widen trusted returned-package or edited review-source-folder intake beyond the shared import-staging boundary.</li>\n<li>Registered collection recognizers still inspect an untouched candidate before generic Markdown or suffix handling. A trusted format encountered outside staging is not downgraded to an ordinary import.</li>\n<li>The server accepts and returns only <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code>-relative directory markers. Absolute machine paths never become browser-owned state or public response data.</li>\n<li>Folder resolution reuses the shared external-workspace confinement primitives. It rejects blank paths, parent traversal, non-directories, symlinks, symlink escapes, missing paths, and resolved paths outside the configured base.</li>\n<li>Candidate discovery remains one level deep. It lists supported standalone files and eligible direct-child Markdown-package directories; it does not recursively scan the selected tree.</li>\n<li>Existing source-format allowlists and standalone-media exclusions remain authoritative. Folder selection does not make arbitrary files importable.</li>\n<li>A selected Markdown-package directory retains its own folder as the package root for relative image and attachment resolution.</li>\n<li>Opening Import still freezes one validated <code>{scope}</code> or <code>{scope, sub_scope}</code> destination for ordinary candidates. Source location is never a destination signal.</li>\n<li>The remembered folder is a local UI convenience, not configuration or authority. The server revalidates it and the browser replaces an invalid value with <code>data-sharing/import-staging/</code>.</li>\n<li>The source folder is read-only. Import neither moves nor deletes its inputs.</li>\n<li>Results and diagnostics use base-relative markers and never expose absolute local paths.</li>\n</ul>\n<h2>Interaction Shape</h2>\n<ul>\n<li>Reuse the existing Import modal and candidate list.</li>\n<li>Show the current relative source folder in the modal's existing header area with <strong>Choose folder…</strong> and <strong>Use import staging</strong> actions.</li>\n<li><strong>Choose folder…</strong> opens one focused, server-backed directory browser rooted at <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code>; it is not a native browser filesystem picker and cannot select outside that root.</li>\n<li>Browse directories lazily by direct children and show a base-relative breadcrumb. Confirming a folder refreshes the existing candidate list.</li>\n<li>Store the last confirmed relative folder in local browser state only after the server accepts it. Cancel leaves the current folder unchanged.</li>\n<li>Keep the existing Import status, retry, modal ownership, and stale-status clearing behavior.</li>\n</ul>\n<h2>Rough Delivery Steps</h2>\n<h3>IFS-1.0 — Freeze Source-Location Contract</h3>\n<ul>\n<li>[ ] Trace candidate listing, recognizer dispatch, marker paths, Import modal ownership, local preference patterns, and existing external-workspace confinement helpers.</li>\n<li>[ ] Freeze the base-relative directory request/response shape, direct-child listing rules, trusted-format behavior, symlink policy, fallback, and persisted browser key.</li>\n<li>[ ] Record exact proposed files, reused owners, rough production/test diff, and any expansion before implementation approval.</li>\n</ul>\n<p><strong>Checkpoint:</strong> one reviewed contract covers staging, a normal project folder, a one-document Markdown package, a missing remembered folder, a symlink, and a trusted format encountered outside staging.</p>\n<h3>IFS-1.1 — Confined Source Directory And Candidate Listing</h3>\n<ul>\n<li>[ ] Add one server-owned base-relative source-directory resolver and lazy child-directory listing without weakening the fixed staging workspace owner.</li>\n<li>[ ] Parameterize ordinary candidate inventory by the validated directory while preserving recognizer-first classification and safe body-free rows.</li>\n<li>[ ] Keep preview/apply bound to the exact selected directory and candidate; reject stale, changed, or mismatched source-location inputs.</li>\n</ul>\n<h3>IFS-1.2 — Folder Browser And Remembered Selection</h3>\n<ul>\n<li>[ ] Add the focused folder-browser subview and staging reset to the existing Import modal.</li>\n<li>[ ] Persist only the last server-accepted relative directory in local browser state and revalidate it on every new Import session.</li>\n<li>[ ] Preserve existing destination display, candidate selection, Review actions, confirmation, result links, and modal lifecycle.</li>\n</ul>\n<h3>IFS-1.3 — Verification And Closeout</h3>\n<ul>\n<li>[ ] Verify root confinement, traversal and symlink rejection, direct-child listing, format filtering, recognizer-first behavior, stale selection, remembered-folder fallback, and exact preview/apply source binding.</li>\n<li>[ ] Verify ordinary file and Markdown-package imports from staging and one non-staging folder into parent and configured sub-scope destinations.</li>\n<li>[ ] Run focused service, API, route/module, lint, and whitespace checks, then complete manual folder-browser acceptance.</li>\n<li>[ ] Update Docs Import and Docs Import Architecture to describe the shipped source-location contract only after acceptance.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>recursive search or whole-tree candidate expansion;</li>\n<li>multi-selection, batch apply, jobs, progress, or resumable corpus migration;</li>\n<li>PRJ-2 Apple Notes migration or <code>folder_path</code> assignment;</li>\n<li>trusted package or edited review-source intake outside import staging;</li>\n<li>native operating-system folder access or paths outside the Projects base;</li>\n<li>new source formats, media rules, destination selection, or overwrite rules;</li>\n<li>copying, moving, deleting, or synchronizing source material; or</li>\n<li>server-wide or multi-user preference synchronization.</li>\n</ul>\n<h2>Completion Evidence</h2>\n<ul>\n<li>Import staging remains the default and behaves byte-for-byte as before when no alternate directory is selected.</li>\n<li>Every selected and remembered source directory is server-resolved beneath <code>$DOTLINEFORM_PROJECTS_BASE_DIR</code> and represented externally by a relative marker only.</li>\n<li>Ordinary candidates from the selected folder retain existing classification, conversion, exact destination, create-only, media, and rebuild behavior.</li>\n<li>Trusted formats cannot bypass staging or fall through to ordinary Import.</li>\n<li>Invalid remembered folders fail closed to import staging without changing the current destination.</li>\n<li>No recursive or batch-import capability is introduced.</li>\n</ul>\n"
}
