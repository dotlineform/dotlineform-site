{
  "doc_id": "d-20260501-174746-f92667",
  "title": "Run Checks",
  "added_date": "2026-05-01 17:47:46",
  "last_updated": "2026-08-30 14:39:24",
  "viewer_url": "/docs/?scope=studio&doc=d-20260501-174746-f92667",
  "parent_id": "d-20260419-000000-d2e47b",
  "content_html": "<h1>Run Checks</h1>\n<h2>Role</h2>\n<p><code>tests/run_checks.py</code> coordinates optional repo check profiles and writes one local evidence snapshot under <code>var/test-runs/</code>.</p>\n<p>It is not a mandatory gate. Use a focused command when one owner changed; use a profile when several checks form useful evidence for one blast radius.</p>\n<h2>Commands</h2>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 tests/run_checks.py --list\n$HOME/miniconda3/bin/python3 tests/run_checks.py --profile quick\n$HOME/miniconda3/bin/python3 tests/run_checks.py --profile quick --profile catalogue\n</code></pre>\n<p><code>--list</code> and the profile definitions in code are the exact current command inventory.</p>\n<h2>Repository Source Lint</h2>\n<p><code>tooling/lint/</code> owns the pinned repository lint tools, rules, lockfile, and\nmaintained boundaries. Install its isolated tool environments with:</p>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 -m venv tooling/lint/.venv\ntooling/lint/.venv/bin/python -m pip install -r tooling/lint/python-requirements.txt\nnpm ci --prefix tooling/lint\n</code></pre>\n<p>Use explicit paths for new or materially changed source:</p>\n<pre><code class=\"language-bash\">bin/lint-python &lt;path&gt; [path ...]\nbin/lint-js &lt;path&gt; [path ...]\n</code></pre>\n<p>Use the checked maintained boundary for an application-wide claim:</p>\n<pre><code class=\"language-bash\">bin/lint --list\nbin/lint --scope studio\n$HOME/miniconda3/bin/python3 tests/run_checks.py --profile source-lint\n</code></pre>\n<p>Explicit commands fail without eligible targets. Scope commands resolve\n<code>tooling/lint/targets.json</code>; Ruff and ESLint rules remain in that package.\nCommand output reports language, file count, and maintained roots. Processing,\ngenerated/vendor source, dependency trees, caches, canonical data, and retired\nsource remain outside the Python/JavaScript coverage claim.</p>\n<h2>Docs Viewer Site Code Projection</h2>\n<p>Shared/public Docs Viewer runtime code is canonical under <code>docs-viewer/</code>; its tracked public deployment copy remains under <code>site/docs-viewer/</code>. After changing a canonical file represented by <code>site-tools/config/site-code-update.json</code>, run:</p>\n<pre><code class=\"language-bash\">bin/site-code-update\ngit diff -- site/docs-viewer\nbin/site-code-update --check\nbin/site-validate\n</code></pre>\n<p>Inspect the generated delta before handoff and commit canonical and projected files together. Add, remove, or change the public status of a runtime file by editing the manifest explicitly; local-only files in mixed source directories remain excluded. <code>--check</code> and <code>bin/site-validate</code> are non-mutating and require no external Docs Viewer scope workspace.</p>\n<p>This is a code-development gate only for represented runtime files. It is not part of document Build, Publish, Deploy Repo, Deploy Public, or ordinary document editing.</p>\n<h2>Profile Intent</h2>\n<ul>\n<li><code>quick</code> — cheap repo/config/projection/readiness/Python confidence.</li>\n<li><code>studio</code> — complete Studio deterministic Python coverage plus Studio source lint.</li>\n<li><code>catalogue</code> — catalogue Python contracts plus representative build preview.</li>\n<li><code>docs</code> — Docs Viewer/Data Sharing Python contracts plus Studio docs/search generation.</li>\n<li><code>source-lint</code> — all six adopted maintained Python/JavaScript boundaries.</li>\n<li><code>docs-viewer-smoke</code>, <code>studio-smoke</code> —\napp-owned browser/runtime boundaries.</li>\n<li><code>full</code> — the broad configured aggregate, including <code>source-lint</code>; inspect\n<code>--list</code> because it does not imply every app smoke or Processing coverage.</li>\n</ul>\n<p>Do not mirror every child command here; that list changes with code.</p>\n<h2>Outputs</h2>\n<p>Each run writes:</p>\n<ul>\n<li><code>var/test-runs/&lt;run-id&gt;/summary.md</code></li>\n<li><code>var/test-runs/&lt;run-id&gt;/summary.json</code></li>\n<li>one log per command</li>\n</ul>\n<p>Closeout should report profile, pass/fail, and the Markdown summary path.</p>\n<h2>Extension Method</h2>\n<ul>\n<li>add a command only when it proves a durable contract;</li>\n<li>assign it to the app/profile that owns the behavior;</li>\n<li>add a source root to <code>tooling/lint/targets.json</code> only after its runtime\ncontext, exclusions, findings, and complete clean boundary are reviewed;</li>\n<li>require a maintained target boundary before promoting a lint check into a normal profile; a fixed specimen or toolchain smoke is not app-wide evidence;</li>\n<li>keep expensive/niche checks focused unless routine grouped confidence is valuable;</li>\n<li>do not add UI choreography merely because a browser harness exists;</li>\n<li>keep <code>source-lint</code>, app-profile placement, and <code>full</code> composition intentional\nand visible in <code>--list</code>.</li>\n</ul>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260501-174746-efd581\">Testing</a> owns evidence selection. <a href=\"/docs/?scope=studio&amp;doc=d-20260501-000000-49b626\">Browser Smoke Testing</a> owns browser harness rules.</p>\n"
}
