{
  "doc_id": "d-20260602-160839-6d3cbb",
  "title": "Docs Viewer Search",
  "added_date": "2026-06-02 16:08:39",
  "last_updated": "2026-08-13 20:23:57",
  "viewer_url": "/docs/?scope=studio&doc=d-20260602-160839-6d3cbb",
  "parent_id": "d-20260331-000000-5dcf32",
  "content_html": "<h1>Docs Viewer Search</h1>\n<p>Docs Viewer Search provides one metadata search index and one browser query path for every configured parent scope.</p>\n<h2>Shipped Index Contract</h2>\n<p><code>docs-viewer/build/build_search.py</code> emits <code>docs_viewer_search_index_v2</code>. Each index contains a deterministic document table plus a term map whose field-specific posting lists reference positions in that table. The content hash excludes generation time, so an identical rebuild skips its write.</p>\n<p><code>docs-viewer/config/scopes/docs_scopes.json</code> owns each scope's source, working and optional public destinations, viewer route, membership exclusions, and ordered <code>search_fields</code>. Studio, Analysis, Notes, Processing, and dotlineform currently enable the same metadata fields: <code>title</code>, <code>parent_title</code>, <code>identity</code>, and <code>last_updated</code>.</p>\n<p>Title and parent title are tokenized. Unicode and case are normalized; useful technical compounds and their components are retained; a small fixed stop-word list, URLs, paths, markup, file-extension-only components, numeric-only fragments, and full document IDs are excluded from ordinary terms. Full document identity remains available through its explicit exact field, and <code>last_updated</code> remains one field value rather than date fragments.</p>\n<p>Manage-only roots, non-publishable documents, and their descendants do not enter the parent index. Registered sub-scope documents remain in their own collection products rather than the parent search corpus.</p>\n<h2>Build And Publication</h2>\n<p>The builder always reconstructs one complete selected-scope search index. Watcher and write-service plans may still compute affected identities to target document payloads or decide whether search needs to run, but they do not patch postings. No build touches an unrelated scope.</p>\n<p>Working indexes live at their configured published-search locations. External-local scopes are served through the Docs Viewer service. A public scope's explicit Publish copies its accepted working index to the configured public destination; Analysis currently projects the same metadata-only v2 bytes to <code>site/assets/data/search/analysis/index.json</code>.</p>\n<h2>Browser Query Path</h2>\n<p>Generated browser config declares the Docs Viewer domain, v2 schema, index URL, and <code>whole_index</code> rebuild policy. The collection provider reads the active scope's URL, <code>docs-viewer-search-controller.js</code> validates v2 and owns query/history/loading/paging state, and <code>docs-viewer-search.js</code> is the sole tokenizer, postings-intersection, ranking, and tie-breaking owner.</p>\n<p>A multi-term query requires one matching posting set per normalized term. Prefix matching begins at three characters. Ranking prefers exact identity, exact title, title, parent title, and update metadata in that order for the fields currently enabled; ties sort by title and then exact identity. Each document appears at most once.</p>\n<h2>Extension Boundary</h2>\n<p>Adding a normal parent scope is configuration work, not a new builder or browser branch. A scope-specific field decision changes that scope's <code>search_fields</code> and any shared source extraction needed to supply the field; it does not create another schema, tokenizer, or query engine.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260715-084611-c2fa6f\">SM-1 — Studio Full-Text Search Delivery</a> is eligible for its readiness step and separately owns proposed Studio heading, body, and code adoption. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-165124-16b0ab\">Analysis Full-Text Search Extension</a> remains a separate public vocabulary, membership, payload, and publication decision. Notes, Processing, and dotlineform remain metadata-only until an equivalent scope-owned decision is approved. Catalogue Search retains its separate builder, records, and runtime.</p>\n<p>The intentionally small normalization is expressed once in Python generation and once in browser JavaScript, with one shared contract fixture proving parity. Current weak spots are the duplicated scalar/nested browser search URL projection and the practical payload ceiling of one client-loaded static index.</p>\n"
}
