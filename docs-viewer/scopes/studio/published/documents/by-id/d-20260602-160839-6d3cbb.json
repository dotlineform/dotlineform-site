{
  "doc_id": "d-20260602-160839-6d3cbb",
  "title": "Docs Viewer Search",
  "added_date": "2026-06-02 16:08:39",
  "last_updated": "2026-08-13 21:43:50",
  "viewer_url": "/docs/?scope=studio&doc=d-20260602-160839-6d3cbb",
  "parent_id": "d-20260331-000000-5dcf32",
  "content_html": "<h1>Docs Viewer Search</h1>\n<p>Docs Viewer Search provides one static inverted-index format and one browser query path for every configured parent scope. Studio uses that common machinery for authored full text; other scopes remain metadata-only until separately approved.</p>\n<h2>Durable Contracts</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260813-214350-4c7a21\">Docs Viewer Search Index</a> owns payload structure, per-scope field policy, Studio source extraction, ranking inputs, complete rebuilds, and the extension boundary.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260813-214351-98d3f0\">Docs Viewer Search Tokenizer</a> owns normalization, technical-term derivation, exclusions, stop words, query prefix semantics, and Python/JavaScript parity.</li>\n</ul>\n<p><code>docs-viewer/build/build_search.py</code>, <code>docs-viewer/config/scopes/docs_scopes.json</code>, <code>site/docs-viewer/runtime/js/shared/docs-viewer-search.js</code>, and the generated JSON remain exact code/config authority.</p>\n<h2>Scope Policies</h2>\n<p>Studio enables <code>title</code>, <code>heading</code>, <code>body</code>, and <code>code</code>. Its parent title, identity, update time, and route remain document-table metadata for display, navigation, and deterministic ordering but emit no postings. Studio source extraction includes visible authored headings, prose, inline code, fenced code, image alt text, and visible raw-HTML text; it excludes front matter, report declarations, link destinations, media paths, attributes, scripts, styles, document IDs, date fragments, and 64-character hexadecimal digests.</p>\n<p>Analysis, Notes, Processing, and dotlineform retain <code>title</code>, <code>parent_title</code>, exact <code>identity</code>, and exact <code>last_updated</code>. Analysis's explicit Publish projects its accepted metadata-only index to the public site. Enabling body text for another scope requires a separate inclusion, privacy, payload, and publication decision.</p>\n<p>Manage-only roots, non-publishable documents, and their descendants do not enter a parent index. Registered sub-scope documents remain in their own collection products rather than the parent corpus.</p>\n<h2>Build And Publication</h2>\n<p>The builder reconstructs one complete selected-scope index. Watcher and write-service plans may target document payloads or decide whether search needs to run, but they do not patch postings. The content version excludes generation time, so an identical rebuild skips its write. No build touches an unrelated scope.</p>\n<p>Working indexes live at their configured published-search locations. External-local scopes are served through the Docs Viewer service. A public scope's explicit Publish copies its accepted working index to the configured public destination; Analysis currently projects the same metadata-only v2 bytes to <code>site/assets/data/search/analysis/index.json</code>.</p>\n<h2>Browser Query Path</h2>\n<p>Generated browser config declares the Docs Viewer domain, v2 schema, index URL, and <code>whole_index</code> rebuild policy. The collection provider reads the active scope's URL, <code>docs-viewer-search-controller.js</code> validates v2 and owns query/history/loading/paging state, and <code>docs-viewer-search.js</code> is the sole tokenizer, postings-intersection, ranking, and tie-breaking owner.</p>\n<p>A multi-term query requires one matching posting set per normalized term. Prefix matching begins at three characters. Generic ranking prefers exact identity, exact title, title, heading, parent title, body/code, and update metadata; only enabled fields participate. Studio therefore ranks exact title, title, heading, then body/code. Ties sort by title and identity, and each document appears at most once.</p>\n<h2>Extension Boundary</h2>\n<p>Adding a normal parent scope is configuration work, not a new builder or browser branch. A scope-specific field decision changes that scope's <code>search_fields</code> and supplies eligible values through shared extraction; it does not create another schema, tokenizer, cache, reader, or query engine.</p>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260813-165124-16b0ab\">Analysis Full-Text Search Extension</a> remains a separate public vocabulary, membership, payload, and publication decision. Notes, Processing, and dotlineform remain metadata-only until an equivalent scope-owned decision is approved. Catalogue Search retains its separate builder, records, runtime, and roadmap.</p>\n<p>The practical ceiling remains one client-loaded static index. Short prefixes can return broad result sets after full-text adoption; snippets, highlighting, stemming, synonyms, spelling correction, phrase positions, and section results remain outside the current contract.</p>\n"
}
