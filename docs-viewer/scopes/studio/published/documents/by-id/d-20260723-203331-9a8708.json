{
  "doc_id": "d-20260723-203331-9a8708",
  "title": "Improving Repository Context for Coding Agents",
  "added_date": "2026-07-23 20:33:31",
  "last_updated": "2026-07-23 20:55:45",
  "viewer_url": "/docs/?scope=studio&doc=d-20260723-203331-9a8708",
  "parent_id": "d-20260723-212313-90dc78",
  "ui_status": "research",
  "content_html": "<h1>Improving Repository Context for Coding Agents</h1>\n<blockquote>\n<p>Research doc: not indicative of current implementation.</p>\n</blockquote>\n<h2>Purpose</h2>\n<p>This guide explains how coding agents use repository context, the difference between grep-based and semantic retrieval, and how documentation, type information, linting, and tests can make a codebase easier for agents and humans to understand.</p>\n<p>The proposed additions are:</p>\n<ul>\n<li>JSDoc for JavaScript</li>\n<li>Python docstrings</li>\n<li>Python type hints</li>\n<li>Ruff for Python linting</li>\n<li>ESLint for JavaScript linting</li>\n</ul>\n<p>These additions should improve context evaluation, but they do so in different ways.</p>\n<hr />\n<h2>1. What <code>grep</code> is</h2>\n<p><code>grep</code> is a command-line text-search tool. It searches files for literal text or patterns and returns matching lines.</p>\n<p>A recursive search might look like this:</p>\n<pre><code class=\"language-bash\">grep -R &quot;make_work_images&quot; .\n</code></pre>\n<p>This searches the current directory and its subdirectories for the exact text <code>make_work_images</code>.</p>\n<p>Modern development tools often use <code>ripgrep</code>, invoked with <code>rg</code>, because it is fast and works well with source-code repositories:</p>\n<pre><code class=\"language-bash\">rg &quot;media_prefix&quot;\nrg &quot;thumb-480&quot;\nrg &quot;function.*thumbnail&quot; --glob &quot;*.js&quot;\n</code></pre>\n<p>Typical useful forms include:</p>\n<pre><code class=\"language-bash\"># Search the whole repository\nrg &quot;series_ids&quot;\n\n# Search selected folders\nrg &quot;work_id&quot; scripts assets\n\n# Restrict the search to JavaScript\nrg &quot;thumbnail&quot; --glob &quot;*.js&quot;\n\n# Search for several alternatives\nrg &quot;thumbnail|thumb|image.*size|srcset&quot;\n\n# List files containing a match\nrg -l &quot;media_base&quot;\n\n# Show surrounding lines\nrg -n -C 3 &quot;has_details&quot;\n</code></pre>\n<p><code>grep</code> and <code>ripgrep</code> do not understand the meaning of the code. They find matching text.</p>\n<hr />\n<h2>2. Grep-based context in coding agents</h2>\n<p>A coding agent cannot normally place an entire large repository into its model context at once. It must select relevant material.</p>\n<p>With grep-based retrieval, the agent builds context by repeatedly:</p>\n<ol>\n<li>interpreting the request;</li>\n<li>generating likely search terms;</li>\n<li>searching the repository;</li>\n<li>opening matching files;</li>\n<li>following imports, callers, configuration, tests, and related symbols;</li>\n<li>refining its searches.</li>\n</ol>\n<p>The process can be represented as:</p>\n<pre><code class=\"language-text\">Natural-language request\n        ↓\nModel infers likely concepts and vocabulary\n        ↓\nExact repository searches\n        ↓\nRelevant files and code sections\n        ↓\nMore precise searches\n        ↓\nWorking context for the task\n</code></pre>\n<p>For example, a request such as:</p>\n<blockquote>\n<p>Where is the work thumbnail path generated?</p>\n</blockquote>\n<p>might lead to:</p>\n<pre><code class=\"language-bash\">rg &quot;thumb-480&quot;\nrg &quot;media_prefix&quot;\nrg &quot;thumbnail&quot;\nrg &quot;srcset&quot;\n</code></pre>\n<p>Once the agent discovers the actual identifier <code>media_base</code>, it can perform a more precise search:</p>\n<pre><code class=\"language-bash\">rg &quot;media_base&quot;\n</code></pre>\n<p>This is an important distinction:</p>\n<ul>\n<li>the <strong>model</strong> can reason semantically about the prompt;</li>\n<li>the <strong>retrieval operation</strong> can still be an exact text search.</li>\n</ul>\n<p>A repository does not necessarily need a separate embeddings index for an agent to handle vague language. The model can translate vague language into several candidate searches, inspect the results, learn the repository vocabulary, and search again.</p>\n<hr />\n<h2>3. Why grep-based retrieval works well for code</h2>\n<p>Source code contains many exact identifiers:</p>\n<ul>\n<li>function names;</li>\n<li>class names;</li>\n<li>module names;</li>\n<li>filenames;</li>\n<li>import paths;</li>\n<li>route paths;</li>\n<li>configuration keys;</li>\n<li>JSON properties;</li>\n<li>command names;</li>\n<li>error messages;</li>\n<li>test names.</li>\n</ul>\n<p>For example:</p>\n<pre><code class=\"language-bash\">rg &quot;series_ids&quot;\n</code></pre>\n<p>could locate:</p>\n<ul>\n<li>the schema definition;</li>\n<li>data-generation code;</li>\n<li>JSON output;</li>\n<li>validation;</li>\n<li>UI consumers;</li>\n<li>tests;</li>\n<li>migration logic.</li>\n</ul>\n<p>Exact search is especially strong when the task is to:</p>\n<ul>\n<li>find every use of an identifier;</li>\n<li>locate the source of a specific error message;</li>\n<li>identify callers of a function;</li>\n<li>trace a configuration value;</li>\n<li>confirm whether an old code path still exists;</li>\n<li>find all files that depend on a field or route.</li>\n</ul>\n<p>It is fast, deterministic, inexpensive, and easy to verify.</p>\n<hr />\n<h2>4. Limitations of grep-based retrieval</h2>\n<p>Exact search works only when the search vocabulary overlaps with the repository vocabulary.</p>\n<p>Suppose the request is:</p>\n<blockquote>\n<p>Where does the site decide whether a work has extra metadata?</p>\n</blockquote>\n<p>The implementation might use:</p>\n<pre><code class=\"language-json\">{\n  &quot;has_details&quot;: true,\n  &quot;details_count&quot;: 4\n}\n</code></pre>\n<p>A search for <code>extra metadata</code> may return nothing because those words never appear in the code.</p>\n<p>The agent must infer better search terms:</p>\n<pre><code class=\"language-bash\">rg &quot;details|metadata|has_.*detail|count&quot;\n</code></pre>\n<p>This is often successful, but it becomes harder when:</p>\n<ul>\n<li>names are obscure;</li>\n<li>the prompt is conceptual;</li>\n<li>documentation and implementation use different language;</li>\n<li>similar behaviour is distributed across multiple modules;</li>\n<li>there are many abandoned or duplicate implementations;</li>\n<li>domain rules are implied rather than stated.</li>\n</ul>\n<hr />\n<h2>5. Semantic retrieval</h2>\n<p>Semantic retrieval searches by meaning rather than exact wording.</p>\n<p>A semantic query such as:</p>\n<blockquote>\n<p>logic controlling whether a work has additional information</p>\n</blockquote>\n<p>might retrieve code involving:</p>\n<ul>\n<li><code>has_details</code>;</li>\n<li><code>details_count</code>;</li>\n<li>metadata completeness;</li>\n<li>work-detail rendering;</li>\n<li>conditional display rules.</li>\n</ul>\n<p>This can happen even when none of the query wording appears literally in the source.</p>\n<p>Semantic retrieval systems commonly:</p>\n<ol>\n<li>split files into chunks;</li>\n<li>convert each chunk into a numerical representation called an embedding;</li>\n<li>convert the query into the same representation;</li>\n<li>retrieve chunks that appear conceptually similar.</li>\n</ol>\n<p>Semantic retrieval is useful for discovery when the user does not know the repository vocabulary.</p>\n<p>It is less reliable for proving completeness. A semantic search may identify several highly relevant references but still miss an exact caller or an unusual use of a symbol.</p>\n<hr />\n<h2>6. The best approach is hybrid</h2>\n<p>Grep-based and semantic retrieval solve different problems.</p>\n<table>\n<thead>\n<tr>\n<th>Task</th>\n<th style=\"text-align:right\">Exact search</th>\n<th style=\"text-align:right\">Semantic retrieval</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Find every use of <code>media_prefix</code></td>\n<td style=\"text-align:right\">Excellent</td>\n<td style=\"text-align:right\">Unnecessary</td>\n</tr>\n<tr>\n<td>Locate a specific error message</td>\n<td style=\"text-align:right\">Excellent</td>\n<td style=\"text-align:right\">Usually unnecessary</td>\n</tr>\n<tr>\n<td>Find logic related to image fallback behaviour</td>\n<td style=\"text-align:right\">Requires guessed terms</td>\n<td style=\"text-align:right\">Useful</td>\n</tr>\n<tr>\n<td>Discover code when the vocabulary is unknown</td>\n<td style=\"text-align:right\">Limited</td>\n<td style=\"text-align:right\">Strong</td>\n</tr>\n<tr>\n<td>Confirm all callers of an exact function</td>\n<td style=\"text-align:right\">Strong</td>\n<td style=\"text-align:right\">May be incomplete</td>\n</tr>\n<tr>\n<td>Find conceptually similar code with different names</td>\n<td style=\"text-align:right\">Weak</td>\n<td style=\"text-align:right\">Stronger</td>\n</tr>\n<tr>\n<td>Verify a configuration key has been removed</td>\n<td style=\"text-align:right\">Excellent</td>\n<td style=\"text-align:right\">Weak</td>\n</tr>\n<tr>\n<td>Locate architectural intent</td>\n<td style=\"text-align:right\">Depends on documentation wording</td>\n<td style=\"text-align:right\">Useful</td>\n</tr>\n</tbody>\n</table>\n<p>A strong agent workflow is:</p>\n<pre><code class=\"language-text\">Semantic interpretation or semantic retrieval\n        ↓\nLikely modules, concepts, and identifiers\n        ↓\nExact grep/ripgrep searches\n        ↓\nDefinitions, callers, imports, tests, and configuration\n        ↓\nVerified implementation context\n</code></pre>\n<p>Semantic techniques are strongest for <strong>discovery</strong>.</p>\n<p>Exact search is strongest for <strong>verification and completeness</strong>.</p>\n<hr />\n<h2>7. How repository documentation improves context</h2>\n<p>Documentation does not automatically create a semantic-search system. It does, however, add useful semantic clues and searchable vocabulary.</p>\n<p>Compare an undocumented function:</p>\n<pre><code class=\"language-js\">export function makeUrl(id, size, config) {\n  return `${config.base}/${id}-${size}.webp`;\n}\n</code></pre>\n<p>with a documented function:</p>\n<pre><code class=\"language-js\">/**\n * Builds the URL for a generated work-image derivative.\n *\n * The function selects only the URL. It does not create the derivative.\n * The returned URL may be site-relative or use the remote media host,\n * depending on the supplied media configuration.\n *\n * @param {string} workId - Canonical work identifier.\n * @param {800 | 1200 | 1600 | 2400} width - Requested derivative width.\n * @param {MediaConfig} config - Local or remote media configuration.\n * @returns {string} Absolute or site-relative image URL.\n */\nexport function buildWorkImageUrl(workId, width, config) {\n  return `${config.base}/${workId}-primary-${width}.webp`;\n}\n</code></pre>\n<p>The second version adds searchable concepts such as:</p>\n<ul>\n<li>generated derivative;</li>\n<li>work image;</li>\n<li>local or remote media;</li>\n<li>URL selection;</li>\n<li>canonical work identifier.</li>\n</ul>\n<p>A vague request such as:</p>\n<blockquote>\n<p>Find the runtime code that chooses remote primary images.</p>\n</blockquote>\n<p>now has much more textual overlap with the implementation.</p>\n<hr />\n<h2>8. JSDoc for JavaScript</h2>\n<p>JSDoc adds descriptions, parameter types, return types, object structures, and domain rules directly beside JavaScript code.</p>\n<p>Official JSDoc documentation describes it as a way to document JavaScript APIs through comments attached to modules, classes, functions, parameters, and other symbols.</p>\n<h3>8.1 Function contract</h3>\n<pre><code class=\"language-js\">/**\n * Returns the ordered work IDs for a series.\n *\n * The order is editorial and must not be replaced with lexical work-ID order.\n *\n * @param {string} seriesId - Canonical series identifier.\n * @param {SeriesIndex} index - Loaded series index.\n * @returns {string[]} Ordered work identifiers.\n * @throws {Error} If the series does not exist in the index.\n */\nexport function getSeriesWorkIds(seriesId, index) {\n  const series = index.series[seriesId];\n\n  if (!series) {\n    throw new Error(`Unknown series: ${seriesId}`);\n  }\n\n  return series.work_ids;\n}\n</code></pre>\n<p>This explains a non-obvious rule: ordering is editorial rather than lexical.</p>\n<h3>8.2 Object structure</h3>\n<pre><code class=\"language-js\">/**\n * @typedef {object} MediaConfig\n * @property {string} mediaBase\n *   Empty for local assets, or an absolute remote-media origin.\n * @property {string} mediaPrefix\n *   Path prefix below the origin.\n * @property {boolean} useRemotePrimaries\n *   Whether primary derivatives are loaded from remote storage.\n */\n</code></pre>\n<p>The type can then be reused:</p>\n<pre><code class=\"language-js\">/**\n * @param {MediaConfig} config\n * @returns {string}\n */\nexport function getMediaRoot(config) {\n  return `${config.mediaBase}${config.mediaPrefix}`;\n}\n</code></pre>\n<h3>8.3 Constrained values</h3>\n<pre><code class=\"language-js\">/**\n * @typedef {0 | 1 | 2 | 3 | 4 | 5} DimensionScore\n */\n\n/**\n * @typedef {&quot;unipolar&quot; | &quot;bipolar&quot; | &quot;axis2d&quot; | &quot;vector&quot;} DimensionShape\n */\n</code></pre>\n<p>This makes the permitted values explicit.</p>\n<h3>8.4 Module-level description</h3>\n<pre><code class=\"language-js\">/**\n * Work image URL and derivative-selection utilities.\n *\n * This module maps canonical work IDs and derivative widths to asset URLs.\n * It does not generate, resize, upload, or validate image files.\n *\n * @module work-image-urls\n */\n</code></pre>\n<p>A concise module comment can prevent an agent from confusing runtime URL selection with the offline image-generation pipeline.</p>\n<h3>8.5 When JSDoc is most useful</h3>\n<p>Use it primarily for:</p>\n<ul>\n<li>exported functions;</li>\n<li>public classes and methods;</li>\n<li>complex object shapes;</li>\n<li>domain-specific values;</li>\n<li>non-obvious return values;</li>\n<li>side effects;</li>\n<li>exceptional cases;</li>\n<li>important invariants;</li>\n<li>boundaries between subsystems.</li>\n</ul>\n<p>Avoid comments that merely restate obvious syntax:</p>\n<pre><code class=\"language-js\">/**\n * Adds two numbers.\n */\nfunction add(a, b) {\n  return a + b;\n}\n</code></pre>\n<p>Excessive low-value comments increase retrieval noise.</p>\n<hr />\n<h2>9. Python docstrings and type hints</h2>\n<p>Python type hints provide machine-readable structural information. Docstrings add purpose, domain meaning, side effects, invariants, and exceptions.</p>\n<p>Python’s type-hint syntax is designed to support static analysis, editor assistance, and refactoring. Python’s docstring conventions describe the high-level structure of documentation attached to public modules, classes, functions, and methods.</p>\n<h3>9.1 Typed function with a domain-focused docstring</h3>\n<pre><code class=\"language-python\">from collections.abc import Mapping, Sequence\n\n\ndef build_work_record(\n    row: Mapping[str, object],\n    series_ids: Sequence[str],\n) -&gt; dict[str, object]:\n    &quot;&quot;&quot;Convert a validated source row into a canonical work record.\n\n    Null values are retained because null means that a field has not been\n    supplied. Private notes must never be included in public JSON output.\n\n    Args:\n        row: Validated source fields for one work.\n        series_ids: Canonical series identifiers in editorial order.\n\n    Returns:\n        A canonical record suitable for the per-work JSON output.\n\n    Raises:\n        ValueError: If the work identifier is missing or invalid.\n    &quot;&quot;&quot;\n    work_id = str(row.get(&quot;work_id&quot;, &quot;&quot;)).strip()\n\n    if not work_id:\n        raise ValueError(&quot;work_id is required&quot;)\n\n    return {\n        &quot;work_id&quot;: work_id,\n        &quot;series_ids&quot;: list(series_ids),\n        &quot;title&quot;: row.get(&quot;title&quot;),\n        &quot;year&quot;: row.get(&quot;year&quot;),\n    }\n</code></pre>\n<p>The annotations describe the data flow. The docstring records the rules that matter.</p>\n<h3>9.2 Typed domain model</h3>\n<pre><code class=\"language-python\">from dataclasses import dataclass\nfrom typing import Literal, TypeAlias\n\nDimensionScore: TypeAlias = Literal[0, 1, 2, 3, 4, 5]\n\n\n@dataclass(frozen=True)\nclass WorkSummary:\n    &quot;&quot;&quot;Lightweight work metadata used in the generated search index.&quot;&quot;&quot;\n\n    work_id: str\n    title: str | None\n    year: int | None\n    series_ids: tuple[str, ...]\n    details_count: int\n    has_details: bool\n</code></pre>\n<p>This gives the agent a compact statement of the intended data model.</p>\n<h3>9.3 Explicit semantic distinction</h3>\n<pre><code class=\"language-python\">def include_score_in_statistics(score: int | None) -&gt; bool:\n    &quot;&quot;&quot;Return whether a dimension score contributes to statistics.\n\n    ``None`` means unscored and is excluded. Zero means explicitly absent\n    and is included.\n    &quot;&quot;&quot;\n    return score is not None\n</code></pre>\n<p>The null-versus-zero distinction is exactly the kind of rule an agent may otherwise misunderstand.</p>\n<h3>9.4 Module-level docstring</h3>\n<pre><code class=\"language-python\">&quot;&quot;&quot;Generate public work JSON from canonical source records.\n\nThis module owns validation, canonical field ordering, checksums, and public\noutput filtering. It does not generate image derivatives or upload media.\n&quot;&quot;&quot;\n</code></pre>\n<p>This makes responsibility boundaries searchable and explicit.</p>\n<h3>9.5 Where docstrings add the most value</h3>\n<p>Use docstrings for:</p>\n<ul>\n<li>public modules;</li>\n<li>public functions and classes;</li>\n<li>pipeline stages;</li>\n<li>data transformations;</li>\n<li>non-obvious validation;</li>\n<li>privacy rules;</li>\n<li>canonical-versus-derived distinctions;</li>\n<li>ordering rules;</li>\n<li>null semantics;</li>\n<li>side effects;</li>\n<li>filesystem or network operations.</li>\n</ul>\n<p>Private helpers with obvious behaviour often need only a descriptive name and accurate types.</p>\n<hr />\n<h2>10. How type information helps agent context</h2>\n<p>Type information reduces the number of interpretations an agent must consider.</p>\n<p>Without types:</p>\n<pre><code class=\"language-python\">def generate(value, config):\n    ...\n</code></pre>\n<p>The agent must infer:</p>\n<ul>\n<li>what <code>value</code> is;</li>\n<li>what <code>config</code> contains;</li>\n<li>what the function returns;</li>\n<li>whether it mutates anything;</li>\n<li>whether it can return <code>None</code>.</li>\n</ul>\n<p>With types:</p>\n<pre><code class=\"language-python\">def generate_series_index(\n    series: list[SeriesRecord],\n    output_dir: Path,\n    *,\n    dry_run: bool = False,\n) -&gt; GenerationReport:\n    ...\n</code></pre>\n<p>The likely role of the function is much clearer before the body is even read.</p>\n<p>Types are also strong grep targets:</p>\n<pre><code class=\"language-bash\">rg &quot;GenerationReport&quot;\nrg &quot;SeriesRecord&quot;\nrg &quot;MediaConfig&quot;\n</code></pre>\n<p>They help agents follow data through the repository.</p>\n<hr />\n<h2>11. Ruff for Python linting</h2>\n<p>Ruff improves context indirectly by making Python code more consistent and reducing misleading noise.</p>\n<p>It can help identify:</p>\n<ul>\n<li>unused imports;</li>\n<li>undefined names;</li>\n<li>accidental shadowing;</li>\n<li>unreachable or suspicious code;</li>\n<li>inconsistent import organisation;</li>\n<li>obsolete helpers;</li>\n<li>unsafe exception patterns;</li>\n<li>style inconsistencies;</li>\n<li>documentation issues, when selected.</li>\n</ul>\n<p>Ruff can be configured in <code>pyproject.toml</code>, <code>ruff.toml</code>, or <code>.ruff.toml</code>.</p>\n<h3>11.1 Conservative starting configuration</h3>\n<pre><code class=\"language-toml\">[tool.ruff]\ntarget-version = &quot;py312&quot;\nline-length = 100\nextend-exclude = [\n  &quot;.venv&quot;,\n  &quot;_site&quot;,\n  &quot;vendor&quot;,\n]\n\n[tool.ruff.lint]\nselect = [\n  &quot;E&quot;,    # pycodestyle errors\n  &quot;F&quot;,    # Pyflakes\n  &quot;I&quot;,    # import sorting\n  &quot;UP&quot;,   # Python syntax upgrades\n  &quot;B&quot;,    # likely bugs and design problems\n  &quot;SIM&quot;,  # simplifications\n]\nignore = [\n  &quot;E501&quot;, # allow the formatter/editor to manage line wrapping initially\n]\n\n[tool.ruff.format]\nquote-style = &quot;double&quot;\nindent-style = &quot;space&quot;\ndocstring-code-format = true\n</code></pre>\n<p>This is intentionally a baseline rather than an aggressively strict configuration.</p>\n<h3>11.2 Commands</h3>\n<pre><code class=\"language-bash\"># Check the repository\nruff check .\n\n# Apply safe automatic fixes\nruff check . --fix\n\n# Format Python\nruff format .\n\n# Verify formatting without modifying files\nruff format . --check\n</code></pre>\n<h3>11.3 Optional docstring enforcement later</h3>\n<p>Once existing public APIs are documented consistently, docstring rules can be introduced selectively:</p>\n<pre><code class=\"language-toml\">[tool.ruff.lint]\nextend-select = [\n  &quot;D&quot;,\n]\n\n[tool.ruff.lint.pydocstyle]\nconvention = &quot;google&quot;\n</code></pre>\n<p>Do not enable strict docstring enforcement before deciding:</p>\n<ul>\n<li>which modules are public;</li>\n<li>which functions require documentation;</li>\n<li>which docstring convention the project will use;</li>\n<li>which generated or legacy folders should be excluded.</li>\n</ul>\n<p>Otherwise, the initial lint migration can become a large mechanical exercise with little semantic benefit.</p>\n<hr />\n<h2>12. ESLint for JavaScript linting</h2>\n<p>ESLint also improves context indirectly. It reduces ambiguity, unused code, accidental globals, and inconsistent patterns.</p>\n<p>Modern ESLint projects can use a flat configuration file such as:</p>\n<pre><code class=\"language-text\">eslint.config.js\n</code></pre>\n<h3>12.1 Conservative starting configuration</h3>\n<pre><code class=\"language-js\">import js from &quot;@eslint/js&quot;;\n\nexport default [\n  {\n    ignores: [\n      &quot;_site/**&quot;,\n      &quot;vendor/**&quot;,\n      &quot;node_modules/**&quot;,\n      &quot;assets/generated/**&quot;,\n    ],\n  },\n\n  js.configs.recommended,\n\n  {\n    files: [&quot;**/*.js&quot;],\n\n    languageOptions: {\n      ecmaVersion: &quot;latest&quot;,\n      sourceType: &quot;module&quot;,\n      globals: {\n        window: &quot;readonly&quot;,\n        document: &quot;readonly&quot;,\n        fetch: &quot;readonly&quot;,\n        URL: &quot;readonly&quot;,\n        console: &quot;readonly&quot;,\n      },\n    },\n\n    rules: {\n      &quot;no-unused-vars&quot;: [\n        &quot;error&quot;,\n        {\n          argsIgnorePattern: &quot;^_&quot;,\n          varsIgnorePattern: &quot;^_&quot;,\n        },\n      ],\n      &quot;no-constant-condition&quot;: [\n        &quot;error&quot;,\n        {\n          checkLoops: false,\n        },\n      ],\n      &quot;prefer-const&quot;: &quot;error&quot;,\n      &quot;eqeqeq&quot;: [&quot;error&quot;, &quot;always&quot;],\n    },\n  },\n];\n</code></pre>\n<p>The exact global list should reflect the runtime environments in the repository. Browser scripts, Node scripts, service workers, and tests may require separate configuration blocks.</p>\n<h3>12.2 Commands</h3>\n<pre><code class=\"language-bash\"># Check JavaScript\nnpx eslint .\n\n# Apply available fixes\nnpx eslint . --fix\n</code></pre>\n<h3>12.3 Separate browser and Node contexts</h3>\n<pre><code class=\"language-js\">import js from &quot;@eslint/js&quot;;\nimport globals from &quot;globals&quot;;\n\nexport default [\n  js.configs.recommended,\n\n  {\n    files: [&quot;assets/**/*.js&quot;],\n    languageOptions: {\n      sourceType: &quot;module&quot;,\n      globals: globals.browser,\n    },\n  },\n\n  {\n    files: [&quot;scripts/**/*.js&quot;, &quot;*.config.js&quot;],\n    languageOptions: {\n      sourceType: &quot;module&quot;,\n      globals: globals.node,\n    },\n  },\n];\n</code></pre>\n<p>Separating runtime contexts helps prevent an agent from assuming that browser-only globals are available in build scripts, or that Node APIs are available in public site code.</p>\n<hr />\n<h2>13. What linting contributes to context quality</h2>\n<p>Linting does not explain the domain by itself.</p>\n<p>Its value is mainly that it reduces false leads.</p>\n<p>For example, an unused import can make an agent believe that a module still participates in a workflow:</p>\n<pre><code class=\"language-js\">import { buildLegacyIndex } from &quot;./legacy-index.js&quot;;\n</code></pre>\n<p>If the import is unused, ESLint can expose it. Removing it makes the actual architecture clearer.</p>\n<p>Likewise, an abandoned Python helper may look authoritative even though it is never called. Ruff can help identify surrounding unused imports and inconsistent code, while exact reference searches can confirm whether the helper is active:</p>\n<pre><code class=\"language-bash\">rg &quot;build_legacy_index&quot;\n</code></pre>\n<p>Linting improves the repository’s signal-to-noise ratio.</p>\n<hr />\n<h2>14. Tests are also semantic context</h2>\n<p>Descriptively named tests can provide some of the strongest context in a repository because they express executable rules.</p>\n<pre><code class=\"language-python\">def test_private_notes_are_excluded_from_public_work_json() -&gt; None:\n    ...\n</code></pre>\n<pre><code class=\"language-python\">def test_null_dimension_score_is_excluded_from_statistics() -&gt; None:\n    ...\n</code></pre>\n<pre><code class=\"language-js\">test(&quot;series navigation loops from the final work to the first&quot;, () =&gt; {\n  // ...\n});\n</code></pre>\n<pre><code class=\"language-js\">test(&quot;direct work URLs do not show series navigation&quot;, () =&gt; {\n  // ...\n});\n</code></pre>\n<p>These names are:</p>\n<ul>\n<li>searchable;</li>\n<li>precise;</li>\n<li>tied to behaviour;</li>\n<li>less likely than comments to drift unnoticed.</li>\n</ul>\n<p>A useful hierarchy of evidence is:</p>\n<pre><code class=\"language-text\">Passing tests and schemas\n        ↓\nTypes and executable validation\n        ↓\nCurrent implementation\n        ↓\nDocstrings and JSDoc\n        ↓\nArchitecture documents\n        ↓\nHistorical comments and notes\n</code></pre>\n<p>This does not mean comments are unimportant. It means agents should verify important claims against executable evidence where possible.</p>\n<hr />\n<h2>15. Architecture and schema documents</h2>\n<p>Small, focused Markdown documents can provide broader context that code comments cannot.</p>\n<p>Useful documents might include:</p>\n<pre><code class=\"language-text\">docs/\n├── architecture/\n│   ├── data-flow.md\n│   ├── media-pipeline.md\n│   ├── search-pipeline.md\n│   └── repository-boundaries.md\n├── schemas/\n│   ├── works-json.md\n│   ├── series-json.md\n│   └── dimensions.md\n└── development/\n    ├── testing.md\n    ├── linting.md\n    └── generated-files.md\n</code></pre>\n<p>A short boundary statement can prevent major misunderstandings:</p>\n<pre><code class=\"language-markdown\">## Image responsibilities\n\n- `processing/` creates source artwork and offline visual assets.\n- `scripts/images/` creates image derivatives.\n- `assets/js/media/` selects derivative URLs at runtime.\n- R2 stores primary derivatives and attachments.\n- Repository-local thumbnails remain part of the static site build.\n</code></pre>\n<p>This gives a vague prompt several searchable routes into the implementation.</p>\n<hr />\n<h2>16. Documentation principles for agent-friendly code</h2>\n<h3>16.1 Document intent, not syntax</h3>\n<p>Weak:</p>\n<pre><code class=\"language-js\">/**\n * Set hasDetails to true if detailsCount is greater than zero.\n */\nconst hasDetails = detailsCount &gt; 0;\n</code></pre>\n<p>Better:</p>\n<pre><code class=\"language-js\">/**\n * Whether the lightweight index should link to a detailed work record.\n *\n * A value of zero means that no optional detail fields survived public\n * filtering.\n */\nconst hasDetails = detailsCount &gt; 0;\n</code></pre>\n<h3>16.2 Record boundaries</h3>\n<pre><code class=\"language-python\">&quot;&quot;&quot;Read canonical work records and write generated search indexes.\n\nThis package must not modify source records.\n&quot;&quot;&quot;\n</code></pre>\n<h3>16.3 Record invariants</h3>\n<pre><code class=\"language-js\">/**\n * Work IDs are stable strings. Never parse them as numbers because leading\n * zeroes and future non-numeric prefixes are permitted.\n */\n</code></pre>\n<h3>16.4 Record exclusions</h3>\n<pre><code class=\"language-python\">&quot;&quot;&quot;Return a public work record.\n\nThe output excludes ``notes_private`` and internal storage metadata.\n&quot;&quot;&quot;\n</code></pre>\n<h3>16.5 Explain ordering</h3>\n<pre><code class=\"language-js\">/**\n * Preserve the input order. It represents editorial sequencing rather than\n * an automatically sortable property.\n */\n</code></pre>\n<h3>16.6 Avoid historical narration in active code</h3>\n<p>Weak:</p>\n<pre><code class=\"language-js\">// We used to do this through Liquid, but later moved it into JavaScript...\n</code></pre>\n<p>Better:</p>\n<pre><code class=\"language-js\">// Runtime route resolution belongs to the JavaScript shell.\n</code></pre>\n<p>Put historical decisions in an architecture decision record or changelog rather than active implementation comments.</p>\n<hr />\n<h2>17. Recommended source-file structure</h2>\n<p>A predictable structure helps both agents and humans:</p>\n<pre><code class=\"language-text\">module-level purpose\nimports\ntypes and constants\npublic functions\nprivate helpers\nentry point or exports\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-js\">/**\n * Search result shaping for works and series.\n *\n * Applies ranking-independent rules after raw matches have been produced.\n */\n\n/** @typedef {import(&quot;./types.js&quot;).SearchResult} SearchResult */\n\n/**\n * Collapse subordinate work results when an exact series match dominates.\n *\n * @param {SearchResult[]} results\n * @returns {SearchResult[]}\n */\nexport function shapeSearchResults(results) {\n  return collapseSubordinateWorks(results);\n}\n\nfunction collapseSubordinateWorks(results) {\n  // ...\n}\n</code></pre>\n<p>The public behaviour is encountered before the implementation details.</p>\n<hr />\n<h2>18. Suggested validation commands for agents</h2>\n<p>A repository can define a small set of standard checks:</p>\n<pre><code class=\"language-bash\"># Python\nruff check .\nruff format . --check\npython -m pytest\n\n# JavaScript\nnpx eslint .\nnpm test\n\n# Site or generated-output validation\nbundle exec jekyll build\npython scripts/validate_generated_json.py\n</code></pre>\n<p>A single wrapper command is even more useful:</p>\n<pre><code class=\"language-bash\">./scripts/check.sh\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-bash\">#!/usr/bin/env bash\nset -euo pipefail\n\nruff check .\nruff format . --check\npython -m pytest\n\nnpx eslint .\nnpm test\n\nbundle exec jekyll build\n</code></pre>\n<p>This gives coding agents a clear definition of “finished”.</p>\n<hr />\n<h2>19. Suggested staged rollout</h2>\n<p>Introducing all rules at maximum strictness in one change is likely to produce a large amount of low-value cleanup.</p>\n<h3>Stage 1: establish boundaries</h3>\n<ol>\n<li>Identify generated, vendored, build-output, and archived directories.</li>\n<li>Exclude them from linting where appropriate.</li>\n<li>Document the main repository subsystems.</li>\n<li>Add standard lint and test commands.</li>\n</ol>\n<h3>Stage 2: introduce conservative linting</h3>\n<ol>\n<li>Add Ruff with core error, import, bug, and upgrade rules.</li>\n<li>Add ESLint’s recommended rules.</li>\n<li>Fix clear defects and unused code.</li>\n<li>Avoid broad auto-fixes without reviewing the diff.</li>\n</ol>\n<h3>Stage 3: add semantic documentation</h3>\n<ol>\n<li>Add module-level descriptions to important files.</li>\n<li>Document exported JavaScript functions with JSDoc.</li>\n<li>Add type hints to Python public functions.</li>\n<li>Add Python docstrings for domain rules and pipeline stages.</li>\n<li>Define reusable JavaScript object types with <code>@typedef</code>.</li>\n</ol>\n<h3>Stage 4: strengthen executable contracts</h3>\n<ol>\n<li>Add tests for important domain rules.</li>\n<li>Add JSON Schema or validation models where appropriate.</li>\n<li>Add checks for privacy exclusions and generated-output structure.</li>\n<li>Add golden-output tests for stable generated artifacts.</li>\n</ol>\n<h3>Stage 5: increase strictness selectively</h3>\n<ol>\n<li>Introduce stricter rules only where they improve correctness.</li>\n<li>Enable docstring enforcement after the documentation convention is stable.</li>\n<li>Split browser, Node, test, and generated-code ESLint configurations.</li>\n<li>Consider static type checking for Python as a separate later step.</li>\n</ol>\n<hr />\n<h2>20. Relative value for context evaluation</h2>\n<table>\n<thead>\n<tr>\n<th>Addition</th>\n<th style=\"text-align:right\">Direct semantic value</th>\n<th style=\"text-align:right\">Structural value</th>\n<th style=\"text-align:right\">Noise reduction</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Clear names and module boundaries</td>\n<td style=\"text-align:right\">Very high</td>\n<td style=\"text-align:right\">Very high</td>\n<td style=\"text-align:right\">High</td>\n</tr>\n<tr>\n<td>Python type hints</td>\n<td style=\"text-align:right\">High</td>\n<td style=\"text-align:right\">Very high</td>\n<td style=\"text-align:right\">Medium</td>\n</tr>\n<tr>\n<td>Python docstrings</td>\n<td style=\"text-align:right\">Very high when selective</td>\n<td style=\"text-align:right\">Medium</td>\n<td style=\"text-align:right\">Medium</td>\n</tr>\n<tr>\n<td>JSDoc contracts and typedefs</td>\n<td style=\"text-align:right\">Very high when selective</td>\n<td style=\"text-align:right\">High</td>\n<td style=\"text-align:right\">Medium</td>\n</tr>\n<tr>\n<td>Ruff</td>\n<td style=\"text-align:right\">Low</td>\n<td style=\"text-align:right\">High</td>\n<td style=\"text-align:right\">High</td>\n</tr>\n<tr>\n<td>ESLint</td>\n<td style=\"text-align:right\">Low</td>\n<td style=\"text-align:right\">High</td>\n<td style=\"text-align:right\">High</td>\n</tr>\n<tr>\n<td>Descriptive tests</td>\n<td style=\"text-align:right\">Very high</td>\n<td style=\"text-align:right\">Very high</td>\n<td style=\"text-align:right\">High</td>\n</tr>\n<tr>\n<td>Schemas and validation</td>\n<td style=\"text-align:right\">High</td>\n<td style=\"text-align:right\">Very high</td>\n<td style=\"text-align:right\">High</td>\n</tr>\n<tr>\n<td>Small architecture documents</td>\n<td style=\"text-align:right\">Very high</td>\n<td style=\"text-align:right\">High</td>\n<td style=\"text-align:right\">Medium</td>\n</tr>\n<tr>\n<td>Comments on obvious syntax</td>\n<td style=\"text-align:right\">Low</td>\n<td style=\"text-align:right\">Low</td>\n<td style=\"text-align:right\">Negative if excessive</td>\n</tr>\n</tbody>\n</table>\n<p>The proposed tools should therefore help, but not equally:</p>\n<ul>\n<li><strong>JSDoc, docstrings, and types</strong> provide additional meaning and searchable vocabulary.</li>\n<li><strong>Ruff and ESLint</strong> improve consistency and reduce misleading code.</li>\n<li><strong>Tests and schemas</strong> provide executable evidence.</li>\n<li><strong>Architecture documents</strong> explain system-wide intent and boundaries.</li>\n</ul>\n<hr />\n<h2>21. Important limitations</h2>\n<h3>Documentation can become stale</h3>\n<p>Incorrect documentation is worse than missing documentation because an agent may treat it as authoritative.</p>\n<p>Documentation should be updated in the same change as the code it describes.</p>\n<h3>Linting does not prove architecture</h3>\n<p>A repository can pass all lint checks while still containing:</p>\n<ul>\n<li>poor module boundaries;</li>\n<li>duplicated implementations;</li>\n<li>misleading names;</li>\n<li>weak tests;</li>\n<li>undocumented business rules.</li>\n</ul>\n<p>Linting is a foundation, not a substitute for design.</p>\n<h3>Semantic retrieval does not prove completeness</h3>\n<p>A semantic result can look highly relevant while missing an unusual caller or edge case.</p>\n<p>Use exact searches to verify:</p>\n<pre><code class=\"language-bash\">rg &quot;functionName&quot;\nrg &quot;config_key&quot;\nrg &quot;specific error text&quot;\n</code></pre>\n<h3>More context is not always better</h3>\n<p>Large docstrings, repeated explanations, and historical comments can crowd out the code that matters.</p>\n<p>The target should be:</p>\n<pre><code class=\"language-text\">concise purpose\n+ explicit contracts\n+ domain rules\n+ accurate types\n+ executable tests\n</code></pre>\n<hr />\n<h2>22. Recommended repository policy</h2>\n<p>A concise policy could be:</p>\n<pre><code class=\"language-markdown\">## Code documentation and quality policy\n\n- Public Python modules, classes, and functions should use type hints.\n- Public Python APIs should have concise docstrings where purpose, domain\n  rules, side effects, or exceptions are not obvious.\n- Exported JavaScript functions and shared object structures should use\n  JSDoc.\n- Module comments should state responsibility boundaries rather than repeat\n  implementation details.\n- Important domain rules should be represented by tests or validation, not\n  comments alone.\n- Ruff and ESLint should run before changes are considered complete.\n- Generated, vendored, and build-output directories should be explicitly\n  identified and excluded where appropriate.\n- Documentation must be changed alongside the behaviour it describes.\n</code></pre>\n<hr />\n<h2>23. Overall conclusion</h2>\n<p>Introducing JSDoc, Python docstrings, Python type hints, Ruff, and ESLint should make vague development prompts easier for coding agents to resolve.</p>\n<p>They will not independently create a semantic retrieval system. Instead, they improve the material available to both semantic reasoning and exact search:</p>\n<pre><code class=\"language-text\">descriptive names\n+ clear module boundaries\n+ types\n+ concise documentation\n+ linting\n+ tests\n+ schemas\n+ architecture notes\n</code></pre>\n<p>For coding agents:</p>\n<ul>\n<li>semantic reasoning or semantic retrieval identifies likely concepts;</li>\n<li>documentation supplies useful vocabulary and intent;</li>\n<li>types expose data structures and contracts;</li>\n<li>grep confirms exact definitions and references;</li>\n<li>linting reduces false leads;</li>\n<li>tests and schemas verify behaviour.</li>\n</ul>\n<p>The strongest repository context is therefore not produced by a single tool. It comes from combining semantic clues with precise, verifiable structure.</p>\n<hr />\n<h2>References</h2>\n<ul>\n<li><a href=\"https://jsdoc.app/about-getting-started\">JSDoc: Getting Started</a></li>\n<li><a href=\"https://peps.python.org/pep-0257/\">Python PEP 257: Docstring Conventions</a></li>\n<li><a href=\"https://peps.python.org/pep-0484/\">Python PEP 484: Type Hints</a></li>\n<li><a href=\"https://docs.astral.sh/ruff/configuration/\">Ruff: Configuration</a></li>\n<li><a href=\"https://docs.astral.sh/ruff/linter/\">Ruff: Linter</a></li>\n<li><a href=\"https://eslint.org/docs/latest/use/configure/configuration-files\">ESLint: Configuration Files</a></li>\n</ul>\n"
}
