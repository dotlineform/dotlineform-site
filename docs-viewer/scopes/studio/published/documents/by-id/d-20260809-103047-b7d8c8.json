{
  "doc_id": "d-20260809-103047-b7d8c8",
  "title": "DXM-1 — Packaged Media In Dated Export Delivery",
  "added_date": "2026-08-09 10:30:47",
  "last_updated": "2026-08-09 10:47:19",
  "viewer_url": "/docs/?scope=studio&doc=d-20260809-103047-b7d8c8",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "package exact scope-owned media referenced by selected generated documents inside guarded dated HTML snapshots",
  "ui_status": "proposed",
  "content_html": "<h1>DXM-1 — Packaged Media In Dated Export Delivery</h1>\n<h2>Status</h2>\n<p>Proposed and not started. This standalone Roadmap delivery specifies one complete generic Export outcome and does not authorize implementation. Begin with DXM-1.0, record the bounded findings, and stop for implementation approval.</p>\n<p>The earlier Dated Export planning sequence is closed and is not revived as a container for this result. <a href=\"/docs/?scope=studio&amp;doc=d-20260629-152159-ac1af9\">Export</a> remains the durable current-state owner and receives the shipped contract at closeout.</p>\n<h2>Complete Result</h2>\n<p>Docs Viewer Export produces a self-contained dated HTML snapshot for the exact checked documents by copying every referenced, readable, scope-owned media object into the snapshot and rewriting its matching generated HTML reference to the packaged relative path. The result remains provider-neutral across repository, external-local, and R2 media locations and remains usable after the source scope, remote media, or served route is unavailable.</p>\n<p>The snapshot does not crawl the web or copy unrelated scope media. References outside the active scope's exact configured served prefixes remain unchanged and are recorded as external dependencies. The package remains a visual and text reference, not canonical Markdown, an import package, a restorable scope backup, or a managed archive.</p>\n<h2>Why This Matters Now</h2>\n<p>The current snapshot copies generated <code>content_html</code> while leaving images, downloads, rendered assets, and other media at their original URLs. That makes an apparently complete backup depend on repository routes, local services, or remote R2 objects that may later move or disappear.</p>\n<p>The immediate Moments export exposes this gap, but Moments is expected to be removed after backup. The solution therefore belongs to generic Export and must not add Moments-specific Markdown, CSS, routing, storage, or cleanup behavior.</p>\n<h2>Package Contract</h2>\n<p>The proposed <code>docs_static_html_snapshot_v2</code> artifact is:</p>\n<pre><code class=\"language-text\">&lt;dated folder&gt;/\n  index.html\n  styles.css\n  snapshot.json\n  docs/\n    &lt;selected doc_id&gt;.html\n  media/\n    &lt;media type&gt;/\n      &lt;confined relative identity&gt;\n</code></pre>\n<p><code>media/&lt;media type&gt;/&lt;identity&gt;</code> preserves the configured media type and confined logical identity. Identical references in several selected documents produce one packaged object. Rewritten document-page references are relative to the packaged object and preserve fragments where meaningful; CDN cache-query syntax does not become part of frozen media identity.</p>\n<p><code>snapshot.json</code> records each packaged object's media type, logical identity, provider kind, packaged relative path, byte size, SHA-256 digest, and referencing selected document ids. It also records unchanged external dependencies without credentials or absolute local paths. The preview plan revision covers selected generated documents, resolved media identities, and media byte digests so an apply cannot silently install a different package.</p>\n<h2>Reference And Authority Boundary</h2>\n<p>Export inspects only the selected generated <code>content_html</code>. It recognizes references only from supported HTML URL attributes and only when an exact normalized URL is beneath an exact served prefix configured for the active scope and media type. DXM-1.0 freezes the supported attribute set, including the handling of <code>src</code>, <code>srcset</code>, <code>poster</code>, configured media links in <code>href</code>, and object data where current generated content demonstrates a need.</p>\n<p>After a reference maps to <code>(scope, media type, confined identity)</code>, Export reads authoritative bytes through the existing artifact-location adapters. It does not download the served URL, trust a browser-facing URL as byte authority, search several providers, infer identity from filenames, or fetch an arbitrary remote host. Repository, external-local, and R2 remain physical providers behind the same exact read contract.</p>\n<p>An exact matching scope-owned reference that cannot be normalized, confined, read, or verified blocks the plan before target replacement. A reference that does not match configured scope ownership remains external and unchanged. Provider failures cannot downgrade an owned reference into an external dependency.</p>\n<p>Packaged HTML media objects are copied as exact objects but are not recursively crawled for their own scripts, styles, frames, or media. CSS <code>url()</code> discovery, site-shell asset capture, arbitrary download mirroring, and recursive dependency graphs are outside this delivery.</p>\n<h2>Preview And Apply Contract</h2>\n<p>Preview remains read-only but may perform the provider reads and hashing required to produce an exact plan. Its confirmation adds packaged media count and total bytes without introducing an <strong>Include media</strong> mode; referenced scope-owned media is the normal v2 snapshot behavior.</p>\n<p>Apply recomputes the selected generated-document plan, re-reads and re-hashes each exact media identity, and rejects stale source, media, configuration, or target state. It renders documents, copies media, rewrites recognized references, writes provenance, and validates the complete result in the existing sibling staging boundary before any final installation or guarded replacement.</p>\n<p>Version 2 is the sole newly written schema. Existing version 1 snapshot folders do not gain a compatibility writer or hidden upgrade path; DXM-1.0 must confirm their explicit replacement classification under the existing recognized, unrecognized, and collision safeguards.</p>\n<h2>Delivery Boundary</h2>\n<p>DXM-1 includes:</p>\n<ul>\n<li>exact selected-generated-HTML media-reference extraction;</li>\n<li>mapping only through active-scope configured served prefixes and confined identities;</li>\n<li>provider-neutral reads through current repository, external-local, and R2 artifact locations;</li>\n<li>exact-byte hashing, deduplication, manifest provenance, staged copying, and relative URL rewriting;</li>\n<li>unchanged and recorded external dependencies;</li>\n<li>v2 preview, stale-plan, target-replacement, and snapshot contracts;</li>\n<li>focused backend, route, capability, and existing management-workflow follow-through where the v2 payload requires it; and</li>\n<li>durable Export documentation transfer and proportional generated-document verification at closeout.</li>\n</ul>\n<p>DXM-1 excludes:</p>\n<ul>\n<li>canonical Markdown, scope configuration, generated payload, or full-scope backup and restore;</li>\n<li>copying unchecked-document media or inventorying every object in the selected scope;</li>\n<li>arbitrary URL downloads, CDN crawling, provider fallback, a second media registry, or a generic asset bundler;</li>\n<li>recursive HTML, CSS, script, iframe, font, or site-shell dependency capture;</li>\n<li>media migration, deletion, garbage collection, R2 cleanup, scope deletion, or Moments retirement;</li>\n<li>an export mode, destination picker, background schedule, archive manager, or import workflow;</li>\n<li>Moments-specific Markdown rendering, stylesheet capture, or CSS correction; and</li>\n<li>weakening the existing exact checked-id, generated-payload, staged replacement, symlink, traversal, and user-owned target boundaries.</li>\n</ul>\n<h2>Likely Owners To Inspect</h2>\n<p>DXM-1.0 should start with the current owners and narrow the final footprint before implementation:</p>\n<ul>\n<li><code>docs-viewer/services/docs_static_html_export.py</code> for selection planning, HTML rendering, provenance, stale revisions, target inspection, staging, and guarded replacement;</li>\n<li>a focused <code>docs-viewer/services/docs_static_html_export_media.py</code> seam for reference extraction, exact ownership mapping, media planning, hashing, packaged paths, and URL rewriting if inspection confirms that split;</li>\n<li><code>docs-viewer/services/docs_artifact_locations.py</code> for existing confined repository, external-local, and R2 reads, without importing a wider transfer workflow or adding provider search;</li>\n<li>scope configuration and served-reference helpers only where current exact media-prefix resolution is already owned;</li>\n<li><code>docs-viewer/tests/python/test_docs_static_html_export.py</code> plus a focused media-export test module if the new seam warrants it;</li>\n<li>current management capability, route, client, and workflow owners only for changed v2 preview/apply fields; and</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260629-152159-ac1af9\">Export</a> for the final durable contract.</li>\n</ul>\n<p>This is an inspection route, not a preapproved file list. Stop and re-scope if the outcome requires a new provider framework, recursive package engine, generic download service, media registry, or source-backup format.</p>\n<h2>DXM-1.0 Checkpoint Record</h2>\n<p>DXM-1.0 passed its documentation and code sanity gate on 2026-08-09. The result is architecturally coherent against the current integrated exporter and media-location contracts. No media bytes were fetched from a served URL, no remote object was read or changed, and no implementation began.</p>\n<h3>Current Evidence</h3>\n<p>The current exporter is a 1,014-line backend owner. It already owns exact checked-id selection, generated payload reads, deterministic preview/apply revisions, target inspection, complete in-memory file planning, sibling staging, exact staged-file validation, guarded replacement, rollback, and v1 provenance. Its current plan revision covers selection, tree, and titles but not the complete <code>content_html</code>; v2 must add a digest of every selected generated body as well as media digests.</p>\n<p>Scope configuration already supplies every required ownership input per media type: <code>reference_prefix</code>, exact <code>served_path_prefix</code>, one <code>ArtifactLocation</code>, provider kind, and confined location path. <code>artifact_location_adapter()</code>, <code>authenticated_remote_client_for_locations()</code>, and <code>normalize_artifact_identity()</code> already provide targeted repository, external-local, and R2 reads without using the served URL as byte authority. Only media types referenced by the checked generated documents should be authenticated or resolved.</p>\n<p>A read-only scan of current generated <code>content_html</code> found:</p>\n<table>\n<thead>\n<tr>\n<th>provider evidence</th>\n<th>generated scope evidence</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>repository</td>\n<td>Library has two unique owned references through <code>img[src]</code> and <code>iframe[src]</code>; Analysis has eight image references plus one file link; Processing has three image references plus seventeen file links.</td>\n</tr>\n<tr>\n<td>external-local</td>\n<td>Notes has fifteen unique owned references: twelve images, two SVG images, and one file link.</td>\n</tr>\n<tr>\n<td>R2</td>\n<td>Moments has thirty-six unique owned image references through absolute R2-served URLs; the Analysis file link also maps to an R2 location.</td>\n</tr>\n<tr>\n<td>unchanged dependency candidates</td>\n<td>Processing currently has five relative image references outside its configured media prefixes; Notes has one. These remain unchanged and are reported rather than inferred as owned.</td>\n</tr>\n</tbody>\n</table>\n<p>The demonstrated owned attributes are <code>img[src]</code>, <code>iframe[src]</code>, and <code>a[href]</code>. No current owned <code>srcset</code>, <code>poster</code>, or <code>data</code> occurrence was found, but those standard URL-bearing media forms remain within the bounded parser contract below so raw generated HTML does not create a silent packaging gap.</p>\n<h3>Frozen V2 Contract</h3>\n<ul>\n<li>Newly written provenance, preview, and apply responses use <code>docs_static_html_snapshot_v2</code>, <code>docs_static_html_snapshot_preview_v2</code>, and <code>docs_static_html_snapshot_apply_v2</code>. Apply request fields remain unchanged because <code>plan_revision</code> owns document and media freshness.</li>\n<li>Version 1 folders are classified as <strong>unrecognized</strong> after the v2 cutover. They remain explicitly replaceable through the existing danger confirmation; there is no dual reader, compatibility writer, or in-place upgrade.</li>\n<li>One focused HTML parser/rewriter processes <code>src</code>, <code>srcset</code>, <code>poster</code>, and <code>data</code>. <code>href</code> is packaged only when it maps to an exact configured media prefix; an unmatched ordinary hyperlink is not an external media dependency. <code>link[href]</code> and unmatched values in the other supported URL attributes are dependency candidates. Inline <code>data:</code> and fragment-only references remain unchanged and are not external dependencies.</li>\n<li>Each <code>srcset</code> candidate is planned independently and its descriptor is preserved. A configured absolute prefix matches only the same scheme, authority, and strict path boundary; a configured root-relative prefix matches only its strict path boundary. Scheme-relative, other-scope, lookalike-prefix, and other-host references do not match.</li>\n<li>Ownership matching ignores query and fragment. The path is URL-decoded once and then passed through <code>normalize_artifact_identity()</code>; an invalid, escaping, empty, missing, or unreadable owned identity blocks preview. Rewriting drops the cache query after bytes are frozen and preserves a fragment.</li>\n<li>Packaged output uses the exact confined path <code>media/&lt;media type&gt;/&lt;identity&gt;</code>. The plan rejects output-path collisions or a path the destination filesystem cannot represent; it does not invent renamed identities or a second mapping registry.</li>\n<li><code>snapshot.json</code> adds <code>media_count</code>, <code>media_bytes</code>, <code>media</code>, <code>external_dependency_count</code>, and <code>external_dependencies</code>. Each media row contains <code>media_type</code>, <code>identity</code>, <code>provider</code>, <code>packaged_path</code>, <code>size</code>, <code>sha256</code>, and ordered referencing <code>doc_ids</code>. Dependency rows contain a query-free browser reference, element/attribute context, and ordered referencing <code>doc_ids</code>; generated HTML retains its original unmatched URL.</li>\n<li>Preview exposes only non-negative <code>media_count</code>, <code>media_bytes</code>, and <code>external_dependency_count</code>, not media identities or filesystem paths. Confirmation retains the destination as its first line, adds a human-readable included-media line, and adds an unchanged-external-media line only when the count is non-zero.</li>\n<li>Preview reads and hashes each exact referenced object once. Apply recomputes the plan, reads and hashes each exact object again, compares the v2 revision, retains those verified bytes only for the lifetime of that apply plan, and stages them through the existing exact file-set boundary. It does not list a provider or fetch a served URL.</li>\n<li>HTML media objects such as the current Library iframe are copied as exact objects. Their internal scripts, styles, frames, and assets are not recursively inspected or packaged.</li>\n<li>Export capability remains generated-document readiness. R2 credentials, object availability, and selected media bytes are exact preview concerns; capability projection must not authenticate unrelated providers or claim remote byte readiness.</li>\n</ul>\n<h3>Exact Proposed Change Set</h3>\n<p>Production files for DXM-1.1 and DXM-1.2:</p>\n<ul>\n<li>add <code>docs-viewer/services/docs_static_html_export_media.py</code> for parser events, exact served-prefix matching, confined identity resolution, selected adapter creation, byte hashing/deduplication, dependency records, packaged paths, and HTML rewriting;</li>\n<li>update <code>docs-viewer/services/docs_static_html_export.py</code> to carry the media plan, include complete <code>content_html</code> and media digests in v2 revisions, expose preview counts, render rewritten pages, add media bytes/provenance, and retain existing staging/replacement behavior; and</li>\n<li>update <code>docs-viewer/runtime/js/management/docs-viewer-static-html-export-workflow.js</code> to validate the v2 counts/schema and present the media summary in the existing confirmation.</li>\n</ul>\n<p>Focused test files:</p>\n<ul>\n<li>add <code>docs-viewer/tests/python/test_docs_static_html_export_media.py</code> for pure parsing/matching/rewriting plus isolated repository, external-local, and fake-R2 adapter evidence;</li>\n<li>update <code>docs-viewer/tests/python/test_docs_static_html_export.py</code> for v2 integration, body/media stale plans, package/provenance shape, selected-only media, missing owned media, staged preservation, and route payloads; and</li>\n<li>update <code>docs-viewer/tests/smoke/docs_viewer_index_selection_modules.py</code> only for v2 preview validation and confirmation projection.</li>\n</ul>\n<p><code>docs_scope_config.py</code>, <code>docs_artifact_locations.py</code>, media inventory/storage, management routes/service/capabilities/client, selection controllers, CSS, scope configuration, and Moments source are inspected reused owners and are not proposed changes. <a href=\"/docs/?scope=studio&amp;doc=d-20260629-152159-ac1af9\">Export</a> changes only at DXM-1.3 durable transfer. Routine Studio by-ID/tree/Recent/search projections follow documentation edits.</p>\n<h3>Scale, Risks, And Stop Conditions</h3>\n<p>The expected implementation diff is approximately 370–540 production lines: 260–360 in the focused Python media module, 90–140 in exporter integration, and 20–40 in the existing JavaScript workflow. Focused evidence is approximately 420–610 test lines: 260–360 in the new pure/service test module, 120–180 in exporter integration tests, and 40–70 in the existing module smoke. This is a material feature slice, but it remains three production owners and does not justify a registry, crawler, downloader, streaming framework, or alternate export format.</p>\n<p>Preview and apply cost is proportional to the referenced media bytes, and the current adapter contract returns bytes in memory. DXM-1 may retain verified bytes for one apply plan, but if representative packages require streaming, caching, background jobs, archive chunking, or a new size-policy workflow, stop and propose that separately rather than widening this delivery. HTML rewriting must also preserve code blocks, inline Mermaid source, iframe attributes, entities, and ordinary links semantically; focused parser fixtures own that risk.</p>\n<p>Actual R2 byte access was not tested at this checkpoint. The provider-neutral adapter contract and fake-client seam are proven current owners, while a credentialed Moments preview remains implementation/runtime evidence after DXM-1.1—not a claim made by DXM-1.0.</p>\n<h3>Baseline Verification</h3>\n<ul>\n<li><code>test_docs_static_html_export.py</code>: 26 passed with an isolated writable <code>DOTLINEFORM_PROJECTS_BASE_DIR</code>.</li>\n<li><code>test_docs_artifact_locations.py</code>: 12 passed with the same isolated workspace.</li>\n<li>The first exporter test invocation reached no assertions because the sandbox made the real external workspace read-only; rerunning against the prescribed isolated workspace passed and did not touch real external data.</li>\n</ul>\n<p><strong>DXM-1.0 outcome:</strong> ready for an explicit DXM-1.1 implementation decision. DXM-1.2 packaging/apply, DXM-1.3 closeout, Moments cleanup, and any CSS work remain unstarted.</p>\n<h2>Delivery Steps</h2>\n<h3>DXM-1.0 — Sanity And Contract Gate</h3>\n<ul>\n<li>[x] Inventory representative generated references from repository, external-local, and R2-backed scopes without reading arbitrary remote URLs or modifying source data.</li>\n<li>[x] Confirm the exact configured served-prefix-to-artifact-location seam, confinement behavior, supported HTML URL attributes, query/fragment normalization, <code>srcset</code> parsing, and non-recursive HTML-media rule.</li>\n<li>[x] Freeze <code>docs_static_html_snapshot_v2</code>, media manifest and external-dependency fields, packaged path rules, preview media summary, v1 target classification, and owned-reference failure behavior.</li>\n<li>[x] Confirm that media hashes can enter the existing plan revision and be revalidated during staged apply without weakening guarded replacement.</li>\n<li>[x] State the exact proposed production, focused-test, and durable-document files, reused seams, expected diff scale, and scope-creep stop condition.</li>\n<li>[x] Stop if exact ownership cannot be derived from active scope configuration or if provider-neutral bytes cannot be read without a web fetch, fallback lookup, or second registry.</li>\n</ul>\n<p><strong>Gate:</strong> Record the bounded findings in this delivery or a temporary child note and stop for implementation approval. Documentation completion is not runtime completion.</p>\n<h3>DXM-1.1 — Exact Media Plan And Preview</h3>\n<ul>\n<li>[ ] Extract only supported references from the exact selected generated HTML and map them through active-scope configured media prefixes.</li>\n<li>[ ] Read, hash, deduplicate, and attribute exact owned objects through artifact-location adapters; keep external references unchanged and visible.</li>\n<li>[ ] Extend the deterministic v2 plan, revision, provenance, and confirmation summary with media count, bytes, identities, and external dependencies.</li>\n<li>[ ] Prove provider, identity, traversal, encoding, query, fragment, <code>srcset</code>, duplicate-reference, missing-object, and unowned-URL behavior with isolated fixtures.</li>\n</ul>\n<h3>DXM-1.2 — Staged Package And Rewriting</h3>\n<ul>\n<li>[ ] Copy planned media into confined <code>media/&lt;media type&gt;/&lt;identity&gt;</code> paths and rewrite only the corresponding selected document references.</li>\n<li>[ ] Re-read and verify media during apply, detect stale bytes or configuration, and validate the complete v2 package before final installation.</li>\n<li>[ ] Preserve the original target on media-read, render, copy, manifest, validation, final-switch, or rollback failure.</li>\n<li>[ ] Retain existing exact checked-set, hierarchy, default-document, same-scope document-link, target-collision, symlink, and selection-preservation behavior.</li>\n</ul>\n<h3>DXM-1.3 — Integration, Evidence, And Durable Transfer</h3>\n<ul>\n<li>[ ] Prove self-contained viewing for selected owned images, SVGs, files, and demonstrated HTML media across repository, external-local, and R2 fixtures.</li>\n<li>[ ] Prove unchecked media is absent, external dependencies remain unchanged and recorded, and no test or production path performs an arbitrary served-URL fetch.</li>\n<li>[ ] Exercise preview/apply routes and the existing confirmation workflow only as far as their changed v2 fields require.</li>\n<li>[ ] Run focused Python and management tests, source lint for changed owners, generated Docs/search assertions, JSON parsing, and <code>git diff --check</code> proportional to the final footprint.</li>\n<li>[ ] Update Export from the v1 reference-only artifact contract to the shipped v2 packaged-media contract, then present this standalone delivery for explicit retain-or-retire closeout.</li>\n</ul>\n<h2>Completion Gate</h2>\n<p>DXM-1 is complete only when a dated snapshot of an exact checked selection contains one verified local copy of every referenced readable scope-owned media identity, its selected document pages use those local copies, manifest provenance accounts for packaged and external dependencies, provider and target changes are detected before installation, and failure preserves the prior target.</p>\n<p>A package that relies on current R2 or local-service URLs, copies the whole scope inventory, omits an owned missing object, fetches arbitrary remote URLs, follows recursive dependencies, leaks credentials or absolute paths, rewrites unowned references, changes source/configuration, or adds Moments-specific behavior is not a pass.</p>\n"
}
