{
  "doc_id": "d-20260723-203332-eaca11",
  "title": "Introducing JavaScript Configuration to the Repository",
  "added_date": "2026-07-23 20:33:32",
  "last_updated": "2026-07-23 20:55:33",
  "viewer_url": "/docs/?scope=studio&doc=d-20260723-203332-eaca11",
  "parent_id": "d-20260723-212313-90dc78",
  "ui_status": "research",
  "content_html": "<h1>Introducing JavaScript Configuration to the Repository</h1>\n<blockquote>\n<p>Research doc: not indicative of current implementation.</p>\n</blockquote>\n<h2>Purpose</h2>\n<p>This guide outlines a practical approach to introducing project-wide JavaScript configuration.</p>\n<p>The main aims are to:</p>\n<ul>\n<li>make JavaScript behaviour consistent across the repository;</li>\n<li>give VS Code, Codex, and other tools a shared understanding of the project;</li>\n<li>distinguish browser code from Node-based scripts;</li>\n<li>exclude generated and vendored files from analysis;</li>\n<li>support JSDoc-based type information;</li>\n<li>provide repeatable validation commands;</li>\n<li>introduce stricter checking gradually rather than all at once.</li>\n</ul>\n<p>The main configuration files considered here are:</p>\n<pre><code class=\"language-text\">jsconfig.json\neslint.config.js\npackage.json\n</code></pre>\n<p>A formatter configuration can be added separately if required.</p>\n<hr />\n<h2>1. Why configuration matters</h2>\n<p>Without project configuration, tools may interpret individual files differently.</p>\n<p>For example:</p>\n<ul>\n<li>one JavaScript file may use <code>// @ts-check</code>;</li>\n<li>another may not be checked;</li>\n<li>browser globals such as <code>window</code> may be recognised in one context but not another;</li>\n<li>Node globals such as <code>process</code> may be incorrectly assumed to exist in browser code;</li>\n<li>generated files may be linted unnecessarily;</li>\n<li>different developers may run different validation commands.</li>\n</ul>\n<p>Project configuration establishes a shared definition of the repository.</p>\n<p>It tells tools:</p>\n<pre><code class=\"language-text\">which files belong to the project\nwhich JavaScript version is expected\nwhich module system is used\nwhich globals are valid\nwhich folders are generated or external\nwhich rules are errors\nwhich checks define a valid change\n</code></pre>\n<p>This improves consistency for both humans and coding agents.</p>\n<hr />\n<h2>2. The role of each configuration file</h2>\n<h3><code>jsconfig.json</code></h3>\n<p>Defines how VS Code and the TypeScript language service interpret JavaScript files.</p>\n<p>It can control:</p>\n<ul>\n<li>project scope;</li>\n<li>JavaScript version;</li>\n<li>module format;</li>\n<li>import resolution;</li>\n<li>path aliases;</li>\n<li>JSDoc-based type checking;</li>\n<li>excluded directories.</li>\n</ul>\n<h3><code>eslint.config.js</code></h3>\n<p>Defines code-quality and correctness rules.</p>\n<p>It can control:</p>\n<ul>\n<li>unused variables;</li>\n<li>undefined names;</li>\n<li>equality rules;</li>\n<li>browser versus Node environments;</li>\n<li>file-specific rules;</li>\n<li>excluded folders;</li>\n<li>suspicious patterns.</li>\n</ul>\n<h3><code>package.json</code></h3>\n<p>Provides standard commands for developers and agents.</p>\n<p>It can define:</p>\n<pre><code class=\"language-bash\">npm run lint\nnpm run lint:fix\nnpm run check:js\nnpm test\nnpm run check\n</code></pre>\n<p>This avoids relying on undocumented command sequences.</p>\n<hr />\n<h2>3. Recommended starting principle</h2>\n<p>Introduce configuration conservatively.</p>\n<p>The first objective should be to establish:</p>\n<ul>\n<li>file scope;</li>\n<li>exclusions;</li>\n<li>runtime environments;</li>\n<li>basic correctness rules;</li>\n<li>standard commands.</li>\n</ul>\n<p>Do not begin with maximum strictness.</p>\n<p>A very strict initial configuration can create hundreds of warnings that are mostly mechanical. This can obscure real defects and lead to broad automated changes that are difficult to review.</p>\n<p>A staged approach is safer:</p>\n<pre><code class=\"language-text\">scope and exclusions\n        ↓\nbasic linting\n        ↓\nselective JSDoc\n        ↓\nselective type checking\n        ↓\nbroader project checks\n</code></pre>\n<hr />\n<h2>4. Introducing <code>jsconfig.json</code></h2>\n<p>A <code>jsconfig.json</code> file tells VS Code that the repository contains a defined JavaScript project.</p>\n<p>A conservative starting point might be:</p>\n<pre><code class=\"language-json\">{\n  &quot;compilerOptions&quot;: {\n    &quot;allowJs&quot;: true,\n    &quot;checkJs&quot;: false,\n    &quot;target&quot;: &quot;ES2022&quot;,\n    &quot;module&quot;: &quot;ESNext&quot;,\n    &quot;moduleResolution&quot;: &quot;Bundler&quot;\n  },\n  &quot;include&quot;: [\n    &quot;assets/**/*.js&quot;,\n    &quot;scripts/**/*.js&quot;\n  ],\n  &quot;exclude&quot;: [\n    &quot;_site&quot;,\n    &quot;node_modules&quot;,\n    &quot;vendor&quot;,\n    &quot;assets/generated&quot;\n  ]\n}\n</code></pre>\n<p>This configuration:</p>\n<ul>\n<li>recognises JavaScript files as part of one project;</li>\n<li>assumes modern JavaScript;</li>\n<li>treats files as ES modules;</li>\n<li>limits the project to selected source folders;</li>\n<li>excludes build output and external code;</li>\n<li>does not yet enable project-wide type checking.</li>\n</ul>\n<hr />\n<h2>5. Why start with <code>checkJs: false</code></h2>\n<p>Setting:</p>\n<pre><code class=\"language-json\">&quot;checkJs&quot;: false\n</code></pre>\n<p>means that VS Code can still use JSDoc for autocomplete, hover information, navigation, and local inference without reporting type errors across every JavaScript file.</p>\n<p>This is useful during the initial adoption stage.</p>\n<p>Individual files can opt into checking with:</p>\n<pre><code class=\"language-js\">// @ts-check\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-js\">// @ts-check\n\n/**\n * @param {number} a\n * @param {number} b\n * @returns {number}\n */\nexport function add(a, b) {\n  return a + b;\n}\n</code></pre>\n<p>An invalid call may then be reported:</p>\n<pre><code class=\"language-js\">add(&quot;1&quot;, 2);\n</code></pre>\n<p>This allows checking to be introduced module by module.</p>\n<p>Later, when enough important files have accurate JSDoc, the project can move to:</p>\n<pre><code class=\"language-json\">&quot;checkJs&quot;: true\n</code></pre>\n<hr />\n<h2>6. Recommended <code>jsconfig.json</code> rollout</h2>\n<h3>Stage 1: project definition only</h3>\n<pre><code class=\"language-json\">{\n  &quot;compilerOptions&quot;: {\n    &quot;allowJs&quot;: true,\n    &quot;checkJs&quot;: false,\n    &quot;target&quot;: &quot;ES2022&quot;,\n    &quot;module&quot;: &quot;ESNext&quot;\n  },\n  &quot;include&quot;: [\n    &quot;assets/**/*.js&quot;,\n    &quot;scripts/**/*.js&quot;\n  ],\n  &quot;exclude&quot;: [\n    &quot;_site&quot;,\n    &quot;node_modules&quot;,\n    &quot;vendor&quot;\n  ]\n}\n</code></pre>\n<p>This gives tools a shared project boundary without introducing type-checking failures.</p>\n<h3>Stage 2: selective file checking</h3>\n<p>Add:</p>\n<pre><code class=\"language-js\">// @ts-check\n</code></pre>\n<p>to stable modules with good JSDoc.</p>\n<p>Suitable candidates include:</p>\n<ul>\n<li>media URL utilities;</li>\n<li>shared data-loading modules;</li>\n<li>search-index code;</li>\n<li>route configuration;</li>\n<li>tag and dimension models.</li>\n</ul>\n<h3>Stage 3: broader checking</h3>\n<p>Once type information is reliable:</p>\n<pre><code class=\"language-json\">{\n  &quot;compilerOptions&quot;: {\n    &quot;allowJs&quot;: true,\n    &quot;checkJs&quot;: true,\n    &quot;target&quot;: &quot;ES2022&quot;,\n    &quot;module&quot;: &quot;ESNext&quot;,\n    &quot;moduleResolution&quot;: &quot;Bundler&quot;,\n    &quot;strict&quot;: false\n  }\n}\n</code></pre>\n<p>The initial project-wide mode should usually keep:</p>\n<pre><code class=\"language-json\">&quot;strict&quot;: false\n</code></pre>\n<p>until the volume and quality of reported issues are understood.</p>\n<h3>Stage 4: increase strictness selectively</h3>\n<p>Possible later options include:</p>\n<pre><code class=\"language-json\">{\n  &quot;compilerOptions&quot;: {\n    &quot;checkJs&quot;: true,\n    &quot;strict&quot;: true,\n    &quot;noImplicitAny&quot;: false\n  }\n}\n</code></pre>\n<p>This would require a deliberate migration and should not be introduced as a casual cleanup.</p>\n<hr />\n<h2>7. Include and exclude rules</h2>\n<p>The project should explicitly define which files are source code and which are not.</p>\n<p>Example:</p>\n<pre><code class=\"language-json\">{\n  &quot;include&quot;: [\n    &quot;assets/js/**/*.js&quot;,\n    &quot;scripts/**/*.js&quot;,\n    &quot;tests/**/*.js&quot;\n  ],\n  &quot;exclude&quot;: [\n    &quot;_site&quot;,\n    &quot;node_modules&quot;,\n    &quot;vendor&quot;,\n    &quot;assets/generated&quot;,\n    &quot;coverage&quot;,\n    &quot;tmp&quot;\n  ]\n}\n</code></pre>\n<p>Common exclusions include:</p>\n<pre><code class=\"language-text\">_site/\nnode_modules/\nvendor/\ncoverage/\ndist/\nbuild/\ngenerated output\nthird-party libraries\ntemporary files\n</code></pre>\n<p>These exclusions matter because generated and third-party files can:</p>\n<ul>\n<li>produce large numbers of irrelevant warnings;</li>\n<li>contain styles that do not match the repository;</li>\n<li>waste agent context;</li>\n<li>make validation slower;</li>\n<li>create pressure to edit files that should not be changed manually.</li>\n</ul>\n<hr />\n<h2>8. Browser and Node environments</h2>\n<p>A repository may contain more than one JavaScript runtime.</p>\n<p>For example:</p>\n<pre><code class=\"language-text\">assets/js/\n    browser code\n\nscripts/\n    Node-based build or maintenance scripts\n\ntests/\n    test-runner environment\n</code></pre>\n<p>These environments should not be treated as interchangeable.</p>\n<p>Browser code may use:</p>\n<pre><code class=\"language-js\">window\ndocument\nfetch\nlocalStorage\n</code></pre>\n<p>Node scripts may use:</p>\n<pre><code class=\"language-js\">process\nBuffer\n__dirname\nfs\npath\n</code></pre>\n<p>Tests may use:</p>\n<pre><code class=\"language-js\">describe\ntest\nexpect\nbeforeEach\n</code></pre>\n<p>Configuration should represent these differences explicitly.</p>\n<hr />\n<h2>9. Introducing ESLint</h2>\n<p>Modern ESLint projects commonly use a flat configuration file:</p>\n<pre><code class=\"language-text\">eslint.config.js\n</code></pre>\n<p>A conservative starting point could be:</p>\n<pre><code class=\"language-js\">import js from &quot;@eslint/js&quot;;\nimport globals from &quot;globals&quot;;\n\nexport default [\n  {\n    ignores: [\n      &quot;_site/**&quot;,\n      &quot;node_modules/**&quot;,\n      &quot;vendor/**&quot;,\n      &quot;assets/generated/**&quot;,\n      &quot;coverage/**&quot;,\n    ],\n  },\n\n  js.configs.recommended,\n\n  {\n    files: [&quot;assets/js/**/*.js&quot;],\n\n    languageOptions: {\n      ecmaVersion: &quot;latest&quot;,\n      sourceType: &quot;module&quot;,\n      globals: globals.browser,\n    },\n\n    rules: {\n      &quot;eqeqeq&quot;: [&quot;error&quot;, &quot;always&quot;],\n      &quot;prefer-const&quot;: &quot;error&quot;,\n      &quot;no-unused-vars&quot;: [\n        &quot;error&quot;,\n        {\n          argsIgnorePattern: &quot;^_&quot;,\n          varsIgnorePattern: &quot;^_&quot;,\n        },\n      ],\n    },\n  },\n\n  {\n    files: [&quot;scripts/**/*.js&quot;, &quot;*.config.js&quot;],\n\n    languageOptions: {\n      ecmaVersion: &quot;latest&quot;,\n      sourceType: &quot;module&quot;,\n      globals: globals.node,\n    },\n  },\n];\n</code></pre>\n<p>This configuration:</p>\n<ul>\n<li>ignores generated and external folders;</li>\n<li>enables ESLint’s recommended rules;</li>\n<li>treats browser files as browser code;</li>\n<li>treats scripts and config files as Node code;</li>\n<li>adds a small number of useful consistency rules.</li>\n</ul>\n<hr />\n<h2>10. Why environment-specific configuration matters</h2>\n<p>Without environment separation, tools may fail in two directions.</p>\n<h3>False errors</h3>\n<p>Browser code may be incorrectly flagged:</p>\n<pre><code class=\"language-js\">document.querySelector(&quot;#app&quot;);\n</code></pre>\n<p>because <code>document</code> is not recognised.</p>\n<h3>Missed errors</h3>\n<p>Node-specific code may accidentally enter browser modules:</p>\n<pre><code class=\"language-js\">const config = process.env.APP_CONFIG;\n</code></pre>\n<p>If <code>process</code> is incorrectly accepted everywhere, the error may not be detected until runtime.</p>\n<p>Environment-specific configuration makes module boundaries clearer.</p>\n<p>This also helps coding agents understand where code is intended to run.</p>\n<hr />\n<h2>11. Recommended initial ESLint rules</h2>\n<p>A useful starting set is:</p>\n<pre><code class=\"language-js\">rules: {\n  &quot;eqeqeq&quot;: [&quot;error&quot;, &quot;always&quot;],\n  &quot;prefer-const&quot;: &quot;error&quot;,\n  &quot;no-unused-vars&quot;: [\n    &quot;error&quot;,\n    {\n      argsIgnorePattern: &quot;^_&quot;,\n      varsIgnorePattern: &quot;^_&quot;,\n    },\n  ],\n}\n</code></pre>\n<p>These rules help identify:</p>\n<ul>\n<li>accidental loose equality;</li>\n<li>variables that should not be reassigned;</li>\n<li>unused variables and parameters;</li>\n<li>abandoned code paths;</li>\n<li>misleading imports.</li>\n</ul>\n<p>Avoid adding a very large style rule set initially.</p>\n<p>Rules should primarily target correctness and clarity before cosmetic uniformity.</p>\n<hr />\n<h2>12. Example repository-specific ESLint structure</h2>\n<p>A more detailed configuration could distinguish browser modules, management code, build scripts, and tests:</p>\n<pre><code class=\"language-js\">import js from &quot;@eslint/js&quot;;\nimport globals from &quot;globals&quot;;\n\nexport default [\n  {\n    ignores: [\n      &quot;_site/**&quot;,\n      &quot;node_modules/**&quot;,\n      &quot;vendor/**&quot;,\n      &quot;coverage/**&quot;,\n      &quot;assets/generated/**&quot;,\n    ],\n  },\n\n  js.configs.recommended,\n\n  {\n    files: [&quot;assets/js/**/*.js&quot;],\n\n    languageOptions: {\n      ecmaVersion: &quot;latest&quot;,\n      sourceType: &quot;module&quot;,\n      globals: globals.browser,\n    },\n\n    rules: {\n      &quot;eqeqeq&quot;: [&quot;error&quot;, &quot;always&quot;],\n      &quot;prefer-const&quot;: &quot;error&quot;,\n      &quot;no-unused-vars&quot;: [\n        &quot;error&quot;,\n        {\n          argsIgnorePattern: &quot;^_&quot;,\n          varsIgnorePattern: &quot;^_&quot;,\n        },\n      ],\n    },\n  },\n\n  {\n    files: [&quot;scripts/**/*.js&quot;],\n\n    languageOptions: {\n      ecmaVersion: &quot;latest&quot;,\n      sourceType: &quot;module&quot;,\n      globals: globals.node,\n    },\n  },\n\n  {\n    files: [&quot;tests/**/*.js&quot;, &quot;**/*.test.js&quot;],\n\n    languageOptions: {\n      ecmaVersion: &quot;latest&quot;,\n      sourceType: &quot;module&quot;,\n      globals: {\n        ...globals.node,\n        describe: &quot;readonly&quot;,\n        test: &quot;readonly&quot;,\n        expect: &quot;readonly&quot;,\n        beforeEach: &quot;readonly&quot;,\n        afterEach: &quot;readonly&quot;,\n      },\n    },\n  },\n];\n</code></pre>\n<p>The exact test globals should match the actual test framework.</p>\n<hr />\n<h2>13. Package configuration</h2>\n<p>The required packages might be installed with:</p>\n<pre><code class=\"language-bash\">npm install --save-dev eslint @eslint/js globals\n</code></pre>\n<p>A minimal <code>package.json</code> script section could be:</p>\n<pre><code class=\"language-json\">{\n  &quot;scripts&quot;: {\n    &quot;lint&quot;: &quot;eslint .&quot;,\n    &quot;lint:fix&quot;: &quot;eslint . --fix&quot;\n  }\n}\n</code></pre>\n<p>Later, JSDoc-based checking can be added through TypeScript:</p>\n<pre><code class=\"language-bash\">npm install --save-dev typescript\n</code></pre>\n<p>Then:</p>\n<pre><code class=\"language-json\">{\n  &quot;scripts&quot;: {\n    &quot;lint&quot;: &quot;eslint .&quot;,\n    &quot;lint:fix&quot;: &quot;eslint . --fix&quot;,\n    &quot;check:js&quot;: &quot;tsc --noEmit -p jsconfig.json&quot;,\n    &quot;check&quot;: &quot;npm run lint &amp;&amp; npm run check:js &amp;&amp; npm test&quot;\n  }\n}\n</code></pre>\n<p>This gives developers and coding agents a standard command:</p>\n<pre><code class=\"language-bash\">npm run check\n</code></pre>\n<hr />\n<h2>14. One command should define completion</h2>\n<p>A coding agent benefits from a clear validation target.</p>\n<p>Instead of relying on several undocumented commands, provide one standard command:</p>\n<pre><code class=\"language-bash\">npm run check\n</code></pre>\n<p>That command might run:</p>\n<pre><code class=\"language-text\">ESLint\nJSDoc-based type checking\nJavaScript tests\nsite build checks\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-json\">{\n  &quot;scripts&quot;: {\n    &quot;lint&quot;: &quot;eslint .&quot;,\n    &quot;check:js&quot;: &quot;tsc --noEmit -p jsconfig.json&quot;,\n    &quot;test&quot;: &quot;node --test&quot;,\n    &quot;build:site&quot;: &quot;bundle exec jekyll build&quot;,\n    &quot;check&quot;: &quot;npm run lint &amp;&amp; npm run check:js &amp;&amp; npm test &amp;&amp; npm run build:site&quot;\n  }\n}\n</code></pre>\n<p>The exact command should reflect the repository’s actual tooling.</p>\n<hr />\n<h2>15. Configuration comments</h2>\n<p>JSON does not officially support comments.</p>\n<p>Therefore, <code>jsconfig.json</code> should remain concise and self-explanatory.</p>\n<p>For more complex decisions, use a nearby Markdown file such as:</p>\n<pre><code class=\"language-text\">docs/development/javascript-configuration.md\n</code></pre>\n<p>That file can explain:</p>\n<ul>\n<li>why checking is currently selective;</li>\n<li>which folders are excluded;</li>\n<li>why browser and Node files are separated;</li>\n<li>which strictness options are deferred;</li>\n<li>how to add new file groups.</li>\n</ul>\n<p>The ESLint flat config is JavaScript, so limited comments are possible:</p>\n<pre><code class=\"language-js\">{\n  files: [&quot;assets/js/**/*.js&quot;],\n\n  // Public site code runs in the browser and must not depend on Node globals.\n  languageOptions: {\n    globals: globals.browser,\n  },\n}\n</code></pre>\n<p>Comments should explain decisions, not restate syntax.</p>\n<hr />\n<h2>16. Configuration and JSDoc</h2>\n<p>JSDoc comments can exist without <code>jsconfig.json</code>.</p>\n<p>Example:</p>\n<pre><code class=\"language-js\">/**\n * @param {string} workId\n * @returns {Promise&lt;WorkRecord&gt;}\n */\nexport async function loadWork(workId) {\n  // ...\n}\n</code></pre>\n<p>However, project configuration makes the treatment of those comments more consistent.</p>\n<p>It tells VS Code and other tools:</p>\n<ul>\n<li>which JavaScript files belong together;</li>\n<li>how imports should be resolved;</li>\n<li>whether JSDoc types should be checked;</li>\n<li>which language features are expected;</li>\n<li>which folders should be ignored.</li>\n</ul>\n<p>The relationship is:</p>\n<pre><code class=\"language-text\">JSDoc\n    describes functions, objects, and contracts\n\njsconfig.json\n    defines how the project interprets JavaScript and JSDoc\n\nESLint\n    enforces code-quality and environment rules\n\npackage.json\n    provides standard commands\n</code></pre>\n<hr />\n<h2>17. Configuration and Codex</h2>\n<p>Project configuration helps Codex because it provides an explicit machine-readable description of the repository.</p>\n<p>Codex can inspect:</p>\n<pre><code class=\"language-text\">jsconfig.json\neslint.config.js\npackage.json\n</code></pre>\n<p>and infer:</p>\n<ul>\n<li>the expected module format;</li>\n<li>whether code is browser or Node based;</li>\n<li>which folders are generated;</li>\n<li>which lint rules matter;</li>\n<li>whether JSDoc types are checked;</li>\n<li>which command validates a change.</li>\n</ul>\n<p>This reduces guesswork.</p>\n<p>It also gives Codex a reliable completion process:</p>\n<pre><code class=\"language-bash\">npm run check\n</code></pre>\n<p>Without configuration, Codex may have to infer different commands and environments from the code itself.</p>\n<hr />\n<h2>18. Recommended staged introduction</h2>\n<h3>Stage 1: map the JavaScript codebase</h3>\n<p>Identify:</p>\n<ul>\n<li>browser code;</li>\n<li>Node scripts;</li>\n<li>tests;</li>\n<li>generated files;</li>\n<li>vendored files;</li>\n<li>archived code;</li>\n<li>build output.</li>\n</ul>\n<p>Do not configure linting before these boundaries are understood.</p>\n<h3>Stage 2: add <code>jsconfig.json</code></h3>\n<p>Start with:</p>\n<pre><code class=\"language-json\">&quot;checkJs&quot;: false\n</code></pre>\n<p>Define:</p>\n<ul>\n<li>target;</li>\n<li>module format;</li>\n<li>included folders;</li>\n<li>excluded folders.</li>\n</ul>\n<h3>Stage 3: add basic ESLint</h3>\n<p>Enable:</p>\n<ul>\n<li>recommended rules;</li>\n<li>browser and Node environments;</li>\n<li>unused-variable checking;</li>\n<li>strict equality;</li>\n<li><code>prefer-const</code>.</li>\n</ul>\n<h3>Stage 4: add standard package scripts</h3>\n<p>At minimum:</p>\n<pre><code class=\"language-json\">{\n  &quot;scripts&quot;: {\n    &quot;lint&quot;: &quot;eslint .&quot;,\n    &quot;lint:fix&quot;: &quot;eslint . --fix&quot;\n  }\n}\n</code></pre>\n<h3>Stage 5: introduce JSDoc selectively</h3>\n<p>Prioritise:</p>\n<ul>\n<li>exported functions;</li>\n<li>shared data structures;</li>\n<li>configuration types;</li>\n<li>subsystem boundaries;</li>\n<li>non-obvious rules.</li>\n</ul>\n<h3>Stage 6: enable file-level type checking</h3>\n<p>Use:</p>\n<pre><code class=\"language-js\">// @ts-check\n</code></pre>\n<p>on stable modules.</p>\n<h3>Stage 7: add project-wide checking</h3>\n<p>Move to:</p>\n<pre><code class=\"language-json\">&quot;checkJs&quot;: true\n</code></pre>\n<p>only after reviewing the likely error volume.</p>\n<h3>Stage 8: consider stricter rules</h3>\n<p>Increase strictness only where it improves correctness and remains maintainable.</p>\n<hr />\n<h2>19. Suggested first implementation</h2>\n<p>A sensible first change would include:</p>\n<pre><code class=\"language-text\">jsconfig.json\neslint.config.js\npackage.json scripts\ndocumentation of exclusions and environments\n</code></pre>\n<p>It should not include:</p>\n<ul>\n<li>broad behavioural rewrites;</li>\n<li>conversion to TypeScript;</li>\n<li>comments on every function;</li>\n<li>maximum strictness;</li>\n<li>mass auto-fixing without review.</li>\n</ul>\n<p>Example scope:</p>\n<pre><code class=\"language-markdown\">Introduce baseline JavaScript project configuration.\n\nAdd:\n\n- `jsconfig.json` with explicit source folders and exclusions;\n- ESLint flat configuration;\n- separate browser and Node environments;\n- conservative correctness rules;\n- standard `npm run lint` and `npm run lint:fix` commands.\n\nKeep project-wide JSDoc type checking disabled initially.\n\nDo not change runtime behaviour.\n</code></pre>\n<hr />\n<h2>20. Suggested Codex task</h2>\n<pre><code class=\"language-markdown\">Introduce conservative JavaScript project configuration.\n\nRequirements:\n\n1. Add `jsconfig.json`.\n2. Include the active JavaScript source and script folders.\n3. Exclude generated output, vendored code, build output, dependencies, and\n   temporary folders.\n4. Use modern ECMAScript and ES modules where consistent with the current\n   repository.\n5. Keep project-wide `checkJs` disabled initially.\n6. Add an ESLint flat configuration.\n7. Separate browser code, Node scripts, and tests where applicable.\n8. Enable ESLint recommended rules plus a small set of correctness-oriented\n   rules.\n9. Add package scripts for linting and automatic fixes.\n10. Document the configuration and standard validation commands.\n\nDo not:\n\n- convert files to TypeScript;\n- enable maximum strictness;\n- add JSDoc to every function;\n- change runtime behaviour;\n- lint generated or vendored files.\n\nRun the configured checks and report any existing issues separately from\nconfiguration changes.\n</code></pre>\n<hr />\n<h2>21. Review checklist</h2>\n<p>Before accepting the configuration, check:</p>\n<ul>\n<li>Are all active JavaScript source folders included?</li>\n<li>Are generated and vendored folders excluded?</li>\n<li>Are browser and Node environments separated?</li>\n<li>Does the module setting match the current code?</li>\n<li>Does the target JavaScript version match supported browsers and tools?</li>\n<li>Are test globals configured correctly?</li>\n<li>Are lint rules focused on correctness rather than stylistic churn?</li>\n<li>Is project-wide type checking still appropriately conservative?</li>\n<li>Are standard commands documented?</li>\n<li>Does <code>npm run lint</code> complete successfully?</li>\n<li>Are automatic fixes reviewed rather than accepted blindly?</li>\n<li>Does the configuration avoid changing runtime behaviour?</li>\n</ul>\n<hr />\n<h2>22. Recommended initial policy</h2>\n<pre><code class=\"language-markdown\">## JavaScript configuration policy\n\n- JavaScript source folders and runtime environments must be declared\n  explicitly.\n- Browser code, Node scripts, and tests should use separate configuration\n  blocks where their globals differ.\n- Generated, vendored, build-output, dependency, and temporary folders must\n  be excluded from analysis.\n- ESLint should initially enforce correctness and clarity rather than broad\n  stylistic rules.\n- JSDoc type checking should be introduced gradually.\n- Stable modules may opt into checking with `// @ts-check`.\n- Project-wide `checkJs` should be enabled only after the existing error\n  volume has been reviewed.\n- Standard validation commands must be exposed through `package.json`.\n- Configuration changes should not alter runtime behaviour.\n</code></pre>\n<hr />\n<h2>Conclusion</h2>\n<p>Configuration is primarily about consistency, but its value is broader than visual uniformity.</p>\n<p>It creates a shared machine-readable definition of the JavaScript project:</p>\n<pre><code class=\"language-text\">project scope\n+ runtime environments\n+ module format\n+ exclusions\n+ lint rules\n+ JSDoc interpretation\n+ validation commands\n</code></pre>\n<p>This helps VS Code, Codex, developers, and automated checks interpret the repository in the same way.</p>\n<p>The recommended approach is to establish boundaries and conservative checks first, then introduce JSDoc-based type checking gradually.</p>\n"
}
