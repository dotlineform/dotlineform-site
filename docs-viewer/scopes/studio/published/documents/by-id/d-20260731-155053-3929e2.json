{
  "doc_id": "d-20260731-155053-3929e2",
  "title": "Sub-Scope Customisation Architecture",
  "added_date": "2026-07-31 15:50:53",
  "last_updated": "2026-08-09 22:41:25",
  "viewer_url": "/docs/?scope=studio&doc=d-20260731-155053-3929e2",
  "parent_id": "d-20260801-084127-752d7e",
  "summary": "define the default docs_subscope report, unified registered aspects, access-safe projection, exact targets, browser composition, and extension checks",
  "content_html": "<h1>Sub-Scope Customisation Architecture</h1>\n<h2>Purpose</h2>\n<p>This document is the current implementation reference for the default <code>docs_subscope</code> report and its one optional registered collection extension. It fixes configuration, aspect ownership, generated data, browser composition, exact targets, access projection, and the checks required when a registration changes.</p>\n<pre><code class=\"language-text\">configured sub-scope\n  -&gt; default report host, manifests, by-ID records, and standard Manage contribution\n  -&gt; optional sub_scope_customisation {id, settings}\n       -&gt; registered server aspects\n       -&gt; access-safe manifest and browser projections\n       -&gt; access-specific browser contribution\n</code></pre>\n<p>The configured sub-scope is the lifecycle and collection record. The default remains complete; a customisation adds only the collection-specific aspects owned by its registered definition.</p>\n<h2>Default Report And Exact Identity</h2>\n<p>Every configured sub-scope receives the shared flat list/detail reader and normal lifecycle assets. The report owns configured collection validation, complete supplied membership, list/filter/sort projection, by-ID identity validation, <code>subdoc</code> navigation, history, empty/unavailable/error states, and contribution hosts.</p>\n<p>Manage composes the standard contribution first. It owns lifecycle presentation, collection-local selection, New, Prepare Package, <strong>Copy to…</strong>, Copy Link, Delete, refresh reconciliation, and app-level Import handoff. <strong>Copy to…</strong> passes the registrar's immutable exact collection selection to the shared document-transfer workflow; it never derives a target from the report host, displayed row, route, or active document. A registered browser contribution is composed second in its own hosts.</p>\n<p>The exact targets are:</p>\n<pre><code class=\"language-json\">{&quot;scope&quot;: &quot;&lt;scope-id&gt;&quot;, &quot;sub_scope&quot;: &quot;&lt;sub-scope-id&gt;&quot;}\n</code></pre>\n<pre><code class=\"language-json\">{&quot;scope&quot;: &quot;&lt;scope-id&gt;&quot;, &quot;sub_scope&quot;: &quot;&lt;sub-scope-id&gt;&quot;, &quot;doc_id&quot;: &quot;&lt;document-id&gt;&quot;}\n</code></pre>\n<p>A selection target adds an immutable, de-duplicated <code>doc_ids</code> array to the exact collection. Collection, selection, and validated-detail actions receive these targets from the report/action registrar. Titles, filenames, routes, parent documents, displayed rows, and current UI selection remain presentation inputs rather than identity authorities.</p>\n<p>The indexed report host is an ordinary parent-scope document with <code>viewer_report: docs_subscope</code> and <code>viewer_report_subscope: &lt;sub-scope-id&gt;</code>. The host selects the report and collection; the configured sub-scope selects the optional customisation. Child documents remain outside the parent Index, Recent, and global search projections. For a publish-capable sub-scope, a publishable child enters the prepared public manifest, but its route is reachable only when the ordinary parent host is also publishable and declares <code>viewer_report_access: public</code>; scope-wide Publish then promotes both existing parent-host and child projections.</p>\n<h2>Configuration Contract</h2>\n<p>A default collection uses its ordinary configured record:</p>\n<pre><code class=\"language-json\">{\n  &quot;sub_scope&quot;: &quot;default&quot;,\n  &quot;title&quot;: &quot;Default&quot;,\n  &quot;ui_statuses&quot;: [&quot;draft&quot;, &quot;done&quot;],\n  &quot;public_projection&quot;: null\n}\n</code></pre>\n<p>A customised collection adds exactly one <code>sub_scope_customisation</code> object:</p>\n<pre><code class=\"language-json\">{\n  &quot;sub_scope&quot;: &quot;tags&quot;,\n  &quot;title&quot;: &quot;Tags&quot;,\n  &quot;public_title&quot;: &quot;Concepts&quot;,\n  &quot;sub_scope_customisation&quot;: {\n    &quot;id&quot;: &quot;analysis_tags&quot;,\n    &quot;settings&quot;: {\n      &quot;groups&quot;: [&quot;subject&quot;, &quot;domain&quot;, &quot;form&quot;, &quot;theme&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The Python registry owns valid IDs and the exact settings normalizer. Configuration contains data only; executable names, field arrays, and availability flags remain code-owned by the registered definition.</p>\n<h2>Registered Definition</h2>\n<p><code>DocsSubScopeCustomisationDefinition</code> owns <code>customisation_id</code>, <code>normalize_settings</code>, and the smallest optional aspect records required by the collection:</p>\n<table>\n<thead>\n<tr>\n<th>Aspect</th>\n<th>Registered responsibility</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>manifest_projection</code></td>\n<td>Project the strict custom root and row data for an enabled browser access.</td>\n</tr>\n<tr>\n<td><code>document_groups</code></td>\n<td>Resolve ordered document-group values from validated settings.</td>\n</tr>\n<tr>\n<td><code>source_validation</code></td>\n<td>Validate custom source front matter during document loading/building.</td>\n</tr>\n<tr>\n<td><code>metadata</code></td>\n<td>Read custom metadata and optionally normalize its existing revision-bound update.</td>\n</tr>\n<tr>\n<td><code>import_front_matter</code></td>\n<td>Normalize custom create-only import front matter.</td>\n</tr>\n<tr>\n<td><code>browser_composition</code></td>\n<td>Declare the <code>public</code> and/or <code>manage</code> accesses with a browser implementation.</td>\n</tr>\n<tr>\n<td><code>assignable_field_groups</code></td>\n<td>Declare typed group IDs and server-owned field names for future focused authoring.</td>\n</tr>\n<tr>\n<td><code>authoring_subject</code></td>\n<td>Declare which canonical subject fields the exact collection reads, independently of assignment availability.</td>\n</tr>\n<tr>\n<td><code>transfer</code></td>\n<td>Declare a typed server-owned non-subject transfer contract, its owned field names, and target-settings validator.</td>\n</tr>\n<tr>\n<td><code>document_lineage</code></td>\n<td>Opt one exact source or editorial collection into a named private lineage contract without projecting that contract to the browser manifest.</td>\n</tr>\n</tbody>\n</table>\n<p>Registry validation checks definition identity, aspect record types and callbacks, supported browser accesses, manifest/browser coupling, unique group and field identities, Manage access for assignable groups, canonical subject field declarations, transfer identity, lineage contract identity and source/editorial role, and the prohibition on claiming shared subject fields as custom transfer fields.</p>\n<p><code>sub_scope_customisation_authoring_subject_fields()</code>, <code>sub_scope_customisation_assignable_field_groups()</code>, <code>sub_scope_customisation_transfer_contract()</code>, and <code>sub_scope_customisation_document_lineage_contract()</code> expose the server declarations to their owning services. Projects and Analysis Works both read the exact <code>folder_path</code>, <code>work_id</code>, and <code>series_id</code> subject field set, but only Projects declares the assignable <code>authoring_subject</code> group. Copy planning freezes each declared non-subject field as retained, omitted, or rejected; apply executes only retained and omitted decisions from the revalidated receipt while ordinary subject fields remain outside this custom seam. The matching <code>dotlineform_projects_to_analysis_works</code> lineage declarations opt only exact <code>dotlineform/projects</code> Working and <code>analysis/works</code> Editorial collections into Working-owned New/Replace planning.</p>\n<h2>Collection Copy Apply Ownership</h2>\n<p>The shared document-transfer apply service writes fresh identities to the resolved collection source root. For the one matching lineage contract, it instead permits an explicit exact Replace selected from the source document's current private lineage rows. New keeps the fresh-identity transformation; Replace preserves the selected editorial filename, <code>doc_id</code>, <code>parent_id</code>, <code>added_date</code>, and current <code>publishable</code> representation while replacing authored content and transferable metadata. Parent targets use the coordinated parent docs/search rebuild; child targets use the existing exact sub-scope rebuild, so child documents remain outside the parent Index, Recent, and global search. Parent scope configuration continues to own media and registered build sources for either target shape.</p>\n<p>Copy composes exact target URLs through the configured parent report host: parent documents use <code>doc</code>, and child documents use the report <code>doc</code> plus copied <code>subdoc</code>. Receipt-owned internal links are transformed between those shapes without resolving titles or subjects. Apply results, activity, and failure evidence carry the exact <code>{scope}</code> or <code>{scope, sub_scope}</code> collection, while browser selection and destination surfaces remain separate owners. Server capabilities enumerate eligible exact parent and child destinations; the shared browser workflow excludes only the exact source collection and does not reproduce collection availability policy.</p>\n<h2>Working-Owned Lineage Ownership</h2>\n<p>The strict private <code>docs_document_publication_lineage_v3</code> envelope stores the one configured Working collection and Editorial collection, then records only Working IDs with at least one current Editorial child. Every child has exact <code>doc_id</code>, <code>created_at</code>, <code>last_copied_at</code>, and nullable <code>published_url</code> fields. Records and children are deterministic, a Working ID is unique, and an Editorial ID belongs to only one Working record. There is no browser or public lineage payload, empty Working-only record, separate Published state, lineage ID, compatibility reader, or inferred relationship.</p>\n<p>New appends one fresh Editorial child after the target mutation and exact child rebuild succeed; Replace updates only one explicitly selected current child while preserving Editorial identity and its current <code>publishable</code> representation. Set Publishable changes source eligibility and rebuilds its exact collection without touching lineage. Successful Publish reconciles only current child <code>published_url</code> evidence. Confirmed Editorial Delete removes its exact public projection through the shared cleanup owner before removing that child; confirmed Working Delete removes its complete record without mutating independently canonical Editorial or public documents. Only a changed lineage table triggers the exact private Projects collection rebuild.</p>\n<p>The Projects private manifest joins each current Working row to its deterministic <code>publication_targets</code> children. Its Manage contribution renders no lineage cue for zero children, one highest Editorial or Published row cue for one or more children, and every exact child in focused detail. The list has equal-width sortable <strong>Doc title</strong> and <strong>Subject</strong> columns plus the narrow aggregate publication cue: Folder subjects keep their exact stored path and validated Finder activation, while exact Work and Series IDs join to the existing Catalogue target lookup for the current target title and configured local-preview route. An unavailable Catalogue target falls back to its exact ID rather than the document title. Project State reuses the same subject icons for its distinct subject-placement meaning. Analysis retains only the shared publishability-exclusion cue.</p>\n<h2>Generated And Browser Projection</h2>\n<p>The server retains normalized settings, concrete field names, validation, metadata, import, and transfer authority. Browser configuration receives only the access-safe descriptor selected for the exact configured collection.</p>\n<p>Manage descriptors contain the selected identity and any access-safe capabilities declared by that exact registration. Analysis Tags currently projects identity only:</p>\n<pre><code class=\"language-json\">{&quot;sub_scope_customisation&quot;: {&quot;id&quot;: &quot;analysis_tags&quot;}}\n</code></pre>\n<p>Projects additionally projects its assignable group identity:</p>\n<pre><code class=\"language-json\">{\n  &quot;sub_scope_customisation&quot;: {\n    &quot;id&quot;: &quot;dotlineform_projects&quot;,\n    &quot;capabilities&quot;: {\n      &quot;assignable_field_groups&quot;: [&quot;authoring_subject&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The shared config controller normalizes those IDs to <code>capabilities.assignableFieldGroups</code>. <code>hasDocsViewerAssignableFieldGroup()</code> is the browser availability check for focused actions and modals. Concrete field names and accepted values remain with the exact server definition and its validated settings.</p>\n<p>The public manifest contains publishable <code>{doc_id, title}</code> rows. The Manage manifest contains complete collection rows plus compact lifecycle metadata. Child publishability controls the next child projection only; it does not activate or publish a non-publishable parent report host. Local collections carry no publishability field. An access-enabled <code>manifest_projection</code> may add the existing strict <code>customisation</code> data envelope:</p>\n<pre><code class=\"language-json\">{\n  &quot;customisation&quot;: {\n    &quot;id&quot;: &quot;analysis_tags&quot;,\n    &quot;data&quot;: {&quot;groups&quot;: [&quot;subject&quot;, &quot;domain&quot;, &quot;form&quot;, &quot;theme&quot;]}\n  },\n  &quot;docs&quot;: [\n    {\n      &quot;doc_id&quot;: &quot;d-example&quot;,\n      &quot;title&quot;: &quot;Example&quot;,\n      &quot;ui_status&quot;: &quot;draft&quot;,\n      &quot;publishable&quot;: false,\n      &quot;last_updated&quot;: &quot;2026-08-01 08:00:00&quot;,\n      &quot;customisation&quot;: {&quot;group&quot;: &quot;subject&quot;}\n    }\n  ]\n}\n</code></pre>\n<p>The descriptor and manifest IDs agree exactly. Custom row data targets supplied manifest members and remains namespaced from builder-owned fields. Browser routes consume the configured manifest and by-ID URLs rather than constructing storage paths.</p>\n<h3>Authoring Subjects And Private Associations</h3>\n<p>The shared source reader normalizes the exact raw <code>folder_path</code>, <code>work_id</code>, and <code>series_id</code> declarations into one <code>authoring_subject</code> object. Its states are <code>none</code>, valid Folder, valid Work, valid Series, <code>malformed</code>, or <code>conflicting</code>. A valid Work ID is an exact five-digit string; a valid Series ID is an exact lowercase alphanumeric-or-hyphen string. Folder is valid only when the exact collection registers <code>folder_path</code> in an assignable field group. Work and Series reading is collection-independent. Invalid declarations retain their raw field/value evidence as private observational state and remain ordinary source.</p>\n<p>Private Manage sub-scope manifests carry <code>authoring_subject</code> on every row participating in this projection and one top-level <code>subject_generation</code> receipt. The matching <code>subject-associations.json</code> uses <code>docs_subject_associations_v1</code>, repeats the exact collection and receipt, and groups each valid <code>{kind, key}</code> subject to a deterministically ordered list of documents. Every document entry contains the exact <code>{scope, sub_scope, doc_id}</code> target and its current composed Manage location. Several documents may share one subject. Association generation reads only canonical front matter and already-composed document locations; current folder existence, Catalogue membership, titles, filenames, body text, route selection, and UI state remain separate owners.</p>\n<p>The builder writes the Manage manifest and private association product from one normalized generation. Later exact rebuilds remove retired declarations and leave an empty association set when the final declaration is cleared. A consumer that reads both products must require matching <code>subject_generation</code> values. Public-readonly building retains its public-safe manifest, internal Manage manifest, and by-ID schemas, while private subject state, invalid evidence, local paths, assignment capability, and <code>subject-associations.json</code> stay with local Manage delivery.</p>\n<h2>Browser Contribution Contract</h2>\n<p>The access-specific JavaScript registry resolves the descriptor ID to one lazy factory. The factory validates the descriptor and returns only its needed contribution callbacks:</p>\n<table>\n<thead>\n<tr>\n<th>Callback</th>\n<th>Ownership</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>createFilters(context)</code></td>\n<td>Add collection-specific filters.</td>\n</tr>\n<tr>\n<td><code>renderRow(context)</code></td>\n<td>Populate detached row contribution hosts.</td>\n</tr>\n<tr>\n<td><code>renderListToolbar(context)</code></td>\n<td>Add exact-collection controls.</td>\n</tr>\n<tr>\n<td><code>renderSelectionToolbar(context)</code></td>\n<td>Add controls for the immutable exact selection snapshot.</td>\n</tr>\n<tr>\n<td><code>renderDetailToolbar(context)</code></td>\n<td>Add controls after manifest/by-ID identity validation.</td>\n</tr>\n<tr>\n<td><code>mountMetadataEditor(context)</code></td>\n<td>Mount the current strict metadata contribution and return <code>read</code>/<code>destroy</code> owners.</td>\n</tr>\n<tr>\n<td><code>notify(event)</code></td>\n<td>Observe explicit collection-scoped lifecycle events.</td>\n</tr>\n</tbody>\n</table>\n<p>The shared report supplies detached hosts and immutable context. The default contribution keeps its own lifecycle and action ownership. Custom callbacks contribute additively and retain exact targets supplied by the host.</p>\n<p>An explicitly selected descriptor resolves through its access registry. Unknown IDs, unavailable loaders, invalid factories, descriptor/manifest mismatches, and callback failures produce a contained report error under the selected collection identity.</p>\n<h2>Current Registrations</h2>\n<table>\n<thead>\n<tr>\n<th>ID</th>\n<th>Server aspects</th>\n<th>Browser access and contribution</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>analysis_tags</code></td>\n<td>settings normalization, manifest projection, document groups, browser composition, <code>analysis_tag_fields</code> transfer validation over <code>group</code></td>\n<td>Manage group filter using projected ordered groups and row group values</td>\n</tr>\n<tr>\n<td><code>analysis_works</code></td>\n<td>empty settings normalization, read-only <code>folder_path</code>/<code>work_id</code>/<code>series_id</code> subject declaration, and editorial role in <code>dotlineform_projects_to_analysis_works</code> lineage</td>\n<td>No custom browser descriptor, assignable group, or contribution; uses the shared reader</td>\n</tr>\n<tr>\n<td><code>dotlineform_projects</code></td>\n<td>settings normalization, manifest projection, metadata projection and assignment normalization, import front matter, <code>authoring_subject</code> over <code>folder_path</code>/<code>work_id</code>/<code>series_id</code>, Working role in <code>dotlineform_projects_to_analysis_works</code> lineage, browser composition</td>\n<td>Manage Folder/Work/Series/None and invalid subject state, exact-detail read-only Subject information, capability-gated <strong>Assign subject</strong>, one highest Editorial/Published row cue with exact child detail, and Folder-only <strong>Open in Finder</strong></td>\n</tr>\n</tbody>\n</table>\n<p>Analysis Tags and Projects declare Manage browser composition. Analysis Works deliberately declares none, so its Manage and public projections use the shared reader without custom controls. All public projections retain the shared public manifest and by-ID boundary. <code>studio/default</code> remains the default-only reference.</p>\n<h2>Access Boundary</h2>\n<p>Python <code>browser_composition.accesses</code> is the access authority. Builder descriptor projection, manifest projection, and JavaScript loader registration agree with it. Public code imports the shared report and public registry; Manage code owns the default contribution, management registries, services, actions, and mutation-capable factories.</p>\n<p>Current public descriptors and custom manifest namespaces are empty for Analysis Tags and Projects. Public rows retain their ordinary public fields and routes. Manage settings, normalized subjects, private associations, field declarations, metadata, and controls remain in local server/Manage owners.</p>\n<h2>Adding Or Extending A Registration</h2>\n<ol>\n<li>Confirm the requirement is additive to the default membership, list/detail, navigation, and Manage behavior.</li>\n<li>Add or extend one registered definition with a stable ID, strict settings normalizer, and the smallest aspect records.</li>\n<li>Register a lazy browser factory only for each declared browser access.</li>\n<li>Configure the exact sub-scope with <code>{id, settings}</code> and rebuild its generated projections.</li>\n<li>Inspect the default collection, descriptor, public/Manage manifests, by-ID payloads, exact targets, access registries, and report host.</li>\n<li>Add focused pure/service, builder, registry-boundary, module, and route coverage proportional to the new behavior.</li>\n</ol>\n<p>For assignable workflows, the registered <code>DocsSubScopeAssignableFieldGroup</code> is the availability and server field authority. The browser consumes its projected group ID; <code>POST /docs/assign-field-group</code> resolves the exact collection again, accepts only the registered server-owned fields, requires the current source revision, and performs the exact atomic source write and sub-scope rebuild. The focused collection contribution owns value acquisition and response validation. A registration's read-only <code>DocsSubScopeAuthoringSubjectAspect</code> enables Folder meaning for that exact collection without granting assignment; raw <code>work_id</code> and <code>series_id</code> continue to use the collection-independent subject reader. For transfer workflows, <code>DocsSubScopeTransferAspect</code> remains server-side, validates an explicitly matching contract against target settings, and cannot own shared subject fields.</p>\n<h2>Code Authority</h2>\n<table>\n<thead>\n<tr>\n<th>Concern</th>\n<th>Authority</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Config validation and configured collection model</td>\n<td><code>docs-viewer/services/docs_scope_config.py</code></td>\n</tr>\n<tr>\n<td>Definition registry, aspects, settings, access, and safe descriptor projection</td>\n<td><code>docs-viewer/services/docs_subscope_customisations.py</code></td>\n</tr>\n<tr>\n<td>Private exact Working-to-Editorial lineage table</td>\n<td><code>docs-viewer/services/docs_document_publication_lineage.py</code> and <code>docs-viewer/data/canonical/document-publication-lineage.json</code></td>\n</tr>\n<tr>\n<td>Normalized subject state, generation receipts, and private associations</td>\n<td><code>docs-viewer/services/docs_document_subjects.py</code></td>\n</tr>\n<tr>\n<td>Projects validation, metadata, manifest, and import adapters</td>\n<td><code>docs-viewer/services/docs_dotlineform_projects_customisation.py</code></td>\n</tr>\n<tr>\n<td>Exact assignable-field-group mutation</td>\n<td><code>docs-viewer/services/docs_management_mutations.py</code>, <code>docs_management_mutation_service.py</code>, and <code>docs_management_service.py</code></td>\n</tr>\n<tr>\n<td>Manifest and by-ID generation</td>\n<td><code>docs-viewer/build/docs_builder/sub_scope.py</code></td>\n</tr>\n<tr>\n<td>Bounded external-local private-product reads</td>\n<td><code>docs-viewer/services/docs_generated_reads.py</code></td>\n</tr>\n<tr>\n<td>Browser config projection</td>\n<td><code>docs-viewer/build/docs_builder/browser_config.py</code></td>\n</tr>\n<tr>\n<td>Browser descriptor normalization and group availability</td>\n<td><code>site/docs-viewer/runtime/js/shared/docs-viewer-config-controller.js</code></td>\n</tr>\n<tr>\n<td>Shared list/detail engine</td>\n<td><code>site/docs-viewer/runtime/js/shared/docs-subscope-report.js</code></td>\n</tr>\n<tr>\n<td>Public customisation registry</td>\n<td><code>site/docs-viewer/runtime/js/shared/docs-subscope-customisation-registry.js</code></td>\n</tr>\n<tr>\n<td>Manage customisation registry</td>\n<td><code>docs-viewer/runtime/js/management/docs-viewer-management-subscope-customisation-registry.js</code></td>\n</tr>\n<tr>\n<td>Manage default/custom composition</td>\n<td><code>docs-viewer/runtime/js/management/docs-viewer-management-subscope-default-contribution.js</code> and <code>docs-viewer-management-subscope-composition.js</code></td>\n</tr>\n<tr>\n<td>Projects subject Info, action, and modal</td>\n<td><code>docs-viewer/runtime/js/management/docs-viewer-management-subscope-dotlineform-projects.js</code> and <code>docs-viewer-management-project-subject-modal.js</code></td>\n</tr>\n<tr>\n<td>Browser subject-object validation</td>\n<td><code>docs-viewer/runtime/js/management/docs-viewer-management-document-subject.js</code></td>\n</tr>\n<tr>\n<td>Exact managed target resolution</td>\n<td><code>docs-viewer/runtime/js/management/docs-viewer-management-document-target.js</code> and <code>docs-viewer/services/docs_management_document_target.py</code></td>\n</tr>\n<tr>\n<td>Exact collection transfer planning and receipt</td>\n<td><code>docs-viewer/services/docs_document_transfer.py</code></td>\n</tr>\n</tbody>\n</table>\n<h2>Verification Reference</h2>\n<p>Use an isolated writable <code>DOTLINEFORM_PROJECTS_BASE_DIR</code> for checks that import the complete scope registry.</p>\n<pre><code class=\"language-bash\">python -m pytest \\\n  docs-viewer/tests/python/test_docs_document_subjects.py \\\n  docs-viewer/tests/python/test_docs_dotlineform_projects_customisation.py \\\n  docs-viewer/tests/python/test_docs_subscope_customisations.py \\\n  docs-viewer/tests/python/test_docs_scope_config.py \\\n  docs-viewer/tests/python/test_build_docs_subscopes.py \\\n  docs-viewer/tests/python/test_docs_viewer_public_runtime_boundaries.py -q\n\npython docs-viewer/tests/smoke/docs_viewer_subscope_report_modules.py \\\n  --site-root .\n\npython admin-app/commands/run_checks.py --profile docs-viewer-smoke\n</code></pre>\n<p>After documentation changes, rebuild the affected Studio document and search projections. Finish with source lint for the changed implementation files, JSON/tree/search inspection, <code>git diff --check</code>, and a bounded diff review.</p>\n"
}
