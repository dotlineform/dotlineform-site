{
  "doc_id": "d-20260805-141842-8115c8",
  "title": "SSP-2.0 — Project State Sanity Note",
  "added_date": "2026-08-05 14:18:42",
  "last_updated": "2026-08-05 18:25:27",
  "viewer_url": "/docs/?scope=studio&doc=d-20260805-141842-8115c8",
  "parent_id": "d-20260801-133318-15e8fa",
  "summary": "confirm the bounded fit, direct-project-folder inventory, live scan timing, owners, risks and local-only boundary for Project State",
  "ui_status": "done",
  "content_html": "<h1>SSP-2.0 — Project State Sanity Note</h1>\n<h2>Outcome</h2>\n<p>SSP-2 fits the shipped SSP-1 subject products, canonical catalogue readers, local Docs Viewer report mechanism, exact local-folder links, and generated-product writers as one Project-specific producer plus one versioned lookup. The existing owners support the specified report and lookup without a reporting framework, generic graph, event bus, repair system, catalogue-reader replacement, result cache, or image-audit migration. The checkpoint is complete at its review gate and stopped before SSP-2.1 approval.</p>\n<p>The sanity pass resolved one row-boundary contradiction. Project State rows are the immediate project directories under <code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code>. Nested directories remain contents of their owning project row. The Work projection reads only <code>work_id</code>, <code>project_folder</code>, and <code>series_ids</code>; <code>project_subfolder</code>, <code>project_filename</code>, and exact media resolution remain catalogue/media organization. This matches the 206-row planning snapshot, all 193 current Folder association keys, and the existing Studio audit's default scan boundary. A recursive walk currently finds 484 directories and would turn 278 internal directories into additional project rows.</p>\n<h2>Closeout Note</h2>\n<p>The completed SSP-2 delivery supersedes this sanity note's tentative post-commit follow-through plan. SSP-2 closes without Studio or Docs mutation hooks. SSP-3 owns refreshing the shipped Project State producer once before a selected Manage build consumes Project Resolver Tokens; individual token expansion remains a one-row lookup read.</p>\n<h2>Accepted Inputs And Scheduling</h2>\n<p>SSP-1 is complete and currently publishes matching <code>subject_generation</code> receipts in the private Projects Manage manifest and <code>docs_subject_associations_v1</code> product. The observed generation contains 241 documents, 193 exact Folder keys, and 194 total subject associations. Project State consumes only the 193 Folder associations whose exact key matches the immediate-child scan.</p>\n<p>The current canonical inputs contain 1,940 Works and 143 Series. Every Work has <code>project_folder</code>; the current direct-project inventory matches 144 Work folder keys, including two folders with Works in more than one Series. Eight Works currently use <code>project_folder: wa</code> with <code>project_subfolder: ink</code>; all eight physical images are present below <code>projects/wa/ink/</code>, and all eight retain the <code>projects/wa</code> Project State relationship because <code>project_subfolder</code> is absent from the Project State key and input projection. Their Series <code>140</code> membership is then counted from <code>series_ids</code>. Work identity and current <code>series_ids</code> remain explicit in Project State, while catalogue status, media filenames, and presentation URLs retain their existing catalogue owners.</p>\n<p>SSP-3 depends on the versioned folder lookup accepted in SSP-2.1. The Folder-first report remains an independently useful SSP-2.2 checkpoint; Series grouping, search, sorting, and clipboard transforms remain SSP-2.3. The completed closeout transfers one relevant-build refresh to SSP-3 instead of adding committed-mutation hooks. The current image audit remains SSP-6-owned.</p>\n<h2>Timing And Mount Decision</h2>\n<p>On 2026-08-05, eleven read-only trials over the real immediate-child inventory returned 206 rows with a 0.725 ms median and 0.934 ms maximum scan time. Seven read-only trials that scanned those rows and loaded the current Manage manifest, subject associations, Works, and Series inputs took a 10.767 ms median and 12.464 ms maximum. These are warm local measurements of the bounded inputs rather than an implemented producer benchmark.</p>\n<p>Automatic run-on-mount is suitable at the observed scale. SSP-2.1 retains deterministic validation and atomic lookup publication as the likely material work; SSP-2.2 must recheck end-to-end run timing before accepting mount behavior.</p>\n<h2>Current Owners And Blast Radius</h2>\n<ul>\n<li>The Projects sub-scope builder owns the private Manage manifest, normalized authoring subjects, matching generation receipt, and exact subject associations. SSP-2 reads those generated products as one generation.</li>\n<li>Canonical catalogue source readers own Work and Series identity. SSP-2 builds a focused independent join and retains semantic identities rather than copying catalogue titles, URLs, or status into the lookup.</li>\n<li>The local report registry, allowlisted loader map, report service, and ordinary document metadata own report mounting. SSP-2 adds one registered local report and one focused service operation through those seams.</li>\n<li>The local-link service owns normalized <code>projects/&lt;project-directory&gt;</code> activation. Exact Docs Viewer document targets and Semantic Tokens catalogue targets retain link composition.</li>\n<li>Existing source-write/rebuild and catalogue commit paths retain canonical mutations without Project State hooks. SSP-3 later refreshes the derived lookup immediately before relevant Manage-build consumption.</li>\n<li>The public report projection currently carries public report entries only. The Project State host, input products, lookup, service operation, and folder activation remain in Manage/local products.</li>\n</ul>\n<p>The broad blast radius therefore covers immediate project-folder inventory and reconciliation, canonical Works/Series reads, matching Projects products, one versioned lookup and stale marker, report registry/loader/service composition, the live Folder/Series/Docs response, browser projections and clipboard transforms, exact local/document/Series links, committed Work and Project-document impact classification, focused evidence, and concise durable owner updates.</p>\n<h2>Risks And Required Evidence</h2>\n<p>The principal risks are joining mismatched subject generations, allowing recorded-only paths or nested directories to establish rows, making a document mediate the Work join, double-counting multi-Series Works, copying catalogue presentation into the resolver lookup, returning live rows from a partially published generation, replacing a complete lookup after failure, and making a canonical mutation depend on derived follow-through.</p>\n<p>SSP-2.1 must prove immediate-child inventory ownership, Project-document exact-key joins, Work keys projected solely from <code>project_folder</code>, subfolder-resident Work retention, no Project State impact from <code>project_subfolder</code> or <code>project_filename</code> changes, receipt agreement, semantic Work/Series retention, multi-Series counts, deterministic ordering, validated live/lookup generation, stale-state retention, and public isolation. Later checkpoints retain their delivery-owned report, browser-projection, interaction, and mutation evidence rather than expanding SSP-2.1.</p>\n<h2>Stop Condition</h2>\n<p>Stop before or during implementation if the outcome requires recursive project rows, a detail-level <code>project_subfolder</code> report, another report mechanism, a generic relationship graph or query service, an event bus, automatic repair, a rewritten catalogue reader, persisted report presentation, a result cache, public Project State products, or partial migration of the Studio image audit. Any such result changes the accepted boundary and requires a separate review.</p>\n"
}
