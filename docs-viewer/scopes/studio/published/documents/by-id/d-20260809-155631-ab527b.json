{
  "doc_id": "d-20260809-155631-ab527b",
  "title": "CII-1 — Catalogue Image Source Insertion Delivery",
  "added_date": "2026-08-09 15:56:31",
  "last_updated": "2026-08-09 16:34:15",
  "viewer_url": "/docs/?scope=studio&doc=d-20260809-155631-ab527b",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "add exact Work and Series images to Docs source with current image presentation controls and Catalogue destinations",
  "ui_status": "in-progress",
  "content_html": "<h1>CII-1 — Catalogue Image Source Insertion Delivery</h1>\n<h2>Status</h2>\n<p>CII-1.1 is complete at its exact image-projection and rendering gate. The active standalone delivery remains directly under the <a href=\"/docs/?scope=studio&amp;doc=d-20260428-000000-f5ff18\">Roadmap</a> and is waiting for explicit approval before the CII-1.2 source-editor action. The <strong>🏞️ Add Catalogue image</strong> action has not been implemented.</p>\n<h2>Complete Result</h2>\n<p>The Docs source editor adds <strong>Add Catalogue image</strong> beside <strong>Add image</strong> and <strong>Add catalogue token</strong>. An author searches the existing mixed Catalogue target set, selects an exact published Work or Series, applies the current image presentation options, and inserts one semantic Catalogue image occurrence into the source buffer.</p>\n<p>After the ordinary source save and rebuild, the occurrence displays the target's current Catalogue image. Clicking the image opens the exact Catalogue destination: a Work image opens that Work, while a Series image opens that Series even though its displayed image comes from the Series' primary Work.</p>\n<p>This is one source-authoring and rendering workflow. CII-1.0 selected rebuild-time resolution because canonical source should retain semantic identity and authored presentation rather than a detached image URL. The ordinary save and rebuild remains the only author-facing flow; there is no separate mode, phase, or concurrency requirement.</p>\n<h2>Accepted Contract</h2>\n<h3>Identity, Image, And Destination</h3>\n<p>The source occurrence preserves three distinct meanings:</p>\n<ul>\n<li>semantic identity is the selected Catalogue family, target type, and canonical target id;</li>\n<li>image presentation is resolved from the exact target lookup row; and</li>\n<li>activation uses that same row's exact Catalogue destination.</li>\n</ul>\n<p>For a Work, the image projection comes from that Work's current primary Catalogue image and the destination remains the Work page.</p>\n<p>For a Series, the image projection comes from the Work identified by the Series' exact <code>primary_work_id</code>, while semantic identity and destination remain the Series. The image source must not retarget the occurrence to the primary Work.</p>\n<p>Only current published lookup targets are selectable. Missing image data, a missing or invalid Series <code>primary_work_id</code>, a primary Work that is not a current Series member, or an unusable destination fails closed. The workflow does not fall back to the first member, infer identity from title or route, or choose another image.</p>\n<h3>Source And Rendering</h3>\n<p>The new occurrence belongs to the existing Catalogue semantic family but has an explicit image presentation. Existing text Catalogue tokens keep their current grammar and rendering.</p>\n<p>Canonical source stores the exact target identity and authored presentation values rather than copying Catalogue media into the Docs scope. Rebuild resolves the browser-safe image and destination through the existing generated target lookup and emits a linked image or linked semantic figure.</p>\n<p>The lookup remains the single focused projection shared by authoring and rendering. Extending its Work and Series rows with an allowlisted image projection must not expose canonical records, filesystem paths, provider credentials, or unrelated media metadata.</p>\n<p>The occurrence contributes the same exact Catalogue identity to semantic usage and broken-link behavior as a text occurrence. Image presentation does not create a second relationship family or usage index.</p>\n<h3>Authoring Surface</h3>\n<p><strong>Add Catalogue image</strong> reuses the current mixed Work and Series search, result identity, keyboard navigation, and exact selection contract from <strong>Add catalogue token</strong>.</p>\n<p>Its toolbar glyph is <strong>🏞️</strong>, with <strong>Add Catalogue image</strong> retained as the visible tooltip and accessible label.</p>\n<p>After target selection it provides the current <strong>Add image</strong> presentation values:</p>\n<ul>\n<li>required alt text;</li>\n<li>optional caption;</li>\n<li>optional summary when a caption is present;</li>\n<li>full-column, image-left, or image-right placement; and</li>\n<li>fill available width or natural width.</li>\n</ul>\n<p>Target selection may suggest the current Catalogue title for alt text and caption, but those values are authored occurrence snapshots. A later Catalogue title change does not rewrite source text automatically.</p>\n<p>Confirmation inserts at the captured source selection, returns focus, and uses the normal dirty state. It does not save or rebuild independently, publish media, or mutate Catalogue data.</p>\n<p>Placing the caret inside an image occurrence opens the Catalogue Info view with immutable target identity and editable presentation fields. Update and Remove retain the existing exact-range stale guard; changing the Work or Series target remains Remove followed by <strong>Add Catalogue image</strong>.</p>\n<h2>Delivery Boundary</h2>\n<p>CII-1 includes:</p>\n<ul>\n<li>one manage-only <strong>Add Catalogue image</strong> source action;</li>\n<li>exact Work and Series target selection through the current Catalogue picker;</li>\n<li>one focused semantic image occurrence and renderer without changing text-token behavior;</li>\n<li>allowlisted Work and Series image fields in the existing generated target lookup;</li>\n<li>linked plain-image and semantic-figure output using the existing image presentation classes and validation;</li>\n<li>Catalogue Info inspection, presentation update, and removal for the image occurrence;</li>\n<li>existing semantic usage and broken-link follow-through for the exact Catalogue identity;</li>\n<li>focused parser, lookup, rendering, source-adapter, and contribution evidence; and</li>\n<li>current durable UI and target-lookup documentation at closeout.</li>\n</ul>\n<p>CII-1 excludes:</p>\n<ul>\n<li>uploading, copying, replacing, resizing, or publishing Catalogue media;</li>\n<li>changing Work media, Series membership, <code>primary_work_id</code>, or Catalogue save validation;</li>\n<li>selecting a Series member other than its exact primary Work;</li>\n<li>Moment targets or another semantic family;</li>\n<li>retroactively converting existing text tokens, Markdown images, or figures;</li>\n<li>a generic media-token framework, second Catalogue lookup, compatibility grammar, or fallback reader;</li>\n<li>automatic source rewriting when Catalogue titles, images, or Series primary Works change;</li>\n<li>public Catalogue route redesign or Docs image-layout redesign; and</li>\n<li>background work, receipts, locking, or multi-user concurrency machinery.</li>\n</ul>\n<h2>Likely Owners To Inspect</h2>\n<p>Implementation should begin at the current ownership seams and narrow the exact changed set during CII-1.0:</p>\n<ul>\n<li>the semantic-token registry, generated target-lookup builder, and Catalogue Work/Series source adapters for the allowlisted image projection;</li>\n<li>the semantic-token parser, renderer, usage projection, and broken-link owner for the visual occurrence;</li>\n<li>the Catalogue target picker and source-action contribution for exact selection and insertion;</li>\n<li>the existing staged-image presentation and figure-fragment owners for shared validation and output classes without invoking staged-media publication; and</li>\n<li>the focused semantic-token, target-lookup, staged-media-fragment, source-editor module, and builder tests.</li>\n</ul>\n<p>This is an inspection route, not a frozen file inventory. Stop and re-scope if the result requires copied media, a new provider abstraction, a second identity registry, a generic token engine, or changes to Catalogue write authority.</p>\n<h2>CII-1.0 Checkpoint Record</h2>\n<p>CII-1.0 passed its documentation and code sanity gate on 2026-08-09. The result is coherent against the current semantic-token, Catalogue publication, source-editor, image-fragment, Docs rendering, and Manage/public link boundaries. No production source, generated target lookup, Catalogue record, media object, or public route changed.</p>\n<h3>Frozen Source Form</h3>\n<p>Existing text occurrences remain byte-compatible:</p>\n<pre><code class=\"language-text\">[[catalogue:work:00638|3 symbols]]\n</code></pre>\n<p>An image occurrence adds an explicit <code>image</code> presentation segment and one canonical RFC 3986 percent-encoded field string:</p>\n<pre><code class=\"language-text\">[[catalogue:image:work:00638|alt=3%20symbols]]\n[[catalogue:image:series:005|alt=3%20symbols&amp;caption=3%20symbols&amp;summary=intangible%0Ashifting%20boundaries&amp;placement=left&amp;fill_width=true]]\n</code></pre>\n<p>The identity is <code>catalogue:image:&lt;target type&gt;:&lt;canonical target id&gt;</code>. Fields serialize once in the order <code>alt</code>, optional <code>caption</code>, optional <code>summary</code>, <code>placement</code>, and <code>fill_width</code>. <code>alt</code> is always required. Caption absence produces a linked plain image; caption presence requires <code>placement</code> and explicit boolean <code>fill_width</code>, while <code>summary</code> remains optional. Text normalization, allowed placement values, and HTML escaping reuse the current staged-image presentation contract.</p>\n<p>The parser rejects unknown, duplicate, empty, non-canonical, or contradictory fields. It recognizes the complete occurrence as one exact source range outside fenced code, inline code, and HTML comments. Existing three-part text parsing and serialization do not reinterpret the four-part form.</p>\n<p>The visual occurrence's usage-index <code>title</code> is its caption when present and otherwise its alt text. The current usage-index row shape remains sufficient because relationship identity, source range, raw source, title, and destination are unchanged; image presentation does not enter the relationship schema.</p>\n<h3>Frozen Lookup And Rendering Contract</h3>\n<p>The existing generated file cuts over in place to <code>docs_semantic_token_target_lookup_v2</code>; there is no v1 reader, compatibility writer, second lookup, or fallback. Every current published target keeps its existing identity, title, <code>href</code>, and metadata. An optional <code>image</code> object adds only one browser-safe <code>src</code>.</p>\n<p>Work <code>image.src</code> is the public 1600-pixel primary WebP URL assembled from <code>site-tools/config/site-tools.json</code>, <code>_data/pipeline.json</code>, the canonical Work id, and positive <code>media_version</code>, including the existing <code>?v=&lt;media_version&gt;</code> cache version. A Work row without complete published media remains a valid text target but has no image projection.</p>\n<p>Series <code>image.src</code> is copied from the exact canonical Work named by <code>primary_work_id</code> only when that Work exists, is published, belongs to the Series through <code>series_ids</code>, and has a valid Work image projection. The Series row retains its Series identity and <code>/series/?series=&lt;id&gt;</code> destination. Missing or invalid image data never removes an otherwise valid text target and never falls back to another member.</p>\n<p>The image picker lists only rows with a valid image projection. A visual source occurrence whose target later loses its image remains source text, emits no resolved usage, and appears in Docs Broken Links as <code>missing_image</code>; text occurrences for the same target continue to resolve normally.</p>\n<p>Rendering emits one escaped semantic anchor with the existing target identity data attributes, <code>_blank</code>/<code>noopener noreferrer</code>, and either a plain image or the current <code>docsViewerFigure</code> classes. Manage rebases that anchor through the existing public-preview link mount; public Docs use the same root-relative Catalogue destination. A focused linked-image wrapper extends the current figure selectors for width, natural-width, left/right placement, mobile stacking, and keyboard focus without redesigning the layout.</p>\n<p>The source occurrence is not rewritten when the target image, title, or Series primary Work changes. A later target-lookup refresh plus ordinary Docs rebuild updates rendered image and destination projection; CII-1 adds no background rebuild or synchronization owner.</p>\n<h3>Current Data And Baseline</h3>\n<p>The current lookup is unchanged at 2,074 targets: 1,935 Works and 139 Series. All 1,935 published Works currently have a filename, positive media version, and pixel dimensions. All 139 published Series currently have an exact published primary Work that belongs to the Series and has valid media. These counts prove the present canonical dataset, not a permanent requirement that every future text target carry an image.</p>\n<p>The public Work route already constructs <code>https://media.dotlineform.com/works/img/&lt;work id&gt;-primary-1600.webp?v=&lt;media version&gt;</code> from the same public-media and pipeline settings. CII-1 reuses that contract; it does not fetch a served URL or verify remote bytes during lookup generation.</p>\n<p>Baseline evidence passed before implementation:</p>\n<ul>\n<li>29 focused Python checks across target lookup, semantic rendering/usage, Broken Links, and staged-image fragments;</li>\n<li>the focused Catalogue-token browser module smoke, including mixed Work/Series search, exact-range update/remove, keyboard behavior, public-preview link rebasing, and source-adapter guards;</li>\n<li>target-lookup dry-run unchanged with 2,074 rows; and</li>\n<li>a canonical-data audit with zero published Work media gaps, zero published Series primary-work gaps, and no existing four-part Catalogue image occurrence.</li>\n</ul>\n<p>The first Python attempt stopped during collection because <code>.env.local</code> resolved an external Projects workspace unavailable to the sandbox. Repeating the same checks with an isolated readable/writable Projects root passed; this was environment setup, not product evidence.</p>\n<h3>Exact Proposed Change Set</h3>\n<p>CII-1.1 production changes are limited to the semantic-token registry lookup-field declaration, target-lookup builder, semantic occurrence parser/renderer, Broken Links classifier, browser lookup normalizer, and shared Docs Viewer figure stylesheet. The existing site-tools and pipeline configs, Catalogue source/publication validators, staged-media publication service, image-fragment validation, public Catalogue routes, and semantic usage artifact writer are reused without changing ownership.</p>\n<p>CII-1.1 focused evidence updates the Catalogue contract fixture plus target-lookup, semantic rendering/usage, Broken Links, and browser target-normalization checks. The generated target lookup is written only after its v2 dry run is reviewed.</p>\n<p>CII-1.2 adds focused <code>catalogue-image-modal.js</code> and <code>catalogue-image-contribution.js</code> owners, extracts the current private image-presentation controls into one source-editor presentation module reused by staged <strong>Add image</strong>, and extends the existing family parser, Catalogue Info view, action definitions, hosted control registry, and Manage composition. The toolbar order becomes <strong>Add image</strong> (<code>🧜‍♀️</code>), <strong>Add Catalogue image</strong> (<code>🏞️</code>), <strong>Add file</strong> (<code>📎</code>), then <strong>Add catalogue token</strong> (<code>📍</code>).</p>\n<p>CII-1.2 focused evidence extends the existing Catalogue-token module smoke and management composition checks. It does not add a second modal shell, picker, source adapter, token registry, or browser-choreography-only permanent suite.</p>\n<h3>Risks And Stop Conditions</h3>\n<p>The main risks are cross-language source serialization drift, accidentally making image eligibility gate text links, unsafe decoded occurrence fields, Series-to-Work identity leakage, linked-wrapper CSS changing ordinary figures, and stale Manage links using the local service origin. The frozen source fixture, optional image row, exact Series validation, escaping checks, targeted selectors, and existing public-preview mount each own one of those risks.</p>\n<p>Stop and re-scope if implementation requires copied Catalogue bytes, remote-object verification during lookup generation, a second lookup or registry, a generic presentation engine, text-token migration, Catalogue write changes, automatic Docs rebuild after Catalogue saves, or a layout redesign. Actual remote image loading and tactile modal/figure fit remain CII-1.3 integration evidence, not claims made at this checkpoint.</p>\n<h2>CII-1.1 Checkpoint Record</h2>\n<p>CII-1.1 passed its focused implementation gate on 2026-08-09. The target lookup cut over in place to v2, the visual source occurrence now resolves during the ordinary Docs build, and no CII-1.2 authoring control or modal was added.</p>\n<p>The generated lookup remains one allowlisted projection with 2,074 unique targets. All 1,935 current Work rows and 139 current Series rows have one HTTPS <code>image.src</code>; an independent assertion confirmed every Work URL against its own canonical id and media version, and every Series URL against only its exact published member named by <code>primary_work_id</code>. Focused fixtures also prove that an otherwise valid text target remains present without <code>image</code> when complete media or exact Series membership is unavailable.</p>\n<p>The Python parser accepts the frozen plain-image and figure forms as one exact occurrence, requires canonical RFC 3986 field encoding and ordering, and rejects duplicate, unknown, empty, contradictory, non-canonical, or incomplete fields. It remains inactive in fenced code, inline code, and HTML comments. Resolved output is an escaped semantic anchor around either a plain image or the existing semantic figure classes; caption-or-alt continues to supply the unchanged usage-index v1 <code>title</code>. A target without <code>image</code> remains literal, emits no usage row, and is classified as <code>missing_image</code>, while the same target's text occurrence still resolves.</p>\n<p>The browser lookup reader now requires v2, preserves only a safe HTTPS or root-relative image source, and has no v1 fallback. Linked-image CSS is limited to the new plain-image and figure-anchor wrappers while retaining existing ordinary image and figure selectors, including right placement, natural width, and public mobile stacking. Manage destination rebasing continues through the existing semantic-anchor mount.</p>\n<p>Focused evidence passed:</p>\n<ul>\n<li>18 lookup, parser, renderer, usage-index, contract-fixture, and Broken Links Python checks;</li>\n<li>2 public Docs payload regression checks;</li>\n<li>explicit Python lint across 11 changed owners/tests and JavaScript lint for the browser lookup normalizer;</li>\n<li>the Catalogue-token browser module smoke, including v2 image safety and linked-image computed CSS;</li>\n<li>the sub-scope report browser smoke for the shared lookup-reader cutover;</li>\n<li>post-write target-lookup dry run unchanged at 2,074 rows; and</li>\n<li>independent bounded-diff and whitespace review with no source-editor action, Catalogue write owner, staged-media publication owner, public route, or semantic usage schema change.</li>\n</ul>\n<p>There are no existing <code>catalogue:image</code> source occurrences, so this checkpoint does not claim an authored end-to-end browser flow or remote image load. Those remain CII-1.2 and CII-1.3 evidence respectively.</p>\n<h2>Delivery Steps</h2>\n<h3>CII-1.0 — Sanity And Source Contract Gate</h3>\n<ul>\n<li>[x] Compare the complete result with the current Catalogue lookup, semantic-token grammar/rendering, source-action contribution, image-presentation fragment, and Work/Series image owners.</li>\n<li>[x] Confirm the smallest unambiguous source representation for one Catalogue image occurrence while leaving existing text tokens byte-compatible and separately editable.</li>\n<li>[x] Freeze the allowlisted lookup image shape, exact Work and Series resolution rules, authored occurrence fields, unresolved behavior, and linked output contract.</li>\n<li>[x] Confirm the likely blast radius across generated lookup refresh, source insertion, Docs rebuild, semantic usage, broken links, Manage rendering, and public rendering.</li>\n<li>[x] Record the bounded production owners, focused evidence, risks, and stop conditions, then stop for implementation approval.</li>\n</ul>\n<p><strong>Gate:</strong> Documentation completion is not product implementation. Stop before CII-1.1 until the source and projection contract is reviewed.</p>\n<h3>CII-1.1 — Exact Image Projection And Rendering</h3>\n<ul>\n<li>[x] Extend the existing target lookup with the minimum browser-safe image projection for eligible Works and Series.</li>\n<li>[x] Resolve Series presentation only through its exact valid <code>primary_work_id</code>, while preserving the Series identity and destination.</li>\n<li>[x] Parse and render the focused Catalogue image occurrence as a linked plain image or linked semantic figure using current presentation validation and classes.</li>\n<li>[x] Preserve exact semantic usage and broken-link evidence for resolved and unresolved visual occurrences without changing text-token output.</li>\n</ul>\n<p><strong>Gate:</strong> Focused lookup, parser, renderer, usage, and broken-link evidence passes; independently review the bounded diff before the source-editor action begins.</p>\n<h3>CII-1.2 — Add Catalogue Image Authoring</h3>\n<ul>\n<li>[ ] Register <strong>Add Catalogue image</strong> as a separate manage-only source action using the existing action-contribution boundary.</li>\n<li>[ ] Reuse the mixed Catalogue picker, then collect alt text, caption, summary, placement, and fill-width values through the current image-presentation contract.</li>\n<li>[ ] Insert the serialized occurrence through the captured-selection adapter and preserve cancellation, focus return, dirty-state, save, and rebuild behavior.</li>\n<li>[ ] Extend Catalogue Info to inspect, update, and remove the image occurrence through the existing immutable-identity and stale-range contract.</li>\n<li>[ ] Keep <strong>Add image</strong> and <strong>Add catalogue token</strong> unchanged and independently available.</li>\n</ul>\n<p><strong>Gate:</strong> Focused source-action and adapter evidence passes, followed by manual review of keyboard selection, formatting controls, cancellation, insertion, and focus behavior.</p>\n<h3>CII-1.3 — Integration And Closeout</h3>\n<ul>\n<li>[ ] Verify one Work and one Series from source insertion through rebuilt Manage output, including exact image, exact click destination, presentation variants, and semantic usage.</li>\n<li>[ ] Verify draft/unpublished targets and missing or invalid image projections are unavailable or unresolved without fallback.</li>\n<li>[ ] Confirm public-safe rendering uses only projected browser data and no local service or canonical Catalogue source.</li>\n<li>[ ] Run the smallest relevant source lint, focused Python and JavaScript checks, generated lookup and Docs projection assertions, and whitespace review.</li>\n<li>[ ] Update <a href=\"/docs/?scope=studio&amp;doc=d-20260623-180405-631ef4\">Semantic Tokens Source Editor UI</a> and <a href=\"/docs/?scope=studio&amp;doc=d-20260726-185814-c5e581\">Target Lookup</a> with shipped behavior, then prepare the explicit retain-or-retire closeout recommendation.</li>\n</ul>\n<h2>Completion Evidence</h2>\n<ul>\n<li>A Work selected through <strong>Add Catalogue image</strong> rebuilds as its current image and opens that exact Work destination when clicked.</li>\n<li>A Series selected through the same action rebuilds with its exact primary Work's image but opens the exact Series destination and remains a Series semantic occurrence.</li>\n<li>Alt text, optional caption and summary, placement, and fill-width behavior match the current <strong>Add image</strong> presentation contract.</li>\n<li>Catalogue Info updates or removes one exact image occurrence without changing its Work or Series identity.</li>\n<li>Existing text Catalogue tokens and staged <strong>Add image</strong> behavior remain unchanged.</li>\n<li>The generated target lookup is the only Catalogue-to-Docs image projection and contains no private source or provider data.</li>\n<li>Missing, draft, stale, or malformed targets create no inferred image, destination, or relationship.</li>\n<li>Source insertion remains an ordinary local buffer mutation committed by the existing save and rebuild workflow.</li>\n</ul>\n<h2>Durable Documentation At Closeout</h2>\n<ul>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260623-180405-631ef4\">Semantic Tokens Source Editor UI</a> owns the shipped authoring, occurrence, formatting, and interaction contract.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260726-185814-c5e581\">Target Lookup</a> owns the shipped Work/Series image projection and exact resolution boundary.</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260523-190651-7157ec\">Development Checklist</a> changes only if implementation establishes a reusable guardrail not already recorded there.</li>\n</ul>\n"
}
