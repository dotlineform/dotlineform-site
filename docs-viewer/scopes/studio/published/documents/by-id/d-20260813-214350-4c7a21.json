{
  "doc_id": "d-20260813-214350-4c7a21",
  "title": "Docs Viewer Search Index",
  "added_date": "2026-08-13 21:43:50",
  "last_updated": "2026-08-14 13:03:37",
  "viewer_url": "/docs/?scope=studio&doc=d-20260813-214350-4c7a21",
  "parent_id": "d-20260602-160839-6d3cbb",
  "summary": "Durable structure, field policy, build ownership, ranking inputs, and extension boundary for Docs Viewer search indexes.",
  "content_html": "<h1>Docs Viewer Search Index</h1>\n<p>Docs Viewer uses one static inverted-index format for every configured parent scope. The browser receives one compact document table plus term-to-document posting lists rather than a second copy of each document body.</p>\n<h2>Payload Contract</h2>\n<p><code>docs-viewer/build/build_search.py</code> emits <code>docs_viewer_search_index_v2</code> with:</p>\n<ul>\n<li>a header containing the schema, scope, content version, generation time, and document count;</li>\n<li>an ordered <code>fields</code> list copied from the scope's <code>search_fields</code> policy;</li>\n<li>a deterministic <code>docs</code> table containing identity, title, route, update, parent, display context, and any structured sub-scope target; and</li>\n<li>a <code>terms</code> map whose field-specific sorted posting lists reference zero-based positions in <code>docs</code>.</li>\n</ul>\n<p>Each exact parent or <code>{sub_scope, doc_id}</code> child target occurs once; the same <code>doc_id</code> may independently identify a parent and a child in different collections. A term occurs once, and a document position occurs at most once in each term/field posting list. The content version excludes generation time, so an identical complete rebuild skips its write.</p>\n<p>Parent title, document identity, and update time may remain in the document table without emitting postings. Searchable fields are decided explicitly per scope:</p>\n<ul>\n<li>Studio: <code>title</code>, <code>heading</code>, <code>body</code>, and <code>code</code>;</li>\n<li>Analysis, Notes, Processing, and dotlineform: <code>title</code>, <code>parent_title</code>, <code>identity</code>, and <code>last_updated</code>.</li>\n</ul>\n<p>Studio therefore supports authored full-text retrieval while the other scopes retain metadata-only retrieval. Analysis publishes its accepted metadata-only index through its explicit public projection; enabling its body text remains a separate public vocabulary and payload decision.</p>\n<h2>Scope Corpus Composition</h2>\n<p>The selected-scope artifact contains eligible ordinary parent documents and eligible documents from registered sub-scopes. A child enters only from its collection's public-safe working <code>manifest.json</code>, with matching canonical source and by-ID identity, and when one exact eligible parent <code>docs_subscope</code> report host places that collection. A public parent scope additionally requires a public report host. Missing, stale, ambiguous, hidden, non-publishable, orphaned, and unplaced targets stop or remain outside the corpus rather than being inferred.</p>\n<p>Child rows retain <code>id</code> as <code>doc_id</code> and add exact <code>sub_scope</code>, <code>report_doc_id</code>, and display-only <code>collection_title</code>. The browser constructs the canonical parent URL from that structured identity and the active Manage or public route, preserving the child as <code>subdoc</code>; the compatible stored <code>href</code>, titles, filenames, selected report, and current UI state are not navigation authority. The existing compact <code>display_meta</code> line shows update time plus configured collection title without making collection structure searchable.</p>\n<p>The combined index remains one atomic artifact and one relevance corpus. It does not create per-sub-scope indexes, make collection structure searchable, or merge child documents into parent Index or Recent.</p>\n<h2>Studio Source Boundary</h2>\n<p>Studio full text is derived from canonical Markdown through existing owners. Validated report declarations are projected away, semantic references contribute their visible title, configured media tokens are removed, and the shared Markdown renderer separates non-duplicate headings, readable prose, inline code, and fenced code.</p>\n<p>The index excludes front matter, generated report hosts, link destinations, media paths, raw markup attributes, scripts, styles, complete document IDs, date fragments, and exact 64-character hexadecimal content digests. Image alt text and visible raw-HTML text remain searchable.</p>\n<h2>Query And Ranking</h2>\n<p><code>site/docs-viewer/runtime/js/shared/docs-viewer-search.js</code> is the sole browser postings, ranking, and tie-breaking owner. A multi-term query intersects its normalized term matches; prefix matching begins at three characters; each document appears at most once; ties sort by title and then identity.</p>\n<p>The generic score order is exact identity, exact title, title, heading, parent title, body/code, then update metadata. Only fields enabled by the selected scope participate. Studio's active order is therefore exact title, title, heading, then body/code; parent context is display-only.</p>\n<p>Short prefix searches can be broad after full-text adoption. For example, the accepted Studio <code>reg</code> query returns 145 documents while its two established title matches remain first. Snippets, highlighting, stemming, synonyms, typo correction, term-frequency scoring, phrase positions, section results, and deep links are not part of this index.</p>\n<h2>Build And Extension</h2>\n<p>Every search-relevant parent change rebuilds the complete selected-scope index. An eligible child content or membership change keeps document-payload work collection-targeted and then rebuilds the same complete parent Search index. Rollback recovery and the live watcher use the same child-Docs-then-parent-Search order. Public Delete removes one exact child target and remaps postings without deleting a same-ID parent or sibling-collection target. No build touches an unrelated scope.</p>\n<p>To add or change searchable content, update the owning scope's ordered <code>search_fields</code>, supply eligible source values through the shared extraction boundary, extend the focused Python/query contract, rebuild the complete scope index, and measure any public or unusually large payload. Do not add a second schema, tokenizer, cache, reader, query engine, or scope-name branch.</p>\n<p>Exact code and generated JSON remain the authority for current keys and values. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-214351-98d3f0\">Docs Viewer Search Tokenizer</a> owns term recognition. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-223337-e6c261\">Docs Viewer Search Relevance</a> owns proposed scoring. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-225547-cd82d8\">DSI-2 — Scope-Level Sub-Scope Search Delivery</a> retains combined-corpus and exact-route delivery evidence. <a href=\"/docs/?scope=studio&amp;doc=d-20260715-084611-c2fa6f\">SM-1 — Studio Full-Text Search Delivery</a> retains the first full-text implementation evidence.</p>\n"
}
