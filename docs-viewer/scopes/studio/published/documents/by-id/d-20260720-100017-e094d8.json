{
  "doc_id": "d-20260720-100017-e094d8",
  "title": "Docs Media Report Concept",
  "added_date": "2026-07-20 10:00:17",
  "last_updated": "2026-08-12 21:39:20",
  "viewer_url": "/docs/?scope=studio&doc=d-20260720-100017-e094d8",
  "parent_id": "d-20260720-100017-9855dd",
  "summary": "shipped local list of ready-media and build-source files with exact document references",
  "ui_status": "done",
  "content_html": "<h1>Docs Media Report Concept</h1>\n<h2>Purpose</h2>\n<p>Give authors one simple local report showing every ready-media and registered build-source file currently present in one selected Docs Viewer scope and the exact parent or sub-scope documents, if any, that reference it.</p>\n<p>The report is named <strong>Docs Media</strong> and is hosted by one manage-only report document in the Dotlineform scope. It defaults to Dotlineform data but can inspect another configured parent scope through one dropdown. Its presentation uses the same list-backed report format as <a href=\"/docs/?scope=dotlineform&amp;doc=d-20260805-155400-4c341f\">Project State</a>.</p>\n<h2>Exact Row Contract</h2>\n<p>One row represents one distinct exact media identity:</p>\n<pre><code class=\"language-text\">selected scope + media type + media identity\n</code></pre>\n<p>The row set comes only from the live provider-neutral listing of the selected scope's configured ready-media and registered build-source locations. For external-local or repository storage that means the files currently beneath those configured roots; for R2 it means the objects currently beneath the configured prefixes. The report uses both ready-media and build-source inventory items, but not a persisted media table, manifest, <code>media-source-evidence.json</code>, or missing-reference rows. An existing file therefore remains one row when no document references it.</p>\n<p>Each row collects the distinct exact documents that reference that listed identity. Repeated occurrences in one document are deduplicated, while several documents become several links in the same Documents cell. A referenced identity absent from the live file listing does not create a row. A build-source row does not inherit the documents referencing its derived ready-media output merely because the names match; without an exact direct reference its Documents cell remains blank. The server resolves exact parent and sub-scope document identity, title, Manage URL, media or build type, identity, role-specific local activation target, and any exact document references; the browser does not infer them from labels or the current report context.</p>\n<h2>Three-Column Surface</h2>\n<pre><code class=\"language-text\">┌ Docs Media ────────────────────────────────────────────────────┐\n│ Scope [Dotlineform ▾]  [↻]  [Search…                        ×] │\n├─────────┬────────────────────────────┬──────────────────────────┤\n│ Type ▲  │ File name                 │ Documents                │\n│ files   │ attachment-01.pdf         │ nerve (abattoir)         │\n│ img     │ image-01.webp             │ nerve (abattoir)         │\n│ img     │ image-02.webp             │                          │\n│ mermaid │ diagram-01.mmd            │                          │\n└─────────┴────────────────────────────┴──────────────────────────┘\n</code></pre>\n<p>This is the Project State list pattern: a report toolbar, sortable header buttons, list rows, and stacked links within a cell. It does not adopt Project State's grouping or copy controls. All three column headers are sortable. The default order is Type ascending and then File name ascending, preserving the order of the previous combined media path. Type sorting uses the exact configured media-type id with File name as its tie-breaker. File name sorting uses the exact relative identity with Type as its tie-breaker. Within each Documents cell, links are ordered by document title and then exact target. Documents sorting compares that ordered title set, places unreferenced rows after referenced rows in ascending order, and uses Type plus File name as its deterministic tie-breaker. Selecting the active column reverses its direction; selecting another column starts ascending. Scope and sort state use the existing report query-state vocabulary so reload and copy-link preserve the current view.</p>\n<h3>Type</h3>\n<p>Type displays the exact configured ready-media type or build-source type, such as <code>img</code>, <code>svg</code>, <code>html</code>, <code>files</code>, or <code>mermaid</code>. It comes from the inventory's canonical <code>media_type</code> field and is not inferred from the file extension. Therefore <code>build-source/mermaid/diagram-01.mmd</code> displays Type <code>mermaid</code>, not <code>build-source</code> or <code>mmd</code>. The cell is plain text.</p>\n<h3>File name</h3>\n<p>File name displays the exact relative identity without the ready-media type or <code>build-source/&lt;type&gt;</code> prefix, for example <code>example.webp</code>, <code>attachment.pdf</code>, <code>diagram-01.mmd</code>, or <code>sub-folder/example.webp</code>. It comes from the inventory's canonical <code>identity</code> field; the browser does not split or reconstruct identity from a combined display label. A build source is a registered workspace file, while a ready-media file is the managed artifact and a retained original plus optional source evidence remain separate concerns.</p>\n<p>The File name label is a local action that reveals this configured target in Finder:</p>\n<pre><code class=\"language-text\">ready media:  docs-viewer/media/&lt;scope&gt;/&lt;type&gt;/&lt;identity&gt;\nbuild source: docs-viewer/media/&lt;scope&gt;/build-source/&lt;type&gt;/&lt;identity&gt;\n</code></pre>\n<p>The server supplies the exact role-specific target; the browser never reconstructs it from Type and File name. The existing confined local-target service owns activation and uses Finder's reveal-file behavior. The browser receives only a Projects-relative target, never the absolute external workspace path. If a listed file is removed between report loading and activation, the action may report it unavailable and the next report load omits its row.</p>\n<h3>Documents</h3>\n<p>The cell contains one link for every exact document that references the listed identity. A parent document keeps its exact <code>{scope, doc}</code> target. A sub-scope document keeps its exact parent report-host <code>doc</code> plus <code>subdoc</code>; the report never substitutes a title, filename, selected row, or current host document as identity. Repeated titles remain separate links when their exact targets differ. An existing ready-media or build-source file with no direct references has a blank Documents cell.</p>\n<h2>Scope Selection</h2>\n<p>The dropdown lists configured parent scopes and defaults to <code>dotlineform</code>. Changing it loads that scope's exact live ready-media and build-source listing plus parent and sub-scope references and persists the selection as <code>report_scope</code>. A scope with no listed files shows a contained empty state, regardless of missing references. An unavailable external workspace or read service produces one report-level unavailable state rather than stale rows or a fallback location.</p>\n<h2>Refresh And Search</h2>\n<p>The toolbar contains a Project State-style refresh pill. Activating it reruns the selected scope's live ready-media and build-source listing plus the current document-reference join, then reapplies the current search and sort state. It does not mutate or build media.</p>\n<p>The Project State-style search field and clear control filter the loaded rows immediately. Matching is a case-insensitive substring search across each displayed File name value and every document title in that row; Type is not a search field. A row remains visible when either searched field matches. No matches produce one contained search-empty state distinct from a scope with no listed files.</p>\n<h2>Report Host Direction</h2>\n<p>The existing report-backed document host provides manage-only composition, scope-prefixed state, the Project State list/table presentation vocabulary, the local report service, and the safe Finder activation seam. The shipped Docs Media report reuses those owners through one focused read model and report module; it does not generalize the report framework or create a dashboard.</p>\n<h2>UI And Delivery Stance</h2>\n<p>This document retains the implemented first-report behavior. <a href=\"/docs/?scope=studio&amp;doc=d-20260720-100017-7e52de\">Docs Media Report Delivery</a> completed DMR-1 through DMR-3, including manual acceptance and durable ownership transfer. The local <a href=\"/docs/?scope=dotlineform&amp;doc=d-20260812-212735-6d9cf3\">Docs Media</a> host extends the existing reference, live inventory, report, and local-target seams only as far as this table needs. The inventory's missing-reference output is not a Docs Media input.</p>\n<h2>Boundaries</h2>\n<ul>\n<li>No public report surface.</li>\n<li>No missing-reference rows, broken-link diagnosis, or Media Broken Links report in this outcome.</li>\n<li>No aggregate counts, byte totals, additional filters, selected-detail panel, grouping control, or copy controls.</li>\n<li>No retained-original or Media Sources evidence join.</li>\n<li>No inferred build-source-to-derived-output relationship or projection of a derived output's document references onto its source row.</li>\n<li>No provider choice, public-projection state, publication-health state, or media registry.</li>\n<li>No inferred identity, fuzzy lookup, filename fallback, source-tree scan, or historical backfill.</li>\n<li>No hash, content comparison, folder monitoring, thumbnail, OCR, archive expansion, cache, or background index.</li>\n<li>No replace, rename, relink, delete, repair, publish, move, rollback, or recovery action.</li>\n<li>No generic report plugin, schema-driven dashboard, or parallel local-file activation path.</li>\n</ul>\n<p>The completed <a href=\"/docs/?scope=studio&amp;doc=d-20260720-100017-9855dd\">feature parent</a> and delivery retain bounded history. <a href=\"/docs/?scope=studio&amp;doc=d-20260514-184303-7914e2\">Media And Asset Handling</a> owns the shipped media contract, and <a href=\"/docs/?scope=studio&amp;doc=d-20260513-105343-6de36b\">Reports</a> owns the implemented report inventory.</p>\n"
}
