{
  "doc_id": "d-20260808-172131-7d4af6",
  "title": "DPD-1.0 — Delete And Public-Projection Ownership Sanity Note",
  "added_date": "2026-08-08 17:21:31",
  "last_updated": "2026-08-08 17:25:59",
  "viewer_url": "/docs/?scope=studio&doc=d-20260808-172131-7d4af6",
  "parent_id": "d-20260808-165751-1f159a",
  "summary": "freeze the exact document Delete entry points, public products, report-host result, Publish exclusion cutover, apply ordering, failure boundary, and implementation stop for DPD-1",
  "ui_status": "done",
  "content_html": "<h1>DPD-1.0 — Delete And Public-Projection Ownership Sanity Note</h1>\n<h2>Outcome</h2>\n<p>DPD-1 fits the current management, publication, document-location, and Catalogue owners without another Delete or Publish workflow. The bounded implementation target is one exact public-cleanup plan shared by ordinary parent and child document Delete, plus focused follow-through inside the existing scope and sub-scope lifecycle owners where those broader operations remove public roots or a report host.</p>\n<p>The configured snapshot has three public parent scopes (<code>library</code>, <code>analysis</code>, and <code>moments</code>), two public child collections (<code>analysis/tags</code> and <code>analysis/works</code>), and no public projection for <code>studio</code>, <code>processing</code>, <code>notes</code>, <code>dotlineform</code>, or <code>dotlineform/projects</code>. Public capability comes only from resolved configuration. Current public-file presence, not <code>publishable</code>, lineage, title, route text, or source existence, proves that a deletion target has an exact public projection.</p>\n<p>DPD-1.0 is complete as a documentation-only gate. No runtime, source, generated public, browser, lineage, or Catalogue behavior changes here. DPD-1.1 requires explicit implementation approval.</p>\n<h2>Participating Delete Owners</h2>\n<table>\n<thead>\n<tr>\n<th>entry point</th>\n<th>DPD-1 result</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Parent Index selection through <code>POST /docs/delete-preview</code> and <code>POST /docs/delete-apply</code></td>\n<td>Participates fully. The existing checked selection and deterministic descendant expansion supply the exact effective parent <code>doc_id</code> set.</td>\n</tr>\n<tr>\n<td>Default sub-scope detail through the same routes</td>\n<td>Participates fully. Exact <code>{scope, sub_scope, doc_id}</code> and the existing source-revision receipt remain the identity and stale-write boundary.</td>\n</tr>\n<tr>\n<td>Scope lifecycle through <code>POST /docs/scopes/delete-preview</code> and <code>POST /docs/scopes/delete-apply</code></td>\n<td>Remains a separate manifest-owned whole-scope operation. It does not enumerate documents into ordinary Delete. For a public lifecycle scope, its existing exact public Docs/search-root removal must be followed by a complete Catalogue document-URL refresh before success; local lifecycle scopes have no public follow-through.</td>\n</tr>\n<tr>\n<td>Sub-scope lifecycle through <code>POST /docs/scopes/sub-scopes/delete-preview</code> and <code>POST /docs/scopes/sub-scopes/delete-apply</code></td>\n<td>Remains a separate configuration/root operation. Its existing manifest-owned child public-root deletion is retained. When it also deletes the exact configured report-host source, it must apply the same planned parent-host public cleanup and Catalogue follow-through before success; it must not convert the child collection into an inferred list of ordinary document deletes.</td>\n</tr>\n</tbody>\n</table>\n<p>No entry point is added, moved, aliased, or inferred from the active document, displayed report, filename, title, URL, or lineage table.</p>\n<h2>Exact Public Product Map</h2>\n<p>The cleanup baseline is the current public snapshot, not the newer working projection. Targeted cleanup removes or rewrites only products that currently contain the exact deletion identities, so pending edits, newly publishable documents, unrelated exclusions, and unrelated Mermaid or media products cannot be promoted.</p>\n<table>\n<thead>\n<tr>\n<th>deletion target</th>\n<th>exact removals</th>\n<th>exact inventory follow-through</th>\n<th>deliberately retained</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>One public parent document</td>\n<td>Its public <code>by-id/&lt;doc_id&gt;.json</code> and any public Mermaid variant pair owned by that document</td>\n<td>Filter the exact identity from public tree, Recent, search, and document locations; refresh affected Work/Series <code>doc_url[]</code> payloads</td>\n<td>Unrelated public documents, child collections, and shared media</td>\n</tr>\n<tr>\n<td>A parent plus owned descendants</td>\n<td>The same by-ID and owned Mermaid products for the effective descendant-expanded set</td>\n<td>Rebuild the same parent inventories once from the current public snapshot minus the complete exact set; refresh Catalogue once</td>\n<td>Documents outside the existing hierarchy-owned deletion set</td>\n</tr>\n<tr>\n<td>One public sub-scope document</td>\n<td>Its child <code>by-id/&lt;doc_id&gt;.json</code> and any Mermaid pair owned by that child</td>\n<td>Filter the child manifest, derived document location, and affected Catalogue URLs</td>\n<td>Report host, sibling child files, parent tree/Recent/search, collection configuration, and shared media</td>\n</tr>\n<tr>\n<td>One published report host</td>\n<td>The host's parent by-ID and owned Mermaid products</td>\n<td>Filter the host from parent tree, Recent, search, and document locations. Removing the host location also removes every child URL derived through that host and refreshes the corresponding Catalogue URLs</td>\n<td>Surviving child manifest, child by-ID files, child Mermaid products, child source, and collection configuration</td>\n</tr>\n</tbody>\n</table>\n<p>The report-host result is explicit: child public files remain independently owned projections while their canonical child sources survive. Reachability is host-derived, so deleting the host removes child document-location and Catalogue URL records, but route shape does not authorize a cascade into child files. The checked current Analysis example confirms the distinction: public host <code>d-20260807-082735-54d9d5</code> supplies the route for child <code>d-20260807-094819-5712bd</code>, while the child has its own manifest/by-ID projection and Series <code>001</code> currently carries the derived URL.</p>\n<p>Document-location and Catalogue planning must retain exact internal <code>{scope, sub_scope, doc_id}</code> identities. Catalogue refresh is a required Delete invariant, not the current Publish path's non-fatal stale follow-through. A Delete response is not successful while any affected Catalogue payload still carries a removed URL.</p>\n<h2>Publish Cutover</h2>\n<p>The current <code>docs_publish_gate.py</code> computes <code>removed</code> by comparing every file below a public root with the complete desired working projection. <code>docs_diff</code>, parent <code>docs.removed</code>, each sub-scope <code>removed</code> and <code>removed_count</code>, aggregate <code>removed_count</code>, <code>summary_text</code> with <code>stale</code>, <code>copy_tree</code> unlinking, and the browser's <strong>Stale files to remove</strong> line therefore combine explicit publishability exclusions, source-deleted projections, and unrelated extra files.</p>\n<p>DPD-1.1 performs one clean response/schema cutover rather than retaining ambiguous aliases:</p>\n<ul>\n<li>replace <code>removed</code> and <code>removed_count</code> with <code>excluded</code> and <code>excluded_count</code> in the Publish plan and response;</li>\n<li>report summaries as changed and excluded, and label the browser count <strong>Files removed by current Publish exclusions</strong>;</li>\n<li>derive every exclusion only from a surviving canonical document's explicit <code>publishable: false</code>, including the established parent-descendant rule and route/location consequences of an explicitly false public report host;</li>\n<li>unlink only exact paths owned by those current exclusions; and</li>\n<li>ignore and retain public files that are absent from source for any other reason, including unsupported out-of-workflow filesystem deletion, without reporting them as Publish work.</li>\n</ul>\n<p>Search, tree, Recent, document-location, and Catalogue bytes changed by a current exclusion remain ordinary changed products. <code>excluded_count</code> counts exact public files removed by the explicit gate. Publish retains positive promotion and unchanged-copy ownership; it gains no filesystem repair or Delete follow-through.</p>\n<h2>Delete Preview And Apply Contract</h2>\n<p>Delete preview retains its existing target and warning fields and adds one <code>public_cleanup</code> object. For a public-capable exact target it reports the exact projected <code>doc_id</code> subset, public paths to remove, inventory paths whose bytes would change, affected Catalogue targets, and the report-host child-route consequences. For a local or local-external target it reports <code>applicable: false</code> with no public paths. Preview derives this evidence from resolved configuration and actual public products and remains write-free.</p>\n<p>Apply re-runs ordinary target, hierarchy, revision, and public-product planning. The frozen order is:</p>\n<ol>\n<li>validate the exact request and current public cleanup plan;</li>\n<li>run the existing canonical source deletion and exact working Docs/search or child rebuild boundary;</li>\n<li>remove the planned exact public by-ID/Mermaid files and rewrite only affected public inventories from the pre-delete public baseline;</li>\n<li>refresh exact Catalogue <code>doc_url[]</code> targets from the resulting public locations; and</li>\n<li>return success with the applied <code>public_cleanup</code> result, then let the existing browser refresh owner reconcile its current surface.</li>\n</ol>\n<p>This sequence never calls scope-wide Publish and never copies working payloads into public output. Parent Delete keeps its current committed-source failure behavior. Sub-scope Delete keeps its existing source restoration only when the source/working rebuild stage fails; DPD adds no cross-product rollback. A later public-cleanup or Catalogue failure returns a non-success result with <code>committed: true</code>, the failed stage, and exact remaining impact. It must not claim retry safety or a successful immediate cleanup. Focused fault evidence is required before closeout because this deliberately preserves the no-new-rollback boundary.</p>\n<h2>Expected Implementation Footprint And Risks</h2>\n<p>The direct server owners are <code>docs_management_mutations.py</code>, <code>docs_management_mutation_service.py</code>, <code>docs_publish_gate.py</code>, <code>docs_document_location_projection.py</code>, <code>docs_catalogue_document_urls.py</code>, the existing Catalogue URL refresh owner, and the two lifecycle services. Browser follow-through belongs to the existing parent Delete modal, sub-scope Delete workflow, and Publish confirmation copy. Focused tests belong with management mutation/service, sub-scope Delete, scope/sub-scope lifecycle, Publish gate, document locations, Catalogue URLs, and the existing management module smokes.</p>\n<p>The main implementation risk is accidentally rebuilding public output from newer working bytes. The second is treating a report route as ownership of child source or files. The third is returning success after non-fatal Catalogue follow-through. Tests must falsify all three, alongside public/local capability isolation, existing revision and descendant rules, already-absent projections, exact Mermaid ownership, next-Publish idempotence, and out-of-workflow stale-file retention.</p>\n<p>No additional Delete control, per-document Publish/Unpublish, public-file browser, scope-wide scan, shared-media cleanup, lineage condition, generic projection-deletion engine, compatibility response alias, or rollback/retry framework is approved.</p>\n<h2>Verification And Review</h2>\n<ul>\n<li>The parent and child document routes, mutation plans, mutation executor, scope and sub-scope lifecycle services, Publish diff/apply owner, document-location producer, Catalogue URL producer/refresh, parent Delete modal, sub-scope Delete workflow, and current focused tests were inspected directly.</li>\n<li>The configured public/local matrix and actual Analysis Works host, child manifest, document locations, and Series <code>001</code> follow-through were checked from current generated products.</li>\n<li>Explicit Studio Docs and search writes were no-ops: 280 documents, zero Docs writes/removals, zero index writes, zero warnings, and zero search writes.</li>\n<li>Generated assertions confirmed the new note's exact ID, parent, done status, one tree placement, one search entry, and the delivery's checkpoint link. <code>git diff --check</code> passed.</li>\n<li>A bounded reviewer pass found only the delivery source, the new evidence source, and their expected Studio Docs/tree/Recent/search projections. It found no runtime implementation, compatibility response, ownership fallback, public promotion, child-file cascade, media inference, or SSP-8 change.</li>\n</ul>\n<h2>Gate</h2>\n<p>DPD-1.0 confirms the exact owners, entry points, current public products, independent child-file/host-route decision, Publish exclusion-only cutover, apply order, failure reporting, expected footprint, risks, and stop conditions. Stop before DPD-1.1 implementation approval.</p>\n"
}
