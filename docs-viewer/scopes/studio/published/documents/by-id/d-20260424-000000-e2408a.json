{
  "doc_id": "d-20260424-000000-e2408a",
  "title": "Publishable field",
  "added_date": "2026-04-24 00:00:00",
  "last_updated": "2026-08-07 23:30:00",
  "viewer_url": "/docs/?scope=studio&doc=d-20260424-000000-e2408a",
  "parent_id": "d-20260424-000000-50b63f",
  "content_html": "<h1>Publishable field</h1>\n<h2>Purpose</h2>\n<p><code>publishable</code> is the source-owned inclusion gate for the next scope-wide <strong>Publish</strong>. It exists only for an exact parent or child collection with a configured public projection. It is not current-public evidence, a local discovery control, a privacy boundary, or a separate per-document Publish action.</p>\n<p>Local and local-external collections have no Publish projection, so they have no <code>publishable</code> field, exclusion icon, or publishability action. Their complete source set remains in local index, Recent, search, by-ID, and management projections.</p>\n<h2>Source Contract</h2>\n<p>The canonical excluded form is:</p>\n<pre><code class=\"language-yaml\">publishable: false\n</code></pre>\n<p>Rules:</p>\n<ul>\n<li>omission means included in the next Publish;</li>\n<li>write paths normalize an included document to omission;</li>\n<li><code>publishable: false</code> excludes the document from the next Publish;</li>\n<li>a false parent also excludes all of its descendants from that public projection without rewriting their source fields;</li>\n<li>the legacy <code>viewable</code> field is rejected rather than read as an alias; and</li>\n<li><code>ui_status</code> remains presentation metadata and does not affect Publish.</li>\n</ul>\n<p>New documents and Copy targets use the included default and therefore omit <code>publishable</code>. Import may explicitly supply the boolean for a publish-capable target; overwrite preserves the current value unless the returned record explicitly changes it. Local targets reject the field.</p>\n<h2>Private Build Projection</h2>\n<p>The builder renders every configured source document for management. For a publish-capable parent collection, the private <code>index-tree.json</code> carries <code>publishable: false</code> only on excluded rows; included rows omit the field. For a publish-capable child collection, the Manage manifest follows the same false-only rule. Local collection projections omit the field entirely.</p>\n<p>Excluded documents remain loadable in Manage through their private by-ID payloads and retain the <strong>Excluded from next Publish</strong> index/report cue. Public reader by-ID payloads and public manifests do not carry management publishability metadata.</p>\n<h2>Search, Recent, And Publish</h2>\n<p>For a publish-capable parent collection, the search and prepared public Recent projections omit excluded documents, their descendants, and configured manage-only roots. Local search and Recent include the normal complete local source set because local collections have no publication gate.</p>\n<p>Scope-wide <strong>Publish</strong> remains the sole public mutation. It copies the prepared public projection, omitting excluded parent and child rows, their public by-ID and reference payloads, and stale public files. Changing <code>publishable</code> does not itself add or remove a public file; that happens only after the next successful Publish.</p>\n<p>This distinction supports simple publication stages: an excluded document may still be <strong>🟢 Published</strong> while its prior public projection exists, then become <strong>🟠 Pre-publish</strong> after Publish removes it.</p>\n<h2>Management And Packages</h2>\n<p><strong>Edit metadata</strong> does not expose or accept publishability. SSP-7.1b owns the later checkbox-aware <strong>Set Publishable…</strong> selection action; SSP-7.1a changes only the field contract and removes the old metadata control.</p>\n<p>Package profiles use <code>include_non_publishable</code>. The option can exclude rows carrying <code>publishable: false</code> from package composition, but it does not mutate source or publish. Local package records omit publishability, so the option subtracts nothing from a local selection.</p>\n<p><code>publishable: false</code> does not prevent local rendering, package export, management access, or direct source access and must not be used as a sensitive-data boundary.</p>\n"
}
