{
  "doc_id": "d-20260401-000000-ebf14a",
  "title": "Catalogue Works",
  "added_date": "2026-04-01 00:00:00",
  "last_updated": "2026-08-11 12:58:29",
  "viewer_url": "/docs/?scope=studio&doc=d-20260401-000000-ebf14a",
  "parent_id": "d-20260423-000000-d015e6",
  "content_html": "<h1>Catalogue Works</h1>\n<h2>Live Report</h2>\n<p><a href=\"/docs/?scope=dotlineform&amp;doc=d-20260810-222148-99daec\">Open the Catalogue Works report</a>. It sits with the other local <code>dotlineform</code> reports; this Studio page remains the durable ownership and extension guide.</p>\n<p>The Studio home <strong>works</strong> entry resolves this target through <code>app.runtime.sites.docs_viewer.base</code>. The server uses <code>DOCS_VIEWER_BASE_URL</code> when supplied and otherwise constructs the Docs Viewer origin from <code>DOCS_VIEWER_HOST</code> and <code>DOCS_VIEWER_PORT</code>, so the link opens the Docs Viewer service instead of inheriting Studio's port.</p>\n<p>The report loads every canonical Work with <code>status: published</code> but shows rows only after a Work or Series search. Results retain exact Work identity, every Series membership, year, and curator-only storage context in one seven-column semantic table. Work, Year, Title, Series, and Storage remain the five embedded and sortable columns. Medium type and Medium caption are present in the same live table but appear only in Expanded Report View. <strong>Copy table</strong> exports the current sorted result as five-column TSV while embedded and seven-column TSV while expanded.</p>\n<h2>Data And Ownership</h2>\n<p>The local report registry maps <code>catalogue_works</code> to its exact allowlisted loader, and <code>docs-viewer/runtime/js/reports/catalogue-works-report.js</code> reads the browser-safe Local Studio Catalogue API keys <code>catalogue_works</code> and <code>catalogue_series</code>. Canonical Work rows already own <code>storage_location</code>, <code>medium_type</code>, and <code>medium_caption</code>; expansion added no API, schema, generated payload, or parallel data read. The report does not read the generated public Catalogue or treat a public projection as its application model.</p>\n<p>The report module owns validation, published-only selection, Work/Series search, five sortable columns, exact public-preview links, one seven-column render, presentation-aware clipboard projection, and contained unavailable or invalid states. Editing remains in the <a href=\"/docs/?scope=studio&amp;doc=d-20260422-000000-45a776\">Catalogue Work Editor</a> and <a href=\"/docs/?scope=studio&amp;doc=d-20260422-000000-ec1a69\">Catalogue Series Editor</a>. <a href=\"/docs/?scope=studio&amp;doc=d-20260418-000000-16bf7e\">Catalogue Drafts</a> remains the recovery and maintenance route for unfinished records.</p>\n<h2>Expanded Presentation</h2>\n<p>After its canonical inputs and first complete render succeed, the module returns the exact live semantic table through the optional <code>expandedPresentation</code> handle. One frozen ordered column model supplies stable ids, labels, and <code>both</code> or <code>expanded</code> visibility; headers and body cells use the same seven identities. Manage moves the existing report root into Content Detail, so search, sorting, rows, links, Copy table, event listeners, and route state remain on one report instance with no second data request or renderer.</p>\n<p>Manage alone supplies accessible pointer and keyboard resize handles. One direct <code>colgroup</code> keeps headers and rows aligned through valid notified rerenders, while a mismatched header/body model fails closed. Widths exist only for the active expanded presentation: <strong>Reset widths</strong>, Back, or document navigation removes them, and reopening starts from the natural layout. Widened output scrolls inside the expanded viewport. Source, registry JSON, URLs, browser storage, canonical data, and generated payloads retain no width state.</p>\n<h2>Extension And Weak Spots</h2>\n<ul>\n<li>Add a report column only when the accepted canonical local read owns its value and its stable id, label, <code>both</code> or <code>expanded</code> visibility, sorting participation, and active TSV projection are explicit.</li>\n<li>Add curator-only context through a Studio-owned read, never by expanding or treating public payloads as an application model.</li>\n<li>Keep the report search-required: empty search reveals no rows, and a failed current read never retains an older result.</li>\n<li>Search continues to match Work and Series only; an expanded-only value does not silently broaden that contract.</li>\n<li>Keep <code>catalogue_works</code> local-only. Public report metadata, loaders, data, and CSS do not expose it.</li>\n</ul>\n"
}
