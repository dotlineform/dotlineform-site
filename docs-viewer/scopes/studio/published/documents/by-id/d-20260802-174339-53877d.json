{
  "doc_id": "d-20260802-174339-53877d",
  "title": "Resolver Tokens Architecture",
  "added_date": "2026-08-02 17:43:39",
  "last_updated": "2026-08-05 19:38:19",
  "viewer_url": "/docs/?scope=studio&doc=d-20260802-174339-53877d",
  "parent_id": "d-20260802-194357-4d857b",
  "summary": "retain detailed resolver boundaries for reconciliation with context-aware current references under Sub-Scope Publishing",
  "ui_status": "proposed",
  "content_html": "<h1>Resolver Tokens Architecture</h1>\n<h2>Purpose</h2>\n<p>Resolver Tokens let an author insert a current-references marker that expands the containing sub-scope document's normalized subject into an ordered relationship result at build time. The initial adapter resolves one exact Folder, Work, or Series subject through focused relationship data, then the destination collection projects only its declared result groups. A relevant build refreshes the required private relationship data once before consumption rather than depending on manual report use or continuous mutation hooks.</p>\n<p>This is a retained component draft. SSP-3.0 must reconcile its initial folder result groups with the accepted typed-subject and focused-lookup contracts in <a href=\"/docs/?scope=studio&amp;doc=d-20260802-194358-7717ee\">Sub-Scope Publishing Concept And Architecture</a>. Its one-hop lookup, source preservation, semantic projection, usage, access, freshness, and non-mutation boundaries remain inputs to that revision.</p>\n<p>Resolver Tokens are adjacent to, but distinct from, Semantic Tokens. A Semantic Token resolves one registered identity to one link. A Resolver Token expands the containing document's normalized subject into one or more declared result groups. Returned registered identities reuse the Semantic Tokens target, link, and usage owners; the resolver retains its own parser, context contract, lookup adapter, group renderer, and access policy.</p>\n<h2>Core Model</h2>\n<p>The initial process is:</p>\n<pre><code class=\"language-text\">Resolver Token + normalized containing subject\n    -&gt; one subject-keyed relationship result in memory\n    -&gt; seed and related semantic target identities\n    -&gt; collection-specific result-group projection\n    -&gt; ordinary HTML links in content_html\n</code></pre>\n<p>The source token remains unchanged and carries no manually authored seed. The containing document's front matter supplies the seed subject; relationship preparation returns that exact seed plus related Folder, Work, and Series identities. Work and Series identities are semantic target descriptors held in memory, not fabricated <code>[[catalogue:...]]</code> source. The shared Semantic Tokens projector resolves them directly to ordinary anchors, and only those anchors enter built <code>content_html</code>.</p>\n<p>Relationship resolution and result projection are separate decisions. Copy preserves canonical subject front matter without interpreting it. The destination collection decides at build time which resolved groups may appear:</p>\n<table>\n<thead>\n<tr>\n<th>subject</th>\n<th><code>dotlineform/projects</code> projection</th>\n<th><code>analysis/works</code> projection</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Folder</td>\n<td>seed Folder, related Series, singleton or otherwise unrepresented Works</td>\n<td>related Series, singleton or otherwise unrepresented Works; Folder suppressed</td>\n</tr>\n<tr>\n<td>Work</td>\n<td>seed Work, related Folder, related Series</td>\n<td>seed Work, related Series; Folder suppressed</td>\n</tr>\n<tr>\n<td>Series</td>\n<td>seed Series, related Folders, singleton or otherwise unrepresented Works</td>\n<td>seed Series, singleton or otherwise unrepresented Works; Folders suppressed</td>\n</tr>\n</tbody>\n</table>\n<p>A Folder subject retained by Copy therefore remains a valid relationship seed in <code>analysis/works</code>; the Analysis projection suppresses the Folder result rather than deleting or ignoring the subject. Resolver depth remains exactly one: the prepared row contains the complete declared relationship result, and token expansion never follows a returned Series to discover Works or a returned Work to discover another Folder.</p>\n<p>The seed subject is explicit result provenance rather than hidden presentation state. When its result type is allowed by the destination projection, it appears first as a link under the Subject part of the current-references block. A Work or Series seed uses the shared semantic link projector; a Folder seed uses the local Project link owner and can appear only in an allowed private projection.</p>\n<p>Related Works remain concise without silently losing navigation. A seed Work always appears when Work results are allowed. Otherwise one related Work appears directly; when several Works are already represented by successfully projected Series links, those Work links are omitted. Any related Work not represented by a projected Series remains eligible for a direct Work link.</p>\n<p>Resolution reads one normalized subject from the containing document's registered collection context, performs one exact subject-row read, and never scans the filesystem, collection manifest, canonical Works, canonical Series, Moments, or another relationship registry during token expansion. Before rendering, the build prepares current subject-keyed relationship rows once from their focused owners. Folder relationships originate in the current Project State generation; Work/Series membership remains catalogue-owned and enters only the focused resolver preparation. Zero matches, unavailable lookup, stale generation, invalid subjects, unavailable semantic targets, and denied access have explicit reader outcomes. Individual Resolver Token expansion never refreshes relationship data or mutates source.</p>\n<h2>Source And Usage Examples</h2>\n<p>The reader-facing Series link can be equivalent in all three cases. Source and generated usage evidence distinguish how it came to exist.</p>\n<h3>Manually Authored Markdown Link</h3>\n<pre><code class=\"language-md\">[nerve](/series/?series=105)\n</code></pre>\n<p>This is an ordinary authored URL. The Markdown renderer produces an anchor, but there is no semantic identity in source and no semantic usage row.</p>\n<h3>Authored Semantic Token</h3>\n<p>Using <strong>Add catalogue token</strong> inserts:</p>\n<pre><code class=\"language-md\">[[catalogue:series:105|nerve]]\n</code></pre>\n<p>The builder resolves <code>(catalogue, series, 105)</code>, renders the current semantic destination, and records one authored-token usage row at that exact source range.</p>\n<h3>Resolver-Generated Semantic Link</h3>\n<p>The exact Resolver Token syntax is an SSP-3.0 decision. Until then this source is illustrative only:</p>\n<pre><code class=\"language-md\">[[resolver:current-references]]\n</code></pre>\n<p>For a document declaring <code>series_id: &quot;105&quot;</code>, the in-memory result may contain:</p>\n<pre><code class=\"language-json\">{\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;series&quot;,\n  &quot;target_id&quot;: &quot;105&quot;\n}\n</code></pre>\n<p>The builder passes that descriptor through the shared semantic target lookup and link projector. It records one resolver-derived usage row for Series <code>105</code> at the containing Resolver Token range. The source and built HTML contain no fabricated <code>[[catalogue:series:...]]</code> text; readers see an ordinary link, while maintainers can inspect its semantic identity and <code>resolver_result</code> origin in the Manage Semantic Tokens report.</p>\n<h3>Collection Projection Examples</h3>\n<p>The examples use current catalogue identities to illustrate the rule; titles and membership remain current lookup values rather than copied resolver data.</p>\n<p><code>work_id: &quot;00635&quot;</code> identifies Work <strong>10,000 (1)</strong>, which belongs to Series <code>001</code> <strong>10,000</strong> and Project Folder <code>projects/10000</code>:</p>\n<pre><code class=\"language-text\">dotlineform/projects -&gt; Subject: Work 00635; Related: Folder projects/10000, Series 001\nanalysis/works       -&gt; Subject: Work 00635; Related: Series 001\n</code></pre>\n<p><code>series_id: &quot;001&quot;</code> identifies Series <strong>10,000</strong>, currently containing Works <code>00635</code>, <code>00636</code>, and <code>00637</code> in Folder <code>projects/10000</code>. The Series is the seed link and already represents all three Works, so individual Work links are omitted:</p>\n<pre><code class=\"language-text\">dotlineform/projects -&gt; Subject: Series 001; Related: Folder projects/10000\nanalysis/works       -&gt; Subject: Series 001\n</code></pre>\n<p><code>series_id: &quot;003&quot;</code> identifies Series <strong>16 forms</strong>, which currently has one Work, <code>00639</code>, in Folder <code>projects/16 forms</code>. The singleton Work remains useful as a direct link:</p>\n<pre><code class=\"language-text\">dotlineform/projects -&gt; Subject: Series 003; Related: Folder projects/16 forms, Work 00639\nanalysis/works       -&gt; Subject: Series 003; Related: Work 00639\n</code></pre>\n<p>A copied document retaining <code>folder_path: &quot;projects/10000&quot;</code> uses that Folder as its relationship seed in both collections:</p>\n<pre><code class=\"language-text\">dotlineform/projects -&gt; Subject: Folder projects/10000; Related: Series 001\nanalysis/works       -&gt; Related: Series 001\n</code></pre>\n<p>The Analysis build consumes the private Folder relationship only to obtain allowed catalogue identities. It emits no Folder seed, local link, <code>folder_path</code>, lookup row, or Project capability into the public projection.</p>\n<p>The usage rows below are illustrative; SSP-3.0 freezes their exact schema and field names:</p>\n<pre><code class=\"language-json\">{\n  &quot;origin&quot;: &quot;authored_token&quot;,\n  &quot;source_doc_id&quot;: &quot;d-...&quot;,\n  &quot;source_range&quot;: {&quot;start&quot;: 120, &quot;end&quot;: 155},\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;series&quot;,\n  &quot;target_id&quot;: &quot;105&quot;\n}\n</code></pre>\n<pre><code class=\"language-json\">{\n  &quot;origin&quot;: &quot;resolver_result&quot;,\n  &quot;source_doc_id&quot;: &quot;d-...&quot;,\n  &quot;source_range&quot;: {&quot;start&quot;: 120, &quot;end&quot;: 178},\n  &quot;resolver_id&quot;: &quot;&lt;project-resolver-id&gt;&quot;,\n  &quot;subject_kind&quot;: &quot;folder&quot;,\n  &quot;subject_key&quot;: &quot;projects/nerve&quot;,\n  &quot;result_group&quot;: &quot;series&quot;,\n  &quot;result_role&quot;: &quot;related&quot;,\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;series&quot;,\n  &quot;target_id&quot;: &quot;105&quot;\n}\n</code></pre>\n<h2>Resolver Definition And Lookup Contract</h2>\n<ul>\n<li>A small code-owned definition registry contains capability metadata only: resolver ID, access, accepted subjects, lookup/schema, and result groups.</li>\n<li>Project State remains the only producer of physical Folder-to-Work/Series evidence. SSP-3 prepares the bounded subject-keyed resolver view once per relevant build from that current generation and the focused catalogue membership projection; token expansion reads one exact row.</li>\n<li>Resolution order is exact parse, access check, normalized subject read, current relationship-row read, seed/related classification, collection result-group projection, and semantic-link projection for allowed registered identities.</li>\n<li>Folder subject keys are normalized paths relative to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code>. The initial Project relationship view accepts the immediate <code>projects/&lt;project-directory&gt;</code> identities established by Project State without widening its discovery scan. Work and Series subjects retain exact canonical string identity.</li>\n<li>Folder results sort by normalized key, Series by canonical Series ID, and Works by canonical Work ID. The seed precedes related groups when its type is allowed.</li>\n<li>A selected build containing a Resolver Token that needs Folder relationships refreshes Project State once after current subject products are available and before resolver preparation. It uses the resulting generation for every selected token; individual token expansion never refreshes it again.</li>\n<li>The token syntax serializes no subject; the registered collection supplies the exact normalized kind and key at resolve time.</li>\n<li>A later resolver may define another exact subject and returned registered target type, but that does not widen the initial Project resolver.</li>\n</ul>\n<h2>Semantic Link And Usage Contract</h2>\n<ul>\n<li>The resolver view stores Work and Series semantic identities, not a second title or URL authority.</li>\n<li>Every Work and Series result, including an allowed seed, passes through the existing Semantic Tokens target lookup and link projector. The resolver never carries or emits a plain catalogue URL and never fabricates Semantic Token syntax in source.</li>\n<li>A missing, stale, or unusable semantic target renders an explicit unavailable result, never falls back to a resolver-owned URL, and produces no resolved usage row.</li>\n<li>Every successfully projected Work or Series adds one resolver-derived row to the containing scope's generated semantic usage index. One Resolver Token may produce several rows with the same containing source range and different identities.</li>\n<li>Usage rows distinguish authored Semantic Tokens from resolver-derived results while retaining source scope/document, containing source range, displayed title, semantic family/type/id, resolved <code>href</code>, resolver/subject provenance, result group, and <code>seed</code> or <code>related</code> role.</li>\n<li>The Semantic Tokens report includes resolver-derived rows and links back to the managed document and containing Resolver Token location.</li>\n</ul>\n<h2>Access And Lifecycle</h2>\n<p>Resolver output is read-only presentation and grants no mutation authority. A local Manage projection may render Folder links. A publication build may privately consume an exact Folder relationship row when a retained Folder subject seeds an <code>analysis/works</code> Resolver Token, but its allowed projection contains only Work and Series anchors. Public payloads and runtime remain free of Folder identities, local paths, private lookup data, resolver definitions, and refresh capabilities.</p>\n<p>Resolver definitions are code-owned and loaded through explicit build composition. The system does not discover resolvers by scanning registries, documents, files, or runtime modules. Targeted builds preserve untouched usage rows and update only occurrences in rebuilt documents.</p>\n<p>Build planning recognises the exact token and normalized subject kinds in the selected source set before scheduling dependencies. Builds whose selected tokens need no Folder relationship perform no Project State work. A publication build may execute the same private prerequisite for a Folder-seeded token, but neither its generated payload nor public runtime receives the lookup, refresh capability, local relationships, or Folder results. A failed prerequisite renders the accepted stale/unavailable outcome for affected tokens and does not silently consume an older lookup as current.</p>\n<h2>Architecture Boundary</h2>\n<p>Not in scope:</p>\n<ul>\n<li>unbounded or inferred subject kinds and result groups;</li>\n<li>recursive traversal or adjustable depth;</li>\n<li>relationship chaining during individual token expansion;</li>\n<li>relationship-path explanations or graph visualization;</li>\n<li>a global relationship database, query language, registry scanner, or live relationship service;</li>\n<li>Studio or Docs post-commit synchronization hooks for the Project State lookup;</li>\n<li>per-document or per-token Project State refresh;</li>\n<li>automatic rebuilding of every document that uses a changed resolver row;</li>\n<li>public projection of local Folder relationships; or</li>\n<li>migration of Semantic Tokens into Resolver Tokens.</li>\n</ul>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260802-121105-c574f9\">Project State Concept And Architecture</a> owns the initial lookup meaning, producer, generation, and failure semantics. Resolver Tokens owns scheduling that producer immediately before relevant build-time consumption. <a href=\"/docs/?scope=studio&amp;doc=d-20260714-234030-434069\">Semantic Tokens Architecture</a> owns semantic target projection and usage evidence. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-194357-4d857b\">Sub-Scope Publishing</a> owns overall sequencing; this document retains resolver-specific architecture.</p>\n"
}
