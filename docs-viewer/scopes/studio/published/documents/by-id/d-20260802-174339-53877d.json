{
  "doc_id": "d-20260802-174339-53877d",
  "title": "Resolver Tokens Architecture",
  "added_date": "2026-08-02 17:43:39",
  "last_updated": "2026-08-04 16:02:07",
  "viewer_url": "/docs/?scope=studio&doc=d-20260802-174339-53877d",
  "parent_id": "d-20260802-194357-4d857b",
  "summary": "retain detailed resolver boundaries for reconciliation with context-aware current references under Sub-Scope Publishing",
  "ui_status": "proposed",
  "content_html": "<h1>Resolver Tokens Architecture</h1>\n<h2>Purpose</h2>\n<p>Resolver Tokens let an author insert a current-references marker that expands the containing sub-scope document's normalized subject into an ordered relationship result at build time. The initial adapter resolves one exact Project subject through a versioned focused lookup and returns its declared result groups.</p>\n<p>This is a retained component draft. SSP-3.0 must reconcile its initial folder result groups with the accepted typed-subject and focused-lookup contracts in <a href=\"/docs/?scope=studio&amp;doc=d-20260802-194358-7717ee\">Sub-Scope Publishing Concept And Architecture</a>. Its one-hop lookup, source preservation, semantic projection, usage, access, freshness, and non-mutation boundaries remain inputs to that revision.</p>\n<p>Resolver Tokens are adjacent to, but distinct from, Semantic Tokens. A Semantic Token resolves one registered identity to one link. A Resolver Token expands the containing document's normalized subject into one or more declared result groups. Returned registered identities reuse the Semantic Tokens target, link, and usage owners; the resolver retains its own parser, context contract, lookup adapter, group renderer, and access policy.</p>\n<h2>Core Model</h2>\n<p>The initial relationship is:</p>\n<pre><code class=\"language-text\">folder -&gt; Series[] + Project documents[]\n</code></pre>\n<p>The source token remains unchanged and carries no manually authored seed. Resolution reads one normalized subject from the containing document's registered collection context, performs one exact focused lookup-row read, and never scans the filesystem, collection manifest, canonical Works, canonical Series, Moments, or another relationship registry. Resolver depth is exactly one and returned items are never resolved again.</p>\n<p>Zero matches, unavailable lookup, stale generation, invalid subjects, unavailable semantic targets, and denied access have explicit reader outcomes. Resolver expansion never refreshes Project State or mutates source.</p>\n<h2>Source And Usage Examples</h2>\n<p>The reader-facing Series link can be equivalent in all three cases. Source and generated usage evidence distinguish how it came to exist.</p>\n<h3>Manually Authored Markdown Link</h3>\n<pre><code class=\"language-md\">[nerve](/series/?series=105)\n</code></pre>\n<p>This is an ordinary authored URL. The Markdown renderer produces an anchor, but there is no semantic identity in source and no semantic usage row.</p>\n<h3>Authored Semantic Token</h3>\n<p>Using <strong>Add catalogue token</strong> inserts:</p>\n<pre><code class=\"language-md\">[[catalogue:series:105|nerve]]\n</code></pre>\n<p>The builder resolves <code>(catalogue, series, 105)</code>, renders the current semantic destination, and records one authored-token usage row at that exact source range.</p>\n<h3>Resolver-Generated Semantic Link</h3>\n<p>The exact Resolver Token syntax is an SSP-3.0 decision. Until then this source is illustrative only:</p>\n<pre><code class=\"language-md\">[[resolver:current-references]]\n</code></pre>\n<p>The exact Project State row may contain:</p>\n<pre><code class=\"language-json\">{\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;series&quot;,\n  &quot;target_id&quot;: &quot;105&quot;\n}\n</code></pre>\n<p>The builder passes that descriptor through the shared semantic target lookup and link projector. It records one resolver-derived usage row for Series <code>105</code> at the containing Resolver Token range. The source contains no fabricated <code>[[catalogue:series:...]]</code> text.</p>\n<p>The usage rows below are illustrative; SSP-3.0 freezes their exact schema and field names:</p>\n<pre><code class=\"language-json\">{\n  &quot;origin&quot;: &quot;authored_token&quot;,\n  &quot;source_doc_id&quot;: &quot;d-...&quot;,\n  &quot;source_range&quot;: {&quot;start&quot;: 120, &quot;end&quot;: 155},\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;series&quot;,\n  &quot;target_id&quot;: &quot;105&quot;\n}\n</code></pre>\n<pre><code class=\"language-json\">{\n  &quot;origin&quot;: &quot;resolver_result&quot;,\n  &quot;source_doc_id&quot;: &quot;d-...&quot;,\n  &quot;source_range&quot;: {&quot;start&quot;: 120, &quot;end&quot;: 178},\n  &quot;resolver_id&quot;: &quot;&lt;project-resolver-id&gt;&quot;,\n  &quot;subject_kind&quot;: &quot;folder&quot;,\n  &quot;subject_key&quot;: &quot;projects/nerve&quot;,\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;series&quot;,\n  &quot;target_id&quot;: &quot;105&quot;\n}\n</code></pre>\n<h2>Resolver Definition And Lookup Contract</h2>\n<ul>\n<li>A small code-owned definition registry contains capability metadata only: resolver ID, access, accepted subjects, lookup/schema, and result groups.</li>\n<li>Project State is the only producer of the initial Project lookup. The Project resolver accepts declared Project subject kinds and returns only the registered groups frozen for each kind; the first folder view remains <code>Series[] + Project documents[]</code>.</li>\n<li>Resolution order is exact parse, access check, lookup load, exact subject read, declared-group rendering, and semantic-link projection for registered identities.</li>\n<li>Folder subject keys are normalized paths relative to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code>, so exact declared contained folders outside <code>projects/</code> remain addressable without widening discovery scans. Work subjects retain exact canonical string identity.</li>\n<li>Series results sort by canonical Series ID. Project documents sort by normalized title then <code>doc_id</code>.</li>\n<li>Lookup refresh and containing-document rebuild are separate operations. Resolver expansion never refreshes Project State.</li>\n<li>The token syntax serializes no subject; the registered collection supplies the exact normalized kind and key at resolve time.</li>\n<li>A later resolver may define another exact subject and returned registered target type, but that does not widen the initial Project resolver.</li>\n</ul>\n<h2>Semantic Link And Usage Contract</h2>\n<ul>\n<li>The Project lookup stores Series semantic identity <code>(catalogue, series, canonical id)</code>, not a second title or URL authority.</li>\n<li>Every Series result passes through the existing Semantic Tokens target lookup and link projector. The resolver never carries or emits a plain catalogue URL and never fabricates Semantic Token syntax in source.</li>\n<li>A missing, stale, or unusable semantic target renders an explicit unavailable result, never falls back to a resolver-owned URL, and produces no resolved usage row.</li>\n<li>Every successfully projected Series adds one resolver-derived row to the containing scope’s generated semantic usage index. One Resolver Token may produce several rows with the same containing source range and different Series identities.</li>\n<li>Usage rows distinguish authored Semantic Tokens from resolver-derived results while retaining source scope/document, containing source range, displayed title, semantic family/type/id, resolved <code>href</code>, and resolver/subject provenance.</li>\n<li>The Semantic Tokens report includes resolver-derived rows and links back to the managed document and containing Resolver Token location.</li>\n<li>Project-document results retain exact Docs Viewer target identity and do not masquerade as Semantic Token targets.</li>\n</ul>\n<h2>Access And Lifecycle</h2>\n<p>The initial Project resolver is Manage-only because its lookup and result documents expose local Project relationships. Resolver output is read-only presentation and grants no mutation authority. Public builds and runtime must remain free of the Project definition, lookup, local paths, and rendered local results.</p>\n<p>Resolver definitions are code-owned and loaded through explicit build composition. The system does not discover resolvers by scanning registries, documents, files, or runtime modules. Targeted builds preserve untouched usage rows and update only occurrences in rebuilt documents.</p>\n<h2>Architecture Boundary</h2>\n<p>Not in scope:</p>\n<ul>\n<li>unbounded or inferred subject kinds and result groups;</li>\n<li>recursive traversal or adjustable depth;</li>\n<li><code>folder -&gt; Series -&gt; Works</code> chaining;</li>\n<li>relationship-path explanations or graph visualization;</li>\n<li>a global relationship database, query language, registry scanner, or live relationship service;</li>\n<li>automatic rebuilding of every document that uses a changed resolver row;</li>\n<li>public exposure of local Project relationships; or</li>\n<li>migration of Semantic Tokens into Resolver Tokens.</li>\n</ul>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260802-121105-c574f9\">Project State Concept And Architecture</a> owns the initial lookup meaning, generation, and freshness. <a href=\"/docs/?scope=studio&amp;doc=d-20260714-234030-434069\">Semantic Tokens Architecture</a> owns semantic target projection and usage evidence. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-194357-4d857b\">Sub-Scope Publishing</a> owns overall sequencing; this document retains resolver-specific architecture.</p>\n"
}
