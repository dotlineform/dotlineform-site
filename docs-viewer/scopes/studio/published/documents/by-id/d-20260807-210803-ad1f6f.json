{
  "doc_id": "d-20260807-210803-ad1f6f",
  "title": "SSP-7.0 — Document Lineage And Publication State Sanity Note",
  "added_date": "2026-08-07 21:08:03",
  "last_updated": "2026-08-07 21:35:22",
  "viewer_url": "/docs/?scope=studio&doc=d-20260807-210803-ad1f6f",
  "parent_id": "d-20260805-231829-947705",
  "summary": "confirm the bounded fit, public-scope publishable cutover, exact owners, private table, mutation ordering, expanded first-stage footprint and stop conditions for SSP-7",
  "ui_status": "done",
  "content_html": "<h1>SSP-7.0 — Document Lineage And Publication State Sanity Note</h1>\n<h2>Outcome</h2>\n<p>SSP-7 fits the shipped Docs Viewer owners as a bounded extension of exact selection, collection-aware Copy, scope-wide Publish, and opt-in sub-scope report composition. SSP-4 already preserves exact source and target collection identities and returns exact source-to-target document mappings. SSP-7 can persist those mappings without comparing content, inferring identity, or creating another transfer or publication workflow.</p>\n<p>The gate was amended after review of the legacy <code>viewable</code> field in local scopes. The accepted replacement is one clean-cut, public-scope-only <code>publishable</code> contract. Local and local-external scopes have no Publish projection and therefore receive neither this field nor <strong>Set Publishable…</strong>. This materially expands SSP-7.1 beyond the original UI-focused estimate, so the first implementation step is split into contract cutover and action batches with a review between them.</p>\n<p>The amended gate is complete. SSP-7.1a may begin only after explicit implementation approval; no runtime behavior, metadata endpoint, source front matter, generated management projection, public projection, or lineage table changes at SSP-7.0.</p>\n<h2>Confirmed Owners And Fit</h2>\n<ul>\n<li>The current one-document <strong>Edit metadata</strong> path reads and writes legacy <code>viewable</code>, removes the explicit field for true, writes <code>viewable: false</code> for false, and rebuilds the exact parent scope or sub-scope. It is not an acceptable checkbox-aware batch boundary because repeated calls could commit only part of a selection.</li>\n<li>The parent Index action owner already resolves the exact checked <code>doc_ids</code> under one <code>{scope}</code>. The default sub-scope contribution separately owns exact checked <code>doc_ids</code> under one <code>{scope, sub_scope}</code> and registers selection actions through the shared composition. Neither owner needs a route, title, filename, active document, report host, or displayed-row fallback.</li>\n<li>Legacy <code>viewable</code> currently combines two meanings: public Publish eligibility and local search/default discovery suppression. A local document remains generated and Manage-loadable when false, so the name does not describe access and <strong>Set Publishable…</strong> would be false in a scope with no Publish capability.</li>\n<li><code>publishable</code> therefore exists only on an exact parent or child collection with a configured public projection owned by a scope that supports Publish. <strong>Set Publishable…</strong> uses that same server-owned capability. The existing scope-level <strong>Publish</strong> action remains separately capability-gated, scope-wide, and unchanged.</li>\n<li>The existing choice-modal primitive already supplies the required radio option group, <strong>OK</strong>, <strong>Cancel</strong>, focus lifecycle, and contained error presentation. The focused workflow supplies the exact selected-count label; SSP-7.1b needs no new modal framework.</li>\n<li>Copy planning and apply already carry exact source and target collections, fresh target IDs, source-to-target mappings, target publication eligibility, viewer URLs, stale-safe receipts, and durable activity evidence. A small registered customisation aspect can opt <code>dotlineform/projects</code> and <code>analysis/works</code> into one lineage contract with explicit source and editorial roles; no loose scope config or literal browser branch is needed.</li>\n<li>Publish already computes and applies the complete public parent, sub-scope, search, and document-location projection. The internal document-location producer retains exact <code>{scope, sub_scope, doc_id}</code> while deriving the public URL, so SSP-7.3 can reconcile actual public B projections without parsing URLs or exposing private identity publicly.</li>\n<li>The normal Projects list composes the default sub-scope row with the <code>dotlineform_projects</code> Manage customisation. Its manage-only manifest projection is the bounded place to attach lineage rows to exact A. The Analysis Works list already owns B identity and the legacy exclusion icon; a separate Manage-only <code>analysis_works</code> contribution can add publication presentation without changing that icon's distinct role or Project State.</li>\n</ul>\n<h2>Private Table Authority</h2>\n<p>Docs Viewer owns <code>docs-viewer/data/canonical/document-publication-lineage.json</code>. It is repository-tracked canonical operational data, outside <code>docs-viewer/data/generated/</code>, every scope output, and <code>site/</code>. Its exact contract is:</p>\n<pre><code class=\"language-json\">{\n  &quot;schema_version&quot;: &quot;docs_document_publication_lineage_v1&quot;,\n  &quot;rows&quot;: [\n    {\n      &quot;source&quot;: {&quot;scope&quot;: &quot;dotlineform&quot;, &quot;sub_scope&quot;: &quot;projects&quot;, &quot;doc_id&quot;: &quot;A&quot;},\n      &quot;editorial&quot;: {&quot;scope&quot;: &quot;analysis&quot;, &quot;sub_scope&quot;: &quot;works&quot;, &quot;doc_id&quot;: &quot;B&quot;},\n      &quot;created_at&quot;: &quot;2026-08-07T20:00:00Z&quot;,\n      &quot;last_copied_at&quot;: &quot;2026-08-07T20:00:00Z&quot;,\n      &quot;publication&quot;: {\n        &quot;public_url&quot;: &quot;/analysis/?doc=d-20260807-082735-54d9d5&amp;subdoc=B&quot;,\n        &quot;published_at&quot;: &quot;2026-08-07T20:30:00Z&quot;,\n        &quot;generation&quot;: &quot;sha256:...&quot;\n      }\n    }\n  ]\n}\n</code></pre>\n<p>Every row contains exactly those five fields. <code>source</code> and <code>editorial</code> each contain exactly <code>scope</code>, <code>sub_scope</code>, and <code>doc_id</code>. <code>publication</code> is either <code>null</code> or contains exactly <code>public_url</code>, <code>published_at</code>, and <code>generation</code>. UTC timestamps use the shown second-precision <code>Z</code> form. <code>generation</code> is <code>sha256:</code> plus the SHA-256 hex digest of the UTF-8 public URL, one null byte, and the exact public by-ID projection bytes. Rows are unique by exact source/editorial pair and sort deterministically by source scope, sub-scope, doc ID, then editorial scope, sub-scope, doc ID. A may own zero, one, or several rows; no row is primary.</p>\n<p>One table service validates the complete file, compares its pre-write digest, renders the complete next table deterministically, and atomically replaces it. Copy writes lineage only after the exact target mutation and rebuild succeed. If table follow-through then fails, Copy returns explicit stale lineage evidence and preserves the exact successful source-to-target mapping in its result/activity as the only recovery input; it never guesses or records a target that did not commit. Publish similarly mutates public output first, then reconciles the table from exact current public inventory. A post-public table failure is explicit and recoverable from that inventory, while a failed public mutation never advances publication evidence.</p>\n<p>Rows survive an unavailable or deleted B as exact historical lineage. Reports show that exceptional target as unavailable; they do not delete the row, choose a substitute B, or infer repair. The table, its digests, A identities, and recovery evidence never enter public configuration, manifests, locations, search, or by-ID payloads.</p>\n<h2>Copy, Replace, And Publish Boundary</h2>\n<p>The opt-in seam is one typed <code>document_lineage</code> customisation aspect with exact <code>contract_id</code>, <code>role</code>, and collection ownership. <code>dotlineform_projects</code> advertises contract <code>dotlineform_projects_to_analysis_works</code> with role <code>source</code>; <code>analysis_works</code> advertises the same contract with role <code>editorial</code>. Copy exposes New/Replace only when the exact source and target collection aspects match. Other Copy routes remain unchanged.</p>\n<p>New retains the shipped fresh-identity Copy transformation. Replace is allowed only for one B selected from A's current exact table rows and binds the confirmation receipt to B's collection, ID, and source digest. It preserves B's ID, filename, collection placement, <code>added_date</code>, and current <code>publishable</code>; replaces content and transferable metadata; advances <code>last_updated</code>; and rebuilds the exact target collection. It performs no diff, merge, synchronization, automatic choice, or arbitrary overwrite.</p>\n<p>Scope-wide Publish remains the only public mutation. Its desired public inventory is the authority for adding, retaining, changing, or clearing B publication evidence. The Projects stage uses exact current public projection, not <code>publishable</code>: a still-public B excluded from the next Publish remains 🟢 Published until successful Publish removes it; an editorial B without an exact public projection is 🟠 Pre-publish.</p>\n<h2>Report Row Grain</h2>\n<p>The normal <code>dotlineform/projects</code> list remains one row per working document A. With no B row it shows <strong>Working</strong>. With lineage, it shows one exact cue per B: <strong>🟠 Pre-publish</strong> when B exists locally without a current public projection, and <strong>🟢 Published</strong> when B has an exact current public projection. Mixed stages remain separate; focused detail exposes each B identity and Manage link. Unavailable is exceptional presentation, not a fourth lifecycle stage.</p>\n<p>The normal <code>analysis/works</code> list remains one row per B. Its existing exclusion icon is renamed to mean <code>publishable: false</code> with the accessible label <strong>Excluded from next Publish</strong>; any later actual-publication cue is separate. Local scopes show no publication-exclusion icon. Project State subject-route icons and clipboard text remain unchanged.</p>\n<h2>SSP-7.1a Proposed Footprint</h2>\n<p>The clean cut is materially larger than the original UI estimate. A static inventory currently finds the legacy names in 53 production/config files and 48 test/smoke files, with roughly 308 production/config and 399 test occurrences. Tracked sources also contain roughly 485 explicit-true declarations, five current public false records, and one local false fixture; configured external-local sources still require a read-only preflight. These counts are migration planning evidence, not a command to rewrite unrelated history.</p>\n<p>The direct production owners are <code>docs_source_model.py</code>; <code>docs_builder/source.py</code>, <code>payloads.py</code>, and <code>sub_scope.py</code>; <code>build_search.py</code>; <code>docs_publish_gate.py</code>; <code>docs_document_transfer_apply.py</code>; <code>docs_import_document.py</code> and the review/import adapters; <code>docs_management_document_target.py</code> and <code>docs_management_mutations.py</code>; <code>docs_live_rebuild_watcher.py</code>; <code>docs_source_config_settings.py</code>; <code>docs_sub_scope_lifecycle.py</code>; the document-package source, selection, transform, return, and service modules; Manage metadata/sub-scope/report modules; shared tree, sidebar, view-context, and report modules under <code>site/docs-viewer/runtime/js/shared/</code>; and the Docs Viewer scope/default, report, and package-profile configuration JSON. The same batch migrates tracked public/local source front matter, performs the explicit external-local migration, and regenerates affected private and public projections. It does not add the selection action yet.</p>\n<p>SSP-7.1a is expected to touch roughly 800–1,100 production/config/source lines and 900–1,300 focused test/fixture lines, mostly coordinated renames and source-fixture migration rather than new logic. It must be reviewed as a schema cutover: old source, requests, generated records, config keys, and packages fail closed after the cutover, and in-flight packages are regenerated rather than accepted through compatibility parsing.</p>\n<h2>SSP-7.1b Proposed Footprint</h2>\n<p>The action batch should add <code>docs-viewer/services/docs_management_publishable.py</code> and <code>docs-viewer/runtime/js/management/docs-viewer-management-publishable-workflow.js</code>; extend <code>docs_management_routes.py</code>, <code>docs_management_service.py</code>, <code>docs-viewer-management-client.js</code>, <code>docs-viewer-action-definitions.js</code>, <code>docs-viewer-management-control-renderers.js</code>, <code>docs-viewer-management-index-controller.js</code>, <code>docs-viewer-management-document-reports.js</code>, <code>docs-viewer-management-subscope-default-contribution.js</code>, and <code>docs-viewer-management.js</code>; and complete removal of the legacy metadata input wiring from <code>docs-viewer-management-shell-renderer.js</code>, <code>docs-viewer-management-modals.js</code>, <code>docs-viewer-management-modal-composition.js</code>, and <code>docs-viewer-management-metadata-workflow.js</code>.</p>\n<p>The endpoint accepts one exact publish-capable collection, non-empty exact <code>doc_ids</code>, and one boolean <code>publishable</code>; it validates every target before writing, maps true to omission, maps false to <code>publishable: false</code>, and uses the existing source-snapshot parent or sub-scope atomic write/rebuild boundary once. It does not loop over <code>/docs/update-metadata</code>, publish, alter descendants' fields, or add another front-matter key. The action batch remains roughly 300–450 production lines plus 300–450 focused test and smoke lines.</p>\n<h2>Durable Owner Transfer</h2>\n<p>After SSP-7.1a implementation, the primary <a href=\"/docs/?scope=studio&amp;doc=d-20260424-000000-e2408a\">Viewable field</a> owner becomes the <code>publishable</code> contract and must be rewritten rather than supplemented by a parallel page. The durable cross-system update also includes <strong>Public Scopes</strong>, <strong>Docs Data</strong>, <strong>Docs Viewer Builder</strong>, <strong>Search Build Pipeline</strong>, <strong>Docs Viewer Search</strong>, <strong>Generated Data Contracts</strong>, <strong>Projection Contract</strong>, <strong>Reports</strong>, <strong>Sub-Scope Index Architecture</strong>, <strong>Sub-Scope Public And Manage Manifests</strong>, <strong>Sub-Scope Customisation Architecture</strong>, <strong>Docs Import</strong>, <strong>Docs Import Architecture</strong>, <strong>Create And Import Endpoints</strong>, <strong>Source Mutation Endpoints</strong>, <strong>Generated Read Endpoints</strong>, <strong>Rebuild Follow-Through Scripts</strong>, <strong>Library Documents</strong>, <strong>Independent Canonical Documents</strong>, and the package Prepare/profile/return/review/export owners. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-194358-7717ee\">Sub-Scope Publishing Concept And Architecture</a> receives the new public-only terminology and local-scope boundary.</p>\n<p>Completed SSP-4 and SSP-5 delivery/evidence documents remain historical records of the contract that existed when those slices shipped. They are not mechanically rewritten; current owner documents and SSP-7 closeout carry the replacement contract, with a concise supersession note added to old delivery summaries only if navigation would otherwise be misleading.</p>\n<h2>Risks, Evidence, And Stop Condition</h2>\n<p>SSP-7.1a evidence must cover complete legacy-name absence, tracked and external-local source migration, public parent/child false values, local false-value review, creation defaults, hidden descendants, search, Publish, import, returned packages, reports, configuration, runtime projections, old-package rejection, and durable-document agreement. SSP-7.1b evidence must cover public parent and sub-scope targets, one and many checked documents, mixed and already-matching values, full prevalidation, stale/unavailable targets, atomic failure recovery, one exact rebuild, retained exclusion icons, metadata-modal removal, local-scope omission, and unchanged scope Publish. Later stages require focused table, Copy/Replace, Publish, generation, report, unavailable-target, and public-isolation evidence at their own gates.</p>\n<p>Stop before or during implementation if a local false value represents intentional discovery policy, the external-local source migration cannot be coordinated with the code cutover, an in-flight package cannot be regenerated, or the change appears to require a <code>viewable</code> alias, dual reader, inferred <code>discoverable</code> replacement, or mixed schema. The existing SSP-7 stops also remain: content comparison, diff, merge, synchronization, automatic New/Replace choice, title/subject/URL inference, one-to-one lineage, arbitrary overwrite, a generic provenance graph, a loose registry, a second Copy or Publish workflow, per-document Publish, selection fallback, public A identity, or changes to Project State or SSP-5 ownership.</p>\n"
}
