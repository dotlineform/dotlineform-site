{
  "doc_id": "d-20260727-224141-d28e4c",
  "title": "Tag Document Bootstrap Delivery",
  "added_date": "2026-07-27 22:41:41",
  "last_updated": "2026-07-27 22:41:41",
  "viewer_url": "/docs/?scope=studio&doc=d-20260727-224141-d28e4c",
  "parent_id": "d-20260726-211842-cc8f6c",
  "summary": "generate and link one Analysis tag document for every current canonical tag, rebuild the complete collection, and stop for management review",
  "ui_status": "proposed",
  "content_html": "<h1>Tag Document Bootstrap Delivery</h1>\n<h2>Target Result</h2>\n<p>One guarded migration creates a new public document in the Analysis <code>tags</code>\nsub-scope for every current canonical tag without a <code>doc_id</code>, copies the\nregistry description into that document once, and stores the allocated ID in\nthe registry row.</p>\n<p>The current planning snapshot contains 245 flat registry tags with no\n<code>doc_id</code> and three independent sub-scope documents. If that snapshot remains\nthe cutover input, the delivery creates 245 new linked sources and leaves the\nthree existing sources unchanged, producing 248 manageable documents in total.\nExecution must freeze and report the actual counts again.</p>\n<p>This is a bootstrap, not feature closeout. It makes the complete real\ncollection available so its navigation, discovery, metadata, and source-editing\nneeds can be reviewed before follow-on work is chosen.</p>\n<h2>Frozen Output Contract</h2>\n<p>For each input registry row, the migration allocates one immutable document ID\nand creates:</p>\n<pre><code class=\"language-markdown\">---\ndoc_id: &lt;allocated immutable document ID&gt;\ntitle: &quot;&lt;tag_id&gt;&quot;\nadded_date: &lt;cutover timestamp&gt;\nlast_updated: &lt;cutover date&gt;\nparent_id: &quot;&quot;\nviewable: true\n---\n# &lt;tag_id&gt;\n\n**&lt;group&gt;**\n\n&lt;tag registry description&gt;\n</code></pre>\n<p>The corresponding registry row gains that exact <code>doc_id</code>. The migration\npreserves tag order, <code>tag_id</code>, scalar <code>group</code>, <code>description</code>, row timestamps,\npolicy, aliases, assignments, and the three existing tag documents.</p>\n<p>The registry schema advances only far enough to retain and validate an optional\nunique <code>doc_id</code> during the feature transition. The bootstrap populates it for\nevery frozen input row; making it required for later New Tag and Edit Tag\nwrites belongs to follow-on delivery work.</p>\n<h2>In Scope</h2>\n<ul>\n<li>Add one repository-owned preview/write/validate command for this one-off\nbootstrap.</li>\n<li>Require the current flat registry schema, unique tag IDs, one allowed scalar\ngroup per tag, a description string, and no pre-existing registry <code>doc_id</code>\nvalues.</li>\n<li>Inventory every existing Analysis <code>tags</code> source before allocating IDs.</li>\n<li>Produce an inspectable preview containing the exact tag-to-document mapping,\ntarget paths, source count, registry projection, and input fingerprints.</li>\n<li>Allocate immutable IDs without matching documents by title, group, or\ncontent.</li>\n<li>Create every new Markdown source with exclusive-create semantics before\natomically replacing the registry.</li>\n<li>Add and validate unique registry <code>doc_id</code> values while preserving every\npre-existing tag value and all alias and assignment sources.</li>\n<li>Rebuild the Analysis documents, sub-scope manifest, and by-ID payloads;\nreload the local management inventory; and confirm the sub-scope documents\nremain excluded from parent-scope search.</li>\n<li>Reconcile the complete registry-to-document relationship and inspect the\nresulting collection through the delivered sub-scope management workflow.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>Reusing, rewriting, moving, linking, or deleting the three existing tag\ndocuments.</li>\n<li>Changing New Tag to create a document.</li>\n<li>Adding <code>doc_id</code> to Edit Tag or implementing stale-link repair.</li>\n<li>Synchronizing registry descriptions with document content in either\ndirection.</li>\n<li>Changing tag deletion or document deletion behavior.</li>\n<li>Adding group data, filtering, styling, or management fields to the public\nsub-scope manifest.</li>\n<li>Redesigning the report, metadata editor, source editor, or management\ntoolbar.</li>\n<li>Creating Tag semantic-token targets.</li>\n<li>Editing the generated prose beyond the fixed one-time seed template.</li>\n</ul>\n<p>Any discovery, filtering, bulk action, repair, or authoring need revealed by\nthe real collection becomes separate follow-on work.</p>\n<h2>Delivery Steps</h2>\n<ol>\n<li>[ ] <strong>Freeze the input.</strong> Record the registry version and hash, exact tag\ncount and order, zero current registry <code>doc_id</code> values, allowed groups, alias\nand assignment hashes, and the exact existing Analysis <code>tags</code> source\ninventory.</li>\n<li>[ ] <strong>Build the guarded preview.</strong> Project the complete immutable ID map,\nMarkdown sources, registry rows, target paths, and reconciliation counts\nwithout writing. A preview is invalidated by any registry or source-inventory\nchange.</li>\n<li>[ ] <strong>Review the exact cutover.</strong> Confirm every tag appears once, every\nallocated ID and path is unique, every source matches the fixed template,\nthe three existing sources are untouched, and only <code>doc_id</code> plus the\nregistry-level schema metadata change in canonical tag data.</li>\n<li>[ ] <strong>Apply once.</strong> Create only the previewed new source paths, replace the\nregistry atomically after all source writes succeed, and report any\nrun-created unreferenced files precisely if the registry commit cannot\ncomplete.</li>\n<li>[ ] <strong>Rebuild and reconcile.</strong> Build the Analysis document payloads, reload\nthe management inventory, and confirm parent-scope search still excludes\nsub-scope documents. Prove every registry <code>doc_id</code> is unique and resolves to\nits exact generated source and public detail URL. Prove aliases,\nassignments, descriptions, and the three pre-existing documents are\nunchanged.</li>\n<li>[ ] <strong>Review management and stop.</strong> Use the local managed <code>tags</code> report to\nnavigate the complete inventory and sample detail, metadata, source,\nBack/Forward, and narrow-layout behavior across all four groups. Record\nconcrete friction and follow-on candidates without implementing them in\nthis delivery.</li>\n</ol>\n<h2>Likely Authority</h2>\n<ul>\n<li><code>studio/data/canonical/tags/tag-registry.json</code></li>\n<li>a focused <code>studio/commands/</code> bootstrap command</li>\n<li>a focused <code>studio/services/tags/</code> projection and validation module</li>\n<li><code>studio/services/tags/tag_source_model.py</code></li>\n<li><code>studio/services/tags/tag_write_transactions.py</code></li>\n<li><code>docs-viewer/services/docs_document_identity.py</code></li>\n<li><code>docs-viewer/scopes/analysis/source/sub-scopes/tags/documents/</code></li>\n<li><code>docs-viewer/build/build_docs.py</code></li>\n<li><code>docs-viewer/build/docs_builder/sub_scope.py</code></li>\n<li><code>docs-viewer/build/build_search.py</code></li>\n<li>focused bootstrap projection, command, and reconciliation tests</li>\n</ul>\n<p>These are inspection starting points. The command owns the one-off\ncross-application composition; ordinary Tag Registry and Docs Viewer UI paths\ndo not become migration hosts.</p>\n<h2>Completion And Evidence</h2>\n<ul>\n<li>The reviewed preview and applied output report the same registry fingerprint,\ntag order, tag-to-document mapping, target paths, and counts.</li>\n<li>Exactly one new immutable document exists for every frozen input tag, and\nevery frozen input registry row contains its unique exact <code>doc_id</code>.</li>\n<li>Every linked ID resolves in the Analysis <code>tags</code> sub-scope and derives a\nworkable <code>/docs/?scope=analysis&amp;doc=d-20260624-213316-478639&amp;subdoc=&lt;doc_id&gt;</code>\nURL.</li>\n<li>The three pre-existing sources are byte-identical, remain independent, and\nare included alongside the generated documents in the management inventory.</li>\n<li>Registry descriptions remain present and unchanged after seeding. Aliases and\nassignments are byte-identical.</li>\n<li>Focused projection/command tests, the Analysis document build, explicit\nparent-search exclusion checks, canonical JSON validation,\n<code>git diff --check</code>, and exact source/payload counts support only the\nbootstrap claims above.</li>\n<li>Manual review records how the complete collection behaves and ends at a\ndecision gate for separate follow-on delivery documents.</li>\n</ul>\n<h2>Durable Destinations</h2>\n<p>After the bootstrap is accepted, record the transitional registry field and\none-time generation result in\n<a href=\"/docs/?scope=studio&amp;doc=d-20260331-000000-4519a5\">Tag Registry</a> and\n<a href=\"/docs/?scope=studio&amp;doc=d-20260419-000000-86255c\">Tag Source Data</a>.</p>\n<p>Any accepted management change belongs to\n<a href=\"/docs/?scope=studio&amp;doc=d-20260726-224323-eb0a65\">Manage Sub-Scope Documents</a>\nor a new focused successor, not to this bootstrap delivery.</p>\n"
}
