{
  "doc_id": "d-20260801-133319-cdfdbf",
  "title": "PRJ-4 — Resolver Tokens Delivery",
  "added_date": "2026-08-01 13:33:19",
  "last_updated": "2026-08-02 11:00:44",
  "viewer_url": "/docs/?scope=studio&doc=d-20260801-133319-cdfdbf",
  "parent_id": "d-20260801-095819-30103c",
  "summary": "add bounded one-hop resolver tokens backed by Project State relationships and semantic catalogue links",
  "ui_status": "proposed",
  "content_html": "<h1>PRJ-4 — Resolver Tokens Delivery</h1>\n<h2>Status And Purpose</h2>\n<p>Proposed as <strong>PRJ-4</strong> after Project State independently publishes a trustworthy Project resolver lookup.</p>\n<p>This delivery lets an author insert one explicit folder or Work reference into a document and render its current direct Project relationships at build time. It establishes a reusable bounded resolver capability without creating a global graph or extending Semantic Tokens beyond their identity-link purpose.</p>\n<h2>Complete Outcome</h2>\n<p>A managed document can contain a validated Resolver Token whose subject is an exact Project folder key or Work ID. Building that document performs one exact read from the Project State lookup and renders deterministic groups:</p>\n<pre><code class=\"language-text\">folder -&gt; Works[] + Project documents[]\nWork   -&gt; folders[] + Project documents[]\n</code></pre>\n<p>The source token remains unchanged. Zero matches, unavailable lookup, stale snapshot, and invalid subject states render explicitly. Expansion never scans the filesystem, Project manifest, canonical Works, Series, Moments, tags, or another relationship registry.</p>\n<p>Any returned item that identifies a registered Catalogue Work, Series, or Moment renders through the existing Semantic Tokens target identity, generated target lookup, and semantic-link projector rather than carrying or emitting a plain URL. It does not create Semantic Token syntax in source, but every successfully rendered semantic result does create a derived semantic usage row pointing to the containing Resolver Token occurrence. Project documents and validated folders retain their existing Docs Viewer and local-folder link contracts.</p>\n<h2>Source And Usage Examples</h2>\n<p>The reader-facing result is deliberately similar in all three cases: readable text links to Work <code>00008</code>. The source and generated usage evidence distinguish how the link came to exist.</p>\n<h3>Manually Authored Markdown Link</h3>\n<pre><code class=\"language-md\">[nerve](/works/?series=105&amp;work=00008)\n</code></pre>\n<p>This is an ordinary authored URL. The Markdown renderer produces an ordinary anchor, but there is no semantic identity in source and no semantic usage row. Changing or auditing the target depends on the URL text.</p>\n<h3>Authored Semantic Token</h3>\n<p>Using <strong>Add catalogue token</strong> inserts:</p>\n<pre><code class=\"language-md\">[[catalogue:work:00008|nerve]]\n</code></pre>\n<p>The builder resolves <code>(catalogue, work, 00008)</code>, renders the current semantic destination, and records one authored-token usage row at that exact source range.</p>\n<h3>Resolver-Generated Semantic Link</h3>\n<p>The exact Resolver Token syntax is a PRJ-4.0 decision. Until then this source is illustrative only; the angle-bracket value is not proposed serialization:</p>\n<pre><code class=\"language-md\">[[resolver:&lt;project-resolver-id&gt;:folder:projects/nerve]]\n</code></pre>\n<p>The Project lookup row selected by that Resolver Token may contain the semantic result descriptor:</p>\n<pre><code class=\"language-json\">{\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;work&quot;,\n  &quot;target_id&quot;: &quot;00008&quot;\n}\n</code></pre>\n<p>The builder passes that descriptor through the same semantic target lookup and link projector. It records one resolver-derived usage row for Work <code>00008</code> at the containing Resolver Token range. The source contains no fabricated <code>[[catalogue:work:...]]</code> text.</p>\n<p>The usage rows below are also illustrative; PRJ-4.0 freezes their exact schema and field names:</p>\n<pre><code class=\"language-json\">{\n  &quot;origin&quot;: &quot;authored_token&quot;,\n  &quot;source_doc_id&quot;: &quot;d-...&quot;,\n  &quot;source_range&quot;: {&quot;start&quot;: 120, &quot;end&quot;: 153},\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;work&quot;,\n  &quot;target_id&quot;: &quot;00008&quot;\n}\n</code></pre>\n<pre><code class=\"language-json\">{\n  &quot;origin&quot;: &quot;resolver_result&quot;,\n  &quot;source_doc_id&quot;: &quot;d-...&quot;,\n  &quot;source_range&quot;: {&quot;start&quot;: 120, &quot;end&quot;: 178},\n  &quot;resolver_id&quot;: &quot;&lt;project-resolver-id&gt;&quot;,\n  &quot;subject_kind&quot;: &quot;folder&quot;,\n  &quot;subject_key&quot;: &quot;projects/nerve&quot;,\n  &quot;family&quot;: &quot;catalogue&quot;,\n  &quot;target_type&quot;: &quot;work&quot;,\n  &quot;target_id&quot;: &quot;00008&quot;\n}\n</code></pre>\n<p>The visible link may be equivalent even when its exact route differs: a manual URL may preserve explicit Series context, while both semantic cases receive their current destination from the semantic target lookup. The same distinction applies to registered Series and Moment results.</p>\n<h2>Fixed Design Notes</h2>\n<ul>\n<li>A small global definition registry contains capability metadata only: resolver ID, access, accepted subjects, lookup/schema, and result groups.</li>\n<li>Relationship rows remain in resolver-owned generated lookups. Project State is the only Project lookup producer.</li>\n<li>Resolver depth is exactly one; returned items are never resolved again.</li>\n<li>Resolution order is exact parse, access check, lookup load, exact key read, declared-group rendering, and semantic-link projection for registered Catalogue targets. Semantic-link projection resolves presentation for one returned identity; it does not expand another relationship edge.</li>\n<li>Project results sort deterministically: Works by normalized Work ID, documents by title then <code>doc_id</code>, and folders by normalized relative path.</li>\n<li>A Work, Series, or Moment result carries the registered semantic identity <code>(catalogue, target type, canonical id)</code>. Project State does not become a second canonical owner of its URL or title.</li>\n<li>The existing generated Semantic Tokens target lookup is the only secondary data source allowed for those catalogue result links. A missing, stale, or unusable semantic target renders the resolver's explicit unavailable state; it never falls back to a plain URL stored in the Project lookup and produces no resolved usage row.</li>\n<li>Reusing semantic-link projection does not imply that the initial Project resolver must return Series or Moment groups. It governs any Work, Series, or Moment item a declared resolver group already returns, without adding a new relationship or chained lookup.</li>\n<li>Each successfully projected catalogue result adds one row to the containing scope's generated semantic usage index. One Resolver Token may therefore produce several rows with the same containing source range and different semantic target identities.</li>\n<li>The usage row distinguishes an authored Semantic Token from a resolver-derived result. Its common fields retain source scope/document, containing source range, displayed title, semantic family/type/id, and resolved <code>href</code>; its resolver origin records enough resolver and subject identity to explain why the link was generated. It does not invent a raw Semantic Token string.</li>\n<li>The Semantic Tokens report includes resolver-derived rows and links back to the managed document and containing Resolver Token location, so every generated semantic link remains discoverable.</li>\n<li>Project folder subject keys are relative to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code>, so explicitly linked contained folders outside <code>projects/</code> remain addressable without widening discovery scans.</li>\n<li>The initial Project resolver and local folder links are Manage-only.</li>\n<li>Resolver expansion is read-only build presentation, not stored relationship truth or permission to mutate an input.</li>\n<li>Lookup refresh and containing-document rebuild are separate operations.</li>\n<li>The token syntax must serialize an exact subject without pretending that a folder path is permanent identity.</li>\n<li>Resolver Tokens have their own parser, authoring contribution, resolver contract, group renderer, and access policy. They do not reuse Semantic Token syntax or authoring semantics. Their renderer reuses the registered semantic target identity, link projection, and usage-index owner for catalogue result items instead of implementing parallel link or relationship inventories.</li>\n</ul>\n<h2>Delivery Discipline</h2>\n<p>PRJ-4 should remain a bounded lookup consumer: a small resolver-definition registry, exact token parser, one-row relationship reader, declared-group renderer, bounded semantic-link projector, and thin Source insertion workflow. It must not become a graph database, general query language, registry scanner, recursive traversal engine, or live relationship service.</p>\n<p>PRJ-4.0 must record the exact proposed files, reused builder/editor seams, and rough production/test diff size. If the generic core grows beyond what the single Project resolver needs, pause for review and defer the additional abstraction until a second proven consumer requires it. Line count remains a scope signal rather than an optimization target.</p>\n<h2>Rough Delivery Steps</h2>\n<h3>PRJ-4.0 — Freeze Syntax And Generic Resolver Contract</h3>\n<ul>\n<li>[ ] Trace the current Markdown build pipeline, Source token insertion, Semantic Token parser/renderer/usage boundaries, access projection, and dependency-aware rebuild behavior.</li>\n<li>[ ] Freeze exact token syntax for resolver ID, subject kind, and subject key; escaping; invalid/unavailable/stale output; and source preservation.</li>\n<li>[ ] Freeze the global definition schema, lookup interface, result-group schema, ordering, semantic-target descriptor, semantic-link projection, and Manage/public access failure.</li>\n<li>[ ] Freeze the rule that Work, Series, and Moment results use the existing semantic target lookup and link projector without producing source token syntax or falling back to plain URLs.</li>\n<li>[ ] Freeze the semantic usage-index schema evolution: authored and resolver-derived origins, common fields, resolver/subject provenance, one row per rendered result, containing source range, targeted-build preservation, and report presentation.</li>\n<li>[ ] Freeze one shared comparison fixture covering the manual Work URL, authored Work Semantic Token, and Resolver-generated Work semantic link above, including their rendered anchors and different usage-index outcomes.</li>\n<li>[ ] Record the exact file list, reused seams, rough production/test diff estimate, and any proposed expansion before implementation approval.</li>\n<li>[ ] Prove the Project State lookup can answer every example with one keyed relationship read and no downstream relationship lookup; semantic link projection may resolve only the exact returned Catalogue identities.</li>\n</ul>\n<p><strong>Checkpoint:</strong> reviewed fixtures cover zero, one, many, duplicate-document, missing-folder, invalid-subject, missing-lookup, stale-lookup, and public-access cases, plus resolved and unavailable Work, Series, and Moment semantic targets. Fixtures also prove the exact derived usage rows for one Resolver Token that renders multiple semantic links. Stop before parser or renderer implementation.</p>\n<h3>PRJ-4.1 — Implement Resolver Core And Project Adapter</h3>\n<ul>\n<li>[ ] Add the strict code-owned definition registry and schema/version checks.</li>\n<li>[ ] Add the builder parser and one-lookup resolver interface.</li>\n<li>[ ] Register the Project resolver against the PRJ-3 generated lookup.</li>\n<li>[ ] Render accessible deterministic groups and explicit empty/error states without mutating source.</li>\n<li>[ ] Route every declared Work, Series, or Moment result through the existing semantic target lookup and semantic-link projector; emit no resolver-owned plain catalogue URL or fabricated Semantic Token source text.</li>\n<li>[ ] Extend the generated semantic usage index and report with explicit resolver-derived occurrences tied to the containing Resolver Token range, while preserving untouched documents during targeted builds.</li>\n<li>[ ] Keep resolver lookup paths, local URLs, and service capabilities out of public build/runtime data.</li>\n</ul>\n<h3>PRJ-4.2 — Add Authoring Workflow</h3>\n<ul>\n<li>[ ] Add a Source-editor contribution that selects or pastes an exact allowed subject, previews current matches, and inserts the validated token.</li>\n<li>[ ] Preserve the committed subject exactly; do not infer it from the current document, selected Work, title, or displayed URL.</li>\n<li>[ ] Reuse existing authoring space and target validation where suitable, but keep Resolver and Semantic Token modes distinct.</li>\n</ul>\n<h3>PRJ-4.3 — Verify And Transfer</h3>\n<ul>\n<li>[ ] Run focused parser, registry/lookup agreement, builder, rendering, authoring, semantic-link projection, derived usage indexing/reporting, targeted-build preservation, stale/unavailable, access-isolation, lint, and whitespace checks.</li>\n<li>[ ] Manually review a Project document with realistic Work/document counts, empty groups, long labels, stale data, and narrow layout.</li>\n<li>[ ] Prove that building a token-bearing document performs no filesystem or canonical registry scan and does not refresh Project State.</li>\n<li>[ ] Document the generic shipped resolver extension seam in a durable Docs Viewer architecture owner, and update Semantic Tokens Architecture and its report contract for resolver-derived usage; retain Project-specific result meaning here.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>recursive traversal or adjustable depth;</li>\n<li><code>folder -&gt; Work -&gt; Series/tags</code> chaining;</li>\n<li>adding Work, Series, or Moment relationship groups that are not already present in the resolver-owned lookup;</li>\n<li>relationship-path explanations or graph visualization;</li>\n<li>a global relationship database;</li>\n<li>automatic rebuilding of every document that uses a changed resolver row;</li>\n<li>public exposure of local Project relationships; or</li>\n<li>migration of Semantic Tokens into Resolver Tokens.</li>\n</ul>\n<h2>Completion Evidence</h2>\n<ul>\n<li>One exact occurrence produces only the precomputed one-hop groups declared by its resolver.</li>\n<li>Every returned Work, Series, or Moment uses its registered semantic identity and the shared semantic-link projection. It never renders from a plain URL owned by the resolver lookup.</li>\n<li>Every resolver-generated semantic link creates one explicit derived usage row tied to its containing Resolver Token range. The Semantic Tokens report can distinguish its origin and open the owning document at that location.</li>\n<li>Maintained examples prove that a manual Work URL, authored Work Semantic Token, and Resolver-generated Work semantic link can present the same useful reader destination while retaining their different source and usage contracts.</li>\n<li>Resolver-derived usage rows never fabricate Semantic Token source syntax. An unavailable semantic target produces neither a link nor a resolved usage row and does not fall back to an unvalidated URL.</li>\n<li>Instrumented tests prove no build-time scan or chained relationship resolution; semantic target projection is bounded to exact returned identities.</li>\n<li>Source remains byte-stable across successful, empty, stale, and unavailable expansion.</li>\n<li>Authoring always inserts an explicit validated subject and never uses a UI fallback.</li>\n<li>Public builds and runtime remain free of the Manage-only Project definition, lookup, local paths, and rendered local results.</li>\n</ul>\n"
}
