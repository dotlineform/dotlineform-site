{
  "doc_id": "d-20260501-000000-49b626",
  "title": "Browser Smoke Testing",
  "added_date": "2026-05-01 00:00:00",
  "last_updated": "2026-08-14 21:18:02",
  "viewer_url": "/docs/?scope=studio&doc=d-20260501-000000-49b626",
  "parent_id": "d-20260501-174746-efd581",
  "content_html": "<h1>Browser Smoke Testing</h1>\n<h2>When A Smoke Is Appropriate</h2>\n<p>Use a permanent browser smoke only when the durable risk exists in the browser integration boundary:</p>\n<ul>\n<li>route shell/config/template/controller boot;</li>\n<li>browser module loading and public/private asset separation;</li>\n<li>local service reachability and request/response agreement;</li>\n<li>shared ready/busy state;</li>\n<li>public read-only versus management capability separation.</li>\n</ul>\n<p>Use pure/service/API tests for deterministic data behavior. Use manual or temporary browser checks for copy, layout, hover/focus, modal timing, and pointer feel. Mobile ergonomics is acceptance only for public <code>site/</code> routes under their owning public feature.</p>\n<p>Do not add a smoke as routine follow-through from a UI change. Its review must identify the single browser-only boundary at risk, the regression it catches, and why lower-level or manual evidence is inadequate.</p>\n<h2>Ownership</h2>\n<table>\n<thead>\n<tr>\n<th>Owner</th>\n<th>Location</th>\n<th>Profile</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Docs Viewer</td>\n<td><code>docs-viewer/tests/smoke/</code></td>\n<td><code>docs-viewer-smoke</code></td>\n</tr>\n<tr>\n<td>Studio/public catalogue</td>\n<td><code>studio/tests/smoke/</code></td>\n<td><code>studio-smoke</code></td>\n</tr>\n</tbody>\n</table>\n<p>Keep niche/expensive/recent-fix scripts focused rather than adding everything to a profile. A retained smoke should have a clear trigger for when to run it.</p>\n<p>The retained Docs Viewer profile contains exactly four browser boundaries:</p>\n<ul>\n<li>standalone Manage route boot and configured service projection;</li>\n<li>Docs Review route and API authority;</li>\n<li>public Analysis read-only isolation and compact payload reads;</li>\n<li>external-local scope build addressability and lazy Mermaid rendering.</li>\n</ul>\n<p>Adding another boundary requires an explicit test-policy decision. Do not collect these scripts through the broad <code>docs</code> pytest profile.</p>\n<p>The retained Studio profile contains exactly three browser boundaries:</p>\n<ul>\n<li>local Catalogue route boot and same-origin service isolation;</li>\n<li>local Tag route boot and same-origin service isolation;</li>\n<li>one representative checked public Catalogue Work route without local Studio capability.</li>\n</ul>\n<p>Catalogue and Tag mutations, rendering helpers, projections, and configuration remain deterministic Python or source contracts. Adding another Studio browser boundary requires an explicit test-policy decision.</p>\n<h2>Size And Structure</h2>\n<p>Each executable smoke owns one route or integration boundary. Shared startup/readiness code lives in a small non-executable support module; executable smokes are not fixture libraries.</p>\n<p>A browser script over 500 lines, or one that spans more than one route/workflow owner, requires deletion or split review before it can be extended. Test-file size is not evidence of assurance.</p>\n<h2>Correct Runtime Target</h2>\n<ul>\n<li>local app route: owning local server/API;</li>\n<li>public site or public Docs Viewer: checked-in <code>site/</code> served over HTTP;</li>\n<li>module-contract smoke: explicit source-module fixture root;</li>\n<li>route-specific fixture: only when the script owns its setup.</li>\n</ul>\n<p>Do not use <code>file://</code>, repo root, or a stale <code>_site</code> as public evidence. A pass proves only the target/profile actually run.</p>\n<p>Run local route smokes at a representative laptop/desktop viewport. Do not add phone-width or touch-specific passes for local routes. A public mobile check must use the checked public artifact and belongs to the public route's feature and test owner.</p>\n<h2>Readiness</h2>\n<ol>\n<li>Navigate and wait for DOM content.</li>\n<li>For participating routes, call <code>tests/smoke/route_ready_helpers.py::wait_for_route_ready</code> with the app root and ready/busy attributes.</li>\n<li>Assert only the route-specific mode, service, asset, or request condition named by the smoke.</li>\n</ol>\n<p>Do not add arbitrary sleeps or additional UI choreography after semantic readiness unless the interaction itself is the approved browser-only boundary.</p>\n<h2>Public Docs Viewer Contract</h2>\n<p>The public Analysis smoke should prove public payload reads and absence of management assets/services. It should use the configured public tree/recent/by-id/search contracts rather than local management indexes.</p>\n<h2>Ready-State Audit</h2>\n<p>Template-level ready/busy structure is cheaper to prove with:</p>\n<pre><code class=\"language-bash\">$HOME/miniconda3/bin/python3 tests/audits/route_ready_state.py --strict\n</code></pre>\n<p>The audit does not replace a smoke when runtime controller behavior is the risk.</p>\n<h2>Manual Pairing</h2>\n<p>When the result also depends on visual fit or tactile behavior, state the manual follow-up separately. Automated evidence should not claim to prove what it did not inspect.</p>\n<h2>Limits</h2>\n<ul>\n<li>Browser fixtures can duplicate runtime config/data and drift from production owners.</li>\n<li>Profile names do not imply exhaustive app coverage.</li>\n<li>Local ports/process cleanup can create false failures unrelated to the product contract.</li>\n</ul>\n"
}
