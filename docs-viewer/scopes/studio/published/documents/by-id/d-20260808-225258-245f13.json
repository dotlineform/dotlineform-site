{
  "doc_id": "d-20260808-225258-245f13",
  "title": "Docs Viewer Scope Type Migration Concept",
  "added_date": "2026-08-08 22:52:58",
  "last_updated": "2026-08-08 23:18:32",
  "viewer_url": "/docs/?scope=studio&doc=d-20260808-225258-245f13",
  "parent_id": "d-20260808-225257-c06ae2",
  "summary": "define exact workspace relocation and public-projection transitions while preserving independent per-media-type storage policy",
  "ui_status": "proposed",
  "content_html": "<h1>Docs Viewer Scope Type Migration Concept</h1>\n<h2>Purpose</h2>\n<p>Changing a Docs Viewer scope type should preserve one established body of work while changing its canonical workspace profile and whether its documents can be publicly projected. It must feel like one management action, but the service must treat it as a planned migration of scope-root storage, configuration, routing, publication capability, and generated products.</p>\n<p>The operation preserves the exact <code>scope_id</code>, document and report-host identities, parent relationships, sub-scope identities, authored Docs links, and logical <code>docs/&lt;scope&gt;/...</code> media references. It does not create a replacement scope or reinterpret content.</p>\n<h2>Supported Profiles</h2>\n<p>The current type choices bundle two distinct concerns:</p>\n<table>\n<thead>\n<tr>\n<th>profile</th>\n<th>canonical and local-published provider</th>\n<th>exposure</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>external (<code>local_external</code>)</td>\n<td><code>external_local</code></td>\n<td>local management only</td>\n</tr>\n<tr>\n<td>local repository (<code>local_committed</code>)</td>\n<td><code>repository</code></td>\n<td>local management only</td>\n</tr>\n<tr>\n<td>public (<code>public_readonly</code>)</td>\n<td><code>repository</code></td>\n<td>local management plus public route and projections</td>\n</tr>\n</tbody>\n</table>\n<p>External storage with public exposure is not a supported fourth profile. The management UI may present the three familiar types, but migration planning should distinguish two exact changes:</p>\n<ol>\n<li><strong>Relocate storage</strong> between <code>external_local</code> and <code>repository</code> while remaining local.</li>\n<li><strong>Change public exposure</strong> between local-repository and public while remaining repository-backed.</li>\n</ol>\n<p>A later interaction may compose these stages, but their ownership and proof remain separate.</p>\n<h2>Independent Media Policy</h2>\n<p>Per-media-type storage is not part of scope type. <a href=\"/docs/?scope=studio&amp;doc=d-20260808-231126-6a5d72\">Scope Media Storage Locations</a> owns repository, external-local, and R2 destination policy plus verified provider migration.</p>\n<p>Scope Type Migration preserves every explicit media destination. Media configured as scope-local follows <code>scope_root</code> when that root moves because following the root is its chosen location policy. Entering public mode validates that referenced media destinations are publicly deliverable and blocks when they are not; it does not select or move a provider. Leaving public mode reports media that remains publicly served but does not move or delete it. Local scope access is not a promise that all media bytes are private.</p>\n<h2>Operator Workflow</h2>\n<p>An eligible scope gains <strong>Change scope type</strong> beside its existing lifecycle actions.</p>\n<ol>\n<li>Choose one of the other supported target profiles.</li>\n<li>For a public target, choose and validate the public route.</li>\n<li>Request a server-owned preview.</li>\n<li>Review canonical-root movement, any scope-local media that follows it, preserved explicit media destinations, regenerated products, config/manifest changes, routes and public projections, publication consequences, cleanup, and blockers.</li>\n<li>Confirm the exact previewed target profile.</li>\n<li>Apply recomputes and revalidates the plan, performs the migration, refreshes capabilities, and returns an exact result and navigation target.</li>\n</ol>\n<p>The browser submits identities and choices, never authoritative paths or a filesystem plan. There is no in-place form for <code>scope_type</code>, scope-root provider, or <code>public_projection</code>; media destination changes use their separate lifecycle workflow.</p>\n<h2>Transition Contracts</h2>\n<table>\n<thead>\n<tr>\n<th>transition</th>\n<th>required result</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>external -&gt; local repository</td>\n<td>Copy the lifecycle-owned scope container into <code>docs-viewer/scopes/&lt;scope&gt;</code>, including media configured as scope-local; byte-verify relocated canonical/scope-local material, preserve explicit media destinations, rebuild documents/search, switch config and manifest ownership, then remove the inactive external root. Git may show additions without becoming migration authority.</td>\n</tr>\n<tr>\n<td>local repository -&gt; external</td>\n<td>Verify that the configured external provider is available and the exact target is unoccupied, copy and byte-verify the scope container including scope-local media, preserve explicit media destinations, rebuild under the target configuration, switch authority, then remove the repository root. Git may show removals.</td>\n</tr>\n<tr>\n<td>local repository -&gt; public</td>\n<td>Keep canonical source and media policy unchanged; require publicly deliverable destinations for media needed by public documents; add the exact public route and parent/child document/search projections; rebuild the local workspace; establish an unpublished public capability; do not promote document content.</td>\n</tr>\n<tr>\n<td>public -&gt; local repository</td>\n<td>Preserve repository source and media policy; remove public-only document metadata, route, document/search projections, and derived Catalogue document locations; report any media that remains publicly served; rebuild the local-only workspace.</td>\n</tr>\n<tr>\n<td>external &lt;-&gt; public</td>\n<td>Compose the relevant storage relocation and public-exposure stages through local repository. The first implementation should not offer a direct combined transition.</td>\n</tr>\n</tbody>\n</table>\n<p>Each direction is an explicit supported transition, not the inverse invocation of Create or Delete. Create defaults cannot reconstruct an established scope, and Delete cannot serve as the commit mechanism for an identity that must continue to exist.</p>\n<h2>Migration Inventory</h2>\n<p>Preview classifies current state by ownership rather than assuming every file should be moved:</p>\n<table>\n<thead>\n<tr>\n<th>class</th>\n<th>migration treatment</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>canonical Markdown and scope-local source/published media</td>\n<td>copy with <code>scope_root</code> when workspace storage changes; byte-verify before commit</td>\n</tr>\n<tr>\n<td>explicitly located published media</td>\n<td>preserve the configured destination; validate public-delivery compatibility without relocating bytes</td>\n</tr>\n<tr>\n<td>reproducible documents, search, manifests, Recent, Index, and derived references</td>\n<td>rebuild from canonical source using the target configuration</td>\n</tr>\n<tr>\n<td>scope configuration and lifecycle manifest</td>\n<td>transform only migration-owned topology fields while preserving the established scope record and ownership history</td>\n</tr>\n<tr>\n<td>public route, route registry, public document/search snapshots, and derived Catalogue URLs</td>\n<td>add or remove exactly when exposure changes</td>\n</tr>\n<tr>\n<td>independently owned external asset collections</td>\n<td>report but do not move, delete, or adopt</td>\n</tr>\n</tbody>\n</table>\n<p>The target configuration must be derived from the current record, not generated afresh from scope-creation defaults. Existing default document identity, configured sub-scopes, report hosts, source build inputs, media types, custom published locations, non-loadable ids, manage-only roots, and other unaffected settings remain intact unless the target profile makes one explicitly invalid.</p>\n<h2>Preview And Blockers</h2>\n<p>The write-free plan should include:</p>\n<ul>\n<li>current and target profiles and their storage/exposure axes;</li>\n<li>exact current and target canonical roots;</li>\n<li>canonical file and scope-local-media counts, sizes, providers, and verification method;</li>\n<li>explicit media destinations that remain unchanged and their public-delivery compatibility;</li>\n<li>generated roots to rebuild rather than copy;</li>\n<li>config, manifest, route registry, route shell, public document/search projection, and Catalogue changes;</li>\n<li>all configured one-level sub-scopes and their report hosts;</li>\n<li>exact documents whose publication-only front matter must be normalized;</li>\n<li>documents that omission would make eligible at the next public Publish;</li>\n<li>the proposed commit point, cleanup stages, and recovery meaning;</li>\n<li>management and public URLs after success; and</li>\n<li>a server-issued plan identity or source snapshot that apply can revalidate.</li>\n</ul>\n<p>The plan blocks at least:</p>\n<ul>\n<li>a missing, unreadable, unsafe, or unavailable current source;</li>\n<li>a missing or unwritable target provider;</li>\n<li>an occupied target root, route collision, or escaping/symlink path;</li>\n<li>incomplete lifecycle ownership or unexpected state that makes cleanup ambiguous;</li>\n<li>unsupported recursive sub-scope topology or a child that cannot receive the target projection contract;</li>\n<li>invalid source/front matter or a target rebuild failure;</li>\n<li>a public target with referenced media whose configured destination is not publicly deliverable; and</li>\n<li>any source/config revision change between preview and apply.</li>\n</ul>\n<p>Unavailable does not mean empty. Migration must never create a replacement source at an unavailable current root or infer content from stale generated products.</p>\n<h2>Apply And Recovery</h2>\n<p>Apply should use one exact scope-level busy boundary so the live watcher cannot rebuild or publish that scope during migration. Local services do not need a multi-user transaction framework.</p>\n<p>The normal sequence is:</p>\n<ol>\n<li>Recompute and revalidate the preview against current config, manifest, source revision, paths, route ownership, and provider capabilities.</li>\n<li>Create a lifecycle-owned staging location under the target provider. It must not use a sync-excluded generic <code>tmp</code> directory name.</li>\n<li>Copy canonical source and scope-local media that follows a relocated <code>scope_root</code>.</li>\n<li>Compare the complete relocated source/scope-local inventories and byte hashes.</li>\n<li>Build target local documents and search without changing current configured authority.</li>\n<li>Validate target configuration and generated products.</li>\n<li>Revalidate that preserved explicit media destinations remain compatible with the target document-access profile.</li>\n<li>Commit transformed config, route records, route shell, and lifecycle manifest as the new authority.</li>\n<li>Remove or retire the old root and perform exact negative public document/search cleanup where required.</li>\n<li>Refresh browser config, capabilities, indexes, watcher state, and navigation for the unchanged scope identity.</li>\n</ol>\n<p>Before the commit point, failure removes or reports the inactive stage and leaves current authority unchanged. After the commit point, the service does not restore the old config or read from both roots. It returns <code>committed: true</code>, the failed stage, completed verification, exact remaining follow-through, and whether the same cleanup action is safely retryable. If old-root cleanup fails, the old bytes remain an explicitly inactive orphan rather than a fallback source.</p>\n<h2>Public Exposure And Publish</h2>\n<p>Type migration must not become a second positive publication workflow.</p>\n<h3>Entering Public</h3>\n<p>The transition creates the public-capable configuration, route shell, and empty or absent-safe public projections. The public route reports <strong>Not published yet</strong> until the existing scope-wide Publish promotes content. It must not copy the complete local working projection merely because the type changed.</p>\n<p>Local documents cannot already contain <code>publishable</code>, so their omission means included at the next Publish under the established false-only exclusion contract. Preview reports the exact eligible document counts for the parent and every sub-scope. After migration, the operator may use the existing Set Publishable action before choosing Publish; migration does not invent an initial all-excluded state or an additional approval gate.</p>\n<h3>Leaving Public</h3>\n<p>Local collections do not accept <code>publishable</code> front matter. Preview therefore lists every exact document containing the field, and confirmed migration removes that field as type normalization without changing title, body, identity, relationships, or other front matter.</p>\n<p>Leaving public is complete when the route shell/registry, public document and search projections, parent and child document-location records, and derived Catalogue document URLs are removed. Existing negative public-delete planning can inform this cleanup, but the continuing scope identity requires a dedicated migration receipt.</p>\n<p>Configured repository-public or R2 media remains at its explicit destination. Preview reports those remaining public URLs so the operator does not mistake local document access for media privacy. Moving or deleting that exposure is a separate confirmed Scope Media Storage Locations operation.</p>\n<h2>Sub-Scopes And Identity</h2>\n<p>The migration unit is the top-level scope plus all configured one-level sub-scopes. Storage relocation moves their canonical source and local products inside the common container. Public exposure adds or removes each child projection and preserves its exact report host.</p>\n<p>If any child cannot be planned exactly, the parent transition is blocked. The service cannot migrate only the parent, drop a child, infer a report host from title or route, or materialize recursive sub-scopes.</p>\n<p>Because <code>scope_id</code>, <code>sub_scope</code>, and <code>doc_id</code> do not change, authored Docs links and logical media references remain stable. Generated viewer URLs and public locations are rebuilt from the target configuration; source text is not searched or rewritten.</p>\n<h2>Eligibility</h2>\n<p>The first eligibility boundary is user-created, lifecycle-created scopes whose manifest and configured topology agree. System/backfilled scopes remain visible but cannot be migrated through this workflow. Supporting them later requires an explicit ownership/adoption decision rather than weakening the manifest check.</p>\n<p>The UI projects eligible targets per scope. A scope may support workspace relocation but not public exposure when one or more configured media destinations are not publicly deliverable. Capability absence is truthful product state and should route the operator to the separate media-location action rather than silently changing policy during scope migration.</p>\n<h2>Proof Gate</h2>\n<p>A delivery is not complete without focused evidence that proves:</p>\n<ul>\n<li>canonical source and scope-local media inventories are byte-identical across workspace relocation;</li>\n<li>the immutable scope, document, sub-scope, and report-host identities and relationships are unchanged;</li>\n<li>established unaffected configuration fields survive transformation;</li>\n<li>one and only one configured canonical root remains after success;</li>\n<li>target local documents, search, media reads, browser config, and capabilities rebuild successfully;</li>\n<li>generated products were rebuilt rather than accepted as migration authority;</li>\n<li>entering public exposes no document content until the existing Publish action is applied;</li>\n<li>the first Publish uses the established <code>publishable</code> false-only contract for parent and child collections;</li>\n<li>entering public preserves compatible explicit media destinations and blocks incompatible ones without moving bytes;</li>\n<li>leaving public removes exact document routes, projections, locations, and Catalogue URLs while reporting unchanged public media exposure;</li>\n<li>independently owned external asset collections are untouched;</li>\n<li>watcher suppression and refresh prevent mixed-profile products; and</li>\n<li>injected failures before and after the commit point produce the documented unchanged or committed-recovery result without dual-root fallback.</li>\n</ul>\n<p>The proof should cover all four adjacent directed transitions. External-to-public composition is not claimed until both stages can be executed and recovered independently.</p>\n<h2>Delivery Seams</h2>\n<p>The concept admits independently complete deliveries without pre-authorizing them:</p>\n<ul>\n<li>common migration preview, transformed-config, receipt, and fault/recovery contract;</li>\n<li>external/local-repository storage relocation;</li>\n<li>local-repository-to-public enablement while preserving the single positive Publish workflow;</li>\n<li>public-to-local-repository conversion with publication-only metadata normalization and exact public document cleanup; and</li>\n<li>optional composed external/public interaction after the adjacent transitions are proven.</li>\n</ul>\n<p>Any future delivery must choose one complete outcome, begin with the Roadmap sanity gate, inspect the current owners, and stop rather than silently absorbing another transition.</p>\n<h2>Exclusions</h2>\n<p>This concept does not introduce:</p>\n<ul>\n<li>external public scopes or arbitrary source roots;</li>\n<li>a generic provider matrix exposed to operators;</li>\n<li>Create-target-then-Delete-source composition;</li>\n<li>scope, document, or media-reference renaming;</li>\n<li>document comparison, merge, synchronization, or automatic editorial decisions;</li>\n<li>automatic public publication during migration;</li>\n<li>media destination changes or R2/repository/external-local byte migration, which belong to Scope Media Storage Locations;</li>\n<li>duplicate config readers, aliases, fallback roots, or compatibility routing;</li>\n<li>migration of independently owned asset collections;</li>\n<li>a persistent migration daemon, background queue, or iCloud/R2 synchronization service; or</li>\n<li>a claim that a partial config edit or remaining public document/search projections constitute success.</li>\n</ul>\n<h2>Durable Transfer</h2>\n<p>When the feature ships, <a href=\"/docs/?scope=studio&amp;doc=d-20260515-000000-f43740\">Scope Lifecycle</a> should own the migration workflow, eligibility, transaction/recovery behavior, and current weak spots. <a href=\"/docs/?scope=studio&amp;doc=d-20260331-000000-f992b9\">Public Scopes</a> should own the entering/leaving-public and sole-positive-Publish contract. Endpoint request/response details should live in <a href=\"/docs/?scope=studio&amp;doc=d-20260607-222033-3e391b\">Scope Lifecycle Endpoints</a>.</p>\n"
}
