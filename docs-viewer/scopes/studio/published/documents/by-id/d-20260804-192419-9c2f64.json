{
  "doc_id": "d-20260804-192419-9c2f64",
  "title": "SSP-0.0 — Projects Subject Assignment Sanity Note",
  "added_date": "2026-08-04 19:24:19",
  "last_updated": "2026-08-04 19:31:43",
  "viewer_url": "/docs/?scope=studio&doc=d-20260804-192419-9c2f64",
  "parent_id": "d-20260804-164519-c7ad4e",
  "summary": "accept the Projects None and Folder assignment foundation while retaining later Work and Series outcomes in SSP-1 and SSP-4",
  "ui_status": "done",
  "content_html": "<h1>SSP-0.0 — Projects Subject Assignment Sanity Note</h1>\n<h2>Decision</h2>\n<p>The Projects None/Folder foundation fits the shipped sub-scope customisation, exact managed-document target, metadata, modal, local-folder normalization, revision-safe mutation, targeted rebuild, and access-projection owners. Info can remain passive, the registered definition can remain the single capability and field authority, exact managed targets can remain the mutation authority, and public routes can retain their current compact metadata policy.</p>\n<p>The original SSP-0 outcome also required Work/Series display, malformed/conflicting subject states, and acceptance evidence for an <code>analysis/works</code> copy. Those outcomes did not fit the current schedule: SSP-1 introduces <code>work_id</code> and <code>series_id</code> meaning and normalized subject states, while SSP-4 owns the later <code>analysis/works</code> editorial workflow. The configured Analysis scope currently contains only the <code>tags</code> sub-scope.</p>\n<p>The approved resolution keeps SSP-0 responsible for passive exact-target Info, the Projects <code>authoring_subject</code> capability, None/Folder assignment, generic metadata cleanup, and the SSP-1 extension seam. SSP-1 adds and proves Work/Series display and invalid/conflicting state projection; SSP-4 explicitly creates <code>analysis/works</code> and proves the eventual copy remains read-only. SSP-0.1 may proceed on that boundary.</p>\n<h2>Scheduling And Accepted Foundation</h2>\n<p>SSC-1 is complete. The registered customisation definition owns typed <code>DocsSubScopeAssignableFieldGroup</code> declarations, validates unique group and field identities, requires Manage browser access, exposes the server declarations through <code>sub_scope_customisation_assignable_field_groups()</code>, and projects only group IDs to the browser. The shared browser configuration already normalizes those IDs and provides <code>hasDocsViewerAssignableFieldGroup()</code> as the focused-action availability check.</p>\n<p>The <code>dotlineform_projects</code> registration remains the exact current owner of Projects manifest data, source validation, metadata read/update, import normalization, and Manage-only browser composition. SSP-0 may add <code>authoring_subject</code> with server-owned <code>folder_path</code>; other registrations retain their own declared capabilities. Browser availability derives only from the resolved registered group.</p>\n<p>The shared source model does not yet own normalized Work/Series authoring subjects. The configured Analysis scope has the <code>tags</code> sub-scope and no current <code>works</code> collection. Pulling either responsibility into SSP-0 would reverse the feature sequence and broaden this UI/mutation foundation into SSP-1 or SSP-4 implementation.</p>\n<h2>Broad Owners And Blast Radius</h2>\n<table>\n<thead>\n<tr>\n<th>concern</th>\n<th>current owner and SSP-0 boundary</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Info hosting</td>\n<td>the shared hosted-view context, Info panel host, and <code>metadata-info</code> view retain view-agnostic passive presentation; SSP-0 supplies an explicit active managed-document target and a Projects None/Folder read projection</td>\n</tr>\n<tr>\n<td>exact sub-scope detail</td>\n<td>the shared sub-scope report owns validated detail identity and exact <code>{scope, sub_scope, doc_id}</code> actions; the Info bridge must use that active detail rather than the parent report document or Index selection</td>\n</tr>\n<tr>\n<td>capability and subject fields</td>\n<td>the registered sub-scope customisation definition owns availability and server field names; Projects adds only <code>authoring_subject</code> and <code>folder_path</code> in SSP-0</td>\n</tr>\n<tr>\n<td>folder normalization</td>\n<td>the shared local-link service owns confined absolute path, local <code>file:</code> URL, and portable relative-target normalization; the focused workflow reuses that result rather than adding another parser</td>\n</tr>\n<tr>\n<td>focused interaction</td>\n<td>the Manage modal shell and exact customisation composition own lifecycle, focus return, contained status, cancellation, and capability-selected contribution loading</td>\n</tr>\n<tr>\n<td>mutation and rebuild</td>\n<td>exact managed-target resolution, source revision checks, front-matter formatting, atomic source write, and sub-scope rebuild remain with the management service owners; the subject mutation restricts accepted keys through the resolved assignable group</td>\n</tr>\n<tr>\n<td>generic metadata separation</td>\n<td>the app-level metadata workflow retains universal document fields; its Projects custom editor is removed only after the focused None/Folder workflow proves equivalent ownership</td>\n</tr>\n<tr>\n<td>assurance</td>\n<td>focused customisation, exact-target metadata/mutation, sub-scope report module, metadata Info, service-route, and public-runtime boundaries cover the changed contracts without policing modal choreography</td>\n</tr>\n</tbody>\n</table>\n<p>The credible SSP-0 blast radius is therefore limited to the shared hosted-view target context, sub-scope detail contribution, Projects registration and projection, Manage focused-action/modal composition, the existing folder normalizer and mutation planner, generic metadata cleanup, targeted rebuild follow-through, and their focused tests. Public configuration and runtime remain passive consumers of their current compact document metadata. Work/Series normalization and Analysis Works configuration remain outside this blast radius under the current sequence.</p>\n<h2>Accepted Deliverables</h2>\n<ul>\n<li><code>metadata-info</code> shows one compact read-only None or Folder summary for the exact active Projects document using the accepted current source projection.</li>\n<li>Only a resolved sub-scope declaring <code>authoring_subject</code> exposes <strong>Assign subject</strong>. Projects initially offers None and Folder; currently configured collections retain their own declared capabilities.</li>\n<li>The modal carries the exact target and source revision, reuses the existing folder normalizer, confirms clear or replacement explicitly, contains cancellation and error state, and delegates atomic write plus the required rebuild to existing service owners.</li>\n<li>The app-level <strong>Edit metadata</strong> workflow stops reading, rendering, and submitting Projects <code>folder_path</code> after the focused owner is proven.</li>\n<li>SSP-1 can extend the same Projects group and modal with server-owned <code>work_id</code> and <code>series_id</code> plus existing Catalogue search components while Info remains read-only.</li>\n</ul>\n<p>The delivery now defers Work, Series, malformed/conflicting multi-field states, and the future <code>analysis/works</code> copy to SSP-1 and SSP-4 rather than treating them as current SSP-0 acceptance targets.</p>\n<h2>Exact Target And Access Boundary</h2>\n<p>The action target is always the explicit <code>{scope, sub_scope, doc_id}</code> resolved from the active validated sub-scope detail. A parent report document, selected Index row, title, route label, filename, or previously active document never substitutes for that target. Opening Info or opening and cancelling the modal performs no repair, precedence choice, or mutation.</p>\n<p>Projects customisation browser composition remains Manage-only. Public routes retain their current compact metadata projection and passive runtime. Read/display support and quick-assignment support remain separate capabilities, allowing SSP-1 and SSP-4 to add a read-only Analysis Work/Series subject later while the quick editor remains selected by the registered Projects group.</p>\n<h2>Risks And Stop Gate</h2>\n<p>The main implementation risks are projecting the parent report instead of the active sub-document, leaving two <code>folder_path</code> editors, accepting unregistered fields through a generic mutation, duplicating folder normalization between Source and modal workflows, losing stale-revision protection, leaking private subject data into public payloads, or activating the workflow through a Projects name check. The sequencing risk is importing Work/Series normalization or Analysis Works creation merely to satisfy premature SSP-0 evidence. Focused exact-target, wrong-target, group-presence, invalid/outside-path, clear/cancel, stale-revision, atomic-write, targeted-build, generic-metadata, and public-isolation evidence must contain the implementation risks at the owning stages.</p>\n<p>The scheduling contradiction is resolved through the accepted delivery split. Stop and return for review if the revised slice requires inline Info editing, a generic field-schema engine, broad application-state access, literal scope/sub-scope name checks, an unrestricted cross-collection write service, duplicated folder normalization, public subject controls, Tag workflow changes, or another parallel metadata owner.</p>\n"
}
