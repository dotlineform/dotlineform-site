{
  "doc_id": "d-20260801-133318-15e8fa",
  "title": "SSP-2 — Docs Viewer Project State Delivery",
  "added_date": "2026-08-01 13:33:18",
  "last_updated": "2026-08-04 23:31:24",
  "viewer_url": "/docs/?scope=studio&doc=d-20260801-133318-15e8fa",
  "parent_id": "d-20260802-194357-4d857b",
  "summary": "deliver scanned-real-folder Project State with assigned documents and canonical Work counts grouped by Series",
  "ui_status": "proposed",
  "content_html": "<h1>SSP-2 — Docs Viewer Project State Delivery</h1>\n<h2>Status And Purpose</h2>\n<p>Proposed as <strong>SSP-2</strong> after the completed SSP-1 subject foundation. <a href=\"/docs/?scope=studio&amp;doc=d-20260802-121105-c574f9\">Project State Concept And Architecture</a> owns the folder-only filter and reconciliation model consumed by this delivery.</p>\n<p>This delivery builds Project State as a persistent generated Docs Viewer document plus a versioned folder relationship snapshot and Manage-only resolver lookup. It does not retire the existing image-audit workflow; that belongs to <a href=\"/docs/?scope=studio&amp;doc=d-20260802-121106-15ea55\">SSP-6 — Project Media Audit Delivery</a>.</p>\n<h2>Accepted SSP-1 Input</h2>\n<p>SSP-2 consumes the private <code>dotlineform/projects</code> <code>manage-manifest.json</code> and <code>subject-associations.json</code> as one generation. The Manage manifest supplies <code>subject_generation</code> plus exact document rows whose <code>authoring_subject</code> is <code>{state, kind, key, fields}</code> with optional private <code>evidence</code>. The association product uses <code>docs_subject_associations_v1</code>, repeats that generation and exact collection, and groups each valid <code>{kind, key}</code> to ordered exact document targets and current Manage locations. SSP-2 must require matching receipts before joining document presentation to folder associations.</p>\n<p>Only associations whose subject is <code>{kind: &quot;folder&quot;, key: &quot;projects/&lt;path&gt;&quot;}</code> and whose exact key matches a scanned real-folder row enter Project State. Valid folder assignments elsewhere under the base root are ordinary non-participants, as are Work, Series, None, malformed, and conflicting subject rows. The Folder icon, Catalogue icon, warning cue, and Info presentation are Manage UI projections rather than Project State inputs. SSP-2 takes subject identity exclusively from normalized state and the matching association receipt.</p>\n<h2>Complete Outcome</h2>\n<p>One stable document in scope <code>dotlineform</code> contains the last successfully generated Project State Markdown. Its primary table has exactly three relationship columns: <strong>Folder</strong>, <strong>Docs</strong>, and <strong>Works by Series</strong>. Each row originates from one scanned real folder. Project documents and canonical Works join independently by exact normalized folder identity; a document never mediates the Work relationship. Each linked canonical Series label is followed by the number of matching canonical Works whose current <code>series_ids</code> include that Series. Project State counts records in <code>works.json</code>; it does not enumerate files or distinguish draft from published Works.</p>\n<p>The producer scans <code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code> only; it never scans the base root or sibling trees. Every row therefore has an exact normalized key beginning <code>projects/</code>. Report labels remove that guaranteed prefix and prepend <code>/</code>, so <code>projects/curve/poems (2025)</code> appears as <code>/curve/poems (2025)</code>. Finder activation and all snapshot/lookup joins continue to use the complete normalized key.</p>\n<p>For example:</p>\n<table>\n<thead>\n<tr>\n<th>Folder</th>\n<th>Docs</th>\n<th>Works by Series</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>/curve/poems (2025)</code> opens in Finder</td>\n<td>one or more exact Project-document links</td>\n<td><a href=\"/series/?series=036\"><code>curve poems</code></a> — 87 works</td>\n</tr>\n<tr>\n<td><code>/example/document-only</code> opens in Finder</td>\n<td>one or more exact Project-document links</td>\n<td>no canonical Works</td>\n</tr>\n<tr>\n<td><code>/example/works-only</code> opens in Finder</td>\n<td>no document</td>\n<td><a href=\"/series/?series=001\"><code>series 1</code></a> — 12 works</td>\n</tr>\n<tr>\n<td><code>/example/folder-only</code> opens in Finder</td>\n<td>no document</td>\n<td>no canonical Works</td>\n</tr>\n</tbody>\n</table>\n<p>If a folder's Works belong to two Series, the final cell renders one linked line per Series, such as <code>series 1 — 87 works</code> and <code>series 2 — 1 work</code>. A Work belonging to both Series contributes once to both membership counts; the figures are not a physical-file count or an implied unique total.</p>\n<p>Running or explicitly refreshing Project State computes the folder-centred snapshot, saved report source, and resolver lookup from one generation, then rebuilds the exact report document. Opening the saved document does not scan the filesystem. Known Docs Viewer and Studio mutations update only affected relationships after their canonical commits; full refresh remains available for direct Finder changes and recovery.</p>\n<p>SSP-3 can consume the accepted lookup as soon as its generation, failure, and access contracts are proven. The current <code>/studio/project-state/</code> route and <code>var/studio/reports/project-state.md</code> remain until SSP-6 preserves and renames their image-audit capability.</p>\n<h2>Fixed Design Notes</h2>\n<ul>\n<li>The normalized relationship is <code>scanned real folder -&gt; documents[]</code> and, independently, <code>scanned real folder -&gt; works[] -&gt; series memberships[]</code>.</li>\n<li>One row represents one real folder established by the current scan. Multiple Project documents, Works, and Series remain ordered arrays rather than duplicate rows or scalar shortcuts.</li>\n<li>Only documents with one valid <code>folder_path</code> beginning <code>projects/</code> and exactly matching a scanned row participate. Valid assignments elsewhere under the base root, no-subject, Work-subject, Series-subject, malformed, conflicting, and recorded-only paths remain outside Project State.</li>\n<li>Every canonical Work whose recorded source folder matches the normalized row remains explicit by semantic Work identity and current canonical <code>series_ids</code>. Draft/published status and publication date are outside Project State.</li>\n<li>Matched Works are grouped by canonical Series ID for report presentation. Each Series count is the number of matched Work records whose <code>series_ids</code> include that ID; one Work may contribute to more than one Series count.</li>\n<li>Folder identity is relative to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code>, while the complete scan boundary is its <code>projects/</code> subtree. The report omits the guaranteed leading <code>projects/</code> from the displayed label; identity and activation retain it.</li>\n<li>The saved report is generated Markdown with a stable <code>doc_id</code>, not a dynamic report module. Manual edits to its generated body are not preserved.</li>\n<li>Opening displays the last complete report. Explicit Run/Refresh performs the full scan; no filesystem watcher is introduced.</li>\n<li>The folder lookup returns <code>Work[] + Series[] + Project documents[]</code> for one exact folder subject. Work and Series items carry semantic catalogue identity rather than stored titles or URLs; Series membership is retained without Work publication status.</li>\n<li>Trusted application mutations default to targeted follow-through. Work create/delete, folder fields, and <code>series_ids</code> can change row evidence; Work title, status, publication date, and <code>project_filename</code> are unrelated.</li>\n<li>The exact Work observer is server-side after canonical commit and read-only with respect to Studio. It uses exact before/after records, never rewrites Studio, and never makes the canonical commit depend on Project State.</li>\n<li>Derived-generation failure never rolls back or repeats a committed Work or document mutation. The prior complete generation remains readable and is marked stale.</li>\n<li>Project State is Manage-only derived data and unusable as canonical truth.</li>\n</ul>\n<h2>Delivery Discipline</h2>\n<p>SSP-2 is inherently larger than the completed Projects glue deliveries, so its size must be controlled per checkpoint rather than hidden in one replacement diff. It should compose existing canonical readers, the Projects Manage manifest, exact Docs Viewer source/build seams, generated-payload writers, and server-side post-commit boundaries around one Project-specific producer.</p>\n<p>SSP-2.0 is a short scope-and-spec sanity gate over the producer/lookup, saved report workflow, and targeted follow-through. It confirms broad owners, blast radius, deliverables, risks, and stop conditions without freezing exact files, fixtures, estimates, or implementation. A dynamic report framework, generic graph, event bus, repair system, rewritten catalogue reader, or media-audit migration is scope creep. Each implementation stage ends with focused evidence and an independent code/diff review before the next stage begins.</p>\n<h2>Delivery Steps</h2>\n<h3>SSP-2.0 — Confirm Scope And Delivery</h3>\n<ul>\n<li>[ ] Sanity-check the folder-centred Project State, Series-grouped canonical Work counts, saved-report, focused-lookup, and targeted-follow-through outcome against the accepted SSP-1 Projects subject contract and current catalogue, Projects manifest, generated-document, and post-commit models; resolve only contradictions that would change the result.</li>\n<li>[ ] Confirm the broad blast radius across folder inventory/reconciliation, canonical Works and Series reads, Project associations, versioned snapshot/lookup, generated Markdown, explicit Run/Refresh, exact builds, and post-commit impact handling.</li>\n<li>[ ] Confirm the deliverables: one matching report/snapshot/lookup generation, stable Folder/Docs/Works-by-Series document, exact scanned-folder lookup, full refresh, bounded follow-through, stale-state retention, focused evidence, and concise durable owner updates.</li>\n<li>[ ] Stop for review if the result requires a dynamic report framework, generic graph, event bus, repair system, rewritten catalogue reader, media-audit migration, or another boundary excluded above. Do not produce an exact file inventory, line estimate, frozen implementation, fixture implementation, or complete test architecture at this gate.</li>\n</ul>\n<p><strong>Checkpoint:</strong> a concise sanity note confirms the revised typed-subject spec, scheduling dependencies, broad owners, blast radius, deliverables, risks, local/public boundary, and stop condition. Stop before implementation approval.</p>\n<h3>SSP-2.1 — Build The Folder-Centred Producer And Lookup</h3>\n<ul>\n<li>[ ] Implement the confined physical scan of <code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code> that establishes the complete real-folder row inventory before recorded-system evidence is joined; prove the base root and sibling trees are not scanned.</li>\n<li>[ ] Build one exact Project-document index from the customisation-owned Manage manifest and one independent canonical Work index from recorded source folders; left-join both indexes to scanned rows without allowing either recorded route to create a row.</li>\n<li>[ ] Retain each matched canonical Work by semantic identity and current canonical <code>series_ids</code>.</li>\n<li>[ ] Derive each folder's ordered Series memberships and exact Work counts from the retained Works' canonical <code>series_ids</code>.</li>\n<li>[ ] Retain fully reconciled, document-only, Works-only, folder-only, duplicate-document, and incomplete-Series states for scanned rows without treating other authoring-subject kinds or recorded-only paths as Project State rows.</li>\n<li>[ ] Publish one versioned snapshot and exact scanned-folder-keyed lookup whose groups are <code>Work[] + Series[] + Project documents[]</code>.</li>\n<li>[ ] Prove lookup rows contain semantic Work/Series identities and exact Series membership without Work publication status, Work/Series document associations, stored catalogue URLs, or live-scan fallbacks.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused physical-inventory, independent document/Work join, normalization, Series-membership-count, lookup-schema, generation, and public-isolation evidence passes. An independent code/diff review confirms the physical scan alone establishes rows, the producer retains exact Work identity without copying catalogue presentation, and recorded-only paths remain with a separate report owner. Stop before SSP-2.2.</p>\n<h3>SSP-2.2 — Publish The Saved Docs Viewer Report</h3>\n<ul>\n<li>[ ] Create one stable generated Markdown document in scope <code>dotlineform</code> with a clearly owned body and ordinary Docs Viewer reachability.</li>\n<li>[ ] Render the Folder/Docs/Works-by-Series table, compact freshness context, Finder-opening folder links, exact Project-document links, linked Series labels with matching canonical Work counts, and explicit document-only, Works-only, folder-only, and fully reconciled states.</li>\n<li>[ ] Apply the display-only folder rule: <code>projects/&lt;path&gt;</code> appears as <code>/&lt;path&gt;</code>, while stored identity, lookup joins, and <code>dlf-local:</code> activation retain the complete base-relative key.</li>\n<li>[ ] Add the explicit Run/Refresh workflow that computes and validates one generation, saves the report source, publishes its matching snapshot/lookup, and rebuilds the exact document.</li>\n<li>[ ] Preserve the previous complete report and lookup on generation failure; opening the document must never trigger a scan.</li>\n<li>[ ] Verify Source readability, normal word wrapping, search/bookmark behavior, long cells, and table presentation at representative laptop/desktop widths.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused generated-document, source-write, exact-build, report/lookup agreement, failure-retention, opening-without-scan, and representative presentation evidence passes. An independent code/diff review confirms stable document ownership while targeted mutation follow-through remains SSP-2.3-owned and Studio media-audit retirement remains SSP-6-owned. Stop before SSP-2.3.</p>\n<h3>SSP-2.3 — Add Targeted Follow-Through And Verify</h3>\n<ul>\n<li>[ ] Add one Project State impact classifier at the shared server-side Work committed-mutation boundary.</li>\n<li>[ ] Route create, save, delete, bulk save, and import through complete exact before/after deltas or the explicit full-rebuild fallback.</li>\n<li>[ ] Add equivalent exact Project-document metadata/create/delete follow-through for valid <code>folder_path</code> declarations only.</li>\n<li>[ ] Recompute affected folder memberships and Series counts for Work folder/Series changes and Project-document folder changes only when the affected exact key is present in the scanned inventory; prove recorded-only keys do not create Project State rows.</li>\n<li>[ ] Regenerate the compact saved Markdown from the updated snapshot without scanning unrelated inputs.</li>\n<li>[ ] Preserve the last complete generation and report follow-through failure without retrying the canonical mutation.</li>\n<li>[ ] Run focused model, normalization, folder-to-Work, Series-membership-count, display-path, generated-document, mutation-delta, generation-agreement, stale-state, exact-link, semantic-descriptor, public-isolation, lint, and whitespace checks.</li>\n<li>[ ] Manually review fully reconciled, document-only, Works-only, folder-only, no-Series, duplicate-document, error, and long-cell states at representative laptop/desktop widths.</li>\n<li>[ ] Perform the independent final code/delivery review, reconcile completion claims with exact evidence and omissions, and update only durable owners whose shipped contracts changed.</li>\n</ul>\n<h2>Not In Scope</h2>\n<ul>\n<li>automatic repair or synchronization;</li>\n<li>mutation controls in the report;</li>\n<li>image discovery or primary-image auditing, which belongs to SSP-6;</li>\n<li>scanning <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code> itself or reporting physical folders and recorded paths outside its <code>projects/</code> subtree;</li>\n<li>reverse reconciliation of document or Work paths that do not match a scanned real folder, which belongs to a separate report;</li>\n<li>continuous folder watching;</li>\n<li>public Project State data;</li>\n<li>Work/Series reverse associations, Manage index subject cues, editorial copy, or public Work/Series <code>doc_url[]</code> projection;</li>\n<li>retirement of <code>/studio/project-state/</code> or <code>var/studio/reports/project-state.md</code>, which belongs to SSP-6; or</li>\n<li>Resolver Token parsing and rendering, which belongs to <a href=\"/docs/?scope=studio&amp;doc=d-20260802-174340-2fe9f4\">SSP-3</a>.</li>\n</ul>\n<h2>Completion Evidence</h2>\n<ul>\n<li>Fixture and real-data evidence prove the physical scan alone establishes folder rows and independent joins produce fully reconciled, document-only, Works-only, folder-only, duplicate-document, and incomplete-Series states.</li>\n<li>The saved Docs Viewer document persists the latest successful Folder/Docs/Works-by-Series Markdown, presents default <code>projects/</code> paths without that prefix, and opens without scanning.</li>\n<li>Full refresh observes direct Finder changes and publishes one matching report/snapshot/lookup generation.</li>\n<li>Targeted tests show bounded relationship effects for exact scanned-folder matches, no Project State row creation from recorded-only paths, and no effect from Work filename, title, status, or publication-date changes.</li>\n<li>SSP-3 can resolve one folder row to semantic Work/Series descriptors and exact Project-document targets without a live scan.</li>\n<li>Public projections contain no local folder paths, Project State data, lookup path, service URL, or refresh capability.</li>\n<li>The current Studio image-audit workflow remains unchanged for SSP-6 rather than being partially retired here.</li>\n</ul>\n"
}
