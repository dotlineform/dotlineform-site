{
  "doc_id": "d-20260801-150013-f26b87",
  "title": "Local File And Folder Links Concept",
  "added_date": "2026-08-01 15:00:13",
  "last_updated": "2026-08-01 15:16:10",
  "viewer_url": "/docs/?scope=studio&doc=d-20260801-150013-f26b87",
  "parent_id": "d-20260801-095819-30103c",
  "summary": "define base-relative local targets, dlf-local Markdown links, confined Manage activation, and bounded public projection",
  "ui_status": "proposed",
  "content_html": "<h1>Local File And Folder Links Concept</h1>\n<h2>Purpose</h2>\n<p>Docs Viewer needs links that can open local files or folders without saving a\nmachine-specific absolute path in document source, front matter, manifests, or\nStudio data.</p>\n<p>The configured <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code> is runtime infrastructure.\nThe durable target is only a normalized POSIX path relative to that root:</p>\n<pre><code class=\"language-text\">projects/3 symbols\n</code></pre>\n<p>This is a navigation convenience, not a filesystem identity, registry entry,\nupload destination, or promise that the target exists.</p>\n<h2>Target And Link Syntax</h2>\n<p>Structured fields already supply the local-target meaning, so they store the\nplain relative value:</p>\n<pre><code class=\"language-yaml\">folder_path: &quot;projects/3 symbols&quot;\n</code></pre>\n<p>An ordinary Markdown destination such as <code>projects/3%20symbols</code> is ambiguous:\nit already means a relative web or document link. Body links therefore use an\nexplicit behaviour marker:</p>\n<pre><code class=\"language-markdown\">[3 symbols](dlf-local:projects/3%20symbols)\n</code></pre>\n<p><code>dlf-local:</code> says how Docs Viewer should activate or project the link. It is\nnot part of the target identity. After parsing and decoding, the local service\nreceives only <code>projects/3 symbols</code>.</p>\n<p>This avoids both problematic alternatives:</p>\n<ul>\n<li>a <code>file:///Users/...</code> link embeds private, machine-specific configuration;</li>\n<li>treating every relative Markdown link as local would break ordinary relative\nweb, document, image, and attachment links.</li>\n</ul>\n<h2>Paste Experience</h2>\n<p>There is no clipboard monitoring. When the author presses Command-V in an\neligible Source editor, the browser supplies <code>text/plain</code> on that one <code>paste</code>\nevent. Docs Viewer may intercept that event without polling the clipboard or\nrequesting continuing clipboard-read permission.</p>\n<p>The recognizer accepts one complete <code>file:</code> URL, absolute POSIX path, or\nshell-backslash-escaped absolute path. It treats the text as inert data and\nchecks whether the decoded path is contained below the configured base root.</p>\n<pre><code class=\"language-text\">paste event\n    -&gt; recognize one supported absolute local path\n    -&gt; verify it is below DOTLINEFORM_PROJECTS_BASE_DIR\n    -&gt; strip the configured root\n    -&gt; normalize to a relative POSIX target\n    -&gt; insert a dlf-local Markdown link\n</code></pre>\n<p>For example, this pasted text:</p>\n<pre><code class=\"language-text\">/configured/base/projects/3\\ symbols\n</code></pre>\n<p>produces:</p>\n<pre><code class=\"language-markdown\">[3 symbols](dlf-local:projects/3%20symbols)\n</code></pre>\n<p>If the pasted text is outside the configured root, mixed with other prose,\nmultiline, ambiguous, or in a code context, Docs Viewer does not intercept it;\nthe browser performs the ordinary paste. Manually authored <code>dlf-local:</code> links\nmust pass the same relative-target validation when rendered or activated.</p>\n<p>The editor reports the conversion and preserves ordinary Undo. A body paste\nnever writes front matter. A dedicated metadata field may reuse the same\nnormalizer but saves only the relative target.</p>\n<h2>Manage Rendering And Activation</h2>\n<p>The Markdown renderer recognizes <code>dlf-local:</code> as an explicit local-link kind.\nIn local Manage it produces a normal visible anchor carrying the relative\ntarget. The Docs Viewer runtime intercepts its click and sends that exact\ntarget to the local management service instead of asking the browser to invoke\na custom protocol.</p>\n<p>The server:</p>\n<ol>\n<li>rejects empty or absolute targets, URL schemes, NUL bytes, and <code>..</code>\ntraversal;</li>\n<li>joins the relative target beneath the configured base root;</li>\n<li>resolves the existing file or folder and rechecks containment, including\nsymlink resolution; and</li>\n<li>only then invokes the platform action, initially Open in Finder on macOS.</li>\n</ol>\n<h3>What Server Confinement Means</h3>\n<p>The server does not sandbox Finder or the macOS <code>open</code> command. “Confinement”\nmeans server-side contained path resolution before <code>open</code> is called. The\nendpoint must not trust a browser-supplied string merely because the ordinary\nUI created it.</p>\n<p>Without that validation, a manually authored or directly requested target\ncould attempt to escape the configured root:</p>\n<pre><code class=\"language-text\">../../Desktop\n%2e%2e/%2e%2e/Desktop\nprojects/link-to-somewhere-else\n</code></pre>\n<p>The server decodes first, rejects traversal and absolute input, joins the\nrelative value to <code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code>, and verifies the resolved\nexisting target still lies below that root. It then calls <code>open</code> with the\nvalidated path as one direct process argument, never by interpolating it into\na shell command.</p>\n<p>This check confines the <strong>local-link endpoint</strong>, not <code>open</code>. It prevents the\nendpoint from becoming an arbitrary-filesystem opener while leaving the final\nplatform action very simple.</p>\n<p>Symlinks are the one explicit policy choice. Strict containment rejects an\nin-root symlink whose resolved target is outside the configured base. Allowing\nsuch links would be possible, but it would mean the configured root is a\nlexical authoring boundary rather than the real activation boundary. PRJ-1.0\nmust freeze that choice; strict resolved-path containment is the proposed\ndefault.</p>\n<p>A contained target may be stored before it exists. Activation of a missing\ntarget returns an explicit not-found response and never falls back to another\nfolder, the current document, its parent, or a browser URL.</p>\n<p>The configured base root is the activation boundary. The Projects feature\nusually targets <code>projects/...</code>, but any valid relative target below\n<code>$DOTLINEFORM_PROJECTS_BASE_DIR/</code> is permitted.</p>\n<h2>Public Projection</h2>\n<p>Local activation is Manage-only, but a <code>dlf-local:</code> link may be authored in a\ndocument that also has a public projection. The public builder must recognize\nthe marker and emit inert output rather than a clickable custom-scheme link.</p>\n<table>\n<thead>\n<tr>\n<th>authored link</th>\n<th>public result</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>safe descriptive label</td>\n<td>retain the label as inert text</td>\n</tr>\n<tr>\n<td>empty label</td>\n<td>emit an explicit local-link placeholder</td>\n</tr>\n<tr>\n<td>label that itself exposes a disallowed path</td>\n<td>emit the placeholder rather than the label</td>\n</tr>\n</tbody>\n</table>\n<p>This is a bounded link-projection refinement. The generated source and target\ncontain no absolute base path, and the public payload contains neither the\n<code>dlf-local:</code> marker nor an activation endpoint.</p>\n<p>It does not solve the wider private-content problem. An absolute path pasted\nas prose, raw HTML, or fenced code is deliberately not converted by the\nlocal-link recognizer and still needs the general public-build secret/path\nverification policy. That policy is a separate delivery because it applies to\nall content, not only explicit local links.</p>\n<h2>Invariants</h2>\n<ul>\n<li>The canonical target is a normalized base-relative POSIX path.</li>\n<li><code>$DOTLINEFORM_PROJECTS_BASE_DIR</code> is never stored in authored or generated\nProject link data.</li>\n<li><code>dlf-local:</code> is a link-behaviour marker, not a registry or semantic token.</li>\n<li>Structured metadata stores the target, not the marker or a <code>file:</code> URL.</li>\n<li>Local links and <code>folder_path</code> are Docs Viewer concerns; this link contract\nneither reads nor writes Studio records, fields, UI, routes, or services.</li>\n<li>Body links are never silently promoted into metadata.</li>\n<li>Storage does not assert existence; every activation revalidates it.</li>\n<li>Local activation is available only through the local Manage service.</li>\n<li>Public projection makes explicit local links inert.</li>\n<li>Ordinary relative and external links retain their existing meaning.</li>\n</ul>\n<h2>Ownership</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260801-133316-a435ad\">PRJ-1 — Docs Viewer Local Folder Links Delivery</a>\ndelivers the reusable authoring, rendering, activation, and inert public\nprojection contract. <a href=\"/docs/?scope=studio&amp;doc=d-20260801-133315-80f527\">Projects Architecture</a>\ndefines how Project metadata, Project State, and Resolver Tokens consume the\nrelative target without taking ownership of the folder.</p>\n"
}
