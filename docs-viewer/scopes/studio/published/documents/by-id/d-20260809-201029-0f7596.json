{
  "doc_id": "d-20260809-201029-0f7596",
  "title": "CPB-1 — Catalogue Projection Boundary Cutover Delivery",
  "added_date": "2026-08-09 20:10:29",
  "last_updated": "2026-08-09 20:34:55",
  "viewer_url": "/docs/?scope=studio&doc=d-20260809-201029-0f7596",
  "parent_id": "d-20260428-000000-f5ff18",
  "summary": "make Studio read canonical Catalogue data and make each public route consume only its own lean or exact generated projection",
  "ui_status": "in-progress",
  "content_html": "<h1>CPB-1 — Catalogue Projection Boundary Cutover Delivery</h1>\n<h2>Status And Purpose</h2>\n<p>CPB-1.1 completed on 2026-08-09 and the delivery is parked before CPB-1.2 implementation approval. CPB-1 removes accidental cross-layer consumers left around the public Catalogue aggregate indexes: Studio currently reads public-site payloads, selected public routes load global indexes for focused records, Recent uses the Series index to choose destinations, and the Catalogue Search builder reads public outputs instead of canonical source.</p>\n<p>The tracked public artifact also currently contains five draft Works in <code>works_index.json</code> and in exact public Work payloads, plus one stale exact payload for a draft Series. CPB-1 therefore begins with a separate containment checkpoint before the wider ownership cutover.</p>\n<h2>Complete Outcome</h2>\n<p>Studio reads canonical Catalogue source or Studio-owned lookup/service projections only. It never reads <code>site/</code> as source or as a local application read model.</p>\n<p>The public Catalogue has these runtime boundaries:</p>\n<table>\n<thead>\n<tr>\n<th>route</th>\n<th>owned runtime data</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>/series/</code></td>\n<td>one lean Series list index containing only fields needed to render, sort, and route Series cards</td>\n</tr>\n<tr>\n<td><code>/series/?series=&lt;series_id&gt;</code></td>\n<td>the exact Series payload, including its canonically ordered lightweight Work members</td>\n</tr>\n<tr>\n<td><code>/works/?work=&lt;work_id&gt;</code></td>\n<td>the exact Work payload plus the exact relevant Series payload when Series navigation is available</td>\n</tr>\n<tr>\n<td><code>/work-details/?detail=&lt;detail_uid&gt;</code></td>\n<td>the exact parent Work payload</td>\n</tr>\n<tr>\n<td><code>/recent/</code></td>\n<td>the Recent index, including each entry's resolved public destination</td>\n</tr>\n<tr>\n<td><code>/catalogue/search/</code></td>\n<td>the Catalogue Search index</td>\n</tr>\n<tr>\n<td>bare <code>/works/</code></td>\n<td>redirect to <code>/series/</code></td>\n</tr>\n</tbody>\n</table>\n<p><code>site/assets/data/works_index.json</code> and its generator, configuration, route, test, and documentation obligations retire after their last consumer is removed. The <code>/works/</code> shell remains because exact Work routes use it. All public Work and Series projections contain published records only.</p>\n<h2>Accepted Ownership Rules</h2>\n<ul>\n<li>Canonical Works, Series, status, membership, ordering, and primary-Work meaning remain owned by canonical Catalogue source.</li>\n<li>Studio browser routes consume Studio-owned reads. A generated public artifact does not become a Studio authority because it is convenient or already present.</li>\n<li>A public list index contains only cross-record list context. A selected route reads the exact selected-record payload rather than reconstructing that record from global indexes.</li>\n<li>An exact Series payload owns the Series fields needed by its selected page and one lightweight ordered member-Work projection. It does not require every rich Work payload.</li>\n<li>Recent owns the destination frozen for each Recent entry. Catalogue Search generation reads canonical source and emits its own flat public projection; the public Search browser reads only that projection.</li>\n<li>Record identity, membership, ordering, and destinations are exact. There is no title, filename, route, first-record, or public-index fallback.</li>\n<li>Each consumer cuts over directly. CPB-1 adds no compatibility alias, dual reader, generic projection registry, or second canonical source.</li>\n</ul>\n<h2>Current Owners To Inspect</h2>\n<p>Implementation should begin at the existing ownership seams and narrow the exact changed set during CPB-1.0:</p>\n<ul>\n<li>canonical Catalogue reads, Studio generated lookups, exact Studio Work/Series read APIs, and their configured browser paths;</li>\n<li>public Catalogue serializers, build planning, generated-record cleanup, field-to-artifact impact, and required-file validation;</li>\n<li>the Series, Work, Work Detail, Recent, and Catalogue Search route modules;</li>\n<li>the Catalogue Search builder and Recent projection builder; and</li>\n<li><a href=\"/docs/?scope=studio&amp;doc=d-20260401-000000-a11bf3\">Catalogue Architecture</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260519-202931-b05d27\">Catalogue Indexes And Payloads</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260331-000000-33a9c5\">Public Catalogue Data Flow</a>, <a href=\"/docs/?scope=studio&amp;doc=d-20260331-154730-885d0f\">Catalogue Search Policy</a>, and <a href=\"/docs/?scope=studio&amp;doc=d-20260418-000000-16bf7e\">Catalogue Drafts</a> as the likely durable documentation owners.</li>\n</ul>\n<p>This is an inspection route, not a frozen file inventory.</p>\n<h2>Delivery Steps</h2>\n<h3>CPB-1.0 — Confirm Boundary And Delivery Fit</h3>\n<ul>\n<li>[x] Confirm the current public and Studio consumers of the Series aggregate, Works aggregate, exact Series payloads, exact Work payloads, Recent index, and Catalogue Search index.</li>\n<li>[x] Confirm the five current draft Work projections, the one current draft Series projection, the public published-only rule, and the exact generator and stale-output cleanup owners needed for containment.</li>\n<li>[x] Freeze the lean Series-list fields, exact Series member-Work shape and ordering, exact Work-to-Series navigation rule, Recent destination field, and bare <code>/works/</code> redirect behavior.</li>\n<li>[x] Confirm the broad blast radius across Studio reads, public generators and cleanup, route modules, Search generation, Recent generation, field-impact planning, required-file validation, focused tests, and durable documentation.</li>\n<li>[x] Stop and re-scope if the result requires a public draft-preview contract, changed public Work or Series URLs, a new canonical store, a generic projection framework, or a second flat Works report.</li>\n</ul>\n<p><strong>Checkpoint:</strong> a concise sanity record confirms the ownership fit, public containment requirement, final read matrix, deliverables, risks, and scope-creep stop conditions. Stop before CPB-1.1 implementation approval.</p>\n<h4>CPB-1.0 Sanity Record</h4>\n<p>The ownership fit is confirmed. Canonical Work and Series source plus the existing Studio Catalogue read API and lookup builders can replace every Studio public-artifact read. The existing Catalogue index/record builders, cleanup owner, route modules, Recent builder, Search builder, field registry, projection audit, and required-file configuration cover the public cutover without a new store, registry, or compatibility layer.</p>\n<table>\n<thead>\n<tr>\n<th>consumer</th>\n<th>current read</th>\n<th>accepted final read</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>/series/</code></td>\n<td>Series aggregate</td>\n<td>lean Series list index</td>\n</tr>\n<tr>\n<td>selected Series</td>\n<td>Series aggregate + Works aggregate + exact Series</td>\n<td>exact Series only</td>\n</tr>\n<tr>\n<td>selected Work</td>\n<td>exact Work + Series aggregate</td>\n<td>exact Work + exact relevant Series</td>\n</tr>\n<tr>\n<td>Work Detail</td>\n<td>exact parent Work</td>\n<td>unchanged</td>\n</tr>\n<tr>\n<td>Recent</td>\n<td>Recent + Series aggregate</td>\n<td>Recent only</td>\n</tr>\n<tr>\n<td>Catalogue Search runtime</td>\n<td>Search index</td>\n<td>unchanged</td>\n</tr>\n<tr>\n<td>Catalogue Search builder</td>\n<td>Series aggregate + Works aggregate + exact Work payloads</td>\n<td>canonical Catalogue source and exact canonical prose/detail owners when required by the existing Search contract</td>\n</tr>\n<tr>\n<td>bare <code>/works/</code></td>\n<td>Works aggregate + Series aggregate</td>\n<td>redirect to <code>/series/</code></td>\n</tr>\n<tr>\n<td>Studio Works and tag surfaces</td>\n<td>public aggregates and, for Series Tag Editor media, exact public Work</td>\n<td>canonical maps or existing Studio lookup/service reads only</td>\n</tr>\n</tbody>\n</table>\n<p>Containment evidence is exact: canonical source has five draft Works (<code>00373</code>–<code>00377</code>), all five appear in <code>works_index.json</code> and have exact public Work payloads. Canonical source has four draft Series; the Series aggregate already excludes them, but <code>site/assets/series/index/002.json</code> is stale. The Works aggregate and exact Work generator currently accept draft and published records, while exact Series generation skips drafts without removing stale files. <code>catalogue_generation_indexes.py</code> and <code>generate_work_pages.py</code> remain the generation owners; <code>catalogue_cleanup.py</code> is the existing exact-artifact cleanup seam.</p>\n<p>The frozen public shapes and route rules are:</p>\n<ul>\n<li>lean Series-list rows contain <code>series_id</code>, <code>title</code>, <code>year</code>, <code>year_display</code>, <code>primary_work_id</code>, and <code>single_work_id</code> only; <code>single_work_id</code> is present only when the Series has exactly one published member;</li>\n<li>exact Series payloads retain their focused metadata/prose and add published member Works shaped as <code>work_id</code>, <code>title</code>, <code>year</code>, and <code>year_display</code>, ordered by canonical Series <code>sort_fields</code> with <code>work_id</code> as the deterministic tie-break;</li>\n<li>a selected Work honors a query-Series context only when that exact <code>series</code> ID appears in the Work's canonical <code>series_ids</code> and the exact Series member projection contains the Work. Independently, the first canonical <code>series_ids</code> entry remains the primary Series link. A missing query uses only that exact primary Series; a valid non-primary query may read both exact Series payloads because they serve different current links; an invalid query does not fall back as navigation context; and no aggregate is read;</li>\n<li>Recent entries own an <code>href</code>: Work entries route to the exact Work with <code>from=recent</code>; Series entries route to their sole published Work when there is exactly one, otherwise to the selected Series with <code>from=recent</code>; and</li>\n<li><code>/works/</code> without a non-empty <code>work</code> parameter replaces the location with <code>/series/</code> and discards retired list query state. Exact Work and Work Detail URLs do not change.</li>\n</ul>\n<p>The delivery remains broad but separable at its existing checkpoints. CPB-1.1 is limited to published-only aggregate/exact generation, stale exact cleanup, the bare-Works redirect, and retirement of the Works-list module. Its proposed production/config files are <code>catalogue_generation_indexes.py</code>, <code>generate_work_pages.py</code>, <code>catalogue_cleanup.py</code>, <code>work-page.js</code>, <code>site/works/index.html</code>, <code>site-tools.json</code>, and deletion of <code>works-index.js</code>; focused evidence belongs in <code>test_catalogue_generation_indexes.py</code>, <code>test_catalogue_cleanup.py</code>, and <code>public_route_simplification.py</code>. The estimate is about 100–160 production/config lines and 70–120 test lines, plus deletion of the 376-line list module and six stale exact JSON files. Later checkpoints own Studio read cutover, exact public route shapes, Recent/Search ownership, Works-aggregate retirement, audit/field-planning transfer, and durable documentation transfer.</p>\n<p>Main risks are stale exact payloads surviving a scoped build, Series member order diverging between canonical and public/Studio projections, multi-Series Work navigation silently changing target, Search retaining a hidden public-output input, and validation or field-impact logic continuing to require the retired aggregate. Stop and re-scope if implementation needs public draft preview, URL changes, another canonical store, a generic projection framework, a replacement flat Works report, content comparison, or a compatibility/dual-read period.</p>\n<p>CPB-1.0 is complete. No product code has changed, and CPB-1.1 has not started.</p>\n<h3>CPB-1.1 — Contain Public Drafts And Retire The Bare Works List</h3>\n<ul>\n<li>[x] Generate public aggregate and exact Work and Series payloads from published records only, and remove stale exact public payloads for records that are not published.</li>\n<li>[x] Remove the unparameterized all-Works list UI and route module while retaining the exact Work page shell.</li>\n<li>[x] Redirect bare <code>/works/</code> to <code>/series/</code> without changing <code>/works/?work=&lt;work_id&gt;</code> or Work Detail routes.</li>\n<li>[x] Prove that no draft Work or Series remains in tracked public JSON, that draft IDs do not resolve through exact public payloads, and that published Work and Series deep links still load.</li>\n<li>[x] Do not retire <code>works_index.json</code> yet while selected Series or other accepted consumers remain.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused generator, stale-cleanup, route-boot, exact published-Work, draft-absence, redirect, and public-artifact evidence passes. An independent diff review confirms that containment did not widen into the later data-contract cutover. Stop before CPB-1.2.</p>\n<h4>CPB-1.1 Completion Record</h4>\n<p>Public Work aggregate and exact generation now admit only <code>status=published</code>. Exact Work and Series cleanup uses the existing Catalogue cleanup owner to remove files whose IDs are not in the current published sets. The full dry run identified exactly <code>00373</code>–<code>00377</code> and Series <code>002</code>; the write run deleted those six files, rewrote <code>works_index.json</code> from 1,940 to 1,935 rows, and rebuilt Catalogue Search from 2,079 to 2,074 entries. Studio <code>work-storage-index.json</code> remained draft-inclusive and unchanged.</p>\n<p>The unparameterized Works list markup and its 376-line route module are removed, while the exact Work shell and module remain. <code>/works/</code> and query-only <code>/works/</code> state now replace the location with <code>/series/</code> and discard retired list query state. Focused Playwright evidence passed for both redirects, exact published Work <code>00001</code>, selected Series <code>009</code>, Work Detail <code>00001-001</code>, and draft Work <code>00373</code> resolving only to the unavailable state.</p>\n<p>Focused Python tests passed 8 assertions across published-only aggregate/storage separation and stale exact cleanup. Python compilation and lint, JavaScript lint, site validation, the public route smoke, generated-projection assertions, and <code>git diff --check</code> passed. The artifact assertion matched 1,935 canonical published Works to 1,935 aggregate, exact, and Search Work IDs and 139 canonical published Series to 139 aggregate, exact, and Search Series IDs, with all canonical draft intersections empty.</p>\n<p>A separate diff review confirmed that the aggregate and Search diffs remove only the five draft Work rows, every surviving row is unchanged, the six exact deletions are the expected draft/stale records, <code>works_index.json</code> remains present, and no CPB-1.2 Studio read cutover or later payload-shape work entered this checkpoint. CPB-1.1 is complete; CPB-1.2 has not started.</p>\n<h3>CPB-1.2 — Cut Studio Over To Canonical Reads</h3>\n<ul>\n<li>[ ] Move the Studio Works route to canonical Catalogue reads or an existing Studio-owned projection while preserving its current filtering, ordering, Series labels, and storage context.</li>\n<li>[ ] Move Series Tag Editor reads to exact Studio Series and Work service/lookup projections, including primary-Work media context, without reading public by-ID payloads.</li>\n<li>[ ] Remove public Catalogue index and by-ID paths from Studio templates, runtime configuration, browser modules, and stale error copy.</li>\n<li>[ ] Preserve current exact target identity and editor behavior; add no service fallback to public artifacts.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused Studio read, route, lookup, wrong-target, ordering, and configuration evidence passes. A bounded scan confirms that Studio frontend and server owners no longer reference public Catalogue indexes or exact public Catalogue payloads. Stop before CPB-1.3.</p>\n<h3>CPB-1.3 — Make Selected Public Routes Exact And Self-Contained</h3>\n<ul>\n<li>[ ] Extend the exact Series payload with the Series metadata and canonically ordered lightweight member Works required by the selected Series page.</li>\n<li>[ ] Make <code>/series/?series=&lt;series_id&gt;</code> load only that exact Series payload while preserving thumbnails, grid/list behavior, title/year metadata, pagination controls, and the Series-index return link.</li>\n<li>[ ] Make exact Work Series navigation load the exact relevant Series payload rather than the global Series index, preserving exact membership validation and previous/next order.</li>\n<li>[ ] Reduce <code>series_index.json</code> to the fields required by the unselected Series list, including any explicit single-Work routing fact needed to preserve current card destinations.</li>\n<li>[ ] Preserve Work Detail reuse of the parent exact Work payload and add no global detail or membership index.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused payload-schema, ordering, selected-Series, selected-Work navigation, Series-list, single-Work destination, Work Detail, and missing-record evidence passes. An independent diff review confirms that focused routes no longer reconstruct selected records from aggregate indexes. Stop before CPB-1.4.</p>\n<h3>CPB-1.4 — Make Recent And Search Own Their Reads</h3>\n<ul>\n<li>[ ] Add each Recent entry's deterministic public destination to the Recent projection and remove the Series-index fetch from the Recent browser route.</li>\n<li>[ ] Make the Catalogue Search builder read canonical Catalogue source and exact canonical detail/prose owners rather than public aggregate or by-ID outputs.</li>\n<li>[ ] Preserve Recent snapshot semantics and current Catalogue Search entry kinds, matching, ranking, destinations, and browser index contract.</li>\n<li>[ ] Prove that Recent and public Catalogue Search each load only their own runtime payload and that rebuild output remains deterministic.</li>\n</ul>\n<p><strong>Checkpoint:</strong> focused Recent generation/runtime and Catalogue Search source/build/runtime evidence passes. An independent diff review confirms that neither subsystem retains a hidden dependency on public list or selected-record payloads. Stop before CPB-1.5.</p>\n<h3>CPB-1.5 — Retire The Works Aggregate And Transfer The Contract</h3>\n<ul>\n<li>[ ] Remove the final <code>works_index.json</code> generator, artifact, URL helper, build option, field-impact rule, required-file entry, cleanup branch, fixture, test expectation, and documentation reference after a zero-consumer scan.</li>\n<li>[ ] Verify the lean Series index and every exact public route against the accepted read matrix, including direct deep links, browser history/return behavior, missing records, and cache-safe generated replacement.</li>\n<li>[ ] Run focused Catalogue generation, source/lookup, Search, route-module, site-validation, lint, generated-projection, and whitespace checks proportional to the final changed contracts.</li>\n<li>[ ] Perform an independent final code/delivery review for ownership drift, compatibility residue, dead paths, draft leakage, duplicated membership, and incomplete generator cleanup.</li>\n<li>[ ] Transfer the shipped boundary to the durable Catalogue architecture, payload, route-flow, Search, and Draft owners, then present CPB-1 for closeout without automatically retiring it.</li>\n</ul>\n<h2>Completion Evidence</h2>\n<ul>\n<li>Studio has no runtime or configuration reference to <code>site/assets/data/series_index.json</code>, <code>site/assets/data/works_index.json</code>, or public Work/Series by-ID payloads.</li>\n<li>The tracked public artifact contains no draft Work or Series projection.</li>\n<li>Bare <code>/works/</code> redirects to <code>/series/</code>; exact Work and Work Detail deep links remain supported.</li>\n<li><code>/series/</code> reads one lean Series index; selected Series and Work routes read exact payloads only.</li>\n<li>Recent and Catalogue Search runtime each read only their own generated projection, and Search generation reads canonical rather than public output.</li>\n<li><code>works_index.json</code> and all of its production, configuration, validation, cleanup, test, and documentation obligations are absent.</li>\n<li>Focused evidence and the independent final review support the complete outcome, and durable documentation describes the shipped ownership boundary.</li>\n</ul>\n<h2>Boundaries</h2>\n<p>CPB-1 does not change canonical Catalogue schema, Catalogue editing workflows, Series membership or primary-Work meaning, public Work/Series URL shapes, Work Detail ownership, Search ranking or interaction design, Recent snapshot meaning, Catalogue media publication, or the visual design of Series and Work pages. It creates no replacement flat Works report. A future need for that report belongs in a separate Docs Viewer delivery rather than restoring a public aggregate and route.</p>\n<p>CPB-1 is active and parked at the completed CPB-1.1 review gate. Explicit approval is required before CPB-1.2 product implementation.</p>\n"
}
