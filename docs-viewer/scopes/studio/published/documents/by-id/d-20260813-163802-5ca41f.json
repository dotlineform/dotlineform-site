{
  "doc_id": "d-20260813-163802-5ca41f",
  "title": "Docs Viewer Search Tokenizer Concept",
  "added_date": "2026-08-13 16:38:02",
  "last_updated": "2026-08-13 21:43:50",
  "viewer_url": "/docs/?scope=studio&doc=d-20260813-163802-5ca41f",
  "parent_id": "d-20260715-092247-6bbb5e",
  "summary": "define the deterministic terms shared by every Docs Viewer inverted-index build and browser query",
  "content_html": "<h1>Docs Viewer Search Tokenizer Concept</h1>\n<p>This Roadmap concept is retained as the proposal and reasoning record used by DSI-1 and SM-1. The shipped current-state term contract now belongs to the durable <a href=\"/docs/?scope=studio&amp;doc=d-20260813-214351-98d3f0\">Docs Viewer Search Tokenizer</a>.</p>\n<h2>Purpose</h2>\n<p>A search term is a normalized prose word or authored technical identifier that remains useful as a retrieval key after predictable noise has been removed. Full-text retrieval does not require indexing every string separated by spaces.</p>\n<p>The tokenizer is a shared Docs Viewer contract: every scope's index generation and browser queries must pass text through the same normalization rules. This lets a query such as <code>subscope</code> find a document containing <code>docs_subscope</code>, while the complete <code>docs_subscope</code> query remains more precise.</p>\n<h2>Proposed Contract</h2>\n<p>For each eligible title, parent title, heading, body-prose, inline-code, or fenced-code value, the tokenizer should:</p>\n<ol>\n<li>normalize Unicode and case so equivalent authored forms produce the same term;</li>\n<li>recognize compound technical identifiers before discarding punctuation;</li>\n<li>retain the normalized complete identifier and add useful components split on underscore, hyphen, dot, and camel-case boundaries;</li>\n<li>retain mixed alphabetic and numeric identifiers such as <code>r2</code>, <code>h1</code>, and <code>sm-1.0</code>;</li>\n<li>omit numeric-only tokens, one-character tokens, known document IDs, URLs, paths, markup artifacts, and file-extension-only components from ordinary term derivation;</li>\n<li>omit a small, fixed, versioned stop-word list rather than deriving stop words from the current corpus; and</li>\n<li>deduplicate the resulting terms within each document field before adding its document position to the inverted index.</li>\n</ol>\n<p>Each scope's field policy owns which source values reach the tokenizer. Exact structural identifiers or metadata retained by an existing scope are explicit policy fields rather than ordinary derived terms. Studio body extraction remains owned by <a href=\"/docs/?scope=studio&amp;doc=d-20260813-161942-b1e9e1\">Studio Full-Text Search Index</a>; the tokenizer receives its eligible authored text after front matter, generated markup, report declarations, link destinations, media paths, scripts, and styles have been excluded.</p>\n<h3>Exact tokenizer edges</h3>\n<ul>\n<li>Unicode-aware lowercase normalization.</li>\n<li>Preserve and split technical compounds.</li>\n<li>Prefix matching from three characters.</li>\n<li>A query reduced entirely to stop words returns no results.</li>\n</ul>\n<h2>Examples</h2>\n<table>\n<thead>\n<tr>\n<th>authored text</th>\n<th>indexed terms</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>Full-text search</code></td>\n<td><code>full-text</code>, <code>full</code>, <code>text</code>, <code>search</code></td>\n</tr>\n<tr>\n<td><code>docs_subscope</code></td>\n<td><code>docs_subscope</code>, <code>docs</code>, <code>subscope</code></td>\n</tr>\n<tr>\n<td><code>handleEditMetadataSave</code></td>\n<td><code>handleeditmetadatasave</code>, <code>handle</code>, <code>edit</code>, <code>metadata</code>, <code>save</code></td>\n</tr>\n<tr>\n<td><code>build_search.py</code></td>\n<td><code>build_search.py</code>, <code>build</code>, <code>search</code></td>\n</tr>\n<tr>\n<td><code>SM-1.0</code></td>\n<td><code>sm-1.0</code>, <code>sm</code></td>\n</tr>\n<tr>\n<td><code>2026</code></td>\n<td>omitted: numeric-only</td>\n</tr>\n<tr>\n<td><code>d-20260715-084611-c2fa6f</code></td>\n<td>omitted: document identity</td>\n</tr>\n<tr>\n<td><code>the</code>, <code>and</code>, <code>of</code></td>\n<td>omitted: fixed stop words</td>\n</tr>\n</tbody>\n</table>\n<h2>Stop words</h2>\n<p>The initial stop-word candidate should remain deliberately small: <code>a</code>, <code>an</code>, <code>and</code>, <code>are</code>, <code>as</code>, <code>at</code>, <code>be</code>, <code>by</code>, <code>for</code>, <code>from</code>, <code>in</code>, <code>is</code>, <code>it</code>, <code>of</code>, <code>on</code>, <code>or</code>, <code>that</code>, <code>the</code>, <code>to</code>, and <code>with</code>. DSI-1.0 should confirm the shared list against representative scope metadata, and SM-1.0 should confirm it against representative Studio body and code queries.</p>\n<p>These are deliberately selected grammatical glue words, rather than a standard NLP list containing hundreds of common words.</p>\n<p>A word should be excluded only when:</p>\n<ul>\n<li>Searching for it alone would provide no useful result.</li>\n<li>Removing it from a longer query would not materially change the query’s intent.</li>\n<li>It has no important meaning in the relevant Docs Viewer corpus.</li>\n</ul>\n<p>Candidates like <code>before</code>, <code>after</code>, and <code>while</code> are interesting because generic search systems often treat them as stop words, but in technical documentation they may be meaningful:</p>\n<ul>\n<li><code>before publish</code></li>\n<li><code>after import</code></li>\n<li><code>while saving</code></li>\n</ul>\n<p>Removing them would reduce those queries to <code>publish</code>, <code>import</code>, and <code>saving</code>, losing the lifecycle distinction. Their posting lists cost little, especially after compression, so I would initially <strong>retain all three</strong>.</p>\n<p>The initial stop list in the doc contains 20 words:</p>\n<pre><code class=\"language-text\">a, an, and, are, as, at, be, by, for, from,\nin, is, it, of, on, or, that, the, to, with\n</code></pre>\n<p>Even that should be treated conservatively. I’d call it a <strong>minimal function-word list</strong>, and test every proposed addition against the question: “Could this word distinguish two useful technical searches?” If yes, retain it.</p>\n<h2>Deliberate Limits</h2>\n<p>The first tokenizer should not add stemming, synonyms, spelling correction, or language detection. <code>report</code>, <code>reports</code>, and <code>reporting</code> therefore remain distinct terms, although query-side prefix matching can still let <code>report</code> retrieve the longer forms.</p>\n<p>It should not remove terms merely because they occur in many documents. Corpus-frequency pruning would make the contract change as documents are added and could silently remove common but meaningful technical vocabulary.</p>\n<h2>Delivery Relationship</h2>\n<p><a href=\"/docs/?scope=studio&amp;doc=d-20260813-171341-8bfa80\">DSI-1 — Docs Viewer Search Index v2 Delivery</a> and <a href=\"/docs/?scope=studio&amp;doc=d-20260715-084611-c2fa6f\">SM-1 — Studio Full-Text Search Delivery</a> retain implementation evidence. <a href=\"/docs/?scope=studio&amp;doc=d-20260813-161942-b1e9e1\">Studio Full-Text Search Index Concept</a> retains the searchable-source design, prototype payload findings, and initial limits. Shipped behavior belongs to the durable <a href=\"/docs/?scope=studio&amp;doc=d-20260813-214351-98d3f0\">Docs Viewer Search Tokenizer</a> and <a href=\"/docs/?scope=studio&amp;doc=d-20260813-214350-4c7a21\">Docs Viewer Search Index</a>.</p>\n"
}
