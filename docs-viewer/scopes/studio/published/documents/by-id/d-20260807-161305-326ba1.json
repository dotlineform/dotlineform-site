{
  "doc_id": "d-20260807-161305-326ba1",
  "title": "SSP-6.0 — Uncataloged And Missing Source Files Sanity Note",
  "added_date": "2026-08-07 16:13:05",
  "last_updated": "2026-08-07 16:13:05",
  "viewer_url": "/docs/?scope=studio&doc=d-20260807-161305-326ba1",
  "parent_id": "d-20260802-121106-15ea55",
  "summary": "confirm the corrected two-report contracts, existing local seams, implementation footprint, path risks and retirement boundary",
  "ui_status": "done",
  "content_html": "<h1>SSP-6.0 — Uncataloged And Missing Source Files Sanity Note</h1>\n<h2>Outcome</h2>\n<p>SSP-6 fits the current architecture as two independent local Docs Viewer reports. <strong>Uncataloged Files</strong> starts from exact existing Work source directories and compares their direct ordinary files with complete canonical Work source paths. <strong>Missing Source Files</strong> starts from each complete canonical Work source declaration and checks its exact file. They share catalogue and contained filesystem inputs but not a response, row grain, report mode, or presentation module.</p>\n<p>The gate is complete. SSP-6.1 may begin only after explicit implementation approval; no producer, endpoint, report registry, browser module, external report-host document, Studio retirement, or generated report behavior changes at SSP-6.0.</p>\n<h2>Confirmed Report Contracts</h2>\n<ul>\n<li>Uncataloged Files derives each distinct represented directory from <code>project_folder</code> plus optional <code>project_subfolder</code>, scans only that directory's direct ordinary non-hidden files regardless of extension, and excludes known Work-detail directory trees. Folder displays the real directory relative to the <code>projects/</code> root, such as <code>wa/ink</code>; File name preserves the discovered filesystem spelling.</li>\n<li>A discovered file is catalogued when it is the same existing filesystem object as at least one complete canonical Work source path. The producer must compare resolved identity rather than raw Unicode path strings and must not rewrite the canonical record or displayed filesystem name.</li>\n<li>Activating an Uncataloged Files filename uses the existing encoded target <code>projects/&lt;represented-directory&gt;/&lt;actual-filename&gt;</code> and <code>/docs/open-local-target</code>. The existing service revalidates configured-base containment and calls Finder reveal with <code>open -R</code> for a file; no absolute path enters the response or browser state.</li>\n<li>Missing Source Files independently constructs <code>project_folder[/project_subfolder]/project_filename</code> for each complete Work declaration and applies direct file semantics. It emits one row per missing Work, including when several Works name the same missing path; incomplete source declarations remain outside this report rather than becoming a third diagnostic.</li>\n<li>Missing Source Files displays Work ID, Work title, and the expected path relative to the <code>projects/</code> root. Only Work ID links, using <code>/studio/catalogue-work/?work=&lt;five-digit-id&gt;</code>; a nonexistent file has no Finder target.</li>\n<li>Each response contains its versioned report identity and ordered typed rows but no count, total, timestamp, environment/path summary, statistical warning, prose recap, or generated Markdown presentation. The browser renders only the specified columns and rows plus ordinary Run/Refresh, empty, busy, and contained current-error states.</li>\n</ul>\n<h2>Confirmed Owners And Fit</h2>\n<ul>\n<li><code>records_from_json_source</code> remains the canonical Work and Work-detail reader. SSP-6 composes paths only from the existing canonical fields and does not add <code>doc_url[]</code>, file inventory, audit status, or filesystem evidence to <code>studio/data/canonical/catalogue/*.json</code>.</li>\n<li><code>$DOTLINEFORM_PROJECTS_BASE_DIR/projects/</code> remains the contained source root. The reports do not scan the configured base, unrelated sibling trees, unrepresented project directories, or arbitrary recursive descendants.</li>\n<li>The existing local report registry, loader, management report service, <code>POST</code> dispatch, report-host front matter, and report CSS classes are sufficient. SSP-6 adds two explicit report IDs, endpoints, producers, and modules; it does not change or consume the <code>docs_project_state_report_v2</code> producer or response.</li>\n<li>The existing local-target service remains the only Finder activation authority. The Uncataloged Files browser module supplies one encoded base-relative file target; the Missing Source Files module has no local activation path.</li>\n<li>The two ordinary parent/report-host documents belong to the local external <code>dotlineform</code> scope, have local report access, and remain separate from the <code>dotlineform/projects</code> child collection and its lifecycle-created report host.</li>\n<li>The old Studio generator remains untouched until SSP-6.3. Its <code>IMAGE_EXTENSIONS</code> constant is currently imported by Work-detail and catalogue API code, so that media-policy owner must be relocated before <code>studio/services/catalogue/project_state_report.py</code> can be deleted.</li>\n</ul>\n<h2>Current Evidence</h2>\n<p>The current legacy dry run still scans only top-level image candidates and reports nested canonical paths as missing. A separate read-only exact-path check found that every current complete Work source path resolves to a file; in particular, all current <code>wa/ink</code> primary-source paths resolve. This is a current verification snapshot, not a permanent empty-report expectation, so SSP-6.2 requires a genuinely missing fixture.</p>\n<p>Work <code>00894</code> records the NFC spelling <code>doll moiré reduced.jpg</code>, while Finder exposes a canonically equivalent decomposed spelling. The raw filename strings differ, NFC comparison agrees, and <code>samefile</code> confirms one filesystem object. SSP-6 must therefore use filesystem identity for existing-file reconciliation and preserve the discovered name for display.</p>\n<p>The direct represented-directory inspection found uncataloged candidates with image, video, audio, document, spreadsheet, and working-file extensions, including <code>.jpg</code>, <code>.mp4</code>, <code>.wav</code>, <code>.pdf</code>, <code>.docx</code>, <code>.xlsx</code>, and <code>.psd</code>. This confirms that an image allowlist would discard useful evidence; exact filenames and rows matter, aggregate extension counts do not.</p>\n<p>Existing focused seams pass when collected against an isolated writable external workspace: <code>docs-viewer/tests/python/test_docs_local_links.py</code> and <code>docs-viewer/tests/python/test_project_state_report_contract.py</code> completed with 15 passing tests. The initial run against the configured external Docs root stopped during collection because the sandbox cannot write that root; no product assertion ran or failed.</p>\n<h2>SSP-6.1 Proposed Footprint</h2>\n<p>The bounded production change is expected to add <code>docs-viewer/services/docs_uncataloged_files.py</code> and <code>docs-viewer/runtime/js/reports/uncataloged-files-report.js</code>; extend <code>docs-viewer/services/docs_management_routes.py</code>, <code>docs-viewer/services/docs_management_service.py</code>, <code>docs-viewer/config/reports/reports.json</code>, <code>docs-viewer/runtime/js/reports/docs-viewer-reports.js</code>, and <code>docs-viewer/runtime/js/reports/docs-viewer-report-service.js</code>; and create one new local external <code>dotlineform</code> parent document titled <strong>Uncataloged Files</strong>. Existing Project State source, response, report module, host document, and Studio workflow remain unchanged at this stage.</p>\n<p>Focused coverage is expected in new <code>docs-viewer/tests/python/test_docs_uncataloged_files.py</code>, <code>docs-viewer/tests/python/test_uncataloged_files_report_contract.py</code>, and <code>docs-viewer/tests/smoke/docs_viewer_uncataloged_files_report_modules.py</code>, with the existing route-constant test extended only if required. The stage should remain roughly 350–450 production lines plus 300–450 focused test and smoke lines. Material expansion beyond this, a shared mode-driven audit, a generic filesystem report framework, or changes to Project State is a stop-and-review signal.</p>\n<p>SSP-6.1 verification should cover complete and incomplete Work declarations, distinct represented directories, optional subfolders, all ordinary extensions, hidden/detail exclusions, duplicate canonical references, filesystem-equivalent Unicode spellings, deterministic ordering, containment, exact Finder reveal targets, table-only response and presentation, current-run failure clearing, report registry/loader/service wiring, local-only access, external report-host generation, focused Python and JavaScript lint, and <code>git diff --check</code>.</p>\n<h2>Later Blast Radius And Retirement Risks</h2>\n<p>SSP-6.2 repeats the explicit producer/endpoint/module/report-host shape for Missing Source Files and must not consume the Uncataloged Files response or candidate scan. Its focused evidence must prove row-per-Work behavior, nested paths, Unicode-equivalent existing files, genuinely absent files, exact editor routes, table-only presentation, and public isolation.</p>\n<p>SSP-6.3 owns removal of the old Studio route, frontend, APIs, activity contract, configuration, generator entry point, static Markdown, tests, and admin-check registration only after both replacement reports are accepted. It must first move the unrelated image-extension policy to an appropriate catalogue-media owner, then prove that Work-detail and catalogue media behavior is unchanged. It does not delete the Docs Viewer Project State report, producer, endpoint, module, tests, or local report host.</p>\n<h2>Stop Condition</h2>\n<p>Stop before or during SSP-6.1 if implementation requires recursive or arbitrary filesystem discovery, persisted inventories, result counts or generated Markdown, repair/delete/import actions, continuous watching, mutation hooks, a registry or generic audit framework, a combined mode-driven report, Project State relationship changes, browser absolute paths, Unicode rewriting, inferred Work identity, or early retirement of any Studio workflow. Those outcomes require a separately reviewed delivery boundary.</p>\n"
}
