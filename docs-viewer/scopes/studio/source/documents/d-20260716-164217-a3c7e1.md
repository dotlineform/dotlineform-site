---
doc_id: d-20260716-164217-a3c7e1
title: Recent Documents Working Notes
added_date: 2026-07-16 16:42:17
last_updated: "2026-07-16 18:16:53"
ui_status: done
summary: Temporary inventories and detailed implementation evidence for Recent Documents delivery.
parent_id: d-20260715-225512-80198b
viewable: true
---
# Recent Documents Working Notes

Temporary sibling for implementation detail that should not overload the delivery tracker. Offer this document for retirement during closeout unless some result still needs to be folded into a durable owner.

## RD-1 Mutation Inventory

- Ordinary canonical creates: management create, import create, and scope bootstrap create. All set matching `added_date` and `last_updated`; the first two now use the shared timestamp helper and bootstrap already used one captured timestamp for both fields.
- Ordinary canonical updates use one positive comparison of body, `title`, and `summary`. Source-editor body rebuilds, qualifying metadata changes, import overwrites, and Data Sharing summary applies advance through the shared timestamp owner; other canonical rewrites preserve the existing timestamp.
- No-op owners: metadata/viewability/move planners already suppress writes; source-editor identical-body requests now suppress both timestamp and rebuild; import overwrites retain the existing source text when the canonical candidate is identical.
- Derived or exceptional writes excluded from the ordinary contract: Docs Review materialization, generated/search builders, export/preview artifacts, scope/config lifecycle JSON, and the one-time document-identity migration.
- Direct edits: watcher parsed snapshots compare the same positive edit content before rebuild and log a timestamp-evidence warning only when that content changed without an advanced full timestamp. Suppressed managed writes refresh the baseline without producing that warning.

## RD-2 Seed Evidence

- The corrected apply used one fixed `2026-07-16 17:52:03` dirty-write boundary and its own in-memory hash-checked plan.
- The bounded union still contained 53 documents: Analysis manage 3/public 2, Library manage 10/public 10, Moments manage 10, Notes manage 10, Processing manage 10, and Studio manage 10. The corrective apply rewrote 37 repository-owned timestamps; a follow-up repair corrected six obsolete full timestamps from the earlier field-agnostic seed, and the already seeded external Notes set required no second write.
- Repository-owned history now follows renames to the latest Git author time at which body, `title`, or `summary` changed. Addition/deletion pairs with identical qualifying content are followed even when Git's rename detector does not pair them. Dirty qualifying content uses the fixed real write boundary.
- The Processing Project Setup document still resolves to the document-identity migration because that commit rewrote a link in its canonical body; under the positive rule, that is a qualifying body change rather than a front-matter-only migration.
- Public Moments required no seed because its public projection is added-based. Editorial Moments received only its edited manage set.
- Full rebuilds produced every required edited projection at its configured limit where that many eligible documents exist. Date-only source outside the union stayed unchanged and remains ineligible for edited Recent.
- The first full semantic history walk took several minutes. A complete existing full-timestamp set now provides an idempotent fast path: the immediate all-scope rerun finished without Git history scanning and reported zero writes; a future projection that cannot fill falls back to history. The explicit `--repair-existing` mode repeats the expensive semantic audit if a completed seed ever needs correction again.

## Verification Notes

- Post-correction timestamp, mutation, import, watcher, seed, builder, publication, and generated-read set: 89 passed.
- Timestamp/mutation/import/watcher/seed helper set: 59 passed.
- Builder, publication, generated-read, lifecycle, settings, and contract fixture set: 81 passed.
- Maintained `docs` profile passed; summary at `var/admin/test-runs/recent-documents-delivery/summary.md`.
- Maintained `docs-viewer-smoke` profile passed all five checks, including public Library/Analysis/Moments, manage route, router modules, and the Docs Review negative boundary; summary at `var/admin/test-runs/recent-documents-delivery-smoke/summary.md`.
- Public payloads declare Library `edited`, Analysis `edited`, and Moments `added`; working manage payloads, including Moments, declare `edited`.
- `bin/site-validate`, `git diff --check`, the active Added-specific contract scan, and the focused source/code/config private-path and credential scan passed.
