---
doc_id: d-20260718-125756-a41c70
title: Docs Viewer Source Documentation And Linting Pilot Delivery
added_date: "2026-07-18 12:57:56"
last_updated: "2026-07-25 00:12:43"
ui_status: done
summary: Prove minimal JSDoc, Python docstring, Ruff, and ESLint conventions on representative Docs Viewer boundaries before the Core Refactor worktree is activated.
parent_id: d-20260713-000000-d9bad2
viewable: true
---
# Docs Viewer Source Documentation And Linting Pilot Delivery

## Outcome

Establish and prove the smallest useful source-documentation and linting conventions needed for new or materially changed Docs Viewer core code.

This is a bounded pilot, not repository-wide backfill. It begins after Core Definition Baseline v1 identifies representative boundaries and completes before the long-lived Core Refactor worktree is created.

## Scope

- Select a small set of representative JavaScript and Python Docs Viewer boundaries from the accepted core definition.
- Inventory the active Docs Viewer source roots, delivery environments, generated/vendor exclusions, and current check owners before configuring tools.
- Agree practical JSDoc and Python docstring conventions from those real modules.
- Document contracts, intent, non-obvious data shapes, lifecycle, side effects, and failure behavior without commenting obvious code.
- Add Docs Viewer-owned, pinned Ruff and ESLint development tooling with explicit, reviewable configuration.
- Begin with correctness, unused-code, and other low-noise rules appropriate to the selected surfaces.
- Provide short project-local commands that lint the pilot area and can later target new or changed Core Refactor files.
- Resolve genuine findings inside the selected pilot surfaces and record any justified local suppressions.
- Update stable repository guidance only after the examples and commands have proved useful.

## Decisions

- Current code and canonical configuration remain exact behavior authority. Source comments explain and route; tests and schemas provide executable evidence; accepted architecture and delivery documents own intended change.
- The pilot toolchain is owned by Docs Viewer rather than a new repository-wide JavaScript project. It may target both `docs-viewer/runtime/js/` and site-owned public/shared Docs Viewer modules through explicit paths.
- The existing Mermaid renderer package remains isolated and does not acquire repository lint dependencies.
- `jsconfig.json`, TypeScript, and `// @ts-check` are not initial pilot requirements. The delivery includes one bounded evaluation and persists them only if the selected surface proves useful, low-noise checking.
- Normal completion remains compatible with the existing focused `run_checks.py` profiles. The pilot does not introduce one repository-wide `npm run check`.
- Java/Processing, unrelated applications, semantic retrieval infrastructure, generated reference pages, formatting, and broad automatic fixes remain separate outcomes.

## Planned Tooling

Required when the delivery becomes active:

- one exact Ruff version in a Python development-dependency owner;
- exact ESLint, `@eslint/js`, and `globals` versions in a private Docs Viewer-owned JavaScript tooling package and lockfile;
- focused commands that run from the repository checkout without relying on global Ruff or ESLint installations.

Conditional:

- pin TypeScript only if the selective JSDoc type-checking evaluation is retained;
- add no JSDoc coverage plugin by default; syntax or tag validation must prove useful before becoming another dependency.

SDL-P0 selects the file locations and supported environments. SDL-P2 selects and pins exact versions while installing and running them together. Do not copy version, target, module-resolution, or global settings from background research without verifying them against the application.

## Representative Surfaces

The pilot must include enough variety to test the conventions without becoming a backfill project:

- one exported browser/module contract with a non-trivial object or callback shape;
- one Python service or domain boundary with meaningful inputs, outputs, or failure behavior;
- one builder, filesystem, configuration, or process boundary with side effects;
- one current example where documentation reveals that simplification or splitting is preferable to a longer comment.

[Core Definition Baseline v1](/docs/?scope=studio&doc=d-20260724-213457-6e1cf7) nominates the exact SDL-P0 surfaces so the pilot documents likely future boundaries rather than fossilizing accidental current structure.

## Adoption Rules

- Require the proven conventions for new or materially changed exported/public APIs in Core Refactor deliveries.
- Use types for types and documentation for meaning; do not repeat signatures in prose.
- Treat lint as focused feedback, not as authority for mechanical redesign.
- Report the exact lint targets and relevant omissions; a passing subset is not evidence for uninspected Docs Viewer source.
- Review each failure or suppression in the context of the current contract.
- Keep behavioral edits separate from broad formatting where practical.
- Do not add compatibility code merely to satisfy a lint rule or documentation tool.
- Use canonical repository terminology rather than adding synonyms for agent retrieval.
- Update affected source documentation in the same change as a documented contract.
- Require each material documented claim to have a current code/config owner and executable evidence or an explicitly named evidence gap.

## SDL-P0 Baseline

### Exact Pilot Contracts

| Requirement | Selected contract | Current executable evidence |
| --- | --- | --- |
| Browser/module contract | `createDocsViewerCollectionProvider()`, `createDocsViewerAppComposition()`, and `startDocsViewerStartupPhases()` in `site/docs-viewer/runtime/js/shared/docs-viewer-app-composition.js`. SDL-P1 documents the provider, composition result, startup callbacks, ordering, and failure cleanup; the exported defaults object is supporting configuration rather than a separate documentation target. | `docs-viewer/tests/smoke/docs_viewer_router_modules.py` imports the real browser module and directly exercises provider validation and feature-gated startup ordering. The real route runtime calls the composition and startup APIs from `site/docs-viewer/runtime/js/shared/docs-viewer-app-runtime.js`. |
| Python domain boundary | `ImportContent`, its validation and `as_dict()` copy contract, and its exported content-intent and content-format constants in `docs-viewer/services/docs_import_content.py`. The internal `normalized_text()` helper is not a public documentation target. | `test_docs_import_content.py`, `test_docs_import_data_sharing_content.py`, and `test_docs_import_document.py`. |
| Builder side-effect boundary | `WritePlanMixin.build_write_plan()` and `WritePlanMixin.write_outputs()` in `docs-viewer/build/docs_builder/write_plan.py`. SDL-P1 documents the plan/write split, targeted-build deletion constraint, mutations, and reporting boundary; print helpers remain implementation detail. | `test_build_docs_payloads.py` and `test_build_docs_cli.py`, both already reached by the `docs` check profile. |
| Simplification candidate | The provider-neutral `ArtifactLocationAdapter` and `artifact_location_adapter()` factory in `docs-viewer/services/docs_artifact_locations.py`, with `authenticated_remote_client_for_locations()` inspected as the exact coupling candidate. SDL-P1 must document current provider, containment, mutation, and failure behavior, then classify the direct `studio.services.media` authentication import as extraction work rather than explaining it away with a long comment. | `test_docs_artifact_locations.py`, plus current media, Mermaid, move, and transfer tests that consume the adapter boundary. |

### Environments And Check Ownership

- JavaScript runtime: native browser ESM loaded by `<script type="module">`, without bundling or transpilation. The selected file is in `site/docs-viewer/runtime/js/shared/`; the other active browser roots are `site/docs-viewer/runtime/js/{public,reports,shared}/` and `docs-viewer/runtime/js/{import,management,packages,reports,review}/`.
- JavaScript tooling host at this checkpoint: Node `22.22.1` and npm `10.9.4`. ESLint will lint browser ESM with explicit browser globals; Node globals apply only to the tooling configuration.
- Python runtime at this checkpoint: Python `3.12.7` through the repository-supported Miniconda interpreter. The selected roots are `docs-viewer/services/` and `docs-viewer/build/docs_builder/`.
- Test and Workbench context: `docs-viewer/tests/smoke/`, `docs-viewer/tests/python/`, and `docs-viewer/tests/workbench/` provide evidence and fixtures but are not pilot lint targets.
- Ongoing lint owner: delivery-specific direct commands with explicit paths. The `docs` profile retains its Python, Studio docs-build, and search-build ownership; lint joins a normal profile only after there is a maintained adopted source boundary.
- SDL-P4 closure: the `docs` profile now reaches `test_docs_import_content.py` and `test_docs_artifact_locations.py` as well as the existing builder tests.

### Pilot Selection And Exclusions

- SDL-P1 and the first SDL-P2 lint run target only the four files named above. Other active Docs Viewer source remains eligible for later adoption but is not part of this pilot baseline.
- Exclude `docs-viewer/build/mermaid/**` and its existing package, lockfile, and `node_modules`; `docs-viewer/runtime/vendor/**` and `site/docs-viewer/runtime/vendor/**`; `docs-viewer/scopes/**/published/**`; `docs-viewer/data/generated/**`; generated browser configuration and site payloads; caches; and installed dependency directories.
- Exclude tests and Workbench fixtures from the initial lint target, while retaining them as executable evidence.
- Exclude Processing, Admin, Studio, Catalogue, public-site code outside `site/docs-viewer/`, and every other repository application. No current retired Docs Viewer source root was found, so the explicit four-file allowlist is the retired/unrelated safeguard rather than an invented retired-path glob.

### Chosen Tool Locations

| Owner | SDL-P2 location |
| --- | --- |
| Python dependency pin | `docs-viewer/tooling/lint/python-requirements.txt` |
| Ruff configuration | `docs-viewer/tooling/lint/ruff.toml` |
| Ruff environment | untracked `docs-viewer/tooling/lint/.venv/`, created with the supported Miniconda Python |
| JavaScript dependency owner | private `docs-viewer/tooling/lint/package.json` and `package-lock.json` |
| ESLint configuration | `docs-viewer/tooling/lint/eslint.config.mjs` |
| JavaScript install | untracked `docs-viewer/tooling/lint/node_modules/` |
| Direct commands | `docs-viewer/bin/lint-python` and `docs-viewer/bin/lint-js` |
| Normal check integration | Not retained for the fixed pilot specimens; a later maintained adopted source boundary is required |

These locations create one isolated Docs Viewer lint-tool owner without turning `docs-viewer/` into a runtime JavaScript package, changing root `requirements.txt`, or reusing the Mermaid renderer package.

## SDL-P1 Proven Conventions

- Add a module-responsibility comment only when it clarifies ownership. The composition module now distinguishes dependency assembly and startup ordering from feature-controller behavior and generated-payload authority.
- Document exported JavaScript boundaries when callers need lifecycle, injection, ordering, cleanup, or failure information. `DocsViewerCollectionProvider`, its factory context, and the injected factory callback are the one shared JSDoc shape; obvious internal helpers and the defaults object remain undocumented.
- Keep JavaScript types broad where the runtime intentionally accepts route-specific implementations. SDL-P1 does not imply TypeScript, `checkJs`, or a stricter provider schema.
- Let Python annotations carry types. Add docstrings only for domain invariants, detached-copy behavior, plan-versus-write ownership, create-versus-replace semantics, containment, staging lifetime, remote I/O, and translated failures.
- Use plain source docstrings without documentation-generator markup or repeated argument lists. The only type-support edit was restoring the missing `Path` import used by the builder's existing annotation.
- Do not comment through an ownership problem. The provider-neutral location protocol, adapters, identity confinement, and `artifact_location_adapter()` remain candidate Core. Automatic R2 credential discovery and client construction must move out of `docs_artifact_locations.py` into integrated composition, which can inject the existing `RemoteArtifactClient` boundary.

Current evidence directly covers provider validation, successful startup ordering, rejected-startup cleanup, ImportContent validation and projection, builder dry-run/write behavior, filesystem/R2 adapter workflows, and the integrated R2 authentication handoff.

## SDL-P2 Tooling Baseline

- Python linting is pinned to Ruff `0.16.0` in `python-requirements.txt`, installed in the ignored lint-local virtual environment, and configured for Python 3.12. The explicit `E4`, `E7`, `E9`, and `F` selection covers syntax, import placement, undefined names, and unused code without formatting, docstring coverage, or automatic fixes.
- JavaScript linting is pinned to ESLint `10.8.0`, `@eslint/js` `10.0.1`, and `globals` `17.7.0` in the private package and lockfile. The flat configuration applies the recommended correctness rules to current browser ESM with browser globals and no Node runtime globals, JSDoc plugin, TypeScript, or stylistic rule set.
- `docs-viewer/bin/lint-python` and `docs-viewer/bin/lint-js` require explicit paths. The SDL-P2 pilot named its three Python specimens and one JavaScript specimen directly; missing targets now fail rather than silently rechecking that fixed set.
- Both configurations retain the SDL-P0 generated, vendor, Mermaid, test, and unrelated exclusions. Ruff also forces those exclusions for explicit paths and keeps its cache inside the ignored lint-tool directory.
- Initial command output was classified in temporary delivery evidence. SDL-P2 introduced no source fix, suppression, formatter, repository-wide package, or check-profile change.

## SDL-P3 Type-Checking Decision

- TypeScript `7.0.2` was installed temporarily without package or lockfile changes and run only against `site/docs-viewer/runtime/js/shared/docs-viewer-app-composition.js`. The evaluation used `allowJs`, `checkJs`, `noEmit`, browser DOM types, and ESM-preserving settings; it added no `jsconfig.json`, `tsconfig.json`, or runtime build step.
- A non-strict `noResolve` run produced 9 local diagnostics with one root cause: the `createDocsViewerCollectionProvider()` options JSDoc names only `createCollectionProvider`, while the implementation also consumes eight route and service context properties. This is a genuine documentation finding, but `noResolve` cannot validate the imported contracts.
- Normal non-strict resolution produced 14 diagnostics across 3 files: the same 9 local diagnostics plus 5 off-target findings in imported modules. Strict checking expanded to 498 diagnostics across 26 files, including 443 in imports, so retaining it would turn the single-module pilot into a broad annotation backfill.
- The pilot therefore retains documentation-only JSDoc. The temporary TypeScript installation was removed; TypeScript, a type-check command, `jsconfig.json`, and `checkJs` remain deferred until extraction creates a stable module boundary or a later delivery explicitly accepts the wider typed dependency graph.
- SDL-P4 resolved the one genuine finding by documenting the complete collection-provider options shape without adding a broad suppression or changing runtime behavior.

## SDL-P4 Findings And Explicit Check Ownership

- `DocsViewerCollectionProviderOptions` now names the injected factory and all eight route/service context inputs consumed by `createDocsViewerCollectionProvider()`. Replaying the bounded SDL-P3 check produced no diagnostic; TypeScript was removed again and remains deferred.
- The router-module smoke now proves that rejected startup work is absorbed, reports failure, hides and clears stale document results, skips the remaining index phase, and always releases the initial busy state.
- Focused artifact-location tests now prove that the R2 path obtains credentials and constructs its client through the integrated Studio media service, and translates its credential exit to `RuntimeError`.
- The `docs` profile Python collection now includes the previously omitted ImportContent and artifact-location test files. Fixed-specimen lint steps are not normal profile members: each delivery must name its changed paths, and future profile integration requires a maintained adopted source boundary.
- No lint suppression, automatic fix, formatter, runtime behavior change, unrelated cleanup, TypeScript dependency, or additional check owner was introduced.

## Generated Reference Illustration

The exact layout depends on the generator. A generated API reference derived from the pilot comments would contain material like the following; this illustration is not a selected generator, committed output, or new documentation authority.

### JavaScript Example

#### `createDocsViewerCollectionProvider(options?)`

Create and validate the collection provider for the resolved route. The injected factory receives the same route/service context as the configured-scope fallback. Additional provider methods are preserved.

| Parameter | Type | Description |
| --- | --- | --- |
| `options.createCollectionProvider` | `CreateDocsViewerCollectionProvider` | Optional injected provider factory. |
| `options.generatedData` | `Object` | Generated-data read runtime. |
| `options.routeContext` | `Object` | Resolved route and application policy. |
| `options.routeSession` | `Object` | Route navigation state. |
| `options.scopeConfig` | `Object` | Configured-scope state. |
| `options.serviceContext` | `Object` | Available route services. |
| `options.source` | `Object \| null` | Optional source-service adapter. |
| `options.viewerScope` | `Function` | Current scope accessor. |
| `options.window` | `Object` | Browser window used by the provider. |

- **Returns:** `DocsViewerCollectionProvider`
- **Throws:** `Error` when the selected provider lacks `readIndex` or `readDocument`.

### Python Example

#### `ImportContent`

```text
ImportContent(
    source_kind: str,
    source_identity: str,
    record_identity: str,
    doc_id: str,
    title: str,
    content_intent: str,
    content_format: str,
    content: str | None = None,
    ...
)
```

Wrapper-neutral input for one planned Docs Import document. Construction rejects empty identities, unsupported intents or formats, conflicting content, and malformed metadata collections. The frozen record retains caller-owned nested values.

#### `ImportContent.as_dict()`

```text
as_dict() -> dict[str, Any]
```

Return a JSON-ready mapping with detached mutable metadata.

## SDL-P5 Stable Guidance And Closeout

- The Development Checklist now owns the proven adoption, authoring, suppression, explicit-target, and evidence-truthfulness rules, including a pre-implementation prerequisite question and mandatory independent end-of-delivery review.
- That review explicitly asks whether the delivery exposed an insufficient documentation, linting, or type boundary, then requires the failed surface and triggered separate adoption decision to be recorded rather than left to materialise informally.
- Run Checks now owns the isolated lint-tool installation and explicit command semantics. It rejects fixed pilot specimens as normal profile coverage and requires a maintained target boundary before future profile promotion.
- The research material has been moved outside the repository. No background note remains as repository authority, and the generated-reference illustration above does not select a generator or create a maintained output.
- No-target Ruff and ESLint commands exit 2; explicit runs against the three Python specimens and one JavaScript specimen pass. The corrected `docs` profile passed its actual 3 commands with 404 Python tests plus Studio docs and search generation.
- All completion gates are satisfied without a false coverage claim, suppressions, TypeScript, formatting churn, historical backfill, or unrelated cleanup.

## Delivery State

**Current state:** SDL-P5 complete; the source-documentation and linting pilot is closed with stable guidance, pinned isolated tools, reviewed examples, explicit delivery-owned lint targets, and no normal-profile claim beyond a maintained source boundary.

**Next checkpoint:** none inside this delivery. The Core Refactor Worktree Creation Gate may consume this completed prerequisite, but this closeout does not approve Core Refactor implementation or worktree creation.

## Ordered Steps

### [x] SDL-P0 — Bound The Pilot And Its Tool Owners

- Select the exact representative JavaScript and Python boundaries from the accepted Core Definition Baseline.
- Confirm the active browser-module roots, Python roots, workbench/test context, and generated, vendor, retired, and unrelated exclusions.
- Record the supported direct-browser JavaScript environment, Python interpreter target, and existing check-profile owner.
- Choose the smallest Docs Viewer-owned configuration, dependency, lockfile, and command locations without reusing the Mermaid renderer package or creating a repository-wide package boundary.

**Checkpoint:** the selected surfaces and tool owners are narrow, reproducible, and sufficient to exercise every representative-surface requirement before any dependency is installed.

### [x] SDL-P1 — Prove The Source-Documentation Conventions

- Add concise module responsibility documentation where ownership is not clear from the path and name.
- Add JSDoc to the selected exported JavaScript contract, including one useful shared object or callback shape.
- Add Python type hints where needed, then add concise docstrings to the selected service/domain and side-effect boundaries.
- Verify purpose, inputs, outputs, mutation, failure behavior, lifecycle, invariants, ordering, privacy, and exclusions only where they materially apply.
- Review any function that needs a long explanation and classify whether a small same-outcome simplification or a separate follow-up is the correct response.

**Checkpoint:** reviewed examples improve real code re-entry without restating signatures, inventing contracts, or documenting accidental structure.

### [x] SDL-P2 — Pin And Configure Focused Linting

- Install and pin Ruff through the chosen Python development-dependency owner.
- Create the private Docs Viewer JavaScript tooling package and lockfile; install and pin ESLint, `@eslint/js`, and `globals`.
- Configure only the selected Python surfaces and the real Docs Viewer browser-module contexts, with explicit generated, vendor, retired, and unrelated exclusions.
- Begin with correctness, undefined-name, unused-code, and similarly low-noise rules; do not enable docstring coverage, formatting, or broad automatic fixes.
- Add short direct commands and capture the initial findings in temporary delivery evidence rather than this durable document.

**Checkpoint:** both linters run reproducibly against only the agreed surfaces, and their initial findings are classified before source fixes begin.

### [x] SDL-P3 — Evaluate Selective JSDoc Type Checking

- Begin without a repository or Docs Viewer `jsconfig.json`.
- Evaluate one stable, well-documented JavaScript module with file-level or equivalently bounded TypeScript/JSDoc checking.
- Compare useful contract errors with false positives, annotation overhead, browser-module compatibility, and maintenance cost.
- If the result is useful and low-noise, pin TypeScript and persist the smallest Docs Viewer-owned configuration and command limited to the proven surface.
- If the result is not useful, remove the experiment and explicitly defer `jsconfig.json`, TypeScript, and `checkJs` without blocking the lint/documentation pilot.

**Checkpoint:** the delivery has one evidence-backed documentation-only or selectively type-checked JavaScript convention; project-wide `checkJs` remains out of scope.

### [x] SDL-P4 — Resolve Findings And Establish Check Ownership

- Fix genuine lint or documentation defects only inside the selected surfaces.
- Keep suppressions rare, local, and justified; remove misleading unused imports or abandoned references only after exact-use verification.
- Add or adjust focused executable evidence only when a newly documented material rule lacks adequate current coverage.
- Make the direct lint commands able to target new or materially changed Core Refactor files.
- Require explicit changed-source targets. Add lint to a `run_checks.py` profile only when that profile owns a maintained adopted source boundary; do not promote fixed pilot specimens as broad confidence.

**Checkpoint:** explicit-path lint commands and the independently owned Python/build/search profile checks pass, source behavior remains unchanged except for separately reviewed genuine fixes, and no unrelated cleanup entered the diff.

### [x] SDL-P5 — Stabilize Guidance And Close The Pilot

- Record only the proven JSDoc, docstring, type-hint, lint, suppression, and command rules in stable repository guidance.
- Do not add a generated API/reference site in this pilot. Retain the illustration above only as a preview; selecting a generator and owning its output remain a separate delivery.
- Confirm new or materially changed Core Refactor APIs have a clear adoption rule.
- Run the declared focused checks and `git diff --check`, report generated follow-through, and close only when every completion gate is true.

**Checkpoint:** the pilot leaves a small reproducible toolchain, useful reviewed examples, and a low-noise rule for subsequent Core Refactor deliveries.

## Out Of Scope

- Repository-wide JSDoc or docstring backfill
- Repository-wide zero-warning lint baselines
- A root JavaScript package, repository-wide `jsconfig.json`, or repository-wide `checkJs`
- Missing-documentation enforcement across historical code
- Broad reformatting
- Formatter adoption or broad automatic-fix batches
- Production generated API/reference documentation beyond the illustration above
- Semantic retrieval infrastructure or agent-retrieval benchmarking
- Complexity-driven mechanical rewrites
- Processing or unrelated application adoption
- Changes to the isolated Mermaid renderer toolchain
- Expansion of rule sets beyond what the pilot proves

Those remain separate outcomes under the owning repository feature.

## Completion Gates

- The chosen JavaScript and Python conventions are demonstrated by useful reviewed examples.
- Ruff and ESLint dependencies, lockfiles, configurations, and direct commands are Docs Viewer-owned, pinned, and reproducible.
- Configured source roots and runtime contexts match the actual application and exclude generated, vendor, retired, and unrelated sources.
- Focused project-local lint commands pass when the representative paths are explicit, and fail rather than imply coverage when no target is supplied.
- The selective JSDoc type-checking evaluation ends with an explicit retained or deferred decision; `jsconfig.json` is not required for completion.
- New and materially changed Core Refactor code has a clear, low-noise adoption rule.
- Material source-documentation claims agree with current code/config and executable evidence or name the remaining evidence gap.
- Genuine pilot findings are fixed or explicitly classified; suppressions are local and justified.
- New or materially changed sources have an explicit-path lint rule; normal profile integration is deferred until a maintained adopted source boundary exists.
- Stable repository guidance and check documentation reflect only the proven rules and commands.
- No repository-wide backfill, formatting churn, or unrelated cleanup has entered the delivery.

## Status

Complete. SDL-P0 through SDL-P5 established representative source-documentation conventions, pinned isolated Ruff and ESLint tooling, an explicit TypeScript deferral, focused evidence, explicit-path adoption rules, and stable guidance against false-green reporting. The fixed pilot specimens are not normal `docs` profile coverage. The delivery introduces no runtime behavior change, suppression, generated API/reference site, or repository-wide backfill.
