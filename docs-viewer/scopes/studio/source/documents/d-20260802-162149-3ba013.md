---
doc_id: d-20260802-162149-3ba013
title: "TE-2 — Table Detail View"
added_date: "2026-08-02 16:21:49"
last_updated: "2026-08-03 21:39:26"
summary: recognise the exact table-detail comment and deliver the public-safe static Content Detail table presentation that consumes it
ui_status: done
parent_id: d-20260802-150245-4e7f85
viewable: true
---
# TE-2 — Table Detail View

## Status And Purpose

Complete. **TE-2.0–TE-2.3** shipped as the second [Table Enhancements](/docs/?scope=studio&doc=d-20260802-150245-4e7f85) delivery, and the user accepted the verified implementation and representative live presentation for closeout on 2026-08-03. The delivery combines source eligibility and its first useful consumer into one complete outcome: an author marks a Markdown table with the maintained comment, and public or Manage readers can open that exact table in the shared static Content Detail View.

[Authoring Directives Concept And Examples](/docs/?scope=studio&doc=d-20260802-160429-69a5f6) owns exact-comment, next-table, source-preservation, and harmless no-op behavior. [Content Detail View Concept And Architecture](/docs/?scope=studio&doc=d-20260802-150246-bb3f88) owns the shared view, exact-target, expanded-layout, toolbar, public-composition, and static-presentation boundaries implemented here.

## Complete Outcome

The initial source form is:

```markdown
<!-- dotlineform:table-detail -->

| Project | Folder | Series |
| --- | --- | --- |
| Nerve | projects/nerve | Series 105 |
```

The build recognises only the maintained comment when its next non-blank source block is a Markdown table and projects `data-docs-content-detail="table"` onto that semantic table. Canonical Markdown and Markdown export retain the comment and table. Other comments and unassociated forms remain ordinary source and ordinary rendered output.

Every projected table in a public or Manage rendered document receives the same expand icon used on SVG diagrams in a compact pill control, with an accessible **Open table N** label. Invoking it supplies the exact table target from the current document mount to the registered `content-detail` main view. The existing main panel expands, the existing `main-view` toolbar supplies **Back to document** and a presentation label, and the table receives natural column sizing up to a `36ch` default maximum, wrapping for long text/links/code, a sticky header row, a sticky first column, and one bounded horizontal/vertical viewport. Leaving restores the prior rendered-document and panel context.

The document-level `viewer_content_detail: table` hint remains TE-5-owned. It later projects the same eligibility marker for its sole table without requiring a redundant comment. TE-2 does not add managed resizing, CSV, automatic entry, SVG publication, or table identity in the URL.

## Current Seams And Scope Guard

The likely blast radius is the Markdown build path that associates comments with tables and projects rendered payloads, plus the shared main-view/panel lifecycle, an exact Markdown-table target adapter, static table CSS, and public/Manage composition.

If implementation requires a central directive parser, browser comment parser, Markdown AST replacement, new shell, toolbar host, global view registry, route model, persistent table identity, or Manage dependencies in the public composition, stop for review and split the result.

## TE-2.0 Sanity Note

Completed on 2026-08-03. The feature parent is in progress, TE-1 is complete, and its delivery order names TE-2 as the next bounded outcome, so the scheduling dependency is satisfied.

The specified result fits the current broad owners. The shared Markdown renderer already preserves raw comments and exposes source-positioned Markdown blocks from which one exact comment can be associated with its next table while the Docs payload builder continues to own rendered `content_html`. The rendered-document controller already owns monotonically increasing document-mount generations and mounts focused adapters before dynamic document extras. The code-owned view registry, main hosted-view lifecycle, panel-layout projection, and stable `main-view` toolbar provide the existing shell seams for an exact transient target, expanded-main presentation, Back, restoration, and cleanup. Public and Manage have distinct composition entrypoints over the shared public-safe runtime, so both can opt into the same static table view while Review retains its returned-package composition.

The broad blast radius is source association and payload projection; rendered-document mount and adapter lifecycle; hosted-view request context and restoration; main/index/info layout projection; exact table registration and semantic-clone presentation; shared static table styling; public and Manage composition; and focused builder, lifecycle, target, layout, and import-boundary evidence. The deliverables remain exact recognition and marker projection, exact view entry and restoration, one bounded table viewport with natural widths below a readable default column maximum, sticky header and first column, transient-state cleanup, focused verification, and concise updates to the durable owners whose shipped contracts change.

The main benefit is one public-safe reading path from an explicit author choice to a substantially more usable large-table presentation. The main risks are false source association, stale or reselected targets, incomplete shell restoration, sticky-layer/overflow defects, and management code entering the public graph; exact block association, mount-generation validation, immutable request context, panel-owned projection, adapter-owned cleanup, and explicit public/Manage composition bound those risks. Manage retains its desktop/laptop route contract, while the public composition retains narrow-layout acceptance. If implementation crosses the existing scope guard, pause and split before expanding the architecture.

## Delivery Steps

### TE-2.0 — Confirm Scope And Delivery

- [x] Sanity-check the exact comment-to-static-view outcome against the current Markdown build, rendered-document mount, hosted-view, panel-layout, and app-composition model; resolve only contradictions that would change the result.
- [x] Confirm the broad blast radius across source association, payload projection, shared view/layout lifecycle, exact table targeting, static presentation, and public/Manage composition.
- [x] Confirm the deliverables: exact recognition and projection, shared view entry/restoration, bounded static table presentation, focused evidence for the changed contracts, and concise durable owner updates.
- [x] Stop for review if the result crosses the scope guard above. Do not produce an exact file inventory, line estimate, frozen implementation, fixture implementation, or complete test architecture at this gate.

**Checkpoint:** complete. The sanity note confirms the spec, broad owners, deliverables, risks, public/local boundary, and stop condition. The delivery stopped here and received approval to continue with implementation.

### TE-2.1 — Recognise And Project Eligibility

- [x] Recognise only the exact maintained comment without interpreting ordinary HTML comments or classifying malformed lookalikes.
- [x] Associate it with one immediately following source Markdown table through maintained rendering/source-position seams rather than matching rendered HTML.
- [x] Project only `data-docs-content-detail="table"` onto the associated semantic table while preserving canonical Markdown, export, table cell HTML, links, alignment, headings, captions, and ordinary fallback behavior.
- [x] Support the same marker independently for the later TE-5 document-default case without implementing automatic entry here.

### TE-2.2 — Add The Shared Static Table View

- [x] Register one public-safe `content-detail` main hosted view through the existing view registry and explicit public/Manage composition.
- [x] Extend hosted-view requests with a bounded immutable target context, expanded-main panel projection, existing-toolbar Back/label controls, restoration, and lifecycle cleanup.
- [x] Decorate only eligible tables in the accepted rendered-document mount and supply the exact target without reselecting from title, headers, URL, displayed row, or another table.
- [x] Present the accepted semantic table in one bounded two-axis viewport with natural widths up to a readable default maximum, long-content wrapping, sticky header and first column, correct intersection layering, and ordinary link/text behavior.
- [x] Release transient target, scroll, and event state on Back, navigation, mount replacement, or view disposal.

**Implemented contract:** the Markdown renderer projects the marker from one exact standalone comment to its immediately associated table and exposes the same one-table projection as an explicit future-default option. The rendered-document adapter registers only marked tables from the accepted mount and validates the immutable document target, mount generation, kind, and opaque record before cloning the semantic table. The registered `content-detail` view uses the panel-owned `expanded-main` layout and existing `main-view` controls; public and Manage compose it explicitly, and Review retains its returned-package composition. Static table columns size naturally below a `36ch` default maximum, with long text, links, and code allowed to wrap; TE-3 retains ownership of user-controlled widths. Back restores live document context, while navigation, mount replacement, disposal, stale targets, and failures release transient state.

### TE-2.3 — Verify And Transfer

- [x] Run the smallest focused checks that prove exact source association, marker projection, view/layout lifecycle, exact target use, and public/Manage composition through the production owners changed.
- [x] Accept the representative live Projection Contract review in Manage desktop, focused public/Manage composition and lifecycle evidence, and shared narrow-layout boundary for closeout; the public narrow route was not separately hand-reviewed.
- [x] Inspect representative generated output only when the projection changed or its freshness is uncertain; let expected watcher output stand and add broader checks only for a concrete failure they can catch.
- [x] Update only the durable authoring, payload, panel, toolbar, or table-presentation owners whose shipped contract changed.

**TE-2.3 evidence:** focused renderer and payload-builder evidence covers exact association, inert lookalikes, source preservation, one-table default projection, marker count, link rewriting, and ordinary-table fallback. The maintained Table Detail module smoke covers explicit public/Manage composition, Review's existing composition, accepted-target validation, semantic clone relationships, expanded-main state, restoration/cleanup, failure handling, and the accessible icon-only entry control; router and Diagram Detail compatibility smokes remain green. Public runtime-boundary tests cover the deployable import graph. On the live Projection Contract Catalogue table, the default column cap reduced the presentation from about `5149px` to `1756px`; long columns resolved near `385px` and wrapped while horizontal scrolling remained available. The live review also confirmed the `44×32px` pill using the existing SVG expand glyph, its accessible **Open table 1** label, Content Detail entry, and Back restoration. The user accepted this representative presentation and requested closeout on 2026-08-03. A separate hands-on public narrow route review was not performed; the shared CSS boundary and focused public app-kind composition remain the evidence for that surface.

## Not In Scope

- resizing columns, Reset, Copy as CSV, clipboard behavior, or persisted widths, owned by TE-3;
- diagram migration into the shared view, owned by TE-4;
- default initial presentation for a dedicated table document, owned by TE-5;
- table-to-SVG preparation or public substitution, owned by TE-6;
- automatic eligibility based on table geometry, content, headings, row count, or viewport;
- dynamic report/component tables, `docs_subscope` surfaces, search results, Recent, or Review composition;
- stable table IDs, fragments, direct table-detail URLs, persisted viewer state, sorting, filtering, selection, editing, formulas, charts, virtualization, clipboard, or download export; or
- a generic directive registry, diagnostics system, browser source parser, modal table application, second toolbar, or parallel view model.

The [Table Enhancements feature](/docs/?scope=studio&doc=d-20260802-150245-4e7f85) owns sequencing. TE-2 is complete and supplies the public-safe table-detail foundation consumed independently by TE-3, TE-4, and TE-5. This delivery remains parked beneath the active feature until a later feature-level closeout list receives explicit approval.
