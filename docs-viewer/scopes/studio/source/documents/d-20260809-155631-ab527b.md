---
doc_id: d-20260809-155631-ab527b
title: "CII-1 — Catalogue Image Source Insertion Delivery"
added_date: "2026-08-09 15:56:31"
last_updated: "2026-08-09 17:49:47"
summary: add exact Work and Series images to Docs source with current image presentation controls and Catalogue destinations
ui_status: in-progress
parent_id: d-20260428-000000-f5ff18

---
# CII-1 — Catalogue Image Source Insertion Delivery

## Status

CII-1.2 is complete at its source-authoring gate. The active standalone delivery remains directly under the [Roadmap](/docs/?scope=studio&doc=d-20260428-000000-f5ff18) and is waiting for explicit approval before CII-1.3 integration and closeout. The **🏞️ Add Catalogue image** action is available in Manage Markdown source; this checkpoint does not claim the saved-and-rebuilt Work and Series integration evidence reserved for CII-1.3.

## Complete Result

The Docs source editor adds **Add Catalogue image** beside **Add image** and **Add catalogue token**. An author searches the existing mixed Catalogue target set, selects an exact published Work or Series, applies the current image presentation options, and inserts one semantic Catalogue image occurrence into the source buffer.

After the ordinary source save and rebuild, the occurrence displays the target's current Catalogue image. Clicking the image opens the exact Catalogue destination: a Work image opens that Work, while a Series image opens that Series even though its displayed image comes from the Series' primary Work.

This is one source-authoring and rendering workflow. CII-1.0 selected rebuild-time resolution because canonical source should retain semantic identity and authored presentation rather than a detached image URL. The ordinary save and rebuild remains the only author-facing flow; there is no separate mode, phase, or concurrency requirement.

## Accepted Contract

### Identity, Image, And Destination

The source occurrence preserves three distinct meanings:

- semantic identity is the selected Catalogue family, target type, and canonical target id;
- image presentation is resolved from the exact target lookup row; and
- activation uses that same row's exact Catalogue destination.

For a Work, the image projection comes from that Work's current primary Catalogue image and the destination remains the Work page.

For a Series, the image projection comes from the Work identified by the Series' exact `primary_work_id`, while semantic identity and destination remain the Series. The image source must not retarget the occurrence to the primary Work.

Only current published lookup targets are selectable. Missing image data, a missing or invalid Series `primary_work_id`, a primary Work that is not a current Series member, or an unusable destination fails closed. The workflow does not fall back to the first member, infer identity from title or route, or choose another image.

### Source And Rendering

The new occurrence belongs to the existing Catalogue semantic family but has an explicit image presentation. Existing text Catalogue tokens keep their current grammar and rendering.

Canonical source stores the exact target identity and authored presentation values rather than copying Catalogue media into the Docs scope. Rebuild resolves the browser-safe image and destination through the existing generated target lookup and emits a linked image or linked semantic figure.

The lookup remains the single focused projection shared by authoring and rendering. Extending its Work and Series rows with an allowlisted image projection must not expose canonical records, filesystem paths, provider credentials, or unrelated media metadata.

The occurrence contributes the same exact Catalogue identity to semantic usage and broken-link behavior as a text occurrence. Image presentation does not create a second relationship family or usage index.

### Authoring Surface

**Add Catalogue image** reuses the current mixed Work and Series search, result identity, keyboard navigation, and exact selection contract from **Add catalogue token**.

Its toolbar glyph is **🏞️**, with **Add Catalogue image** retained as the visible tooltip and accessible label.

After target selection it provides the current **Add image** presentation values:

- required alt text;
- optional caption;
- optional summary when a caption is present;
- full-column, image-left, or image-right placement; and
- fill available width or natural width.

Target selection may suggest the current Catalogue title for alt text and caption, but those values are authored occurrence snapshots. A later Catalogue title change does not rewrite source text automatically.

Confirmation inserts at the captured source selection, returns focus, and uses the normal dirty state. It does not save or rebuild independently, publish media, or mutate Catalogue data.

Placing the caret inside an image occurrence opens the Catalogue Info view with immutable target identity and editable presentation fields. Update and Remove retain the existing exact-range stale guard; changing the Work or Series target remains Remove followed by **Add Catalogue image**.

## Delivery Boundary

CII-1 includes:

- one manage-only **Add Catalogue image** source action;
- exact Work and Series target selection through the current Catalogue picker;
- one focused semantic image occurrence and renderer without changing text-token behavior;
- allowlisted Work and Series image fields in the existing generated target lookup;
- linked plain-image and semantic-figure output using the existing image presentation classes and validation;
- Catalogue Info inspection, presentation update, and removal for the image occurrence;
- existing semantic usage and broken-link follow-through for the exact Catalogue identity;
- focused parser, lookup, rendering, source-adapter, and contribution evidence; and
- current durable UI and target-lookup documentation at closeout.

CII-1 excludes:

- uploading, copying, replacing, resizing, or publishing Catalogue media;
- changing Work media, Series membership, `primary_work_id`, or Catalogue save validation;
- selecting a Series member other than its exact primary Work;
- Moment targets or another semantic family;
- retroactively converting existing text tokens, Markdown images, or figures;
- a generic media-token framework, second Catalogue lookup, compatibility grammar, or fallback reader;
- automatic source rewriting when Catalogue titles, images, or Series primary Works change;
- public Catalogue route redesign or Docs image-layout redesign; and
- background work, receipts, locking, or multi-user concurrency machinery.

## Likely Owners To Inspect

Implementation should begin at the current ownership seams and narrow the exact changed set during CII-1.0:

- the semantic-token registry, generated target-lookup builder, and Catalogue Work/Series source adapters for the allowlisted image projection;
- the semantic-token parser, renderer, usage projection, and broken-link owner for the visual occurrence;
- the Catalogue target picker and source-action contribution for exact selection and insertion;
- the existing staged-image presentation and figure-fragment owners for shared validation and output classes without invoking staged-media publication; and
- the focused semantic-token, target-lookup, staged-media-fragment, source-editor module, and builder tests.

This is an inspection route, not a frozen file inventory. Stop and re-scope if the result requires copied media, a new provider abstraction, a second identity registry, a generic token engine, or changes to Catalogue write authority.

## CII-1.0 Checkpoint Record

CII-1.0 passed its documentation and code sanity gate on 2026-08-09. The result is coherent against the current semantic-token, Catalogue publication, source-editor, image-fragment, Docs rendering, and Manage/public link boundaries. No production source, generated target lookup, Catalogue record, media object, or public route changed.

### Frozen Source Form

Existing text occurrences remain byte-compatible:

```text
[[catalogue:work:00638|3 symbols]]
```

An image occurrence adds an explicit `image` presentation segment and one canonical RFC 3986 percent-encoded field string:

```text
[[catalogue:image:work:00638|alt=3%20symbols]]
[[catalogue:image:series:005|alt=3%20symbols&caption=3%20symbols&summary=intangible%0Ashifting%20boundaries&placement=left&fill_width=true]]
```

The identity is `catalogue:image:<target type>:<canonical target id>`. Fields serialize once in the order `alt`, optional `caption`, optional `summary`, `placement`, and `fill_width`. `alt` is always required. Caption absence produces a linked plain image; caption presence requires `placement` and explicit boolean `fill_width`, while `summary` remains optional. Text normalization, allowed placement values, and HTML escaping reuse the current staged-image presentation contract.

The parser rejects unknown, duplicate, empty, non-canonical, or contradictory fields. It recognizes the complete occurrence as one exact source range outside fenced code, inline code, and HTML comments. Existing three-part text parsing and serialization do not reinterpret the four-part form.

The visual occurrence's usage-index `title` is its caption when present and otherwise its alt text. The current usage-index row shape remains sufficient because relationship identity, source range, raw source, title, and destination are unchanged; image presentation does not enter the relationship schema.

### Frozen Lookup And Rendering Contract

The existing generated file cuts over in place to `docs_semantic_token_target_lookup_v2`; there is no v1 reader, compatibility writer, second lookup, or fallback. Every current published target keeps its existing identity, title, `href`, and metadata. An optional `image` object adds only one browser-safe `src`.

Work `image.src` is the public 1600-pixel primary WebP URL assembled from `site-tools/config/site-tools.json`, `_data/pipeline.json`, the canonical Work id, and positive `media_version`, including the existing `?v=<media_version>` cache version. A Work row without complete published media remains a valid text target but has no image projection.

Series `image.src` is copied from the exact canonical Work named by `primary_work_id` only when that Work exists, is published, belongs to the Series through `series_ids`, and has a valid Work image projection. The Series row retains its Series identity and `/series/?series=<id>` destination. Missing or invalid image data never removes an otherwise valid text target and never falls back to another member.

The image picker lists only rows with a valid image projection. A visual source occurrence whose target later loses its image remains source text, emits no resolved usage, and appears in Docs Broken Links as `missing_image`; text occurrences for the same target continue to resolve normally.

Rendering emits one escaped semantic anchor with the existing target identity data attributes, `_blank`/`noopener noreferrer`, and either a plain image or the current `docsViewerFigure` classes. Manage rebases that anchor through the existing public-preview link mount; public Docs use the same root-relative Catalogue destination. A focused linked-image wrapper extends the current figure selectors for width, natural-width, left/right placement, mobile stacking, and keyboard focus without redesigning the layout.

The source occurrence is not rewritten when the target image, title, or Series primary Work changes. A later target-lookup refresh plus ordinary Docs rebuild updates rendered image and destination projection; CII-1 adds no background rebuild or synchronization owner.

### Current Data And Baseline

The current lookup is unchanged at 2,074 targets: 1,935 Works and 139 Series. All 1,935 published Works currently have a filename, positive media version, and pixel dimensions. All 139 published Series currently have an exact published primary Work that belongs to the Series and has valid media. These counts prove the present canonical dataset, not a permanent requirement that every future text target carry an image.

The public Work route already constructs `https://media.dotlineform.com/works/img/<work id>-primary-1600.webp?v=<media version>` from the same public-media and pipeline settings. CII-1 reuses that contract; it does not fetch a served URL or verify remote bytes during lookup generation.

Baseline evidence passed before implementation:

- 29 focused Python checks across target lookup, semantic rendering/usage, Broken Links, and staged-image fragments;
- the focused Catalogue-token browser module smoke, including mixed Work/Series search, exact-range update/remove, keyboard behavior, public-preview link rebasing, and source-adapter guards;
- target-lookup dry-run unchanged with 2,074 rows; and
- a canonical-data audit with zero published Work media gaps, zero published Series primary-work gaps, and no existing four-part Catalogue image occurrence.

The first Python attempt stopped during collection because `.env.local` resolved an external Projects workspace unavailable to the sandbox. Repeating the same checks with an isolated readable/writable Projects root passed; this was environment setup, not product evidence.

### Exact Proposed Change Set

CII-1.1 production changes are limited to the semantic-token registry lookup-field declaration, target-lookup builder, semantic occurrence parser/renderer, Broken Links classifier, browser lookup normalizer, and shared Docs Viewer figure stylesheet. The existing site-tools and pipeline configs, Catalogue source/publication validators, staged-media publication service, image-fragment validation, public Catalogue routes, and semantic usage artifact writer are reused without changing ownership.

CII-1.1 focused evidence updates the Catalogue contract fixture plus target-lookup, semantic rendering/usage, Broken Links, and browser target-normalization checks. The generated target lookup is written only after its v2 dry run is reviewed.

CII-1.2 adds focused `catalogue-image-modal.js` and `catalogue-image-contribution.js` owners, extracts the current private image-presentation controls into one source-editor presentation module reused by staged **Add image**, and extends the existing family parser, Catalogue Info view, action definitions, hosted control registry, and Manage composition. The toolbar order becomes **Add image** (`🧜‍♀️`), **Add Catalogue image** (`🏞️`), **Add file** (`📎`), then **Add catalogue token** (`📍`).

CII-1.2 focused evidence extends the existing Catalogue-token module smoke and management composition checks. It does not add a second modal shell, picker, source adapter, token registry, or browser-choreography-only permanent suite.

### Risks And Stop Conditions

The main risks are cross-language source serialization drift, accidentally making image eligibility gate text links, unsafe decoded occurrence fields, Series-to-Work identity leakage, linked-wrapper CSS changing ordinary figures, and stale Manage links using the local service origin. The frozen source fixture, optional image row, exact Series validation, escaping checks, targeted selectors, and existing public-preview mount each own one of those risks.

Stop and re-scope if implementation requires copied Catalogue bytes, remote-object verification during lookup generation, a second lookup or registry, a generic presentation engine, text-token migration, Catalogue write changes, automatic Docs rebuild after Catalogue saves, or a layout redesign. Actual remote image loading and tactile modal/figure fit remain CII-1.3 integration evidence, not claims made at this checkpoint.

## CII-1.1 Checkpoint Record

CII-1.1 passed its focused implementation gate on 2026-08-09. The target lookup cut over in place to v2, the visual source occurrence now resolves during the ordinary Docs build, and no CII-1.2 authoring control or modal was added.

The generated lookup remains one allowlisted projection with 2,074 unique targets. All 1,935 current Work rows and 139 current Series rows have one HTTPS `image.src`; an independent assertion confirmed every Work URL against its own canonical id and media version, and every Series URL against only its exact published member named by `primary_work_id`. Focused fixtures also prove that an otherwise valid text target remains present without `image` when complete media or exact Series membership is unavailable.

The Python parser accepts the frozen plain-image and figure forms as one exact occurrence, requires canonical RFC 3986 field encoding and ordering, and rejects duplicate, unknown, empty, contradictory, non-canonical, or incomplete fields. It remains inactive in fenced code, inline code, and HTML comments. Resolved output is an escaped semantic anchor around either a plain image or the existing semantic figure classes; caption-or-alt continues to supply the unchanged usage-index v1 `title`. A target without `image` remains literal, emits no usage row, and is classified as `missing_image`, while the same target's text occurrence still resolves.

The browser lookup reader now requires v2, preserves only a safe HTTPS or root-relative image source, and has no v1 fallback. Linked-image CSS is limited to the new plain-image and figure-anchor wrappers while retaining existing ordinary image and figure selectors, including right placement, natural width, and public mobile stacking. Manage destination rebasing continues through the existing semantic-anchor mount.

Focused evidence passed:

- 18 lookup, parser, renderer, usage-index, contract-fixture, and Broken Links Python checks;
- 2 public Docs payload regression checks;
- explicit Python lint across 11 changed owners/tests and JavaScript lint for the browser lookup normalizer;
- the Catalogue-token browser module smoke, including v2 image safety and linked-image computed CSS;
- the sub-scope report browser smoke for the shared lookup-reader cutover;
- post-write target-lookup dry run unchanged at 2,074 rows; and
- independent bounded-diff and whitespace review with no source-editor action, Catalogue write owner, staged-media publication owner, public route, or semantic usage schema change.

There are no existing `catalogue:image` source occurrences, so this checkpoint does not claim an authored end-to-end browser flow or remote image load. Those remain CII-1.2 and CII-1.3 evidence respectively.

## CII-1.2 Checkpoint Record

CII-1.2 passed its source-authoring gate on 2026-08-09. Manage Markdown source now presents **Add image** (`🧜‍♀️`), **Add Catalogue image** (`🏞️`), **Add file** (`📎`), and **Add catalogue token** (`📍`) as separate actions in that order.

The new action reuses the existing mixed Catalogue picker but lists only exact Work and Series rows carrying the v2 image projection. Like Projects **Assign subject**, selecting a target projects its title into Search Catalogue, closes and empties the result list, and keeps no separate selected-item message; editing the search reopens current matches. It serializes the frozen plain-image or semantic-figure occurrence through the captured-selection adapter, so insertion remains one ordinary source-buffer mutation. Alt text, optional caption and summary, placement, and fill width use the presentation controls extracted from staged **Add image** without invoking staged-media publication.

Catalogue Info recognizes the same visual occurrence and keeps target identity and destination immutable while updating or removing only its exact guarded source range. Existing text-token parsing, **Add catalogue token**, and staged **Add image** remain independently available.

Focused evidence passed:

- JavaScript lint for the changed source-editor and Manage composition owners, Python lint for the two focused smoke owners, and whitespace review;
- the Catalogue-token module smoke covering strict visual parsing and serialization, image-only target eligibility, keyboard selection, suggestions, cancellation, exact insertion, stale-range rejection, focus return, and Catalogue Info update/remove;
- the full Manage service smoke covering production composition, the exact toolbar order, and the staged **Add image** presentation regression; and
- manual browser review of mixed Work/Series keyboard search and exact Series selection, selected-title projection and result-list collapse/reopen, caption-dependent control enablement, right placement, natural width, cancellation, exact insertion, unsaved-state projection, and post-modal source focus.

The manual occurrence was removed from the source buffer without saving. CII-1.2 did not rebuild an authored occurrence, load a remote Catalogue image, follow a Work or Series destination, change Catalogue data, or update closeout documentation; those remain CII-1.3 work.

## Delivery Steps

### CII-1.0 — Sanity And Source Contract Gate

- [x] Compare the complete result with the current Catalogue lookup, semantic-token grammar/rendering, source-action contribution, image-presentation fragment, and Work/Series image owners.
- [x] Confirm the smallest unambiguous source representation for one Catalogue image occurrence while leaving existing text tokens byte-compatible and separately editable.
- [x] Freeze the allowlisted lookup image shape, exact Work and Series resolution rules, authored occurrence fields, unresolved behavior, and linked output contract.
- [x] Confirm the likely blast radius across generated lookup refresh, source insertion, Docs rebuild, semantic usage, broken links, Manage rendering, and public rendering.
- [x] Record the bounded production owners, focused evidence, risks, and stop conditions, then stop for implementation approval.

**Gate:** Documentation completion is not product implementation. Stop before CII-1.1 until the source and projection contract is reviewed.

### CII-1.1 — Exact Image Projection And Rendering

- [x] Extend the existing target lookup with the minimum browser-safe image projection for eligible Works and Series.
- [x] Resolve Series presentation only through its exact valid `primary_work_id`, while preserving the Series identity and destination.
- [x] Parse and render the focused Catalogue image occurrence as a linked plain image or linked semantic figure using current presentation validation and classes.
- [x] Preserve exact semantic usage and broken-link evidence for resolved and unresolved visual occurrences without changing text-token output.

**Gate:** Focused lookup, parser, renderer, usage, and broken-link evidence passes; independently review the bounded diff before the source-editor action begins.

### CII-1.2 — Add Catalogue Image Authoring

- [x] Register **Add Catalogue image** as a separate manage-only source action using the existing action-contribution boundary.
- [x] Reuse the mixed Catalogue picker, then collect alt text, caption, summary, placement, and fill-width values through the current image-presentation contract.
- [x] Insert the serialized occurrence through the captured-selection adapter and preserve cancellation, focus return, dirty-state, save, and rebuild behavior.
- [x] Extend Catalogue Info to inspect, update, and remove the image occurrence through the existing immutable-identity and stale-range contract.
- [x] Keep **Add image** and **Add catalogue token** unchanged and independently available.

**Gate:** Focused source-action and adapter evidence passes, followed by manual review of keyboard selection, formatting controls, cancellation, insertion, and focus behavior.

### CII-1.3 — Integration And Closeout

- [ ] Verify one Work and one Series from source insertion through rebuilt Manage output, including exact image, exact click destination, presentation variants, and semantic usage.
- [ ] Verify draft/unpublished targets and missing or invalid image projections are unavailable or unresolved without fallback.
- [ ] Confirm public-safe rendering uses only projected browser data and no local service or canonical Catalogue source.
- [ ] Run the smallest relevant source lint, focused Python and JavaScript checks, generated lookup and Docs projection assertions, and whitespace review.
- [ ] Update [Semantic Tokens Source Editor UI](/docs/?scope=studio&doc=d-20260623-180405-631ef4) and [Target Lookup](/docs/?scope=studio&doc=d-20260726-185814-c5e581) with shipped behavior, then prepare the explicit retain-or-retire closeout recommendation.

## Completion Evidence

- A Work selected through **Add Catalogue image** rebuilds as its current image and opens that exact Work destination when clicked.
- A Series selected through the same action rebuilds with its exact primary Work's image but opens the exact Series destination and remains a Series semantic occurrence.
- Alt text, optional caption and summary, placement, and fill-width behavior match the current **Add image** presentation contract.
- Catalogue Info updates or removes one exact image occurrence without changing its Work or Series identity.
- Existing text Catalogue tokens and staged **Add image** behavior remain unchanged.
- The generated target lookup is the only Catalogue-to-Docs image projection and contains no private source or provider data.
- Missing, draft, stale, or malformed targets create no inferred image, destination, or relationship.
- Source insertion remains an ordinary local buffer mutation committed by the existing save and rebuild workflow.

## Durable Documentation At Closeout

- [Semantic Tokens Source Editor UI](/docs/?scope=studio&doc=d-20260623-180405-631ef4) owns the shipped authoring, occurrence, formatting, and interaction contract.
- [Target Lookup](/docs/?scope=studio&doc=d-20260726-185814-c5e581) owns the shipped Work/Series image projection and exact resolution boundary.
- [Development Checklist](/docs/?scope=studio&doc=d-20260523-190651-7157ec) changes only if implementation establishes a reusable guardrail not already recorded there.
