---
doc_id: d-20260719-123719-fb7565
title: Document Diagrams
added_date: "2026-07-19 12:37:19"
last_updated: "2026-07-26 17:54:41"
summary: Author, build, publish, render, inspect, and extend scope-owned Mermaid diagrams in Docs Viewer.
parent_id: d-20260424-000000-50b63f
viewable: true
---
# Document Diagrams

## Purpose

Docs Viewer uses Mermaid for authored architecture and workflow diagrams that benefit from durable, reviewable source. It supports two source contracts:

- canonical `.mmd` plus a same-basename sanitized SVG for reusable or independently managed diagram media;
- one accessible fenced `mermaid` block for a diagram that belongs only to its document.

The fenced block remains canonical Markdown. Managed local readers render it in memory. Configured public preparation derives a validated light and dark SVG pair, replaces the fence only in the public payload copy, and ships neither source nor Mermaid runtime to public browsers.

Finished SVG and Mermaid source share the source editor's **Add image…** action but keep different contracts. A staged `.svg` is already-finished vector media; a staged `.mmd` is retained as canonical source and rendered before its SVG token is returned.

The static-media workflow is shipped for every scope. Fenced Mermaid rendering is shipped for both managed-local scope types, and fenced Mermaid public projection is shipped for configured public scopes.

## Dependency And Registration

The feature-local Node package root is `docs-viewer/build/mermaid/`:

- `package.json` pins the direct Mermaid CLI dependency;
- `package-lock.json` is the resolved dependency authority;
- `mermaid-config.json` owns shared renderer settings;
- `node_modules/` is local, ignored installation output.

Install the locked dependency tree from the repository root:

```bash
npm ci --prefix docs-viewer/build/mermaid
```

[Runtime Dependencies](/docs/?scope=studio&doc=d-20260423-204839-111501) owns Node package-root policy. [Local Setup Toolchain](/docs/?scope=studio&doc=d-20260519-210908-2f8f00) owns the supported Node/npm baseline and environment checks.

Registration has two layers:

1. `docs-viewer/build/docs_builder/media_builds.py` allowlists the code-owned `mermaid` producer implemented by `docs-viewer/services/docs_mermaid_media.py`.
2. Each participating scope opts in through reciprocal source and published-media records in `docs-viewer/config/scopes/docs_scopes.json`.

```json
"source": {
  "build_media": {
    "mermaid": {
      "path": "media/mermaid",
      "producer": "mermaid",
      "publishes_to": "svg"
    }
  }
}
```

The same scope's published `svg` record must declare:

```json
"build_inputs": ["mermaid"]
```

Create `<scope source root>/media/mermaid/` as the visible canonical-source location. Sub-scopes do not inherit their parent's registration; configure one separately only when it independently owns diagrams and an SVG publication contract.

## Author Workflow

For initial intake through Docs Viewer:

1. save an accessible `.mmd` as a direct child of shared import staging;
2. open the target document's Markdown source editor and choose **Add image…**;
3. select the Mermaid source and provide useful Markdown alt text;
4. allow the service to render in temporary storage, publish and verify canonical source plus SVG, and insert the SVG token;
5. review the dirty Markdown buffer and choose **Rebuild doc**.

A render or publication failure inserts no token and writes no document source. Staged input remains in place after success.

For later edits, view the rendered document and open Document Info. Its manage-only **Diagrams** section lists verified diagram-title links. Choosing one delegates the logical source identity to VS Code without exposing a physical path. Save the canonical `.mmd`; Docs Watcher renders only that changed identity. The Markdown token and search index do not change.

Public routes never show these source links. Markdown source mode retains its semantic-token interface rather than becoming a Mermaid editor.

## Repository Workflow

Repository authors and Codex can work directly:

1. write canonical source to `<scope source root>/media/mermaid/<diagram-name>.mmd`;
2. add the same-basename SVG token to the owning Markdown document;
3. run the targeted document build:

```bash
$HOME/miniconda3/bin/python3 docs-viewer/build/build_docs.py --scope <scope> --only-doc-ids <doc-id> --write
```

The producer runs before the selected document payload is rendered. A full Docs build renders every configured Mermaid identity in the scope. The watcher, staged intake, targeted builds, and full builds all call the same registered producer.

## Source And Publication Contract

Use lowercase kebab-case identities:

```text
<scope source root>/media/mermaid/<diagram-name>.mmd
<configured published svg root>/<diagram-name>.svg
```

Markdown uses the scope's ordinary SVG media token:

<pre><code>![Useful diagram alt text](&#91;&#91;media:docs/&lt;scope&gt;/svg/&lt;diagram-name&gt;.svg&#93;&#93;)</code></pre>

Each scope owns its source and derived SVG. Do not share canonical source across scopes, edit generated SVG by hand, or register an unversioned global `mmdc` executable.

The repository-pinned renderer uses a neutral theme, white background, strict security, `htmlLabels: false`, and an explicit 1200 by 800 browser viewport. Every source requires non-empty Mermaid `accTitle` and `accDescr`; every Markdown reference requires useful alt text. The shared SVG sanitizer must preserve a responsive `viewBox`, accessibility metadata, and visible content before publication succeeds.

## Code Authority And Extension

- dependency and renderer settings: `docs-viewer/build/mermaid/`
- producer implementation: `docs-viewer/services/docs_mermaid_media.py`
- producer registry and full/targeted invocation: `docs-viewer/build/docs_builder/media_builds.py`
- scope activation and locations: `docs-viewer/config/scopes/docs_scopes.json`
- staged author intake: `docs-viewer/services/docs_staged_media_service.py`
- rendered source discovery/opening: `docs-viewer/services/docs_diagram_source_service.py`
- changed-identity rebuild: `docs-viewer/services/docs_live_rebuild_watcher.py`
- public fence planner and manifest contract: `docs-viewer/services/docs_public_mermaid_projection.py`
- public pair producer and build entrypoint: `docs-viewer/services/docs_public_mermaid_producer.py`, `docs-viewer/build/build_public_mermaid_projection.py`
- public payload projection and publish boundary: `docs-viewer/services/docs_public_mermaid_payload.py`, `docs-viewer/services/docs_publish_gate.py`
- public theme selection: `site/docs-viewer/runtime/js/public/docs-viewer-public-themed-diagrams.js`
- public projection contracts: `test_docs_public_mermaid_projection.py`, `test_docs_public_mermaid_producer.py`, `test_docs_public_mermaid_payload.py`, `test_docs_publish_gate.py`, `docs_viewer_diagram_detail_modules.py`, and `public_docs_viewer_readonly.py`
- focused contracts: `docs-viewer/tests/python/test_docs_mermaid_media.py`, `test_docs_staged_media_service.py`, `test_docs_diagram_source_service.py`, and `test_docs_live_rebuild_watcher.py`

To add another build-source type, register one code-owned producer, configure its confined source path and published target, add the reciprocal published `build_inputs` declaration, and test full builds, targeted builds, intake, watcher invalidation, publication verification, and reader-runtime exclusion together.

## Examples

- [Configuration And Extension Points](/docs/?scope=studio&doc=d-20260512-142027-54b354) demonstrates configuration, executable ownership, generated projection, and runtime capability.
- [Docs Import](/docs/?scope=studio&doc=d-20260424-000000-fc8b5d) demonstrates document intake, embedded media, source-editor media, publication, source writing, and rebuild boundaries.

## Fenced Document Workflow

Author an accessible fenced `mermaid` block directly in the document when the diagram belongs only to that document and does not need its own managed media identity. Keep non-empty `accTitle` and `accDescr` metadata in the fence. Canonical Markdown, the managed payload, Markdown export, and static HTML export retain the fence and its source.

In either configured managed local scope type, the manage reader finds those fences after the ordinary document mount, lazily loads the checked Mermaid `11.16.0` browser asset once for the application session, and replaces each successful fence with an inline SVG host. Several fences render sequentially. Navigation invalidates late results, and a failed fence retains its source with a concise visible status while technical detail stays in the console.

For a configured public projection, first make the ordinary Docs payload current, then prepare the complete theme pairs:

```bash
$HOME/miniconda3/bin/python3 docs-viewer/build/build_public_mermaid_projection.py --scope <scope> --write
```

Add `--sub-scope <sub-scope>` when the configured public sub-scope owns the document. The command uses the pinned renderer, shared sanitizer, and configured working-documents artifact adapter. It writes only projection-owned light/dark pairs plus `.publish/public-mermaid-projection/manifest.json`. Both variants must render, sanitize, preserve matching accessibility metadata and geometry, publish, and byte-verify before the family enters that manifest.

Run this preparation before the existing public publish status, confirmation, and apply flow. The publish gate does not render Mermaid. It revalidates the prepared manifest and exact SVG bytes, matches each family to the current document fence and source digest, replaces only the public payload copy, and publishes only consumed variants. A missing, partial, unsafe, or stale family blocks projection. The manifest remains private, and public routes receive neither Mermaid source nor the managed adapter or vendor runtime.

The active managed-local scope type remains the in-browser rendering rule: both `local` and `local_external` are eligible. External is a storage provider distinction, not a different ownership model, and no separate external-local trust capability is configured. An eligible document without a Mermaid fence loads no browser asset.

Runtime authority is `docs-viewer/runtime/js/management/docs-viewer-inline-mermaid.js`, composed only by the manage entrypoint and invoked through the ordinary document controller. The checked browser asset is owned beside other local runtime code at `docs-viewer/runtime/vendor/mermaid/11.16.0/`; the Docs Viewer service preserves its stable browser URL. Neither file lives in the public site tree or public runtime manifest. Shared content CSS owns the responsive neutral host and failure treatment.

## Reader Detail View

Supported rendered diagrams receive one overlaid **Open diagram in new tab** link without making the diagram body clickable. A configured persistent SVG image opens its existing stable served URL. A projected public Mermaid image opens the active light or dark variant and updates that target with public-site theme changes. A successfully rendered managed-local inline Mermaid host opens a temporary standalone `image/svg+xml` Blob created from its direct SVG child, preserving the diagram's `viewBox`, visible presentation, title, and description.

The shared detail adapter accepts only a generated persistent or themed marker, or the inline adapter's successful host. Ordinary images, arbitrary page SVG, failed Mermaid fences, reports, source mode, review/detail surfaces, and exports do not gain the control. Public readers can decorate marked persistent SVG without loading the manage-owned inline Mermaid adapter or browser runtime.

Temporary inline resources are revoked before ordinary document content is replaced. The control remains a native labelled `_blank` link with `noopener`, visible keyboard focus, and stable top-right overlay geometry at narrow widths and in both themes.
