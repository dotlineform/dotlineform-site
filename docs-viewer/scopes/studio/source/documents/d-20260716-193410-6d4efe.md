---
doc_id: d-20260716-193410-6d4efe
title: Copy And Move Documents Between Scopes
added_date: "2026-07-16 19:34:10"
last_updated: "2026-07-24 11:09:17"
ui_status: in-progress
summary: Identity-preserving Copy and Move workflows between scopes.
parent_id: d-20260714-234030-1e0af6
viewable: true
---
# Copy And Move Documents Between Scopes

## Current Copy Contract

- The action starts from exactly one document and includes every descendant automatically. It does not depend on checkbox multiple selection.
- The target may be any other configured local or external-local Docs Viewer scope whose canonical source root is available for managed writes. Public scopes are valid sources but are never offered or accepted as targets.
- Every copied document receives a new immutable `doc_id`, matching filename, and one captured copy-time `added_date` and `last_updated`. Repeating the copy therefore creates another independent subtree without overwrite choices.
- The copied parent is placed at the target scope root. Descendant `parent_id` values are remapped through the operation's old-id to new-id map.
- Canonical body and front matter are preserved except for `doc_id`, `added_date`, `last_updated`, `parent_id`, and `viewable`. Viewability follows the non-public target scope's ordinary create default.
- Docs Viewer links between copied documents are remapped to the target scope and new ids. Links outside the copied set, semantic references, media tokens, media URLs, downloads, and interactive assets continue to reference their original targets.
- No media or attachment bytes are copied.
- The source scope is read-only for the operation. No `copied_from` field, synchronization marker, merge path, or permanent source-to-copy relationship is added.

The action uses a write-free preview before confirmation. The preview names the source root, descendant count, target scope, and target root placement. Apply revalidates every planned identity and target path, never overwrites an existing document, writes the complete target set, and rebuilds only the target scope.

## Extension

[Copy And Move Documents Between Scopes Delivery](/docs/?scope=studio&doc=d-20260723-223400-c7a1e4) replaces the singular UI with an Index-owned checkbox **Actions** mode and menu containing **Copy to scope…** and **Move to scope…**:

- Copy uses exact checked documents by default and offers optional descendants.
- Move forces descendants for every checked parent, preserves document identities, and removes sources only after the target succeeds.
- Both deduplicate overlapping parent/child selections and place effective roots at the target scope root.
- Both transfer the effective documents' referenced source-scope media through the configured provider boundary. Copy retains all source media; Move removes only transferred media no longer referenced by documents left in the source.
- Inline Mermaid remains inline for a local target and becomes target-owned canonical Mermaid source plus published SVG media for an external-local target.

This extension is documented but not implemented. The shipped singular Copy contract above remains current until the delivery cutover is verified.

## Durable Home

[Create And Import Endpoints](/docs/?scope=studio&doc=d-20260607-222033-78a47c) owns the shipped server workflow. [Document Identity](/docs/?scope=studio&doc=d-20260715-094411-2b6e65) owns the rule that a duplicate receives a new identity. The existing control-surface and action owners remain authoritative for placement and target resolution.
