---
doc_id: d-20260813-171341-8bfa80
title: "DSI-1 — Docs Viewer Search Index v2 Delivery"
added_date: "2026-08-13 17:13:41"
last_updated: "2026-08-14 15:20:41"
summary: completed all-scope cutover to one field-aware inverted index and one browser query path
ui_status: done
parent_id: d-20260715-092247-6bbb5e
---
# DSI-1 — Docs Viewer Search Index v2 Delivery

## Status

DSI-1 is **complete and closed**. Every configured runtime and generated search index uses v2, the reviewed compatibility residue is removed, durable current-state ownership has transferred, and no full-text scope extension started during closeout.

## Complete Result

Every configured Docs Viewer scope uses one versioned inverted-index structure and one browser query and ranking path. Each scope still controls which source fields are searchable, but index structure is not a scope-specific choice. The runtime retains no v1 reader, format fallback, or Studio-only engine.

With DSI-1 complete, [SM-1 — Studio Full-Text Search Delivery](/docs/?scope=studio&doc=d-20260715-084611-c2fa6f) is eligible for its read-only readiness step. Its proposed headings, body prose, and authored code remain a separate Studio field-policy delivery.

## Boundary

DSI-1 includes the common document table and field-aware postings shape, shared tokenizer contract, per-scope field-policy seam, whole-index search rebuild behavior, one browser query/ranking path, coordinated migration of every configured Docs Viewer search index, and the metadata-only Analysis public projection.

It preserves each scope's accepted retrieval fields during the structural migration. It does not add body-text retrieval to any scope, change Catalogue Search, retain a dual-format transition reader, introduce an incremental postings cache, or redesign search results.

[Studio Full-Text Search Index](/docs/?scope=studio&doc=d-20260813-161942-b1e9e1) explains the inverted structure. [Docs Viewer Search Tokenizer](/docs/?scope=studio&doc=d-20260813-163802-5ca41f) owns term semantics. The later [All-Scope Full-Text Search Baseline](/docs/?scope=studio&doc=d-20260813-165124-16b0ab) now owns field-policy alignment beyond this metadata-only schema migration.

## Delivery Steps

The verification tables are initial ceilings, not quotas. Confirm or reduce the relevant table after inspecting that step's exact owners; expand it only when implementation reveals a new credible risk and the step is updated before proceeding.

### DSI-1.0 — Readiness And Sequence

- [x] Confirm the current Docs Viewer builder, browser query path, scope-policy owner, targeted-search orchestration, configured scope set, and Analysis public output at broad-owner level.
- [x] Confirm each scope's accepted searchable fields as the migration baseline without freezing final code, fixtures, schema names, or file inventory.
- [x] Confirm that every currently served scope can cut over together without a retained v1 reader or unavailable external-scope gap.
- [x] Confirm DSI-1.1 can implement the shared transformation/query contract without changing active generated output, and DSI-1.2 can perform the coordinated producer/consumer cutover.
- [x] Record only genuine ownership, migration, public-projection, or sequencing stop conditions, then stop for implementation approval.

No executable verification is budgeted for this read-only step. Source/config/document inspection is sufficient; prototypes, generated deltas, test changes, and a recognisable production diff belong to DSI-1.1 or DSI-1.2.

**Gate:** Review the readiness result and linear sequence before any production or test implementation begins.

**Record:** Completed 2026-08-13. `build_search.py` is the single producer and currently hard-codes one v1 metadata policy for every scope; `docs_scopes.json` and `docs_scope_config.py` own scope identity, membership exclusions, routes, and output locations but have no explicit search-field policy. `docs-viewer-search.js` is the single browser normalization/ranking owner and `docs-viewer-search-controller.js` is its shared loader and UI caller. `docs_write_rebuild.py` and `docs_live_rebuild_watcher.py` own targeted search execution, while mutation plans supply affected identities.

The configured set is Studio, Analysis, Notes, Processing, and dotlineform. All five source roots and working search indexes were available during readiness, including both external-local roots. All five currently accept the same retrieval fields: title, parent title, update metadata, and exact full document identity; identity fragments are not ordinary terms. The proposed minimal stop-word list remains suitable for representative metadata across the five scopes, and lifecycle-bearing words such as `before`, `after`, and `while` remain indexed.

Analysis working search is copied unchanged to `site/assets/data/search/analysis/index.json`. The public publish gate is format-agnostic, but `docs_document_location_projection.py` and `docs_public_delete_cleanup.py` currently read or filter `entries`; DSI-1.2 must move those identity-only consumers to the v2 document table and remap postings during public deletion rather than retain `entries` compatibility.

The linear sequence remains valid. DSI-1.1 can add the explicit scope policy plus pure Python transformation/tokenizer and pure JavaScript tokenizer/query contract behind the inactive v1 producer/consumer, using one shared fixture. The expected review shape is roughly 250-350 production/config lines and 200-300 focused fixture/test lines across the existing scope-config, builder, and shared runtime owners; material expansion beyond that shape requires another review. DSI-1.2 then switches the producer, browser loader/query caller, expected schema, whole-index orchestration, Analysis auxiliary consumers, all five working indexes, and the tracked Analysis public index together, with no v1 reader.

Two sequencing conditions are explicit. Exact full-ID lookup is retained as structural identity handling outside ordinary tokenizer terms despite the broader full-text concept's statement that `doc_id` is not searchable. Both external-local roots must still be available at DSI-1.2; otherwise the all-scope cutover stops rather than leaving a v1 scope or adding a fallback reader. Gate outcome: ready for DSI-1.1 implementation approval; no production code, tests, prototypes, or v2 outputs were changed or run. The routine docs watcher regenerated only this delivery record's existing Studio document and v1 search projections.

### DSI-1.1 — Shared V2 Transformation And Query Contract

- [x] Add one explicit Docs Viewer search-field policy seam that can preserve each configured scope's accepted retrieval fields.
- [x] Implement the shared tokenizer contract and deterministic document-table/field-postings transformation.
- [x] Implement one pure postings-based browser query, ranking, and tie-breaking path against the v2 structure.
- [x] Make the v2 search builder treat a search-relevant document change as a whole-index operation while preserving skip-on-identical-output behavior.
- [x] Keep the active v1 producer/consumer seam unchanged until the coordinated DSI-1.2 cutover.

| contract or credible risk | lowest adequate evidence | permanent test change? | why this evidence is needed |
| --- | --- | --- | --- |
| build-time and query-time token normalization diverge | one shared representative fixture exercised by focused Python and direct JavaScript module assertions | extend focused checks; no browser test | catches terms that can be emitted but never queried, or queried but never emitted |
| document positions, field postings, intersection, rank, and tie-breaking are nondeterministic | focused pure builder and query assertions over a small multi-document corpus | extend the current builder test and the smallest available pure runtime check | proves the common data and ranking contract without route choreography |
| a targeted v2 request patches postings or leaves stale removal state | focused builder behavior covering edit, create, delete, and explicit whole-index fallback | extend the current search-builder test | protects the global postings invariant without an incremental-cache suite |

**Gate:** The shared v2 owners and focused evidence are reviewable, but no configured runtime or generated index has cut over. Stop before DSI-1.2.

**Record:** Completed 2026-08-13. `docs_scope_config.py` now normalizes one flat `search_fields` tuple and all five configured scopes explicitly retain `title`, `parent_title`, `identity`, and `last_updated`. `build_search.py` contains inactive v2 tokenizer, deterministic document-table/postings transformation, and whole-index build methods. `docs-viewer-search.js` contains the matching inactive browser tokenizer, postings intersection, ranking, and deterministic tie-breaking exports. One shared JSON fixture drives focused Python and direct JavaScript assertions.

The first implementation draft reached 500 production/config lines and was paused at the size-review condition. On user review it was reduced to 311 production/config insertions and 215 focused fixture/test lines: no inactive loader or validation framework, no generic policy registry, and no second query owner. Exact identity and update metadata remain explicit fields outside ordinary tokenizer terms. A v2 request carrying changed document IDs always reconstructs the complete index; edit, create, delete, deterministic ordering, and identical-version skipping are covered without adding an incremental cache.

Focused evidence passed: `pytest -q docs-viewer/tests/python/test_docs_scope_config.py docs-viewer/tests/python/test_build_search_python.py` reported 59 passed; `node --experimental-default-type=module docs-viewer/tests/js/docs_viewer_search_v2_contract.mjs` reported the JavaScript contract OK; explicit Python and JavaScript lint passed; Python `py_compile` passed; all five configured policies loaded through an isolated writable Projects base; and `git diff --check` passed. A direct all-scope read against the real external workspace was unavailable under the sandbox's write-capability check, so DSI-1.2 retains its readiness requirement to confirm both external-local roots before cutover.

The active CLI still calls the v1 producer and `docs-viewer-search-controller.js` still calls the v1 runtime functions. No watcher/write-service orchestration, public projection consumer, browser controller, generated search index, or Catalogue Search owner changed. Gate outcome: DSI-1.1 is ready for review; stop before DSI-1.2.

### DSI-1.2 — Atomic Runtime And All-Scope Cutover

- [x] Wire the shared v2 transformation into the Docs Viewer search builder and the single browser query path.
- [x] Update expected schemas and scope policy without hard-coded Studio or Analysis query branches.
- [x] Convert existing targeted search orchestration to an explicit whole-index v2 search rebuild while leaving safe document-payload builds targeted.
- [x] Regenerate every configured Docs Viewer search index and the tracked metadata-only Analysis public projection in the same cutover.
- [x] Remove the v1 reader and obsolete `entries`, `search_terms`, and `search_text` production handling rather than retaining compatibility code.
- [x] Confirm Catalogue Search production and generated files remain outside the change.

| contract or credible risk | lowest adequate evidence | permanent test change? | why this evidence is needed |
| --- | --- | --- | --- |
| configured scopes emit different structures or lose approved metadata retrieval | focused builder/config tests plus exact JSON/schema assertions for the regenerated scope indexes | update existing builder/config contracts | proves one structure and preserves scope-owned field policy |
| watcher or write-service orchestration rebuilds too little or rebuilds every document payload | focused existing orchestration tests for targeted docs plus whole v2 search | update existing watcher/write-service checks only where behavior changes | protects the intended split between targeted document output and global search output |
| Analysis publishes excluded identities or new body vocabulary | exact public-projection comparison and the narrowest existing publication-boundary assertion | extend an existing projection contract only if it owns this payload | protects the public corpus boundary introduced by the schema migration |
| shared runtime cannot load and query v2 on local and public routes | direct module/payload integration plus one temporary manual check of `/docs/` and `/analysis/` | no permanent browser test unless a browser-only failure is discovered and separately justified | proves the producer/consumer agreement without adding UI choreography coverage |
| Catalogue Search is changed by the Docs Viewer migration | bounded path/diff inspection | none | ownership isolation is visible directly and needs no synthetic test |

**Gate:** All configured Docs Viewer indexes and consumers use v2, focused evidence passes, and no v1 reader remains. Stop for the explicit code-review step.

**Record:** Completed 2026-08-13. `build_search.py` now emits only `docs_viewer_search_index_v2`; the shared browser controller loads that schema directly and calls the sole postings query path. The old entries normalizer, v1 scoring, targeted search CLI, and `search_terms`/`search_text` production handling were removed. Browser config declares the common schema plus `rebuild_policy: whole_index` for every scope, while watcher and write-service flows still select document payloads narrowly and run one complete search build when affected identities are non-empty.

Studio, Analysis, Notes, Processing, and dotlineform were rebuilt together after both external-local roots passed live preflight. Their `(documents, terms, bytes)` measurements were respectively `(236, 630, 140666)`, `(17, 66, 10538)`, `(244, 833, 174908)`, `(137, 486, 87038)`, and `(9, 33, 4887)`. Analysis working search was copied byte-for-byte to the tracked public index; its identity-only location projection rebuilt to 266 records. The only added cutover complexity was remapping v2 postings positions when immediate public Delete removes an Analysis document.

Focused evidence passed: 185 Python checks across the builder/config, orchestration, Analysis projection/Delete/Publish, generated-read, move/transfer, and generated-contract owners; the shared direct JavaScript contract; focused Python and JavaScript lint; and an actual-payload module check for Studio and Analysis. Temporary local route checks returned `DSI-1 — Docs Viewer Search Index v2 Delivery` for `/docs/?scope=studio&q=search+index` and `Analysis` plus `Concepts` for `/analysis/?q=analysis`, with no console errors. The five generated search-policy records matched the builder exactly, Analysis working/public indexes matched byte-for-byte, and bounded diff inspection found no Catalogue production or generated path. Gate outcome: DSI-1.2 is complete and stopped for the explicit DSI-1.3 code review.

### DSI-1.3 — Code Review

- [x] Review the bounded production, config, test, documentation, and generated diff independently after DSI-1.2 evidence.
- [x] Check specifically for a hidden second query path, scope-name branching, duplicated tokenizer rules, compatibility residue, stale targeted-postings behavior, public Analysis leakage, and Catalogue ownership drift.
- [x] Review the test diff for duplicated cases, unnecessary fixtures or frameworks, browser choreography, broad-profile expansion, and checks that cannot name a unique regression.
- [x] Record every finding and its resolution or explicitly accepted omission.
- [x] Rerun only the verification-budget rows affected by review changes, then stop for review approval.

No additional evidence category is budgeted here. A review finding may require updating the affected implementation step's table before new tests or broader checks are added.

**Gate:** Present the actual code-review findings, resolutions, final bounded diff, and remaining risks before closeout.

**Record:** Completed 2026-08-13. The bounded review found two cleanup issues and no unresolved defect.

- **P2 — resolved:** the watcher still named its affected-id threshold, CLI flag, environment key, and operational guidance as targeted search after search had become whole-index. The threshold now explicitly owns only targeted document payloads (`--targeted-docs-threshold` and `DOCS_WATCH_TARGETED_DOCS_THRESHOLD`), search continues to rebuild whole when affected identities are non-empty, and the six operational documents that stated the retired targeted-search contract now describe the shipped split. No compatibility alias was retained and the current local environment has no old override.
- **P3 — resolved:** `build_search.py` retained a test-only changed-ID argument and diagnostic tuple, unused JSON and record fields, and v1 hierarchy ordering that the deterministic v2 builder immediately replaced by identity order. The browser scorer also retained unreachable identity-prefix and identity-field branches. Those paths were deleted, and the existing whole-index test now exercises only the active builder API while retaining edit, create, delete, deterministic output, and identical-version skip coverage.
- **No finding:** there is one browser query entrypoint and no Studio/Analysis query branch. Python and JavaScript necessarily express the same small normalization at build time and query time; the existing shared fixture remains the sole parity evidence, with no new registry, adapter, framework, or browser choreography. The test diff adds no duplicated suite, broad profile, or permanent browser check.
- **No finding:** Analysis working and public indexes remain byte-identical, expose only `title`, `parent_title`, `identity`, and `last_updated`, contain 17 documents, and expose no body/code/heading field. The final bounded path scan contains no Catalogue production or generated file.

Affected verification only: `test_build_search_python.py` reported 10 passed; `test_docs_live_rebuild_watcher.py` reported 22 passed; the direct JavaScript v2 contract passed; focused Python and JavaScript lint passed; Python compilation passed; the regenerated Studio index completed and an immediate repeat skipped on identical version; Analysis working/public bytes and metadata-only fields were checked directly; and `git diff --check` passed. Net review cleanup removes more code than it adds and introduces no new test asset. Gate outcome: DSI-1.3 is complete and parked for review approval; DSI-1.4 closeout has not begun.

### DSI-1.4 — Closeout And Durable Transfer

- [x] Confirm the complete result and every completion claim against the accepted implementation and review records.
- [x] Update [Docs Viewer Search](/docs/?scope=studio&doc=d-20260602-160839-6d3cbb) with the shipped v2 schema, field policy, build behavior, and browser query path.
- [x] Update [Search](/docs/?scope=studio&doc=d-20260331-000000-5dcf32) only if its Docs Viewer/Catalogue authority map changed materially.
- [x] Mark DSI-1 complete, make SM-1 eligible for readiness, and record remaining Analysis full-text and other scope-field decisions as separate outcomes.
- [x] Present the retain-or-retire recommendation for temporary search concept and delivery documents without deleting them.

No implementation retest is automatic at closeout. Run only documentation projection checks made necessary by the closeout edits.

**Gate:** Stop for explicit closeout approval.

**Record:** Completed and closed 2026-08-13 after explicit user approval. The accepted implementation and DSI-1.2/DSI-1.3 records confirm one `docs_viewer_search_index_v2` producer, one browser query path, explicit per-scope fields, whole-index search rebuilding, metadata-only output for all five configured scopes, byte-identical Analysis working/public projection, no v1 reader, and no Catalogue Search change.

[Docs Viewer Search](/docs/?scope=studio&doc=d-20260602-160839-6d3cbb) now owns the shipped schema, current metadata fields, tokenizer boundary, membership, whole-index build/publication behavior, browser query and rank path, extension rule, and known configuration/payload weak spots. The general [Search](/docs/?scope=studio&doc=d-20260331-000000-5dcf32) authority map remains unchanged because DSI-1 changed neither the Docs/Catalogue domain split nor the named builder and runtime owners. [Search Mechanics](/docs/?scope=studio&doc=d-20260715-092247-6bbb5e) records DSI-1 as closed and SM-1 as eligible for readiness.

At DSI-1 closeout, SM-1 remained proposed and Analysis full-text adoption was still treated as a separate public outcome while Notes, Processing, and Dotlineform remained metadata-only. That later proposal has since been simplified to the [All-Scope Full-Text Search Baseline](/docs/?scope=studio&doc=d-20260813-165124-16b0ab).

Retention recommendation at DSI-1 closeout: keep [Docs Viewer Search](/docs/?scope=studio&doc=d-20260602-160839-6d3cbb) as the permanent current-state owner and retain this completed DSI-1 record as bounded delivery evidence. Retain [Studio Full-Text Search Index](/docs/?scope=studio&doc=d-20260813-161942-b1e9e1) and [Docs Viewer Search Tokenizer](/docs/?scope=studio&doc=d-20260813-163802-5ca41f) through SM-1 readiness because SM-1 still consumes their examples and source boundary, then reconsider them at SM-1 closeout. Nothing was deleted or retired at DSI-1 closeout.

Closeout-only evidence: the four affected durable/status documents completed a targeted Studio docs projection with registered media builds skipped, reporting 236 emitted documents, zero writes on the already-current watcher projection, and zero warnings; the Studio search builder immediately skipped its already-current identical version. Changed generated JSON parsed successfully and `git diff --check` passed. No implementation test or browser profile was rerun. Gate outcome: DSI-1 is complete and closed; stop without beginning SM-1.0.
