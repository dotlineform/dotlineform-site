---
doc_id: d-20260714-234030-19a064
title: External Asset Collections Architecture
added_date: 2026-07-14 23:40:30
last_updated: "2026-07-17 22:19:08"
ui_status: proposed
summary: Proposed ownership, execution, extension, and safety boundaries for External Asset Collections.
parent_id: d-20260714-194217-b2bdd7
viewable: true
---
# External Asset Collections Architecture

## Architectural Outcome

External Asset Collections should extend the existing scope lifecycle, capabilities, local-service, and report boundaries without creating a second document system or a general asset application.

This document records the proposed structure and its known pressure points. Exact current behavior remains authoritative in the linked code and focused tests.

## Ownership And Storage

Ordinary scope-owned media is not part of this feature. Each media type has a registered published location using a repository, external-local, or R2 provider; its path and lifecycle follow that scope configuration.

An External Asset Collection instead uses an independently named root outside `source/<scope>/`. A possible confined convention is:

```text
$DOTLINEFORM_PROJECTS_BASE_DIR/docs-viewer/media/<collection-name>/
```

`<collection-name>` is collection identity, not a scope id. The eventual association should record a marker-relative collection root and report document id without projecting an absolute path to the browser.

- Docs Viewer associates and inventories the collection but does not own its files.
- Scope create does not create the collection root.
- Scope rename may update the association record but never moves the collection.
- Scope delete removes Docs Viewer-owned association/report state but never deletes the collection.
- The same collection identity could later be associated with another eligible local scope without a filesystem relocation.

An ordinary media type using an `external_local` location remains scope-owned published media. That provider does not identify, enable, or confer ownership over an External Asset Collection.

## Execution Path

```text
collection association
  -> management capability projection
  -> report metadata selects scope_assets
  -> local report loader requests confined inventory
  -> local service derives the root and scans metadata
  -> report renders relative paths and reference state
```

The initial scan should be synchronous and metadata-only. It preserves nested relative paths, avoids symlink following, skips minimal platform noise, and reports partial warnings without making unreadable entries fatal.

One report-backed source document exposes the inventory. Asset-specific loading, state, and rendering remain in a focused report owner rather than the shared document controller.

## Local File Boundary

Nested collection links use a separate loopback-service route from the existing confined `/docs/media/<scope>/<type>/<filename>` published-media route.

The service must:

- require a configured local scope with the capability enabled
- derive and confine the root on the server
- reject absolute paths, traversal, malformed encoding, and symlink escape
- expose relative paths only
- apply `nosniff` and a narrow inline MIME policy
- force executable, interactive, unknown, and ordinary HTML content to download

Public route shells and browser config receive no index, local root, or service URL.

## Extension Method

Use existing owners:

- scope or focused collection config — association, marker-relative collection root, and report document id
- scope lifecycle and manifest — remove only Docs Viewer-owned association/report state
- management capabilities — service support and per-scope enabled state
- report metadata registry — selection of the `scope_assets` presentation
- focused local service — confined inventory and nested file reads

Filesystem inventory is runtime data, not another registry. Do not add a plugin, action, or general asset registry framework.

## Current Code Pressure Points

- No current config owner represents a collection identity and marker-relative root independently of scope id.
- `docs-viewer/services/docs_scope_manifest.py` models Docs Viewer lifecycle-owned file records; a collection root must not be inserted into that deletable set.
- Scope delete and rename need an explicit association consequence without treating the collection as a filesystem move/delete target.
- The existing `/docs/media/` contract resolves one configured media type and safe relative identity within that type's local published root.
- Project State has a proven metadata traversal, but its walker is mixed with catalogue-specific filtering and cannot be reused directly.

These are inspection starting points, not an exhaustive implementation list. A delivery should update only the owners required for its outcome.

## Known Weak Spots

- The proposed `media/` collection base could still be confused with scope-owned `media/` folders unless configuration and UI consistently use “collection” terminology.
- Association removal and collection deletion are deliberately different actions; Docs Viewer initially owns only the association.
- Large-tree responsiveness is unknown. Measure Processing and I Ching before adding caches, jobs, hashes, or previews.
- Reference correlation may initially understand only current Docs media and asset routes; any compact authoring token is a separate design decision.

## Delivery Boundary

The feature parent owns its delivery boundaries. The [Docs Viewer Roadmap](/docs/?scope=studio&doc=d-20260714-234030-1e0af6) records only suggested feature order. Each delivery must complete one improvement to this architecture and transfer shipped behavior to the named durable owner.
