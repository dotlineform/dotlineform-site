---
doc_id: d-20260727-224141-d28e4c
title: Tag Document Bootstrap Delivery
added_date: "2026-07-27 22:41:41"
last_updated: "2026-07-27 22:41:41"
ui_status: proposed
summary: generate and link one Analysis tag document for every current canonical tag, rebuild the complete collection, and stop for management review
parent_id: d-20260726-211842-cc8f6c
---
# Tag Document Bootstrap Delivery

## Target Result

One guarded migration creates a new public document in the Analysis `tags`
sub-scope for every current canonical tag without a `doc_id`, copies the
registry description into that document once, and stores the allocated ID in
the registry row.

The current planning snapshot contains 245 flat registry tags with no
`doc_id` and three independent sub-scope documents. If that snapshot remains
the cutover input, the delivery creates 245 new linked sources and leaves the
three existing sources unchanged, producing 248 manageable documents in total.
Execution must freeze and report the actual counts again.

This is a bootstrap, not feature closeout. It makes the complete real
collection available so its navigation, discovery, metadata, and source-editing
needs can be reviewed before follow-on work is chosen.

## Frozen Output Contract

For each input registry row, the migration allocates one immutable document ID
and creates:

```markdown
---
doc_id: <allocated immutable document ID>
title: "<tag_id>"
added_date: <cutover timestamp>
last_updated: <cutover date>
parent_id: ""
viewable: true
---
# <tag_id>

**<group>**

<tag registry description>
```

The corresponding registry row gains that exact `doc_id`. The migration
preserves tag order, `tag_id`, scalar `group`, `description`, row timestamps,
policy, aliases, assignments, and the three existing tag documents.

The registry schema advances only far enough to retain and validate an optional
unique `doc_id` during the feature transition. The bootstrap populates it for
every frozen input row; making it required for later New Tag and Edit Tag
writes belongs to follow-on delivery work.

## In Scope

- Add one repository-owned preview/write/validate command for this one-off
  bootstrap.
- Require the current flat registry schema, unique tag IDs, one allowed scalar
  group per tag, a description string, and no pre-existing registry `doc_id`
  values.
- Inventory every existing Analysis `tags` source before allocating IDs.
- Produce an inspectable preview containing the exact tag-to-document mapping,
  target paths, source count, registry projection, and input fingerprints.
- Allocate immutable IDs without matching documents by title, group, or
  content.
- Create every new Markdown source with exclusive-create semantics before
  atomically replacing the registry.
- Add and validate unique registry `doc_id` values while preserving every
  pre-existing tag value and all alias and assignment sources.
- Rebuild the Analysis documents, sub-scope manifest, and by-ID payloads;
  reload the local management inventory; and confirm the sub-scope documents
  remain excluded from parent-scope search.
- Reconcile the complete registry-to-document relationship and inspect the
  resulting collection through the delivered sub-scope management workflow.

## Not In Scope

- Reusing, rewriting, moving, linking, or deleting the three existing tag
  documents.
- Changing New Tag to create a document.
- Adding `doc_id` to Edit Tag or implementing stale-link repair.
- Synchronizing registry descriptions with document content in either
  direction.
- Changing tag deletion or document deletion behavior.
- Adding group data, filtering, styling, or management fields to the public
  sub-scope manifest.
- Redesigning the report, metadata editor, source editor, or management
  toolbar.
- Creating Tag semantic-token targets.
- Editing the generated prose beyond the fixed one-time seed template.

Any discovery, filtering, bulk action, repair, or authoring need revealed by
the real collection becomes separate follow-on work.

## Delivery Steps

1. [ ] **Freeze the input.** Record the registry version and hash, exact tag
   count and order, zero current registry `doc_id` values, allowed groups, alias
   and assignment hashes, and the exact existing Analysis `tags` source
   inventory.
2. [ ] **Build the guarded preview.** Project the complete immutable ID map,
   Markdown sources, registry rows, target paths, and reconciliation counts
   without writing. A preview is invalidated by any registry or source-inventory
   change.
3. [ ] **Review the exact cutover.** Confirm every tag appears once, every
   allocated ID and path is unique, every source matches the fixed template,
   the three existing sources are untouched, and only `doc_id` plus the
   registry-level schema metadata change in canonical tag data.
4. [ ] **Apply once.** Create only the previewed new source paths, replace the
   registry atomically after all source writes succeed, and report any
   run-created unreferenced files precisely if the registry commit cannot
   complete.
5. [ ] **Rebuild and reconcile.** Build the Analysis document payloads, reload
   the management inventory, and confirm parent-scope search still excludes
   sub-scope documents. Prove every registry `doc_id` is unique and resolves to
   its exact generated source and public detail URL. Prove aliases,
   assignments, descriptions, and the three pre-existing documents are
   unchanged.
6. [ ] **Review management and stop.** Use the local managed `tags` report to
   navigate the complete inventory and sample detail, metadata, source,
   Back/Forward, and narrow-layout behavior across all four groups. Record
   concrete friction and follow-on candidates without implementing them in
   this delivery.

## Likely Authority

- `studio/data/canonical/tags/tag-registry.json`
- a focused `studio/commands/` bootstrap command
- a focused `studio/services/tags/` projection and validation module
- `studio/services/tags/tag_source_model.py`
- `studio/services/tags/tag_write_transactions.py`
- `docs-viewer/services/docs_document_identity.py`
- `docs-viewer/scopes/analysis/source/sub-scopes/tags/documents/`
- `docs-viewer/build/build_docs.py`
- `docs-viewer/build/docs_builder/sub_scope.py`
- `docs-viewer/build/build_search.py`
- focused bootstrap projection, command, and reconciliation tests

These are inspection starting points. The command owns the one-off
cross-application composition; ordinary Tag Registry and Docs Viewer UI paths
do not become migration hosts.

## Completion And Evidence

- The reviewed preview and applied output report the same registry fingerprint,
  tag order, tag-to-document mapping, target paths, and counts.
- Exactly one new immutable document exists for every frozen input tag, and
  every frozen input registry row contains its unique exact `doc_id`.
- Every linked ID resolves in the Analysis `tags` sub-scope and derives a
  workable `/docs/?scope=analysis&doc=d-20260624-213316-478639&subdoc=<doc_id>`
  URL.
- The three pre-existing sources are byte-identical, remain independent, and
  are included alongside the generated documents in the management inventory.
- Registry descriptions remain present and unchanged after seeding. Aliases and
  assignments are byte-identical.
- Focused projection/command tests, the Analysis document build, explicit
  parent-search exclusion checks, canonical JSON validation,
  `git diff --check`, and exact source/payload counts support only the
  bootstrap claims above.
- Manual review records how the complete collection behaves and ends at a
  decision gate for separate follow-on delivery documents.

## Durable Destinations

After the bootstrap is accepted, record the transitional registry field and
one-time generation result in
[Tag Registry](/docs/?scope=studio&doc=d-20260331-000000-4519a5) and
[Tag Source Data](/docs/?scope=studio&doc=d-20260419-000000-86255c).

Any accepted management change belongs to
[Manage Sub-Scope Documents](/docs/?scope=studio&doc=d-20260726-224323-eb0a65)
or a new focused successor, not to this bootstrap delivery.
