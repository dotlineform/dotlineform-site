---
doc_id: d-20260728-205013-41deb8
title: Sub-Scope Public And Manage Manifests
added_date: "2026-07-28 20:50:13"
last_updated: "2026-07-28 22:41:36"
summary: define sibling public-safe and Manage sub-scope manifests, document-owned grouping, and the Tags-to-Concepts presentation boundary
ui_status: proposed
parent_id: d-20260424-000000-50b63f
viewable: true
---
# Sub-Scope Public And Manage Manifests

## Purpose

A configured Docs Viewer sub-scope should expose two inspectable list
projections built from the same document-source snapshot:

- a minimal public-safe manifest; and
- a richer local Manage manifest.

Public and Manage retain different data and configuration boundaries while the
shared `docs_subscope` report uses the same manifest-loading mechanism.

## Tags And Concepts

The stable sub-scope and registry identity is `tags`. Manage presents the
collection as **Tags**; public browser configuration presents the same
document collection as **Concepts**. Paths, manifests, report parameters, and
service targets retain `sub_scope: tags`.

`tag` also remains the semantic-token kind. Resolution continues through the
Tag Registry tag and its `work_id`, `series_id`, and `doc_id` relationships.
**Concept** describes the additional context supplied by a linked document; it
is not another semantic token, registry, identifier, resolver, or relationship
model.

Tag Editor may create and initially seed a linked document when it creates a
tag. Once created, the document is canonical Docs Viewer content and may
diverge from its originating tag. A standalone sub-scope document may likewise
be presented publicly as a concept without acquiring a Tag Registry identity.

## Projection Flow

```text
source Markdown
  + optional document-owned group
  |
  +-- sub-scope build
        +-- manifest.json
        +-- manage-manifest.json
        `-- local by-ID payloads
                 |
                 `-- Publish copies only public-safe artifacts to site/

public list/detail: published manifest.json + public by-ID payloads
manage list/detail: manage-manifest.json + local by-ID payloads
```

The current Manage runtime uses the source-backed
`GET /docs/sub-scope-documents` inventory instead of its configured local
manifest. [Sub-Scope Report List Filter Delivery](/docs/?scope=studio&doc=d-20260728-141505-277932)
owns the proposed cutover to the generated Manage manifest. Until that delivery
ships, the inventory remains current code authority.

## Public-Safe Manifest

The sub-scope builder writes:

```text
docs-viewer/scopes/<scope>/published/documents/sub-scopes/<sub-scope>/manifest.json
```

It contains only viewable list records:

```json
{"doc_id": "<id>", "title": "<title>"}
```

For a public scope, Publish copies the configured sub-scope artifacts to:

```text
site/assets/data/docs/scopes/<scope>/<sub-scope>/manifest.json
```

Publication continues to copy this manifest unchanged. Management status,
viewability, document groups, source paths, and full source metadata do not
enter the public manifest.

## Manage Manifest

The same sub-scope build writes:

```text
docs-viewer/scopes/<scope>/published/documents/sub-scopes/<sub-scope>/manage-manifest.json
```

The Manage manifest contains every source document, including non-viewable
records:

```json
{
  "groups": ["subject", "domain", "form", "theme"],
  "docs": [
    {
      "doc_id": "<id>",
      "title": "<title>",
      "ui_status": "<status>",
      "viewable": true,
      "group": "subject"
    }
  ]
}
```

`groups` comes from the owning Docs Viewer sub-scope configuration and preserves
its allowed display order. `group` is optional on each document. Counts remain
a browser projection over the returned records.

The publication gate must explicitly exclude `manage-manifest.json`. Public
browser config points to the published public manifest; Manage browser config
points to the local Manage manifest.

## Document Group Ownership

For `{ scope: analysis, sub_scope: tags }`, `group` is an optional scalar
document front-matter field:

```yaml
group: subject
```

Docs Viewer owns this field:

- the sub-scope configuration owns its allowed values and order;
- **Edit metadata** reads, validates, writes, or removes it;
- the document builder projects it into the Manage manifest;
- an absent value leaves the document under **All tags** only; and
- the public manifest and public detail projection omit it.

`doc.group` is not an alias or live projection of `tag.group`. There are two
permitted create-only seeds:

- the migration may initialize the 245 existing generated documents from the
  current Tag Registry once; and
- creating a new tag in Tag Editor continues to create its linked Analysis Tag
  document and may initialize that new document's `group` from `tag.group`.

After the document exists, ordinary builds, reads, writes, repairs, and
validation do not compare or synchronize the two fields. A later Tag Registry
group change does not mutate or rebuild the document. The bootstrap must not
overwrite an existing document or become a repair path; subsequent document
group changes belong only to Docs Viewer.

## Freshness And Gates

Both manifests and local by-ID payloads reflect the last successful document
build. This deliberately follows the existing:

```text
source -> build -> publish -> GitHub Actions
```

Manage list and rendered detail therefore share one build snapshot. Source or
metadata edits become visible through the existing targeted build/rebuild
gate, rather than through an independent live list projection.

## Delivery Boundary

The list-filter delivery owns document-group metadata, dual-manifest
generation, the one-time seed, Manage runtime cutover, and filtering. It does
not make Tag Registry a Docs Viewer build dependency, introduce group
synchronization, or publish the Manage manifest.

Current and target code authority:

- source and metadata model:
  `docs-viewer/services/docs_source_model.py`
- metadata read/write:
  `docs-viewer/services/docs_management_document_target.py` and
  `docs-viewer/services/docs_management_mutations.py`
- manifest generation:
  `docs-viewer/build/docs_builder/sub_scope.py`
- browser config generation:
  `docs-viewer/build/docs_builder/browser_config.py`
- publication:
  `docs-viewer/services/docs_publish_gate.py`
- Manage report handoff:
  `docs-viewer/runtime/js/management/docs-viewer-management-document-reports.js`
- shared report:
  `site/docs-viewer/runtime/js/shared/docs-subscope-report.js`
