---
doc_id: d-20260727-172206-0cadb4
title: Tag Registry Label Retirement Delivery
added_date: "2026-07-27 17:22:06"
last_updated: "2026-07-27 17:22:06"
ui_status: proposed
summary: remove the redundant label field from canonical tag registry rows and make every consumer display the flat tag_id directly
parent_id: d-20260727-110004-ab0433
---
# Tag Registry Label Retirement Delivery

## Target Result

`tag_id` is both canonical identity and the displayed tag name. Canonical
registry rows no longer store a duplicate `label`.

```json
{
  "tag_id": "moss",
  "group": "subject",
  "description": "<temporary document seed content>",
  "updated_at_utc": "<timestamp>"
}
```

The current cutover input has 245 registry rows. All 245 labels are identical
to their `tag_id`, so retirement requires no naming decision, identity rewrite,
alias migration, or assignment migration.

## In Scope

- Remove `label` from every canonical registry row and bump the registry schema
  to `tag_registry_v3`.
- Preserve every `tag_id`, scalar `group`, description, row timestamp, policy,
  alias target, and assignment reference.
- Stop create, edit, promotion, and manual-patch paths from writing `label`.
- Stop validation and mutation evidence from requiring or returning a registry
  label where `tag_id` already carries the same value.
- Make Tag Registry, Tag Aliases, Series Tags, and Series Tag Editor consume and
  display `tag_id` directly.
- Update focused fixtures, planners, APIs, browser modules, validation, checks,
  and durable source documentation.

## Not In Scope

- Renaming a tag or changing any tag identity.
- Changing group membership, descriptions, aliases, assignments, weights, or
  inheritance.
- Removing generic UI or configuration fields named `label` when they do not
  represent `tag_registry.tags[*].label`.
- Adding `doc_id`, creating tag documents, or removing registry descriptions.
- Changing the group taxonomy or group-derived reporting.

## Delivery Steps

1. [ ] **Freeze and validate the input.** Require `tag_registry_v2`, exactly one
   allowed scalar group per tag, globally unique flat IDs, and `label ===
   tag_id` for every row. Record tag, alias-target, and assignment-reference
   counts before changing source.
2. [ ] **Change the canonical contract.** Add `tag_registry_v3` validation and
   a one-file projection that removes only `label`. Preserve tag order, policy,
   groups, descriptions, and row timestamps; update only the source-level
   version and cutover timestamp.
3. [ ] **Update every writer.** Remove `label` from create, edit, promotion, API
   response, activity detail, state projection, and manual-patch behavior.
   Ensure ordinary group/description edits cannot reintroduce the field.
4. [ ] **Update every reader.** Remove raw-label requirements and use `tag_id`
   for registry display, search, ordering, alias target choices, Series Tags,
   and Series Tag Editor suggestions/chips. Keep generic presentation labels
   only where they are derived values rather than canonical source.
5. [ ] **Cut over once.** Apply the validated v2-to-v3 registry projection
   atomically. Do not add a dual-read source mode or retain empty/derived label
   compatibility fields.
6. [ ] **Reconcile and close.** Prove 245 IDs, groups, descriptions, and row
   timestamps are preserved; aliases and assignments are byte-unchanged and
   still resolve; no registry row contains `label`; and no writer recreates it.
   Run focused model/planner/API tests, `studio-tag-smoke`, scoped source
   searches, and manual Registry/Aliases/Series Tags/Series Tag Editor checks.

## Likely Authority

- `studio/data/canonical/tags/tag-registry.json`
- `studio/services/tags/tag_source_model.py`
- `studio/services/tags/tag_registry_mutations.py`
- `studio/services/tags/tag_promotion_mutations.py`
- `studio/services/tags/tag_flat_identity_migration.py`
- `studio/app/server/studio/studio_tag_api/`
- `studio/app/frontend/js/studio-tag-data.js`
- `studio/app/frontend/js/tag-registry-*`
- `studio/app/frontend/js/tag-aliases-*`
- `studio/app/frontend/js/series-tags-*`
- `studio/app/frontend/js/analytics-tag-*`
- focused tag fixtures, planner/API tests, and Studio tag smokes

These are inspection starting points. The exact implementation should change
only consumers of the canonical registry label field.

## Completion Evidence

- Before/after registry counts and an exact preserved-field reconciliation.
- Confirmation that all canonical rows omit `label` and validate as
  `tag_registry_v3`.
- Confirmation that alias and assignment source bytes are unchanged.
- Focused test counts, maintained `studio-tag-smoke` results, scoped source
  search results, and manual route observations.

## Durable Destinations

Update
[Tag Source Data](/docs/?scope=studio&doc=d-20260419-000000-86255c),
[Tag Registry](/docs/?scope=studio&doc=d-20260331-000000-4519a5), and
[Series Tags](/docs/?scope=studio&doc=d-20260331-000000-974bf0) with the shipped
schema and display contract.

[Flat Tag Identity Concept](/docs/?scope=studio&doc=d-20260727-110045-a48cc4)
continues to own the identity/group model. This delivery owns only retirement
of the duplicate registry field.
