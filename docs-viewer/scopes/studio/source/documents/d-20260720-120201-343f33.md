---
doc_id: d-20260720-120201-343f33
title: Diagram Detail View Delivery
added_date: "2026-07-20 12:02:01"
last_updated: "2026-07-20 15:34:46"
ui_status: proposed
summary: Add one accessible open-in-new-tab detail control for published SVG diagrams and in-memory inline Mermaid diagrams.
parent_id: d-20260715-181845-0158f9
viewable: true
---
# Diagram Detail View Delivery

## Status And Outcome

This is an **active sibling delivery** under Diagrams. It does not need a separate concept document. DDV-0 through DDV-2 are complete; DDV-3 begins after checkpoint review.

A reader can use a small overlaid diagram-detail control to open a rendered diagram in a new browser tab for easier inspection and browser zoom. The same visible action works for both diagram forms while preserving their different storage contracts:

- a published SVG diagram opens its stable served SVG URL;
- a successfully rendered inline Mermaid diagram opens a temporary standalone SVG created from its in-memory SVG DOM.

The control is labelled **Open diagram in new tab** for assistive technology and uses an expand/open-in-new visual treatment. The diagram body itself remains available for ordinary selection and any diagram-owned links rather than becoming one large click target.

This delivery owns only the detail-view affordance. The shipped Mermaid producer remains responsible for canonical `.mmd` and published SVG, while [Inline Mermaid Rendering](/docs/?scope=studio&doc=d-20260720-102658-7de33d) remains responsible for turning a local Markdown fence into an in-memory SVG.

## Settled Placement And Visibility

The control is an HTML element outside the `<svg>` or persistent `<img>`. A shared HTML frame contains the existing diagram surface and one native new-tab link. The detail adapter may wrap or register the diagram, but it must not insert the control into Mermaid-generated SVG, include it in temporary standalone SVG serialization, or make the diagram body the trigger.

Representative ownership shape:

```html
<div class="docsViewer__diagramFrame">
  <div class="docsViewer__diagramViewport">
    <!-- existing persistent image or adapter-owned inline SVG host -->
  </div>
  <a class="docsViewer__diagramDetailControl"
     href="..."
     target="_blank"
     rel="noopener"
     aria-label="Open diagram in new tab">…</a>
</div>
```

The frame is the positioning container. The diagram viewport keeps its normal size and owns any horizontal scrolling. The compact control is absolutely positioned over the frame's top-right corner with a sufficient stacking level and its own quiet contrasting background. It remains pinned while a wide diagram scrolls underneath. Only the control's small box intercepts pointer input; the rest of the diagram remains selectable and any diagram-owned links remain operable.

The control is permanently present and never creates a toolbar row, reserved top gap, or layout shift. On hover-capable fine-pointer devices it is visible but muted at rest. It becomes fully unmuted when any of these is true:

- the diagram frame is hovered;
- the control itself is hovered;
- the control has keyboard focus, or the frame matches `:focus-within`.

The direct control-hover rule is explicit rather than relying only on frame hover. On touch or other non-hover input, the control remains fully unmuted because there is no discoverable hover state. Reveal styling changes opacity or color only; it must not toggle `display`, `visibility`, dimensions, positioning, padding, or document flow. A visible focus ring remains independent of the muted treatment, and reduced-motion preferences suppress any nonessential opacity transition.

This accepts the possibility that the compact control temporarily overlays a small amount of diagram content. That bounded overlap is preferable to permanently shrinking the diagram, leaving unexplained whitespace, or moving the document when interaction begins.

## Delivery Boundary

In scope:

- one shared rendered-diagram frame and absolutely overlaid detail-control contract;
- persistent SVG diagrams in ordinary rendered document content;
- inline Mermaid diagrams after their browser render succeeds;
- native link and keyboard behavior, a concise accessible label, focus visibility, stable overlay geometry, and responsive placement;
- stable served-URL navigation for persistent SVG;
- temporary Blob URL or equivalent standalone-SVG navigation for inline Mermaid;
- cleanup of temporary browser resources when document content is replaced;
- focused public, local, navigation-remount, accessibility, and failure checks.

Out of scope:

- an in-page lightbox, modal, pan-and-zoom canvas, custom zoom toolbar, or fullscreen viewer;
- making the whole diagram clickable or intercepting links inside a diagram;
- a layout-flow toolbar, reserved empty control gutter, or hover/focus behavior that shifts document content;
- rasterizing a diagram to WebP, PNG, or another image format;
- persisting inline Mermaid output, allocating media identities, or adding it to Scope Media inventory;
- changing Mermaid authoring, rendering, sanitization, publication, source opening, or export behavior;
- adding the control to arbitrary icons, decorative SVG, report/detail embeds, source mode, or exported static HTML without an explicit host decision.

## Likely Blast Radius

| boundary | likely owner or evidence | expected change |
| --- | --- | --- |
| Persistent diagram recognition | document payload rendering or a focused post-mount adapter, active scope media configuration | Identify eligible document SVG media explicitly; do not guess from a filename suffix or expose Mermaid producer mechanics in the UI. |
| Shared diagram host | new focused shared Docs Viewer module | Wrap or register supported diagrams, add one HTML control outside the diagram DOM, avoid duplicate decoration after navigation or repeated mounts, and clean up owned resources. |
| Published SVG navigation | rendered `<img>` source and configured served SVG location | Use the existing stable served URL directly with a new tab and `noopener`; do not fetch, copy, or reserialize persistent bytes. |
| Inline Mermaid integration | shipped inline-Mermaid adapter | Register the successfully rendered SVG with the shared host and provide a temporary standalone-SVG target without creating media. |
| Document mount | `site/docs-viewer/runtime/js/shared/docs-viewer-document-controller.js` | Invoke the detail adapter after ordinary document content mounts with explicit scope and host context. |
| Presentation | shared Docs Viewer content CSS and existing icon conventions | Pin one compact control over the frame's top-right without changing layout; mute it at rest for mouse users, unmute it on frame hover, direct control hover, or focus, and keep it unmuted for non-hover input. |
| Assurance | focused browser-module tests and manual document review | Prove eligibility, stable versus temporary targets, keyboard operation, navigation cleanup, multiple diagrams, popup behavior, and no export/runtime leakage. |

## Settled Eligibility And Host Contract

DDV-0 uses two existing source-aware boundaries and adds no media registry:

- a persistent diagram is a Markdown image whose semantic media token resolves through the active scope's configured `svg` published-media role; the content-token renderer will mark its generated `<img>` with `data-docs-viewer-diagram-kind="persistent-svg"` before the configured served URL replaces the logical token;
- an inline diagram is the shipped adapter-owned `.docsViewer__diagram[data-docs-viewer-diagram-kind="inline-mermaid"]` host after rendering succeeds.

The persistent rule is based on the configured media role, not a filename suffix, producer name, provider, or URL pattern. Ordinary `img` media, direct HTML images, inline decorative SVG, icons, and arbitrary page SVG remain unmarked. Using a scope-owned `svg` semantic image token is the explicit authoring boundary for an eligible persistent document diagram; a future need to distinguish diagram and decorative uses inside that same role must introduce a separate authored contract rather than runtime guessing.

The shared adapter will accept only those two markers inside the ordinary mounted document-content root. It will normalize either existing surface into this frame without moving the control into the image or SVG DOM:

```html
<div class="docsViewer__diagramFrame">
  <div class="docsViewer__diagramViewport">
    <!-- marked persistent <img>, or the marked inline host and its direct <svg> -->
  </div>
  <a class="docsViewer__diagramDetailControl" ...></a>
</div>
```

The frame is `position: relative` and remains the surface's block-size container. The viewport retains the current surface and owns horizontal overflow; the absolutely positioned control contributes no intrinsic width, height, margin, padding, or flow content. The control is a sibling of the viewport, so inline standalone-SVG serialization can select only the adapter-owned SVG and can never include UI chrome.

Persistent decoration is public-safe and appropriate for `local`, `local_external`, and `public` ordinary document routes: the generated image already contains the correct stable served URL, and the control uses that current URL without fetching, copying, or interpreting its provider. Inline registration remains limited to the existing successful `local` render. Reports, review/detail and embedded surfaces, source mode, and exports do not invoke the ordinary document detail adapter. A generated persistent marker may remain inert in exported HTML, but no frame, control, detail runtime, or temporary SVG resource is exported.

This contract fits the existing payload renderer, shared document controller, and shipped inline host. It does not require a general media registry, browser config expansion, or broad Markdown-output refactor, so the DDV-0 stop condition is not triggered.

## Delivery Tracker

**Current state:** DDV-0 through DDV-2 complete. Configured SVG media tokens use their stable served URLs, while successfully rendered local inline Mermaid hosts receive distinct temporary standalone-SVG targets through the same accessible detail control. Temporary resources are revoked before ordinary document content is replaced.

**Next checkpoint:** DDV-3 — integration and manual closeout across multiple diagram forms, themes, widths, input states, navigation, browser zoom, and failed-open behavior. Do not begin until DDV-2 is reviewed.

## Delivery Checkpoints

### DDV-0 — Eligibility And Host Contract

- [x] Decide the smallest explicit marker or payload/runtime contract that distinguishes supported document diagrams from ordinary images and decorative SVG.
- [x] Confirm the same host shape can contain the current published-SVG `<img>` and the shipped inline Mermaid `<svg>` without changing either source contract.
- [x] Keep the detail control as an HTML sibling outside the SVG/image surface so standalone SVG serialization never includes UI chrome.
- [x] Confirm absolute top-right placement changes no diagram or document geometry and leaves only the compact control box above diagram content.
- [x] Confirm persistent SVG detail viewing is appropriate on local, external-local, and public document routes.
- [x] Keep reports, review/detail surfaces, embedded documents, source mode, and exports excluded unless their host context is explicitly added later.
- [x] Stop if reliable persistent-diagram recognition would require a general media registry or broad Markdown-output refactor.

DDV-0 inspected the configured media projection, content-token renderer, shared ordinary document mount, static export path, existing image layout rules, and shipped inline Mermaid host. The smallest contract is one generated persistent marker beside the existing inline marker. DDV-1 can use the resolved `<img>` URL directly and compose a public-safe shared adapter; DDV-2 remains a separate temporary-resource slice.

### DDV-1 — Persistent SVG Detail Control

- [x] Decorate each eligible published SVG diagram after an ordinary document mount with one absolutely overlaid top-right **Open diagram in new tab** control.
- [x] Open the current stable served SVG URL directly; preserve absolute and same-origin configured media locations without provider-specific branching.
- [x] Use native keyboard-capable link behavior, visible focus, an accessible label, and safe new-tab relationship attributes.
- [x] Keep the control visibly muted at rest for fine-pointer hover devices, fully unmute it on frame hover, its own hover, or focus, and keep it fully unmuted when hover is unavailable.
- [x] Change only paint properties during interaction; never add/remove the control or change spacing, dimensions, positioning, or document flow.
- [x] Avoid duplicate controls across navigation, repeated mounts, or management-mode projection.
- [x] Verify the existing Configuration And Extension Points and Docs Import diagrams as proof fixtures.

DDV-1 added the generated `persistent-svg` marker at the configured-media renderer, one site-owned shared read-only detail adapter, explicit public and manage composition, ordinary-document controller handoff, and shared overlay presentation. Docs Review does not compose the adapter. The native anchor retains the generated `src` string as its `href`, uses `_blank`, `noopener`, and the exact accessible label, and contains only an `aria-hidden` open-in-new icon. The frame and viewport wrap only the marked image; ordinary images, arbitrary SVG, inline Mermaid, and missing targets remain untouched. Repeated mounts add no duplicate frame or control.

The Configuration And Extension Points and Docs Import payloads were rebuilt through one targeted dry-run/write pair and each contains its expected marker. Eleven focused builder/public-boundary tests, JavaScript and Python syntax checks, the diagram-detail module smoke, the adjacent inline-Mermaid smoke, router-module smoke, public Library/Analysis/Moments read-only smoke with an injected persistent SVG, site validation, generated-payload inspection, and `git diff --check` form the DDV-1 acceptance evidence. The focused browser smoke also proves stable same-origin and absolute targets, unchanged image geometry, one compact control, independent diagram semantics, focus visibility, and duplicate avoidance. Ruff was unavailable in the configured Python environment; no Ruff result is claimed.

### DDV-2 — Inline Mermaid Detail Target

- [x] Depend on a successfully rendered inline Mermaid host rather than parsing the Markdown fence again.
- [x] Serialize only the adapter-owned rendered SVG into a standalone `image/svg+xml` browser resource; do not accept arbitrary page SVG.
- [x] Preserve the diagram's responsive `viewBox`, visible content, title/description accessibility metadata, and required standalone presentation.
- [x] Open the temporary target from direct user activation and manage its lifetime without leaking one object URL per remount.
- [x] Leave the rendered inline diagram usable when detail-target creation or opening fails.

DDV-2 consumes only the shipped inline adapter's successful host and direct SVG child. After Mermaid replaces its owned fence, it registers that host with the shared detail adapter; it does not read or parse the fence again. The detail adapter clones only the direct SVG, requires its responsive `viewBox` and non-empty title and description, preserves all SVG-owned presentation and visible content, and creates a distinct `image/svg+xml` Blob URL. The existing native `_blank` link provides direct browser activation without making the diagram body clickable or adding popup scripting.

Object URLs are tracked per mounted document root and revoked immediately before status, loading, or replacement content clears that root. Target creation and cleanup failures are contained warnings. A failed target leaves the rendered host and SVG in place, while a detail-registration exception cannot change a successful Mermaid render into a failure. The focused diagram-detail smoke proves direct producer handoff, rejection of arbitrary or non-direct SVG, standalone bytes and MIME type, distinct targets, exact idempotent cleanup, native link semantics, and usable failure fallback. The adjacent inline-Mermaid, router-module, and public read-only smokes also pass. DDV-3 retains the cross-form and manual browser closeout boundary.

### DDV-3 — Integration And Closeout

- [ ] Prove multiple persistent and inline diagrams receive one control each and keep distinct targets.
- [ ] Prove public routes add no inline Mermaid runtime while published SVG detail links continue to work.
- [ ] Check narrow width, light/dark themes, rest/frame-hover/direct-control-hover/focus/touch treatments, zero layout shift, keyboard and screen-reader labelling, browser zoom, navigation remount, and failed-open behavior manually.
- [ ] Add focused module/route contracts without testing ordinary visual choreography.
- [ ] Update Document Diagrams and the Diagrams feature parent with the shipped contract; do not add export promises.

## Completion Evidence

- A published Mermaid-backed SVG opens its stable configured SVG URL in a new tab from the overlaid control.
- A local inline Mermaid diagram opens a visually equivalent temporary standalone SVG without writing media or source files.
- The two forms share one accessible control and diagram-host contract while retaining distinct target creation.
- Diagram-owned links remain operable because the diagram body is not the detail trigger.
- Rest, hover, direct control hover, focus, and touch states change no diagram or document geometry and leave no unexplained control gap.
- Navigation and repeated mounts create no duplicate controls or abandoned temporary URLs.
- Public and external-local routes do not acquire the inline Mermaid renderer through this feature.
