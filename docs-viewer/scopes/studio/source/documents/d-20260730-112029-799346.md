---
doc_id: d-20260730-112029-799346
title: Edited Docs Review Source Round-Trip Delivery
added_date: "2026-07-30 11:20:29"
last_updated: "2026-07-30 22:12:09"
summary: rebuild edited review sources and import their complete staged source folder back to an exact scope or sub-scope
ui_status: done
parent_id: d-20260730-121423-78b0f6
viewable: true
---
# Edited Docs Review Source Round-Trip Delivery

## Status And Dependencies

Started as ERS-1 under
[Edited Docs Review Source Round-Trip](/docs/?scope=studio&doc=d-20260730-121423-78b0f6).
It replaces the former independent Docs Review Build delivery. Both
prerequisites below are shipped. ERS-1.0 through ERS-1.5 are complete, and the
durable workflow, endpoint, architecture, and ownership contracts now describe
the shipped result.

Build and edited-source-folder Import are one complete feature outcome. This
delivery depended on both sub-scope Import-parity prerequisites:

1. [Sub-Scope Ordinary Document Import Delivery](/docs/?scope=studio&doc=d-20260730-121423-469e54);
2. [Sub-Scope Returned Package Import Delivery](/docs/?scope=studio&doc=d-20260730-102954-1b363f).

The exact package-source **Open in VS Code** action and the synthetic Docs
Review builder are already shipped. The prerequisites establish shared
scope/sub-scope Import targets before this delivery adds another trusted
collection format.

## Complete Outcome

For a validated, import-capable review package from either a top-level scope or
a configured sub-scope, the user can:

1. edit package-local `source/*.md` files in VS Code;
2. select **Build** in Docs Review to regenerate and inspect the complete
   retained preview;
3. copy the package's complete `source/` folder into Import staging;
4. choose that folder in Docs Import as an **Edited review source** collection;
5. preview the complete exact overwrite plan and accept the derived-Markdown
   fidelity warning once; and
6. apply every edited source to its original canonical collection or none.

The edited Markdown sources are the content authority for this Import. The
original JSON/JSONL and trusted export metadata remain authority for provenance,
capability, exact prepared membership, routing, and prepared canonical source
versions.

## Docs Review Build Contract

- The disabled **Built** status becomes an enabled **Build** action whenever one
  valid package is active and no build request is running.
- The request freezes only the validated active `package_id`; it accepts no
  source path, configured scope, URL fallback, or canonical target.
- Explicit **Build** always regenerates the complete package-local projection,
  even when existing generated JSON is structurally complete.
- The existing review-specific synthetic configuration and Docs Viewer builder
  remain the only renderer and compiler.
- Build reads package-local `source/*.md` and writes only package-local
  generated projection. It does not rewrite source front matter, advance the
  package-local or canonical `last_updated`, or mutate canonical documents.
- Success reloads the same `package` and selected `doc`. Failure reports the
  error, stays on the current preview, and re-enables **Build** for retry.
- Ordinary reads retain conditional repair: damaged generated output may be
  repaired, while healthy output is not rebuilt merely by navigation.
- No watcher registration, source-freshness inference, background rebuild, or
  per-document incremental build is added.

## Edited Review Source Folder Contract

- The format recognizer runs before generic Markdown-package handling and
  accepts one staged folder containing the complete set of direct Markdown
  children materialized by a validated Docs Review package.
- Every source must have strict canonical front matter and the same
  `review_folder_id`, profile, export ID, source scope, and optional source
  sub-scope. Filename stems and `doc_id` values must agree and remain unique.
- After validation, the staged-source list displays the folder as
  `<review_folder_id> (reviewed)`, for example
  `20260730-095512-documents-document-content (reviewed)`. This is presentation
  only: selection and requests retain the exact staged candidate identity, and
  the label supplies no routing or overwrite authority.
- Package-local `added_date` and `last_updated` remain validated review
  transport metadata. Editing or building the package does not make either
  value canonical or eligible to overwrite destination date provenance.
- Trusted export metadata and the original validated review manifest establish
  package capability and the exact prepared document set. Missing, extra,
  duplicate, nested, symlinked, malformed, mixed-package, or cross-collection
  sources block the folder.
- A standalone review-source file selected through ordinary Markdown Import is
  rejected with guidance to stage and select the complete source folder. Its
  front matter is never rendered as body text.
- Generic `.md` files and ordinary one-document Markdown packages retain their
  current body-oriented create-only behavior.
- The folder is overwrite-only. Every source `doc_id` must resolve to exactly
  one existing `{ scope, doc_id }` or `{ scope, sub_scope, doc_id }` target.
  There is no create, skip, record selection, title match, or parent fallback.

## Dispatch Precedence

The integrated recognizer must receive the untouched staged candidate before
ordinary Markdown normalization:

1. inspect a directory and its unmodified source front matter for one complete
   edited review package;
2. accept a valid complete folder into trusted collection planning;
3. reject an individually selected review source with guidance to stage the
   complete folder; then
4. pass only unclaimed ordinary `.md` files and one-document Markdown packages
   to the generic normalizer, which may strip a well-formed leading front-matter
   block before create-only preview.

The generic normalizer may use a scalar `title` only as an ordinary title
fallback. It ignores `doc_id`, `review_*`, hierarchy, lifecycle, and status
fields and never converts them into overwrite or routing authority.

## Canonical Apply Contract

- Review transport fields, including package-local `added_date` and
  `last_updated`, are validated but never written to canonical front matter.
- Edited body, title, and summary may replace their canonical counterparts.
  Immutable `doc_id`, original `added_date`, and destination-owned fields such
  as `group`, `ui_status`, and collection configuration remain canonical.
- Apply compares edited body, title, and summary with the current canonical
  source through the shared recent-edit comparison. A qualifying change
  advances canonical `last_updated` inside the Import mutation boundary; a
  non-qualifying apply preserves the existing canonical timestamp.
- Parent and visibility fields apply only where the exact destination
  collection already permits them. Flat sub-scopes reject hierarchy rather
  than inventing it.
- Preview is write-free and shows folder identity, exact collection, document
  count, all target documents, stale-source state, and the fidelity warning.
- One explicit confirmation accepts the complete overwrite plan. Prepared
  canonical versions are revalidated immediately before apply.
- Apply validates and prepares every write before entering one scope or
  confined sub-scope mutation boundary. A write or rebuild failure restores
  the complete pre-apply source snapshot and reports no successful Import.
- Success refreshes the relevant top-level Docs Viewer state or mounted
  sub-scope report without changing selection in another surface.

## Fidelity Boundary

`document-content` review source is derived Markdown. Rich content, tokens,
comments, raw embeds, source formatting, or package assets may have been lost
during rendered conversion. The user owns that acceptance decision by
rebuilding and visually inspecting Docs Review before staging the folder.

The Import preview repeats that warning but does not compare with, restore
from, or merge content from the original JSON/JSONL package.

[Full Document Export Delivery](/docs/?scope=studio&doc=d-20260711-012238-8a04a6)
separately proposes exact canonical Markdown and asset/dependency export. ERS-1
does not duplicate that work or make the proposed export-only profile
returnable.

## Likely Authority

- The Docs Review controller and provider own Build state, exact active package
  identity, status, retry, and route-preserving reload.
- The existing review package service and synthetic builder own explicit
  regeneration versus conditional read-time repair.
- The integrated Import format registry owns edited-source-folder recognition
  before generic Markdown formats.
- Existing trusted export metadata, returned-package validation, and review
  manifests own capability, provenance, and exact membership.
- Shared `ImportContent`, collection planning, scope/sub-scope target
  resolution, mutation, rollback, and rebuild owners perform the complete
  overwrite without a second document parser or mutation model.
- Scope and report Import controllers reuse one modal, preview, confirmation,
  busy, retry, and result lifecycle.

## Boundary

In scope:

- explicit complete-package Build in Docs Review;
- staged edited-review-source-folder discovery and strict validation;
- one fidelity warning and whole-folder confirmation;
- overwrite-only exact scope and sub-scope apply;
- stale-source revalidation, rollback, and appropriate rebuild/refresh;
- rejection of individual review sources through ordinary Markdown Import; and
- focused service, module, route, and realistic browser evidence for both
  destination kinds.

Not in scope:

- implementing either sub-scope Import prerequisite;
- editing or regenerating the original returned JSON/JSONL;
- arbitrary front-matter-aware Markdown Import;
- creates, partial apply, record selection, moves, deletes, or identity changes;
- automatic rich-content, token, embed, dependency, or asset restoration;
- extending `docs-watcher`; or
- making `document-full-source` returnable.

## Delivery Tracker

**Current state:** ERS-1 is complete through ERS-1.5.

**Review rule:** complete one phase, record exact evidence, and stop for review
before the next phase.

### [x] ERS-1.0 — Confirm Prerequisites And Freeze Authority

- [x] Confirm ordinary and returned-package Import parity for exact configured
  sub-scopes, including shared target and rollback seams.
- [x] Freeze edited Markdown as content authority while retaining trusted
  package metadata for capability, membership, routing, and staleness.

#### ERS-1.0 authority and evidence

- SSO-1 and SRR-2 are closed. Both adopt the shared exact
  `ManagedDocumentCollection`; SRR-2 additionally reuses normalized collection
  planning and the confined sub-scope source-snapshot, write, rollback, and
  rebuild boundary. There is no parent-scope fallback in those seams.
- For ERS-1, the selected complete `source/` folder owns candidate body, title,
  summary, and only destination-permitted presentation fields. Trusted export
  metadata plus the validated Docs Review manifest own capability, profile,
  export and package identity, exact `selected_doc_ids`, source scope and
  optional sub-scope, and prepared `source_last_updated` values. Review front
  matter must agree with those authorities but cannot independently grant
  routing or overwrite permission.
- Recognition stays in integrated Import before ordinary Markdown
  normalization. A recognized folder must enter the existing `ImportContent`
  and collection-planning model through the frozen exact destination; it does
  not add a second parser, target resolver, or mutation model.
- The current sub-scope collection apply is atomic and restores its complete
  source snapshot plus projection after write or rebuild failure. The current
  top-level collection apply is not atomic and has no snapshot rollback; ERS-1.3
  must add or extend an owned top-level atomic boundary rather than reuse that
  path unchanged.
- At this checkpoint the existing Docs Review build endpoint validated an exact
  `package_id` and used the synthetic builder, but short-circuited when
  generated output was complete. ERS-1.1 owned changing explicit Build to
  unconditional regeneration while leaving conditional read-time repair
  intact.
- Focused prerequisite and seam evidence passed 46 tests across managed target
  resolution, collection apply and rollback, and validated review packages.
  No Build UI, edited-source recognizer, canonical write, or browser workflow
  was implemented or claimed in ERS-1.0.

**Checkpoint / review gate:** prerequisites and authority are frozen. Do not
begin ERS-1.1 without review.

### [x] ERS-1.1 — Enable Complete Docs Review Build

- [x] Make explicit Build run the existing synthetic package builder even when
  generated output is complete; retain conditional read-time repair.
- [x] Add Build busy, success, failure, retry, and route-preserving reload
  behavior.
- [x] Prove Build changes only package-local generated projection and leaves
  review-source and canonical timestamps untouched.

#### ERS-1.1 implementation and evidence

- `POST /docs-review/packages/build` now accepts exactly `package_id`,
  revalidates that complete package, and always runs the existing synthetic
  builder. Conditional `index-tree` and document reads retain their shipped
  repair-only behavior and do not rebuild healthy output.
- The review toolbar now exposes enabled **Build** for one validated active
  package regardless of current generated completeness. The controller freezes
  that active package identity, disables duplicate requests while building,
  reports failure without leaving the preview, re-enables same-package retry,
  and reloads the unchanged `package`, `doc`, and fragment route after success.
- The returned-package provider rejects blank or non-active Build identities
  before sending the exact `{ package_id }` body. No source path, configured
  scope, URL text, canonical target, or displayed-parent fallback enters the
  request.
- A real fixture route first loaded a healthy retained projection, then changed
  its package-local Markdown externally. Explicit Build regenerated the edited
  content and reloaded the same package/document route. The edited source bytes
  and their transport dates remained unchanged, and an external canonical
  timestamp sentinel was byte-identical after Build.
- Focused Python evidence passed 25 tests across package validation, explicit
  Build, conditional read repair, initial review materialization, and review
  source projection. The controller module smoke passed Build busy,
  failure/retry, exact identity, success, and route preservation; the real
  Docs Review route smoke passed healthy-package rebuild and rendered-result
  acceptance.
- Explicit Python lint passed for the changed service and focused test modules;
  JavaScript lint passed for the controller and provider; repository whitespace
  validation passed. No edited-source recognizer, staging dispatch, Docs Import
  plan/apply, or canonical mutation was added.

**Checkpoint / review gate:** explicit complete-package Build is ready. Do not
begin ERS-1.2 without review.

### [x] ERS-1.2 — Recognize Edited Review Sources

- [x] Add the complete-folder recognizer, strict front-matter parsing, shared
  provenance validation, and individual-file rejection before generic
  Markdown dispatch.
- [x] Project one validated staged-folder label as
  `<review_folder_id> (reviewed)` without changing its staged request identity.
- [x] Prove exact trusted membership and fail closed for unsafe, incomplete,
  mixed, or non-importable folders.

#### ERS-1.2 implementation and evidence

- Integrated Import now claims a staged directory only when untouched leading
  front matter contains Docs Review provenance. Claimed folders are validated
  before generic Markdown-package dispatch; unclaimed Markdown files and
  ordinary Markdown folders keep their existing create-only path.
- Recognition requires direct UTF-8 Markdown children, canonical scalar front
  matter, unique filename/`doc_id` agreement, one consistent review package
  identity, and unchanged retained transport/provenance fields. Nested,
  symlinked, unsupported, duplicate-field, mixed-package, and partial source
  sets fail closed.
- The retained validated manifest establishes package identity, capability, and
  exact `selected_doc_ids`/`source_files` membership. The export-ID-selected
  trusted sidecar must independently pass shared returned-package validation,
  advertise an importable profile, and contain exact nonblank
  `source_last_updated` membership for the complete prepared set.
- One valid staged folder projects
  `20260730-095512-documents-document-content (reviewed)` through
  `display_name`; its option value and request body retain the exact staged
  candidate name. A standalone reviewed Markdown file remains visible as an
  ordinary file but is rejected before Markdown normalization with guidance to
  stage and select the complete folder.
- Focused source-listing evidence passed 18 tests, including exact display
  projection plus incomplete, mixed, non-importable, nested, unsupported,
  duplicate-field, mixed-provenance, missing-version, symlink, standalone, and
  ordinary-source cases. Broader review/package service evidence passed 73
  tests with one intentional skip, and all ordinary source-format evidence
  passed 44 tests.
- The Docs Import browser module smoke passed with the reviewed label rendered
  exactly while its staged option value remained unchanged. Explicit Python
  and JavaScript lint plus repository whitespace validation passed.
- No collection plan, stale canonical comparison, apply, rebuild, refresh, or
  canonical source write was added. A recognized valid folder stops at the
  ERS-1.3 planning boundary rather than falling through to generic Markdown.

**Checkpoint / review gate:** strict edited-source recognition is ready. Do not
begin ERS-1.3 without review.

### [x] ERS-1.3 — Plan And Apply Scope Folders

- [x] Map edited sources through the shared normalized collection planner with
  exact overwrite-only field ownership and stale-source validation.
- [x] Ignore review transport dates and use the shared body/title/summary
  comparison to preserve or advance canonical `last_updated` during apply.
- [x] Apply or restore the complete top-level scope set and refresh the normal
  Docs Viewer state.

#### ERS-1.3 implementation and evidence

- Recognized folders now enter the existing `ImportContent` collection planner
  as one complete overwrite-only set. Body, title, summary, and only explicitly
  supplied parent/visibility fields enter the plan; review transport dates,
  immutable identity, and destination-owned metadata do not.
- The trusted prepared `source_last_updated` map is compared with every current
  canonical target during preview and again before apply. The complete staged
  source bytes and trusted metadata each have a confirmed hash, so edited
  folder, sidecar, target, or planned-action drift returns a refreshed plan
  instead of writing.
- Scope apply now snapshots every exact canonical source, rechecks those bytes
  inside the mutation boundary, writes the complete set, and runs the normal
  targeted Docs/search rebuild. A write or rebuild failure restores all source
  bytes, rebuilds the restored projection, and reports every record failed.
- The shared recent-edit comparison advances canonical `last_updated` only
  when body, title, or summary changes. Focused evidence proved that review
  transport dates never replace canonical dates, an edited document advances,
  an unchanged document preserves its timestamp, and `group`/`ui_status`
  survive.
- Edited review folders now use the modal's single-select collection lane,
  retain their exact staged request identity and source format through
  confirmation, and display the exact reviewed-folder label. They no longer
  participate in ordinary multi-file **Select all**.
- Focused and regression service evidence passed 132 tests with one intentional
  skip. The collection-controller and multi-selection browser module smokes
  passed, including exact display labeling, collection isolation, normal
  top-level terminal refresh, and unchanged child returned-package routing.
  Explicit Python/JavaScript lint and repository whitespace validation passed.
- No configured sub-scope edited-folder apply or toolbar/action consolidation
  was added. Docs Review remains optional, and direct JSON/JSONL collection
  Import remains available.

**Checkpoint / review gate:** top-level scope folders are ready. Do not begin
ERS-1.4 without review.

### [x] ERS-1.4 — Apply The Same Contract To Sub-Scopes

- [x] Reuse the exact configured sub-scope target, flat collection validation,
  confined mutation, rollback, rebuild, and report refresh owners.
- [x] Prove no parent-scope fallback, discovery rebuild, or cross-collection
  mutation.

#### ERS-1.4 implementation and evidence

- The same edited-folder adapter now resolves current documents from the exact
  configured `ManagedDocumentCollection`. Folder provenance must match both
  requested `scope` and `sub_scope`; a same-ID parent document or sibling
  collection cannot satisfy the request.
- Sub-scope records remain flat. Non-empty edited `parent_id` and non-flat
  canonical child data block the complete plan. Body, title, and summary may
  change, while hierarchy, `group`, `ui_status`, and visibility remain owned by
  the configured child collection.
- Confirmed child plans reuse the shipped confined atomic collection owner:
  every source is snapshotted and rechecked, the exact child projection is
  rebuilt with no parent search rebuild, and any write or rebuild failure
  restores the complete child snapshot before reporting failure.
- Validated staged folders now project their exact collection and import
  capability in the staged-file record. A fixed child Import destination shows
  only a matching edited folder in the single-select package lane, alongside
  its existing returned JSON/JSONL packages; the displayed reviewed label and
  staged request identity remain distinct.
- Focused and regression service evidence passed 135 tests with one intentional
  skip. It covered exact child discovery, write-free preview, overwrite-only
  apply, stale metadata, flatness, no parent/sibling fallback, unchanged
  same-ID documents outside the target, and confined rollback. The collection
  and multi-selection browser module smokes passed exact destination filtering,
  child terminal refresh/retry, and unchanged returned-package behavior.
  Explicit Python/JavaScript lint and repository whitespace validation passed.
- No ERS-1.5 end-to-end acceptance or durable authority transfer was performed,
  and no toolbar/action consolidation was added. Docs Review remains optional.

**Checkpoint / review gate:** exact sub-scope folders are ready. Do not begin
ERS-1.5 without review.

### [x] ERS-1.5 — Verify And Transfer Durable Authority

- [x] Exercise realistic scope and Analysis/Tags packages through edit, Build,
  staged-folder preview, confirmation, apply, and rendered result.
- [x] Cover fidelity warning, individual-source rejection, source-version
  conflicts, rollback, and unchanged generic Markdown/package behavior.
- [x] Update
  [Docs Review](/docs/?scope=studio&doc=d-20260711-214307-b4c800),
  [Docs Review Endpoints](/docs/?scope=studio&doc=d-20260711-214307-6d6d46),
  [Docs Import](/docs/?scope=studio&doc=d-20260424-000000-fc8b5d), and exact
  endpoint/ownership documentation, then rebuild Studio docs and search.

#### ERS-1.5 acceptance and closeout evidence

- One joined service acceptance edited retained sources for a parent-scope
  package and an Analysis/Tags package, ran the real synthetic Docs Review
  builder, verified rendered edited content and byte-identical package source
  plus canonical source after Build, copied each complete source folder into
  staging, previewed and confirmed it through the normal Import service, and
  rendered both canonical destinations after apply.
- The parent and child plans were overwrite-only and carried the fidelity
  warning. The Analysis/Tags result retained destination-owned `group`,
  `ui_status`, and visibility. Both staged entries used
  `<review_folder_id> (reviewed)` for display while requests retained their
  staged names. Ordinary Markdown and returned JSONL staging bytes and format
  classification remained unchanged.
- Existing focused evidence continued to cover standalone reviewed-Markdown
  rejection, stale prepared versions, edited-folder drift, concurrent target
  change, exact child routing, no parent/sibling fallback, and complete source
  plus projection rollback after write or rebuild failure.
- Focused and regression Python evidence passed 136 tests with one intentional
  skip. The maintained `source-lint` profile passed all seven boundaries; its
  summary is
  `var/admin/test-runs/ers-1-5-source-lint/summary.md`. The changed acceptance
  module also passed explicit repository Python lint.
- The collection and multi-selection browser module smokes passed. The
  maintained `docs-viewer-smoke` profile passed 25/25; its summary is
  `var/admin/test-runs/ers-1-5-docs-viewer-smoke/summary.md`. That profile's
  real Docs Review route visually accepted an externally edited source after
  explicit Build while retaining the exact package/document route.
- Durable authority moved into Docs Review, Docs Review Endpoints, Docs Import,
  Create And Import Endpoints, Endpoint Overview, Import Scripts, and Docs
  Import Architecture. The parent feature and capability quick reference now
  report the shipped result.
- An explicit Studio docs rebuild scanned and emitted 234 documents with zero
  warnings and no residual changes after the watcher projection. The explicit
  Studio search rebuild likewise reported the current index unchanged.
  Repository whitespace validation passed.
- No Import/Review package toolbar consolidation was added. Docs Review remains
  optional, direct returned JSON/JSONL Import remains available, and no
  `document-full-source`, asset restoration, partial apply, create, move,
  delete, or automatic watcher behavior entered ERS-1.

## Completion Gate

One scope package and one configured sub-scope package are edited in VS Code,
rebuilt and visually accepted in Docs Review, staged as complete source
folders, revalidated against their exact prepared and current canonical sets,
and applied completely through one shared Import format. Both destinations show
the edited content after their appropriate rebuild. Docs Review Build leaves
package-local and canonical timestamps untouched; Import advances canonical
`last_updated` exactly when body, title, or summary changed. Generic Markdown,
the original JSON/JSONL, unrelated metadata, other collections, and public
surfaces remain unchanged.
