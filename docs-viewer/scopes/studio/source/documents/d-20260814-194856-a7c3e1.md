---
doc_id: d-20260814-194856-a7c3e1
title: Admin Retirement
added_date: "2026-08-14 19:48:56"
last_updated: "2026-08-14 21:29:09"
summary: Retire the local Admin application after extracting the small surviving assurance boundary.
ui_status: in-progress
parent_id: d-20260428-000000-f5ff18
---
# Admin Retirement

## Status

Active. AR-1 assurance extraction, AR-2 activity-pipeline retirement, the separate Work Storage Index retirement, and AR-3 Admin application removal are implemented. AR-4 review and durable-documentation reconciliation are complete. Final closeout and document disposition await user approval.

## Complete Result

The local Admin application, its routes, APIs, reports, Workbench, runners, smokes, and current-state documentation are retired. Independently useful repository assurance remains under top-level or application-owned tests without an Admin compatibility layer.

## Boundary

- retain the repository check-profile runner as test-owned evidence coordination;
- retain only independently justified projection, route-ready, and Catalogue consistency checks;
- put cross-repository contracts under `tests/` and Catalogue consistency under `studio/tests/`;
- move new profile evidence from `var/admin/test-runs/` to `var/test-runs/`;
- do not move Admin Checks reports, target-map machinery, CSS reporting, UI/API artifact readers, or Admin smokes into the test boundary;
- retire the shared activity pipeline end to end rather than moving its files or retained output;
- preserve ordinary diagnostic, service, script, and operation-specific logging that has an owner independent of the Admin feed;
- leave existing ignored `var/admin/activity/` files untouched and create no migration or compatibility reader;
- keep the separately classified Work storage index outside AR-2; its later retirement belongs to the archived *Work Storage Index Retirement* delivery;
- add no aliases for old commands, paths, routes, or output locations.

## AR-0 — Readiness

- [x] Confirm Admin is not a canonical owner for Studio, Docs Viewer, or public-site data.
- [x] Separate the interactive Admin toolset from repository assurance currently stored beneath `admin-app/`.
- [x] Confirm `tests/` is the cross-repository evidence owner and application-specific checks stay with their application.
- [x] Confirm assurance extraction precedes retirement work.
- [x] Split activity-pipeline retirement from broader Admin removal after its cross-application producer and response plumbing became clear.

Record: readiness found one repo-wide profile runner, two cross-repository contract audits, and one Catalogue-owned consistency audit worth retaining. The legacy public-build wrapper and its broken optional projection-audit mode were not retained; `bin/site-validate` already owns the deploy root. Admin Checks reports, target mapping, Activity/Testing readers, Workbench, and Admin runtime remain retirement scope rather than test infrastructure.

Gate outcome: AR-1 approved by the user on 2026-08-14. AR-2 activity-pipeline retirement approved as a bounded slice on 2026-08-14.

## AR-1 — Assurance Extraction

- [x] Move the profile runner and its contract tests to top-level `tests/`.
- [x] Move the projection manifest and cross-repository audits to `tests/contracts/` and `tests/audits/`.
- [x] Move Catalogue site-consistency and its focused test to `studio/tests/`.
- [x] Write new profile evidence below `var/test-runs/` and update the temporary Admin Testing reader.
- [x] Update maintained lint ownership, active commands, source documentation, and exact path consumers.
- [x] Run the accepted evidence and review the bounded diff.

### Verification Budget

| Contract or risk | Lowest adequate evidence | Permanent test change? | Reason |
| --- | --- | --- | --- |
| Profile discovery, path resolution, isolated workspace, and evidence output survive the move | moved runner contract tests plus `tests/run_checks.py --list` | move and update existing tests | catches stale repo-root, command, and output paths |
| Projection and route-ready audits load their moved owners | direct projection and route-ready audit runs | none | catches broken manifest/import/default-path resolution |
| Catalogue consistency retains Studio-owned source resolution | moved focused pytest module | move existing test | catches repo-root and canonical-source resolution drift |
| Coordinated quick profile uses only current paths | one fresh `quick` profile run | none | exercises the changed coordinator and retained audit paths together |
| Changed Python and documentation remain clean | explicit-path lint, targeted docs generation if the watcher is unavailable, and `git diff --check` | none | catches source/lint and generated-document follow-through defects |

Record: `tests/run_checks.py` now owns profile coordination and writes new evidence under `var/test-runs/`. The projection manifest and working cross-repository audits moved to `tests/contracts/` and `tests/audits/`; Catalogue consistency moved with its focused test to `studio/tests/`. Current commands, lint ownership, CI path triggers, Admin's temporary read/launch consumers, and durable operational docs point directly at the new paths without aliases or ignored-output migration.

Implementation inspection proved the legacy public-build wrapper and the projection audit's optional built-root mode were stale: neither profile executed the wrapper, and the documented `site/` run could not resolve a manifest that mixed repository-prefixed and deploy-root-relative paths. Both were removed rather than repaired; `bin/site-validate` remains the deploy-root authority.

Evidence:

- `tests/run_checks.py --list` passed and exposed the existing named profile set from its new owner;
- projection contract passed against `tests/contracts/projection_contract.json`;
- strict route-ready audit passed for 3 applications, 20 templates, and 20 ready roots with zero findings;
- moved runner, Catalogue consistency, and temporary Admin reader tests passed 20/20;
- Admin checks-config and repository lint-ownership tests passed 12/12 after removing the stale `admin-app/commands/` target-map entry;
- explicit Python lint passed all 11 changed Python paths; JSON parsing and `git diff --check` passed;
- the Docs watcher regenerated changed document payloads, and the complete Studio search rebuild wrote 230 search documents;
- `quick` evidence is recorded at `var/test-runs/admin-retirement-ar1-20260814/summary.md`: 9/10 commands passed, with the sole failure an existing `E402` in `studio/tests/python/test_tag_document_declarations.py`, last changed by TDL-1.2 and outside this checkpoint.

Code review found no compatibility path, duplicate manifest, or moved Admin report machinery. Admin Testing temporarily reads `var/test-runs/`, and Admin Audits temporarily launches `tests/audits/route_ready_state.py`; these are explicit AR-3 deletion consumers. Existing ignored evidence under `var/admin/test-runs/` is not migrated. The unrelated Studio lint finding remains separate.

Gate outcome: AR-1 evidence accepted for continuation into the separately bounded activity retirement. Broader Admin removal remains unapproved.

## AR-2 — Activity Pipeline Retirement

- [x] Trace and classify the activity producers, shared contract/store, response plumbing, Admin consumer, and activity-specific evidence.
- [x] Remove the Admin Activity route, frontend, API, contract audit, and feed configuration without removing other Admin features.
- [x] Remove Catalogue, Tag, and Docs Viewer emission hooks plus `activity_context` request and `activity_log` response plumbing.
- [x] Remove the shared activity contract/writer and activity-specific tests/profile inputs.
- [x] Preserve ordinary diagnostics and independently owned operation logs; record stale adjacent paths without widening the checkpoint.
- [x] Run focused owner checks, inspect the retirement diff, and stop for review.

### Verification Budget

| Contract or risk | Lowest adequate evidence | Permanent test change? | Reason |
| --- | --- | --- | --- |
| Catalogue, Tag, and Docs Viewer writes retain their domain behavior after telemetry arguments and response fields are removed | focused existing service/API tests for affected write owners | delete activity-only assertions; retain behavioral assertions | catches accidental mutation, validation, or response regressions while removing the cross-cutting hook |
| Browser request builders no longer send the retired context | explicit changed-path JavaScript lint plus source inspection | remove only activity-context fixture/assertion fragments | catches stale identifiers and malformed request-object edits without adding UI choreography coverage |
| Admin no longer exposes or advertises Activity while its other routes remain intact | focused Admin server/config test | narrow existing test; delete Activity-only smoke | catches route/config/API residue before the broader application is removed |
| The shared writer, contract, feed paths, and activity-only profile inputs are gone while the Catalogue work-storage projection remains | targeted repository reference scan, direct projection audit, and runner contract/list checks | update existing ownership contracts | catches dangling imports, deleted projection expectations, and accidental deletion of the unrelated storage index |
| Changed Python/config/docs remain syntactically and structurally valid | explicit-path lint/compile or focused pytest as appropriate, docs watcher output, search rebuild, and `git diff --check` | none | catches import, JSON, generated-document, search, and whitespace follow-through defects |

Decision: the activity data is not retained or relocated. Its completeness and utility are unproven, so continuing to collect it would preserve cost and coupling without a justified consumer. Existing ignored feed/journal files are not migrated or deleted by this checkpoint.

Record: Admin Activity, the shared contract/writer/feed, and the Catalogue, Tag, Docs Viewer, Studio-browser, and Admin-audit telemetry hooks were one cross-application pipeline rather than a standalone frontend. The route/API/assets, browser `activity_context`, service `activity_log`, emitter helpers, contract verification, and activity-only tests/profile inputs are removed together. Ordinary script/service diagnostics remain. Docs Move, Copy, Import, source-open, and review-package logs remain operation-owned events; misleading activity-named symbols were renamed without changing their event names or payload behavior.

Adjacent findings: AR-2 kept `studio/data/generated/activity/work-storage-index.json` because its generator, cleanup, static exposure, field registry, and tests formed a separate Catalogue-owned chain. A follow-up consumer trace found that the current Catalogue Works report reads canonical `storage_location` through the Studio API and no checked-in runtime consumes the index; the now-archived *Work Storage Index Retirement* delivery owned its separate removal. Existing ignored `var/admin/activity/` files may remain locally but have no retained reader or producer; this checkpoint intentionally neither migrates nor deletes them. The Admin target-map `activity` heuristic and the `bin/local-studio` activity-route message were stale direct consumers and were removed. Broader Admin audits, checks, testing, Workbench, runtime, and launchers remain AR-3 scope.

Evidence:

- focused owner tests passed 218 checks with one existing skip: 18 Admin route/config checks, 60 Catalogue/Tag service and API checks, 128 Docs Viewer management/import/package/move/copy checks, and 12 Studio runtime/Catalogue cleanup checks;
- the moved runner contract passed 11/11 and `tests/run_checks.py --list` exposed the current profile set;
- explicit changed-path lint passed 44 Python and 20 JavaScript files; changed Python compiled and changed JSON parsed;
- the projection contract passed while retaining the work-storage index, and the strict route-ready audit passed for 3 apps, 19 templates, and 19 ready roots with zero findings;
- the Activity source/current-state document was removed, the Docs watcher reconciled 227 Studio documents, the complete Studio search rebuild wrote 229 search documents, and the Studio broken-links audit reported zero issues;
- targeted repository scans found no retired telemetry identifier, route, API, contract, helper, or activity-only test reference in executable/test ownership; only the separately classified work-storage path remains;
- `git diff --check` passed.

Review: no compatibility reader, replacement telemetry store, moved feed data, or broader Admin deletion was introduced. Activity removal did not change canonical mutation, lookup/build, media, publication, package, audit-result, or operation-log ownership. AR-2 is ready for user review.

Gate outcome: stopped at AR-2 review. Do not start broader Admin deletion without explicit approval.

## AR-3 — Admin Application Removal

- [x] Remove Admin frontend, server, routes, APIs, Checks/report machinery, Workbench, and Admin-only tests.
- [x] Remove `bin/local-admin`, Admin startup/configuration from `bin/local-all`, and Admin smoke/profile/lint ownership.
- [x] Remove current-state Admin documentation and regenerate affected Docs Viewer projections/search.
- [x] Run proportional owner checks and review the final retirement diff.

Record: the 57 tracked files under `admin-app/`, `bin/local-admin`, the Admin-hosted Docs Viewer Workbench specimen registry, and 14 Admin-specific current-state documents are removed. Workbench was a leaf consumer: its test-only registry imported production Docs Viewer modal and Import renderers, but no production owner imported the registry or Admin host, and the registry performed no API, storage, logging, or generation work. Production Studio, Docs Viewer, Catalogue, Tag, public-site, and shared runtime modules were not changed.

The retained `tests/run_checks.py`, projection contract, route-ready audit, Catalogue consistency audit, Studio and Docs Viewer test owners, and `var/test-runs/` evidence boundary remain independent of Admin. Admin launch/profile/lint/CI ownership and stale test consumers were removed rather than aliased. The Site Consistency and Route Ready-State Audit pages were reparented under Testing; the route-ready audit now covers only its two surviving applications. Existing ignored `var/admin/` output was not migrated or deleted, and its final ignore/disposition review remains AR-4 scope.

Evidence:

- targeted scans found no Admin or Workbench path, route, launcher, profile, lint scope, classification, or import reference in retained executable/test ownership;
- focused retained-owner tests passed 34/34 across the runner, lint configuration, local browser/runner contracts, Catalogue Works contract, and Docs Viewer static route mapping;
- an initial broader focused selection passed 42 checks and exposed one unrelated existing document-package assertion that expects `page_id: "docs-manage"` in a production controller where it is already absent; that separate Docs Viewer contract was not changed;
- the projection contract passed, and the strict route-ready audit passed for 2 applications, 14 templates, and 14 ready roots with zero findings;
- shell syntax, changed Python compilation, lint-target JSON parsing, `tests/run_checks.py --list`, and `bin/lint --list` passed with no Admin profile or lint boundary;
- explicit Python lint passed all 9 changed Python paths;
- the complete Studio docs reconciliation retained 213 documents, removed 14 retired by-ID projections, and reported zero warnings; the complete search rebuild wrote 215 search documents.

Review: no compatibility command, route, profile, static host, Workbench copy, replacement dashboard, moved Admin report, or product telemetry/data hook was introduced. Planned and delivery-history documents concerning superseded Admin reports remain for explicit disposition in AR-4; broader durable Testing, Source Tree Ownership, Local Runners, and configuration documentation review has not started.

Gate outcome: stopped at AR-3 user review. Do not start AR-4 code review or closeout without explicit approval.

## AR-4 — Code Review And Closeout

- [x] Review for stale Admin paths, aliases, dead configuration, duplicated tests, and retained ignored-output assumptions.
- [x] Update durable Testing, Source Tree Ownership, Local Runners, and other surviving owner documents.
- [x] Present retain-or-retire recommendations for this delivery and superseded Admin feature documents.
- [ ] Close only after the user approves the shipped retirement.

Record: retained executable, test, runner, lint, CI, and configuration ownership contains no Admin path, alias, profile, route, import, or duplicated assurance owner. `var/admin/` remains physically untouched and ignored by the existing broad `var/` rule; the redundant `var/admin/`, `var/studio/activity/`, and `var/studio/risk/` entries were removed because they no longer describe separate retained contracts.

Durable current-state documentation now describes Studio, Docs Viewer, repository checks, focused audits, operation logs, local runners, route readiness, source ownership, configuration, testing, and proposed mobile/iPad/review work without treating Admin or the retired activity feed as an active owner. Historical completed-delivery evidence, including exact old `var/admin/...` paths, is unchanged.

Evidence:

- executable/config/test scans found no retained Admin path, runner, environment variable, route, API, profile, lint boundary, or import;
- `bin/lint --list` reports the six surviving maintained source boundaries, and `tests/run_checks.py --list` reports no Admin profile;
- the full Studio docs reconciliation retained 213 documents with zero writes, removals, or warnings after watcher follow-through, and the complete search rebuild wrote 215 search documents;
- the Studio broken-links audit reports exactly two issues, both inside the superseded *Admin Checks Responsibility Reports* plan and both resolved by the recommended family archive; no surviving current-state owner links to an AR-3 deletion;
- `git diff --check` passes.

### Document Disposition Recommendation

Retain this delivery through final approval, then archive it as the retirement record. Retain the extracted [Testing](/docs/?scope=studio&doc=d-20260501-174746-efd581), [Run Checks](/docs/?scope=studio&doc=d-20260501-174746-f92667), [Site Consistency Audit](/docs/?scope=studio&doc=d-20260331-000000-cdc571), and [Route Ready-State Audit](/docs/?scope=studio&doc=d-20260625-174523-74cf61) owners.

Archive the following superseded Admin report family together. These plans depend on the deleted Admin Checks runner, registry, artifacts, API, or UI, and retaining them as active proposals would imply an owner that no longer exists:

- [Admin Checks Responsibility Reports](/docs/?scope=studio&doc=d-20260531-000000-6679ea)
- [Admin Checks Operational Reports](/docs/?scope=studio&doc=d-20260610-225919-188d54)
- [Report Dependencies](/docs/?scope=studio&doc=d-20260609-212515-238344)
- [Imports Report](/docs/?scope=studio&doc=d-20260609-212515-520398)
- [Tests Report](/docs/?scope=studio&doc=d-20260609-212515-6d8aa2)
- [Git History Report](/docs/?scope=studio&doc=d-20260609-212515-e7f0ac)
- [Static Searches Report](/docs/?scope=studio&doc=d-20260610-114020-544e51)
- [Code Surface Report](/docs/?scope=studio&doc=d-20260610-225919-78b56a)
- [Generated Payloads Report](/docs/?scope=studio&doc=d-20260610-114020-8e5197)
- [Runtime Checks Report](/docs/?scope=studio&doc=d-20260610-114020-b56fed)

The superseded family is intentionally unchanged pending disposition. It is not a source for extracting more tooling: any future useful repository or application audit should start from a current contract under `tests/` or its owning application's tests.

Gate: stop for user approval of the shipped retirement and the document disposition above. Do not mark this delivery done or archive any document before that approval.
