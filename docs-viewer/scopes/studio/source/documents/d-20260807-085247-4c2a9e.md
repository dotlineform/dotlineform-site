---
doc_id: d-20260807-085247-4c2a9e
title: "SSP-4.2 — Exact Collection Copy Apply Evidence"
added_date: "2026-08-07 08:52:47"
last_updated: "2026-08-07 08:56:36"
summary: record receipt-bound exact collection Copy writes, transformations, follow-through, results, staleness, and contained failure evidence
ui_status: done
parent_id: d-20260801-233534-239eab
viewable: true
---
# SSP-4.2 — Exact Collection Copy Apply Evidence

## Outcome

SSP-4.2 is complete at its review gate. The existing Copy apply pipeline now accepts the SSP-4.1 exact collection receipt, writes fresh documents only to its resolved parent or child target root, and invokes the existing coordinated parent or exact sub-scope rebuild. Browser destination and selection surfaces remain stopped at SSP-4.3.

## Applied Boundary

- Transformation sets the target-parent default explicitly when non-viewable, executes the receipt's retained or omitted non-subject metadata decisions, leaves raw `folder_path`, `work_id`, and `series_id` values untouched, and maps only the receipt-owned exact source links between `doc` and report-host `subdoc` shapes.
- Media and registered build-source transfer retain parent-scope ownership and the existing create, exact-byte reuse, collision, production, and verification rules. Documents use atomic create-only writes under the exact target collection.
- Parent targets retain the targeted parent docs/search rebuild. Child targets invoke the configured sub-scope rebuild once; result URLs use the exact report host and `subdoc`, and activity plus v2 success/failure payloads carry exact source and target collection objects.
- Apply restores and compares the complete receipt before mutation. A stale source, target, parent or collection configuration, custom metadata decision, link decision, media item, or build-source plan cannot proceed; later partial failure reports observed artifacts only under the exact target collection.

## Focused Evidence

- 139 focused Python checks passed across exact planning and receipt restoration, parent and child Copy apply, current transfer routes, Move regression, customisations, managed targets, sub-scope building, checked scope configuration, and management capabilities.
- Direct apply coverage proves child-to-child, child-to-parent, and parent-to-child link transformation alongside the existing parent-to-parent flow; exact child rebuild routing; public-parent child `viewable: false`; matching custom metadata retention; unsupported metadata omission; pre-apply rejection; parent-owned media; exact local result URLs; unchanged source bytes; and exact activity records.
- Stale target occupation fails before media or document mutation. Simulated child write failure leaves one exact created child and one missing planned child, reports that state, does not log success, and does not write into the parent document collection.
- Explicit Python lint passed for the changed planner/apply services and focused tests. The focused route pair and current Move apply suite passed without changing route, Move, browser, publication, or lineage owners.

## Independent Review And Stop

The code/diff review found one transfer pipeline, one exact managed-collection resolver, parent-owned media/build sources, atomic new document writes, and the existing parent/sub-scope rebuild boundaries. It found no title, filename, route, selected-row, or subject fallback; no compatibility receipt reader; no overwrite, replacement, lineage, publication, Move expansion, or browser selection change; and no literal Projects-to-Analysis branch.

SSP-4.3 remains the owner of the renamed main action, default sub-scope action, exact destination listing, preview/confirmation presentation, and result navigation from browser selection. Stop before SSP-4.3 approval.
