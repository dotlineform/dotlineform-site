---
doc_id: d-20260801-233534-239eab
title: "SSP-4 — Collection-Aware Document Copy Delivery"
added_date: "2026-08-01 23:35:34"
last_updated: "2026-08-07 09:58:55"
ui_status: done
summary: copy exact managed collections while preserving subject metadata through the existing independent-copy transformation
parent_id: d-20260802-194357-4d857b
viewable: true
---
# SSP-4 — Collection-Aware Document Copy Delivery

## Status And Purpose

SSP-4 is complete at its closeout review gate. The [collection-aware Copy sanity note](/docs/?scope=studio&doc=d-20260806-225724-aa5e6a) confirms the accepted spec and owner fit; [SSP-4.1 evidence](/docs/?scope=studio&doc=d-20260807-083338-7e4a91) records the configured collection and exact planner, [SSP-4.2 evidence](/docs/?scope=studio&doc=d-20260807-085247-4c2a9e) records exact collection apply, [SSP-4.3 evidence](/docs/?scope=studio&doc=d-20260807-093311-8f2c4a) records the main and sub-scope surfaces plus four-subject acceptance, and [SSP-4.4 evidence](/docs/?scope=studio&doc=d-20260807-095855-6c52de) records the full regression matrix, one reviewed public Analysis copy, durable authority transfer, and parked Roadmap closeout list.

The **Copy to…** workflow now starts from either the checked main Index selection or a registered exact sub-scope selection and operates between exact managed document collections. Either end can be a configured parent scope or one-level sub-scope.

The immediate workflow is:

```text
dotlineform/projects working documents
  -> an Analysis editorial sub-scope, initially non-viewable
  -> manual content cleanup and review
  -> viewable documents included by the existing Analysis Publish workflow
```

Folder notes, Work/Series writing, and generic notes can therefore remain in one working collection. Any of them can become an independent editorial copy; `folder_path`, `work_id`, or `series_id` subject metadata is retained unchanged when present, and no subject is required for Copy or later publication. The destination build owns its normalized read projection and any subject-driven public metadata. Copy does not interpret the subject.

## Accepted SSP-1 Transfer Input

Copy transforms only canonical front matter required to create an independent target document: fresh identity and dates, target placement, target-default viewability, and existing hierarchy rules. It retains the source values of `folder_path`, `work_id`, and `series_id` without normalizing, validating, translating, dropping, or using them for target eligibility. The source and target builders independently own derived `authoring_subject`, `subject_generation`, cue, evidence, association, and public projection products. Missing or unusable subject values remain non-gating and are interpreted only by those destination build owners.

The target collection rebuild derives its own normalized rows and private associations from the copied canonical source. A read-only Analysis target recognizes retained Folder, Work, or Series subject metadata while its capability set excludes the `authoring_subject` assignable group; Info and cues may present retained metadata while focused assignment remains available only to collections that declare the group. If the author later invokes SSP-3, the Source action reads that exact current subject and inserts one snapshot link; Copy neither invokes nor prepares it. SSP-4 owns exact structural copy rules and the explicit creation of `analysis/works`.

## Complete Outcome

In Manage mode, **Copy to…** is available from checked main-Index documents and checked sub-scope documents. Its picker lists every exact configured collection that can accept the proposed copy. A target may be `{scope}` or `{scope, sub_scope}`; a sub-scope source is likewise carried explicitly rather than inferred from the report host or current URL.

Preview and confirmed apply preserve the existing independent-copy guarantees: fresh immutable document identities, create-only writes, target-owned default viewability, bounded media transfer, internal-link remapping, stale receipt revalidation, one coordinated target rebuild, and an exact result URL.

The normal `dotlineform/projects`-to-`analysis/works` copy creates flat, non-viewable Analysis sub-scope documents and retains any `folder_path`, `work_id`, or `series_id` source value unchanged. Their absence or unusable value does not block Copy. The user can then edit the independent copies, make reviewed documents viewable, and use the existing parent Analysis **Publish** action. SSP-4 does not automate editorial review or publication, decide which subject relationships appear, or couple later source-subject edits to the independent copy.

[SSP-7 — Document Lineage And Publication State Delivery](/docs/?scope=studio&doc=d-20260805-231829-947705) later extends the configured working-to-editorial path with one persisted exact A-to-B table, an explicit New-or-Replace choice, successful-Publish reconciliation, and publication-state cues. SSP-4 remains the create-only New-copy foundation and does not pre-empt SSP-7's overwrite or lineage authority.

## Fixed Contracts

### Exact Collection Identity

- Source and target are exact collection records: `{scope}` or `{scope, sub_scope}`. Checked source documents add `doc_ids` to that frozen source identity.
- No route text, report host, displayed document, active parent, filename, or selected row is a fallback target.
- A source and target may share the same parent scope when their exact collections differ. Copy to the same exact collection is rejected.
- Only configured one-level sub-scopes participate. SSP-4 does not introduce nested collections or arbitrary source directories.
- Collection eligibility is server-owned and based on the exact source root, target root, provider capabilities, and selected operation. An eligible managed child of a public scope can accept non-viewable canonical source without making every public parent scope a transfer target.
- Copy planning, actions, and apply contain no scope/sub-scope name branches. Configuration and projected capabilities determine eligible exact collections; the Projects-to-Analysis path is one normal use of the generic workflow.

### Flat And Hierarchical Sources

- Existing parent-scope-to-parent-scope Copy retains its optional descendant closure, included hierarchy, and target-root placement.
- Sub-scope sources are flat. Their Copy surface has no descendant option and every selected document becomes an independent target root.
- A sub-scope target is also flat. The planner rejects descendant inclusion or a source selection containing an internal parent/child relationship; it never silently flattens a hierarchy.
- Copy remains based only on the exact checked set after those declared expansion rules. It never falls back to the active document.

### Identity, Viewability, And Publication

- Every copied document receives a fresh immutable `doc_id`, matching filename, copy-time `added_date`, and copy-time `last_updated`.
- Source identity, source bytes, and source media are unchanged.
- Source `viewable` is not carried across. The copy uses the target collection's existing default derived from its owning scope. A copy into an Analysis editorial sub-scope is therefore non-viewable until explicitly changed.
- SSP-4 does not add a new publishing action or publish automatically. Public sub-scope output remains owned by the parent scope's existing Publish preview, confirmation, gate, and apply workflow.

### Subject Preservation And Target-Supported Custom Metadata

- Generic document front matter continues to follow the shared Copy transformation. Customisation-owned front matter follows registered transfer support, not a field-name exception or an interpretation of the field's meaning.
- The three canonical authoring-subject fields are a deliberate shared transfer contract even though their assignment UI and consumer projections remain collection-specific. Copy retains their raw source values and does not decide whether the target will normalize or project them.
- A registered sub-scope customisation definition must explicitly declare the canonical front-matter fields it owns and a stable transfer-compatibility contract. SSP-4.1 implements the smallest transfer declaration on SSC-1's accepted aspect seam and reviews it at that step's checkpoint.
- A source-owned custom field is copied only when the target collection advertises the same registered transfer contract and accepts the value under its current settings.
- When the target does not support that contract, the field is omitted. The preview, apply receipt, and result report the exact document, field, source customisation, and omission reason.
- When the target advertises the contract but rejects the current value, preview is blocked. Apply never silently drops a value that the target claims to support.
- `folder_path`, `work_id`, and `series_id` are shared optional document metadata for transfer. Copy retains their source values without using them as eligibility, destination, or blocker inputs; SSP-1 owns current normalization, SSP-3 owns explicit subject-link insertion, and SSP-5 owns exact subject-driven public reverse-association behavior.
- SSP-4 does not infer compatibility from matching field names and does not translate between customisation meanings.

### Links, Media, And Rebuild

- Links between documents in the copied set are remapped whether the source or target uses a normal `doc` URL or a report-host `subdoc` URL. Links outside the copied set remain unchanged and are reported under the existing retained dependency rules.
- Document roots belong to the exact source and target collections. Published media and registered build-media sources remain owned by their parent scopes and use the current provider-neutral collision and byte-verification rules.
- A parent target receives the existing scope rebuild and targeted search follow-through. A child target receives the existing sub-scope rebuild only; its documents remain excluded from the parent Index, Recent, and global search.
- A successful result returns exact target document URLs. Parent targets use `doc`; child targets use the configured report host plus `subdoc`.

### Preview, Receipt, And Apply

- Preview is write-free and returns a bounded plan only when no blocker exists.
- The receipt records exact source and target collections, document and media digests, copied identities, retained and omitted custom metadata, link decisions, configuration revisions, and the intended rebuild owner.
- The owned browser and server move together to one current receipt schema. No alias or old-client compatibility bridge is introduced.
- Apply requires explicit confirmation, restores the receipt, and revalidates every bounded fact before writing. Documents remain create-only and are never overwritten.
- Media completion still precedes canonical document writes. Post-write failures return exact observed evidence and do not claim success or retry the mutation implicitly.

## Likely Authority To Inspect

This is a routing map for delivery work, not a required entry audit or fixed file-change list.

| boundary | likely current owners | delivery pressure |
| --- | --- | --- |
| Main Index Copy action | management Index controller, action definitions, capability reader, and document-transfer workflow | replace scope-only targets with exact collection choices without changing checked-ID ownership |
| Sub-scope Copy action | default Manage sub-scope contribution, selection registrar, and management composition callbacks | add one checked-selection action beside Prepare Package using the existing exact `{scope, sub_scope, doc_ids}` target |
| Exact collection resolution and URLs | `docs_management_document_target.py` and `docs_document_location.py` | reuse confined collection roots and canonical parent/child result URLs |
| Target capability projection | management capability service and transfer capability model | project parent and child copy eligibility separately; do not globally relax public-scope policy |
| Planning and receipt | `docs_document_transfer.py`, management service, and transfer routes | carry exact collections, flat-child constraints, metadata decisions, and configuration/rebuild evidence |
| Copy transformation and apply | `docs_document_transfer_apply.py` and `docs_write_rebuild.py` | use collection roots, `doc`/`subdoc` rewriting, parent-owned media, and the correct coordinated rebuild |
| Custom metadata | unified sub-scope customisation registry and registered definitions | declare owned fields and transfer compatibility once, then validate against the target |
| Assurance | focused transfer, apply, capability, route, action, and sub-scope module tests | prove the collection matrix and preserve current top-level Copy behavior without browser-choreography assertions |

## Delivery Discipline

SSP-4.0 is a short scope-and-spec sanity gate over exact collection identity, planning/receipt, apply, custom metadata, route surfaces, and rebuild ownership. It confirms broad owners, blast radius, deliverables, risks, and stop conditions without freezing exact files, fixtures, estimates, or implementation. Each implementation stage ends with focused evidence and an independent code/diff review before the next stage begins.

## Delivery Steps

### SSP-4.0 — Confirm Scope And Delivery

- [x] Confirm SSC-1's accepted customisation and transfer-aspect seam, then sanity-check exact parent/sub-scope Copy against the accepted SSP-1 transfer contract and current selection, capability, preview/receipt/apply, link/media, result-navigation, and parent/child rebuild models; resolve only contradictions that would change the result.
- [x] Confirm the broad blast radius across exact collection targets, capability projection, flat/hierarchical planning, target-owned metadata/viewability, receipt revalidation, copy apply, route surfaces, and coordinated rebuilds.
- [x] Confirm the deliverables: exact collection planning and apply, unchanged Folder/Work/Series subject retention, structural target transformations, explicit non-subject custom-metadata decisions, correct links/media/rebuilds, main and sub-scope actions, focused evidence, and concise durable owner updates.
- [x] Stop for review if the result requires nested collections, a second transfer workflow, Move expansion, publication coupling, compatibility aliases, or another boundary excluded above. Do not produce an exact file inventory, line estimate, frozen implementation, fixture implementation, or complete test architecture at this gate.

**Checkpoint:** [SSP-4.0 evidence](/docs/?scope=studio&doc=d-20260806-225724-aa5e6a) confirms the spec, SSP-1 dependency, broad owners, blast radius, deliverables, risks, collection/publication boundary, and stop condition. SSP-4.0 is complete; stop before SSP-4.1 implementation approval.

### SSP-4.1 — Exact Collection Planner And Receipt

- [x] Create and configure the flat `analysis/works` editorial sub-scope through the existing Analysis sub-scope lifecycle/configuration owner, with non-viewable target defaults, normalized Folder/Work/Series read support for destination build consumers, and no assignable authoring-subject group.
- [x] Generalize Copy planning from top-level scopes to exact managed collections while keeping Move on its current parent-scope contract.
- [x] Add child-source inventory, flat-child validation, exact target capability evaluation, target-root allocation, and parent-owned media plans.
- [x] Extend customisation definitions with the reviewed ownership/transfer contract and project exact retained, omitted, rejected, and blocked non-subject fields while keeping `folder_path`, `work_id`, and `series_id` in the ordinary non-gating document transformation.
- [x] Issue and restore one new stale-safe Copy receipt containing the complete collection and custom-metadata plan without writing.

**Checkpoint:** [SSP-4.1 evidence](/docs/?scope=studio&doc=d-20260807-083338-7e4a91) passes the exact collection, flatness, capability, metadata, media-owner, link-decision, target-default, receipt-restoration, parent Copy/Move regression, configuration, and read-only subject matrix. The independent code/diff review confirms that child apply, UI, publication, lineage, and current parent Copy/Move behavior retain their existing owners and values. SSP-4.1 is complete; stop for approval before SSP-4.2.

### SSP-4.2 — Collection-Aware Copy Apply

- [x] Transform documents using target-default viewability, reviewed custom metadata decisions, and exact parent/child link remapping.
- [x] Transfer and verify parent-scope-owned media and registered build sources under the existing create/reuse/collision rules.
- [x] Create documents only in the exact target collection and invoke the correct coordinated parent or sub-scope rebuild.
- [x] Return exact target URLs and activity/failure evidence, then revalidate the complete receipt against stale source, target, config, metadata, media, and build-source changes.

**Checkpoint:** [SSP-4.2 evidence](/docs/?scope=studio&doc=d-20260807-085247-4c2a9e) proves byte-preserving source behavior, fresh target identities, exact subject-value retention, correct non-subject retention/omission/blocking, `doc`/`subdoc` links, target-only rebuild, full receipt revalidation, and contained partial failure. The independent code/diff review confirms writes remain create-only, receipt-bound, and exact-collection-owned while subject interpretation, browser surfaces, Move, and publication retain their existing owners. SSP-4.2 is complete; stop for approval before SSP-4.3.

### SSP-4.3 — Main And Sub-Scope Copy Surfaces

- [x] Rename the existing Manage action to **Copy to…** and retain its exact checked main-Index selection contract.
- [x] Add the same action to the default sub-scope selection Actions menu using the registered exact selection target and independent selection owner.
- [x] List writable parent and child collections with unambiguous labels, preserve server-owned capability truth, and prevent the current exact collection from being chosen.
- [x] Adapt the existing preview and confirmation workflow to collection placement, custom-metadata omissions, blockers, flat-child constraints, and exact result navigation.

**Checkpoint:** [SSP-4.3 evidence](/docs/?scope=studio&doc=d-20260807-093311-8f2c4a) passes focused capability, route, workflow, main/sub-scope selection, and exact navigation checks. Manual acceptance copied checked Work-subject, Series-subject, Folder-subject, and no-subject documents from `dotlineform/projects` into `analysis/works`; each fresh non-viewable `subdoc` retains the exact subject field/value when present, preserves no-subject state, and leaves its working source byte-identical. The independent code/diff review confirms exact selection ownership, server-owned collection eligibility, subject-preserving but subject-unaware Copy, the existing action workflow, and unchanged Move/publication authority. SSP-4.3 is complete; stop for approval before SSP-4.4.

### SSP-4.4 — Regression, Publication Acceptance, And Closeout

- [x] Re-run current parent-scope Copy planner/apply and main-Index action evidence to prove no regression.
- [x] Verify all four source/target collection shapes, same-parent child copy, unsupported/supported custom metadata, mixed links, media, target defaults, stale receipts, provider failures, and parent/child generated follow-through.
- [x] Manually clean one copied Analysis document, make it viewable, run the existing parent Analysis Publish preview/apply, and confirm only existing public-projection rules were used.
- [x] Run focused service, API, module, lint, whitespace, generated-projection, and public-isolation checks, then perform the mandatory end-of-delivery diff and omission review.
- [x] Update the durable document-transfer endpoint and sub-scope architecture owners with shipped behavior, then prepare the Roadmap closeout list.

**Checkpoint:** [SSP-4.4 evidence](/docs/?scope=studio&doc=d-20260807-095855-6c52de) records the green exact-collection regression matrix, existing-workflow public acceptance, public report-host activation correction, durable owner updates, final bounded diff/omission review, and explicit closeout list. SSP-4 is complete and parked; no delivery material is retired without user approval.

## Not In Scope

- Move from or into a sub-scope, or any change to Move's current hierarchy and source-removal guarantees;
- nested sub-scopes, arbitrary directories, cross-application documents, or a second transfer workflow;
- overwrite, persisted lineage, replacement, merge, synchronization, return import, or automatic source cleanup; exact lineage-bound Replace belongs to SSP-7 rather than this create-only delivery;
- semantic translation between different customisations or compatibility based only on identical field names;
- automatic editorial decisions, content redaction, folder-note assessment, viewability promotion, or publication;
- automatic Work-to-Series roll-up, Series-to-Work fan-out, or post-copy synchronization of subjects;
- relaxing all public top-level scopes into writable Copy targets;
- new media identity, provider, collision, build, publication, or rollback semantics;
- batch jobs, progress persistence, resumable queues, or multi-user locking; or
- compatibility aliases for the replaced scope-only Copy receipt.

## Completion Evidence

- The current parent-scope Copy workflow still produces the same independent copies, hierarchy, media, target rebuild, and exact result behavior.
- Parent and child sources can copy checked flat documents into eligible parent or child targets, including a different child of the same parent scope.
- The source and target are explicit in every browser request, server plan, receipt, apply check, result, and activity record.
- The configured `analysis/works` editorial collection is created as an explicit SSP-4 action and exposes retained Folder/Work/Series subjects read-only to destination build consumers while advertising no quick-assignment group.
- A Projects-to-Analysis child copy receives a fresh identity, is non-viewable, retains the exact `folder_path`, `work_id`, or `series_id` source value when present, opens through `subdoc`, and does not alter or remain synchronized with the working document.
- A Folder-subject copy retains `folder_path` without translating it into a catalogue subject. If explicitly invoked, SSP-3 inserts a Manage-capable `dlf-local:` snapshot whose public promotion becomes inert text; Copy and the destination build do not insert it automatically. A no-subject copy remains fully valid.
- A fixture customisation supported by both ends retains and validates its declared front matter; unsupported fields are visibly omitted and supported invalid values block before mutation.
- Child-target rebuilds do not add their documents to the parent Index, Recent, or global search, while the existing parent Publish workflow can later publish reviewed viewable child documents.
- Move, public runtime dependencies, publication policy, and unrelated selection owners remain unchanged.
- SSP-4 exposes the exact Copy mapping and configured collection boundary required by SSP-7 but persists no lineage table and replaces no existing target.

## Durable Documentation At Closeout

- [Create And Import Endpoints](/docs/?scope=studio&doc=d-20260607-222033-78a47c) owns the current exact collection Copy request, receipt, apply, result, capability, and surface contract.
- [Sub-Scope Architecture](/docs/?scope=studio&doc=d-20260731-155053-3929e2) owns the stable sub-scope Copy action, custom-metadata transfer rule, rebuild/navigation ownership, and public report-host activation rule.
- [Development Checklist](/docs/?scope=studio&doc=d-20260523-190651-7157ec) is unchanged because implementation established no new reusable authoring or safety rule.

SSP-4 is complete and parked at its closeout-list review gate. SSP-5, SSP-6, and SSP-7 remain unstarted, and no delivery or evidence document has been retired.
