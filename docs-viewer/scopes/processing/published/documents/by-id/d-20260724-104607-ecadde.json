{
  "doc_id": "d-20260724-104607-ecadde",
  "title": "Processing Blob Studio for Web Gallery Shapes",
  "added_date": "2026-07-24 10:46:07",
  "last_updated": "2026-07-24 10:46:07",
  "viewer_url": "/docs/?scope=processing&doc=d-20260724-104607-ecadde",
  "content_html": "<h1>Processing Blob Studio for Web Gallery Shapes</h1>\n<h2>Purpose</h2>\n<p>This guide describes an offline Processing tool for creating reusable organic SVG shapes for the website gallery.</p>\n<p>The tool lives inside the existing website repository:</p>\n<pre><code class=\"language-text\">dotlineform-site/\n├── processing/\n│   └── blob-studio/\n├── assets/\n├── docs/\n├── _works/\n└── ...\n</code></pre>\n<p>Processing is used only as an authoring and generation environment.</p>\n<p>The website runtime remains standard:</p>\n<ul>\n<li>HTML;</li>\n<li>CSS;</li>\n<li>SVG;</li>\n<li>JavaScript;</li>\n<li>JSON.</li>\n</ul>\n<p>The Processing tool generates clean SVG paths and shape metadata that the site can use for:</p>\n<ul>\n<li>clipping individual thumbnails;</li>\n<li>defining circular, elliptical or organic gallery boundaries;</li>\n<li>testing packed thumbnail positions;</li>\n<li>creating repeatable families of related forms;</li>\n<li>exporting deterministic shapes from saved seeds and parameters.</li>\n</ul>\n<hr />\n<h2>1. Recommended repository structure</h2>\n<p>A practical initial structure is:</p>\n<pre><code class=\"language-text\">dotlineform-site/\n├── processing/\n│   └── blob-studio/\n│       ├── BlobStudio.pde\n│       ├── Blob.pde\n│       ├── BlobGenerator.pde\n│       ├── BlobEditor.pde\n│       ├── BlobPreset.pde\n│       ├── SvgPathWriter.pde\n│       ├── ShapeRegistryWriter.pde\n│       ├── data/\n│       │   └── preview-image.webp\n│       ├── presets/\n│       │   ├── blob-001.json\n│       │   └── blob-002.json\n│       └── exports/\n│           ├── svg/\n│           ├── json/\n│           └── registry/\n├── assets/\n│   ├── shapes/\n│   │   ├── shape-registry.json\n│   │   └── gallery-shapes.svg\n│   └── ...\n└── docs/\n    └── processing-blob-studio.md\n</code></pre>\n<p>The <code>processing/</code> directory contains authoring tools that are not part of the public Jekyll output.</p>\n<p>If necessary, explicitly exclude it from Jekyll:</p>\n<pre><code class=\"language-yaml\">exclude:\n  - processing\n</code></pre>\n<hr />\n<h2>2. Processing’s role</h2>\n<p>Processing should be responsible for:</p>\n<ul>\n<li>seeded procedural shape generation;</li>\n<li>interactive parameter controls;</li>\n<li>manual point and handle editing;</li>\n<li>previewing the shape with a real artwork thumbnail;</li>\n<li>previewing many repeated shapes;</li>\n<li>testing a form as a gallery boundary;</li>\n<li>exporting SVG;</li>\n<li>exporting normalised path data;</li>\n<li>saving editable presets;</li>\n<li>generating or updating a site shape registry.</li>\n</ul>\n<p>Processing should not be required by the deployed website.</p>\n<p>The workflow is:</p>\n<pre><code class=\"language-text\">Processing Blob Studio\n        ↓\nSVG + JSON preset\n        ↓\nshape registry\n        ↓\nwebsite build/runtime\n        ↓\nCSS/SVG clipping and JavaScript layout\n</code></pre>\n<hr />\n<h2>3. Recommended Blob Studio features</h2>\n<h3>Minimum viable version</h3>\n<p>The first version should support:</p>\n<ul>\n<li>seed;</li>\n<li>point count;</li>\n<li>base radius;</li>\n<li>irregularity;</li>\n<li>smoothness;</li>\n<li>horizontal scale;</li>\n<li>vertical scale;</li>\n<li>rotation;</li>\n<li>regenerate;</li>\n<li>save preset;</li>\n<li>export SVG;</li>\n<li>export JSON;</li>\n<li>copy SVG path.</li>\n</ul>\n<h3>Useful later additions</h3>\n<p>Later versions could add:</p>\n<ul>\n<li>draggable anchor points;</li>\n<li>draggable Bézier handles;</li>\n<li>concavity control;</li>\n<li>radial symmetry control;</li>\n<li>point locking;</li>\n<li>path simplification;</li>\n<li>path normalisation;</li>\n<li>thumbnail crop preview;</li>\n<li>repeated gallery preview;</li>\n<li>custom gallery-boundary preview;</li>\n<li>packing preview;</li>\n<li>batch generation;</li>\n<li>shape family generation;</li>\n<li>registry management;</li>\n<li>favourite shapes;</li>\n<li>comparison view;</li>\n<li>SVG import and re-editing.</li>\n</ul>\n<hr />\n<h2>4. Suggested interface</h2>\n<pre><code class=\"language-text\">┌──────────────────────────────────────────────────────────┐\n│ Blob Studio                                              │\n├──────────────────────────────────────────────────────────┤\n│                                                          │\n│                    shape preview                         │\n│                                                          │\n├───────────────────────────────┬──────────────────────────┤\n│ Seed              [ 14827 ]   │ Preview mode             │\n│ Points            [ 8 ]       │ ○ Solid                  │\n│ Irregularity      [────●──]   │ ○ Image crop             │\n│ Smoothness        [─────●─]   │ ○ Repeated gallery       │\n│ Concavity         [──●────]   │ ○ Boundary               │\n│ Scale X           [────●──]   │ ○ Packing                │\n│ Scale Y           [───●───]   │                          │\n│ Rotation          [─●─────]   │                          │\n├───────────────────────────────┴──────────────────────────┤\n│ [Regenerate] [Save preset] [Export SVG] [Export JSON]    │\n│ [Copy path] [Update registry]                            │\n└──────────────────────────────────────────────────────────┘\n</code></pre>\n<p>A generated form should be editable before export.</p>\n<hr />\n<h2>5. Shape data model</h2>\n<p>A blob can be represented as a sequence of anchors and Bézier controls.</p>\n<pre><code class=\"language-java\">class BlobSegment {\n  PVector anchor;\n  PVector controlIn;\n  PVector controlOut;\n\n  BlobSegment(\n    PVector anchor,\n    PVector controlIn,\n    PVector controlOut\n  ) {\n    this.anchor = anchor;\n    this.controlIn = controlIn;\n    this.controlOut = controlOut;\n  }\n}\n</code></pre>\n<p>The complete form stores its generator parameters and path segments.</p>\n<pre><code class=\"language-java\">class Blob {\n  String shapeId;\n\n  int seed;\n  int pointCount;\n\n  float irregularity;\n  float smoothness;\n  float scaleX;\n  float scaleY;\n  float rotationDeg;\n\n  ArrayList&lt;BlobSegment&gt; segments;\n\n  Blob() {\n    segments = new ArrayList&lt;BlobSegment&gt;();\n  }\n}\n</code></pre>\n<p>This data structure should drive:</p>\n<ul>\n<li>the on-screen preview;</li>\n<li>Processing SVG export;</li>\n<li>SVG path-string export;</li>\n<li>JSON preset export;</li>\n<li>normalised website paths.</li>\n</ul>\n<hr />\n<h2>6. Seeded generation</h2>\n<p>A deterministic seed ensures that a shape can be regenerated exactly.</p>\n<pre><code class=\"language-java\">void generateBlob(Blob blob) {\n  randomSeed(blob.seed);\n  noiseSeed(blob.seed);\n\n  blob.segments.clear();\n\n  ArrayList&lt;PVector&gt; anchors =\n    createRadialAnchors(\n      blob.pointCount,\n      width * 0.5,\n      height * 0.5,\n      min(width, height) * 0.38,\n      blob.irregularity\n    );\n\n  createBezierSegments(\n    blob,\n    anchors,\n    blob.smoothness\n  );\n}\n</code></pre>\n<p>A basic radial anchor generator:</p>\n<pre><code class=\"language-java\">ArrayList&lt;PVector&gt; createRadialAnchors(\n  int count,\n  float centreX,\n  float centreY,\n  float baseRadius,\n  float irregularity\n) {\n  ArrayList&lt;PVector&gt; points =\n    new ArrayList&lt;PVector&gt;();\n\n  for (int index = 0; index &lt; count; index++) {\n    float angle =\n      TWO_PI * index / count;\n\n    float variation =\n      1.0 +\n      random(\n        -irregularity,\n        irregularity\n      );\n\n    float radius =\n      baseRadius * variation;\n\n    float x =\n      centreX +\n      cos(angle) * radius;\n\n    float y =\n      centreY +\n      sin(angle) * radius;\n\n    points.add(\n      new PVector(x, y)\n    );\n  }\n\n  return points;\n}\n</code></pre>\n<p>A more refined version could combine:</p>\n<ul>\n<li>seeded random values;</li>\n<li>Perlin noise;</li>\n<li>radial constraints;</li>\n<li>minimum point spacing;</li>\n<li>concavity limits;</li>\n<li>symmetry controls.</li>\n</ul>\n<hr />\n<h2>7. Bézier control generation</h2>\n<p>A smooth closed path can be constructed by calculating tangents around each anchor.</p>\n<pre><code class=\"language-java\">void createBezierSegments(\n  Blob blob,\n  ArrayList&lt;PVector&gt; anchors,\n  float smoothness\n) {\n  int count = anchors.size();\n\n  for (int index = 0; index &lt; count; index++) {\n    PVector previous =\n      anchors.get(\n        (index - 1 + count) % count\n      );\n\n    PVector current =\n      anchors.get(index);\n\n    PVector next =\n      anchors.get(\n        (index + 1) % count\n      );\n\n    PVector tangent =\n      PVector.sub(next, previous);\n\n    tangent.normalize();\n\n    float previousDistance =\n      PVector.dist(current, previous);\n\n    float nextDistance =\n      PVector.dist(current, next);\n\n    float controlLength =\n      min(\n        previousDistance,\n        nextDistance\n      ) *\n      smoothness *\n      0.5;\n\n    PVector controlIn =\n      PVector.sub(\n        current,\n        PVector.mult(\n          tangent,\n          controlLength\n        )\n      );\n\n    PVector controlOut =\n      PVector.add(\n        current,\n        PVector.mult(\n          tangent,\n          controlLength\n        )\n      );\n\n    blob.segments.add(\n      new BlobSegment(\n        current.copy(),\n        controlIn,\n        controlOut\n      )\n    );\n  }\n}\n</code></pre>\n<p>This provides a usable baseline. Manual handle editing can later override the generated controls.</p>\n<hr />\n<h2>8. Drawing the blob in Processing</h2>\n<pre><code class=\"language-java\">void drawBlob(Blob blob) {\n  if (blob.segments.isEmpty()) {\n    return;\n  }\n\n  pushMatrix();\n\n  translate(\n    width * 0.5,\n    height * 0.5\n  );\n\n  rotate(\n    radians(blob.rotationDeg)\n  );\n\n  scale(\n    blob.scaleX,\n    blob.scaleY\n  );\n\n  translate(\n    -width * 0.5,\n    -height * 0.5\n  );\n\n  beginShape();\n\n  BlobSegment first =\n    blob.segments.get(0);\n\n  vertex(\n    first.anchor.x,\n    first.anchor.y\n  );\n\n  for (\n    int index = 1;\n    index &lt;= blob.segments.size();\n    index++\n  ) {\n    BlobSegment previous =\n      blob.segments.get(\n        (index - 1) %\n        blob.segments.size()\n      );\n\n    BlobSegment current =\n      blob.segments.get(\n        index %\n        blob.segments.size()\n      );\n\n    bezierVertex(\n      previous.controlOut.x,\n      previous.controlOut.y,\n\n      current.controlIn.x,\n      current.controlIn.y,\n\n      current.anchor.x,\n      current.anchor.y\n    );\n  }\n\n  endShape(CLOSE);\n\n  popMatrix();\n}\n</code></pre>\n<p>The same segment structure should be used for both display and export.</p>\n<hr />\n<h2>9. Previewing a thumbnail inside the blob</h2>\n<p>A real site thumbnail can be loaded into Processing:</p>\n<pre><code class=\"language-java\">PImage previewImage;\n\nvoid setup() {\n  size(900, 900);\n\n  previewImage =\n    loadImage(&quot;preview-image.webp&quot;);\n}\n</code></pre>\n<p>There are several ways to preview the crop.</p>\n<p>A simple first implementation is:</p>\n<ol>\n<li>render the image into an off-screen <code>PGraphics</code>;</li>\n<li>render the blob into a mask buffer;</li>\n<li>apply the mask;</li>\n<li>draw the result.</li>\n</ol>\n<pre><code class=\"language-java\">PImage createMaskedPreview(\n  PImage source,\n  Blob blob\n) {\n  PGraphics imageBuffer =\n    createGraphics(width, height);\n\n  PGraphics maskBuffer =\n    createGraphics(width, height);\n\n  imageBuffer.beginDraw();\n  imageBuffer.background(0, 0);\n  imageBuffer.imageMode(CENTER);\n  imageBuffer.image(\n    source,\n    width * 0.5,\n    height * 0.5,\n    width,\n    height\n  );\n  imageBuffer.endDraw();\n\n  maskBuffer.beginDraw();\n  maskBuffer.background(0);\n  maskBuffer.fill(255);\n  maskBuffer.noStroke();\n  drawBlobInto(maskBuffer, blob);\n  maskBuffer.endDraw();\n\n  PImage result =\n    imageBuffer.get();\n\n  result.mask(\n    maskBuffer.get()\n  );\n\n  return result;\n}\n</code></pre>\n<p>The implementation details can be refined by Codex, but the important point is that Blob Studio should preview actual artwork rather than only solid black shapes.</p>\n<hr />\n<h2>10. SVG export with Processing</h2>\n<p>Processing’s SVG renderer can export the visible vector form.</p>\n<pre><code class=\"language-java\">import processing.svg.*;\n</code></pre>\n<pre><code class=\"language-java\">void exportProcessingSvg(\n  Blob blob,\n  String filename\n) {\n  beginRecord(\n    SVG,\n    filename\n  );\n\n  background(255);\n  noStroke();\n  fill(0);\n\n  drawBlob(blob);\n\n  endRecord();\n}\n</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-java\">exportProcessingSvg(\n  currentBlob,\n  &quot;exports/svg/blob-001.svg&quot;\n);\n</code></pre>\n<p>This is useful as a visual master and for opening in Inkscape or another SVG editor.</p>\n<p>However, the website should preferably use a deliberately generated clean path string rather than depending on the exact structure of Processing’s complete SVG output.</p>\n<hr />\n<h2>11. Direct SVG path generation</h2>\n<p>A path string can be generated directly from the segment data.</p>\n<pre><code class=\"language-java\">String toSvgPath(Blob blob) {\n  if (blob.segments.isEmpty()) {\n    return &quot;&quot;;\n  }\n\n  StringBuilder path =\n    new StringBuilder();\n\n  BlobSegment first =\n    blob.segments.get(0);\n\n  path.append(\n    &quot;M &quot; +\n    formatNumber(first.anchor.x) +\n    &quot; &quot; +\n    formatNumber(first.anchor.y)\n  );\n\n  for (\n    int index = 1;\n    index &lt;= blob.segments.size();\n    index++\n  ) {\n    BlobSegment previous =\n      blob.segments.get(\n        (index - 1) %\n        blob.segments.size()\n      );\n\n    BlobSegment current =\n      blob.segments.get(\n        index %\n        blob.segments.size()\n      );\n\n    path.append(\n      &quot; C &quot; +\n      formatNumber(previous.controlOut.x) +\n      &quot; &quot; +\n      formatNumber(previous.controlOut.y) +\n      &quot;, &quot; +\n      formatNumber(current.controlIn.x) +\n      &quot; &quot; +\n      formatNumber(current.controlIn.y) +\n      &quot;, &quot; +\n      formatNumber(current.anchor.x) +\n      &quot; &quot; +\n      formatNumber(current.anchor.y)\n    );\n  }\n\n  path.append(&quot; Z&quot;);\n\n  return path.toString();\n}\n</code></pre>\n<pre><code class=\"language-java\">String formatNumber(float value) {\n  return nf(value, 0, 3);\n}\n</code></pre>\n<hr />\n<h2>12. Normalised website path</h2>\n<p>The site should use coordinates ranging from <code>0</code> to <code>1</code>.</p>\n<p>This allows the SVG clipping path to use:</p>\n<pre><code class=\"language-svg\">clipPathUnits=&quot;objectBoundingBox&quot;\n</code></pre>\n<p>Normalisation requires a known source coordinate space.</p>\n<p>For example, if Blob Studio uses a <code>1000 × 1000</code> export canvas:</p>\n<pre><code class=\"language-java\">final float EXPORT_SIZE =\n  1000.0;\n</code></pre>\n<pre><code class=\"language-java\">float normaliseCoordinate(float value) {\n  return value / EXPORT_SIZE;\n}\n</code></pre>\n<p>A normalised path writer:</p>\n<pre><code class=\"language-java\">String toNormalisedSvgPath(Blob blob) {\n  if (blob.segments.isEmpty()) {\n    return &quot;&quot;;\n  }\n\n  StringBuilder path =\n    new StringBuilder();\n\n  BlobSegment first =\n    blob.segments.get(0);\n\n  path.append(\n    &quot;M &quot; +\n    formatNormalised(first.anchor.x) +\n    &quot; &quot; +\n    formatNormalised(first.anchor.y)\n  );\n\n  for (\n    int index = 1;\n    index &lt;= blob.segments.size();\n    index++\n  ) {\n    BlobSegment previous =\n      blob.segments.get(\n        (index - 1) %\n        blob.segments.size()\n      );\n\n    BlobSegment current =\n      blob.segments.get(\n        index %\n        blob.segments.size()\n      );\n\n    path.append(\n      &quot; C &quot; +\n      formatNormalised(\n        previous.controlOut.x\n      ) +\n      &quot; &quot; +\n      formatNormalised(\n        previous.controlOut.y\n      ) +\n      &quot;, &quot; +\n      formatNormalised(\n        current.controlIn.x\n      ) +\n      &quot; &quot; +\n      formatNormalised(\n        current.controlIn.y\n      ) +\n      &quot;, &quot; +\n      formatNormalised(\n        current.anchor.x\n      ) +\n      &quot; &quot; +\n      formatNormalised(\n        current.anchor.y\n      )\n    );\n  }\n\n  path.append(&quot; Z&quot;);\n\n  return path.toString();\n}\n</code></pre>\n<pre><code class=\"language-java\">String formatNormalised(float value) {\n  return nf(\n    value / EXPORT_SIZE,\n    0,\n    6\n  );\n}\n</code></pre>\n<p>A typical result:</p>\n<pre><code class=\"language-svg\">M 0.500000 0.030000\nC 0.780000 0.050000,\n  0.960000 0.270000,\n  0.920000 0.570000\n...\nZ\n</code></pre>\n<hr />\n<h2>13. Standalone SVG output</h2>\n<p>Blob Studio should export a clean standalone SVG:</p>\n<pre><code class=\"language-svg\">&lt;svg\n  xmlns=&quot;http://www.w3.org/2000/svg&quot;\n  viewBox=&quot;0 0 1000 1000&quot;\n&gt;\n  &lt;path\n    d=&quot;M 500 30 C ...&quot;\n    fill=&quot;#000&quot;\n  /&gt;\n&lt;/svg&gt;\n</code></pre>\n<p>A direct writer avoids unnecessary metadata.</p>\n<pre><code class=\"language-java\">void saveStandaloneSvg(\n  Blob blob,\n  String filename\n) {\n  String[] lines = {\n    &quot;&lt;svg&quot;,\n    &quot;  xmlns=\\&quot;http://www.w3.org/2000/svg\\&quot;&quot;,\n    &quot;  viewBox=\\&quot;0 0 1000 1000\\&quot;&quot;,\n    &quot;&gt;&quot;,\n    &quot;  &lt;path&quot;,\n    &quot;    d=\\&quot;&quot; + toSvgPath(blob) + &quot;\\&quot;&quot;,\n    &quot;    fill=\\&quot;#000\\&quot;&quot;,\n    &quot;  /&gt;&quot;,\n    &quot;&lt;/svg&gt;&quot;\n  };\n\n  saveStrings(\n    filename,\n    lines\n  );\n}\n</code></pre>\n<hr />\n<h2>14. Web-ready <code>&lt;clipPath&gt;</code> output</h2>\n<p>Blob Studio can also export a snippet ready for the site:</p>\n<pre><code class=\"language-svg\">&lt;clipPath\n  id=&quot;blob-001&quot;\n  clipPathUnits=&quot;objectBoundingBox&quot;\n&gt;\n  &lt;path d=&quot;M 0.500000 0.030000 C ...&quot; /&gt;\n&lt;/clipPath&gt;\n</code></pre>\n<pre><code class=\"language-java\">void saveClipPathSnippet(\n  Blob blob,\n  String filename\n) {\n  String[] lines = {\n    &quot;&lt;clipPath&quot;,\n    &quot;  id=\\&quot;&quot; + blob.shapeId + &quot;\\&quot;&quot;,\n    &quot;  clipPathUnits=\\&quot;objectBoundingBox\\&quot;&quot;,\n    &quot;&gt;&quot;,\n    &quot;  &lt;path d=\\&quot;&quot; +\n      toNormalisedSvgPath(blob) +\n      &quot;\\&quot; /&gt;&quot;,\n    &quot;&lt;/clipPath&gt;&quot;\n  };\n\n  saveStrings(\n    filename,\n    lines\n  );\n}\n</code></pre>\n<hr />\n<h2>15. JSON preset export</h2>\n<p>The preset should preserve both the generator settings and the final path.</p>\n<pre><code class=\"language-java\">void saveBlobPreset(\n  Blob blob,\n  String filename\n) {\n  JSONObject preset =\n    new JSONObject();\n\n  preset.setString(\n    &quot;shape_id&quot;,\n    blob.shapeId\n  );\n\n  preset.setInt(\n    &quot;seed&quot;,\n    blob.seed\n  );\n\n  preset.setInt(\n    &quot;point_count&quot;,\n    blob.pointCount\n  );\n\n  preset.setFloat(\n    &quot;irregularity&quot;,\n    blob.irregularity\n  );\n\n  preset.setFloat(\n    &quot;smoothness&quot;,\n    blob.smoothness\n  );\n\n  preset.setFloat(\n    &quot;scale_x&quot;,\n    blob.scaleX\n  );\n\n  preset.setFloat(\n    &quot;scale_y&quot;,\n    blob.scaleY\n  );\n\n  preset.setFloat(\n    &quot;rotation_deg&quot;,\n    blob.rotationDeg\n  );\n\n  preset.setString(\n    &quot;svg_path&quot;,\n    toSvgPath(blob)\n  );\n\n  preset.setString(\n    &quot;normalised_path&quot;,\n    toNormalisedSvgPath(blob)\n  );\n\n  JSONArray viewBox =\n    new JSONArray();\n\n  viewBox.setInt(0, 0);\n  viewBox.setInt(1, 0);\n  viewBox.setInt(2, 1000);\n  viewBox.setInt(3, 1000);\n\n  preset.setJSONArray(\n    &quot;view_box&quot;,\n    viewBox\n  );\n\n  saveJSONObject(\n    preset,\n    filename,\n    &quot;indent=2&quot;\n  );\n}\n</code></pre>\n<p>Example output:</p>\n<pre><code class=\"language-json\">{\n  &quot;shape_id&quot;: &quot;blob-001&quot;,\n  &quot;seed&quot;: 14827,\n  &quot;point_count&quot;: 8,\n  &quot;irregularity&quot;: 0.32,\n  &quot;smoothness&quot;: 0.75,\n  &quot;scale_x&quot;: 1.0,\n  &quot;scale_y&quot;: 0.86,\n  &quot;rotation_deg&quot;: 12.0,\n  &quot;view_box&quot;: [0, 0, 1000, 1000],\n  &quot;svg_path&quot;: &quot;M 500.000 30.000 C ... Z&quot;,\n  &quot;normalised_path&quot;: &quot;M 0.500000 0.030000 C ... Z&quot;\n}\n</code></pre>\n<hr />\n<h2>16. Site shape registry</h2>\n<p>The website should not need to load many unrelated SVG files individually.</p>\n<p>Instead, selected shapes can be collected into a registry:</p>\n<pre><code class=\"language-json\">{\n  &quot;version&quot;: 1,\n  &quot;generated_at_utc&quot;: &quot;2026-07-20T12:00:00Z&quot;,\n  &quot;shapes&quot;: [\n    {\n      &quot;shape_id&quot;: &quot;blob-001&quot;,\n      &quot;label&quot;: &quot;Rounded asymmetric&quot;,\n      &quot;type&quot;: &quot;blob&quot;,\n      &quot;normalised_path&quot;: &quot;M 0.500000 0.030000 C ... Z&quot;\n    },\n    {\n      &quot;shape_id&quot;: &quot;leaf-001&quot;,\n      &quot;label&quot;: &quot;Broad leaf&quot;,\n      &quot;type&quot;: &quot;leaf&quot;,\n      &quot;normalised_path&quot;: &quot;M 0.050000 0.550000 C ... Z&quot;\n    }\n  ]\n}\n</code></pre>\n<p>Suggested site location:</p>\n<pre><code class=\"language-text\">assets/shapes/shape-registry.json\n</code></pre>\n<p>The registry could be updated by:</p>\n<ul>\n<li>Blob Studio directly;</li>\n<li>a small Python build script;</li>\n<li>the existing site build pipeline;</li>\n<li>a dedicated command run after exporting shapes.</li>\n</ul>\n<p>For reliability, a build script that reads selected presets and generates the public registry is preferable to letting Blob Studio rewrite site files unpredictably.</p>\n<hr />\n<h2>17. Shared site SVG definitions</h2>\n<p>The registry can be converted into one shared SVG definition block:</p>\n<pre><code class=\"language-html\">&lt;svg\n  width=&quot;0&quot;\n  height=&quot;0&quot;\n  aria-hidden=&quot;true&quot;\n  focusable=&quot;false&quot;\n&gt;\n  &lt;defs&gt;\n    &lt;clipPath\n      id=&quot;blob-001&quot;\n      clipPathUnits=&quot;objectBoundingBox&quot;\n    &gt;\n      &lt;path d=&quot;M 0.500000 0.030000 C ... Z&quot; /&gt;\n    &lt;/clipPath&gt;\n\n    &lt;clipPath\n      id=&quot;leaf-001&quot;\n      clipPathUnits=&quot;objectBoundingBox&quot;\n    &gt;\n      &lt;path d=&quot;M 0.050000 0.550000 C ... Z&quot; /&gt;\n    &lt;/clipPath&gt;\n  &lt;/defs&gt;\n&lt;/svg&gt;\n</code></pre>\n<p>The generated file might be:</p>\n<pre><code class=\"language-text\">assets/shapes/gallery-shapes.svg\n</code></pre>\n<p>Alternatively, the build process can insert the definitions inline into the relevant page or shell.</p>\n<p>Inline definitions are generally easiest to reference with:</p>\n<pre><code class=\"language-css\">clip-path: url(&quot;#blob-001&quot;);\n</code></pre>\n<hr />\n<h2>18. Applying generated shapes on the site</h2>\n<pre><code class=\"language-css\">.gallery-item[data-shape=&quot;blob-001&quot;] {\n  clip-path: url(&quot;#blob-001&quot;);\n}\n\n.gallery-item[data-shape=&quot;leaf-001&quot;] {\n  clip-path: url(&quot;#leaf-001&quot;);\n}\n</code></pre>\n<pre><code class=\"language-html\">&lt;a\n  class=&quot;gallery-item&quot;\n  data-shape=&quot;blob-001&quot;\n  href=&quot;/works/example/&quot;\n&gt;\n  &lt;img\n    src=&quot;/assets/works/img/example-thumb-480.webp&quot;\n    alt=&quot;Example work&quot;\n  &gt;\n&lt;/a&gt;\n</code></pre>\n<p>The original image remains rectangular or square.</p>\n<p>The SVG shape only changes the visible crop.</p>\n<hr />\n<h2>19. Shape assignment</h2>\n<p>Shapes can be assigned in several ways.</p>\n<h3>Repeating sequence</h3>\n<pre><code class=\"language-js\">const shapeIds = [\n  &quot;blob-001&quot;,\n  &quot;blob-002&quot;,\n  &quot;leaf-001&quot;,\n  &quot;droplet-001&quot;,\n];\n\nitems.forEach((item, index) =&gt; {\n  item.dataset.shape =\n    shapeIds[index % shapeIds.length];\n});\n</code></pre>\n<h3>Stable assignment by work ID</h3>\n<pre><code class=\"language-js\">function hashString(value) {\n  let hash = 0;\n\n  for (\n    let index = 0;\n    index &lt; value.length;\n    index++\n  ) {\n    hash =\n      ((hash &lt;&lt; 5) - hash) +\n      value.charCodeAt(index);\n\n    hash |= 0;\n  }\n\n  return Math.abs(hash);\n}\n</code></pre>\n<pre><code class=\"language-js\">function assignStableShape(\n  item,\n  shapeIds\n) {\n  const workId =\n    item.dataset.workId ?? &quot;&quot;;\n\n  const shapeIndex =\n    hashString(workId) %\n    shapeIds.length;\n\n  item.dataset.shape =\n    shapeIds[shapeIndex];\n}\n</code></pre>\n<p>This produces repeatable variation between visits.</p>\n<hr />\n<h2>20. Shapes as gallery boundaries</h2>\n<p>A generated shape can also define the whole gallery boundary:</p>\n<pre><code class=\"language-css\">.gallery[data-boundary=&quot;blob-001&quot;] {\n  clip-path: url(&quot;#blob-001&quot;);\n}\n</code></pre>\n<p>However, visually clipping the gallery is not sufficient if thumbnails must remain fully inside the boundary.</p>\n<p>The JavaScript packing algorithm should use the same path to test candidate positions.</p>\n<p>This gives Blob Studio an additional useful preview mode:</p>\n<ul>\n<li>show the shape as the outer boundary;</li>\n<li>generate candidate thumbnail positions;</li>\n<li>mark valid and invalid positions;</li>\n<li>estimate capacity;</li>\n<li>compare different thumbnail sizes.</li>\n</ul>\n<hr />\n<h2>21. Exporting geometry for packing</h2>\n<p>The website may need the shape in a larger coordinate space for point testing.</p>\n<p>The JSON preset can include both:</p>\n<ul>\n<li>a normalised <code>0–1</code> path for CSS/SVG clipping;</li>\n<li>a <code>0–1000</code> path for geometry and packing.</li>\n</ul>\n<pre><code class=\"language-json\">{\n  &quot;shape_id&quot;: &quot;blob-001&quot;,\n  &quot;view_box&quot;: [0, 0, 1000, 1000],\n  &quot;svg_path&quot;: &quot;M 500 30 C ... Z&quot;,\n  &quot;normalised_path&quot;: &quot;M 0.5 0.03 C ... Z&quot;\n}\n</code></pre>\n<p>The site can create an invisible SVG path and use:</p>\n<pre><code class=\"language-js\">path.isPointInFill(point)\n</code></pre>\n<p>Candidate thumbnail positions can be tested against the same shape used visually.</p>\n<hr />\n<h2>22. Packing preview in Processing</h2>\n<p>Blob Studio could display a hexagonal candidate lattice inside the current shape.</p>\n<p>A basic candidate generator:</p>\n<pre><code class=\"language-java\">ArrayList&lt;PVector&gt; generateHexCandidates(\n  float areaWidth,\n  float areaHeight,\n  float itemDiameter,\n  float gap\n) {\n  ArrayList&lt;PVector&gt; positions =\n    new ArrayList&lt;PVector&gt;();\n\n  float spacingX =\n    itemDiameter + gap;\n\n  float spacingY =\n    spacingX * sqrt(3) / 2;\n\n  int rows =\n    ceil(areaHeight / spacingY);\n\n  int columns =\n    ceil(areaWidth / spacingX);\n\n  for (int row = 0; row &lt;= rows; row++) {\n    float y =\n      row * spacingY;\n\n    float offsetX =\n      row % 2 == 1\n        ? spacingX * 0.5\n        : 0;\n\n    for (\n      int column = 0;\n      column &lt;= columns;\n      column++\n    ) {\n      float x =\n        column * spacingX +\n        offsetX;\n\n      positions.add(\n        new PVector(x, y)\n      );\n    }\n  }\n\n  return positions;\n}\n</code></pre>\n<p>Testing full circular thumbnails against a Bézier boundary is more involved.</p>\n<p>Possible approaches include:</p>\n<ul>\n<li>drawing the boundary into a mask buffer;</li>\n<li>testing the centre and circumference samples against the mask;</li>\n<li>using a flattened polygon approximation;</li>\n<li>exporting the path and relying on the browser for final geometry testing.</li>\n</ul>\n<p>For Blob Studio, a raster mask-based preview may be sufficient.</p>\n<hr />\n<h2>23. Batch generation</h2>\n<p>Processing is particularly useful for creating related families of shapes.</p>\n<pre><code class=\"language-java\">void generateBatch(\n  int count,\n  int startingSeed\n) {\n  for (\n    int index = 0;\n    index &lt; count;\n    index++\n  ) {\n    Blob blob =\n      createBlobFromSeed(\n        startingSeed + index\n      );\n\n    blob.shapeId =\n      &quot;blob-&quot; +\n      nf(index + 1, 3);\n\n    saveStandaloneSvg(\n      blob,\n      &quot;exports/svg/&quot; +\n      blob.shapeId +\n      &quot;.svg&quot;\n    );\n\n    saveBlobPreset(\n      blob,\n      &quot;exports/json/&quot; +\n      blob.shapeId +\n      &quot;.json&quot;\n    );\n  }\n}\n</code></pre>\n<p>A batch preview should make it possible to:</p>\n<ul>\n<li>review all generated forms;</li>\n<li>mark favourites;</li>\n<li>reject weak shapes;</li>\n<li>rename selected forms;</li>\n<li>export only curated shapes;</li>\n<li>create a coherent visual family.</li>\n</ul>\n<hr />\n<h2>24. Curated versus fully random output</h2>\n<p>The website should probably use a curated shape set rather than generating new contours at runtime.</p>\n<p>Recommended model:</p>\n<pre><code class=\"language-text\">Processing generates many candidates\n        ↓\nselected shapes are curated\n        ↓\ncurated shapes enter the site registry\n        ↓\nwebsite assigns them deterministically\n</code></pre>\n<p>This gives:</p>\n<ul>\n<li>visual quality control;</li>\n<li>stable output;</li>\n<li>predictable clipping;</li>\n<li>fewer browser calculations;</li>\n<li>reproducible layouts;</li>\n<li>easier testing.</li>\n</ul>\n<p>The randomness belongs primarily in the authoring process, not necessarily in the deployed site.</p>\n<hr />\n<h2>25. Processing GUI implementation</h2>\n<p>The Blob Studio UI could use:</p>\n<ul>\n<li>custom Processing controls;</li>\n<li>ControlP5;</li>\n<li>G4P;</li>\n<li>an existing GUI system already used in the Processing folder.</li>\n</ul>\n<p>Given the existing Processing work, consistency with current GUI conventions is likely more valuable than introducing a new framework.</p>\n<p>Controls should support:</p>\n<ul>\n<li>keyboard adjustment;</li>\n<li>precise numeric values;</li>\n<li>reset to defaults;</li>\n<li>seed entry;</li>\n<li>regenerate;</li>\n<li>undo;</li>\n<li>preset save/load;</li>\n<li>export status;</li>\n<li>visible current output paths.</li>\n</ul>\n<hr />\n<h2>26. Suggested keyboard shortcuts</h2>\n<pre><code class=\"language-text\">R    regenerate current seed\nN    generate a new seed\nS    save preset\nE    export SVG and JSON\nC    copy normalised SVG path\nG    toggle repeated-gallery preview\nB    toggle boundary preview\nP    toggle packing preview\nH    show or hide handles\nF    mark as favourite\n← →  previous or next generated form\n</code></pre>\n<p>These should supplement rather than replace visible controls.</p>\n<hr />\n<h2>27. Build integration</h2>\n<p>A clean workflow could be:</p>\n<pre><code class=\"language-text\">1. Run processing/blob-studio.\n2. Generate and curate shapes.\n3. Export selected presets to:\n   processing/blob-studio/exports/json/\n4. Run a registry build command.\n5. Generate:\n   assets/shapes/shape-registry.json\n   assets/shapes/gallery-shapes.svg\n6. Rebuild or serve the site.\n7. Test the gallery layouts.\n</code></pre>\n<p>A future command might be:</p>\n<pre><code class=\"language-bash\">python scripts/build_shape_registry.py\n</code></pre>\n<p>The script could:</p>\n<ul>\n<li>validate shape IDs;</li>\n<li>reject duplicate IDs;</li>\n<li>validate normalised coordinates;</li>\n<li>build one registry;</li>\n<li>generate SVG <code>&lt;defs&gt;</code>;</li>\n<li>report malformed paths;</li>\n<li>preserve deterministic output ordering.</li>\n</ul>\n<hr />\n<h2>28. Git policy</h2>\n<p>The following should normally be committed:</p>\n<pre><code class=\"language-text\">processing/blob-studio/*.pde\nprocessing/blob-studio/presets/*.json\nassets/shapes/shape-registry.json\nassets/shapes/gallery-shapes.svg\ndocs/processing-blob-studio.md\n</code></pre>\n<p>The following may be ignored if they are temporary:</p>\n<pre><code class=\"language-text\">processing/blob-studio/exports/tmp/\nprocessing/blob-studio/screenshots/\nprocessing/blob-studio/cache/\n</code></pre>\n<p>A possible <code>.gitignore</code> entry:</p>\n<pre><code class=\"language-gitignore\">processing/blob-studio/exports/tmp/\nprocessing/blob-studio/screenshots/\nprocessing/blob-studio/cache/\n</code></pre>\n<p>Whether standalone exported SVG masters are committed depends on whether they are treated as source assets or reproducible build outputs.</p>\n<p>A reasonable policy is:</p>\n<ul>\n<li>commit curated presets;</li>\n<li>commit site-ready registry outputs;</li>\n<li>optionally commit selected SVG masters;</li>\n<li>ignore uncurated batch exports.</li>\n</ul>\n<hr />\n<h2>29. Naming convention</h2>\n<p>Use stable shape IDs:</p>\n<pre><code class=\"language-text\">blob-001\nblob-002\nleaf-001\ndroplet-001\nboundary-organic-001\n</code></pre>\n<p>Suggested fields:</p>\n<pre><code class=\"language-json\">{\n  &quot;shape_id&quot;: &quot;blob-001&quot;,\n  &quot;label&quot;: &quot;Rounded asymmetric&quot;,\n  &quot;type&quot;: &quot;blob&quot;,\n  &quot;status&quot;: &quot;selected&quot;,\n  &quot;use&quot;: [\n    &quot;thumbnail&quot;,\n    &quot;gallery-boundary&quot;\n  ]\n}\n</code></pre>\n<p>Avoid naming shapes only by visual adjectives that may later become ambiguous.</p>\n<p>The stable ID should be the canonical reference.</p>\n<hr />\n<h2>30. Validation</h2>\n<p>Before a shape enters the site registry, validate that:</p>\n<ul>\n<li>the path is closed;</li>\n<li>the path stays within the expected view box;</li>\n<li>the normalised path remains approximately within <code>0–1</code>;</li>\n<li>the shape has no unintended self-intersections;</li>\n<li>the shape has sufficient visible area;</li>\n<li>the form is not excessively narrow;</li>\n<li>a typical image crop remains readable;</li>\n<li>repeated forms do not create excessive visual noise;</li>\n<li>the shape works at small thumbnail sizes;</li>\n<li>the path renders consistently in supported browsers.</li>\n</ul>\n<p>Blob Studio could display validation warnings before export.</p>\n<hr />\n<h2>31. Recommended first implementation</h2>\n<p>The initial version should remain focused.</p>\n<h3>Version 1</h3>\n<p>Implement:</p>\n<ul>\n<li>radial seeded generation;</li>\n<li>6–12 anchor points;</li>\n<li>irregularity;</li>\n<li>smoothness;</li>\n<li>scale X/Y;</li>\n<li>rotation;</li>\n<li>solid preview;</li>\n<li>image preview;</li>\n<li>save/load JSON preset;</li>\n<li>standalone SVG export;</li>\n<li>normalised path export.</li>\n</ul>\n<h3>Version 2</h3>\n<p>Add:</p>\n<ul>\n<li>draggable anchors;</li>\n<li>draggable Bézier handles;</li>\n<li>repeated gallery preview;</li>\n<li>favourites;</li>\n<li>batch generation;</li>\n<li>curated export.</li>\n</ul>\n<h3>Version 3</h3>\n<p>Add:</p>\n<ul>\n<li>boundary preview;</li>\n<li>packing preview;</li>\n<li>registry generation;</li>\n<li>site-folder export;</li>\n<li>shape validation.</li>\n</ul>\n<hr />\n<h2>32. Recommended architectural separation</h2>\n<p>Keep these parts distinct:</p>\n<pre><code class=\"language-text\">BlobGenerator\n    creates initial anchors\n\nBlobEditor\n    modifies anchors and handles\n\nBlob\n    stores canonical geometry\n\nBlobRenderer\n    draws previews\n\nSvgPathWriter\n    creates clean SVG paths\n\nBlobPreset\n    saves and loads editable settings\n\nShapeRegistryWriter\n    generates website registry data\n</code></pre>\n<p>This prevents the GUI, generation logic and export format from becoming tightly coupled.</p>\n<hr />\n<h2>33. Practical recommendation</h2>\n<p>For the current gallery project:</p>\n<ol>\n<li>Keep Blob Studio under the existing repo’s <code>processing/</code> folder.</li>\n<li>Use Processing for generation, curation and preview.</li>\n<li>Save editable JSON presets as the canonical authoring source.</li>\n<li>Export clean standalone SVG files for inspection and editing.</li>\n<li>Export normalised SVG path data for the website.</li>\n<li>Build a small selected shape registry.</li>\n<li>Let the website use only CSS, SVG and JavaScript.</li>\n<li>Calculate complex gallery layouts lazily and cache them.</li>\n<li>Keep runtime shape assignment deterministic.</li>\n<li>Treat the Processing tool as a reusable site asset generator, not part of the deployed application.</li>\n</ol>\n<p>This approach fits the existing repository structure while keeping the public site lightweight and standards-based.</p>\n"
}
