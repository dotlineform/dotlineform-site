---
doc_id: site-request-studio-source-tree-reorganization-inventory
title: Studio Source Tree Reorganization Inventory
added_date: 2026-05-24
last_updated: 2026-05-24
ui_status: in-progress
parent_id: site-request-studio-source-tree-reorganization
sort_order: 10012
viewable: true
---
# Studio Source Tree Reorganization Inventory

This inventory supports [Studio Source Tree Reorganization Request](/docs/?scope=studio&mode=manage&doc=site-request-studio-source-tree-reorganization) and [Studio Source Tree Reorganization Tasks](/docs/?scope=studio&mode=manage&doc=site-request-studio-source-tree-reorganization-tasks).

The classifications are path-family level decisions for the current tree.
Rows marked with `STSR-004` in the follow-up column need an ownership decision before file moves begin.
This inventory does not authorize compatibility paths, migration aliases, copied source artifacts, or serving old Studio-owned source from public asset paths.

| Path family | Classification | Expected owner / target direction | Notes / follow-up |
| --- | --- | --- | --- |
| `studio/` | Studio source | Move or reshape into the final repo-level `studio/` app boundary. | If Jekyll route shells or legacy route hosts remain after the Local Studio migration, treat them as unfinished cleanup from that migration, not as an acceptable final shape. Audit active versus retired routes before keeping, deleting, or reshaping them. STSR-004. |
| `studio/ui-catalogue/` | Studio source | Keep under a clear `studio/ui-catalogue/` home. | Current demo route source should remain Studio-owned and should not be treated as public Jekyll source. |
| `_ui_catalogue_notes/` | Studio source | Move under `studio/ui-catalogue/notes/` or equivalent. | Source notes for UI Catalogue reference material; not public publishing surface. |
| `_includes/ui_catalogue_notes/` | Studio source | Move with UI Catalogue notes/runtime under `studio/ui-catalogue/`. | UI Catalogue is Studio-owned, so `_includes/` is the wrong long-term home unless a concrete integration reason is proven. Do not treat UI Catalogue artifacts as public by default. STSR-004 should confirm exact target. |
| `assets/ui-catalogue/` | Studio source | Move under `studio/ui-catalogue/assets/` or equivalent. | Demo CSS/JS assets belong with Studio UI Catalogue, not the public Jekyll surface. |
| `assets/docs/ui-catalogue/` | Studio source | Move under `studio/ui-catalogue/demos/` or equivalent. | UI Catalogue demo pages/assets are Studio reference surfaces, not public site assets by default. |
| `_docs/` | Future Docs Viewer extraction candidate | Move now into a clear internal home such as `studio/docs-viewer/source/studio/`. | Canonical Docs Viewer source Markdown for the Studio docs scope. It is not special; it is the default source folder for `/docs/` manage mode. Docs Viewer report documents in this scope, such as `change-history.md`, move with Docs Viewer even when they read Studio-owned data. Generated JSON remains public output. |
| `_docs_analysis/` | Future Docs Viewer extraction candidate | Move now into `studio/docs-viewer/source/analysis/` or equivalent. | Canonical Docs Viewer source Markdown for the analysis scope. Docs Viewer owns whether this scope is available locally through Studio, publicly at `/analysis/`, or both. |
| `_docs_library/` | Future Docs Viewer extraction candidate | Move now into `studio/docs-viewer/source/library/` or equivalent. | Canonical Docs Viewer source Markdown for the library scope. Docs Viewer owns whether this scope is available locally through Studio, publicly at `/library/`, or both. |
| `_docs_logs/entries/` | Studio source | Move under Studio-owned change request workflow source, such as `studio/workflows/change-requests/logs/entries/`. | Change request workflow source data. Not a Docs Viewer scope, even when a Docs Viewer-owned report reads it. |
| `_docs_logs/generated/`, `_docs_logs/reports/` | Studio generated working output | Move or retarget under Studio-owned generated workflow output unless a specific public route requires a stable public artifact. | Generated logs are consumed by Codex and may be read by Docs Viewer reports for human browsing. Report access does not transfer data ownership. STSR-004 should confirm final generated-output paths and read allowlists. |
| `assets/docs-viewer/js/` | Future Docs Viewer extraction candidate | Move now under `studio/docs-viewer/runtime/js/` or equivalent. | Runtime, management browser code, and report modules are currently localized enough to keep as a coherent internal Docs Viewer subtree. Report modules may read allowed data owned by other repo domains. |
| `assets/docs-viewer/css/` | Future Docs Viewer extraction candidate | Move now under `studio/docs-viewer/assets/css/` or equivalent. | Docs Viewer CSS should not be buried in generic Studio CSS. |
| `assets/docs-viewer/data/` | Future Docs Viewer extraction candidate | Move now under `studio/docs-viewer/config/` or equivalent unless a file is generated public output. | Contains Docs Viewer runtime/config/UI text. Distinguish source config from any public install config. STSR-004. |
| `_includes/docs_viewer_shell.html`, `_includes/docs_viewer_management_route.html`, `_includes/docs_viewer_readonly_route.html` | Future Docs Viewer extraction candidate / route integration | Move source ownership under `studio/docs-viewer/runtime/shells/` or equivalent; keep only minimal public route adapters outside Studio if required for read-only installs such as `/library/` and `/analysis/`. | `_includes/` should not be the default home for Docs Viewer-owned source. STSR-004 should decide whether public read-only route adapters remain as Jekyll integration stubs while the shell/config/runtime are owned by Docs Viewer. |
| `scripts/docs/` | Future Docs Viewer extraction candidate | Move now under `studio/docs-viewer/server/`, `studio/docs-viewer/services/`, and `studio/docs-viewer/config/` as appropriate. | Includes Docs Viewer build, management, source model, import/export, watcher, generated-read, and scope config code. Preserve internal Docs Viewer boundary. |
| `scripts/docs_logs/` | Studio source | Move under a Studio-owned change request workflow service home. | Current code supports docs log source/index workflows. It is not Docs Viewer service code, though its reports may be surfaced through `/docs/`. |
| `assets/data/docs/scopes/` | Public generated output | Remain outside `studio/` in the public Jekyll asset surface. | Generated JSON payloads read by Docs Viewer public installs and Studio docs routes. Generators move; output path remains public. |
| `assets/data/search/studio/`, `assets/data/search/library/`, `assets/data/search/analysis/` | Public generated output | Remain outside `studio/` in the public Jekyll asset surface. | Generated search payloads. Builders may move under Studio/Docs Viewer, but outputs stay public. |
| `assets/data/docs/reports.json` | Future Docs Viewer extraction candidate / public runtime payload | Move source/config ownership under `studio/docs-viewer/config/` or equivalent; keep a generated public copy outside `studio/` when read-only Docs Viewer installs need report registry/config at runtime. | Docs Viewer report registry/config, not Studio change-log data. Reports can be available in read-only installs such as `/library/`; manage-only reports display only in manage mode, currently `/docs/`. STSR-004 should define source path versus generated/served public payload path. |
| `_docs_catalogue/` | Studio source | Move under Studio canonical data, not under Docs Viewer. | Canonical publishing Markdown for works, series, and moments. Studio writes it and public catalogue JSON is generated from it. |
| `assets/studio/data/catalogue/` | Studio source | Move under `studio/data/canonical/catalogue/` or equivalent. | Canonical catalogue JSON source should not remain under public assets. |
| `assets/studio/data/catalogue_lookup/` | Studio generated lookup data | Move under Studio as generated editor lookup/read-model data. | This supports Studio editor pages, not public pages such as `/series/`. Treat lookup as a derived read model with a schema/contract; treat cache as disposable regeneration state. STSR-004 should name the final lookup/cache distinction and path. |
| `assets/studio/data/ui_text/` | Studio source | Move under `studio/app/config/` or `studio/app/frontend/config/`. | Studio UI copy/config belongs inside Studio. |
| `assets/studio/data/studio_config.json` | Studio source | Move under Studio runtime/config. | Local Studio static config, not public site data. |
| `assets/studio/js/` | Studio source | Move under `studio/app/frontend/js/` or route-family frontend homes. | Studio app browser code, editors, shell, clients, and workflow modules. |
| `assets/studio/css/` | Studio source | Move under `studio/app/assets/css/` or equivalent. | Studio-only CSS. Also split Studio-only selectors out of public `assets/css/main.css`. |
| `assets/studio/img/panel-backgrounds/` | Studio source | Move under `studio/app/assets/img/` or route-specific Studio assets. | Studio-only visual assets. |
| `assets/studio/img/thumbnail-quality/` | Studio generated/reusable workflow asset | Move under Studio local/generated review output or a retained Studio workflow asset home. | This was a one-off Studio workflow artifact retained in case it can be reused. It is not public publishing surface. |
| `_includes/studio_header_nav.html`, `_includes/studio_module_script.html` | Studio source / cleanup candidate | Delete if unused; if still active, move the behavior into Studio shell/runtime ownership. | These should not remain in `_includes/`. Any remaining active use indicates unfinished Local Studio cleanup. STSR-004. |
| `_layouts/studio.html` | Studio source / cleanup candidate | Remove after confirming no active route depends on it. | This should be redundant after Local Studio. If still required, treat that as unfinished Local Studio cleanup, not a final source-tree shape. STSR-004. |
| `scripts/studio/` | Studio source | Move under `studio/app/server/` and `studio/app/views/` or equivalent. | Local Studio server, APIs, config, views, and route adapters. |
| `scripts/analytics/` | Studio source | Move under `studio/services/analytics/`. | Studio-owned analytics/tag maintenance services and write workflows. |
| `scripts/catalogue/` | Studio source | Move under `studio/services/catalogue/`. | Studio-owned catalogue services, source mutations, build orchestration, prose import, validation, and projections. Public outputs remain outside Studio. |
| `scripts/media/` | Studio source | Move under `studio/services/media/`. | Studio-owned media maintenance and preview workflows. Public published media remains outside Studio. |
| `scripts/checks/` | Studio source | Move under Studio checks/test ownership. | Checks may validate the public publishing surface, but they are development/testing infrastructure run by Studio/Codex, not public site source. `projection_contract.json`, source-boundary checks, and public-build audits should move under Studio. STSR-004. |
| `scripts/search/` | Studio source / future Docs Viewer extraction candidate | Move builders/config under the owner for each search domain: Studio owns Catalogue search generation and policy; Docs Viewer owns Docs Viewer search generation; generated public search payloads remain outside Studio. | Public routes own only the browser runtime that displays published search data, such as `/series/` and public search pages. STSR-004 should split catalogue versus Docs Viewer search ownership. |
| `scripts/build_docs.rb`, `scripts/docs/build_docs.rb` | Future Docs Viewer extraction candidate | Prefer one Docs Viewer-owned build entrypoint under `studio/docs-viewer/`; decide whether a root developer command remains. | Root command retention must not become old-path compatibility for source. STSR-004. |
| `scripts/build_search.rb`, `scripts/docs/build_search.rb`, `scripts/search/build_search.rb` | Studio source / future Docs Viewer extraction candidate | Split by search domain owner; generated search outputs remain public. | Catalogue search builder/config belongs to Studio. Docs Viewer search builder/config belongs to Docs Viewer. Root commands should be intentional developer entrypoints, not old-path compatibility. STSR-004. |
| `scripts/run_checks.py` | Studio source | Move under Studio/Codex testing ownership or keep only a deliberate root convenience entrypoint that delegates to Studio. | Testing is development infrastructure, not public site publishing source. Root command retention must not preserve old Studio source paths. STSR-004. |
| `scripts/display_paths.py`, `scripts/local_env.py`, `scripts/pipeline_config.py`, `scripts/script_logging.py` | Studio source / shared development utility | Move under Studio-owned shared utility support unless a root convenience entrypoint has a deliberate reason to remain. | These are development/build/runtime helper utilities, not public published-page runtime. STSR-004 should resolve import ownership before rewrites. |
| `scripts/jekyll_markdown_renderer.rb`, `scripts/render_markdown_with_jekyll.rb`, `scripts/jekyll_webrick_client_reset_filter.rb` | Future Docs Viewer extraction candidate / Studio source | Move build/render helper ownership under Docs Viewer when used to build Docs Viewer payloads; otherwise move under Studio-owned build support. | Build scripts and render helpers are not public site runtime. Public site should keep only publishing config and runtime scripts for published pages. STSR-004 should map exact consumers. |
| `bin/local-studio` | Studio source | Move under Studio command/runtime ownership or replace with a new intentional root command. | A root convenience entrypoint may be acceptable, but not as an old-path compatibility layer. STSR-004. |
| `bin/public-site-build`, `bin/public-site-preview` | Studio source / root convenience entrypoint | Move implementation under Studio-owned development/build tooling; keep only deliberate root convenience entrypoints if needed. | Public-site build/preview commands are development infrastructure, not published-page runtime. They may validate or preview the public Jekyll site, but ownership belongs with Studio/Codex tooling. STSR-004. |
| `tests/smoke/` | Studio source | Move under Studio/Codex test ownership. | Smoke tests may validate public routes, but testing is development infrastructure and should live under Studio, not the public publishing surface. STSR-004. |
| `tests/python/` | Studio source | Move under Studio/Codex test ownership. | Python tests validate Studio, Docs Viewer, generators, checks, and public-output contracts; they are not public site runtime. STSR-004. |
| `tests/fixtures/` | Studio source | Move fixtures with the tests, services, or Docs Viewer modules that own them. | Fixture ownership follows development/testing ownership, not public publishing. STSR-004. |
| `assets/css/main.css` | Public Jekyll source with Studio leakage | Keep public CSS outside Studio; move Studio-only selectors into Studio CSS. | STSR-014 owns the selector-level split. STSR-004 can record the ownership principle only. |
| `assets/js/` | Public Jekyll source | Remain outside `studio/`. | Public route JavaScript reads generated JSON and powers published pages. Verify no Studio-only modules remain. |
| `assets/data/works_index.json`, `assets/data/series_index.json`, `assets/data/moments_index.json`, `assets/data/recent_index.json` | Public generated output | Remain outside `studio/`. | Public route indexes consumed by Jekyll pages/browser runtime. Generators move; outputs stay public. |
| `assets/works/index/`, `assets/series/index/`, `assets/moments/index/`, `assets/work_details/` | Public generated output | Remain outside `studio/`. | Generated public catalogue detail/index payloads. |
| `assets/works/img/`, `assets/series/img/`, `assets/moments/img/`, `assets/work_details/img/`, `assets/home/img/`, `assets/site/` | Public Jekyll source / public assets | Remain outside `studio/`. | Published thumbnails, media, and site assets required by GitHub Pages/Jekyll. |
| `assets/docs/interactive/` | Public Jekyll source / future Docs Viewer extraction candidate | Keep public if published docs pages need these assets; otherwise move with Docs Viewer source/runtime. | Interactive docs assets need consumer audit. STSR-004. |
| `_includes/artist_line.html`, `_includes/nav_item.html`, `_includes/work_index_item.html` | Public Jekyll source | Remain outside `studio/`. | Public Jekyll includes used by published layouts/routes. |
| `_layouts/default.html`, `_layouts/about.html`, `_layouts/work.html`, `_layouts/series.html`, `_layouts/moment.html`, `_layouts/work_details.html` | Public Jekyll source | Remain outside `studio/`. | Public site layouts. |
| `_works/`, `_series/`, `_moments/`, `_work_details/`, `_works_print/` | Public Jekyll source | Remain outside `studio/` unless a subpath is proven Studio-only. | Public collection/source pages for GitHub Pages/Jekyll. |
| `works/`, `series/`, `catalogue/`, `recent/`, `search/`, `analysis/`, `library/`, `docs/`, `data/`, `logs/` | Public Jekyll source / public route surface | Remain outside `studio/` for published routes and route shells. | Route pages may read public generated output from `assets/data/...`. Confirm any Studio-only local route shells before moves. STSR-004. |
| `_data/` | Public Jekyll source | Remain outside `studio/` unless individual data files are Studio-only. | Jekyll data source for public site. |
| `_config.yml`, `.ruby-version`, `Gemfile`, `Gemfile.lock` | Public Jekyll source / out-of-scope dependency | Remain outside `studio/`. | Jekyll/GitHub Pages infrastructure and package tooling are not Studio source. |
| `var/docs/catalogue/import-staging/` | Studio local working output | Keep local/unpublished; move or retarget under Studio-owned local working paths if needed. | Used by catalogue prose import for `_docs_catalogue/` staging. Not source of record. |
| `var/docs/`, `var/studio/`, `var/catalogue/` | Studio local working output | Keep local runtime/backups/logs outside published site; retarget paths if Studio source moves require it. | These are operational outputs, backups, logs, previews, and staging, not public publishing source. |
| `var/test-runs/`, `.pytest_cache/`, `__pycache__/`, `.DS_Store`, `_site/`, `tmp/` | Out-of-scope dependency | Do not move as source; clean only under explicit cleanup tasks. | Generated caches/build artifacts/local noise. |
