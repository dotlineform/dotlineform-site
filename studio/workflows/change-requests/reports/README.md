# Log Reports

This folder stores generated diagnostics for the change-history corpus.

Migration and validation reports should identify entries that need attention, but the v1 migration should not depend on a large manual curation pass.

