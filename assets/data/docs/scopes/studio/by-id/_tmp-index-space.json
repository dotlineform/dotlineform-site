{
  "scope": "studio",
  "doc_id": "_tmp-index-space",
  "title": "index workspace",
  "added_date": "2026-05-21",
  "last_updated": "2026-05-21",
  "parent_id": "_tmp",
  "sort_order": 100,
  "published": true,
  "hidden": true,
  "viewable": false,
  "source_path": "_tmp-index-space.md",
  "viewer_url": "/docs/?scope=studio&doc=_tmp-index-space",
  "content_html": "<h1 id=\"codex-discussion\">Codex discussion</h1>\n\n<p>here is a potential upcoming requirement for docs viewer. it is still rough. what is the blast radius for this request as it stands, and does it have any relationship with any of the risk mitigation work identified?</p>\n\n<p>request:\ndocs viewer: the index panel currently expands/collapses.</p>\n<ul>\n  <li>we need it to have a third state where it expands to fit the full content area. It can therefore be in states collapsed, normal, expanded.</li>\n  <li>the current expand/collapse button is used to expand/collapse, moving through the states collapsed, normal, expanded.</li>\n  <li>when expanded, the currently selected document is hidden.\nThis new behaviour is a precursor to introducing alternative index structures (e.g. graph) which will only be displayed in ‘expanded mode’.</li>\n</ul>\n\n<hr />\n\n<p>Blast radius is <strong>medium</strong>, mostly because this changes layout state semantics rather than just CSS.</p>\n\n<p>Current sidebar state is binary in <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/docs-viewer/js/docs-viewer.js:587\">docs-viewer.js</a>: <code class=\"language-plaintext highlighter-rouge\">state.sidebarCollapsed</code> plus <code class=\"language-plaintext highlighter-rouge\">data-sidebar-state=\"collapsed|expanded\"</code>. The first design issue is that today’s <code class=\"language-plaintext highlighter-rouge\">\"expanded\"</code> really means “normal”. A third state would need a small state-machine cleanup.</p>\n\n<p>Likely touched files:</p>\n\n<ul>\n  <li><a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/docs-viewer/js/docs-viewer.js:587\">docs-viewer.js</a>: replace boolean <code class=\"language-plaintext highlighter-rouge\">sidebarCollapsed</code> with something like <code class=\"language-plaintext highlighter-rouge\">sidebarState: \"collapsed\" | \"normal\" | \"expanded\"</code>, update localStorage, toggle order, labels, resize behavior.</li>\n  <li><a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/docs-viewer/css/docs-viewer.css:23\">docs-viewer.css</a>: add grid rules for full-width index mode and hide the main/document column in expanded mode.</li>\n  <li><a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/_includes/docs_viewer_shell.html:145\">_includes/docs_viewer_shell.html</a>: possibly update button label/icon assumptions, maybe no structural change.</li>\n  <li><a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/docs-viewer/js/docs-viewer-document-controller.js:160\">docs-viewer-document-controller.js</a>: only if “expanded mode hides selected document” is handled as pane state rather than pure CSS.</li>\n  <li><a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/docs-viewer/js/docs-viewer-management-interactions.js\">docs-viewer-management-interactions.js</a> and management CSS: needs smoke testing because drag/drop/context menu operate in the index.</li>\n</ul>\n\n<p>Relationship to risk mitigation: yes, directly.</p>\n\n<ul>\n  <li>It lands on <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/docs-viewer/js/docs-viewer.js:587\">docs-viewer.js</a>, which the new inventory marks as high risk because shared runtime state can attract unrelated behavior.</li>\n  <li>It touches the “status ownership” lesson indirectly: this should become an <strong>index layout/view-state owner</strong>, not another broad set of ad hoc conditionals in the entry controller.</li>\n  <li>It relates to <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/_docs/docs-viewer-javascript-inventory.md:91\">docs-viewer-javascript-inventory.md</a>: “alternative index structures” are exactly the kind of future feature that argues for a focused boundary before the entry controller grows again.</li>\n  <li>It also brushes <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/_docs/docs-viewer-runtime-boundary.md:116\">docs-viewer-runtime-boundary.md</a>: a graph index is okay as an alternate index view if the viewer remains the same route/content model, but a fundamentally different navigation model would be a runtime-boundary concern.</li>\n</ul>\n\n<p>Implementation recommendation: don’t just extend <code class=\"language-plaintext highlighter-rouge\">sidebarCollapsed</code>. Add a small <code class=\"language-plaintext highlighter-rouge\">docs-viewer-index-layout</code> or similar helper if this is genuinely a precursor to graph/index modes. For a one-off tri-state, it is easy to do inline; for “expanded mode hosts alternate index structures”, inline would increase the exact risk we just documented.</p>\n\n<p>Open requirement points before implementation:</p>\n<ul>\n  <li>In expanded mode, does clicking a document select it but keep content hidden, or return to normal mode?</li>\n  <li>Does expanded mode apply on mobile, where collapse is currently disabled?</li>\n  <li>Should search/recent results also be hidden in expanded mode, or only document content?</li>\n  <li>Should the tri-state be persisted per scope, and how should existing stored <code class=\"language-plaintext highlighter-rouge\">\"expanded\"</code> values migrate to <code class=\"language-plaintext highlighter-rouge\">\"normal\"</code>?</li>\n</ul>\n\n<hr />\n\n<p>most of these open questions are fine (e.g. mobile = no) but the big question is around the index/document relationship. I don’t think the current ‘click on index, show document’ is relevant here, because a graph index is about surfacing document relationships. we have only just added a simple semantic token into the source markdown documents, this is going to be an area of focus. it might be that if you open a doc from the graph, it opens in a new window, or displays summary information in a pop-up or info panel. the whole direction is that the index becomes more than a helper, it is an interactive surface. so this will need be supported by distinct modules, especially since a graph index will likely need external libraries as well.</p>\n\n<p>Codex:</p>\n\n<p>Yes. With that direction, I would treat the requested tri-state less as “sidebar gets wider” and more as the first step toward an <strong>index workspace mode</strong>.</p>\n\n<p>That changes the architecture call:</p>\n\n<ul>\n  <li><strong>Do not</strong> bury this in <code class=\"language-plaintext highlighter-rouge\">docs-viewer.js</code> as more sidebar conditionals.</li>\n  <li>Create a distinct owner for index mode/layout state, probably something like <code class=\"language-plaintext highlighter-rouge\">docs-viewer-index-controller.js</code> or <code class=\"language-plaintext highlighter-rouge\">docs-viewer-index-workspace.js</code>.</li>\n  <li>Keep the current tree index as one index surface.</li>\n  <li>Let expanded mode become a host area for alternate index surfaces later: tree now, graph later.</li>\n  <li>Keep document opening behavior separate from graph interactions. A graph node click should not inherit the current nav-link behavior by default.</li>\n</ul>\n\n<p>The graph requirement has a clear relationship to the risk work: it is exactly the kind of feature that could push <code class=\"language-plaintext highlighter-rouge\">docs-viewer.js</code> and <code class=\"language-plaintext highlighter-rouge\">docs-viewer-sidebar.js</code> from “acceptable orchestration/rendering” into mixed interactive-surface controllers. The new inventory should probably get updated when this requirement firms up, because it introduces a new risk theme: <strong>index surface ownership</strong>.</p>\n\n<p>I would model the next slice like this:</p>\n\n<ul>\n  <li><code class=\"language-plaintext highlighter-rouge\">docs-viewer-index-state.js</code> or similar: owns <code class=\"language-plaintext highlighter-rouge\">collapsed | normal | expanded</code>, persistence, toggle transitions, labels.</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">docs-viewer-index-controller.js</code>: owns index workspace behavior and decides which index surface is active.</li>\n  <li>Existing <code class=\"language-plaintext highlighter-rouge\">docs-viewer-sidebar.js</code>: remains the tree renderer, not the owner of future graph behavior.</li>\n  <li>Future <code class=\"language-plaintext highlighter-rouge\">docs-viewer-graph-index.js</code>: graph-specific rendering/interactions, lazy-loaded only in expanded mode if external libraries are needed.</li>\n</ul>\n\n<p>For the immediate tri-state requirement, the implementation can still be modest: introduce the state boundary, make expanded mode hide the document/search pane, and keep rendering the existing tree index full-width. That gives you the structural affordance without committing to graph behavior yet.</p>\n\n<p>The main requirement to settle before implementation is this: in expanded mode, should tree item clicks continue navigating documents, or should expanded mode suppress document navigation and treat the tree as an index surface only? Given your direction, I’d lean toward keeping current tree clicks as-is for now, but making graph behavior independent when it arrives.</p>\n\n<hr />\n\n<p>yes, the tree-index bahviour doesn’t change, it just makes viewing a many-levels tree easier because of the expanded width. but the expanded mode also gives us a generic surface in which to put other things later.</p>\n\n<p>Codex:</p>\n\n<p>That’s a good boundary.</p>\n\n<p>So the requirement becomes:</p>\n\n<ul>\n  <li>Add a tri-state index layout: <code class=\"language-plaintext highlighter-rouge\">collapsed</code>, <code class=\"language-plaintext highlighter-rouge\">normal</code>, <code class=\"language-plaintext highlighter-rouge\">expanded</code>.</li>\n  <li>Existing tree index behavior stays unchanged in all states.</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">expanded</code> gives the index full content width and hides the document/search pane.</li>\n  <li>The implementation should create an index workspace/layout boundary now, so future graph/semantic views can plug into expanded mode without growing <code class=\"language-plaintext highlighter-rouge\">docs-viewer.js</code>.</li>\n  <li>No mobile behavior change; keep the current desktop-only collapse/expand control.</li>\n  <li>Existing persisted <code class=\"language-plaintext highlighter-rouge\">expanded</code> should migrate to <code class=\"language-plaintext highlighter-rouge\">normal</code>, because today’s stored value means “not collapsed”, not new expanded workspace mode.</li>\n</ul>\n\n<p>I’d implement the first slice with:</p>\n<ul>\n  <li>a small focused module for index layout state and labels</li>\n  <li>CSS for <code class=\"language-plaintext highlighter-rouge\">data-index-layout=\"collapsed|normal|expanded\"</code> or equivalent</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">docs-viewer.js</code> wiring only</li>\n  <li>no graph code yet</li>\n  <li>a browser smoke check for the three states, tree click behavior, and document pane hiding/restoring</li>\n</ul>\n\n<p>This is now a cleaner medium-small feature rather than a risky layout hack.</p>\n\n<hr />\n\n<p>I suppose that the key point to get right now is that the expanded mode isn’t just a bigger tree index. it is that there is a generic panel which can be collapsed, normal or expanded, and it is currently populated with a tree index which fills the space available to it. it is not the index itself changing or expanding.</p>\n\n<p>Codex:</p>\n\n<p>Exactly. That distinction is the architecture.</p>\n\n<p>I’d name and model it as an <strong>index panel</strong> or <strong>index workspace</strong>, not “expanded tree index”.</p>\n\n<p>Current content:</p>\n<ul>\n  <li>panel content = tree index</li>\n  <li>tree index behavior = unchanged</li>\n  <li>tree index layout = fills whatever panel size it is given</li>\n</ul>\n\n<p>Panel state:</p>\n<ul>\n  <li><code class=\"language-plaintext highlighter-rouge\">collapsed</code>: panel is icon-width, tree hidden</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">normal</code>: panel is sidebar-width, tree visible</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">expanded</code>: panel occupies the main content area, document/search pane hidden, tree visible with more width</li>\n</ul>\n\n<p>Future content:</p>\n<ul>\n  <li>panel content could be tree, graph, semantic references, relationship map, etc.</li>\n  <li>expanded mode is the natural host for richer interactive index surfaces</li>\n  <li>graph interactions do not need to inherit tree document navigation behavior</li>\n</ul>\n\n<p>That means the implementation should avoid names like <code class=\"language-plaintext highlighter-rouge\">sidebarExpanded</code> or <code class=\"language-plaintext highlighter-rouge\">expandTree</code>. Better names would be:</p>\n\n<ul>\n  <li><code class=\"language-plaintext highlighter-rouge\">indexPanelState</code></li>\n  <li><code class=\"language-plaintext highlighter-rouge\">docs-viewer-index-panel.js</code></li>\n  <li><code class=\"language-plaintext highlighter-rouge\">data-index-panel-state</code></li>\n  <li><code class=\"language-plaintext highlighter-rouge\">toggleIndexPanelState()</code></li>\n  <li>labels like <code class=\"language-plaintext highlighter-rouge\">Collapse index panel</code>, <code class=\"language-plaintext highlighter-rouge\">Restore index panel</code>, <code class=\"language-plaintext highlighter-rouge\">Expand index panel</code></li>\n</ul>\n\n<p>The existing <code class=\"language-plaintext highlighter-rouge\">docs-viewer-sidebar.js</code> can stay as a tree renderer inside the panel. The new state owner should be about the panel, not the tree.</p>\n\n<hr />\n"
}
