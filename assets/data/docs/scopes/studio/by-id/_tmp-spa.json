{
  "scope": "studio",
  "doc_id": "_tmp-spa",
  "title": "SPA",
  "added_date": "2026-05-22",
  "last_updated": "2026-05-22",
  "parent_id": "_tmp",
  "sort_order": 1000,
  "published": true,
  "hidden": true,
  "viewable": false,
  "source_path": "_tmp_spa.md",
  "viewer_url": "/docs/?scope=studio&doc=_tmp-spa",
  "content_html": "<ul>\n  <li>there is no reason why /studio/… pages need to be on the public site. they are only run on local server. on public site, they are read-only shells but essentially non-functional as a public github jekyll page.</li>\n  <li>There is no reason why site documentation at /docs/ should be publicly viewable. If it does need to be published publicly, it could be published in a read-only curated docs viewer install.</li>\n  <li>Docs Viewer manage-mode of any docs viewer install is only functional when run off local server. Manage-only tools are not even displayed on public read-only docs viewer installs.</li>\n</ul>\n\n<p>conclusions:</p>\n\n<ul>\n  <li>studio infrastructure and front-end could be in a completely separate repo.</li>\n  <li>they could be hosted as web pages on local server but that server does not need to be tied to github software versions or dependencies.</li>\n  <li>studio infrastructure does not have to be Jekyll-managed web pages. it could be a pure javascript app. it could be a javascript app wrapped in a native app-shell. it could be entirely a native app front-end, built in Swift.</li>\n  <li>we currently use the html-markdown library bundled with the jekyll server, but that does not mean that the studio web pages themselves need to be Jekyll. we could use any python or ruby library for the html conversion. producing github/jekyll-friendly markdown is not a requirement for use in Docs Viewer is not an actual requirement, because Docs Viewer doesn’t render the markdown, it renders html. The markdown is a data source, not a published artifact.</li>\n</ul>\n\n<p>Codex:</p>\n\n<p>I agree with your conclusions. The current structure already points that way.</p>\n\n<p>Current facts in the repo:</p>\n\n<ul>\n  <li>Public <code class=\"language-plaintext highlighter-rouge\">_config.yml</code> has <code class=\"language-plaintext highlighter-rouge\">docs_viewer_management_enabled: false</code>, so <code class=\"language-plaintext highlighter-rouge\">/docs/?mode=manage</code> is intentionally read-only on public builds.</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">bin/local-studio</code> serves management mode through the local Python app.</li>\n  <li>public Jekyll preview/build stays separate.</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">/studio/</code> still ships as public static pages, and <code class=\"language-plaintext highlighter-rouge\">assets/studio/</code> is about 26 MB of local-only UI/data/assets.</li>\n  <li>Generated Studio docs/search payloads are also published today unless excluded.</li>\n</ul>\n\n<p>Relevant files:\n<a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/_config.yml\">public config</a>, <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/bin/local-studio\">local launcher</a>, <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/_includes/docs_viewer_management_route.html\">docs shell gate</a>.</p>\n\n<p><strong>Recommended Direction</strong></p>\n\n<p>Treat the public site as a publishing artifact, Studio as a local application, and Docs Viewer as a portable document viewer/manager.</p>\n\n<p>I would do this in stages:</p>\n\n<ol>\n  <li>\n    <p><strong>Public build hygiene first</strong>\nExclude <code class=\"language-plaintext highlighter-rouge\">/studio/</code>, <code class=\"language-plaintext highlighter-rouge\">assets/studio/</code>, <code class=\"language-plaintext highlighter-rouge\">/docs/</code>, Studio docs payloads, and Studio docs search from public builds. Keep <code class=\"language-plaintext highlighter-rouge\">/library/</code> or <code class=\"language-plaintext highlighter-rouge\">/analysis/</code> public only if they are intentionally curated read-only corpora.</p>\n  </li>\n  <li>\n    <p><strong>Keep local Studio in-repo for now</strong>\nDo not jump straight to a new repo until the public/local boundary is clean. A low-risk first move is keeping <code class=\"language-plaintext highlighter-rouge\">_config.yml</code> public-only and serving Local Studio through the Python app.</p>\n  </li>\n  <li>\n    <p><strong>Then split Studio if it still feels valuable</strong>\nA separate Studio repo makes sense once ownership is clear. The key decision is where canonical source data lives: public-site repo, Studio repo, or a shared/private data repo. That choice matters more than whether the frontend is Jekyll, Vite, Electron/Tauri, or Swift.</p>\n  </li>\n  <li>\n    <p><strong>Do not start with native Swift</strong>\nA local web app is the pragmatic next architecture. Native shell only becomes attractive if you need OS integration, offline packaging, file pickers, tray behavior, or a more app-like distribution model.</p>\n  </li>\n</ol>\n\n<p>On Markdown/rendering: yes, the markdown does not need to be GitHub/Jekyll-publishable if Docs Viewer consumes generated HTML. The real contract is “source markdown plus tokens in, viewer-safe HTML JSON out.” Current docs rendering is Jekyll-coupled through <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/scripts/jekyll_markdown_renderer.rb\">scripts/jekyll_markdown_renderer.rb</a>, but that can be replaced later with golden-output tests around headings, links, code, media tokens, semantic refs, and interactive HTML.</p>\n\n<p>One caveat: excluding docs from the published site does not make them private if the GitHub repo itself is public. If privacy is the goal, source docs need a private repo/submodule or separate private data package.</p>\n\n<hr />\n\n<ul>\n  <li>yes, <code class=\"language-plaintext highlighter-rouge\">/library/</code> and <code class=\"language-plaintext highlighter-rouge\">/analysis/</code> need to be public viewable, intentionally read-only.</li>\n  <li>canonical source data doesn’t need to live in the public site repo. if the public site repo cleanly reflects what is actually published, then presumably, canonical source data shouldn’t live there, it should live next to the things that manage it.</li>\n  <li>agree, some native shell functionality would be nice but not essential. native shell is not currently a realistic option given developer constraints.</li>\n  <li>note that privacy is not goal. general public visibility through dotlineform.com is the key factor, that is what I mean when I say ‘public’. repos are public, there is no reason for them to be private. there is no private data (excepting security tokens).</li>\n</ul>\n\n<p>Codex:</p>\n\n<p>That clarification changes the framing: this is not a privacy/security boundary, it is a <strong>published-surface boundary</strong>. The public repo can stay public; dotlineform.com should only expose what is meant to be a public site.</p>\n\n<p><strong>Revised Direction</strong></p>\n\n<p>I’d separate the system into three conceptual products:</p>\n\n<ol>\n  <li>\n    <p><strong>Public site</strong>\nPublishes dotlineform.com. Includes works, series, moments, public catalogue data, public assets, <code class=\"language-plaintext highlighter-rouge\">/library/</code>, <code class=\"language-plaintext highlighter-rouge\">/analysis/</code>, and read-only Docs Viewer runtime needed by those routes.</p>\n  </li>\n  <li>\n    <p><strong>Studio</strong>\nLocal management app. Owns canonical source data, editors, local write services, docs management, catalogue management, audits, imports/exports, generated-data builders, and unpublished operational docs.</p>\n  </li>\n  <li>\n    <p><strong>Docs Viewer</strong>\nReusable viewer/manager module. Public installs use read-only mode; local Studio uses manage mode. <code class=\"language-plaintext highlighter-rouge\">/library/</code> and <code class=\"language-plaintext highlighter-rouge\">/analysis/</code> are public read-only consumers.</p>\n  </li>\n</ol>\n\n<p>The cleanest end state is: Studio generates or exports the exact public-site source/artifact set, and the public site repo reflects only what dotlineform.com needs to build and serve.</p>\n\n<p><strong>Near-Term Practical Path</strong></p>\n\n<p>I would not start with a separate repo immediately. First, make the current repo behave like the desired boundary:</p>\n\n<ol>\n  <li>Public build excludes <code class=\"language-plaintext highlighter-rouge\">/studio/</code>, <code class=\"language-plaintext highlighter-rouge\">assets/studio/</code>, local Studio data, and the Studio docs route at <code class=\"language-plaintext highlighter-rouge\">/docs/</code>.</li>\n  <li>Public build keeps <code class=\"language-plaintext highlighter-rouge\">/library/</code> and <code class=\"language-plaintext highlighter-rouge\">/analysis/</code>, plus their generated docs/search payloads.</li>\n  <li>Local <code class=\"language-plaintext highlighter-rouge\">bin/local-studio</code> continues to use the dev config and can still serve <code class=\"language-plaintext highlighter-rouge\">/studio/</code> and management <code class=\"language-plaintext highlighter-rouge\">/docs/</code>.</li>\n  <li>Define a “published artifact manifest”: what files/directories are intentionally part of dotlineform.com.</li>\n  <li>Once that manifest is stable, move canonical source data and Studio app code into a Studio repo or sibling workspace.</li>\n</ol>\n\n<p><strong>Repo Split Option</strong></p>\n\n<p>Best eventual split:</p>\n\n<ul>\n  <li><code class=\"language-plaintext highlighter-rouge\">dotlineform-site</code>: public site only.</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">dotlineform-studio</code>: local app, canonical data, scripts/services, docs source, builders.</li>\n  <li>optional later: <code class=\"language-plaintext highlighter-rouge\">docs-viewer</code>: portable viewer package or subtree used by both.</li>\n</ul>\n\n<p>The important contract becomes a publish/export step from Studio to public site. That step should write only public-safe, intentionally published files.</p>\n\n<p><strong>Jekyll</strong></p>\n\n<p>Public site can stay Jekyll.</p>\n\n<p>Studio does not need to stay Jekyll. A local JS app served by a small Python/Ruby/Node server is enough. The current Jekyll coupling is mostly route/template convenience plus markdown rendering. Since Docs Viewer consumes generated HTML, the renderer can eventually be replaced behind tests without requiring GitHub/Jekyll-friendly markdown as the source contract.</p>\n\n<p>My recommendation: first clean the build/public boundary in-place, then split repos once the generated/published file contract is explicit.</p>\n\n<hr />\n\n<p>yes. happy to stay with one repo until everything settles. disengaging studio from jekyll is more important, because I think it gives us more problems to solve than it helps (e.g. all the regenerating issues we have had)</p>\n\n<p>a further clarification on public vs private. there is some canonical data about works that is not published, but it is not private in the sense that it cannot be saved in a public repo. quote from _docs/data-models-catalogue-source.md: ‘source-only fields such as <code class=\"language-plaintext highlighter-rouge\">notes</code> and <code class=\"language-plaintext highlighter-rouge\">provenance</code> stay out of public projections unless an explicit runtime contract includes them.’</p>\n\n<p>Codex:</p>\n\n<p>Yes. That distinction is important:</p>\n\n<ul>\n  <li><strong>not private</strong>: fine to live in a public repo</li>\n  <li><strong>not published</strong>: must not be emitted into dotlineform.com runtime files, routes, JSON, HTML, search indexes, or viewer payloads</li>\n</ul>\n\n<p>So the boundary is not “secret vs public repo.” It is <strong>canonical authoring data vs published projection</strong>.</p>\n\n<p>I’d frame the direction like this:</p>\n\n<p><strong>Primary Goal</strong>\nDisengage Studio from Jekyll as the local application/runtime, while preserving Jekyll as the public site publisher for now.</p>\n\n<p>That means Studio becomes responsible for:</p>\n\n<ul>\n  <li>editing canonical source data</li>\n  <li>running local services</li>\n  <li>generating public projections</li>\n  <li>generating Docs Viewer payloads</li>\n  <li>managing local-only operational docs</li>\n  <li>previewing outputs</li>\n</ul>\n\n<p>Jekyll becomes responsible for:</p>\n\n<ul>\n  <li>building the public dotlineform.com site</li>\n  <li>rendering public pages from already-prepared source/projection files</li>\n  <li>serving <code class=\"language-plaintext highlighter-rouge\">/library/</code> and <code class=\"language-plaintext highlighter-rouge\">/analysis/</code> as read-only Docs Viewer installs</li>\n</ul>\n\n<p><strong>The Key Architectural Move</strong></p>\n\n<p>Introduce a stronger projection boundary:</p>\n\n<div class=\"language-text highlighter-rouge\"><div class=\"highlight\"><pre class=\"highlight\"><code>canonical source data\n  -&gt; Studio/app builders\n  -&gt; published projections\n  -&gt; Jekyll public site\n</code></pre></div></div>\n\n<p>For catalogue data, the rule you quoted is exactly right: <code class=\"language-plaintext highlighter-rouge\">notes</code>, <code class=\"language-plaintext highlighter-rouge\">provenance</code>, and similar fields can remain in canonical source, but they should only enter generated public artifacts when a specific public/runtime contract says so.</p>\n\n<p>That suggests a useful naming discipline:</p>\n\n<ul>\n  <li><code class=\"language-plaintext highlighter-rouge\">source</code> or <code class=\"language-plaintext highlighter-rouge\">canonical</code>: complete working records</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">projection</code>: generated records for a specific consumer</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">public projection</code>: safe for dotlineform.com</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">studio projection</code>: local app/runtime only</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">viewer payload</code>: generated Docs Viewer JSON/HTML</li>\n</ul>\n\n<p><strong>Why Disengaging Studio From Jekyll Should Come First</strong></p>\n\n<p>I agree this matters more than repo splitting. The current setup makes local app behavior depend on Jekyll rebuild mechanics, Liquid route shells, generated <code class=\"language-plaintext highlighter-rouge\">_site</code> state, and dev/public config overlays. That creates avoidable failure modes.</p>\n\n<p>A local Studio server/app would let you:</p>\n\n<ul>\n  <li>serve Studio routes directly without Jekyll page regeneration</li>\n  <li>make generated-data rebuilds explicit app actions, not side effects of site serving</li>\n  <li>separate “preview public site” from “use Studio”</li>\n  <li>keep Docs Viewer management local without coupling it to Jekyll shell behavior</li>\n  <li>choose a markdown-to-HTML renderer as an implementation detail</li>\n</ul>\n\n<p><strong>Recommended Next Shape</strong></p>\n\n<p>Short term, still one repo:</p>\n\n<div class=\"language-text highlighter-rouge\"><div class=\"highlight\"><pre class=\"highlight\"><code>studio-app/\n  local frontend app\n  local server/services\n\ncanonical-data/\n  works, series, moments, docs source, tags, etc.\n\npublic-site/\n  Jekyll site source, layouts, public pages\n\npackages/docs-viewer/ or assets/docs-viewer/\n  reusable read-only/manage viewer runtime\n\ngenerated/\n  public projections\n  studio projections\n  viewer payloads\n</code></pre></div></div>\n\n<p>The exact folders can wait. The important thing is to stop treating Jekyll as the Studio application host.</p>\n\n<p><strong>Practical Migration Order</strong></p>\n\n<ol>\n  <li>Define the public projection contract for catalogue, docs, search, and assets.</li>\n  <li>Make public Jekyll builds consume only those public projections.</li>\n  <li>Move Studio route serving to a local non-Jekyll server/app while still reusing current JS modules where possible.</li>\n  <li>Keep Jekyll preview/build available as a separate command from Studio.</li>\n  <li>Only after that, decide whether to split repos.</li>\n</ol>\n\n<p>My bias: build the next Studio shell as a plain local web app first. Native shell can remain a later wrapper decision, not an architectural dependency.</p>\n\n<hr />\n\n<p>if studio is a plain local web app, how hard is reusing existing js modules, given that we would essentially want to preserve current functionality, workflows, behaviour, nav?</p>\n\n<p>Not very hard if we keep it as a <strong>plain vanilla local web app</strong> and preserve the DOM contracts. It becomes hard only if we use the move as an excuse to rewrite the UI framework.</p>\n\n<p>Most existing JS modules look reusable because they are already browser ES modules, fetch JSON, bind to stable IDs/data roles, and render into existing DOM. For example, <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/studio/js/studio-data.js\">studio-data.js</a> is already mostly runtime/data helpers, and <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/assets/studio/js/studio-ui.js\">studio-ui.js</a> defines explicit UI contracts. That is good news.</p>\n\n<p>The less reusable part is the route shell. A route like <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/studio/catalogue-work/index.md\">studio/catalogue-work/index.md</a> mixes:</p>\n\n<ul>\n  <li>front matter: route title, permalink, docs link</li>\n  <li>Liquid config expansion from <code class=\"language-plaintext highlighter-rouge\">_config.yml</code> and <code class=\"language-plaintext highlighter-rouge\">_data/pipeline</code></li>\n  <li>HTML markup with fixed IDs/classes/data attributes</li>\n  <li>Jekyll <code class=\"language-plaintext highlighter-rouge\">relative_url</code></li>\n  <li>script include via <a href=\"/Users/dlf/Developer/dotlineform/dotlineform-site/_includes/studio_module_script.html\">studio_module_script.html</a></li>\n</ul>\n\n<p>That is all replaceable. It is not business logic. The migration task is mostly to turn those Jekyll/Liquid shells into local app templates and feed them config from JSON instead of Liquid.</p>\n\n<p><strong>Difficulty Estimate</strong></p>\n\n<ul>\n  <li>Preserving current workflows with a local multi-page vanilla app: <strong>moderate, very feasible</strong></li>\n  <li>Reusing existing JS/CSS without major rewrites: <strong>highly feasible</strong></li>\n  <li>Removing Jekyll route regeneration from Studio: <strong>high value, moderate effort</strong></li>\n  <li>Rebuilding Studio in React/Vue/Svelte/native: <strong>much harder and not justified now</strong></li>\n</ul>\n\n<p>The best path is not “rewrite Studio.” It is “replace Jekyll as the route/template host.”</p>\n\n<p><strong>Recommended Shape</strong></p>\n\n<p>Use a small local server that serves:</p>\n\n<div class=\"language-text highlighter-rouge\"><div class=\"highlight\"><pre class=\"highlight\"><code>/studio/\n/studio/catalogue-work/\n/studio/catalogue-series/\n/studio/analytics/tag-registry/\n...\n/assets/studio/js/...\n/assets/studio/css/...\n/studio/runtime-config.json\n/api/catalogue/...\n/api/docs/...\n/api/tags/...\n</code></pre></div></div>\n\n<p>Then keep the current route controllers mostly intact. The server can provide what Liquid currently injects:</p>\n\n<ul>\n  <li>media bases</li>\n  <li>thumbnail bases</li>\n  <li>pipeline variant config</li>\n  <li>route docs links</li>\n  <li>UI text paths</li>\n  <li>service endpoint URLs</li>\n  <li>cache-busting/no-cache headers</li>\n</ul>\n\n<p><strong>Main Migration Risks</strong></p>\n\n<p>The biggest risk is not JS reuse. It is <strong>implicit Jekyll assumptions</strong>:</p>\n\n<ul>\n  <li>paths generated through <code class=\"language-plaintext highlighter-rouge\">relative_url</code></li>\n  <li>config injected as <code class=\"language-plaintext highlighter-rouge\">data-*</code> attributes</li>\n  <li>route layout/header/nav from <code class=\"language-plaintext highlighter-rouge\">_layouts/studio.html</code></li>\n  <li>links between Studio routes</li>\n  <li>links from Studio to public preview pages</li>\n  <li>any tests that assume Jekyll-served <code class=\"language-plaintext highlighter-rouge\">/studio/...</code></li>\n</ul>\n\n<p>Those are manageable if we migrate one route at a time and keep IDs/classes/data roles unchanged.</p>\n\n<p><strong>Practical Migration Order</strong></p>\n\n<ol>\n  <li>Build a local non-Jekyll Studio server that can serve static assets and one route.</li>\n  <li>Convert a simple route first, probably <code class=\"language-plaintext highlighter-rouge\">/studio/studio-works/</code> or <code class=\"language-plaintext highlighter-rouge\">/studio/activity/</code>.</li>\n  <li>Add a shared Studio shell/nav template outside Jekyll.</li>\n  <li>Convert one complex editor route, probably <code class=\"language-plaintext highlighter-rouge\">/studio/catalogue-work/</code>.</li>\n  <li>Keep current write services/endpoints stable, or proxy them through the new local server.</li>\n  <li>Once representative routes work, move the rest mechanically.</li>\n  <li>Leave public Jekyll build untouched except for excluding Studio from the published site.</li>\n</ol>\n\n<p>My read: preserving functionality is realistic because the existing frontend is already modular enough. The work is mostly route-host extraction, config injection cleanup, and test coverage around the pages where Jekyll currently supplies hidden glue.</p>\n\n<hr />\n\n<p>note that by ‘nav’, I mean ‘user clicks a button and it takes them to a page or a modal’. it doesn’t really matter if they are taken to different url routes or stay on the same page all the time. how we maintain persistent nav states through url queries is only the current (jekyll) enforced solution. the url doesn’t actually matter to the studio user.</p>\n\n<p>Codex:</p>\n\n<p>That makes the local-app direction easier, not harder.</p>\n\n<p>If Studio users do not care about stable URL routes, then Studio does not need to preserve Jekyll-style page navigation. It only needs to preserve <strong>task navigation</strong>:</p>\n\n<ul>\n  <li>click opens editor</li>\n  <li>click opens modal</li>\n  <li>select record</li>\n  <li>return to prior list/filter/context</li>\n  <li>save/build/delete/import flows keep their state</li>\n  <li>preview links go to the right public artifact when needed</li>\n</ul>\n\n<p>So the target can be an <strong>application state machine</strong>, not a static route collection.</p>\n\n<p><strong>Implication</strong></p>\n\n<p>The current URL-query model should be treated as an implementation detail, not a product contract. It exists because Jekyll gives us static routes and the browser URL is the easiest shared state container.</p>\n\n<p>A local Studio app can replace that with:</p>\n\n<ul>\n  <li>in-memory app state for active page/modal/selection</li>\n  <li>browser history only when useful</li>\n  <li>local/session storage for restoring last workspace</li>\n  <li>explicit return targets in state, not <code class=\"language-plaintext highlighter-rouge\">?from=...</code></li>\n  <li>modals/drawers instead of separate pages where that improves workflow</li>\n</ul>\n\n<p><strong>Better Target Shape</strong></p>\n\n<p>Studio could become a single local app shell:</p>\n\n<div class=\"language-text highlighter-rouge\"><div class=\"highlight\"><pre class=\"highlight\"><code>/studio/\n</code></pre></div></div>\n\n<p>with internal views:</p>\n\n<div class=\"language-text highlighter-rouge\"><div class=\"highlight\"><pre class=\"highlight\"><code>catalogue.work\ncatalogue.series\ncatalogue.moment\nanalytics.tagRegistry\ndocs.manage\nactivity\naudits\ndataSharing.prepare\n</code></pre></div></div>\n\n<p>The visible URL could stay <code class=\"language-plaintext highlighter-rouge\">/studio/</code>, or optionally use hash/history state like:</p>\n\n<div class=\"language-text highlighter-rouge\"><div class=\"highlight\"><pre class=\"highlight\"><code>/studio/#catalogue.work:00456\n/studio/#analytics.tagRegistry\n</code></pre></div></div>\n\n<p>But that is for restore/debugging, not because the user needs URL fidelity.</p>\n\n<p><strong>Reuse Impact</strong></p>\n\n<p>This slightly reduces direct reuse of page shells but keeps most JS logic reusable if we wrap it carefully.</p>\n\n<p>Current route controllers likely assume:</p>\n\n<ul>\n  <li>specific root DOM exists</li>\n  <li>specific buttons/inputs exist</li>\n  <li>initial state comes from URL query</li>\n  <li>navigation is <code class=\"language-plaintext highlighter-rouge\">window.location.href = ...</code></li>\n</ul>\n\n<p>Those can be adapted with a small navigation/service layer:</p>\n\n<ul>\n  <li><code class=\"language-plaintext highlighter-rouge\">getInitialRouteState()</code> currently reads URL; later reads app state</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">navigateTo(view, params)</code> currently sets URL; later swaps view or opens modal</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">openModal(name, params)</code> centralizes modal behavior</li>\n  <li><code class=\"language-plaintext highlighter-rouge\">setReturnContext()</code> replaces <code class=\"language-plaintext highlighter-rouge\">?from=...</code></li>\n</ul>\n\n<p>The goal would be to avoid rewriting editor internals. Replace the edges where controllers enter/leave pages.</p>\n\n<p><strong>Recommendation</strong></p>\n\n<p>Do not migrate to SPA all at once. First create a local app shell with a router abstraction, but keep route-like views internally. Then gradually decide which “pages” should become modals or panels.</p>\n\n<p>That preserves current behavior while removing Jekyll as the thing enforcing URL-shaped workflow.</p>\n\n<hr />\n\n<p>‘Rebuilding Studio in React/Vue/Svelte/native: much harder and not justified now’ - agree. if I had made this design decision much earlier, there would have been pros and cons. now it too late and I have no intention of moving unless there is a serious technical/maintenance reason to do so.</p>\n\n<p>Codex:</p>\n\n<p>That is the right call.</p>\n\n<p>At this point, the technical debt is not “missing framework.” The debt is that Studio is hosted by the wrong runtime. Jekyll is forcing app workflows through static-page mechanics: Liquid injection, rebuild coupling, URL-query state, generated <code class=\"language-plaintext highlighter-rouge\">_site</code> behavior, and dev/public config overlays.</p>\n\n<p>A framework migration would add a second problem:</p>\n\n<ul>\n  <li>porting UI and controller behavior</li>\n  <li>revalidating every workflow</li>\n  <li>replacing mature route-specific modules</li>\n  <li>rebuilding CSS/component conventions</li>\n  <li>introducing package/build tooling</li>\n  <li>likely pausing actual product work for a long time</li>\n</ul>\n\n<p>The better move is narrower:</p>\n\n<p><strong>Keep:</strong></p>\n\n<ul>\n  <li>current vanilla ES modules</li>\n  <li>current CSS/UI conventions</li>\n  <li>current DOM contracts where useful</li>\n  <li>current local Python/Ruby services initially</li>\n  <li>current workflows and labels</li>\n</ul>\n\n<p><strong>Change:</strong></p>\n\n<ul>\n  <li>Studio host runtime</li>\n  <li>config injection model</li>\n  <li>app navigation/state ownership</li>\n  <li>public build inclusion</li>\n  <li>generated projection boundaries</li>\n</ul>\n\n<p>So I’d describe the strategic direction as:</p>\n\n<blockquote>\n  <p>Studio should become a local vanilla web app, not a Jekyll section and not a frontend-framework rewrite.</p>\n</blockquote>\n\n<p>A good migration target is boring:</p>\n\n<div class=\"language-text highlighter-rouge\"><div class=\"highlight\"><pre class=\"highlight\"><code>local server\n  serves one Studio app shell\n  serves existing JS/CSS/assets\n  provides runtime config JSON\n  exposes/proxies existing write/read endpoints\n  optionally serves public-site preview/build artifacts\n</code></pre></div></div>\n\n<p>Then the current Jekyll route files become source material for view templates. Existing JS modules continue to carry the behavior until there is a specific reason to refactor them.</p>\n\n<p>The standard for introducing any new framework should be high: it should solve a concrete maintenance problem that cannot be solved cleanly with smaller vanilla modules. Right now, the concrete problem is Jekyll coupling, not lack of React/Vue/Svelte.</p>\n\n<hr />\n"
}
